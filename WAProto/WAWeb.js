/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/
import $protobuf from "protobufjs/minimal.js";
import { WAE2E } from './WAE2E.js';
import { WACommon } from './WACommon.js';

const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;

const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

export const WAWeb = $root.WAWeb = (() => {

    const WAWeb = {};

    WAWeb.WebMessageInfo = (function() {

        function WebMessageInfo(p) {
            this.messageStubParameters = [];
            this.labels = [];
            this.userReceipt = [];
            this.reactions = [];
            this.pollUpdates = [];
            this.eventResponses = [];
            this.statusMentions = [];
            this.messageAddOns = [];
            this.statusMentionSources = [];
            this.supportAiCitations = [];
            this.decisionSources = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        WebMessageInfo.prototype.key = null;
        WebMessageInfo.prototype.message = null;
        WebMessageInfo.prototype.messageTimestamp = null;
        WebMessageInfo.prototype.status = null;
        WebMessageInfo.prototype.participant = null;
        WebMessageInfo.prototype.messageC2STimestamp = null;
        WebMessageInfo.prototype.ignore = null;
        WebMessageInfo.prototype.starred = null;
        WebMessageInfo.prototype.broadcast = null;
        WebMessageInfo.prototype.pushName = null;
        WebMessageInfo.prototype.mediaCiphertextSHA256 = null;
        WebMessageInfo.prototype.multicast = null;
        WebMessageInfo.prototype.urlText = null;
        WebMessageInfo.prototype.urlNumber = null;
        WebMessageInfo.prototype.messageStubType = null;
        WebMessageInfo.prototype.clearMedia = null;
        WebMessageInfo.prototype.messageStubParameters = $util.emptyArray;
        WebMessageInfo.prototype.duration = null;
        WebMessageInfo.prototype.labels = $util.emptyArray;
        WebMessageInfo.prototype.paymentInfo = null;
        WebMessageInfo.prototype.finalLiveLocation = null;
        WebMessageInfo.prototype.quotedPaymentInfo = null;
        WebMessageInfo.prototype.ephemeralStartTimestamp = null;
        WebMessageInfo.prototype.ephemeralDuration = null;
        WebMessageInfo.prototype.ephemeralOffToOn = null;
        WebMessageInfo.prototype.ephemeralOutOfSync = null;
        WebMessageInfo.prototype.bizPrivacyStatus = null;
        WebMessageInfo.prototype.verifiedBizName = null;
        WebMessageInfo.prototype.mediaData = null;
        WebMessageInfo.prototype.photoChange = null;
        WebMessageInfo.prototype.userReceipt = $util.emptyArray;
        WebMessageInfo.prototype.reactions = $util.emptyArray;
        WebMessageInfo.prototype.quotedStickerData = null;
        WebMessageInfo.prototype.futureproofData = null;
        WebMessageInfo.prototype.statusPsa = null;
        WebMessageInfo.prototype.pollUpdates = $util.emptyArray;
        WebMessageInfo.prototype.pollAdditionalMetadata = null;
        WebMessageInfo.prototype.agentID = null;
        WebMessageInfo.prototype.statusAlreadyViewed = null;
        WebMessageInfo.prototype.messageSecret = null;
        WebMessageInfo.prototype.keepInChat = null;
        WebMessageInfo.prototype.originalSelfAuthorUserJIDString = null;
        WebMessageInfo.prototype.revokeMessageTimestamp = null;
        WebMessageInfo.prototype.pinInChat = null;
        WebMessageInfo.prototype.premiumMessageInfo = null;
        WebMessageInfo.prototype.is1PBizBotMessage = null;
        WebMessageInfo.prototype.isGroupHistoryMessage = null;
        WebMessageInfo.prototype.botMessageInvokerJID = null;
        WebMessageInfo.prototype.commentMetadata = null;
        WebMessageInfo.prototype.eventResponses = $util.emptyArray;
        WebMessageInfo.prototype.reportingTokenInfo = null;
        WebMessageInfo.prototype.newsletterServerID = null;
        WebMessageInfo.prototype.eventAdditionalMetadata = null;
        WebMessageInfo.prototype.isMentionedInStatus = null;
        WebMessageInfo.prototype.statusMentions = $util.emptyArray;
        WebMessageInfo.prototype.targetMessageID = null;
        WebMessageInfo.prototype.messageAddOns = $util.emptyArray;
        WebMessageInfo.prototype.statusMentionMessageInfo = null;
        WebMessageInfo.prototype.isSupportAiMessage = null;
        WebMessageInfo.prototype.statusMentionSources = $util.emptyArray;
        WebMessageInfo.prototype.supportAiCitations = $util.emptyArray;
        WebMessageInfo.prototype.botTargetID = null;
        WebMessageInfo.prototype.groupHistoryIndividualMessageInfo = null;
        WebMessageInfo.prototype.groupHistoryBundleInfo = null;
        WebMessageInfo.prototype.interactiveMessageAdditionalMetadata = null;
        WebMessageInfo.prototype.quarantinedMessage = null;
        WebMessageInfo.prototype.nonJIDMentions = null;
        WebMessageInfo.prototype.hsmTag = null;
        WebMessageInfo.prototype.ephemeralExpirationTimestamp = null;
        WebMessageInfo.prototype.scheduledMessageMetadata = null;
        WebMessageInfo.prototype.decisionID = null;
        WebMessageInfo.prototype.decisionSources = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageC2STimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageC2STimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ignore", {
            get: $util.oneOfGetter($oneOfFields = ["ignore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_starred", {
            get: $util.oneOfGetter($oneOfFields = ["starred"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_broadcast", {
            get: $util.oneOfGetter($oneOfFields = ["broadcast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pushName", {
            get: $util.oneOfGetter($oneOfFields = ["pushName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_mediaCiphertextSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["mediaCiphertextSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_multicast", {
            get: $util.oneOfGetter($oneOfFields = ["multicast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_urlText", {
            get: $util.oneOfGetter($oneOfFields = ["urlText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_urlNumber", {
            get: $util.oneOfGetter($oneOfFields = ["urlNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageStubType", {
            get: $util.oneOfGetter($oneOfFields = ["messageStubType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_clearMedia", {
            get: $util.oneOfGetter($oneOfFields = ["clearMedia"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_paymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_finalLiveLocation", {
            get: $util.oneOfGetter($oneOfFields = ["finalLiveLocation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quotedPaymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["quotedPaymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralStartTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralStartTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOffToOn", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOffToOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOutOfSync", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOutOfSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_bizPrivacyStatus", {
            get: $util.oneOfGetter($oneOfFields = ["bizPrivacyStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_verifiedBizName", {
            get: $util.oneOfGetter($oneOfFields = ["verifiedBizName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_photoChange", {
            get: $util.oneOfGetter($oneOfFields = ["photoChange"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quotedStickerData", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStickerData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_futureproofData", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusPsa", {
            get: $util.oneOfGetter($oneOfFields = ["statusPsa"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pollAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pollAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_agentID", {
            get: $util.oneOfGetter($oneOfFields = ["agentID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusAlreadyViewed", {
            get: $util.oneOfGetter($oneOfFields = ["statusAlreadyViewed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_keepInChat", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_originalSelfAuthorUserJIDString", {
            get: $util.oneOfGetter($oneOfFields = ["originalSelfAuthorUserJIDString"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_revokeMessageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["revokeMessageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_pinInChat", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_premiumMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["premiumMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_is1PBizBotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["is1PBizBotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isGroupHistoryMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupHistoryMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_botMessageInvokerJID", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageInvokerJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_commentMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commentMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_reportingTokenInfo", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_newsletterServerID", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterServerID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_eventAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["eventAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isMentionedInStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isMentionedInStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_targetMessageID", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_statusMentionMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_isSupportAiMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isSupportAiMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_botTargetID", {
            get: $util.oneOfGetter($oneOfFields = ["botTargetID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryIndividualMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryIndividualMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryBundleInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryBundleInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_interactiveMessageAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessageAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_quarantinedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quarantinedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_nonJIDMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJIDMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_hsmTag", {
            get: $util.oneOfGetter($oneOfFields = ["hsmTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_ephemeralExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_scheduledMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebMessageInfo.prototype, "_decisionID", {
            get: $util.oneOfGetter($oneOfFields = ["decisionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebMessageInfo.create = function create(properties) {
            return new WebMessageInfo(properties);
        };

        WebMessageInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(18).fork()).ldelim();
            if (m.messageTimestamp != null && Object.hasOwnProperty.call(m, "messageTimestamp"))
                w.uint32(24).uint64(m.messageTimestamp);
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(32).int32(m.status);
            if (m.participant != null && Object.hasOwnProperty.call(m, "participant"))
                w.uint32(42).string(m.participant);
            if (m.messageC2STimestamp != null && Object.hasOwnProperty.call(m, "messageC2STimestamp"))
                w.uint32(48).uint64(m.messageC2STimestamp);
            if (m.ignore != null && Object.hasOwnProperty.call(m, "ignore"))
                w.uint32(128).bool(m.ignore);
            if (m.starred != null && Object.hasOwnProperty.call(m, "starred"))
                w.uint32(136).bool(m.starred);
            if (m.broadcast != null && Object.hasOwnProperty.call(m, "broadcast"))
                w.uint32(144).bool(m.broadcast);
            if (m.pushName != null && Object.hasOwnProperty.call(m, "pushName"))
                w.uint32(154).string(m.pushName);
            if (m.mediaCiphertextSHA256 != null && Object.hasOwnProperty.call(m, "mediaCiphertextSHA256"))
                w.uint32(162).bytes(m.mediaCiphertextSHA256);
            if (m.multicast != null && Object.hasOwnProperty.call(m, "multicast"))
                w.uint32(168).bool(m.multicast);
            if (m.urlText != null && Object.hasOwnProperty.call(m, "urlText"))
                w.uint32(176).bool(m.urlText);
            if (m.urlNumber != null && Object.hasOwnProperty.call(m, "urlNumber"))
                w.uint32(184).bool(m.urlNumber);
            if (m.messageStubType != null && Object.hasOwnProperty.call(m, "messageStubType"))
                w.uint32(192).int32(m.messageStubType);
            if (m.clearMedia != null && Object.hasOwnProperty.call(m, "clearMedia"))
                w.uint32(200).bool(m.clearMedia);
            if (m.messageStubParameters != null && m.messageStubParameters.length) {
                for (var i = 0; i < m.messageStubParameters.length; ++i)
                    w.uint32(210).string(m.messageStubParameters[i]);
            }
            if (m.duration != null && Object.hasOwnProperty.call(m, "duration"))
                w.uint32(216).uint32(m.duration);
            if (m.labels != null && m.labels.length) {
                for (var i = 0; i < m.labels.length; ++i)
                    w.uint32(226).string(m.labels[i]);
            }
            if (m.paymentInfo != null && Object.hasOwnProperty.call(m, "paymentInfo"))
                $root.WAWeb.PaymentInfo.encode(m.paymentInfo, w.uint32(234).fork()).ldelim();
            if (m.finalLiveLocation != null && Object.hasOwnProperty.call(m, "finalLiveLocation"))
                WAE2E.LiveLocationMessage.encode(m.finalLiveLocation, w.uint32(242).fork()).ldelim();
            if (m.quotedPaymentInfo != null && Object.hasOwnProperty.call(m, "quotedPaymentInfo"))
                $root.WAWeb.PaymentInfo.encode(m.quotedPaymentInfo, w.uint32(250).fork()).ldelim();
            if (m.ephemeralStartTimestamp != null && Object.hasOwnProperty.call(m, "ephemeralStartTimestamp"))
                w.uint32(256).uint64(m.ephemeralStartTimestamp);
            if (m.ephemeralDuration != null && Object.hasOwnProperty.call(m, "ephemeralDuration"))
                w.uint32(264).uint32(m.ephemeralDuration);
            if (m.ephemeralOffToOn != null && Object.hasOwnProperty.call(m, "ephemeralOffToOn"))
                w.uint32(272).bool(m.ephemeralOffToOn);
            if (m.ephemeralOutOfSync != null && Object.hasOwnProperty.call(m, "ephemeralOutOfSync"))
                w.uint32(280).bool(m.ephemeralOutOfSync);
            if (m.bizPrivacyStatus != null && Object.hasOwnProperty.call(m, "bizPrivacyStatus"))
                w.uint32(288).int32(m.bizPrivacyStatus);
            if (m.verifiedBizName != null && Object.hasOwnProperty.call(m, "verifiedBizName"))
                w.uint32(298).string(m.verifiedBizName);
            if (m.mediaData != null && Object.hasOwnProperty.call(m, "mediaData"))
                $root.WAWeb.MediaData.encode(m.mediaData, w.uint32(306).fork()).ldelim();
            if (m.photoChange != null && Object.hasOwnProperty.call(m, "photoChange"))
                $root.WAWeb.PhotoChange.encode(m.photoChange, w.uint32(314).fork()).ldelim();
            if (m.userReceipt != null && m.userReceipt.length) {
                for (var i = 0; i < m.userReceipt.length; ++i)
                    $root.WAWeb.UserReceipt.encode(m.userReceipt[i], w.uint32(322).fork()).ldelim();
            }
            if (m.reactions != null && m.reactions.length) {
                for (var i = 0; i < m.reactions.length; ++i)
                    $root.WAWeb.Reaction.encode(m.reactions[i], w.uint32(330).fork()).ldelim();
            }
            if (m.quotedStickerData != null && Object.hasOwnProperty.call(m, "quotedStickerData"))
                $root.WAWeb.MediaData.encode(m.quotedStickerData, w.uint32(338).fork()).ldelim();
            if (m.futureproofData != null && Object.hasOwnProperty.call(m, "futureproofData"))
                w.uint32(346).bytes(m.futureproofData);
            if (m.statusPsa != null && Object.hasOwnProperty.call(m, "statusPsa"))
                $root.WAWeb.StatusPSA.encode(m.statusPsa, w.uint32(354).fork()).ldelim();
            if (m.pollUpdates != null && m.pollUpdates.length) {
                for (var i = 0; i < m.pollUpdates.length; ++i)
                    $root.WAWeb.PollUpdate.encode(m.pollUpdates[i], w.uint32(362).fork()).ldelim();
            }
            if (m.pollAdditionalMetadata != null && Object.hasOwnProperty.call(m, "pollAdditionalMetadata"))
                $root.WAWeb.PollAdditionalMetadata.encode(m.pollAdditionalMetadata, w.uint32(370).fork()).ldelim();
            if (m.agentID != null && Object.hasOwnProperty.call(m, "agentID"))
                w.uint32(378).string(m.agentID);
            if (m.statusAlreadyViewed != null && Object.hasOwnProperty.call(m, "statusAlreadyViewed"))
                w.uint32(384).bool(m.statusAlreadyViewed);
            if (m.messageSecret != null && Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(394).bytes(m.messageSecret);
            if (m.keepInChat != null && Object.hasOwnProperty.call(m, "keepInChat"))
                $root.WAWeb.KeepInChat.encode(m.keepInChat, w.uint32(402).fork()).ldelim();
            if (m.originalSelfAuthorUserJIDString != null && Object.hasOwnProperty.call(m, "originalSelfAuthorUserJIDString"))
                w.uint32(410).string(m.originalSelfAuthorUserJIDString);
            if (m.revokeMessageTimestamp != null && Object.hasOwnProperty.call(m, "revokeMessageTimestamp"))
                w.uint32(416).uint64(m.revokeMessageTimestamp);
            if (m.pinInChat != null && Object.hasOwnProperty.call(m, "pinInChat"))
                $root.WAWeb.PinInChat.encode(m.pinInChat, w.uint32(434).fork()).ldelim();
            if (m.premiumMessageInfo != null && Object.hasOwnProperty.call(m, "premiumMessageInfo"))
                $root.WAWeb.PremiumMessageInfo.encode(m.premiumMessageInfo, w.uint32(442).fork()).ldelim();
            if (m.is1PBizBotMessage != null && Object.hasOwnProperty.call(m, "is1PBizBotMessage"))
                w.uint32(448).bool(m.is1PBizBotMessage);
            if (m.isGroupHistoryMessage != null && Object.hasOwnProperty.call(m, "isGroupHistoryMessage"))
                w.uint32(456).bool(m.isGroupHistoryMessage);
            if (m.botMessageInvokerJID != null && Object.hasOwnProperty.call(m, "botMessageInvokerJID"))
                w.uint32(466).string(m.botMessageInvokerJID);
            if (m.commentMetadata != null && Object.hasOwnProperty.call(m, "commentMetadata"))
                $root.WAWeb.CommentMetadata.encode(m.commentMetadata, w.uint32(474).fork()).ldelim();
            if (m.eventResponses != null && m.eventResponses.length) {
                for (var i = 0; i < m.eventResponses.length; ++i)
                    $root.WAWeb.EventResponse.encode(m.eventResponses[i], w.uint32(490).fork()).ldelim();
            }
            if (m.reportingTokenInfo != null && Object.hasOwnProperty.call(m, "reportingTokenInfo"))
                $root.WAWeb.ReportingTokenInfo.encode(m.reportingTokenInfo, w.uint32(498).fork()).ldelim();
            if (m.newsletterServerID != null && Object.hasOwnProperty.call(m, "newsletterServerID"))
                w.uint32(504).uint64(m.newsletterServerID);
            if (m.eventAdditionalMetadata != null && Object.hasOwnProperty.call(m, "eventAdditionalMetadata"))
                $root.WAWeb.EventAdditionalMetadata.encode(m.eventAdditionalMetadata, w.uint32(514).fork()).ldelim();
            if (m.isMentionedInStatus != null && Object.hasOwnProperty.call(m, "isMentionedInStatus"))
                w.uint32(520).bool(m.isMentionedInStatus);
            if (m.statusMentions != null && m.statusMentions.length) {
                for (var i = 0; i < m.statusMentions.length; ++i)
                    w.uint32(530).string(m.statusMentions[i]);
            }
            if (m.targetMessageID != null && Object.hasOwnProperty.call(m, "targetMessageID"))
                WACommon.MessageKey.encode(m.targetMessageID, w.uint32(538).fork()).ldelim();
            if (m.messageAddOns != null && m.messageAddOns.length) {
                for (var i = 0; i < m.messageAddOns.length; ++i)
                    $root.WAWeb.MessageAddOn.encode(m.messageAddOns[i], w.uint32(546).fork()).ldelim();
            }
            if (m.statusMentionMessageInfo != null && Object.hasOwnProperty.call(m, "statusMentionMessageInfo"))
                $root.WAWeb.StatusMentionMessage.encode(m.statusMentionMessageInfo, w.uint32(554).fork()).ldelim();
            if (m.isSupportAiMessage != null && Object.hasOwnProperty.call(m, "isSupportAiMessage"))
                w.uint32(560).bool(m.isSupportAiMessage);
            if (m.statusMentionSources != null && m.statusMentionSources.length) {
                for (var i = 0; i < m.statusMentionSources.length; ++i)
                    w.uint32(570).string(m.statusMentionSources[i]);
            }
            if (m.supportAiCitations != null && m.supportAiCitations.length) {
                for (var i = 0; i < m.supportAiCitations.length; ++i)
                    $root.WAWeb.Citation.encode(m.supportAiCitations[i], w.uint32(578).fork()).ldelim();
            }
            if (m.botTargetID != null && Object.hasOwnProperty.call(m, "botTargetID"))
                w.uint32(586).string(m.botTargetID);
            if (m.groupHistoryIndividualMessageInfo != null && Object.hasOwnProperty.call(m, "groupHistoryIndividualMessageInfo"))
                $root.WAWeb.GroupHistoryIndividualMessageInfo.encode(m.groupHistoryIndividualMessageInfo, w.uint32(594).fork()).ldelim();
            if (m.groupHistoryBundleInfo != null && Object.hasOwnProperty.call(m, "groupHistoryBundleInfo"))
                $root.WAWeb.GroupHistoryBundleInfo.encode(m.groupHistoryBundleInfo, w.uint32(602).fork()).ldelim();
            if (m.interactiveMessageAdditionalMetadata != null && Object.hasOwnProperty.call(m, "interactiveMessageAdditionalMetadata"))
                $root.WAWeb.InteractiveMessageAdditionalMetadata.encode(m.interactiveMessageAdditionalMetadata, w.uint32(610).fork()).ldelim();
            if (m.quarantinedMessage != null && Object.hasOwnProperty.call(m, "quarantinedMessage"))
                $root.WAWeb.QuarantinedMessage.encode(m.quarantinedMessage, w.uint32(618).fork()).ldelim();
            if (m.nonJIDMentions != null && Object.hasOwnProperty.call(m, "nonJIDMentions"))
                w.uint32(624).uint32(m.nonJIDMentions);
            if (m.hsmTag != null && Object.hasOwnProperty.call(m, "hsmTag"))
                w.uint32(634).string(m.hsmTag);
            if (m.ephemeralExpirationTimestamp != null && Object.hasOwnProperty.call(m, "ephemeralExpirationTimestamp"))
                w.uint32(640).uint64(m.ephemeralExpirationTimestamp);
            if (m.scheduledMessageMetadata != null && Object.hasOwnProperty.call(m, "scheduledMessageMetadata"))
                $root.WAWeb.ScheduledMessageMetadata.encode(m.scheduledMessageMetadata, w.uint32(650).fork()).ldelim();
            if (m.decisionID != null && Object.hasOwnProperty.call(m, "decisionID"))
                w.uint32(658).string(m.decisionID);
            if (m.decisionSources != null && m.decisionSources.length) {
                for (var i = 0; i < m.decisionSources.length; ++i)
                    w.uint32(666).string(m.decisionSources[i]);
            }
            return w;
        };

        WebMessageInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.WebMessageInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.messageTimestamp = r.uint64();
                        break;
                    }
                case 4: {
                        m.status = r.int32();
                        break;
                    }
                case 5: {
                        m.participant = r.string();
                        break;
                    }
                case 6: {
                        m.messageC2STimestamp = r.uint64();
                        break;
                    }
                case 16: {
                        m.ignore = r.bool();
                        break;
                    }
                case 17: {
                        m.starred = r.bool();
                        break;
                    }
                case 18: {
                        m.broadcast = r.bool();
                        break;
                    }
                case 19: {
                        m.pushName = r.string();
                        break;
                    }
                case 20: {
                        m.mediaCiphertextSHA256 = r.bytes();
                        break;
                    }
                case 21: {
                        m.multicast = r.bool();
                        break;
                    }
                case 22: {
                        m.urlText = r.bool();
                        break;
                    }
                case 23: {
                        m.urlNumber = r.bool();
                        break;
                    }
                case 24: {
                        m.messageStubType = r.int32();
                        break;
                    }
                case 25: {
                        m.clearMedia = r.bool();
                        break;
                    }
                case 26: {
                        if (!(m.messageStubParameters && m.messageStubParameters.length))
                            m.messageStubParameters = [];
                        m.messageStubParameters.push(r.string());
                        break;
                    }
                case 27: {
                        m.duration = r.uint32();
                        break;
                    }
                case 28: {
                        if (!(m.labels && m.labels.length))
                            m.labels = [];
                        m.labels.push(r.string());
                        break;
                    }
                case 29: {
                        m.paymentInfo = $root.WAWeb.PaymentInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 30: {
                        m.finalLiveLocation = WAE2E.LiveLocationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 31: {
                        m.quotedPaymentInfo = $root.WAWeb.PaymentInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 32: {
                        m.ephemeralStartTimestamp = r.uint64();
                        break;
                    }
                case 33: {
                        m.ephemeralDuration = r.uint32();
                        break;
                    }
                case 34: {
                        m.ephemeralOffToOn = r.bool();
                        break;
                    }
                case 35: {
                        m.ephemeralOutOfSync = r.bool();
                        break;
                    }
                case 36: {
                        m.bizPrivacyStatus = r.int32();
                        break;
                    }
                case 37: {
                        m.verifiedBizName = r.string();
                        break;
                    }
                case 38: {
                        m.mediaData = $root.WAWeb.MediaData.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 39: {
                        m.photoChange = $root.WAWeb.PhotoChange.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 40: {
                        if (!(m.userReceipt && m.userReceipt.length))
                            m.userReceipt = [];
                        m.userReceipt.push($root.WAWeb.UserReceipt.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 41: {
                        if (!(m.reactions && m.reactions.length))
                            m.reactions = [];
                        m.reactions.push($root.WAWeb.Reaction.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 42: {
                        m.quotedStickerData = $root.WAWeb.MediaData.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 43: {
                        m.futureproofData = r.bytes();
                        break;
                    }
                case 44: {
                        m.statusPsa = $root.WAWeb.StatusPSA.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 45: {
                        if (!(m.pollUpdates && m.pollUpdates.length))
                            m.pollUpdates = [];
                        m.pollUpdates.push($root.WAWeb.PollUpdate.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 46: {
                        m.pollAdditionalMetadata = $root.WAWeb.PollAdditionalMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 47: {
                        m.agentID = r.string();
                        break;
                    }
                case 48: {
                        m.statusAlreadyViewed = r.bool();
                        break;
                    }
                case 49: {
                        m.messageSecret = r.bytes();
                        break;
                    }
                case 50: {
                        m.keepInChat = $root.WAWeb.KeepInChat.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 51: {
                        m.originalSelfAuthorUserJIDString = r.string();
                        break;
                    }
                case 52: {
                        m.revokeMessageTimestamp = r.uint64();
                        break;
                    }
                case 54: {
                        m.pinInChat = $root.WAWeb.PinInChat.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 55: {
                        m.premiumMessageInfo = $root.WAWeb.PremiumMessageInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 56: {
                        m.is1PBizBotMessage = r.bool();
                        break;
                    }
                case 57: {
                        m.isGroupHistoryMessage = r.bool();
                        break;
                    }
                case 58: {
                        m.botMessageInvokerJID = r.string();
                        break;
                    }
                case 59: {
                        m.commentMetadata = $root.WAWeb.CommentMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 61: {
                        if (!(m.eventResponses && m.eventResponses.length))
                            m.eventResponses = [];
                        m.eventResponses.push($root.WAWeb.EventResponse.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 62: {
                        m.reportingTokenInfo = $root.WAWeb.ReportingTokenInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 63: {
                        m.newsletterServerID = r.uint64();
                        break;
                    }
                case 64: {
                        m.eventAdditionalMetadata = $root.WAWeb.EventAdditionalMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 65: {
                        m.isMentionedInStatus = r.bool();
                        break;
                    }
                case 66: {
                        if (!(m.statusMentions && m.statusMentions.length))
                            m.statusMentions = [];
                        m.statusMentions.push(r.string());
                        break;
                    }
                case 67: {
                        m.targetMessageID = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 68: {
                        if (!(m.messageAddOns && m.messageAddOns.length))
                            m.messageAddOns = [];
                        m.messageAddOns.push($root.WAWeb.MessageAddOn.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 69: {
                        m.statusMentionMessageInfo = $root.WAWeb.StatusMentionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 70: {
                        m.isSupportAiMessage = r.bool();
                        break;
                    }
                case 71: {
                        if (!(m.statusMentionSources && m.statusMentionSources.length))
                            m.statusMentionSources = [];
                        m.statusMentionSources.push(r.string());
                        break;
                    }
                case 72: {
                        if (!(m.supportAiCitations && m.supportAiCitations.length))
                            m.supportAiCitations = [];
                        m.supportAiCitations.push($root.WAWeb.Citation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 73: {
                        m.botTargetID = r.string();
                        break;
                    }
                case 74: {
                        m.groupHistoryIndividualMessageInfo = $root.WAWeb.GroupHistoryIndividualMessageInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 75: {
                        m.groupHistoryBundleInfo = $root.WAWeb.GroupHistoryBundleInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 76: {
                        m.interactiveMessageAdditionalMetadata = $root.WAWeb.InteractiveMessageAdditionalMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 77: {
                        m.quarantinedMessage = $root.WAWeb.QuarantinedMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 78: {
                        m.nonJIDMentions = r.uint32();
                        break;
                    }
                case 79: {
                        m.hsmTag = r.string();
                        break;
                    }
                case 80: {
                        m.ephemeralExpirationTimestamp = r.uint64();
                        break;
                    }
                case 81: {
                        m.scheduledMessageMetadata = $root.WAWeb.ScheduledMessageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 82: {
                        m.decisionID = r.string();
                        break;
                    }
                case 83: {
                        if (!(m.decisionSources && m.decisionSources.length))
                            m.decisionSources = [];
                        m.decisionSources.push(r.string());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        WebMessageInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.WebMessageInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.WebMessageInfo();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            if (d.messageTimestamp != null) {
                if ($util.Long)
                    (m.messageTimestamp = $util.Long.fromValue(d.messageTimestamp)).unsigned = true;
                else if (typeof d.messageTimestamp === "string")
                    m.messageTimestamp = parseInt(d.messageTimestamp, 10);
                else if (typeof d.messageTimestamp === "number")
                    m.messageTimestamp = d.messageTimestamp;
                else if (typeof d.messageTimestamp === "object")
                    m.messageTimestamp = new $util.LongBits(d.messageTimestamp.low >>> 0, d.messageTimestamp.high >>> 0).toNumber(true);
            }
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "ERROR":
            case 0:
                m.status = 0;
                break;
            case "PENDING":
            case 1:
                m.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                m.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                m.status = 3;
                break;
            case "READ":
            case 4:
                m.status = 4;
                break;
            case "PLAYED":
            case 5:
                m.status = 5;
                break;
            }
            if (d.participant != null) {
                m.participant = String(d.participant);
            }
            if (d.messageC2STimestamp != null) {
                if ($util.Long)
                    (m.messageC2STimestamp = $util.Long.fromValue(d.messageC2STimestamp)).unsigned = true;
                else if (typeof d.messageC2STimestamp === "string")
                    m.messageC2STimestamp = parseInt(d.messageC2STimestamp, 10);
                else if (typeof d.messageC2STimestamp === "number")
                    m.messageC2STimestamp = d.messageC2STimestamp;
                else if (typeof d.messageC2STimestamp === "object")
                    m.messageC2STimestamp = new $util.LongBits(d.messageC2STimestamp.low >>> 0, d.messageC2STimestamp.high >>> 0).toNumber(true);
            }
            if (d.ignore != null) {
                m.ignore = Boolean(d.ignore);
            }
            if (d.starred != null) {
                m.starred = Boolean(d.starred);
            }
            if (d.broadcast != null) {
                m.broadcast = Boolean(d.broadcast);
            }
            if (d.pushName != null) {
                m.pushName = String(d.pushName);
            }
            if (d.mediaCiphertextSHA256 != null) {
                if (typeof d.mediaCiphertextSHA256 === "string")
                    $util.base64.decode(d.mediaCiphertextSHA256, m.mediaCiphertextSHA256 = $util.newBuffer($util.base64.length(d.mediaCiphertextSHA256)), 0);
                else if (d.mediaCiphertextSHA256.length >= 0)
                    m.mediaCiphertextSHA256 = d.mediaCiphertextSHA256;
            }
            if (d.multicast != null) {
                m.multicast = Boolean(d.multicast);
            }
            if (d.urlText != null) {
                m.urlText = Boolean(d.urlText);
            }
            if (d.urlNumber != null) {
                m.urlNumber = Boolean(d.urlNumber);
            }
            switch (d.messageStubType) {
            default:
                if (typeof d.messageStubType === "number") {
                    m.messageStubType = d.messageStubType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.messageStubType = 0;
                break;
            case "REVOKE":
            case 1:
                m.messageStubType = 1;
                break;
            case "CIPHERTEXT":
            case 2:
                m.messageStubType = 2;
                break;
            case "FUTUREPROOF":
            case 3:
                m.messageStubType = 3;
                break;
            case "NON_VERIFIED_TRANSITION":
            case 4:
                m.messageStubType = 4;
                break;
            case "UNVERIFIED_TRANSITION":
            case 5:
                m.messageStubType = 5;
                break;
            case "VERIFIED_TRANSITION":
            case 6:
                m.messageStubType = 6;
                break;
            case "VERIFIED_LOW_UNKNOWN":
            case 7:
                m.messageStubType = 7;
                break;
            case "VERIFIED_HIGH":
            case 8:
                m.messageStubType = 8;
                break;
            case "VERIFIED_INITIAL_UNKNOWN":
            case 9:
                m.messageStubType = 9;
                break;
            case "VERIFIED_INITIAL_LOW":
            case 10:
                m.messageStubType = 10;
                break;
            case "VERIFIED_INITIAL_HIGH":
            case 11:
                m.messageStubType = 11;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_NONE":
            case 12:
                m.messageStubType = 12;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_HIGH":
            case 13:
                m.messageStubType = 13;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_LOW":
            case 14:
                m.messageStubType = 14;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN":
            case 15:
                m.messageStubType = 15;
                break;
            case "VERIFIED_TRANSITION_UNKNOWN_TO_LOW":
            case 16:
                m.messageStubType = 16;
                break;
            case "VERIFIED_TRANSITION_LOW_TO_UNKNOWN":
            case 17:
                m.messageStubType = 17;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_LOW":
            case 18:
                m.messageStubType = 18;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_UNKNOWN":
            case 19:
                m.messageStubType = 19;
                break;
            case "GROUP_CREATE":
            case 20:
                m.messageStubType = 20;
                break;
            case "GROUP_CHANGE_SUBJECT":
            case 21:
                m.messageStubType = 21;
                break;
            case "GROUP_CHANGE_ICON":
            case 22:
                m.messageStubType = 22;
                break;
            case "GROUP_CHANGE_INVITE_LINK":
            case 23:
                m.messageStubType = 23;
                break;
            case "GROUP_CHANGE_DESCRIPTION":
            case 24:
                m.messageStubType = 24;
                break;
            case "GROUP_CHANGE_RESTRICT":
            case 25:
                m.messageStubType = 25;
                break;
            case "GROUP_CHANGE_ANNOUNCE":
            case 26:
                m.messageStubType = 26;
                break;
            case "GROUP_PARTICIPANT_ADD":
            case 27:
                m.messageStubType = 27;
                break;
            case "GROUP_PARTICIPANT_REMOVE":
            case 28:
                m.messageStubType = 28;
                break;
            case "GROUP_PARTICIPANT_PROMOTE":
            case 29:
                m.messageStubType = 29;
                break;
            case "GROUP_PARTICIPANT_DEMOTE":
            case 30:
                m.messageStubType = 30;
                break;
            case "GROUP_PARTICIPANT_INVITE":
            case 31:
                m.messageStubType = 31;
                break;
            case "GROUP_PARTICIPANT_LEAVE":
            case 32:
                m.messageStubType = 32;
                break;
            case "GROUP_PARTICIPANT_CHANGE_NUMBER":
            case 33:
                m.messageStubType = 33;
                break;
            case "BROADCAST_CREATE":
            case 34:
                m.messageStubType = 34;
                break;
            case "BROADCAST_ADD":
            case 35:
                m.messageStubType = 35;
                break;
            case "BROADCAST_REMOVE":
            case 36:
                m.messageStubType = 36;
                break;
            case "GENERIC_NOTIFICATION":
            case 37:
                m.messageStubType = 37;
                break;
            case "E2E_IDENTITY_CHANGED":
            case 38:
                m.messageStubType = 38;
                break;
            case "E2E_ENCRYPTED":
            case 39:
                m.messageStubType = 39;
                break;
            case "CALL_MISSED_VOICE":
            case 40:
                m.messageStubType = 40;
                break;
            case "CALL_MISSED_VIDEO":
            case 41:
                m.messageStubType = 41;
                break;
            case "INDIVIDUAL_CHANGE_NUMBER":
            case 42:
                m.messageStubType = 42;
                break;
            case "GROUP_DELETE":
            case 43:
                m.messageStubType = 43;
                break;
            case "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE":
            case 44:
                m.messageStubType = 44;
                break;
            case "CALL_MISSED_GROUP_VOICE":
            case 45:
                m.messageStubType = 45;
                break;
            case "CALL_MISSED_GROUP_VIDEO":
            case 46:
                m.messageStubType = 46;
                break;
            case "PAYMENT_CIPHERTEXT":
            case 47:
                m.messageStubType = 47;
                break;
            case "PAYMENT_FUTUREPROOF":
            case 48:
                m.messageStubType = 48;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED":
            case 49:
                m.messageStubType = 49;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED":
            case 50:
                m.messageStubType = 50;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED":
            case 51:
                m.messageStubType = 51;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP":
            case 52:
                m.messageStubType = 52;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP":
            case 53:
                m.messageStubType = 53;
                break;
            case "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER":
            case 54:
                m.messageStubType = 54;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_REMINDER":
            case 55:
                m.messageStubType = 55;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_INVITATION":
            case 56:
                m.messageStubType = 56;
                break;
            case "PAYMENT_ACTION_REQUEST_DECLINED":
            case 57:
                m.messageStubType = 57;
                break;
            case "PAYMENT_ACTION_REQUEST_EXPIRED":
            case 58:
                m.messageStubType = 58;
                break;
            case "PAYMENT_ACTION_REQUEST_CANCELLED":
            case 59:
                m.messageStubType = 59;
                break;
            case "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM":
            case 60:
                m.messageStubType = 60;
                break;
            case "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP":
            case 61:
                m.messageStubType = 61;
                break;
            case "BIZ_INTRO_TOP":
            case 62:
                m.messageStubType = 62;
                break;
            case "BIZ_INTRO_BOTTOM":
            case 63:
                m.messageStubType = 63;
                break;
            case "BIZ_NAME_CHANGE":
            case 64:
                m.messageStubType = 64;
                break;
            case "BIZ_MOVE_TO_CONSUMER_APP":
            case 65:
                m.messageStubType = 65;
                break;
            case "BIZ_TWO_TIER_MIGRATION_TOP":
            case 66:
                m.messageStubType = 66;
                break;
            case "BIZ_TWO_TIER_MIGRATION_BOTTOM":
            case 67:
                m.messageStubType = 67;
                break;
            case "OVERSIZED":
            case 68:
                m.messageStubType = 68;
                break;
            case "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED":
            case 69:
                m.messageStubType = 69;
                break;
            case "GROUP_V4_ADD_INVITE_SENT":
            case 70:
                m.messageStubType = 70;
                break;
            case "GROUP_PARTICIPANT_ADD_REQUEST_JOIN":
            case 71:
                m.messageStubType = 71;
                break;
            case "CHANGE_EPHEMERAL_SETTING":
            case 72:
                m.messageStubType = 72;
                break;
            case "E2E_DEVICE_CHANGED":
            case 73:
                m.messageStubType = 73;
                break;
            case "VIEWED_ONCE":
            case 74:
                m.messageStubType = 74;
                break;
            case "E2E_ENCRYPTED_NOW":
            case 75:
                m.messageStubType = 75;
                break;
            case "BLUE_MSG_BSP_FB_TO_BSP_PREMISE":
            case 76:
                m.messageStubType = 76;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_FB":
            case 77:
                m.messageStubType = 77;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_PREMISE":
            case 78:
                m.messageStubType = 78;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED":
            case 79:
                m.messageStubType = 79;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 80:
                m.messageStubType = 80;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED":
            case 81:
                m.messageStubType = 81;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 82:
                m.messageStubType = 82;
                break;
            case "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE":
            case 83:
                m.messageStubType = 83;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED":
            case 84:
                m.messageStubType = 84;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 85:
                m.messageStubType = 85;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED":
            case 86:
                m.messageStubType = 86;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 87:
                m.messageStubType = 87;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED":
            case 88:
                m.messageStubType = 88;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED":
            case 89:
                m.messageStubType = 89;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED":
            case 90:
                m.messageStubType = 90;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED":
            case 91:
                m.messageStubType = 91;
                break;
            case "BLUE_MSG_SELF_FB_TO_BSP_PREMISE":
            case 92:
                m.messageStubType = 92;
                break;
            case "BLUE_MSG_SELF_FB_TO_SELF_PREMISE":
            case 93:
                m.messageStubType = 93;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED":
            case 94:
                m.messageStubType = 94;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 95:
                m.messageStubType = 95;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED":
            case 96:
                m.messageStubType = 96;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 97:
                m.messageStubType = 97;
                break;
            case "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE":
            case 98:
                m.messageStubType = 98;
                break;
            case "BLUE_MSG_SELF_PREMISE_UNVERIFIED":
            case 99:
                m.messageStubType = 99;
                break;
            case "BLUE_MSG_SELF_PREMISE_VERIFIED":
            case 100:
                m.messageStubType = 100;
                break;
            case "BLUE_MSG_TO_BSP_FB":
            case 101:
                m.messageStubType = 101;
                break;
            case "BLUE_MSG_TO_CONSUMER":
            case 102:
                m.messageStubType = 102;
                break;
            case "BLUE_MSG_TO_SELF_FB":
            case 103:
                m.messageStubType = 103;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED":
            case 104:
                m.messageStubType = 104;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 105:
                m.messageStubType = 105;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 106:
                m.messageStubType = 106;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_VERIFIED":
            case 107:
                m.messageStubType = 107;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED":
            case 108:
                m.messageStubType = 108;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 109:
                m.messageStubType = 109;
                break;
            case "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 110:
                m.messageStubType = 110;
                break;
            case "BLUE_MSG_VERIFIED_TO_UNVERIFIED":
            case 111:
                m.messageStubType = 111;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 112:
                m.messageStubType = 112;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 113:
                m.messageStubType = 113;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 114:
                m.messageStubType = 114;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 115:
                m.messageStubType = 115;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 116:
                m.messageStubType = 116;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 117:
                m.messageStubType = 117;
                break;
            case "E2E_IDENTITY_UNAVAILABLE":
            case 118:
                m.messageStubType = 118;
                break;
            case "GROUP_CREATING":
            case 119:
                m.messageStubType = 119;
                break;
            case "GROUP_CREATE_FAILED":
            case 120:
                m.messageStubType = 120;
                break;
            case "GROUP_BOUNCED":
            case 121:
                m.messageStubType = 121;
                break;
            case "BLOCK_CONTACT":
            case 122:
                m.messageStubType = 122;
                break;
            case "EPHEMERAL_SETTING_NOT_APPLIED":
            case 123:
                m.messageStubType = 123;
                break;
            case "SYNC_FAILED":
            case 124:
                m.messageStubType = 124;
                break;
            case "SYNCING":
            case 125:
                m.messageStubType = 125;
                break;
            case "BIZ_PRIVACY_MODE_INIT_FB":
            case 126:
                m.messageStubType = 126;
                break;
            case "BIZ_PRIVACY_MODE_INIT_BSP":
            case 127:
                m.messageStubType = 127;
                break;
            case "BIZ_PRIVACY_MODE_TO_FB":
            case 128:
                m.messageStubType = 128;
                break;
            case "BIZ_PRIVACY_MODE_TO_BSP":
            case 129:
                m.messageStubType = 129;
                break;
            case "DISAPPEARING_MODE":
            case 130:
                m.messageStubType = 130;
                break;
            case "E2E_DEVICE_FETCH_FAILED":
            case 131:
                m.messageStubType = 131;
                break;
            case "ADMIN_REVOKE":
            case 132:
                m.messageStubType = 132;
                break;
            case "GROUP_INVITE_LINK_GROWTH_LOCKED":
            case 133:
                m.messageStubType = 133;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP":
            case 134:
                m.messageStubType = 134;
                break;
            case "COMMUNITY_LINK_SIBLING_GROUP":
            case 135:
                m.messageStubType = 135;
                break;
            case "COMMUNITY_LINK_SUB_GROUP":
            case 136:
                m.messageStubType = 136;
                break;
            case "COMMUNITY_UNLINK_PARENT_GROUP":
            case 137:
                m.messageStubType = 137;
                break;
            case "COMMUNITY_UNLINK_SIBLING_GROUP":
            case 138:
                m.messageStubType = 138;
                break;
            case "COMMUNITY_UNLINK_SUB_GROUP":
            case 139:
                m.messageStubType = 139;
                break;
            case "GROUP_PARTICIPANT_ACCEPT":
            case 140:
                m.messageStubType = 140;
                break;
            case "GROUP_PARTICIPANT_LINKED_GROUP_JOIN":
            case 141:
                m.messageStubType = 141;
                break;
            case "COMMUNITY_CREATE":
            case 142:
                m.messageStubType = 142;
                break;
            case "EPHEMERAL_KEEP_IN_CHAT":
            case 143:
                m.messageStubType = 143;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST":
            case 144:
                m.messageStubType = 144;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE":
            case 145:
                m.messageStubType = 145;
                break;
            case "INTEGRITY_UNLINK_PARENT_GROUP":
            case 146:
                m.messageStubType = 146;
                break;
            case "COMMUNITY_PARTICIPANT_PROMOTE":
            case 147:
                m.messageStubType = 147;
                break;
            case "COMMUNITY_PARTICIPANT_DEMOTE":
            case 148:
                m.messageStubType = 148;
                break;
            case "COMMUNITY_PARENT_GROUP_DELETED":
            case 149:
                m.messageStubType = 149;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL":
            case 150:
                m.messageStubType = 150;
                break;
            case "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP":
            case 151:
                m.messageStubType = 151;
                break;
            case "MASKED_THREAD_CREATED":
            case 152:
                m.messageStubType = 152;
                break;
            case "MASKED_THREAD_UNMASKED":
            case 153:
                m.messageStubType = 153;
                break;
            case "BIZ_CHAT_ASSIGNMENT":
            case 154:
                m.messageStubType = 154;
                break;
            case "CHAT_PSA":
            case 155:
                m.messageStubType = 155;
                break;
            case "CHAT_POLL_CREATION_MESSAGE":
            case 156:
                m.messageStubType = 156;
                break;
            case "CAG_MASKED_THREAD_CREATED":
            case 157:
                m.messageStubType = 157;
                break;
            case "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED":
            case 158:
                m.messageStubType = 158;
                break;
            case "CAG_INVITE_AUTO_ADD":
            case 159:
                m.messageStubType = 159;
                break;
            case "BIZ_CHAT_ASSIGNMENT_UNASSIGN":
            case 160:
                m.messageStubType = 160;
                break;
            case "CAG_INVITE_AUTO_JOINED":
            case 161:
                m.messageStubType = 161;
                break;
            case "SCHEDULED_CALL_START_MESSAGE":
            case 162:
                m.messageStubType = 162;
                break;
            case "COMMUNITY_INVITE_RICH":
            case 163:
                m.messageStubType = 163;
                break;
            case "COMMUNITY_INVITE_AUTO_ADD_RICH":
            case 164:
                m.messageStubType = 164;
                break;
            case "SUB_GROUP_INVITE_RICH":
            case 165:
                m.messageStubType = 165;
                break;
            case "SUB_GROUP_PARTICIPANT_ADD_RICH":
            case 166:
                m.messageStubType = 166;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_RICH":
            case 167:
                m.messageStubType = 167;
                break;
            case "COMMUNITY_PARTICIPANT_ADD_RICH":
            case 168:
                m.messageStubType = 168;
                break;
            case "SILENCED_UNKNOWN_CALLER_AUDIO":
            case 169:
                m.messageStubType = 169;
                break;
            case "SILENCED_UNKNOWN_CALLER_VIDEO":
            case 170:
                m.messageStubType = 170;
                break;
            case "GROUP_MEMBER_ADD_MODE":
            case 171:
                m.messageStubType = 171;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD":
            case 172:
                m.messageStubType = 172;
                break;
            case "COMMUNITY_CHANGE_DESCRIPTION":
            case 173:
                m.messageStubType = 173;
                break;
            case "SENDER_INVITE":
            case 174:
                m.messageStubType = 174;
                break;
            case "RECEIVER_INVITE":
            case 175:
                m.messageStubType = 175;
                break;
            case "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS":
            case 176:
                m.messageStubType = 176;
                break;
            case "PINNED_MESSAGE_IN_CHAT":
            case 177:
                m.messageStubType = 177;
                break;
            case "PAYMENT_INVITE_SETUP_INVITER":
            case 178:
                m.messageStubType = 178;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY":
            case 179:
                m.messageStubType = 179;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE":
            case 180:
                m.messageStubType = 180;
                break;
            case "LINKED_GROUP_CALL_START":
            case 181:
                m.messageStubType = 181;
                break;
            case "REPORT_TO_ADMIN_ENABLED_STATUS":
            case 182:
                m.messageStubType = 182;
                break;
            case "EMPTY_SUBGROUP_CREATE":
            case 183:
                m.messageStubType = 183;
                break;
            case "SCHEDULED_CALL_CANCEL":
            case 184:
                m.messageStubType = 184;
                break;
            case "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH":
            case 185:
                m.messageStubType = 185;
                break;
            case "GROUP_CHANGE_RECENT_HISTORY_SHARING":
            case 186:
                m.messageStubType = 186;
                break;
            case "PAID_MESSAGE_SERVER_CAMPAIGN_ID":
            case 187:
                m.messageStubType = 187;
                break;
            case "GENERAL_CHAT_CREATE":
            case 188:
                m.messageStubType = 188;
                break;
            case "GENERAL_CHAT_ADD":
            case 189:
                m.messageStubType = 189;
                break;
            case "GENERAL_CHAT_AUTO_ADD_DISABLED":
            case 190:
                m.messageStubType = 190;
                break;
            case "SUGGESTED_SUBGROUP_ANNOUNCE":
            case 191:
                m.messageStubType = 191;
                break;
            case "BIZ_BOT_1P_MESSAGING_ENABLED":
            case 192:
                m.messageStubType = 192;
                break;
            case "CHANGE_USERNAME":
            case 193:
                m.messageStubType = 193;
                break;
            case "BIZ_COEX_PRIVACY_INIT_SELF":
            case 194:
                m.messageStubType = 194;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION_SELF":
            case 195:
                m.messageStubType = 195;
                break;
            case "SUPPORT_AI_EDUCATION":
            case 196:
                m.messageStubType = 196;
                break;
            case "BIZ_BOT_3P_MESSAGING_ENABLED":
            case 197:
                m.messageStubType = 197;
                break;
            case "REMINDER_SETUP_MESSAGE":
            case 198:
                m.messageStubType = 198;
                break;
            case "REMINDER_SENT_MESSAGE":
            case 199:
                m.messageStubType = 199;
                break;
            case "REMINDER_CANCEL_MESSAGE":
            case 200:
                m.messageStubType = 200;
                break;
            case "BIZ_COEX_PRIVACY_INIT":
            case 201:
                m.messageStubType = 201;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION":
            case 202:
                m.messageStubType = 202;
                break;
            case "GROUP_DEACTIVATED":
            case 203:
                m.messageStubType = 203;
                break;
            case "COMMUNITY_DEACTIVATE_SIBLING_GROUP":
            case 204:
                m.messageStubType = 204;
                break;
            case "EVENT_UPDATED":
            case 205:
                m.messageStubType = 205;
                break;
            case "EVENT_CANCELED":
            case 206:
                m.messageStubType = 206;
                break;
            case "COMMUNITY_OWNER_UPDATED":
            case 207:
                m.messageStubType = 207;
                break;
            case "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN":
            case 208:
                m.messageStubType = 208;
                break;
            case "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE":
            case 209:
                m.messageStubType = 209;
                break;
            case "STATUS_MENTION":
            case 210:
                m.messageStubType = 210;
                break;
            case "USER_CONTROLS_SYSTEM_MESSAGE":
            case 211:
                m.messageStubType = 211;
                break;
            case "SUPPORT_SYSTEM_MESSAGE":
            case 212:
                m.messageStubType = 212;
                break;
            case "CHANGE_LID":
            case 213:
                m.messageStubType = 213;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE":
            case 214:
                m.messageStubType = 214;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE":
            case 215:
                m.messageStubType = 215;
                break;
            case "CHANGE_LIMIT_SHARING":
            case 216:
                m.messageStubType = 216;
                break;
            case "GROUP_MEMBER_LINK_MODE":
            case 217:
                m.messageStubType = 217;
                break;
            case "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE":
            case 218:
                m.messageStubType = 218;
                break;
            case "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE":
            case 219:
                m.messageStubType = 219;
                break;
            case "QUARANTINED_MESSAGE":
            case 220:
                m.messageStubType = 220;
                break;
            case "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE":
            case 221:
                m.messageStubType = 221;
                break;
            case "GROUP_OPEN_BOT_ADDED":
            case 222:
                m.messageStubType = 222;
                break;
            case "GROUP_TEE_BOT_ADDED":
            case 223:
                m.messageStubType = 223;
                break;
            case "CONTACT_INFO":
            case 224:
                m.messageStubType = 224;
                break;
            case "SCHEDULED_MESSAGE_CREATED":
            case 225:
                m.messageStubType = 225;
                break;
            }
            if (d.clearMedia != null) {
                m.clearMedia = Boolean(d.clearMedia);
            }
            if (d.messageStubParameters) {
                if (!Array.isArray(d.messageStubParameters))
                    throw TypeError(".WAWeb.WebMessageInfo.messageStubParameters: array expected");
                m.messageStubParameters = [];
                for (var i = 0; i < d.messageStubParameters.length; ++i) {
                    m.messageStubParameters[i] = String(d.messageStubParameters[i]);
                }
            }
            if (d.duration != null) {
                m.duration = d.duration >>> 0;
            }
            if (d.labels) {
                if (!Array.isArray(d.labels))
                    throw TypeError(".WAWeb.WebMessageInfo.labels: array expected");
                m.labels = [];
                for (var i = 0; i < d.labels.length; ++i) {
                    m.labels[i] = String(d.labels[i]);
                }
            }
            if (d.paymentInfo != null) {
                if (typeof d.paymentInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.paymentInfo: object expected");
                m.paymentInfo = $root.WAWeb.PaymentInfo.fromObject(d.paymentInfo, n + 1);
            }
            if (d.finalLiveLocation != null) {
                if (typeof d.finalLiveLocation !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.finalLiveLocation: object expected");
                m.finalLiveLocation = WAE2E.LiveLocationMessage.fromObject(d.finalLiveLocation, n + 1);
            }
            if (d.quotedPaymentInfo != null) {
                if (typeof d.quotedPaymentInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.quotedPaymentInfo: object expected");
                m.quotedPaymentInfo = $root.WAWeb.PaymentInfo.fromObject(d.quotedPaymentInfo, n + 1);
            }
            if (d.ephemeralStartTimestamp != null) {
                if ($util.Long)
                    (m.ephemeralStartTimestamp = $util.Long.fromValue(d.ephemeralStartTimestamp)).unsigned = true;
                else if (typeof d.ephemeralStartTimestamp === "string")
                    m.ephemeralStartTimestamp = parseInt(d.ephemeralStartTimestamp, 10);
                else if (typeof d.ephemeralStartTimestamp === "number")
                    m.ephemeralStartTimestamp = d.ephemeralStartTimestamp;
                else if (typeof d.ephemeralStartTimestamp === "object")
                    m.ephemeralStartTimestamp = new $util.LongBits(d.ephemeralStartTimestamp.low >>> 0, d.ephemeralStartTimestamp.high >>> 0).toNumber(true);
            }
            if (d.ephemeralDuration != null) {
                m.ephemeralDuration = d.ephemeralDuration >>> 0;
            }
            if (d.ephemeralOffToOn != null) {
                m.ephemeralOffToOn = Boolean(d.ephemeralOffToOn);
            }
            if (d.ephemeralOutOfSync != null) {
                m.ephemeralOutOfSync = Boolean(d.ephemeralOutOfSync);
            }
            switch (d.bizPrivacyStatus) {
            default:
                if (typeof d.bizPrivacyStatus === "number") {
                    m.bizPrivacyStatus = d.bizPrivacyStatus;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.bizPrivacyStatus = 0;
                break;
            case "FB":
            case 2:
                m.bizPrivacyStatus = 2;
                break;
            case "BSP":
            case 1:
                m.bizPrivacyStatus = 1;
                break;
            case "BSP_AND_FB":
            case 3:
                m.bizPrivacyStatus = 3;
                break;
            }
            if (d.verifiedBizName != null) {
                m.verifiedBizName = String(d.verifiedBizName);
            }
            if (d.mediaData != null) {
                if (typeof d.mediaData !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.mediaData: object expected");
                m.mediaData = $root.WAWeb.MediaData.fromObject(d.mediaData, n + 1);
            }
            if (d.photoChange != null) {
                if (typeof d.photoChange !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.photoChange: object expected");
                m.photoChange = $root.WAWeb.PhotoChange.fromObject(d.photoChange, n + 1);
            }
            if (d.userReceipt) {
                if (!Array.isArray(d.userReceipt))
                    throw TypeError(".WAWeb.WebMessageInfo.userReceipt: array expected");
                m.userReceipt = [];
                for (var i = 0; i < d.userReceipt.length; ++i) {
                    if (typeof d.userReceipt[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.userReceipt: object expected");
                    m.userReceipt[i] = $root.WAWeb.UserReceipt.fromObject(d.userReceipt[i], n + 1);
                }
            }
            if (d.reactions) {
                if (!Array.isArray(d.reactions))
                    throw TypeError(".WAWeb.WebMessageInfo.reactions: array expected");
                m.reactions = [];
                for (var i = 0; i < d.reactions.length; ++i) {
                    if (typeof d.reactions[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.reactions: object expected");
                    m.reactions[i] = $root.WAWeb.Reaction.fromObject(d.reactions[i], n + 1);
                }
            }
            if (d.quotedStickerData != null) {
                if (typeof d.quotedStickerData !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.quotedStickerData: object expected");
                m.quotedStickerData = $root.WAWeb.MediaData.fromObject(d.quotedStickerData, n + 1);
            }
            if (d.futureproofData != null) {
                if (typeof d.futureproofData === "string")
                    $util.base64.decode(d.futureproofData, m.futureproofData = $util.newBuffer($util.base64.length(d.futureproofData)), 0);
                else if (d.futureproofData.length >= 0)
                    m.futureproofData = d.futureproofData;
            }
            if (d.statusPsa != null) {
                if (typeof d.statusPsa !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.statusPsa: object expected");
                m.statusPsa = $root.WAWeb.StatusPSA.fromObject(d.statusPsa, n + 1);
            }
            if (d.pollUpdates) {
                if (!Array.isArray(d.pollUpdates))
                    throw TypeError(".WAWeb.WebMessageInfo.pollUpdates: array expected");
                m.pollUpdates = [];
                for (var i = 0; i < d.pollUpdates.length; ++i) {
                    if (typeof d.pollUpdates[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.pollUpdates: object expected");
                    m.pollUpdates[i] = $root.WAWeb.PollUpdate.fromObject(d.pollUpdates[i], n + 1);
                }
            }
            if (d.pollAdditionalMetadata != null) {
                if (typeof d.pollAdditionalMetadata !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.pollAdditionalMetadata: object expected");
                m.pollAdditionalMetadata = $root.WAWeb.PollAdditionalMetadata.fromObject(d.pollAdditionalMetadata, n + 1);
            }
            if (d.agentID != null) {
                m.agentID = String(d.agentID);
            }
            if (d.statusAlreadyViewed != null) {
                m.statusAlreadyViewed = Boolean(d.statusAlreadyViewed);
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            if (d.keepInChat != null) {
                if (typeof d.keepInChat !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.keepInChat: object expected");
                m.keepInChat = $root.WAWeb.KeepInChat.fromObject(d.keepInChat, n + 1);
            }
            if (d.originalSelfAuthorUserJIDString != null) {
                m.originalSelfAuthorUserJIDString = String(d.originalSelfAuthorUserJIDString);
            }
            if (d.revokeMessageTimestamp != null) {
                if ($util.Long)
                    (m.revokeMessageTimestamp = $util.Long.fromValue(d.revokeMessageTimestamp)).unsigned = true;
                else if (typeof d.revokeMessageTimestamp === "string")
                    m.revokeMessageTimestamp = parseInt(d.revokeMessageTimestamp, 10);
                else if (typeof d.revokeMessageTimestamp === "number")
                    m.revokeMessageTimestamp = d.revokeMessageTimestamp;
                else if (typeof d.revokeMessageTimestamp === "object")
                    m.revokeMessageTimestamp = new $util.LongBits(d.revokeMessageTimestamp.low >>> 0, d.revokeMessageTimestamp.high >>> 0).toNumber(true);
            }
            if (d.pinInChat != null) {
                if (typeof d.pinInChat !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.pinInChat: object expected");
                m.pinInChat = $root.WAWeb.PinInChat.fromObject(d.pinInChat, n + 1);
            }
            if (d.premiumMessageInfo != null) {
                if (typeof d.premiumMessageInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.premiumMessageInfo: object expected");
                m.premiumMessageInfo = $root.WAWeb.PremiumMessageInfo.fromObject(d.premiumMessageInfo, n + 1);
            }
            if (d.is1PBizBotMessage != null) {
                m.is1PBizBotMessage = Boolean(d.is1PBizBotMessage);
            }
            if (d.isGroupHistoryMessage != null) {
                m.isGroupHistoryMessage = Boolean(d.isGroupHistoryMessage);
            }
            if (d.botMessageInvokerJID != null) {
                m.botMessageInvokerJID = String(d.botMessageInvokerJID);
            }
            if (d.commentMetadata != null) {
                if (typeof d.commentMetadata !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.commentMetadata: object expected");
                m.commentMetadata = $root.WAWeb.CommentMetadata.fromObject(d.commentMetadata, n + 1);
            }
            if (d.eventResponses) {
                if (!Array.isArray(d.eventResponses))
                    throw TypeError(".WAWeb.WebMessageInfo.eventResponses: array expected");
                m.eventResponses = [];
                for (var i = 0; i < d.eventResponses.length; ++i) {
                    if (typeof d.eventResponses[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.eventResponses: object expected");
                    m.eventResponses[i] = $root.WAWeb.EventResponse.fromObject(d.eventResponses[i], n + 1);
                }
            }
            if (d.reportingTokenInfo != null) {
                if (typeof d.reportingTokenInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.reportingTokenInfo: object expected");
                m.reportingTokenInfo = $root.WAWeb.ReportingTokenInfo.fromObject(d.reportingTokenInfo, n + 1);
            }
            if (d.newsletterServerID != null) {
                if ($util.Long)
                    (m.newsletterServerID = $util.Long.fromValue(d.newsletterServerID)).unsigned = true;
                else if (typeof d.newsletterServerID === "string")
                    m.newsletterServerID = parseInt(d.newsletterServerID, 10);
                else if (typeof d.newsletterServerID === "number")
                    m.newsletterServerID = d.newsletterServerID;
                else if (typeof d.newsletterServerID === "object")
                    m.newsletterServerID = new $util.LongBits(d.newsletterServerID.low >>> 0, d.newsletterServerID.high >>> 0).toNumber(true);
            }
            if (d.eventAdditionalMetadata != null) {
                if (typeof d.eventAdditionalMetadata !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.eventAdditionalMetadata: object expected");
                m.eventAdditionalMetadata = $root.WAWeb.EventAdditionalMetadata.fromObject(d.eventAdditionalMetadata, n + 1);
            }
            if (d.isMentionedInStatus != null) {
                m.isMentionedInStatus = Boolean(d.isMentionedInStatus);
            }
            if (d.statusMentions) {
                if (!Array.isArray(d.statusMentions))
                    throw TypeError(".WAWeb.WebMessageInfo.statusMentions: array expected");
                m.statusMentions = [];
                for (var i = 0; i < d.statusMentions.length; ++i) {
                    m.statusMentions[i] = String(d.statusMentions[i]);
                }
            }
            if (d.targetMessageID != null) {
                if (typeof d.targetMessageID !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.targetMessageID: object expected");
                m.targetMessageID = WACommon.MessageKey.fromObject(d.targetMessageID, n + 1);
            }
            if (d.messageAddOns) {
                if (!Array.isArray(d.messageAddOns))
                    throw TypeError(".WAWeb.WebMessageInfo.messageAddOns: array expected");
                m.messageAddOns = [];
                for (var i = 0; i < d.messageAddOns.length; ++i) {
                    if (typeof d.messageAddOns[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.messageAddOns: object expected");
                    m.messageAddOns[i] = $root.WAWeb.MessageAddOn.fromObject(d.messageAddOns[i], n + 1);
                }
            }
            if (d.statusMentionMessageInfo != null) {
                if (typeof d.statusMentionMessageInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.statusMentionMessageInfo: object expected");
                m.statusMentionMessageInfo = $root.WAWeb.StatusMentionMessage.fromObject(d.statusMentionMessageInfo, n + 1);
            }
            if (d.isSupportAiMessage != null) {
                m.isSupportAiMessage = Boolean(d.isSupportAiMessage);
            }
            if (d.statusMentionSources) {
                if (!Array.isArray(d.statusMentionSources))
                    throw TypeError(".WAWeb.WebMessageInfo.statusMentionSources: array expected");
                m.statusMentionSources = [];
                for (var i = 0; i < d.statusMentionSources.length; ++i) {
                    m.statusMentionSources[i] = String(d.statusMentionSources[i]);
                }
            }
            if (d.supportAiCitations) {
                if (!Array.isArray(d.supportAiCitations))
                    throw TypeError(".WAWeb.WebMessageInfo.supportAiCitations: array expected");
                m.supportAiCitations = [];
                for (var i = 0; i < d.supportAiCitations.length; ++i) {
                    if (typeof d.supportAiCitations[i] !== "object")
                        throw TypeError(".WAWeb.WebMessageInfo.supportAiCitations: object expected");
                    m.supportAiCitations[i] = $root.WAWeb.Citation.fromObject(d.supportAiCitations[i], n + 1);
                }
            }
            if (d.botTargetID != null) {
                m.botTargetID = String(d.botTargetID);
            }
            if (d.groupHistoryIndividualMessageInfo != null) {
                if (typeof d.groupHistoryIndividualMessageInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.groupHistoryIndividualMessageInfo: object expected");
                m.groupHistoryIndividualMessageInfo = $root.WAWeb.GroupHistoryIndividualMessageInfo.fromObject(d.groupHistoryIndividualMessageInfo, n + 1);
            }
            if (d.groupHistoryBundleInfo != null) {
                if (typeof d.groupHistoryBundleInfo !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.groupHistoryBundleInfo: object expected");
                m.groupHistoryBundleInfo = $root.WAWeb.GroupHistoryBundleInfo.fromObject(d.groupHistoryBundleInfo, n + 1);
            }
            if (d.interactiveMessageAdditionalMetadata != null) {
                if (typeof d.interactiveMessageAdditionalMetadata !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.interactiveMessageAdditionalMetadata: object expected");
                m.interactiveMessageAdditionalMetadata = $root.WAWeb.InteractiveMessageAdditionalMetadata.fromObject(d.interactiveMessageAdditionalMetadata, n + 1);
            }
            if (d.quarantinedMessage != null) {
                if (typeof d.quarantinedMessage !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.quarantinedMessage: object expected");
                m.quarantinedMessage = $root.WAWeb.QuarantinedMessage.fromObject(d.quarantinedMessage, n + 1);
            }
            if (d.nonJIDMentions != null) {
                m.nonJIDMentions = d.nonJIDMentions >>> 0;
            }
            if (d.hsmTag != null) {
                m.hsmTag = String(d.hsmTag);
            }
            if (d.ephemeralExpirationTimestamp != null) {
                if ($util.Long)
                    (m.ephemeralExpirationTimestamp = $util.Long.fromValue(d.ephemeralExpirationTimestamp)).unsigned = true;
                else if (typeof d.ephemeralExpirationTimestamp === "string")
                    m.ephemeralExpirationTimestamp = parseInt(d.ephemeralExpirationTimestamp, 10);
                else if (typeof d.ephemeralExpirationTimestamp === "number")
                    m.ephemeralExpirationTimestamp = d.ephemeralExpirationTimestamp;
                else if (typeof d.ephemeralExpirationTimestamp === "object")
                    m.ephemeralExpirationTimestamp = new $util.LongBits(d.ephemeralExpirationTimestamp.low >>> 0, d.ephemeralExpirationTimestamp.high >>> 0).toNumber(true);
            }
            if (d.scheduledMessageMetadata != null) {
                if (typeof d.scheduledMessageMetadata !== "object")
                    throw TypeError(".WAWeb.WebMessageInfo.scheduledMessageMetadata: object expected");
                m.scheduledMessageMetadata = $root.WAWeb.ScheduledMessageMetadata.fromObject(d.scheduledMessageMetadata, n + 1);
            }
            if (d.decisionID != null) {
                m.decisionID = String(d.decisionID);
            }
            if (d.decisionSources) {
                if (!Array.isArray(d.decisionSources))
                    throw TypeError(".WAWeb.WebMessageInfo.decisionSources: array expected");
                m.decisionSources = [];
                for (var i = 0; i < d.decisionSources.length; ++i) {
                    m.decisionSources[i] = String(d.decisionSources[i]);
                }
            }
            return m;
        };

        WebMessageInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.messageStubParameters = [];
                d.labels = [];
                d.userReceipt = [];
                d.reactions = [];
                d.pollUpdates = [];
                d.eventResponses = [];
                d.statusMentions = [];
                d.messageAddOns = [];
                d.statusMentionSources = [];
                d.supportAiCitations = [];
                d.decisionSources = [];
            }
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.messageTimestamp != null && m.hasOwnProperty("messageTimestamp")) {
                if (typeof m.messageTimestamp === "number")
                    d.messageTimestamp = o.longs === String ? String(m.messageTimestamp) : m.messageTimestamp;
                else
                    d.messageTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.messageTimestamp) : o.longs === Number ? new $util.LongBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0).toNumber(true) : m.messageTimestamp;
                if (o.oneofs)
                    d._messageTimestamp = "messageTimestamp";
            }
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? $root.WAWeb.WebMessageInfo.Status[m.status] === undefined ? m.status : $root.WAWeb.WebMessageInfo.Status[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            if (m.participant != null && m.hasOwnProperty("participant")) {
                d.participant = m.participant;
                if (o.oneofs)
                    d._participant = "participant";
            }
            if (m.messageC2STimestamp != null && m.hasOwnProperty("messageC2STimestamp")) {
                if (typeof m.messageC2STimestamp === "number")
                    d.messageC2STimestamp = o.longs === String ? String(m.messageC2STimestamp) : m.messageC2STimestamp;
                else
                    d.messageC2STimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.messageC2STimestamp) : o.longs === Number ? new $util.LongBits(m.messageC2STimestamp.low >>> 0, m.messageC2STimestamp.high >>> 0).toNumber(true) : m.messageC2STimestamp;
                if (o.oneofs)
                    d._messageC2STimestamp = "messageC2STimestamp";
            }
            if (m.ignore != null && m.hasOwnProperty("ignore")) {
                d.ignore = m.ignore;
                if (o.oneofs)
                    d._ignore = "ignore";
            }
            if (m.starred != null && m.hasOwnProperty("starred")) {
                d.starred = m.starred;
                if (o.oneofs)
                    d._starred = "starred";
            }
            if (m.broadcast != null && m.hasOwnProperty("broadcast")) {
                d.broadcast = m.broadcast;
                if (o.oneofs)
                    d._broadcast = "broadcast";
            }
            if (m.pushName != null && m.hasOwnProperty("pushName")) {
                d.pushName = m.pushName;
                if (o.oneofs)
                    d._pushName = "pushName";
            }
            if (m.mediaCiphertextSHA256 != null && m.hasOwnProperty("mediaCiphertextSHA256")) {
                d.mediaCiphertextSHA256 = o.bytes === String ? $util.base64.encode(m.mediaCiphertextSHA256, 0, m.mediaCiphertextSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaCiphertextSHA256) : m.mediaCiphertextSHA256;
                if (o.oneofs)
                    d._mediaCiphertextSHA256 = "mediaCiphertextSHA256";
            }
            if (m.multicast != null && m.hasOwnProperty("multicast")) {
                d.multicast = m.multicast;
                if (o.oneofs)
                    d._multicast = "multicast";
            }
            if (m.urlText != null && m.hasOwnProperty("urlText")) {
                d.urlText = m.urlText;
                if (o.oneofs)
                    d._urlText = "urlText";
            }
            if (m.urlNumber != null && m.hasOwnProperty("urlNumber")) {
                d.urlNumber = m.urlNumber;
                if (o.oneofs)
                    d._urlNumber = "urlNumber";
            }
            if (m.messageStubType != null && m.hasOwnProperty("messageStubType")) {
                d.messageStubType = o.enums === String ? $root.WAWeb.WebMessageInfo.StubType[m.messageStubType] === undefined ? m.messageStubType : $root.WAWeb.WebMessageInfo.StubType[m.messageStubType] : m.messageStubType;
                if (o.oneofs)
                    d._messageStubType = "messageStubType";
            }
            if (m.clearMedia != null && m.hasOwnProperty("clearMedia")) {
                d.clearMedia = m.clearMedia;
                if (o.oneofs)
                    d._clearMedia = "clearMedia";
            }
            if (m.messageStubParameters && m.messageStubParameters.length) {
                d.messageStubParameters = [];
                for (var j = 0; j < m.messageStubParameters.length; ++j) {
                    d.messageStubParameters[j] = m.messageStubParameters[j];
                }
            }
            if (m.duration != null && m.hasOwnProperty("duration")) {
                d.duration = m.duration;
                if (o.oneofs)
                    d._duration = "duration";
            }
            if (m.labels && m.labels.length) {
                d.labels = [];
                for (var j = 0; j < m.labels.length; ++j) {
                    d.labels[j] = m.labels[j];
                }
            }
            if (m.paymentInfo != null && m.hasOwnProperty("paymentInfo")) {
                d.paymentInfo = $root.WAWeb.PaymentInfo.toObject(m.paymentInfo, o);
                if (o.oneofs)
                    d._paymentInfo = "paymentInfo";
            }
            if (m.finalLiveLocation != null && m.hasOwnProperty("finalLiveLocation")) {
                d.finalLiveLocation = WAE2E.LiveLocationMessage.toObject(m.finalLiveLocation, o);
                if (o.oneofs)
                    d._finalLiveLocation = "finalLiveLocation";
            }
            if (m.quotedPaymentInfo != null && m.hasOwnProperty("quotedPaymentInfo")) {
                d.quotedPaymentInfo = $root.WAWeb.PaymentInfo.toObject(m.quotedPaymentInfo, o);
                if (o.oneofs)
                    d._quotedPaymentInfo = "quotedPaymentInfo";
            }
            if (m.ephemeralStartTimestamp != null && m.hasOwnProperty("ephemeralStartTimestamp")) {
                if (typeof m.ephemeralStartTimestamp === "number")
                    d.ephemeralStartTimestamp = o.longs === String ? String(m.ephemeralStartTimestamp) : m.ephemeralStartTimestamp;
                else
                    d.ephemeralStartTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.ephemeralStartTimestamp) : o.longs === Number ? new $util.LongBits(m.ephemeralStartTimestamp.low >>> 0, m.ephemeralStartTimestamp.high >>> 0).toNumber(true) : m.ephemeralStartTimestamp;
                if (o.oneofs)
                    d._ephemeralStartTimestamp = "ephemeralStartTimestamp";
            }
            if (m.ephemeralDuration != null && m.hasOwnProperty("ephemeralDuration")) {
                d.ephemeralDuration = m.ephemeralDuration;
                if (o.oneofs)
                    d._ephemeralDuration = "ephemeralDuration";
            }
            if (m.ephemeralOffToOn != null && m.hasOwnProperty("ephemeralOffToOn")) {
                d.ephemeralOffToOn = m.ephemeralOffToOn;
                if (o.oneofs)
                    d._ephemeralOffToOn = "ephemeralOffToOn";
            }
            if (m.ephemeralOutOfSync != null && m.hasOwnProperty("ephemeralOutOfSync")) {
                d.ephemeralOutOfSync = m.ephemeralOutOfSync;
                if (o.oneofs)
                    d._ephemeralOutOfSync = "ephemeralOutOfSync";
            }
            if (m.bizPrivacyStatus != null && m.hasOwnProperty("bizPrivacyStatus")) {
                d.bizPrivacyStatus = o.enums === String ? $root.WAWeb.WebMessageInfo.BizPrivacyStatus[m.bizPrivacyStatus] === undefined ? m.bizPrivacyStatus : $root.WAWeb.WebMessageInfo.BizPrivacyStatus[m.bizPrivacyStatus] : m.bizPrivacyStatus;
                if (o.oneofs)
                    d._bizPrivacyStatus = "bizPrivacyStatus";
            }
            if (m.verifiedBizName != null && m.hasOwnProperty("verifiedBizName")) {
                d.verifiedBizName = m.verifiedBizName;
                if (o.oneofs)
                    d._verifiedBizName = "verifiedBizName";
            }
            if (m.mediaData != null && m.hasOwnProperty("mediaData")) {
                d.mediaData = $root.WAWeb.MediaData.toObject(m.mediaData, o);
                if (o.oneofs)
                    d._mediaData = "mediaData";
            }
            if (m.photoChange != null && m.hasOwnProperty("photoChange")) {
                d.photoChange = $root.WAWeb.PhotoChange.toObject(m.photoChange, o);
                if (o.oneofs)
                    d._photoChange = "photoChange";
            }
            if (m.userReceipt && m.userReceipt.length) {
                d.userReceipt = [];
                for (var j = 0; j < m.userReceipt.length; ++j) {
                    d.userReceipt[j] = $root.WAWeb.UserReceipt.toObject(m.userReceipt[j], o);
                }
            }
            if (m.reactions && m.reactions.length) {
                d.reactions = [];
                for (var j = 0; j < m.reactions.length; ++j) {
                    d.reactions[j] = $root.WAWeb.Reaction.toObject(m.reactions[j], o);
                }
            }
            if (m.quotedStickerData != null && m.hasOwnProperty("quotedStickerData")) {
                d.quotedStickerData = $root.WAWeb.MediaData.toObject(m.quotedStickerData, o);
                if (o.oneofs)
                    d._quotedStickerData = "quotedStickerData";
            }
            if (m.futureproofData != null && m.hasOwnProperty("futureproofData")) {
                d.futureproofData = o.bytes === String ? $util.base64.encode(m.futureproofData, 0, m.futureproofData.length) : o.bytes === Array ? Array.prototype.slice.call(m.futureproofData) : m.futureproofData;
                if (o.oneofs)
                    d._futureproofData = "futureproofData";
            }
            if (m.statusPsa != null && m.hasOwnProperty("statusPsa")) {
                d.statusPsa = $root.WAWeb.StatusPSA.toObject(m.statusPsa, o);
                if (o.oneofs)
                    d._statusPsa = "statusPsa";
            }
            if (m.pollUpdates && m.pollUpdates.length) {
                d.pollUpdates = [];
                for (var j = 0; j < m.pollUpdates.length; ++j) {
                    d.pollUpdates[j] = $root.WAWeb.PollUpdate.toObject(m.pollUpdates[j], o);
                }
            }
            if (m.pollAdditionalMetadata != null && m.hasOwnProperty("pollAdditionalMetadata")) {
                d.pollAdditionalMetadata = $root.WAWeb.PollAdditionalMetadata.toObject(m.pollAdditionalMetadata, o);
                if (o.oneofs)
                    d._pollAdditionalMetadata = "pollAdditionalMetadata";
            }
            if (m.agentID != null && m.hasOwnProperty("agentID")) {
                d.agentID = m.agentID;
                if (o.oneofs)
                    d._agentID = "agentID";
            }
            if (m.statusAlreadyViewed != null && m.hasOwnProperty("statusAlreadyViewed")) {
                d.statusAlreadyViewed = m.statusAlreadyViewed;
                if (o.oneofs)
                    d._statusAlreadyViewed = "statusAlreadyViewed";
            }
            if (m.messageSecret != null && m.hasOwnProperty("messageSecret")) {
                d.messageSecret = o.bytes === String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
                if (o.oneofs)
                    d._messageSecret = "messageSecret";
            }
            if (m.keepInChat != null && m.hasOwnProperty("keepInChat")) {
                d.keepInChat = $root.WAWeb.KeepInChat.toObject(m.keepInChat, o);
                if (o.oneofs)
                    d._keepInChat = "keepInChat";
            }
            if (m.originalSelfAuthorUserJIDString != null && m.hasOwnProperty("originalSelfAuthorUserJIDString")) {
                d.originalSelfAuthorUserJIDString = m.originalSelfAuthorUserJIDString;
                if (o.oneofs)
                    d._originalSelfAuthorUserJIDString = "originalSelfAuthorUserJIDString";
            }
            if (m.revokeMessageTimestamp != null && m.hasOwnProperty("revokeMessageTimestamp")) {
                if (typeof m.revokeMessageTimestamp === "number")
                    d.revokeMessageTimestamp = o.longs === String ? String(m.revokeMessageTimestamp) : m.revokeMessageTimestamp;
                else
                    d.revokeMessageTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.revokeMessageTimestamp) : o.longs === Number ? new $util.LongBits(m.revokeMessageTimestamp.low >>> 0, m.revokeMessageTimestamp.high >>> 0).toNumber(true) : m.revokeMessageTimestamp;
                if (o.oneofs)
                    d._revokeMessageTimestamp = "revokeMessageTimestamp";
            }
            if (m.pinInChat != null && m.hasOwnProperty("pinInChat")) {
                d.pinInChat = $root.WAWeb.PinInChat.toObject(m.pinInChat, o);
                if (o.oneofs)
                    d._pinInChat = "pinInChat";
            }
            if (m.premiumMessageInfo != null && m.hasOwnProperty("premiumMessageInfo")) {
                d.premiumMessageInfo = $root.WAWeb.PremiumMessageInfo.toObject(m.premiumMessageInfo, o);
                if (o.oneofs)
                    d._premiumMessageInfo = "premiumMessageInfo";
            }
            if (m.is1PBizBotMessage != null && m.hasOwnProperty("is1PBizBotMessage")) {
                d.is1PBizBotMessage = m.is1PBizBotMessage;
                if (o.oneofs)
                    d._is1PBizBotMessage = "is1PBizBotMessage";
            }
            if (m.isGroupHistoryMessage != null && m.hasOwnProperty("isGroupHistoryMessage")) {
                d.isGroupHistoryMessage = m.isGroupHistoryMessage;
                if (o.oneofs)
                    d._isGroupHistoryMessage = "isGroupHistoryMessage";
            }
            if (m.botMessageInvokerJID != null && m.hasOwnProperty("botMessageInvokerJID")) {
                d.botMessageInvokerJID = m.botMessageInvokerJID;
                if (o.oneofs)
                    d._botMessageInvokerJID = "botMessageInvokerJID";
            }
            if (m.commentMetadata != null && m.hasOwnProperty("commentMetadata")) {
                d.commentMetadata = $root.WAWeb.CommentMetadata.toObject(m.commentMetadata, o);
                if (o.oneofs)
                    d._commentMetadata = "commentMetadata";
            }
            if (m.eventResponses && m.eventResponses.length) {
                d.eventResponses = [];
                for (var j = 0; j < m.eventResponses.length; ++j) {
                    d.eventResponses[j] = $root.WAWeb.EventResponse.toObject(m.eventResponses[j], o);
                }
            }
            if (m.reportingTokenInfo != null && m.hasOwnProperty("reportingTokenInfo")) {
                d.reportingTokenInfo = $root.WAWeb.ReportingTokenInfo.toObject(m.reportingTokenInfo, o);
                if (o.oneofs)
                    d._reportingTokenInfo = "reportingTokenInfo";
            }
            if (m.newsletterServerID != null && m.hasOwnProperty("newsletterServerID")) {
                if (typeof m.newsletterServerID === "number")
                    d.newsletterServerID = o.longs === String ? String(m.newsletterServerID) : m.newsletterServerID;
                else
                    d.newsletterServerID = o.longs === String ? $util.Long.prototype.toString.call(m.newsletterServerID) : o.longs === Number ? new $util.LongBits(m.newsletterServerID.low >>> 0, m.newsletterServerID.high >>> 0).toNumber(true) : m.newsletterServerID;
                if (o.oneofs)
                    d._newsletterServerID = "newsletterServerID";
            }
            if (m.eventAdditionalMetadata != null && m.hasOwnProperty("eventAdditionalMetadata")) {
                d.eventAdditionalMetadata = $root.WAWeb.EventAdditionalMetadata.toObject(m.eventAdditionalMetadata, o);
                if (o.oneofs)
                    d._eventAdditionalMetadata = "eventAdditionalMetadata";
            }
            if (m.isMentionedInStatus != null && m.hasOwnProperty("isMentionedInStatus")) {
                d.isMentionedInStatus = m.isMentionedInStatus;
                if (o.oneofs)
                    d._isMentionedInStatus = "isMentionedInStatus";
            }
            if (m.statusMentions && m.statusMentions.length) {
                d.statusMentions = [];
                for (var j = 0; j < m.statusMentions.length; ++j) {
                    d.statusMentions[j] = m.statusMentions[j];
                }
            }
            if (m.targetMessageID != null && m.hasOwnProperty("targetMessageID")) {
                d.targetMessageID = WACommon.MessageKey.toObject(m.targetMessageID, o);
                if (o.oneofs)
                    d._targetMessageID = "targetMessageID";
            }
            if (m.messageAddOns && m.messageAddOns.length) {
                d.messageAddOns = [];
                for (var j = 0; j < m.messageAddOns.length; ++j) {
                    d.messageAddOns[j] = $root.WAWeb.MessageAddOn.toObject(m.messageAddOns[j], o);
                }
            }
            if (m.statusMentionMessageInfo != null && m.hasOwnProperty("statusMentionMessageInfo")) {
                d.statusMentionMessageInfo = $root.WAWeb.StatusMentionMessage.toObject(m.statusMentionMessageInfo, o);
                if (o.oneofs)
                    d._statusMentionMessageInfo = "statusMentionMessageInfo";
            }
            if (m.isSupportAiMessage != null && m.hasOwnProperty("isSupportAiMessage")) {
                d.isSupportAiMessage = m.isSupportAiMessage;
                if (o.oneofs)
                    d._isSupportAiMessage = "isSupportAiMessage";
            }
            if (m.statusMentionSources && m.statusMentionSources.length) {
                d.statusMentionSources = [];
                for (var j = 0; j < m.statusMentionSources.length; ++j) {
                    d.statusMentionSources[j] = m.statusMentionSources[j];
                }
            }
            if (m.supportAiCitations && m.supportAiCitations.length) {
                d.supportAiCitations = [];
                for (var j = 0; j < m.supportAiCitations.length; ++j) {
                    d.supportAiCitations[j] = $root.WAWeb.Citation.toObject(m.supportAiCitations[j], o);
                }
            }
            if (m.botTargetID != null && m.hasOwnProperty("botTargetID")) {
                d.botTargetID = m.botTargetID;
                if (o.oneofs)
                    d._botTargetID = "botTargetID";
            }
            if (m.groupHistoryIndividualMessageInfo != null && m.hasOwnProperty("groupHistoryIndividualMessageInfo")) {
                d.groupHistoryIndividualMessageInfo = $root.WAWeb.GroupHistoryIndividualMessageInfo.toObject(m.groupHistoryIndividualMessageInfo, o);
                if (o.oneofs)
                    d._groupHistoryIndividualMessageInfo = "groupHistoryIndividualMessageInfo";
            }
            if (m.groupHistoryBundleInfo != null && m.hasOwnProperty("groupHistoryBundleInfo")) {
                d.groupHistoryBundleInfo = $root.WAWeb.GroupHistoryBundleInfo.toObject(m.groupHistoryBundleInfo, o);
                if (o.oneofs)
                    d._groupHistoryBundleInfo = "groupHistoryBundleInfo";
            }
            if (m.interactiveMessageAdditionalMetadata != null && m.hasOwnProperty("interactiveMessageAdditionalMetadata")) {
                d.interactiveMessageAdditionalMetadata = $root.WAWeb.InteractiveMessageAdditionalMetadata.toObject(m.interactiveMessageAdditionalMetadata, o);
                if (o.oneofs)
                    d._interactiveMessageAdditionalMetadata = "interactiveMessageAdditionalMetadata";
            }
            if (m.quarantinedMessage != null && m.hasOwnProperty("quarantinedMessage")) {
                d.quarantinedMessage = $root.WAWeb.QuarantinedMessage.toObject(m.quarantinedMessage, o);
                if (o.oneofs)
                    d._quarantinedMessage = "quarantinedMessage";
            }
            if (m.nonJIDMentions != null && m.hasOwnProperty("nonJIDMentions")) {
                d.nonJIDMentions = m.nonJIDMentions;
                if (o.oneofs)
                    d._nonJIDMentions = "nonJIDMentions";
            }
            if (m.hsmTag != null && m.hasOwnProperty("hsmTag")) {
                d.hsmTag = m.hsmTag;
                if (o.oneofs)
                    d._hsmTag = "hsmTag";
            }
            if (m.ephemeralExpirationTimestamp != null && m.hasOwnProperty("ephemeralExpirationTimestamp")) {
                if (typeof m.ephemeralExpirationTimestamp === "number")
                    d.ephemeralExpirationTimestamp = o.longs === String ? String(m.ephemeralExpirationTimestamp) : m.ephemeralExpirationTimestamp;
                else
                    d.ephemeralExpirationTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.ephemeralExpirationTimestamp) : o.longs === Number ? new $util.LongBits(m.ephemeralExpirationTimestamp.low >>> 0, m.ephemeralExpirationTimestamp.high >>> 0).toNumber(true) : m.ephemeralExpirationTimestamp;
                if (o.oneofs)
                    d._ephemeralExpirationTimestamp = "ephemeralExpirationTimestamp";
            }
            if (m.scheduledMessageMetadata != null && m.hasOwnProperty("scheduledMessageMetadata")) {
                d.scheduledMessageMetadata = $root.WAWeb.ScheduledMessageMetadata.toObject(m.scheduledMessageMetadata, o);
                if (o.oneofs)
                    d._scheduledMessageMetadata = "scheduledMessageMetadata";
            }
            if (m.decisionID != null && m.hasOwnProperty("decisionID")) {
                d.decisionID = m.decisionID;
                if (o.oneofs)
                    d._decisionID = "decisionID";
            }
            if (m.decisionSources && m.decisionSources.length) {
                d.decisionSources = [];
                for (var j = 0; j < m.decisionSources.length; ++j) {
                    d.decisionSources[j] = m.decisionSources[j];
                }
            }
            return d;
        };

        WebMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.WebMessageInfo";
        };

        WebMessageInfo.BizPrivacyStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "E2EE"] = 0;
            values[valuesById[2] = "FB"] = 2;
            values[valuesById[1] = "BSP"] = 1;
            values[valuesById[3] = "BSP_AND_FB"] = 3;
            return values;
        })();

        WebMessageInfo.StubType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "REVOKE"] = 1;
            values[valuesById[2] = "CIPHERTEXT"] = 2;
            values[valuesById[3] = "FUTUREPROOF"] = 3;
            values[valuesById[4] = "NON_VERIFIED_TRANSITION"] = 4;
            values[valuesById[5] = "UNVERIFIED_TRANSITION"] = 5;
            values[valuesById[6] = "VERIFIED_TRANSITION"] = 6;
            values[valuesById[7] = "VERIFIED_LOW_UNKNOWN"] = 7;
            values[valuesById[8] = "VERIFIED_HIGH"] = 8;
            values[valuesById[9] = "VERIFIED_INITIAL_UNKNOWN"] = 9;
            values[valuesById[10] = "VERIFIED_INITIAL_LOW"] = 10;
            values[valuesById[11] = "VERIFIED_INITIAL_HIGH"] = 11;
            values[valuesById[12] = "VERIFIED_TRANSITION_ANY_TO_NONE"] = 12;
            values[valuesById[13] = "VERIFIED_TRANSITION_ANY_TO_HIGH"] = 13;
            values[valuesById[14] = "VERIFIED_TRANSITION_HIGH_TO_LOW"] = 14;
            values[valuesById[15] = "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN"] = 15;
            values[valuesById[16] = "VERIFIED_TRANSITION_UNKNOWN_TO_LOW"] = 16;
            values[valuesById[17] = "VERIFIED_TRANSITION_LOW_TO_UNKNOWN"] = 17;
            values[valuesById[18] = "VERIFIED_TRANSITION_NONE_TO_LOW"] = 18;
            values[valuesById[19] = "VERIFIED_TRANSITION_NONE_TO_UNKNOWN"] = 19;
            values[valuesById[20] = "GROUP_CREATE"] = 20;
            values[valuesById[21] = "GROUP_CHANGE_SUBJECT"] = 21;
            values[valuesById[22] = "GROUP_CHANGE_ICON"] = 22;
            values[valuesById[23] = "GROUP_CHANGE_INVITE_LINK"] = 23;
            values[valuesById[24] = "GROUP_CHANGE_DESCRIPTION"] = 24;
            values[valuesById[25] = "GROUP_CHANGE_RESTRICT"] = 25;
            values[valuesById[26] = "GROUP_CHANGE_ANNOUNCE"] = 26;
            values[valuesById[27] = "GROUP_PARTICIPANT_ADD"] = 27;
            values[valuesById[28] = "GROUP_PARTICIPANT_REMOVE"] = 28;
            values[valuesById[29] = "GROUP_PARTICIPANT_PROMOTE"] = 29;
            values[valuesById[30] = "GROUP_PARTICIPANT_DEMOTE"] = 30;
            values[valuesById[31] = "GROUP_PARTICIPANT_INVITE"] = 31;
            values[valuesById[32] = "GROUP_PARTICIPANT_LEAVE"] = 32;
            values[valuesById[33] = "GROUP_PARTICIPANT_CHANGE_NUMBER"] = 33;
            values[valuesById[34] = "BROADCAST_CREATE"] = 34;
            values[valuesById[35] = "BROADCAST_ADD"] = 35;
            values[valuesById[36] = "BROADCAST_REMOVE"] = 36;
            values[valuesById[37] = "GENERIC_NOTIFICATION"] = 37;
            values[valuesById[38] = "E2E_IDENTITY_CHANGED"] = 38;
            values[valuesById[39] = "E2E_ENCRYPTED"] = 39;
            values[valuesById[40] = "CALL_MISSED_VOICE"] = 40;
            values[valuesById[41] = "CALL_MISSED_VIDEO"] = 41;
            values[valuesById[42] = "INDIVIDUAL_CHANGE_NUMBER"] = 42;
            values[valuesById[43] = "GROUP_DELETE"] = 43;
            values[valuesById[44] = "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE"] = 44;
            values[valuesById[45] = "CALL_MISSED_GROUP_VOICE"] = 45;
            values[valuesById[46] = "CALL_MISSED_GROUP_VIDEO"] = 46;
            values[valuesById[47] = "PAYMENT_CIPHERTEXT"] = 47;
            values[valuesById[48] = "PAYMENT_FUTUREPROOF"] = 48;
            values[valuesById[49] = "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED"] = 49;
            values[valuesById[50] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED"] = 50;
            values[valuesById[51] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED"] = 51;
            values[valuesById[52] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP"] = 52;
            values[valuesById[53] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP"] = 53;
            values[valuesById[54] = "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER"] = 54;
            values[valuesById[55] = "PAYMENT_ACTION_SEND_PAYMENT_REMINDER"] = 55;
            values[valuesById[56] = "PAYMENT_ACTION_SEND_PAYMENT_INVITATION"] = 56;
            values[valuesById[57] = "PAYMENT_ACTION_REQUEST_DECLINED"] = 57;
            values[valuesById[58] = "PAYMENT_ACTION_REQUEST_EXPIRED"] = 58;
            values[valuesById[59] = "PAYMENT_ACTION_REQUEST_CANCELLED"] = 59;
            values[valuesById[60] = "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM"] = 60;
            values[valuesById[61] = "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP"] = 61;
            values[valuesById[62] = "BIZ_INTRO_TOP"] = 62;
            values[valuesById[63] = "BIZ_INTRO_BOTTOM"] = 63;
            values[valuesById[64] = "BIZ_NAME_CHANGE"] = 64;
            values[valuesById[65] = "BIZ_MOVE_TO_CONSUMER_APP"] = 65;
            values[valuesById[66] = "BIZ_TWO_TIER_MIGRATION_TOP"] = 66;
            values[valuesById[67] = "BIZ_TWO_TIER_MIGRATION_BOTTOM"] = 67;
            values[valuesById[68] = "OVERSIZED"] = 68;
            values[valuesById[69] = "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED"] = 69;
            values[valuesById[70] = "GROUP_V4_ADD_INVITE_SENT"] = 70;
            values[valuesById[71] = "GROUP_PARTICIPANT_ADD_REQUEST_JOIN"] = 71;
            values[valuesById[72] = "CHANGE_EPHEMERAL_SETTING"] = 72;
            values[valuesById[73] = "E2E_DEVICE_CHANGED"] = 73;
            values[valuesById[74] = "VIEWED_ONCE"] = 74;
            values[valuesById[75] = "E2E_ENCRYPTED_NOW"] = 75;
            values[valuesById[76] = "BLUE_MSG_BSP_FB_TO_BSP_PREMISE"] = 76;
            values[valuesById[77] = "BLUE_MSG_BSP_FB_TO_SELF_FB"] = 77;
            values[valuesById[78] = "BLUE_MSG_BSP_FB_TO_SELF_PREMISE"] = 78;
            values[valuesById[79] = "BLUE_MSG_BSP_FB_UNVERIFIED"] = 79;
            values[valuesById[80] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 80;
            values[valuesById[81] = "BLUE_MSG_BSP_FB_VERIFIED"] = 81;
            values[valuesById[82] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 82;
            values[valuesById[83] = "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE"] = 83;
            values[valuesById[84] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED"] = 84;
            values[valuesById[85] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 85;
            values[valuesById[86] = "BLUE_MSG_BSP_PREMISE_VERIFIED"] = 86;
            values[valuesById[87] = "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 87;
            values[valuesById[88] = "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED"] = 88;
            values[valuesById[89] = "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED"] = 89;
            values[valuesById[90] = "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED"] = 90;
            values[valuesById[91] = "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED"] = 91;
            values[valuesById[92] = "BLUE_MSG_SELF_FB_TO_BSP_PREMISE"] = 92;
            values[valuesById[93] = "BLUE_MSG_SELF_FB_TO_SELF_PREMISE"] = 93;
            values[valuesById[94] = "BLUE_MSG_SELF_FB_UNVERIFIED"] = 94;
            values[valuesById[95] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 95;
            values[valuesById[96] = "BLUE_MSG_SELF_FB_VERIFIED"] = 96;
            values[valuesById[97] = "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 97;
            values[valuesById[98] = "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE"] = 98;
            values[valuesById[99] = "BLUE_MSG_SELF_PREMISE_UNVERIFIED"] = 99;
            values[valuesById[100] = "BLUE_MSG_SELF_PREMISE_VERIFIED"] = 100;
            values[valuesById[101] = "BLUE_MSG_TO_BSP_FB"] = 101;
            values[valuesById[102] = "BLUE_MSG_TO_CONSUMER"] = 102;
            values[valuesById[103] = "BLUE_MSG_TO_SELF_FB"] = 103;
            values[valuesById[104] = "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED"] = 104;
            values[valuesById[105] = "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 105;
            values[valuesById[106] = "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 106;
            values[valuesById[107] = "BLUE_MSG_UNVERIFIED_TO_VERIFIED"] = 107;
            values[valuesById[108] = "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED"] = 108;
            values[valuesById[109] = "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 109;
            values[valuesById[110] = "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 110;
            values[valuesById[111] = "BLUE_MSG_VERIFIED_TO_UNVERIFIED"] = 111;
            values[valuesById[112] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 112;
            values[valuesById[113] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 113;
            values[valuesById[114] = "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 114;
            values[valuesById[115] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 115;
            values[valuesById[116] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 116;
            values[valuesById[117] = "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 117;
            values[valuesById[118] = "E2E_IDENTITY_UNAVAILABLE"] = 118;
            values[valuesById[119] = "GROUP_CREATING"] = 119;
            values[valuesById[120] = "GROUP_CREATE_FAILED"] = 120;
            values[valuesById[121] = "GROUP_BOUNCED"] = 121;
            values[valuesById[122] = "BLOCK_CONTACT"] = 122;
            values[valuesById[123] = "EPHEMERAL_SETTING_NOT_APPLIED"] = 123;
            values[valuesById[124] = "SYNC_FAILED"] = 124;
            values[valuesById[125] = "SYNCING"] = 125;
            values[valuesById[126] = "BIZ_PRIVACY_MODE_INIT_FB"] = 126;
            values[valuesById[127] = "BIZ_PRIVACY_MODE_INIT_BSP"] = 127;
            values[valuesById[128] = "BIZ_PRIVACY_MODE_TO_FB"] = 128;
            values[valuesById[129] = "BIZ_PRIVACY_MODE_TO_BSP"] = 129;
            values[valuesById[130] = "DISAPPEARING_MODE"] = 130;
            values[valuesById[131] = "E2E_DEVICE_FETCH_FAILED"] = 131;
            values[valuesById[132] = "ADMIN_REVOKE"] = 132;
            values[valuesById[133] = "GROUP_INVITE_LINK_GROWTH_LOCKED"] = 133;
            values[valuesById[134] = "COMMUNITY_LINK_PARENT_GROUP"] = 134;
            values[valuesById[135] = "COMMUNITY_LINK_SIBLING_GROUP"] = 135;
            values[valuesById[136] = "COMMUNITY_LINK_SUB_GROUP"] = 136;
            values[valuesById[137] = "COMMUNITY_UNLINK_PARENT_GROUP"] = 137;
            values[valuesById[138] = "COMMUNITY_UNLINK_SIBLING_GROUP"] = 138;
            values[valuesById[139] = "COMMUNITY_UNLINK_SUB_GROUP"] = 139;
            values[valuesById[140] = "GROUP_PARTICIPANT_ACCEPT"] = 140;
            values[valuesById[141] = "GROUP_PARTICIPANT_LINKED_GROUP_JOIN"] = 141;
            values[valuesById[142] = "COMMUNITY_CREATE"] = 142;
            values[valuesById[143] = "EPHEMERAL_KEEP_IN_CHAT"] = 143;
            values[valuesById[144] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST"] = 144;
            values[valuesById[145] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE"] = 145;
            values[valuesById[146] = "INTEGRITY_UNLINK_PARENT_GROUP"] = 146;
            values[valuesById[147] = "COMMUNITY_PARTICIPANT_PROMOTE"] = 147;
            values[valuesById[148] = "COMMUNITY_PARTICIPANT_DEMOTE"] = 148;
            values[valuesById[149] = "COMMUNITY_PARENT_GROUP_DELETED"] = 149;
            values[valuesById[150] = "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL"] = 150;
            values[valuesById[151] = "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP"] = 151;
            values[valuesById[152] = "MASKED_THREAD_CREATED"] = 152;
            values[valuesById[153] = "MASKED_THREAD_UNMASKED"] = 153;
            values[valuesById[154] = "BIZ_CHAT_ASSIGNMENT"] = 154;
            values[valuesById[155] = "CHAT_PSA"] = 155;
            values[valuesById[156] = "CHAT_POLL_CREATION_MESSAGE"] = 156;
            values[valuesById[157] = "CAG_MASKED_THREAD_CREATED"] = 157;
            values[valuesById[158] = "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED"] = 158;
            values[valuesById[159] = "CAG_INVITE_AUTO_ADD"] = 159;
            values[valuesById[160] = "BIZ_CHAT_ASSIGNMENT_UNASSIGN"] = 160;
            values[valuesById[161] = "CAG_INVITE_AUTO_JOINED"] = 161;
            values[valuesById[162] = "SCHEDULED_CALL_START_MESSAGE"] = 162;
            values[valuesById[163] = "COMMUNITY_INVITE_RICH"] = 163;
            values[valuesById[164] = "COMMUNITY_INVITE_AUTO_ADD_RICH"] = 164;
            values[valuesById[165] = "SUB_GROUP_INVITE_RICH"] = 165;
            values[valuesById[166] = "SUB_GROUP_PARTICIPANT_ADD_RICH"] = 166;
            values[valuesById[167] = "COMMUNITY_LINK_PARENT_GROUP_RICH"] = 167;
            values[valuesById[168] = "COMMUNITY_PARTICIPANT_ADD_RICH"] = 168;
            values[valuesById[169] = "SILENCED_UNKNOWN_CALLER_AUDIO"] = 169;
            values[valuesById[170] = "SILENCED_UNKNOWN_CALLER_VIDEO"] = 170;
            values[valuesById[171] = "GROUP_MEMBER_ADD_MODE"] = 171;
            values[valuesById[172] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD"] = 172;
            values[valuesById[173] = "COMMUNITY_CHANGE_DESCRIPTION"] = 173;
            values[valuesById[174] = "SENDER_INVITE"] = 174;
            values[valuesById[175] = "RECEIVER_INVITE"] = 175;
            values[valuesById[176] = "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS"] = 176;
            values[valuesById[177] = "PINNED_MESSAGE_IN_CHAT"] = 177;
            values[valuesById[178] = "PAYMENT_INVITE_SETUP_INVITER"] = 178;
            values[valuesById[179] = "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY"] = 179;
            values[valuesById[180] = "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE"] = 180;
            values[valuesById[181] = "LINKED_GROUP_CALL_START"] = 181;
            values[valuesById[182] = "REPORT_TO_ADMIN_ENABLED_STATUS"] = 182;
            values[valuesById[183] = "EMPTY_SUBGROUP_CREATE"] = 183;
            values[valuesById[184] = "SCHEDULED_CALL_CANCEL"] = 184;
            values[valuesById[185] = "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH"] = 185;
            values[valuesById[186] = "GROUP_CHANGE_RECENT_HISTORY_SHARING"] = 186;
            values[valuesById[187] = "PAID_MESSAGE_SERVER_CAMPAIGN_ID"] = 187;
            values[valuesById[188] = "GENERAL_CHAT_CREATE"] = 188;
            values[valuesById[189] = "GENERAL_CHAT_ADD"] = 189;
            values[valuesById[190] = "GENERAL_CHAT_AUTO_ADD_DISABLED"] = 190;
            values[valuesById[191] = "SUGGESTED_SUBGROUP_ANNOUNCE"] = 191;
            values[valuesById[192] = "BIZ_BOT_1P_MESSAGING_ENABLED"] = 192;
            values[valuesById[193] = "CHANGE_USERNAME"] = 193;
            values[valuesById[194] = "BIZ_COEX_PRIVACY_INIT_SELF"] = 194;
            values[valuesById[195] = "BIZ_COEX_PRIVACY_TRANSITION_SELF"] = 195;
            values[valuesById[196] = "SUPPORT_AI_EDUCATION"] = 196;
            values[valuesById[197] = "BIZ_BOT_3P_MESSAGING_ENABLED"] = 197;
            values[valuesById[198] = "REMINDER_SETUP_MESSAGE"] = 198;
            values[valuesById[199] = "REMINDER_SENT_MESSAGE"] = 199;
            values[valuesById[200] = "REMINDER_CANCEL_MESSAGE"] = 200;
            values[valuesById[201] = "BIZ_COEX_PRIVACY_INIT"] = 201;
            values[valuesById[202] = "BIZ_COEX_PRIVACY_TRANSITION"] = 202;
            values[valuesById[203] = "GROUP_DEACTIVATED"] = 203;
            values[valuesById[204] = "COMMUNITY_DEACTIVATE_SIBLING_GROUP"] = 204;
            values[valuesById[205] = "EVENT_UPDATED"] = 205;
            values[valuesById[206] = "EVENT_CANCELED"] = 206;
            values[valuesById[207] = "COMMUNITY_OWNER_UPDATED"] = 207;
            values[valuesById[208] = "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN"] = 208;
            values[valuesById[209] = "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE"] = 209;
            values[valuesById[210] = "STATUS_MENTION"] = 210;
            values[valuesById[211] = "USER_CONTROLS_SYSTEM_MESSAGE"] = 211;
            values[valuesById[212] = "SUPPORT_SYSTEM_MESSAGE"] = 212;
            values[valuesById[213] = "CHANGE_LID"] = 213;
            values[valuesById[214] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE"] = 214;
            values[valuesById[215] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE"] = 215;
            values[valuesById[216] = "CHANGE_LIMIT_SHARING"] = 216;
            values[valuesById[217] = "GROUP_MEMBER_LINK_MODE"] = 217;
            values[valuesById[218] = "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE"] = 218;
            values[valuesById[219] = "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE"] = 219;
            values[valuesById[220] = "QUARANTINED_MESSAGE"] = 220;
            values[valuesById[221] = "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE"] = 221;
            values[valuesById[222] = "GROUP_OPEN_BOT_ADDED"] = 222;
            values[valuesById[223] = "GROUP_TEE_BOT_ADDED"] = 223;
            values[valuesById[224] = "CONTACT_INFO"] = 224;
            values[valuesById[225] = "SCHEDULED_MESSAGE_CREATED"] = 225;
            return values;
        })();

        WebMessageInfo.Status = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "ERROR"] = 0;
            values[valuesById[1] = "PENDING"] = 1;
            values[valuesById[2] = "SERVER_ACK"] = 2;
            values[valuesById[3] = "DELIVERY_ACK"] = 3;
            values[valuesById[4] = "READ"] = 4;
            values[valuesById[5] = "PLAYED"] = 5;
            return values;
        })();

        return WebMessageInfo;
    })();

    WAWeb.PaymentInfo = (function() {

        function PaymentInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PaymentInfo.prototype.currencyDeprecated = null;
        PaymentInfo.prototype.amount1000 = null;
        PaymentInfo.prototype.receiverJID = null;
        PaymentInfo.prototype.status = null;
        PaymentInfo.prototype.transactionTimestamp = null;
        PaymentInfo.prototype.requestMessageKey = null;
        PaymentInfo.prototype.expiryTimestamp = null;
        PaymentInfo.prototype.futureproofed = null;
        PaymentInfo.prototype.currency = null;
        PaymentInfo.prototype.txnStatus = null;
        PaymentInfo.prototype.useNoviFiatFormat = null;
        PaymentInfo.prototype.primaryAmount = null;
        PaymentInfo.prototype.exchangeAmount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_currencyDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["currencyDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_amount1000", {
            get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_receiverJID", {
            get: $util.oneOfGetter($oneOfFields = ["receiverJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_transactionTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["transactionTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_futureproofed", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_currency", {
            get: $util.oneOfGetter($oneOfFields = ["currency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_txnStatus", {
            get: $util.oneOfGetter($oneOfFields = ["txnStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_useNoviFiatFormat", {
            get: $util.oneOfGetter($oneOfFields = ["useNoviFiatFormat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_primaryAmount", {
            get: $util.oneOfGetter($oneOfFields = ["primaryAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInfo.prototype, "_exchangeAmount", {
            get: $util.oneOfGetter($oneOfFields = ["exchangeAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentInfo.create = function create(properties) {
            return new PaymentInfo(properties);
        };

        PaymentInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.currencyDeprecated != null && Object.hasOwnProperty.call(m, "currencyDeprecated"))
                w.uint32(8).int32(m.currencyDeprecated);
            if (m.amount1000 != null && Object.hasOwnProperty.call(m, "amount1000"))
                w.uint32(16).uint64(m.amount1000);
            if (m.receiverJID != null && Object.hasOwnProperty.call(m, "receiverJID"))
                w.uint32(26).string(m.receiverJID);
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(32).int32(m.status);
            if (m.transactionTimestamp != null && Object.hasOwnProperty.call(m, "transactionTimestamp"))
                w.uint32(40).uint64(m.transactionTimestamp);
            if (m.requestMessageKey != null && Object.hasOwnProperty.call(m, "requestMessageKey"))
                WACommon.MessageKey.encode(m.requestMessageKey, w.uint32(50).fork()).ldelim();
            if (m.expiryTimestamp != null && Object.hasOwnProperty.call(m, "expiryTimestamp"))
                w.uint32(56).uint64(m.expiryTimestamp);
            if (m.futureproofed != null && Object.hasOwnProperty.call(m, "futureproofed"))
                w.uint32(64).bool(m.futureproofed);
            if (m.currency != null && Object.hasOwnProperty.call(m, "currency"))
                w.uint32(74).string(m.currency);
            if (m.txnStatus != null && Object.hasOwnProperty.call(m, "txnStatus"))
                w.uint32(80).int32(m.txnStatus);
            if (m.useNoviFiatFormat != null && Object.hasOwnProperty.call(m, "useNoviFiatFormat"))
                w.uint32(88).bool(m.useNoviFiatFormat);
            if (m.primaryAmount != null && Object.hasOwnProperty.call(m, "primaryAmount"))
                WAE2E.Money.encode(m.primaryAmount, w.uint32(98).fork()).ldelim();
            if (m.exchangeAmount != null && Object.hasOwnProperty.call(m, "exchangeAmount"))
                WAE2E.Money.encode(m.exchangeAmount, w.uint32(106).fork()).ldelim();
            return w;
        };

        PaymentInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PaymentInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.currencyDeprecated = r.int32();
                        break;
                    }
                case 2: {
                        m.amount1000 = r.uint64();
                        break;
                    }
                case 3: {
                        m.receiverJID = r.string();
                        break;
                    }
                case 4: {
                        m.status = r.int32();
                        break;
                    }
                case 5: {
                        m.transactionTimestamp = r.uint64();
                        break;
                    }
                case 6: {
                        m.requestMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.expiryTimestamp = r.uint64();
                        break;
                    }
                case 8: {
                        m.futureproofed = r.bool();
                        break;
                    }
                case 9: {
                        m.currency = r.string();
                        break;
                    }
                case 10: {
                        m.txnStatus = r.int32();
                        break;
                    }
                case 11: {
                        m.useNoviFiatFormat = r.bool();
                        break;
                    }
                case 12: {
                        m.primaryAmount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 13: {
                        m.exchangeAmount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PaymentInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PaymentInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PaymentInfo();
            switch (d.currencyDeprecated) {
            default:
                if (typeof d.currencyDeprecated === "number") {
                    m.currencyDeprecated = d.currencyDeprecated;
                    break;
                }
                break;
            case "UNKNOWN_CURRENCY":
            case 0:
                m.currencyDeprecated = 0;
                break;
            case "INR":
            case 1:
                m.currencyDeprecated = 1;
                break;
            }
            if (d.amount1000 != null) {
                if ($util.Long)
                    (m.amount1000 = $util.Long.fromValue(d.amount1000)).unsigned = true;
                else if (typeof d.amount1000 === "string")
                    m.amount1000 = parseInt(d.amount1000, 10);
                else if (typeof d.amount1000 === "number")
                    m.amount1000 = d.amount1000;
                else if (typeof d.amount1000 === "object")
                    m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber(true);
            }
            if (d.receiverJID != null) {
                m.receiverJID = String(d.receiverJID);
            }
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "UNKNOWN_STATUS":
            case 0:
                m.status = 0;
                break;
            case "PROCESSING":
            case 1:
                m.status = 1;
                break;
            case "SENT":
            case 2:
                m.status = 2;
                break;
            case "NEED_TO_ACCEPT":
            case 3:
                m.status = 3;
                break;
            case "COMPLETE":
            case 4:
                m.status = 4;
                break;
            case "COULD_NOT_COMPLETE":
            case 5:
                m.status = 5;
                break;
            case "REFUNDED":
            case 6:
                m.status = 6;
                break;
            case "EXPIRED":
            case 7:
                m.status = 7;
                break;
            case "REJECTED":
            case 8:
                m.status = 8;
                break;
            case "CANCELLED":
            case 9:
                m.status = 9;
                break;
            case "WAITING_FOR_PAYER":
            case 10:
                m.status = 10;
                break;
            case "WAITING":
            case 11:
                m.status = 11;
                break;
            }
            if (d.transactionTimestamp != null) {
                if ($util.Long)
                    (m.transactionTimestamp = $util.Long.fromValue(d.transactionTimestamp)).unsigned = true;
                else if (typeof d.transactionTimestamp === "string")
                    m.transactionTimestamp = parseInt(d.transactionTimestamp, 10);
                else if (typeof d.transactionTimestamp === "number")
                    m.transactionTimestamp = d.transactionTimestamp;
                else if (typeof d.transactionTimestamp === "object")
                    m.transactionTimestamp = new $util.LongBits(d.transactionTimestamp.low >>> 0, d.transactionTimestamp.high >>> 0).toNumber(true);
            }
            if (d.requestMessageKey != null) {
                if (typeof d.requestMessageKey !== "object")
                    throw TypeError(".WAWeb.PaymentInfo.requestMessageKey: object expected");
                m.requestMessageKey = WACommon.MessageKey.fromObject(d.requestMessageKey, n + 1);
            }
            if (d.expiryTimestamp != null) {
                if ($util.Long)
                    (m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp)).unsigned = true;
                else if (typeof d.expiryTimestamp === "string")
                    m.expiryTimestamp = parseInt(d.expiryTimestamp, 10);
                else if (typeof d.expiryTimestamp === "number")
                    m.expiryTimestamp = d.expiryTimestamp;
                else if (typeof d.expiryTimestamp === "object")
                    m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber(true);
            }
            if (d.futureproofed != null) {
                m.futureproofed = Boolean(d.futureproofed);
            }
            if (d.currency != null) {
                m.currency = String(d.currency);
            }
            switch (d.txnStatus) {
            default:
                if (typeof d.txnStatus === "number") {
                    m.txnStatus = d.txnStatus;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.txnStatus = 0;
                break;
            case "PENDING_SETUP":
            case 1:
                m.txnStatus = 1;
                break;
            case "PENDING_RECEIVER_SETUP":
            case 2:
                m.txnStatus = 2;
                break;
            case "INIT":
            case 3:
                m.txnStatus = 3;
                break;
            case "SUCCESS":
            case 4:
                m.txnStatus = 4;
                break;
            case "COMPLETED":
            case 5:
                m.txnStatus = 5;
                break;
            case "FAILED":
            case 6:
                m.txnStatus = 6;
                break;
            case "FAILED_RISK":
            case 7:
                m.txnStatus = 7;
                break;
            case "FAILED_PROCESSING":
            case 8:
                m.txnStatus = 8;
                break;
            case "FAILED_RECEIVER_PROCESSING":
            case 9:
                m.txnStatus = 9;
                break;
            case "FAILED_DA":
            case 10:
                m.txnStatus = 10;
                break;
            case "FAILED_DA_FINAL":
            case 11:
                m.txnStatus = 11;
                break;
            case "REFUNDED_TXN":
            case 12:
                m.txnStatus = 12;
                break;
            case "REFUND_FAILED":
            case 13:
                m.txnStatus = 13;
                break;
            case "REFUND_FAILED_PROCESSING":
            case 14:
                m.txnStatus = 14;
                break;
            case "REFUND_FAILED_DA":
            case 15:
                m.txnStatus = 15;
                break;
            case "EXPIRED_TXN":
            case 16:
                m.txnStatus = 16;
                break;
            case "AUTH_CANCELED":
            case 17:
                m.txnStatus = 17;
                break;
            case "AUTH_CANCEL_FAILED_PROCESSING":
            case 18:
                m.txnStatus = 18;
                break;
            case "AUTH_CANCEL_FAILED":
            case 19:
                m.txnStatus = 19;
                break;
            case "COLLECT_INIT":
            case 20:
                m.txnStatus = 20;
                break;
            case "COLLECT_SUCCESS":
            case 21:
                m.txnStatus = 21;
                break;
            case "COLLECT_FAILED":
            case 22:
                m.txnStatus = 22;
                break;
            case "COLLECT_FAILED_RISK":
            case 23:
                m.txnStatus = 23;
                break;
            case "COLLECT_REJECTED":
            case 24:
                m.txnStatus = 24;
                break;
            case "COLLECT_EXPIRED":
            case 25:
                m.txnStatus = 25;
                break;
            case "COLLECT_CANCELED":
            case 26:
                m.txnStatus = 26;
                break;
            case "COLLECT_CANCELLING":
            case 27:
                m.txnStatus = 27;
                break;
            case "IN_REVIEW":
            case 28:
                m.txnStatus = 28;
                break;
            case "REVERSAL_SUCCESS":
            case 29:
                m.txnStatus = 29;
                break;
            case "REVERSAL_PENDING":
            case 30:
                m.txnStatus = 30;
                break;
            case "REFUND_PENDING":
            case 31:
                m.txnStatus = 31;
                break;
            }
            if (d.useNoviFiatFormat != null) {
                m.useNoviFiatFormat = Boolean(d.useNoviFiatFormat);
            }
            if (d.primaryAmount != null) {
                if (typeof d.primaryAmount !== "object")
                    throw TypeError(".WAWeb.PaymentInfo.primaryAmount: object expected");
                m.primaryAmount = WAE2E.Money.fromObject(d.primaryAmount, n + 1);
            }
            if (d.exchangeAmount != null) {
                if (typeof d.exchangeAmount !== "object")
                    throw TypeError(".WAWeb.PaymentInfo.exchangeAmount: object expected");
                m.exchangeAmount = WAE2E.Money.fromObject(d.exchangeAmount, n + 1);
            }
            return m;
        };

        PaymentInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.currencyDeprecated != null && m.hasOwnProperty("currencyDeprecated")) {
                d.currencyDeprecated = o.enums === String ? $root.WAWeb.PaymentInfo.Currency[m.currencyDeprecated] === undefined ? m.currencyDeprecated : $root.WAWeb.PaymentInfo.Currency[m.currencyDeprecated] : m.currencyDeprecated;
                if (o.oneofs)
                    d._currencyDeprecated = "currencyDeprecated";
            }
            if (m.amount1000 != null && m.hasOwnProperty("amount1000")) {
                if (typeof m.amount1000 === "number")
                    d.amount1000 = o.longs === String ? String(m.amount1000) : m.amount1000;
                else
                    d.amount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber(true) : m.amount1000;
                if (o.oneofs)
                    d._amount1000 = "amount1000";
            }
            if (m.receiverJID != null && m.hasOwnProperty("receiverJID")) {
                d.receiverJID = m.receiverJID;
                if (o.oneofs)
                    d._receiverJID = "receiverJID";
            }
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? $root.WAWeb.PaymentInfo.Status[m.status] === undefined ? m.status : $root.WAWeb.PaymentInfo.Status[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            if (m.transactionTimestamp != null && m.hasOwnProperty("transactionTimestamp")) {
                if (typeof m.transactionTimestamp === "number")
                    d.transactionTimestamp = o.longs === String ? String(m.transactionTimestamp) : m.transactionTimestamp;
                else
                    d.transactionTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.transactionTimestamp) : o.longs === Number ? new $util.LongBits(m.transactionTimestamp.low >>> 0, m.transactionTimestamp.high >>> 0).toNumber(true) : m.transactionTimestamp;
                if (o.oneofs)
                    d._transactionTimestamp = "transactionTimestamp";
            }
            if (m.requestMessageKey != null && m.hasOwnProperty("requestMessageKey")) {
                d.requestMessageKey = WACommon.MessageKey.toObject(m.requestMessageKey, o);
                if (o.oneofs)
                    d._requestMessageKey = "requestMessageKey";
            }
            if (m.expiryTimestamp != null && m.hasOwnProperty("expiryTimestamp")) {
                if (typeof m.expiryTimestamp === "number")
                    d.expiryTimestamp = o.longs === String ? String(m.expiryTimestamp) : m.expiryTimestamp;
                else
                    d.expiryTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber(true) : m.expiryTimestamp;
                if (o.oneofs)
                    d._expiryTimestamp = "expiryTimestamp";
            }
            if (m.futureproofed != null && m.hasOwnProperty("futureproofed")) {
                d.futureproofed = m.futureproofed;
                if (o.oneofs)
                    d._futureproofed = "futureproofed";
            }
            if (m.currency != null && m.hasOwnProperty("currency")) {
                d.currency = m.currency;
                if (o.oneofs)
                    d._currency = "currency";
            }
            if (m.txnStatus != null && m.hasOwnProperty("txnStatus")) {
                d.txnStatus = o.enums === String ? $root.WAWeb.PaymentInfo.TxnStatus[m.txnStatus] === undefined ? m.txnStatus : $root.WAWeb.PaymentInfo.TxnStatus[m.txnStatus] : m.txnStatus;
                if (o.oneofs)
                    d._txnStatus = "txnStatus";
            }
            if (m.useNoviFiatFormat != null && m.hasOwnProperty("useNoviFiatFormat")) {
                d.useNoviFiatFormat = m.useNoviFiatFormat;
                if (o.oneofs)
                    d._useNoviFiatFormat = "useNoviFiatFormat";
            }
            if (m.primaryAmount != null && m.hasOwnProperty("primaryAmount")) {
                d.primaryAmount = WAE2E.Money.toObject(m.primaryAmount, o);
                if (o.oneofs)
                    d._primaryAmount = "primaryAmount";
            }
            if (m.exchangeAmount != null && m.hasOwnProperty("exchangeAmount")) {
                d.exchangeAmount = WAE2E.Money.toObject(m.exchangeAmount, o);
                if (o.oneofs)
                    d._exchangeAmount = "exchangeAmount";
            }
            return d;
        };

        PaymentInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PaymentInfo";
        };

        PaymentInfo.TxnStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PENDING_SETUP"] = 1;
            values[valuesById[2] = "PENDING_RECEIVER_SETUP"] = 2;
            values[valuesById[3] = "INIT"] = 3;
            values[valuesById[4] = "SUCCESS"] = 4;
            values[valuesById[5] = "COMPLETED"] = 5;
            values[valuesById[6] = "FAILED"] = 6;
            values[valuesById[7] = "FAILED_RISK"] = 7;
            values[valuesById[8] = "FAILED_PROCESSING"] = 8;
            values[valuesById[9] = "FAILED_RECEIVER_PROCESSING"] = 9;
            values[valuesById[10] = "FAILED_DA"] = 10;
            values[valuesById[11] = "FAILED_DA_FINAL"] = 11;
            values[valuesById[12] = "REFUNDED_TXN"] = 12;
            values[valuesById[13] = "REFUND_FAILED"] = 13;
            values[valuesById[14] = "REFUND_FAILED_PROCESSING"] = 14;
            values[valuesById[15] = "REFUND_FAILED_DA"] = 15;
            values[valuesById[16] = "EXPIRED_TXN"] = 16;
            values[valuesById[17] = "AUTH_CANCELED"] = 17;
            values[valuesById[18] = "AUTH_CANCEL_FAILED_PROCESSING"] = 18;
            values[valuesById[19] = "AUTH_CANCEL_FAILED"] = 19;
            values[valuesById[20] = "COLLECT_INIT"] = 20;
            values[valuesById[21] = "COLLECT_SUCCESS"] = 21;
            values[valuesById[22] = "COLLECT_FAILED"] = 22;
            values[valuesById[23] = "COLLECT_FAILED_RISK"] = 23;
            values[valuesById[24] = "COLLECT_REJECTED"] = 24;
            values[valuesById[25] = "COLLECT_EXPIRED"] = 25;
            values[valuesById[26] = "COLLECT_CANCELED"] = 26;
            values[valuesById[27] = "COLLECT_CANCELLING"] = 27;
            values[valuesById[28] = "IN_REVIEW"] = 28;
            values[valuesById[29] = "REVERSAL_SUCCESS"] = 29;
            values[valuesById[30] = "REVERSAL_PENDING"] = 30;
            values[valuesById[31] = "REFUND_PENDING"] = 31;
            return values;
        })();

        PaymentInfo.Status = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "PROCESSING"] = 1;
            values[valuesById[2] = "SENT"] = 2;
            values[valuesById[3] = "NEED_TO_ACCEPT"] = 3;
            values[valuesById[4] = "COMPLETE"] = 4;
            values[valuesById[5] = "COULD_NOT_COMPLETE"] = 5;
            values[valuesById[6] = "REFUNDED"] = 6;
            values[valuesById[7] = "EXPIRED"] = 7;
            values[valuesById[8] = "REJECTED"] = 8;
            values[valuesById[9] = "CANCELLED"] = 9;
            values[valuesById[10] = "WAITING_FOR_PAYER"] = 10;
            values[valuesById[11] = "WAITING"] = 11;
            return values;
        })();

        PaymentInfo.Currency = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_CURRENCY"] = 0;
            values[valuesById[1] = "INR"] = 1;
            return values;
        })();

        return PaymentInfo;
    })();

    WAWeb.WebFeatures = (function() {

        function WebFeatures(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        WebFeatures.prototype.labelsDisplay = null;
        WebFeatures.prototype.voipIndividualOutgoing = null;
        WebFeatures.prototype.groupsV3 = null;
        WebFeatures.prototype.groupsV3Create = null;
        WebFeatures.prototype.changeNumberV2 = null;
        WebFeatures.prototype.queryStatusV3Thumbnail = null;
        WebFeatures.prototype.liveLocations = null;
        WebFeatures.prototype.queryVname = null;
        WebFeatures.prototype.voipIndividualIncoming = null;
        WebFeatures.prototype.quickRepliesQuery = null;
        WebFeatures.prototype.payments = null;
        WebFeatures.prototype.stickerPackQuery = null;
        WebFeatures.prototype.liveLocationsFinal = null;
        WebFeatures.prototype.labelsEdit = null;
        WebFeatures.prototype.mediaUpload = null;
        WebFeatures.prototype.mediaUploadRichQuickReplies = null;
        WebFeatures.prototype.vnameV2 = null;
        WebFeatures.prototype.videoPlaybackURL = null;
        WebFeatures.prototype.statusRanking = null;
        WebFeatures.prototype.voipIndividualVideo = null;
        WebFeatures.prototype.thirdPartyStickers = null;
        WebFeatures.prototype.frequentlyForwardedSetting = null;
        WebFeatures.prototype.groupsV4JoinPermission = null;
        WebFeatures.prototype.recentStickers = null;
        WebFeatures.prototype.catalog = null;
        WebFeatures.prototype.starredStickers = null;
        WebFeatures.prototype.voipGroupCall = null;
        WebFeatures.prototype.templateMessage = null;
        WebFeatures.prototype.templateMessageInteractivity = null;
        WebFeatures.prototype.ephemeralMessages = null;
        WebFeatures.prototype.e2ENotificationSync = null;
        WebFeatures.prototype.recentStickersV2 = null;
        WebFeatures.prototype.recentStickersV3 = null;
        WebFeatures.prototype.userNotice = null;
        WebFeatures.prototype.support = null;
        WebFeatures.prototype.groupUiiCleanup = null;
        WebFeatures.prototype.groupDogfoodingInternalOnly = null;
        WebFeatures.prototype.settingsSync = null;
        WebFeatures.prototype.archiveV2 = null;
        WebFeatures.prototype.ephemeralAllowGroupMembers = null;
        WebFeatures.prototype.ephemeral24HDuration = null;
        WebFeatures.prototype.mdForceUpgrade = null;
        WebFeatures.prototype.disappearingMode = null;
        WebFeatures.prototype.externalMdOptInAvailable = null;
        WebFeatures.prototype.noDeleteMessageTimeLimit = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_labelsDisplay", {
            get: $util.oneOfGetter($oneOfFields = ["labelsDisplay"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualOutgoing", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualOutgoing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV3", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV3Create", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3Create"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_changeNumberV2", {
            get: $util.oneOfGetter($oneOfFields = ["changeNumberV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_queryStatusV3Thumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["queryStatusV3Thumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_liveLocations", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocations"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_queryVname", {
            get: $util.oneOfGetter($oneOfFields = ["queryVname"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualIncoming", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualIncoming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_quickRepliesQuery", {
            get: $util.oneOfGetter($oneOfFields = ["quickRepliesQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_payments", {
            get: $util.oneOfGetter($oneOfFields = ["payments"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_stickerPackQuery", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_liveLocationsFinal", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationsFinal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_labelsEdit", {
            get: $util.oneOfGetter($oneOfFields = ["labelsEdit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mediaUpload", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUpload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mediaUploadRichQuickReplies", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUploadRichQuickReplies"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_vnameV2", {
            get: $util.oneOfGetter($oneOfFields = ["vnameV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_videoPlaybackURL", {
            get: $util.oneOfGetter($oneOfFields = ["videoPlaybackURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_statusRanking", {
            get: $util.oneOfGetter($oneOfFields = ["statusRanking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipIndividualVideo", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_thirdPartyStickers", {
            get: $util.oneOfGetter($oneOfFields = ["thirdPartyStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_frequentlyForwardedSetting", {
            get: $util.oneOfGetter($oneOfFields = ["frequentlyForwardedSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupsV4JoinPermission", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV4JoinPermission"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickers", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_catalog", {
            get: $util.oneOfGetter($oneOfFields = ["catalog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_starredStickers", {
            get: $util.oneOfGetter($oneOfFields = ["starredStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_voipGroupCall", {
            get: $util.oneOfGetter($oneOfFields = ["voipGroupCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_templateMessageInteractivity", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessageInteractivity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeralMessages", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessages"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_e2ENotificationSync", {
            get: $util.oneOfGetter($oneOfFields = ["e2ENotificationSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickersV2", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_recentStickersV3", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_userNotice", {
            get: $util.oneOfGetter($oneOfFields = ["userNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_support", {
            get: $util.oneOfGetter($oneOfFields = ["support"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupUiiCleanup", {
            get: $util.oneOfGetter($oneOfFields = ["groupUiiCleanup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_groupDogfoodingInternalOnly", {
            get: $util.oneOfGetter($oneOfFields = ["groupDogfoodingInternalOnly"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_settingsSync", {
            get: $util.oneOfGetter($oneOfFields = ["settingsSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_archiveV2", {
            get: $util.oneOfGetter($oneOfFields = ["archiveV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeralAllowGroupMembers", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralAllowGroupMembers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_ephemeral24HDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeral24HDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_mdForceUpgrade", {
            get: $util.oneOfGetter($oneOfFields = ["mdForceUpgrade"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_externalMdOptInAvailable", {
            get: $util.oneOfGetter($oneOfFields = ["externalMdOptInAvailable"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebFeatures.prototype, "_noDeleteMessageTimeLimit", {
            get: $util.oneOfGetter($oneOfFields = ["noDeleteMessageTimeLimit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebFeatures.create = function create(properties) {
            return new WebFeatures(properties);
        };

        WebFeatures.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.labelsDisplay != null && Object.hasOwnProperty.call(m, "labelsDisplay"))
                w.uint32(8).int32(m.labelsDisplay);
            if (m.voipIndividualOutgoing != null && Object.hasOwnProperty.call(m, "voipIndividualOutgoing"))
                w.uint32(16).int32(m.voipIndividualOutgoing);
            if (m.groupsV3 != null && Object.hasOwnProperty.call(m, "groupsV3"))
                w.uint32(24).int32(m.groupsV3);
            if (m.groupsV3Create != null && Object.hasOwnProperty.call(m, "groupsV3Create"))
                w.uint32(32).int32(m.groupsV3Create);
            if (m.changeNumberV2 != null && Object.hasOwnProperty.call(m, "changeNumberV2"))
                w.uint32(40).int32(m.changeNumberV2);
            if (m.queryStatusV3Thumbnail != null && Object.hasOwnProperty.call(m, "queryStatusV3Thumbnail"))
                w.uint32(48).int32(m.queryStatusV3Thumbnail);
            if (m.liveLocations != null && Object.hasOwnProperty.call(m, "liveLocations"))
                w.uint32(56).int32(m.liveLocations);
            if (m.queryVname != null && Object.hasOwnProperty.call(m, "queryVname"))
                w.uint32(64).int32(m.queryVname);
            if (m.voipIndividualIncoming != null && Object.hasOwnProperty.call(m, "voipIndividualIncoming"))
                w.uint32(72).int32(m.voipIndividualIncoming);
            if (m.quickRepliesQuery != null && Object.hasOwnProperty.call(m, "quickRepliesQuery"))
                w.uint32(80).int32(m.quickRepliesQuery);
            if (m.payments != null && Object.hasOwnProperty.call(m, "payments"))
                w.uint32(88).int32(m.payments);
            if (m.stickerPackQuery != null && Object.hasOwnProperty.call(m, "stickerPackQuery"))
                w.uint32(96).int32(m.stickerPackQuery);
            if (m.liveLocationsFinal != null && Object.hasOwnProperty.call(m, "liveLocationsFinal"))
                w.uint32(104).int32(m.liveLocationsFinal);
            if (m.labelsEdit != null && Object.hasOwnProperty.call(m, "labelsEdit"))
                w.uint32(112).int32(m.labelsEdit);
            if (m.mediaUpload != null && Object.hasOwnProperty.call(m, "mediaUpload"))
                w.uint32(120).int32(m.mediaUpload);
            if (m.mediaUploadRichQuickReplies != null && Object.hasOwnProperty.call(m, "mediaUploadRichQuickReplies"))
                w.uint32(144).int32(m.mediaUploadRichQuickReplies);
            if (m.vnameV2 != null && Object.hasOwnProperty.call(m, "vnameV2"))
                w.uint32(152).int32(m.vnameV2);
            if (m.videoPlaybackURL != null && Object.hasOwnProperty.call(m, "videoPlaybackURL"))
                w.uint32(160).int32(m.videoPlaybackURL);
            if (m.statusRanking != null && Object.hasOwnProperty.call(m, "statusRanking"))
                w.uint32(168).int32(m.statusRanking);
            if (m.voipIndividualVideo != null && Object.hasOwnProperty.call(m, "voipIndividualVideo"))
                w.uint32(176).int32(m.voipIndividualVideo);
            if (m.thirdPartyStickers != null && Object.hasOwnProperty.call(m, "thirdPartyStickers"))
                w.uint32(184).int32(m.thirdPartyStickers);
            if (m.frequentlyForwardedSetting != null && Object.hasOwnProperty.call(m, "frequentlyForwardedSetting"))
                w.uint32(192).int32(m.frequentlyForwardedSetting);
            if (m.groupsV4JoinPermission != null && Object.hasOwnProperty.call(m, "groupsV4JoinPermission"))
                w.uint32(200).int32(m.groupsV4JoinPermission);
            if (m.recentStickers != null && Object.hasOwnProperty.call(m, "recentStickers"))
                w.uint32(208).int32(m.recentStickers);
            if (m.catalog != null && Object.hasOwnProperty.call(m, "catalog"))
                w.uint32(216).int32(m.catalog);
            if (m.starredStickers != null && Object.hasOwnProperty.call(m, "starredStickers"))
                w.uint32(224).int32(m.starredStickers);
            if (m.voipGroupCall != null && Object.hasOwnProperty.call(m, "voipGroupCall"))
                w.uint32(232).int32(m.voipGroupCall);
            if (m.templateMessage != null && Object.hasOwnProperty.call(m, "templateMessage"))
                w.uint32(240).int32(m.templateMessage);
            if (m.templateMessageInteractivity != null && Object.hasOwnProperty.call(m, "templateMessageInteractivity"))
                w.uint32(248).int32(m.templateMessageInteractivity);
            if (m.ephemeralMessages != null && Object.hasOwnProperty.call(m, "ephemeralMessages"))
                w.uint32(256).int32(m.ephemeralMessages);
            if (m.e2ENotificationSync != null && Object.hasOwnProperty.call(m, "e2ENotificationSync"))
                w.uint32(264).int32(m.e2ENotificationSync);
            if (m.recentStickersV2 != null && Object.hasOwnProperty.call(m, "recentStickersV2"))
                w.uint32(272).int32(m.recentStickersV2);
            if (m.recentStickersV3 != null && Object.hasOwnProperty.call(m, "recentStickersV3"))
                w.uint32(288).int32(m.recentStickersV3);
            if (m.userNotice != null && Object.hasOwnProperty.call(m, "userNotice"))
                w.uint32(296).int32(m.userNotice);
            if (m.support != null && Object.hasOwnProperty.call(m, "support"))
                w.uint32(312).int32(m.support);
            if (m.groupUiiCleanup != null && Object.hasOwnProperty.call(m, "groupUiiCleanup"))
                w.uint32(320).int32(m.groupUiiCleanup);
            if (m.groupDogfoodingInternalOnly != null && Object.hasOwnProperty.call(m, "groupDogfoodingInternalOnly"))
                w.uint32(328).int32(m.groupDogfoodingInternalOnly);
            if (m.settingsSync != null && Object.hasOwnProperty.call(m, "settingsSync"))
                w.uint32(336).int32(m.settingsSync);
            if (m.archiveV2 != null && Object.hasOwnProperty.call(m, "archiveV2"))
                w.uint32(344).int32(m.archiveV2);
            if (m.ephemeralAllowGroupMembers != null && Object.hasOwnProperty.call(m, "ephemeralAllowGroupMembers"))
                w.uint32(352).int32(m.ephemeralAllowGroupMembers);
            if (m.ephemeral24HDuration != null && Object.hasOwnProperty.call(m, "ephemeral24HDuration"))
                w.uint32(360).int32(m.ephemeral24HDuration);
            if (m.mdForceUpgrade != null && Object.hasOwnProperty.call(m, "mdForceUpgrade"))
                w.uint32(368).int32(m.mdForceUpgrade);
            if (m.disappearingMode != null && Object.hasOwnProperty.call(m, "disappearingMode"))
                w.uint32(376).int32(m.disappearingMode);
            if (m.externalMdOptInAvailable != null && Object.hasOwnProperty.call(m, "externalMdOptInAvailable"))
                w.uint32(384).int32(m.externalMdOptInAvailable);
            if (m.noDeleteMessageTimeLimit != null && Object.hasOwnProperty.call(m, "noDeleteMessageTimeLimit"))
                w.uint32(392).int32(m.noDeleteMessageTimeLimit);
            return w;
        };

        WebFeatures.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.WebFeatures();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.labelsDisplay = r.int32();
                        break;
                    }
                case 2: {
                        m.voipIndividualOutgoing = r.int32();
                        break;
                    }
                case 3: {
                        m.groupsV3 = r.int32();
                        break;
                    }
                case 4: {
                        m.groupsV3Create = r.int32();
                        break;
                    }
                case 5: {
                        m.changeNumberV2 = r.int32();
                        break;
                    }
                case 6: {
                        m.queryStatusV3Thumbnail = r.int32();
                        break;
                    }
                case 7: {
                        m.liveLocations = r.int32();
                        break;
                    }
                case 8: {
                        m.queryVname = r.int32();
                        break;
                    }
                case 9: {
                        m.voipIndividualIncoming = r.int32();
                        break;
                    }
                case 10: {
                        m.quickRepliesQuery = r.int32();
                        break;
                    }
                case 11: {
                        m.payments = r.int32();
                        break;
                    }
                case 12: {
                        m.stickerPackQuery = r.int32();
                        break;
                    }
                case 13: {
                        m.liveLocationsFinal = r.int32();
                        break;
                    }
                case 14: {
                        m.labelsEdit = r.int32();
                        break;
                    }
                case 15: {
                        m.mediaUpload = r.int32();
                        break;
                    }
                case 18: {
                        m.mediaUploadRichQuickReplies = r.int32();
                        break;
                    }
                case 19: {
                        m.vnameV2 = r.int32();
                        break;
                    }
                case 20: {
                        m.videoPlaybackURL = r.int32();
                        break;
                    }
                case 21: {
                        m.statusRanking = r.int32();
                        break;
                    }
                case 22: {
                        m.voipIndividualVideo = r.int32();
                        break;
                    }
                case 23: {
                        m.thirdPartyStickers = r.int32();
                        break;
                    }
                case 24: {
                        m.frequentlyForwardedSetting = r.int32();
                        break;
                    }
                case 25: {
                        m.groupsV4JoinPermission = r.int32();
                        break;
                    }
                case 26: {
                        m.recentStickers = r.int32();
                        break;
                    }
                case 27: {
                        m.catalog = r.int32();
                        break;
                    }
                case 28: {
                        m.starredStickers = r.int32();
                        break;
                    }
                case 29: {
                        m.voipGroupCall = r.int32();
                        break;
                    }
                case 30: {
                        m.templateMessage = r.int32();
                        break;
                    }
                case 31: {
                        m.templateMessageInteractivity = r.int32();
                        break;
                    }
                case 32: {
                        m.ephemeralMessages = r.int32();
                        break;
                    }
                case 33: {
                        m.e2ENotificationSync = r.int32();
                        break;
                    }
                case 34: {
                        m.recentStickersV2 = r.int32();
                        break;
                    }
                case 36: {
                        m.recentStickersV3 = r.int32();
                        break;
                    }
                case 37: {
                        m.userNotice = r.int32();
                        break;
                    }
                case 39: {
                        m.support = r.int32();
                        break;
                    }
                case 40: {
                        m.groupUiiCleanup = r.int32();
                        break;
                    }
                case 41: {
                        m.groupDogfoodingInternalOnly = r.int32();
                        break;
                    }
                case 42: {
                        m.settingsSync = r.int32();
                        break;
                    }
                case 43: {
                        m.archiveV2 = r.int32();
                        break;
                    }
                case 44: {
                        m.ephemeralAllowGroupMembers = r.int32();
                        break;
                    }
                case 45: {
                        m.ephemeral24HDuration = r.int32();
                        break;
                    }
                case 46: {
                        m.mdForceUpgrade = r.int32();
                        break;
                    }
                case 47: {
                        m.disappearingMode = r.int32();
                        break;
                    }
                case 48: {
                        m.externalMdOptInAvailable = r.int32();
                        break;
                    }
                case 49: {
                        m.noDeleteMessageTimeLimit = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        WebFeatures.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.WebFeatures)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.WebFeatures();
            switch (d.labelsDisplay) {
            default:
                if (typeof d.labelsDisplay === "number") {
                    m.labelsDisplay = d.labelsDisplay;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.labelsDisplay = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.labelsDisplay = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.labelsDisplay = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.labelsDisplay = 3;
                break;
            }
            switch (d.voipIndividualOutgoing) {
            default:
                if (typeof d.voipIndividualOutgoing === "number") {
                    m.voipIndividualOutgoing = d.voipIndividualOutgoing;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.voipIndividualOutgoing = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualOutgoing = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualOutgoing = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualOutgoing = 3;
                break;
            }
            switch (d.groupsV3) {
            default:
                if (typeof d.groupsV3 === "number") {
                    m.groupsV3 = d.groupsV3;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.groupsV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV3 = 3;
                break;
            }
            switch (d.groupsV3Create) {
            default:
                if (typeof d.groupsV3Create === "number") {
                    m.groupsV3Create = d.groupsV3Create;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.groupsV3Create = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV3Create = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV3Create = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV3Create = 3;
                break;
            }
            switch (d.changeNumberV2) {
            default:
                if (typeof d.changeNumberV2 === "number") {
                    m.changeNumberV2 = d.changeNumberV2;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.changeNumberV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.changeNumberV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.changeNumberV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.changeNumberV2 = 3;
                break;
            }
            switch (d.queryStatusV3Thumbnail) {
            default:
                if (typeof d.queryStatusV3Thumbnail === "number") {
                    m.queryStatusV3Thumbnail = d.queryStatusV3Thumbnail;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.queryStatusV3Thumbnail = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.queryStatusV3Thumbnail = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.queryStatusV3Thumbnail = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.queryStatusV3Thumbnail = 3;
                break;
            }
            switch (d.liveLocations) {
            default:
                if (typeof d.liveLocations === "number") {
                    m.liveLocations = d.liveLocations;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.liveLocations = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.liveLocations = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.liveLocations = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.liveLocations = 3;
                break;
            }
            switch (d.queryVname) {
            default:
                if (typeof d.queryVname === "number") {
                    m.queryVname = d.queryVname;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.queryVname = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.queryVname = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.queryVname = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.queryVname = 3;
                break;
            }
            switch (d.voipIndividualIncoming) {
            default:
                if (typeof d.voipIndividualIncoming === "number") {
                    m.voipIndividualIncoming = d.voipIndividualIncoming;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.voipIndividualIncoming = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualIncoming = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualIncoming = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualIncoming = 3;
                break;
            }
            switch (d.quickRepliesQuery) {
            default:
                if (typeof d.quickRepliesQuery === "number") {
                    m.quickRepliesQuery = d.quickRepliesQuery;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.quickRepliesQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.quickRepliesQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.quickRepliesQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.quickRepliesQuery = 3;
                break;
            }
            switch (d.payments) {
            default:
                if (typeof d.payments === "number") {
                    m.payments = d.payments;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.payments = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.payments = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.payments = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.payments = 3;
                break;
            }
            switch (d.stickerPackQuery) {
            default:
                if (typeof d.stickerPackQuery === "number") {
                    m.stickerPackQuery = d.stickerPackQuery;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.stickerPackQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.stickerPackQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.stickerPackQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.stickerPackQuery = 3;
                break;
            }
            switch (d.liveLocationsFinal) {
            default:
                if (typeof d.liveLocationsFinal === "number") {
                    m.liveLocationsFinal = d.liveLocationsFinal;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.liveLocationsFinal = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.liveLocationsFinal = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.liveLocationsFinal = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.liveLocationsFinal = 3;
                break;
            }
            switch (d.labelsEdit) {
            default:
                if (typeof d.labelsEdit === "number") {
                    m.labelsEdit = d.labelsEdit;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.labelsEdit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.labelsEdit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.labelsEdit = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.labelsEdit = 3;
                break;
            }
            switch (d.mediaUpload) {
            default:
                if (typeof d.mediaUpload === "number") {
                    m.mediaUpload = d.mediaUpload;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.mediaUpload = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mediaUpload = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mediaUpload = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mediaUpload = 3;
                break;
            }
            switch (d.mediaUploadRichQuickReplies) {
            default:
                if (typeof d.mediaUploadRichQuickReplies === "number") {
                    m.mediaUploadRichQuickReplies = d.mediaUploadRichQuickReplies;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.mediaUploadRichQuickReplies = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mediaUploadRichQuickReplies = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mediaUploadRichQuickReplies = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mediaUploadRichQuickReplies = 3;
                break;
            }
            switch (d.vnameV2) {
            default:
                if (typeof d.vnameV2 === "number") {
                    m.vnameV2 = d.vnameV2;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.vnameV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.vnameV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.vnameV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.vnameV2 = 3;
                break;
            }
            switch (d.videoPlaybackURL) {
            default:
                if (typeof d.videoPlaybackURL === "number") {
                    m.videoPlaybackURL = d.videoPlaybackURL;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.videoPlaybackURL = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.videoPlaybackURL = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.videoPlaybackURL = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.videoPlaybackURL = 3;
                break;
            }
            switch (d.statusRanking) {
            default:
                if (typeof d.statusRanking === "number") {
                    m.statusRanking = d.statusRanking;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.statusRanking = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.statusRanking = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.statusRanking = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.statusRanking = 3;
                break;
            }
            switch (d.voipIndividualVideo) {
            default:
                if (typeof d.voipIndividualVideo === "number") {
                    m.voipIndividualVideo = d.voipIndividualVideo;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.voipIndividualVideo = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualVideo = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualVideo = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualVideo = 3;
                break;
            }
            switch (d.thirdPartyStickers) {
            default:
                if (typeof d.thirdPartyStickers === "number") {
                    m.thirdPartyStickers = d.thirdPartyStickers;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.thirdPartyStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.thirdPartyStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.thirdPartyStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.thirdPartyStickers = 3;
                break;
            }
            switch (d.frequentlyForwardedSetting) {
            default:
                if (typeof d.frequentlyForwardedSetting === "number") {
                    m.frequentlyForwardedSetting = d.frequentlyForwardedSetting;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.frequentlyForwardedSetting = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.frequentlyForwardedSetting = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.frequentlyForwardedSetting = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.frequentlyForwardedSetting = 3;
                break;
            }
            switch (d.groupsV4JoinPermission) {
            default:
                if (typeof d.groupsV4JoinPermission === "number") {
                    m.groupsV4JoinPermission = d.groupsV4JoinPermission;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.groupsV4JoinPermission = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV4JoinPermission = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV4JoinPermission = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV4JoinPermission = 3;
                break;
            }
            switch (d.recentStickers) {
            default:
                if (typeof d.recentStickers === "number") {
                    m.recentStickers = d.recentStickers;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.recentStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickers = 3;
                break;
            }
            switch (d.catalog) {
            default:
                if (typeof d.catalog === "number") {
                    m.catalog = d.catalog;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.catalog = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.catalog = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.catalog = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.catalog = 3;
                break;
            }
            switch (d.starredStickers) {
            default:
                if (typeof d.starredStickers === "number") {
                    m.starredStickers = d.starredStickers;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.starredStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.starredStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.starredStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.starredStickers = 3;
                break;
            }
            switch (d.voipGroupCall) {
            default:
                if (typeof d.voipGroupCall === "number") {
                    m.voipGroupCall = d.voipGroupCall;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.voipGroupCall = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipGroupCall = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipGroupCall = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipGroupCall = 3;
                break;
            }
            switch (d.templateMessage) {
            default:
                if (typeof d.templateMessage === "number") {
                    m.templateMessage = d.templateMessage;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.templateMessage = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.templateMessage = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.templateMessage = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.templateMessage = 3;
                break;
            }
            switch (d.templateMessageInteractivity) {
            default:
                if (typeof d.templateMessageInteractivity === "number") {
                    m.templateMessageInteractivity = d.templateMessageInteractivity;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.templateMessageInteractivity = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.templateMessageInteractivity = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.templateMessageInteractivity = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.templateMessageInteractivity = 3;
                break;
            }
            switch (d.ephemeralMessages) {
            default:
                if (typeof d.ephemeralMessages === "number") {
                    m.ephemeralMessages = d.ephemeralMessages;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.ephemeralMessages = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeralMessages = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeralMessages = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeralMessages = 3;
                break;
            }
            switch (d.e2ENotificationSync) {
            default:
                if (typeof d.e2ENotificationSync === "number") {
                    m.e2ENotificationSync = d.e2ENotificationSync;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.e2ENotificationSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.e2ENotificationSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.e2ENotificationSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.e2ENotificationSync = 3;
                break;
            }
            switch (d.recentStickersV2) {
            default:
                if (typeof d.recentStickersV2 === "number") {
                    m.recentStickersV2 = d.recentStickersV2;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.recentStickersV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickersV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickersV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickersV2 = 3;
                break;
            }
            switch (d.recentStickersV3) {
            default:
                if (typeof d.recentStickersV3 === "number") {
                    m.recentStickersV3 = d.recentStickersV3;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.recentStickersV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickersV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickersV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickersV3 = 3;
                break;
            }
            switch (d.userNotice) {
            default:
                if (typeof d.userNotice === "number") {
                    m.userNotice = d.userNotice;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.userNotice = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.userNotice = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.userNotice = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.userNotice = 3;
                break;
            }
            switch (d.support) {
            default:
                if (typeof d.support === "number") {
                    m.support = d.support;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.support = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.support = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.support = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.support = 3;
                break;
            }
            switch (d.groupUiiCleanup) {
            default:
                if (typeof d.groupUiiCleanup === "number") {
                    m.groupUiiCleanup = d.groupUiiCleanup;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.groupUiiCleanup = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupUiiCleanup = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupUiiCleanup = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupUiiCleanup = 3;
                break;
            }
            switch (d.groupDogfoodingInternalOnly) {
            default:
                if (typeof d.groupDogfoodingInternalOnly === "number") {
                    m.groupDogfoodingInternalOnly = d.groupDogfoodingInternalOnly;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.groupDogfoodingInternalOnly = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupDogfoodingInternalOnly = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupDogfoodingInternalOnly = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupDogfoodingInternalOnly = 3;
                break;
            }
            switch (d.settingsSync) {
            default:
                if (typeof d.settingsSync === "number") {
                    m.settingsSync = d.settingsSync;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.settingsSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.settingsSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.settingsSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.settingsSync = 3;
                break;
            }
            switch (d.archiveV2) {
            default:
                if (typeof d.archiveV2 === "number") {
                    m.archiveV2 = d.archiveV2;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.archiveV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.archiveV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.archiveV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.archiveV2 = 3;
                break;
            }
            switch (d.ephemeralAllowGroupMembers) {
            default:
                if (typeof d.ephemeralAllowGroupMembers === "number") {
                    m.ephemeralAllowGroupMembers = d.ephemeralAllowGroupMembers;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.ephemeralAllowGroupMembers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeralAllowGroupMembers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeralAllowGroupMembers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeralAllowGroupMembers = 3;
                break;
            }
            switch (d.ephemeral24HDuration) {
            default:
                if (typeof d.ephemeral24HDuration === "number") {
                    m.ephemeral24HDuration = d.ephemeral24HDuration;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.ephemeral24HDuration = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeral24HDuration = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeral24HDuration = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeral24HDuration = 3;
                break;
            }
            switch (d.mdForceUpgrade) {
            default:
                if (typeof d.mdForceUpgrade === "number") {
                    m.mdForceUpgrade = d.mdForceUpgrade;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.mdForceUpgrade = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mdForceUpgrade = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mdForceUpgrade = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mdForceUpgrade = 3;
                break;
            }
            switch (d.disappearingMode) {
            default:
                if (typeof d.disappearingMode === "number") {
                    m.disappearingMode = d.disappearingMode;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.disappearingMode = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.disappearingMode = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.disappearingMode = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.disappearingMode = 3;
                break;
            }
            switch (d.externalMdOptInAvailable) {
            default:
                if (typeof d.externalMdOptInAvailable === "number") {
                    m.externalMdOptInAvailable = d.externalMdOptInAvailable;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.externalMdOptInAvailable = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.externalMdOptInAvailable = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.externalMdOptInAvailable = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.externalMdOptInAvailable = 3;
                break;
            }
            switch (d.noDeleteMessageTimeLimit) {
            default:
                if (typeof d.noDeleteMessageTimeLimit === "number") {
                    m.noDeleteMessageTimeLimit = d.noDeleteMessageTimeLimit;
                    break;
                }
                break;
            case "NOT_STARTED":
            case 0:
                m.noDeleteMessageTimeLimit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.noDeleteMessageTimeLimit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.noDeleteMessageTimeLimit = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.noDeleteMessageTimeLimit = 3;
                break;
            }
            return m;
        };

        WebFeatures.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.labelsDisplay != null && m.hasOwnProperty("labelsDisplay")) {
                d.labelsDisplay = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.labelsDisplay] === undefined ? m.labelsDisplay : $root.WAWeb.WebFeatures.Flag[m.labelsDisplay] : m.labelsDisplay;
                if (o.oneofs)
                    d._labelsDisplay = "labelsDisplay";
            }
            if (m.voipIndividualOutgoing != null && m.hasOwnProperty("voipIndividualOutgoing")) {
                d.voipIndividualOutgoing = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.voipIndividualOutgoing] === undefined ? m.voipIndividualOutgoing : $root.WAWeb.WebFeatures.Flag[m.voipIndividualOutgoing] : m.voipIndividualOutgoing;
                if (o.oneofs)
                    d._voipIndividualOutgoing = "voipIndividualOutgoing";
            }
            if (m.groupsV3 != null && m.hasOwnProperty("groupsV3")) {
                d.groupsV3 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.groupsV3] === undefined ? m.groupsV3 : $root.WAWeb.WebFeatures.Flag[m.groupsV3] : m.groupsV3;
                if (o.oneofs)
                    d._groupsV3 = "groupsV3";
            }
            if (m.groupsV3Create != null && m.hasOwnProperty("groupsV3Create")) {
                d.groupsV3Create = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.groupsV3Create] === undefined ? m.groupsV3Create : $root.WAWeb.WebFeatures.Flag[m.groupsV3Create] : m.groupsV3Create;
                if (o.oneofs)
                    d._groupsV3Create = "groupsV3Create";
            }
            if (m.changeNumberV2 != null && m.hasOwnProperty("changeNumberV2")) {
                d.changeNumberV2 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.changeNumberV2] === undefined ? m.changeNumberV2 : $root.WAWeb.WebFeatures.Flag[m.changeNumberV2] : m.changeNumberV2;
                if (o.oneofs)
                    d._changeNumberV2 = "changeNumberV2";
            }
            if (m.queryStatusV3Thumbnail != null && m.hasOwnProperty("queryStatusV3Thumbnail")) {
                d.queryStatusV3Thumbnail = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.queryStatusV3Thumbnail] === undefined ? m.queryStatusV3Thumbnail : $root.WAWeb.WebFeatures.Flag[m.queryStatusV3Thumbnail] : m.queryStatusV3Thumbnail;
                if (o.oneofs)
                    d._queryStatusV3Thumbnail = "queryStatusV3Thumbnail";
            }
            if (m.liveLocations != null && m.hasOwnProperty("liveLocations")) {
                d.liveLocations = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.liveLocations] === undefined ? m.liveLocations : $root.WAWeb.WebFeatures.Flag[m.liveLocations] : m.liveLocations;
                if (o.oneofs)
                    d._liveLocations = "liveLocations";
            }
            if (m.queryVname != null && m.hasOwnProperty("queryVname")) {
                d.queryVname = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.queryVname] === undefined ? m.queryVname : $root.WAWeb.WebFeatures.Flag[m.queryVname] : m.queryVname;
                if (o.oneofs)
                    d._queryVname = "queryVname";
            }
            if (m.voipIndividualIncoming != null && m.hasOwnProperty("voipIndividualIncoming")) {
                d.voipIndividualIncoming = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.voipIndividualIncoming] === undefined ? m.voipIndividualIncoming : $root.WAWeb.WebFeatures.Flag[m.voipIndividualIncoming] : m.voipIndividualIncoming;
                if (o.oneofs)
                    d._voipIndividualIncoming = "voipIndividualIncoming";
            }
            if (m.quickRepliesQuery != null && m.hasOwnProperty("quickRepliesQuery")) {
                d.quickRepliesQuery = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.quickRepliesQuery] === undefined ? m.quickRepliesQuery : $root.WAWeb.WebFeatures.Flag[m.quickRepliesQuery] : m.quickRepliesQuery;
                if (o.oneofs)
                    d._quickRepliesQuery = "quickRepliesQuery";
            }
            if (m.payments != null && m.hasOwnProperty("payments")) {
                d.payments = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.payments] === undefined ? m.payments : $root.WAWeb.WebFeatures.Flag[m.payments] : m.payments;
                if (o.oneofs)
                    d._payments = "payments";
            }
            if (m.stickerPackQuery != null && m.hasOwnProperty("stickerPackQuery")) {
                d.stickerPackQuery = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.stickerPackQuery] === undefined ? m.stickerPackQuery : $root.WAWeb.WebFeatures.Flag[m.stickerPackQuery] : m.stickerPackQuery;
                if (o.oneofs)
                    d._stickerPackQuery = "stickerPackQuery";
            }
            if (m.liveLocationsFinal != null && m.hasOwnProperty("liveLocationsFinal")) {
                d.liveLocationsFinal = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.liveLocationsFinal] === undefined ? m.liveLocationsFinal : $root.WAWeb.WebFeatures.Flag[m.liveLocationsFinal] : m.liveLocationsFinal;
                if (o.oneofs)
                    d._liveLocationsFinal = "liveLocationsFinal";
            }
            if (m.labelsEdit != null && m.hasOwnProperty("labelsEdit")) {
                d.labelsEdit = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.labelsEdit] === undefined ? m.labelsEdit : $root.WAWeb.WebFeatures.Flag[m.labelsEdit] : m.labelsEdit;
                if (o.oneofs)
                    d._labelsEdit = "labelsEdit";
            }
            if (m.mediaUpload != null && m.hasOwnProperty("mediaUpload")) {
                d.mediaUpload = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.mediaUpload] === undefined ? m.mediaUpload : $root.WAWeb.WebFeatures.Flag[m.mediaUpload] : m.mediaUpload;
                if (o.oneofs)
                    d._mediaUpload = "mediaUpload";
            }
            if (m.mediaUploadRichQuickReplies != null && m.hasOwnProperty("mediaUploadRichQuickReplies")) {
                d.mediaUploadRichQuickReplies = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.mediaUploadRichQuickReplies] === undefined ? m.mediaUploadRichQuickReplies : $root.WAWeb.WebFeatures.Flag[m.mediaUploadRichQuickReplies] : m.mediaUploadRichQuickReplies;
                if (o.oneofs)
                    d._mediaUploadRichQuickReplies = "mediaUploadRichQuickReplies";
            }
            if (m.vnameV2 != null && m.hasOwnProperty("vnameV2")) {
                d.vnameV2 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.vnameV2] === undefined ? m.vnameV2 : $root.WAWeb.WebFeatures.Flag[m.vnameV2] : m.vnameV2;
                if (o.oneofs)
                    d._vnameV2 = "vnameV2";
            }
            if (m.videoPlaybackURL != null && m.hasOwnProperty("videoPlaybackURL")) {
                d.videoPlaybackURL = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.videoPlaybackURL] === undefined ? m.videoPlaybackURL : $root.WAWeb.WebFeatures.Flag[m.videoPlaybackURL] : m.videoPlaybackURL;
                if (o.oneofs)
                    d._videoPlaybackURL = "videoPlaybackURL";
            }
            if (m.statusRanking != null && m.hasOwnProperty("statusRanking")) {
                d.statusRanking = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.statusRanking] === undefined ? m.statusRanking : $root.WAWeb.WebFeatures.Flag[m.statusRanking] : m.statusRanking;
                if (o.oneofs)
                    d._statusRanking = "statusRanking";
            }
            if (m.voipIndividualVideo != null && m.hasOwnProperty("voipIndividualVideo")) {
                d.voipIndividualVideo = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.voipIndividualVideo] === undefined ? m.voipIndividualVideo : $root.WAWeb.WebFeatures.Flag[m.voipIndividualVideo] : m.voipIndividualVideo;
                if (o.oneofs)
                    d._voipIndividualVideo = "voipIndividualVideo";
            }
            if (m.thirdPartyStickers != null && m.hasOwnProperty("thirdPartyStickers")) {
                d.thirdPartyStickers = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.thirdPartyStickers] === undefined ? m.thirdPartyStickers : $root.WAWeb.WebFeatures.Flag[m.thirdPartyStickers] : m.thirdPartyStickers;
                if (o.oneofs)
                    d._thirdPartyStickers = "thirdPartyStickers";
            }
            if (m.frequentlyForwardedSetting != null && m.hasOwnProperty("frequentlyForwardedSetting")) {
                d.frequentlyForwardedSetting = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.frequentlyForwardedSetting] === undefined ? m.frequentlyForwardedSetting : $root.WAWeb.WebFeatures.Flag[m.frequentlyForwardedSetting] : m.frequentlyForwardedSetting;
                if (o.oneofs)
                    d._frequentlyForwardedSetting = "frequentlyForwardedSetting";
            }
            if (m.groupsV4JoinPermission != null && m.hasOwnProperty("groupsV4JoinPermission")) {
                d.groupsV4JoinPermission = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.groupsV4JoinPermission] === undefined ? m.groupsV4JoinPermission : $root.WAWeb.WebFeatures.Flag[m.groupsV4JoinPermission] : m.groupsV4JoinPermission;
                if (o.oneofs)
                    d._groupsV4JoinPermission = "groupsV4JoinPermission";
            }
            if (m.recentStickers != null && m.hasOwnProperty("recentStickers")) {
                d.recentStickers = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.recentStickers] === undefined ? m.recentStickers : $root.WAWeb.WebFeatures.Flag[m.recentStickers] : m.recentStickers;
                if (o.oneofs)
                    d._recentStickers = "recentStickers";
            }
            if (m.catalog != null && m.hasOwnProperty("catalog")) {
                d.catalog = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.catalog] === undefined ? m.catalog : $root.WAWeb.WebFeatures.Flag[m.catalog] : m.catalog;
                if (o.oneofs)
                    d._catalog = "catalog";
            }
            if (m.starredStickers != null && m.hasOwnProperty("starredStickers")) {
                d.starredStickers = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.starredStickers] === undefined ? m.starredStickers : $root.WAWeb.WebFeatures.Flag[m.starredStickers] : m.starredStickers;
                if (o.oneofs)
                    d._starredStickers = "starredStickers";
            }
            if (m.voipGroupCall != null && m.hasOwnProperty("voipGroupCall")) {
                d.voipGroupCall = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.voipGroupCall] === undefined ? m.voipGroupCall : $root.WAWeb.WebFeatures.Flag[m.voipGroupCall] : m.voipGroupCall;
                if (o.oneofs)
                    d._voipGroupCall = "voipGroupCall";
            }
            if (m.templateMessage != null && m.hasOwnProperty("templateMessage")) {
                d.templateMessage = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.templateMessage] === undefined ? m.templateMessage : $root.WAWeb.WebFeatures.Flag[m.templateMessage] : m.templateMessage;
                if (o.oneofs)
                    d._templateMessage = "templateMessage";
            }
            if (m.templateMessageInteractivity != null && m.hasOwnProperty("templateMessageInteractivity")) {
                d.templateMessageInteractivity = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.templateMessageInteractivity] === undefined ? m.templateMessageInteractivity : $root.WAWeb.WebFeatures.Flag[m.templateMessageInteractivity] : m.templateMessageInteractivity;
                if (o.oneofs)
                    d._templateMessageInteractivity = "templateMessageInteractivity";
            }
            if (m.ephemeralMessages != null && m.hasOwnProperty("ephemeralMessages")) {
                d.ephemeralMessages = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.ephemeralMessages] === undefined ? m.ephemeralMessages : $root.WAWeb.WebFeatures.Flag[m.ephemeralMessages] : m.ephemeralMessages;
                if (o.oneofs)
                    d._ephemeralMessages = "ephemeralMessages";
            }
            if (m.e2ENotificationSync != null && m.hasOwnProperty("e2ENotificationSync")) {
                d.e2ENotificationSync = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.e2ENotificationSync] === undefined ? m.e2ENotificationSync : $root.WAWeb.WebFeatures.Flag[m.e2ENotificationSync] : m.e2ENotificationSync;
                if (o.oneofs)
                    d._e2ENotificationSync = "e2ENotificationSync";
            }
            if (m.recentStickersV2 != null && m.hasOwnProperty("recentStickersV2")) {
                d.recentStickersV2 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.recentStickersV2] === undefined ? m.recentStickersV2 : $root.WAWeb.WebFeatures.Flag[m.recentStickersV2] : m.recentStickersV2;
                if (o.oneofs)
                    d._recentStickersV2 = "recentStickersV2";
            }
            if (m.recentStickersV3 != null && m.hasOwnProperty("recentStickersV3")) {
                d.recentStickersV3 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.recentStickersV3] === undefined ? m.recentStickersV3 : $root.WAWeb.WebFeatures.Flag[m.recentStickersV3] : m.recentStickersV3;
                if (o.oneofs)
                    d._recentStickersV3 = "recentStickersV3";
            }
            if (m.userNotice != null && m.hasOwnProperty("userNotice")) {
                d.userNotice = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.userNotice] === undefined ? m.userNotice : $root.WAWeb.WebFeatures.Flag[m.userNotice] : m.userNotice;
                if (o.oneofs)
                    d._userNotice = "userNotice";
            }
            if (m.support != null && m.hasOwnProperty("support")) {
                d.support = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.support] === undefined ? m.support : $root.WAWeb.WebFeatures.Flag[m.support] : m.support;
                if (o.oneofs)
                    d._support = "support";
            }
            if (m.groupUiiCleanup != null && m.hasOwnProperty("groupUiiCleanup")) {
                d.groupUiiCleanup = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.groupUiiCleanup] === undefined ? m.groupUiiCleanup : $root.WAWeb.WebFeatures.Flag[m.groupUiiCleanup] : m.groupUiiCleanup;
                if (o.oneofs)
                    d._groupUiiCleanup = "groupUiiCleanup";
            }
            if (m.groupDogfoodingInternalOnly != null && m.hasOwnProperty("groupDogfoodingInternalOnly")) {
                d.groupDogfoodingInternalOnly = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.groupDogfoodingInternalOnly] === undefined ? m.groupDogfoodingInternalOnly : $root.WAWeb.WebFeatures.Flag[m.groupDogfoodingInternalOnly] : m.groupDogfoodingInternalOnly;
                if (o.oneofs)
                    d._groupDogfoodingInternalOnly = "groupDogfoodingInternalOnly";
            }
            if (m.settingsSync != null && m.hasOwnProperty("settingsSync")) {
                d.settingsSync = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.settingsSync] === undefined ? m.settingsSync : $root.WAWeb.WebFeatures.Flag[m.settingsSync] : m.settingsSync;
                if (o.oneofs)
                    d._settingsSync = "settingsSync";
            }
            if (m.archiveV2 != null && m.hasOwnProperty("archiveV2")) {
                d.archiveV2 = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.archiveV2] === undefined ? m.archiveV2 : $root.WAWeb.WebFeatures.Flag[m.archiveV2] : m.archiveV2;
                if (o.oneofs)
                    d._archiveV2 = "archiveV2";
            }
            if (m.ephemeralAllowGroupMembers != null && m.hasOwnProperty("ephemeralAllowGroupMembers")) {
                d.ephemeralAllowGroupMembers = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.ephemeralAllowGroupMembers] === undefined ? m.ephemeralAllowGroupMembers : $root.WAWeb.WebFeatures.Flag[m.ephemeralAllowGroupMembers] : m.ephemeralAllowGroupMembers;
                if (o.oneofs)
                    d._ephemeralAllowGroupMembers = "ephemeralAllowGroupMembers";
            }
            if (m.ephemeral24HDuration != null && m.hasOwnProperty("ephemeral24HDuration")) {
                d.ephemeral24HDuration = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.ephemeral24HDuration] === undefined ? m.ephemeral24HDuration : $root.WAWeb.WebFeatures.Flag[m.ephemeral24HDuration] : m.ephemeral24HDuration;
                if (o.oneofs)
                    d._ephemeral24HDuration = "ephemeral24HDuration";
            }
            if (m.mdForceUpgrade != null && m.hasOwnProperty("mdForceUpgrade")) {
                d.mdForceUpgrade = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.mdForceUpgrade] === undefined ? m.mdForceUpgrade : $root.WAWeb.WebFeatures.Flag[m.mdForceUpgrade] : m.mdForceUpgrade;
                if (o.oneofs)
                    d._mdForceUpgrade = "mdForceUpgrade";
            }
            if (m.disappearingMode != null && m.hasOwnProperty("disappearingMode")) {
                d.disappearingMode = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.disappearingMode] === undefined ? m.disappearingMode : $root.WAWeb.WebFeatures.Flag[m.disappearingMode] : m.disappearingMode;
                if (o.oneofs)
                    d._disappearingMode = "disappearingMode";
            }
            if (m.externalMdOptInAvailable != null && m.hasOwnProperty("externalMdOptInAvailable")) {
                d.externalMdOptInAvailable = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.externalMdOptInAvailable] === undefined ? m.externalMdOptInAvailable : $root.WAWeb.WebFeatures.Flag[m.externalMdOptInAvailable] : m.externalMdOptInAvailable;
                if (o.oneofs)
                    d._externalMdOptInAvailable = "externalMdOptInAvailable";
            }
            if (m.noDeleteMessageTimeLimit != null && m.hasOwnProperty("noDeleteMessageTimeLimit")) {
                d.noDeleteMessageTimeLimit = o.enums === String ? $root.WAWeb.WebFeatures.Flag[m.noDeleteMessageTimeLimit] === undefined ? m.noDeleteMessageTimeLimit : $root.WAWeb.WebFeatures.Flag[m.noDeleteMessageTimeLimit] : m.noDeleteMessageTimeLimit;
                if (o.oneofs)
                    d._noDeleteMessageTimeLimit = "noDeleteMessageTimeLimit";
            }
            return d;
        };

        WebFeatures.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebFeatures.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.WebFeatures";
        };

        WebFeatures.Flag = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_STARTED"] = 0;
            values[valuesById[1] = "FORCE_UPGRADE"] = 1;
            values[valuesById[2] = "DEVELOPMENT"] = 2;
            values[valuesById[3] = "PRODUCTION"] = 3;
            return values;
        })();

        return WebFeatures;
    })();

    WAWeb.PinInChat = (function() {

        function PinInChat(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PinInChat.prototype.type = null;
        PinInChat.prototype.key = null;
        PinInChat.prototype.senderTimestampMS = null;
        PinInChat.prototype.serverTimestampMS = null;
        PinInChat.prototype.messageAddOnContextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_serverTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChat.prototype, "_messageAddOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PinInChat.create = function create(properties) {
            return new PinInChat(properties);
        };

        PinInChat.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(18).fork()).ldelim();
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(24).int64(m.senderTimestampMS);
            if (m.serverTimestampMS != null && Object.hasOwnProperty.call(m, "serverTimestampMS"))
                w.uint32(32).int64(m.serverTimestampMS);
            if (m.messageAddOnContextInfo != null && Object.hasOwnProperty.call(m, "messageAddOnContextInfo"))
                $root.WAWeb.MessageAddOnContextInfo.encode(m.messageAddOnContextInfo, w.uint32(42).fork()).ldelim();
            return w;
        };

        PinInChat.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PinInChat();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                case 2: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                case 4: {
                        m.serverTimestampMS = r.int64();
                        break;
                    }
                case 5: {
                        m.messageAddOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PinInChat.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PinInChat)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PinInChat();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                m.type = 0;
                break;
            case "PIN_FOR_ALL":
            case 1:
                m.type = 1;
                break;
            case "UNPIN_FOR_ALL":
            case 2:
                m.type = 2;
                break;
            }
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAWeb.PinInChat.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            if (d.serverTimestampMS != null) {
                if ($util.Long)
                    (m.serverTimestampMS = $util.Long.fromValue(d.serverTimestampMS)).unsigned = false;
                else if (typeof d.serverTimestampMS === "string")
                    m.serverTimestampMS = parseInt(d.serverTimestampMS, 10);
                else if (typeof d.serverTimestampMS === "number")
                    m.serverTimestampMS = d.serverTimestampMS;
                else if (typeof d.serverTimestampMS === "object")
                    m.serverTimestampMS = new $util.LongBits(d.serverTimestampMS.low >>> 0, d.serverTimestampMS.high >>> 0).toNumber();
            }
            if (d.messageAddOnContextInfo != null) {
                if (typeof d.messageAddOnContextInfo !== "object")
                    throw TypeError(".WAWeb.PinInChat.messageAddOnContextInfo: object expected");
                m.messageAddOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.fromObject(d.messageAddOnContextInfo, n + 1);
            }
            return m;
        };

        PinInChat.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? $root.WAWeb.PinInChat.Type[m.type] === undefined ? m.type : $root.WAWeb.PinInChat.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            if (m.serverTimestampMS != null && m.hasOwnProperty("serverTimestampMS")) {
                if (typeof m.serverTimestampMS === "number")
                    d.serverTimestampMS = o.longs === String ? String(m.serverTimestampMS) : m.serverTimestampMS;
                else
                    d.serverTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.serverTimestampMS) : o.longs === Number ? new $util.LongBits(m.serverTimestampMS.low >>> 0, m.serverTimestampMS.high >>> 0).toNumber() : m.serverTimestampMS;
                if (o.oneofs)
                    d._serverTimestampMS = "serverTimestampMS";
            }
            if (m.messageAddOnContextInfo != null && m.hasOwnProperty("messageAddOnContextInfo")) {
                d.messageAddOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.toObject(m.messageAddOnContextInfo, o);
                if (o.oneofs)
                    d._messageAddOnContextInfo = "messageAddOnContextInfo";
            }
            return d;
        };

        PinInChat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PinInChat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PinInChat";
        };

        PinInChat.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "PIN_FOR_ALL"] = 1;
            values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
            return values;
        })();

        return PinInChat;
    })();

    WAWeb.MessageAddOn = (function() {

        function MessageAddOn(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageAddOn.prototype.messageAddOnType = null;
        MessageAddOn.prototype.messageAddOn = null;
        MessageAddOn.prototype.senderTimestampMS = null;
        MessageAddOn.prototype.serverTimestampMS = null;
        MessageAddOn.prototype.status = null;
        MessageAddOn.prototype.addOnContextInfo = null;
        MessageAddOn.prototype.messageAddOnKey = null;
        MessageAddOn.prototype.legacyMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOnType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOn", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_serverTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_addOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["addOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_messageAddOnKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOn.prototype, "_legacyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["legacyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAddOn.create = function create(properties) {
            return new MessageAddOn(properties);
        };

        MessageAddOn.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageAddOnType != null && Object.hasOwnProperty.call(m, "messageAddOnType"))
                w.uint32(8).int32(m.messageAddOnType);
            if (m.messageAddOn != null && Object.hasOwnProperty.call(m, "messageAddOn"))
                WAE2E.Message.encode(m.messageAddOn, w.uint32(18).fork()).ldelim();
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(24).int64(m.senderTimestampMS);
            if (m.serverTimestampMS != null && Object.hasOwnProperty.call(m, "serverTimestampMS"))
                w.uint32(32).int64(m.serverTimestampMS);
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(40).int32(m.status);
            if (m.addOnContextInfo != null && Object.hasOwnProperty.call(m, "addOnContextInfo"))
                $root.WAWeb.MessageAddOnContextInfo.encode(m.addOnContextInfo, w.uint32(50).fork()).ldelim();
            if (m.messageAddOnKey != null && Object.hasOwnProperty.call(m, "messageAddOnKey"))
                WACommon.MessageKey.encode(m.messageAddOnKey, w.uint32(58).fork()).ldelim();
            if (m.legacyMessage != null && Object.hasOwnProperty.call(m, "legacyMessage"))
                $root.WAWeb.LegacyMessage.encode(m.legacyMessage, w.uint32(66).fork()).ldelim();
            return w;
        };

        MessageAddOn.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.MessageAddOn();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageAddOnType = r.int32();
                        break;
                    }
                case 2: {
                        m.messageAddOn = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                case 4: {
                        m.serverTimestampMS = r.int64();
                        break;
                    }
                case 5: {
                        m.status = r.int32();
                        break;
                    }
                case 6: {
                        m.addOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.messageAddOnKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.legacyMessage = $root.WAWeb.LegacyMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageAddOn.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.MessageAddOn)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.MessageAddOn();
            switch (d.messageAddOnType) {
            default:
                if (typeof d.messageAddOnType === "number") {
                    m.messageAddOnType = d.messageAddOnType;
                    break;
                }
                break;
            case "UNDEFINED":
            case 0:
                m.messageAddOnType = 0;
                break;
            case "REACTION":
            case 1:
                m.messageAddOnType = 1;
                break;
            case "EVENT_RESPONSE":
            case 2:
                m.messageAddOnType = 2;
                break;
            case "POLL_UPDATE":
            case 3:
                m.messageAddOnType = 3;
                break;
            case "PIN_IN_CHAT":
            case 4:
                m.messageAddOnType = 4;
                break;
            }
            if (d.messageAddOn != null) {
                if (typeof d.messageAddOn !== "object")
                    throw TypeError(".WAWeb.MessageAddOn.messageAddOn: object expected");
                m.messageAddOn = WAE2E.Message.fromObject(d.messageAddOn, n + 1);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            if (d.serverTimestampMS != null) {
                if ($util.Long)
                    (m.serverTimestampMS = $util.Long.fromValue(d.serverTimestampMS)).unsigned = false;
                else if (typeof d.serverTimestampMS === "string")
                    m.serverTimestampMS = parseInt(d.serverTimestampMS, 10);
                else if (typeof d.serverTimestampMS === "number")
                    m.serverTimestampMS = d.serverTimestampMS;
                else if (typeof d.serverTimestampMS === "object")
                    m.serverTimestampMS = new $util.LongBits(d.serverTimestampMS.low >>> 0, d.serverTimestampMS.high >>> 0).toNumber();
            }
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "ERROR":
            case 0:
                m.status = 0;
                break;
            case "PENDING":
            case 1:
                m.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                m.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                m.status = 3;
                break;
            case "READ":
            case 4:
                m.status = 4;
                break;
            case "PLAYED":
            case 5:
                m.status = 5;
                break;
            }
            if (d.addOnContextInfo != null) {
                if (typeof d.addOnContextInfo !== "object")
                    throw TypeError(".WAWeb.MessageAddOn.addOnContextInfo: object expected");
                m.addOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.fromObject(d.addOnContextInfo, n + 1);
            }
            if (d.messageAddOnKey != null) {
                if (typeof d.messageAddOnKey !== "object")
                    throw TypeError(".WAWeb.MessageAddOn.messageAddOnKey: object expected");
                m.messageAddOnKey = WACommon.MessageKey.fromObject(d.messageAddOnKey, n + 1);
            }
            if (d.legacyMessage != null) {
                if (typeof d.legacyMessage !== "object")
                    throw TypeError(".WAWeb.MessageAddOn.legacyMessage: object expected");
                m.legacyMessage = $root.WAWeb.LegacyMessage.fromObject(d.legacyMessage, n + 1);
            }
            return m;
        };

        MessageAddOn.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageAddOnType != null && m.hasOwnProperty("messageAddOnType")) {
                d.messageAddOnType = o.enums === String ? $root.WAWeb.MessageAddOn.MessageAddOnType[m.messageAddOnType] === undefined ? m.messageAddOnType : $root.WAWeb.MessageAddOn.MessageAddOnType[m.messageAddOnType] : m.messageAddOnType;
                if (o.oneofs)
                    d._messageAddOnType = "messageAddOnType";
            }
            if (m.messageAddOn != null && m.hasOwnProperty("messageAddOn")) {
                d.messageAddOn = WAE2E.Message.toObject(m.messageAddOn, o);
                if (o.oneofs)
                    d._messageAddOn = "messageAddOn";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            if (m.serverTimestampMS != null && m.hasOwnProperty("serverTimestampMS")) {
                if (typeof m.serverTimestampMS === "number")
                    d.serverTimestampMS = o.longs === String ? String(m.serverTimestampMS) : m.serverTimestampMS;
                else
                    d.serverTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.serverTimestampMS) : o.longs === Number ? new $util.LongBits(m.serverTimestampMS.low >>> 0, m.serverTimestampMS.high >>> 0).toNumber() : m.serverTimestampMS;
                if (o.oneofs)
                    d._serverTimestampMS = "serverTimestampMS";
            }
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? $root.WAWeb.WebMessageInfo.Status[m.status] === undefined ? m.status : $root.WAWeb.WebMessageInfo.Status[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            if (m.addOnContextInfo != null && m.hasOwnProperty("addOnContextInfo")) {
                d.addOnContextInfo = $root.WAWeb.MessageAddOnContextInfo.toObject(m.addOnContextInfo, o);
                if (o.oneofs)
                    d._addOnContextInfo = "addOnContextInfo";
            }
            if (m.messageAddOnKey != null && m.hasOwnProperty("messageAddOnKey")) {
                d.messageAddOnKey = WACommon.MessageKey.toObject(m.messageAddOnKey, o);
                if (o.oneofs)
                    d._messageAddOnKey = "messageAddOnKey";
            }
            if (m.legacyMessage != null && m.hasOwnProperty("legacyMessage")) {
                d.legacyMessage = $root.WAWeb.LegacyMessage.toObject(m.legacyMessage, o);
                if (o.oneofs)
                    d._legacyMessage = "legacyMessage";
            }
            return d;
        };

        MessageAddOn.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAddOn.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.MessageAddOn";
        };

        MessageAddOn.MessageAddOnType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "REACTION"] = 1;
            values[valuesById[2] = "EVENT_RESPONSE"] = 2;
            values[valuesById[3] = "POLL_UPDATE"] = 3;
            values[valuesById[4] = "PIN_IN_CHAT"] = 4;
            return values;
        })();

        return MessageAddOn;
    })();

    WAWeb.GroupHistoryBundleInfo = (function() {

        function GroupHistoryBundleInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupHistoryBundleInfo.prototype.deprecatedMessageHistoryBundle = null;
        GroupHistoryBundleInfo.prototype.processState = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryBundleInfo.prototype, "_deprecatedMessageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedMessageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryBundleInfo.prototype, "_processState", {
            get: $util.oneOfGetter($oneOfFields = ["processState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupHistoryBundleInfo.create = function create(properties) {
            return new GroupHistoryBundleInfo(properties);
        };

        GroupHistoryBundleInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.deprecatedMessageHistoryBundle != null && Object.hasOwnProperty.call(m, "deprecatedMessageHistoryBundle"))
                WAE2E.MessageHistoryBundle.encode(m.deprecatedMessageHistoryBundle, w.uint32(10).fork()).ldelim();
            if (m.processState != null && Object.hasOwnProperty.call(m, "processState"))
                w.uint32(16).int32(m.processState);
            return w;
        };

        GroupHistoryBundleInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.GroupHistoryBundleInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.deprecatedMessageHistoryBundle = WAE2E.MessageHistoryBundle.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.processState = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupHistoryBundleInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.GroupHistoryBundleInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.GroupHistoryBundleInfo();
            if (d.deprecatedMessageHistoryBundle != null) {
                if (typeof d.deprecatedMessageHistoryBundle !== "object")
                    throw TypeError(".WAWeb.GroupHistoryBundleInfo.deprecatedMessageHistoryBundle: object expected");
                m.deprecatedMessageHistoryBundle = WAE2E.MessageHistoryBundle.fromObject(d.deprecatedMessageHistoryBundle, n + 1);
            }
            switch (d.processState) {
            default:
                if (typeof d.processState === "number") {
                    m.processState = d.processState;
                    break;
                }
                break;
            case "NOT_INJECTED":
            case 0:
                m.processState = 0;
                break;
            case "INJECTED":
            case 1:
                m.processState = 1;
                break;
            case "INJECTED_PARTIAL":
            case 2:
                m.processState = 2;
                break;
            case "INJECTION_FAILED":
            case 3:
                m.processState = 3;
                break;
            case "INJECTION_FAILED_NO_RETRY":
            case 4:
                m.processState = 4;
                break;
            }
            return m;
        };

        GroupHistoryBundleInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.deprecatedMessageHistoryBundle != null && m.hasOwnProperty("deprecatedMessageHistoryBundle")) {
                d.deprecatedMessageHistoryBundle = WAE2E.MessageHistoryBundle.toObject(m.deprecatedMessageHistoryBundle, o);
                if (o.oneofs)
                    d._deprecatedMessageHistoryBundle = "deprecatedMessageHistoryBundle";
            }
            if (m.processState != null && m.hasOwnProperty("processState")) {
                d.processState = o.enums === String ? $root.WAWeb.GroupHistoryBundleInfo.ProcessState[m.processState] === undefined ? m.processState : $root.WAWeb.GroupHistoryBundleInfo.ProcessState[m.processState] : m.processState;
                if (o.oneofs)
                    d._processState = "processState";
            }
            return d;
        };

        GroupHistoryBundleInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistoryBundleInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.GroupHistoryBundleInfo";
        };

        GroupHistoryBundleInfo.ProcessState = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_INJECTED"] = 0;
            values[valuesById[1] = "INJECTED"] = 1;
            values[valuesById[2] = "INJECTED_PARTIAL"] = 2;
            values[valuesById[3] = "INJECTION_FAILED"] = 3;
            values[valuesById[4] = "INJECTION_FAILED_NO_RETRY"] = 4;
            return values;
        })();

        return GroupHistoryBundleInfo;
    })();

    WAWeb.CommentMetadata = (function() {

        function CommentMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CommentMetadata.prototype.commentParentKey = null;
        CommentMetadata.prototype.replyCount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMetadata.prototype, "_commentParentKey", {
            get: $util.oneOfGetter($oneOfFields = ["commentParentKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMetadata.prototype, "_replyCount", {
            get: $util.oneOfGetter($oneOfFields = ["replyCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CommentMetadata.create = function create(properties) {
            return new CommentMetadata(properties);
        };

        CommentMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.commentParentKey != null && Object.hasOwnProperty.call(m, "commentParentKey"))
                WACommon.MessageKey.encode(m.commentParentKey, w.uint32(10).fork()).ldelim();
            if (m.replyCount != null && Object.hasOwnProperty.call(m, "replyCount"))
                w.uint32(16).uint32(m.replyCount);
            return w;
        };

        CommentMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.CommentMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.commentParentKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.replyCount = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CommentMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.CommentMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.CommentMetadata();
            if (d.commentParentKey != null) {
                if (typeof d.commentParentKey !== "object")
                    throw TypeError(".WAWeb.CommentMetadata.commentParentKey: object expected");
                m.commentParentKey = WACommon.MessageKey.fromObject(d.commentParentKey, n + 1);
            }
            if (d.replyCount != null) {
                m.replyCount = d.replyCount >>> 0;
            }
            return m;
        };

        CommentMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.commentParentKey != null && m.hasOwnProperty("commentParentKey")) {
                d.commentParentKey = WACommon.MessageKey.toObject(m.commentParentKey, o);
                if (o.oneofs)
                    d._commentParentKey = "commentParentKey";
            }
            if (m.replyCount != null && m.hasOwnProperty("replyCount")) {
                d.replyCount = m.replyCount;
                if (o.oneofs)
                    d._replyCount = "replyCount";
            }
            return d;
        };

        CommentMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CommentMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.CommentMetadata";
        };

        return CommentMetadata;
    })();

    WAWeb.WebNotificationsInfo = (function() {

        function WebNotificationsInfo(p) {
            this.notifyMessages = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        WebNotificationsInfo.prototype.timestamp = null;
        WebNotificationsInfo.prototype.unreadChats = null;
        WebNotificationsInfo.prototype.notifyMessageCount = null;
        WebNotificationsInfo.prototype.notifyMessages = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_unreadChats", {
            get: $util.oneOfGetter($oneOfFields = ["unreadChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(WebNotificationsInfo.prototype, "_notifyMessageCount", {
            get: $util.oneOfGetter($oneOfFields = ["notifyMessageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebNotificationsInfo.create = function create(properties) {
            return new WebNotificationsInfo(properties);
        };

        WebNotificationsInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.unreadChats != null && Object.hasOwnProperty.call(m, "unreadChats"))
                w.uint32(24).uint32(m.unreadChats);
            if (m.notifyMessageCount != null && Object.hasOwnProperty.call(m, "notifyMessageCount"))
                w.uint32(32).uint32(m.notifyMessageCount);
            if (m.notifyMessages != null && m.notifyMessages.length) {
                for (var i = 0; i < m.notifyMessages.length; ++i)
                    $root.WAWeb.WebMessageInfo.encode(m.notifyMessages[i], w.uint32(42).fork()).ldelim();
            }
            return w;
        };

        WebNotificationsInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.WebNotificationsInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.timestamp = r.uint64();
                        break;
                    }
                case 3: {
                        m.unreadChats = r.uint32();
                        break;
                    }
                case 4: {
                        m.notifyMessageCount = r.uint32();
                        break;
                    }
                case 5: {
                        if (!(m.notifyMessages && m.notifyMessages.length))
                            m.notifyMessages = [];
                        m.notifyMessages.push($root.WAWeb.WebMessageInfo.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        WebNotificationsInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.WebNotificationsInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.WebNotificationsInfo();
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = true;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.unreadChats != null) {
                m.unreadChats = d.unreadChats >>> 0;
            }
            if (d.notifyMessageCount != null) {
                m.notifyMessageCount = d.notifyMessageCount >>> 0;
            }
            if (d.notifyMessages) {
                if (!Array.isArray(d.notifyMessages))
                    throw TypeError(".WAWeb.WebNotificationsInfo.notifyMessages: array expected");
                m.notifyMessages = [];
                for (var i = 0; i < d.notifyMessages.length; ++i) {
                    if (typeof d.notifyMessages[i] !== "object")
                        throw TypeError(".WAWeb.WebNotificationsInfo.notifyMessages: object expected");
                    m.notifyMessages[i] = $root.WAWeb.WebMessageInfo.fromObject(d.notifyMessages[i], n + 1);
                }
            }
            return m;
        };

        WebNotificationsInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.notifyMessages = [];
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            if (m.unreadChats != null && m.hasOwnProperty("unreadChats")) {
                d.unreadChats = m.unreadChats;
                if (o.oneofs)
                    d._unreadChats = "unreadChats";
            }
            if (m.notifyMessageCount != null && m.hasOwnProperty("notifyMessageCount")) {
                d.notifyMessageCount = m.notifyMessageCount;
                if (o.oneofs)
                    d._notifyMessageCount = "notifyMessageCount";
            }
            if (m.notifyMessages && m.notifyMessages.length) {
                d.notifyMessages = [];
                for (var j = 0; j < m.notifyMessages.length; ++j) {
                    d.notifyMessages[j] = $root.WAWeb.WebMessageInfo.toObject(m.notifyMessages[j], o);
                }
            }
            return d;
        };

        WebNotificationsInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebNotificationsInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.WebNotificationsInfo";
        };

        return WebNotificationsInfo;
    })();

    WAWeb.NotificationMessageInfo = (function() {

        function NotificationMessageInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        NotificationMessageInfo.prototype.key = null;
        NotificationMessageInfo.prototype.message = null;
        NotificationMessageInfo.prototype.messageTimestamp = null;
        NotificationMessageInfo.prototype.participant = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NotificationMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NotificationMessageInfo.create = function create(properties) {
            return new NotificationMessageInfo(properties);
        };

        NotificationMessageInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(18).fork()).ldelim();
            if (m.messageTimestamp != null && Object.hasOwnProperty.call(m, "messageTimestamp"))
                w.uint32(24).uint64(m.messageTimestamp);
            if (m.participant != null && Object.hasOwnProperty.call(m, "participant"))
                w.uint32(34).string(m.participant);
            return w;
        };

        NotificationMessageInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.NotificationMessageInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.messageTimestamp = r.uint64();
                        break;
                    }
                case 4: {
                        m.participant = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        NotificationMessageInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.NotificationMessageInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.NotificationMessageInfo();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAWeb.NotificationMessageInfo.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAWeb.NotificationMessageInfo.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            if (d.messageTimestamp != null) {
                if ($util.Long)
                    (m.messageTimestamp = $util.Long.fromValue(d.messageTimestamp)).unsigned = true;
                else if (typeof d.messageTimestamp === "string")
                    m.messageTimestamp = parseInt(d.messageTimestamp, 10);
                else if (typeof d.messageTimestamp === "number")
                    m.messageTimestamp = d.messageTimestamp;
                else if (typeof d.messageTimestamp === "object")
                    m.messageTimestamp = new $util.LongBits(d.messageTimestamp.low >>> 0, d.messageTimestamp.high >>> 0).toNumber(true);
            }
            if (d.participant != null) {
                m.participant = String(d.participant);
            }
            return m;
        };

        NotificationMessageInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.messageTimestamp != null && m.hasOwnProperty("messageTimestamp")) {
                if (typeof m.messageTimestamp === "number")
                    d.messageTimestamp = o.longs === String ? String(m.messageTimestamp) : m.messageTimestamp;
                else
                    d.messageTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.messageTimestamp) : o.longs === Number ? new $util.LongBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0).toNumber(true) : m.messageTimestamp;
                if (o.oneofs)
                    d._messageTimestamp = "messageTimestamp";
            }
            if (m.participant != null && m.hasOwnProperty("participant")) {
                d.participant = m.participant;
                if (o.oneofs)
                    d._participant = "participant";
            }
            return d;
        };

        NotificationMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotificationMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.NotificationMessageInfo";
        };

        return NotificationMessageInfo;
    })();

    WAWeb.ReportingTokenInfo = (function() {

        function ReportingTokenInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ReportingTokenInfo.prototype.reportingTag = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReportingTokenInfo.prototype, "_reportingTag", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ReportingTokenInfo.create = function create(properties) {
            return new ReportingTokenInfo(properties);
        };

        ReportingTokenInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.reportingTag != null && Object.hasOwnProperty.call(m, "reportingTag"))
                w.uint32(10).bytes(m.reportingTag);
            return w;
        };

        ReportingTokenInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.ReportingTokenInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.reportingTag = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ReportingTokenInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.ReportingTokenInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.ReportingTokenInfo();
            if (d.reportingTag != null) {
                if (typeof d.reportingTag === "string")
                    $util.base64.decode(d.reportingTag, m.reportingTag = $util.newBuffer($util.base64.length(d.reportingTag)), 0);
                else if (d.reportingTag.length >= 0)
                    m.reportingTag = d.reportingTag;
            }
            return m;
        };

        ReportingTokenInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.reportingTag != null && m.hasOwnProperty("reportingTag")) {
                d.reportingTag = o.bytes === String ? $util.base64.encode(m.reportingTag, 0, m.reportingTag.length) : o.bytes === Array ? Array.prototype.slice.call(m.reportingTag) : m.reportingTag;
                if (o.oneofs)
                    d._reportingTag = "reportingTag";
            }
            return d;
        };

        ReportingTokenInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReportingTokenInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.ReportingTokenInfo";
        };

        return ReportingTokenInfo;
    })();

    WAWeb.MediaData = (function() {

        function MediaData(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MediaData.prototype.localPath = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaData.prototype, "_localPath", {
            get: $util.oneOfGetter($oneOfFields = ["localPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaData.create = function create(properties) {
            return new MediaData(properties);
        };

        MediaData.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.localPath != null && Object.hasOwnProperty.call(m, "localPath"))
                w.uint32(10).string(m.localPath);
            return w;
        };

        MediaData.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.MediaData();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.localPath = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MediaData.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.MediaData)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.MediaData();
            if (d.localPath != null) {
                m.localPath = String(d.localPath);
            }
            return m;
        };

        MediaData.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.localPath != null && m.hasOwnProperty("localPath")) {
                d.localPath = m.localPath;
                if (o.oneofs)
                    d._localPath = "localPath";
            }
            return d;
        };

        MediaData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.MediaData";
        };

        return MediaData;
    })();

    WAWeb.PhotoChange = (function() {

        function PhotoChange(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PhotoChange.prototype.oldPhoto = null;
        PhotoChange.prototype.newPhoto = null;
        PhotoChange.prototype.newPhotoID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_oldPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["oldPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_newPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["newPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PhotoChange.prototype, "_newPhotoID", {
            get: $util.oneOfGetter($oneOfFields = ["newPhotoID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PhotoChange.create = function create(properties) {
            return new PhotoChange(properties);
        };

        PhotoChange.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.oldPhoto != null && Object.hasOwnProperty.call(m, "oldPhoto"))
                w.uint32(10).bytes(m.oldPhoto);
            if (m.newPhoto != null && Object.hasOwnProperty.call(m, "newPhoto"))
                w.uint32(18).bytes(m.newPhoto);
            if (m.newPhotoID != null && Object.hasOwnProperty.call(m, "newPhotoID"))
                w.uint32(24).uint32(m.newPhotoID);
            return w;
        };

        PhotoChange.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PhotoChange();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.oldPhoto = r.bytes();
                        break;
                    }
                case 2: {
                        m.newPhoto = r.bytes();
                        break;
                    }
                case 3: {
                        m.newPhotoID = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PhotoChange.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PhotoChange)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PhotoChange();
            if (d.oldPhoto != null) {
                if (typeof d.oldPhoto === "string")
                    $util.base64.decode(d.oldPhoto, m.oldPhoto = $util.newBuffer($util.base64.length(d.oldPhoto)), 0);
                else if (d.oldPhoto.length >= 0)
                    m.oldPhoto = d.oldPhoto;
            }
            if (d.newPhoto != null) {
                if (typeof d.newPhoto === "string")
                    $util.base64.decode(d.newPhoto, m.newPhoto = $util.newBuffer($util.base64.length(d.newPhoto)), 0);
                else if (d.newPhoto.length >= 0)
                    m.newPhoto = d.newPhoto;
            }
            if (d.newPhotoID != null) {
                m.newPhotoID = d.newPhotoID >>> 0;
            }
            return m;
        };

        PhotoChange.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.oldPhoto != null && m.hasOwnProperty("oldPhoto")) {
                d.oldPhoto = o.bytes === String ? $util.base64.encode(m.oldPhoto, 0, m.oldPhoto.length) : o.bytes === Array ? Array.prototype.slice.call(m.oldPhoto) : m.oldPhoto;
                if (o.oneofs)
                    d._oldPhoto = "oldPhoto";
            }
            if (m.newPhoto != null && m.hasOwnProperty("newPhoto")) {
                d.newPhoto = o.bytes === String ? $util.base64.encode(m.newPhoto, 0, m.newPhoto.length) : o.bytes === Array ? Array.prototype.slice.call(m.newPhoto) : m.newPhoto;
                if (o.oneofs)
                    d._newPhoto = "newPhoto";
            }
            if (m.newPhotoID != null && m.hasOwnProperty("newPhotoID")) {
                d.newPhotoID = m.newPhotoID;
                if (o.oneofs)
                    d._newPhotoID = "newPhotoID";
            }
            return d;
        };

        PhotoChange.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PhotoChange.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PhotoChange";
        };

        return PhotoChange;
    })();

    WAWeb.StatusPSA = (function() {

        function StatusPSA(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusPSA.prototype.campaignID = null;
        StatusPSA.prototype.campaignExpirationTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusPSA.prototype, "_campaignID", {
            get: $util.oneOfGetter($oneOfFields = ["campaignID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusPSA.prototype, "_campaignExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["campaignExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusPSA.create = function create(properties) {
            return new StatusPSA(properties);
        };

        StatusPSA.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.campaignID != null && Object.hasOwnProperty.call(m, "campaignID"))
                w.uint32(352).uint64(m.campaignID);
            if (m.campaignExpirationTimestamp != null && Object.hasOwnProperty.call(m, "campaignExpirationTimestamp"))
                w.uint32(360).uint64(m.campaignExpirationTimestamp);
            return w;
        };

        StatusPSA.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.StatusPSA();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 44: {
                        m.campaignID = r.uint64();
                        break;
                    }
                case 45: {
                        m.campaignExpirationTimestamp = r.uint64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusPSA.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.StatusPSA)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.StatusPSA();
            if (d.campaignID != null) {
                if ($util.Long)
                    (m.campaignID = $util.Long.fromValue(d.campaignID)).unsigned = true;
                else if (typeof d.campaignID === "string")
                    m.campaignID = parseInt(d.campaignID, 10);
                else if (typeof d.campaignID === "number")
                    m.campaignID = d.campaignID;
                else if (typeof d.campaignID === "object")
                    m.campaignID = new $util.LongBits(d.campaignID.low >>> 0, d.campaignID.high >>> 0).toNumber(true);
            }
            if (d.campaignExpirationTimestamp != null) {
                if ($util.Long)
                    (m.campaignExpirationTimestamp = $util.Long.fromValue(d.campaignExpirationTimestamp)).unsigned = true;
                else if (typeof d.campaignExpirationTimestamp === "string")
                    m.campaignExpirationTimestamp = parseInt(d.campaignExpirationTimestamp, 10);
                else if (typeof d.campaignExpirationTimestamp === "number")
                    m.campaignExpirationTimestamp = d.campaignExpirationTimestamp;
                else if (typeof d.campaignExpirationTimestamp === "object")
                    m.campaignExpirationTimestamp = new $util.LongBits(d.campaignExpirationTimestamp.low >>> 0, d.campaignExpirationTimestamp.high >>> 0).toNumber(true);
            }
            return m;
        };

        StatusPSA.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.campaignID != null && m.hasOwnProperty("campaignID")) {
                if (typeof m.campaignID === "number")
                    d.campaignID = o.longs === String ? String(m.campaignID) : m.campaignID;
                else
                    d.campaignID = o.longs === String ? $util.Long.prototype.toString.call(m.campaignID) : o.longs === Number ? new $util.LongBits(m.campaignID.low >>> 0, m.campaignID.high >>> 0).toNumber(true) : m.campaignID;
                if (o.oneofs)
                    d._campaignID = "campaignID";
            }
            if (m.campaignExpirationTimestamp != null && m.hasOwnProperty("campaignExpirationTimestamp")) {
                if (typeof m.campaignExpirationTimestamp === "number")
                    d.campaignExpirationTimestamp = o.longs === String ? String(m.campaignExpirationTimestamp) : m.campaignExpirationTimestamp;
                else
                    d.campaignExpirationTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.campaignExpirationTimestamp) : o.longs === Number ? new $util.LongBits(m.campaignExpirationTimestamp.low >>> 0, m.campaignExpirationTimestamp.high >>> 0).toNumber(true) : m.campaignExpirationTimestamp;
                if (o.oneofs)
                    d._campaignExpirationTimestamp = "campaignExpirationTimestamp";
            }
            return d;
        };

        StatusPSA.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusPSA.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.StatusPSA";
        };

        return StatusPSA;
    })();

    WAWeb.UserReceipt = (function() {

        function UserReceipt(p) {
            this.pendingDeviceJID = [];
            this.deliveredDeviceJID = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        UserReceipt.prototype.userJID = null;
        UserReceipt.prototype.receiptTimestamp = null;
        UserReceipt.prototype.readTimestamp = null;
        UserReceipt.prototype.playedTimestamp = null;
        UserReceipt.prototype.pendingDeviceJID = $util.emptyArray;
        UserReceipt.prototype.deliveredDeviceJID = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_userJID", {
            get: $util.oneOfGetter($oneOfFields = ["userJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_receiptTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["receiptTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_readTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["readTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(UserReceipt.prototype, "_playedTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["playedTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        UserReceipt.create = function create(properties) {
            return new UserReceipt(properties);
        };

        UserReceipt.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.userJID != null && Object.hasOwnProperty.call(m, "userJID"))
                w.uint32(10).string(m.userJID);
            if (m.receiptTimestamp != null && Object.hasOwnProperty.call(m, "receiptTimestamp"))
                w.uint32(16).int64(m.receiptTimestamp);
            if (m.readTimestamp != null && Object.hasOwnProperty.call(m, "readTimestamp"))
                w.uint32(24).int64(m.readTimestamp);
            if (m.playedTimestamp != null && Object.hasOwnProperty.call(m, "playedTimestamp"))
                w.uint32(32).int64(m.playedTimestamp);
            if (m.pendingDeviceJID != null && m.pendingDeviceJID.length) {
                for (var i = 0; i < m.pendingDeviceJID.length; ++i)
                    w.uint32(42).string(m.pendingDeviceJID[i]);
            }
            if (m.deliveredDeviceJID != null && m.deliveredDeviceJID.length) {
                for (var i = 0; i < m.deliveredDeviceJID.length; ++i)
                    w.uint32(50).string(m.deliveredDeviceJID[i]);
            }
            return w;
        };

        UserReceipt.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.UserReceipt();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.userJID = r.string();
                        break;
                    }
                case 2: {
                        m.receiptTimestamp = r.int64();
                        break;
                    }
                case 3: {
                        m.readTimestamp = r.int64();
                        break;
                    }
                case 4: {
                        m.playedTimestamp = r.int64();
                        break;
                    }
                case 5: {
                        if (!(m.pendingDeviceJID && m.pendingDeviceJID.length))
                            m.pendingDeviceJID = [];
                        m.pendingDeviceJID.push(r.string());
                        break;
                    }
                case 6: {
                        if (!(m.deliveredDeviceJID && m.deliveredDeviceJID.length))
                            m.deliveredDeviceJID = [];
                        m.deliveredDeviceJID.push(r.string());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        UserReceipt.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.UserReceipt)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.UserReceipt();
            if (d.userJID != null) {
                m.userJID = String(d.userJID);
            }
            if (d.receiptTimestamp != null) {
                if ($util.Long)
                    (m.receiptTimestamp = $util.Long.fromValue(d.receiptTimestamp)).unsigned = false;
                else if (typeof d.receiptTimestamp === "string")
                    m.receiptTimestamp = parseInt(d.receiptTimestamp, 10);
                else if (typeof d.receiptTimestamp === "number")
                    m.receiptTimestamp = d.receiptTimestamp;
                else if (typeof d.receiptTimestamp === "object")
                    m.receiptTimestamp = new $util.LongBits(d.receiptTimestamp.low >>> 0, d.receiptTimestamp.high >>> 0).toNumber();
            }
            if (d.readTimestamp != null) {
                if ($util.Long)
                    (m.readTimestamp = $util.Long.fromValue(d.readTimestamp)).unsigned = false;
                else if (typeof d.readTimestamp === "string")
                    m.readTimestamp = parseInt(d.readTimestamp, 10);
                else if (typeof d.readTimestamp === "number")
                    m.readTimestamp = d.readTimestamp;
                else if (typeof d.readTimestamp === "object")
                    m.readTimestamp = new $util.LongBits(d.readTimestamp.low >>> 0, d.readTimestamp.high >>> 0).toNumber();
            }
            if (d.playedTimestamp != null) {
                if ($util.Long)
                    (m.playedTimestamp = $util.Long.fromValue(d.playedTimestamp)).unsigned = false;
                else if (typeof d.playedTimestamp === "string")
                    m.playedTimestamp = parseInt(d.playedTimestamp, 10);
                else if (typeof d.playedTimestamp === "number")
                    m.playedTimestamp = d.playedTimestamp;
                else if (typeof d.playedTimestamp === "object")
                    m.playedTimestamp = new $util.LongBits(d.playedTimestamp.low >>> 0, d.playedTimestamp.high >>> 0).toNumber();
            }
            if (d.pendingDeviceJID) {
                if (!Array.isArray(d.pendingDeviceJID))
                    throw TypeError(".WAWeb.UserReceipt.pendingDeviceJID: array expected");
                m.pendingDeviceJID = [];
                for (var i = 0; i < d.pendingDeviceJID.length; ++i) {
                    m.pendingDeviceJID[i] = String(d.pendingDeviceJID[i]);
                }
            }
            if (d.deliveredDeviceJID) {
                if (!Array.isArray(d.deliveredDeviceJID))
                    throw TypeError(".WAWeb.UserReceipt.deliveredDeviceJID: array expected");
                m.deliveredDeviceJID = [];
                for (var i = 0; i < d.deliveredDeviceJID.length; ++i) {
                    m.deliveredDeviceJID[i] = String(d.deliveredDeviceJID[i]);
                }
            }
            return m;
        };

        UserReceipt.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.pendingDeviceJID = [];
                d.deliveredDeviceJID = [];
            }
            if (m.userJID != null && m.hasOwnProperty("userJID")) {
                d.userJID = m.userJID;
                if (o.oneofs)
                    d._userJID = "userJID";
            }
            if (m.receiptTimestamp != null && m.hasOwnProperty("receiptTimestamp")) {
                if (typeof m.receiptTimestamp === "number")
                    d.receiptTimestamp = o.longs === String ? String(m.receiptTimestamp) : m.receiptTimestamp;
                else
                    d.receiptTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.receiptTimestamp) : o.longs === Number ? new $util.LongBits(m.receiptTimestamp.low >>> 0, m.receiptTimestamp.high >>> 0).toNumber() : m.receiptTimestamp;
                if (o.oneofs)
                    d._receiptTimestamp = "receiptTimestamp";
            }
            if (m.readTimestamp != null && m.hasOwnProperty("readTimestamp")) {
                if (typeof m.readTimestamp === "number")
                    d.readTimestamp = o.longs === String ? String(m.readTimestamp) : m.readTimestamp;
                else
                    d.readTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.readTimestamp) : o.longs === Number ? new $util.LongBits(m.readTimestamp.low >>> 0, m.readTimestamp.high >>> 0).toNumber() : m.readTimestamp;
                if (o.oneofs)
                    d._readTimestamp = "readTimestamp";
            }
            if (m.playedTimestamp != null && m.hasOwnProperty("playedTimestamp")) {
                if (typeof m.playedTimestamp === "number")
                    d.playedTimestamp = o.longs === String ? String(m.playedTimestamp) : m.playedTimestamp;
                else
                    d.playedTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.playedTimestamp) : o.longs === Number ? new $util.LongBits(m.playedTimestamp.low >>> 0, m.playedTimestamp.high >>> 0).toNumber() : m.playedTimestamp;
                if (o.oneofs)
                    d._playedTimestamp = "playedTimestamp";
            }
            if (m.pendingDeviceJID && m.pendingDeviceJID.length) {
                d.pendingDeviceJID = [];
                for (var j = 0; j < m.pendingDeviceJID.length; ++j) {
                    d.pendingDeviceJID[j] = m.pendingDeviceJID[j];
                }
            }
            if (m.deliveredDeviceJID && m.deliveredDeviceJID.length) {
                d.deliveredDeviceJID = [];
                for (var j = 0; j < m.deliveredDeviceJID.length; ++j) {
                    d.deliveredDeviceJID[j] = m.deliveredDeviceJID[j];
                }
            }
            return d;
        };

        UserReceipt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        UserReceipt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.UserReceipt";
        };

        return UserReceipt;
    })();

    WAWeb.Reaction = (function() {

        function Reaction(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Reaction.prototype.key = null;
        Reaction.prototype.text = null;
        Reaction.prototype.groupingKey = null;
        Reaction.prototype.senderTimestampMS = null;
        Reaction.prototype.unread = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_groupingKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Reaction.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Reaction.create = function create(properties) {
            return new Reaction(properties);
        };

        Reaction.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            if (m.groupingKey != null && Object.hasOwnProperty.call(m, "groupingKey"))
                w.uint32(26).string(m.groupingKey);
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(32).int64(m.senderTimestampMS);
            if (m.unread != null && Object.hasOwnProperty.call(m, "unread"))
                w.uint32(40).bool(m.unread);
            return w;
        };

        Reaction.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.Reaction();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.text = r.string();
                        break;
                    }
                case 3: {
                        m.groupingKey = r.string();
                        break;
                    }
                case 4: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                case 5: {
                        m.unread = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Reaction.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.Reaction)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.Reaction();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAWeb.Reaction.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.groupingKey != null) {
                m.groupingKey = String(d.groupingKey);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            if (d.unread != null) {
                m.unread = Boolean(d.unread);
            }
            return m;
        };

        Reaction.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.groupingKey != null && m.hasOwnProperty("groupingKey")) {
                d.groupingKey = m.groupingKey;
                if (o.oneofs)
                    d._groupingKey = "groupingKey";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            if (m.unread != null && m.hasOwnProperty("unread")) {
                d.unread = m.unread;
                if (o.oneofs)
                    d._unread = "unread";
            }
            return d;
        };

        Reaction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Reaction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.Reaction";
        };

        return Reaction;
    })();

    WAWeb.PollUpdate = (function() {

        function PollUpdate(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollUpdate.prototype.pollUpdateMessageKey = null;
        PollUpdate.prototype.vote = null;
        PollUpdate.prototype.senderTimestampMS = null;
        PollUpdate.prototype.serverTimestampMS = null;
        PollUpdate.prototype.unread = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_pollUpdateMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_vote", {
            get: $util.oneOfGetter($oneOfFields = ["vote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_serverTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdate.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollUpdate.create = function create(properties) {
            return new PollUpdate(properties);
        };

        PollUpdate.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.pollUpdateMessageKey != null && Object.hasOwnProperty.call(m, "pollUpdateMessageKey"))
                WACommon.MessageKey.encode(m.pollUpdateMessageKey, w.uint32(10).fork()).ldelim();
            if (m.vote != null && Object.hasOwnProperty.call(m, "vote"))
                WAE2E.PollVoteMessage.encode(m.vote, w.uint32(18).fork()).ldelim();
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(24).int64(m.senderTimestampMS);
            if (m.serverTimestampMS != null && Object.hasOwnProperty.call(m, "serverTimestampMS"))
                w.uint32(32).int64(m.serverTimestampMS);
            if (m.unread != null && Object.hasOwnProperty.call(m, "unread"))
                w.uint32(40).bool(m.unread);
            return w;
        };

        PollUpdate.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PollUpdate();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.pollUpdateMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.vote = WAE2E.PollVoteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                case 4: {
                        m.serverTimestampMS = r.int64();
                        break;
                    }
                case 5: {
                        m.unread = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollUpdate.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PollUpdate)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PollUpdate();
            if (d.pollUpdateMessageKey != null) {
                if (typeof d.pollUpdateMessageKey !== "object")
                    throw TypeError(".WAWeb.PollUpdate.pollUpdateMessageKey: object expected");
                m.pollUpdateMessageKey = WACommon.MessageKey.fromObject(d.pollUpdateMessageKey, n + 1);
            }
            if (d.vote != null) {
                if (typeof d.vote !== "object")
                    throw TypeError(".WAWeb.PollUpdate.vote: object expected");
                m.vote = WAE2E.PollVoteMessage.fromObject(d.vote, n + 1);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            if (d.serverTimestampMS != null) {
                if ($util.Long)
                    (m.serverTimestampMS = $util.Long.fromValue(d.serverTimestampMS)).unsigned = false;
                else if (typeof d.serverTimestampMS === "string")
                    m.serverTimestampMS = parseInt(d.serverTimestampMS, 10);
                else if (typeof d.serverTimestampMS === "number")
                    m.serverTimestampMS = d.serverTimestampMS;
                else if (typeof d.serverTimestampMS === "object")
                    m.serverTimestampMS = new $util.LongBits(d.serverTimestampMS.low >>> 0, d.serverTimestampMS.high >>> 0).toNumber();
            }
            if (d.unread != null) {
                m.unread = Boolean(d.unread);
            }
            return m;
        };

        PollUpdate.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.pollUpdateMessageKey != null && m.hasOwnProperty("pollUpdateMessageKey")) {
                d.pollUpdateMessageKey = WACommon.MessageKey.toObject(m.pollUpdateMessageKey, o);
                if (o.oneofs)
                    d._pollUpdateMessageKey = "pollUpdateMessageKey";
            }
            if (m.vote != null && m.hasOwnProperty("vote")) {
                d.vote = WAE2E.PollVoteMessage.toObject(m.vote, o);
                if (o.oneofs)
                    d._vote = "vote";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            if (m.serverTimestampMS != null && m.hasOwnProperty("serverTimestampMS")) {
                if (typeof m.serverTimestampMS === "number")
                    d.serverTimestampMS = o.longs === String ? String(m.serverTimestampMS) : m.serverTimestampMS;
                else
                    d.serverTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.serverTimestampMS) : o.longs === Number ? new $util.LongBits(m.serverTimestampMS.low >>> 0, m.serverTimestampMS.high >>> 0).toNumber() : m.serverTimestampMS;
                if (o.oneofs)
                    d._serverTimestampMS = "serverTimestampMS";
            }
            if (m.unread != null && m.hasOwnProperty("unread")) {
                d.unread = m.unread;
                if (o.oneofs)
                    d._unread = "unread";
            }
            return d;
        };

        PollUpdate.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollUpdate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PollUpdate";
        };

        return PollUpdate;
    })();

    WAWeb.PollAdditionalMetadata = (function() {

        function PollAdditionalMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollAdditionalMetadata.prototype.pollInvalidated = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollAdditionalMetadata.prototype, "_pollInvalidated", {
            get: $util.oneOfGetter($oneOfFields = ["pollInvalidated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollAdditionalMetadata.create = function create(properties) {
            return new PollAdditionalMetadata(properties);
        };

        PollAdditionalMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.pollInvalidated != null && Object.hasOwnProperty.call(m, "pollInvalidated"))
                w.uint32(8).bool(m.pollInvalidated);
            return w;
        };

        PollAdditionalMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PollAdditionalMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.pollInvalidated = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollAdditionalMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PollAdditionalMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PollAdditionalMetadata();
            if (d.pollInvalidated != null) {
                m.pollInvalidated = Boolean(d.pollInvalidated);
            }
            return m;
        };

        PollAdditionalMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.pollInvalidated != null && m.hasOwnProperty("pollInvalidated")) {
                d.pollInvalidated = m.pollInvalidated;
                if (o.oneofs)
                    d._pollInvalidated = "pollInvalidated";
            }
            return d;
        };

        PollAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PollAdditionalMetadata";
        };

        return PollAdditionalMetadata;
    })();

    WAWeb.InteractiveMessageAdditionalMetadata = (function() {

        function InteractiveMessageAdditionalMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InteractiveMessageAdditionalMetadata.prototype.isGalaxyFlowCompleted = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessageAdditionalMetadata.prototype, "_isGalaxyFlowCompleted", {
            get: $util.oneOfGetter($oneOfFields = ["isGalaxyFlowCompleted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveMessageAdditionalMetadata.create = function create(properties) {
            return new InteractiveMessageAdditionalMetadata(properties);
        };

        InteractiveMessageAdditionalMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.isGalaxyFlowCompleted != null && Object.hasOwnProperty.call(m, "isGalaxyFlowCompleted"))
                w.uint32(8).bool(m.isGalaxyFlowCompleted);
            return w;
        };

        InteractiveMessageAdditionalMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.InteractiveMessageAdditionalMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.isGalaxyFlowCompleted = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InteractiveMessageAdditionalMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.InteractiveMessageAdditionalMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.InteractiveMessageAdditionalMetadata();
            if (d.isGalaxyFlowCompleted != null) {
                m.isGalaxyFlowCompleted = Boolean(d.isGalaxyFlowCompleted);
            }
            return m;
        };

        InteractiveMessageAdditionalMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.isGalaxyFlowCompleted != null && m.hasOwnProperty("isGalaxyFlowCompleted")) {
                d.isGalaxyFlowCompleted = m.isGalaxyFlowCompleted;
                if (o.oneofs)
                    d._isGalaxyFlowCompleted = "isGalaxyFlowCompleted";
            }
            return d;
        };

        InteractiveMessageAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveMessageAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.InteractiveMessageAdditionalMetadata";
        };

        return InteractiveMessageAdditionalMetadata;
    })();

    WAWeb.EventAdditionalMetadata = (function() {

        function EventAdditionalMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EventAdditionalMetadata.prototype.isStale = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventAdditionalMetadata.prototype, "_isStale", {
            get: $util.oneOfGetter($oneOfFields = ["isStale"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventAdditionalMetadata.create = function create(properties) {
            return new EventAdditionalMetadata(properties);
        };

        EventAdditionalMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.isStale != null && Object.hasOwnProperty.call(m, "isStale"))
                w.uint32(8).bool(m.isStale);
            return w;
        };

        EventAdditionalMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.EventAdditionalMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.isStale = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EventAdditionalMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.EventAdditionalMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.EventAdditionalMetadata();
            if (d.isStale != null) {
                m.isStale = Boolean(d.isStale);
            }
            return m;
        };

        EventAdditionalMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.isStale != null && m.hasOwnProperty("isStale")) {
                d.isStale = m.isStale;
                if (o.oneofs)
                    d._isStale = "isStale";
            }
            return d;
        };

        EventAdditionalMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventAdditionalMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.EventAdditionalMetadata";
        };

        return EventAdditionalMetadata;
    })();

    WAWeb.KeepInChat = (function() {

        function KeepInChat(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        KeepInChat.prototype.keepType = null;
        KeepInChat.prototype.serverTimestamp = null;
        KeepInChat.prototype.key = null;
        KeepInChat.prototype.deviceJID = null;
        KeepInChat.prototype.clientTimestampMS = null;
        KeepInChat.prototype.serverTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_keepType", {
            get: $util.oneOfGetter($oneOfFields = ["keepType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_serverTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_deviceJID", {
            get: $util.oneOfGetter($oneOfFields = ["deviceJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_clientTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["clientTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChat.prototype, "_serverTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeepInChat.create = function create(properties) {
            return new KeepInChat(properties);
        };

        KeepInChat.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keepType != null && Object.hasOwnProperty.call(m, "keepType"))
                w.uint32(8).int32(m.keepType);
            if (m.serverTimestamp != null && Object.hasOwnProperty.call(m, "serverTimestamp"))
                w.uint32(16).int64(m.serverTimestamp);
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(26).fork()).ldelim();
            if (m.deviceJID != null && Object.hasOwnProperty.call(m, "deviceJID"))
                w.uint32(34).string(m.deviceJID);
            if (m.clientTimestampMS != null && Object.hasOwnProperty.call(m, "clientTimestampMS"))
                w.uint32(40).int64(m.clientTimestampMS);
            if (m.serverTimestampMS != null && Object.hasOwnProperty.call(m, "serverTimestampMS"))
                w.uint32(48).int64(m.serverTimestampMS);
            return w;
        };

        KeepInChat.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.KeepInChat();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.keepType = r.int32();
                        break;
                    }
                case 2: {
                        m.serverTimestamp = r.int64();
                        break;
                    }
                case 3: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.deviceJID = r.string();
                        break;
                    }
                case 5: {
                        m.clientTimestampMS = r.int64();
                        break;
                    }
                case 6: {
                        m.serverTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        KeepInChat.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.KeepInChat)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.KeepInChat();
            switch (d.keepType) {
            default:
                if (typeof d.keepType === "number") {
                    m.keepType = d.keepType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.keepType = 0;
                break;
            case "KEEP_FOR_ALL":
            case 1:
                m.keepType = 1;
                break;
            case "UNDO_KEEP_FOR_ALL":
            case 2:
                m.keepType = 2;
                break;
            }
            if (d.serverTimestamp != null) {
                if ($util.Long)
                    (m.serverTimestamp = $util.Long.fromValue(d.serverTimestamp)).unsigned = false;
                else if (typeof d.serverTimestamp === "string")
                    m.serverTimestamp = parseInt(d.serverTimestamp, 10);
                else if (typeof d.serverTimestamp === "number")
                    m.serverTimestamp = d.serverTimestamp;
                else if (typeof d.serverTimestamp === "object")
                    m.serverTimestamp = new $util.LongBits(d.serverTimestamp.low >>> 0, d.serverTimestamp.high >>> 0).toNumber();
            }
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAWeb.KeepInChat.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.deviceJID != null) {
                m.deviceJID = String(d.deviceJID);
            }
            if (d.clientTimestampMS != null) {
                if ($util.Long)
                    (m.clientTimestampMS = $util.Long.fromValue(d.clientTimestampMS)).unsigned = false;
                else if (typeof d.clientTimestampMS === "string")
                    m.clientTimestampMS = parseInt(d.clientTimestampMS, 10);
                else if (typeof d.clientTimestampMS === "number")
                    m.clientTimestampMS = d.clientTimestampMS;
                else if (typeof d.clientTimestampMS === "object")
                    m.clientTimestampMS = new $util.LongBits(d.clientTimestampMS.low >>> 0, d.clientTimestampMS.high >>> 0).toNumber();
            }
            if (d.serverTimestampMS != null) {
                if ($util.Long)
                    (m.serverTimestampMS = $util.Long.fromValue(d.serverTimestampMS)).unsigned = false;
                else if (typeof d.serverTimestampMS === "string")
                    m.serverTimestampMS = parseInt(d.serverTimestampMS, 10);
                else if (typeof d.serverTimestampMS === "number")
                    m.serverTimestampMS = d.serverTimestampMS;
                else if (typeof d.serverTimestampMS === "object")
                    m.serverTimestampMS = new $util.LongBits(d.serverTimestampMS.low >>> 0, d.serverTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        KeepInChat.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.keepType != null && m.hasOwnProperty("keepType")) {
                d.keepType = o.enums === String ? WAE2E.KeepType[m.keepType] === undefined ? m.keepType : WAE2E.KeepType[m.keepType] : m.keepType;
                if (o.oneofs)
                    d._keepType = "keepType";
            }
            if (m.serverTimestamp != null && m.hasOwnProperty("serverTimestamp")) {
                if (typeof m.serverTimestamp === "number")
                    d.serverTimestamp = o.longs === String ? String(m.serverTimestamp) : m.serverTimestamp;
                else
                    d.serverTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.serverTimestamp) : o.longs === Number ? new $util.LongBits(m.serverTimestamp.low >>> 0, m.serverTimestamp.high >>> 0).toNumber() : m.serverTimestamp;
                if (o.oneofs)
                    d._serverTimestamp = "serverTimestamp";
            }
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.deviceJID != null && m.hasOwnProperty("deviceJID")) {
                d.deviceJID = m.deviceJID;
                if (o.oneofs)
                    d._deviceJID = "deviceJID";
            }
            if (m.clientTimestampMS != null && m.hasOwnProperty("clientTimestampMS")) {
                if (typeof m.clientTimestampMS === "number")
                    d.clientTimestampMS = o.longs === String ? String(m.clientTimestampMS) : m.clientTimestampMS;
                else
                    d.clientTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.clientTimestampMS) : o.longs === Number ? new $util.LongBits(m.clientTimestampMS.low >>> 0, m.clientTimestampMS.high >>> 0).toNumber() : m.clientTimestampMS;
                if (o.oneofs)
                    d._clientTimestampMS = "clientTimestampMS";
            }
            if (m.serverTimestampMS != null && m.hasOwnProperty("serverTimestampMS")) {
                if (typeof m.serverTimestampMS === "number")
                    d.serverTimestampMS = o.longs === String ? String(m.serverTimestampMS) : m.serverTimestampMS;
                else
                    d.serverTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.serverTimestampMS) : o.longs === Number ? new $util.LongBits(m.serverTimestampMS.low >>> 0, m.serverTimestampMS.high >>> 0).toNumber() : m.serverTimestampMS;
                if (o.oneofs)
                    d._serverTimestampMS = "serverTimestampMS";
            }
            return d;
        };

        KeepInChat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        KeepInChat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.KeepInChat";
        };

        return KeepInChat;
    })();

    WAWeb.MessageAddOnContextInfo = (function() {

        function MessageAddOnContextInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageAddOnContextInfo.prototype.messageAddOnDurationInSecs = null;
        MessageAddOnContextInfo.prototype.messageAddOnExpiryType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAddOnContextInfo.create = function create(properties) {
            return new MessageAddOnContextInfo(properties);
        };

        MessageAddOnContextInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs"))
                w.uint32(8).uint32(m.messageAddOnDurationInSecs);
            if (m.messageAddOnExpiryType != null && Object.hasOwnProperty.call(m, "messageAddOnExpiryType"))
                w.uint32(16).int32(m.messageAddOnExpiryType);
            return w;
        };

        MessageAddOnContextInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.MessageAddOnContextInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageAddOnDurationInSecs = r.uint32();
                        break;
                    }
                case 2: {
                        m.messageAddOnExpiryType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageAddOnContextInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.MessageAddOnContextInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.MessageAddOnContextInfo();
            if (d.messageAddOnDurationInSecs != null) {
                m.messageAddOnDurationInSecs = d.messageAddOnDurationInSecs >>> 0;
            }
            switch (d.messageAddOnExpiryType) {
            default:
                if (typeof d.messageAddOnExpiryType === "number") {
                    m.messageAddOnExpiryType = d.messageAddOnExpiryType;
                    break;
                }
                break;
            case "STATIC":
            case 1:
                m.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                m.messageAddOnExpiryType = 2;
                break;
            }
            return m;
        };

        MessageAddOnContextInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageAddOnDurationInSecs != null && m.hasOwnProperty("messageAddOnDurationInSecs")) {
                d.messageAddOnDurationInSecs = m.messageAddOnDurationInSecs;
                if (o.oneofs)
                    d._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
            }
            if (m.messageAddOnExpiryType != null && m.hasOwnProperty("messageAddOnExpiryType")) {
                d.messageAddOnExpiryType = o.enums === String ? WAE2E.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] === undefined ? m.messageAddOnExpiryType : WAE2E.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] : m.messageAddOnExpiryType;
                if (o.oneofs)
                    d._messageAddOnExpiryType = "messageAddOnExpiryType";
            }
            return d;
        };

        MessageAddOnContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAddOnContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.MessageAddOnContextInfo";
        };

        return MessageAddOnContextInfo;
    })();

    WAWeb.PremiumMessageInfo = (function() {

        function PremiumMessageInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PremiumMessageInfo.prototype.serverCampaignID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PremiumMessageInfo.prototype, "_serverCampaignID", {
            get: $util.oneOfGetter($oneOfFields = ["serverCampaignID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PremiumMessageInfo.create = function create(properties) {
            return new PremiumMessageInfo(properties);
        };

        PremiumMessageInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.serverCampaignID != null && Object.hasOwnProperty.call(m, "serverCampaignID"))
                w.uint32(10).string(m.serverCampaignID);
            return w;
        };

        PremiumMessageInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.PremiumMessageInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.serverCampaignID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PremiumMessageInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.PremiumMessageInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.PremiumMessageInfo();
            if (d.serverCampaignID != null) {
                m.serverCampaignID = String(d.serverCampaignID);
            }
            return m;
        };

        PremiumMessageInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.serverCampaignID != null && m.hasOwnProperty("serverCampaignID")) {
                d.serverCampaignID = m.serverCampaignID;
                if (o.oneofs)
                    d._serverCampaignID = "serverCampaignID";
            }
            return d;
        };

        PremiumMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PremiumMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.PremiumMessageInfo";
        };

        return PremiumMessageInfo;
    })();

    WAWeb.EventResponse = (function() {

        function EventResponse(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EventResponse.prototype.eventResponseMessageKey = null;
        EventResponse.prototype.timestampMS = null;
        EventResponse.prototype.eventResponseMessage = null;
        EventResponse.prototype.unread = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_eventResponseMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_timestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponse.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventResponse.create = function create(properties) {
            return new EventResponse(properties);
        };

        EventResponse.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.eventResponseMessageKey != null && Object.hasOwnProperty.call(m, "eventResponseMessageKey"))
                WACommon.MessageKey.encode(m.eventResponseMessageKey, w.uint32(10).fork()).ldelim();
            if (m.timestampMS != null && Object.hasOwnProperty.call(m, "timestampMS"))
                w.uint32(16).int64(m.timestampMS);
            if (m.eventResponseMessage != null && Object.hasOwnProperty.call(m, "eventResponseMessage"))
                WAE2E.EventResponseMessage.encode(m.eventResponseMessage, w.uint32(26).fork()).ldelim();
            if (m.unread != null && Object.hasOwnProperty.call(m, "unread"))
                w.uint32(32).bool(m.unread);
            return w;
        };

        EventResponse.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.EventResponse();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.eventResponseMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.timestampMS = r.int64();
                        break;
                    }
                case 3: {
                        m.eventResponseMessage = WAE2E.EventResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.unread = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EventResponse.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.EventResponse)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.EventResponse();
            if (d.eventResponseMessageKey != null) {
                if (typeof d.eventResponseMessageKey !== "object")
                    throw TypeError(".WAWeb.EventResponse.eventResponseMessageKey: object expected");
                m.eventResponseMessageKey = WACommon.MessageKey.fromObject(d.eventResponseMessageKey, n + 1);
            }
            if (d.timestampMS != null) {
                if ($util.Long)
                    (m.timestampMS = $util.Long.fromValue(d.timestampMS)).unsigned = false;
                else if (typeof d.timestampMS === "string")
                    m.timestampMS = parseInt(d.timestampMS, 10);
                else if (typeof d.timestampMS === "number")
                    m.timestampMS = d.timestampMS;
                else if (typeof d.timestampMS === "object")
                    m.timestampMS = new $util.LongBits(d.timestampMS.low >>> 0, d.timestampMS.high >>> 0).toNumber();
            }
            if (d.eventResponseMessage != null) {
                if (typeof d.eventResponseMessage !== "object")
                    throw TypeError(".WAWeb.EventResponse.eventResponseMessage: object expected");
                m.eventResponseMessage = WAE2E.EventResponseMessage.fromObject(d.eventResponseMessage, n + 1);
            }
            if (d.unread != null) {
                m.unread = Boolean(d.unread);
            }
            return m;
        };

        EventResponse.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.eventResponseMessageKey != null && m.hasOwnProperty("eventResponseMessageKey")) {
                d.eventResponseMessageKey = WACommon.MessageKey.toObject(m.eventResponseMessageKey, o);
                if (o.oneofs)
                    d._eventResponseMessageKey = "eventResponseMessageKey";
            }
            if (m.timestampMS != null && m.hasOwnProperty("timestampMS")) {
                if (typeof m.timestampMS === "number")
                    d.timestampMS = o.longs === String ? String(m.timestampMS) : m.timestampMS;
                else
                    d.timestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.timestampMS) : o.longs === Number ? new $util.LongBits(m.timestampMS.low >>> 0, m.timestampMS.high >>> 0).toNumber() : m.timestampMS;
                if (o.oneofs)
                    d._timestampMS = "timestampMS";
            }
            if (m.eventResponseMessage != null && m.hasOwnProperty("eventResponseMessage")) {
                d.eventResponseMessage = WAE2E.EventResponseMessage.toObject(m.eventResponseMessage, o);
                if (o.oneofs)
                    d._eventResponseMessage = "eventResponseMessage";
            }
            if (m.unread != null && m.hasOwnProperty("unread")) {
                d.unread = m.unread;
                if (o.oneofs)
                    d._unread = "unread";
            }
            return d;
        };

        EventResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.EventResponse";
        };

        return EventResponse;
    })();

    WAWeb.LegacyMessage = (function() {

        function LegacyMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LegacyMessage.prototype.eventResponseMessage = null;
        LegacyMessage.prototype.pollVote = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LegacyMessage.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LegacyMessage.prototype, "_pollVote", {
            get: $util.oneOfGetter($oneOfFields = ["pollVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LegacyMessage.create = function create(properties) {
            return new LegacyMessage(properties);
        };

        LegacyMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.eventResponseMessage != null && Object.hasOwnProperty.call(m, "eventResponseMessage"))
                WAE2E.EventResponseMessage.encode(m.eventResponseMessage, w.uint32(10).fork()).ldelim();
            if (m.pollVote != null && Object.hasOwnProperty.call(m, "pollVote"))
                WAE2E.PollVoteMessage.encode(m.pollVote, w.uint32(18).fork()).ldelim();
            return w;
        };

        LegacyMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.LegacyMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.eventResponseMessage = WAE2E.EventResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.pollVote = WAE2E.PollVoteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LegacyMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.LegacyMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.LegacyMessage();
            if (d.eventResponseMessage != null) {
                if (typeof d.eventResponseMessage !== "object")
                    throw TypeError(".WAWeb.LegacyMessage.eventResponseMessage: object expected");
                m.eventResponseMessage = WAE2E.EventResponseMessage.fromObject(d.eventResponseMessage, n + 1);
            }
            if (d.pollVote != null) {
                if (typeof d.pollVote !== "object")
                    throw TypeError(".WAWeb.LegacyMessage.pollVote: object expected");
                m.pollVote = WAE2E.PollVoteMessage.fromObject(d.pollVote, n + 1);
            }
            return m;
        };

        LegacyMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.eventResponseMessage != null && m.hasOwnProperty("eventResponseMessage")) {
                d.eventResponseMessage = WAE2E.EventResponseMessage.toObject(m.eventResponseMessage, o);
                if (o.oneofs)
                    d._eventResponseMessage = "eventResponseMessage";
            }
            if (m.pollVote != null && m.hasOwnProperty("pollVote")) {
                d.pollVote = WAE2E.PollVoteMessage.toObject(m.pollVote, o);
                if (o.oneofs)
                    d._pollVote = "pollVote";
            }
            return d;
        };

        LegacyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LegacyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.LegacyMessage";
        };

        return LegacyMessage;
    })();

    WAWeb.StatusMentionMessage = (function() {

        function StatusMentionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusMentionMessage.prototype.quotedStatus = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusMentionMessage.prototype, "_quotedStatus", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusMentionMessage.create = function create(properties) {
            return new StatusMentionMessage(properties);
        };

        StatusMentionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.quotedStatus != null && Object.hasOwnProperty.call(m, "quotedStatus"))
                WAE2E.Message.encode(m.quotedStatus, w.uint32(10).fork()).ldelim();
            return w;
        };

        StatusMentionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.StatusMentionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.quotedStatus = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusMentionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.StatusMentionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.StatusMentionMessage();
            if (d.quotedStatus != null) {
                if (typeof d.quotedStatus !== "object")
                    throw TypeError(".WAWeb.StatusMentionMessage.quotedStatus: object expected");
                m.quotedStatus = WAE2E.Message.fromObject(d.quotedStatus, n + 1);
            }
            return m;
        };

        StatusMentionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.quotedStatus != null && m.hasOwnProperty("quotedStatus")) {
                d.quotedStatus = WAE2E.Message.toObject(m.quotedStatus, o);
                if (o.oneofs)
                    d._quotedStatus = "quotedStatus";
            }
            return d;
        };

        StatusMentionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusMentionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.StatusMentionMessage";
        };

        return StatusMentionMessage;
    })();

    WAWeb.Citation = (function() {

        function Citation(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Citation.prototype.title = null;
        Citation.prototype.subtitle = null;
        Citation.prototype.cmsID = null;
        Citation.prototype.imageURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_subtitle", {
            get: $util.oneOfGetter($oneOfFields = ["subtitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_cmsID", {
            get: $util.oneOfGetter($oneOfFields = ["cmsID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Citation.prototype, "_imageURL", {
            get: $util.oneOfGetter($oneOfFields = ["imageURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Citation.create = function create(properties) {
            return new Citation(properties);
        };

        Citation.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(10).string(m.title);
            if (m.subtitle != null && Object.hasOwnProperty.call(m, "subtitle"))
                w.uint32(18).string(m.subtitle);
            if (m.cmsID != null && Object.hasOwnProperty.call(m, "cmsID"))
                w.uint32(26).string(m.cmsID);
            if (m.imageURL != null && Object.hasOwnProperty.call(m, "imageURL"))
                w.uint32(34).string(m.imageURL);
            return w;
        };

        Citation.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.Citation();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.title = r.string();
                        break;
                    }
                case 2: {
                        m.subtitle = r.string();
                        break;
                    }
                case 3: {
                        m.cmsID = r.string();
                        break;
                    }
                case 4: {
                        m.imageURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Citation.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.Citation)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.Citation();
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.subtitle != null) {
                m.subtitle = String(d.subtitle);
            }
            if (d.cmsID != null) {
                m.cmsID = String(d.cmsID);
            }
            if (d.imageURL != null) {
                m.imageURL = String(d.imageURL);
            }
            return m;
        };

        Citation.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.subtitle != null && m.hasOwnProperty("subtitle")) {
                d.subtitle = m.subtitle;
                if (o.oneofs)
                    d._subtitle = "subtitle";
            }
            if (m.cmsID != null && m.hasOwnProperty("cmsID")) {
                d.cmsID = m.cmsID;
                if (o.oneofs)
                    d._cmsID = "cmsID";
            }
            if (m.imageURL != null && m.hasOwnProperty("imageURL")) {
                d.imageURL = m.imageURL;
                if (o.oneofs)
                    d._imageURL = "imageURL";
            }
            return d;
        };

        Citation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Citation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.Citation";
        };

        return Citation;
    })();

    WAWeb.GroupHistoryIndividualMessageInfo = (function() {

        function GroupHistoryIndividualMessageInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupHistoryIndividualMessageInfo.prototype.bundleMessageKey = null;
        GroupHistoryIndividualMessageInfo.prototype.editedAfterReceivedAsHistory = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_bundleMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["bundleMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_editedAfterReceivedAsHistory", {
            get: $util.oneOfGetter($oneOfFields = ["editedAfterReceivedAsHistory"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupHistoryIndividualMessageInfo.create = function create(properties) {
            return new GroupHistoryIndividualMessageInfo(properties);
        };

        GroupHistoryIndividualMessageInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.bundleMessageKey != null && Object.hasOwnProperty.call(m, "bundleMessageKey"))
                WACommon.MessageKey.encode(m.bundleMessageKey, w.uint32(10).fork()).ldelim();
            if (m.editedAfterReceivedAsHistory != null && Object.hasOwnProperty.call(m, "editedAfterReceivedAsHistory"))
                w.uint32(16).bool(m.editedAfterReceivedAsHistory);
            return w;
        };

        GroupHistoryIndividualMessageInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.GroupHistoryIndividualMessageInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.bundleMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.editedAfterReceivedAsHistory = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupHistoryIndividualMessageInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.GroupHistoryIndividualMessageInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.GroupHistoryIndividualMessageInfo();
            if (d.bundleMessageKey != null) {
                if (typeof d.bundleMessageKey !== "object")
                    throw TypeError(".WAWeb.GroupHistoryIndividualMessageInfo.bundleMessageKey: object expected");
                m.bundleMessageKey = WACommon.MessageKey.fromObject(d.bundleMessageKey, n + 1);
            }
            if (d.editedAfterReceivedAsHistory != null) {
                m.editedAfterReceivedAsHistory = Boolean(d.editedAfterReceivedAsHistory);
            }
            return m;
        };

        GroupHistoryIndividualMessageInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.bundleMessageKey != null && m.hasOwnProperty("bundleMessageKey")) {
                d.bundleMessageKey = WACommon.MessageKey.toObject(m.bundleMessageKey, o);
                if (o.oneofs)
                    d._bundleMessageKey = "bundleMessageKey";
            }
            if (m.editedAfterReceivedAsHistory != null && m.hasOwnProperty("editedAfterReceivedAsHistory")) {
                d.editedAfterReceivedAsHistory = m.editedAfterReceivedAsHistory;
                if (o.oneofs)
                    d._editedAfterReceivedAsHistory = "editedAfterReceivedAsHistory";
            }
            return d;
        };

        GroupHistoryIndividualMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistoryIndividualMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.GroupHistoryIndividualMessageInfo";
        };

        return GroupHistoryIndividualMessageInfo;
    })();

    WAWeb.QuarantinedMessage = (function() {

        function QuarantinedMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        QuarantinedMessage.prototype.originalData = null;
        QuarantinedMessage.prototype.extractedText = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuarantinedMessage.prototype, "_originalData", {
            get: $util.oneOfGetter($oneOfFields = ["originalData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuarantinedMessage.prototype, "_extractedText", {
            get: $util.oneOfGetter($oneOfFields = ["extractedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        QuarantinedMessage.create = function create(properties) {
            return new QuarantinedMessage(properties);
        };

        QuarantinedMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.originalData != null && Object.hasOwnProperty.call(m, "originalData"))
                w.uint32(10).bytes(m.originalData);
            if (m.extractedText != null && Object.hasOwnProperty.call(m, "extractedText"))
                w.uint32(18).string(m.extractedText);
            return w;
        };

        QuarantinedMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.QuarantinedMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.originalData = r.bytes();
                        break;
                    }
                case 2: {
                        m.extractedText = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        QuarantinedMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.QuarantinedMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.QuarantinedMessage();
            if (d.originalData != null) {
                if (typeof d.originalData === "string")
                    $util.base64.decode(d.originalData, m.originalData = $util.newBuffer($util.base64.length(d.originalData)), 0);
                else if (d.originalData.length >= 0)
                    m.originalData = d.originalData;
            }
            if (d.extractedText != null) {
                m.extractedText = String(d.extractedText);
            }
            return m;
        };

        QuarantinedMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.originalData != null && m.hasOwnProperty("originalData")) {
                d.originalData = o.bytes === String ? $util.base64.encode(m.originalData, 0, m.originalData.length) : o.bytes === Array ? Array.prototype.slice.call(m.originalData) : m.originalData;
                if (o.oneofs)
                    d._originalData = "originalData";
            }
            if (m.extractedText != null && m.hasOwnProperty("extractedText")) {
                d.extractedText = m.extractedText;
                if (o.oneofs)
                    d._extractedText = "extractedText";
            }
            return d;
        };

        QuarantinedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuarantinedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.QuarantinedMessage";
        };

        return QuarantinedMessage;
    })();

    WAWeb.ScheduledMessageMetadata = (function() {

        function ScheduledMessageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ScheduledMessageMetadata.prototype.revealKeyID = null;
        ScheduledMessageMetadata.prototype.revealKey = null;
        ScheduledMessageMetadata.prototype.scheduledTime = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKeyID", {
            get: $util.oneOfGetter($oneOfFields = ["revealKeyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKey", {
            get: $util.oneOfGetter($oneOfFields = ["revealKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledMessageMetadata.prototype, "_scheduledTime", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ScheduledMessageMetadata.create = function create(properties) {
            return new ScheduledMessageMetadata(properties);
        };

        ScheduledMessageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.revealKeyID != null && Object.hasOwnProperty.call(m, "revealKeyID"))
                w.uint32(10).string(m.revealKeyID);
            if (m.revealKey != null && Object.hasOwnProperty.call(m, "revealKey"))
                w.uint32(18).bytes(m.revealKey);
            if (m.scheduledTime != null && Object.hasOwnProperty.call(m, "scheduledTime"))
                w.uint32(24).uint64(m.scheduledTime);
            return w;
        };

        ScheduledMessageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAWeb.ScheduledMessageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.revealKeyID = r.string();
                        break;
                    }
                case 2: {
                        m.revealKey = r.bytes();
                        break;
                    }
                case 3: {
                        m.scheduledTime = r.uint64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ScheduledMessageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAWeb.ScheduledMessageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAWeb.ScheduledMessageMetadata();
            if (d.revealKeyID != null) {
                m.revealKeyID = String(d.revealKeyID);
            }
            if (d.revealKey != null) {
                if (typeof d.revealKey === "string")
                    $util.base64.decode(d.revealKey, m.revealKey = $util.newBuffer($util.base64.length(d.revealKey)), 0);
                else if (d.revealKey.length >= 0)
                    m.revealKey = d.revealKey;
            }
            if (d.scheduledTime != null) {
                if ($util.Long)
                    (m.scheduledTime = $util.Long.fromValue(d.scheduledTime)).unsigned = true;
                else if (typeof d.scheduledTime === "string")
                    m.scheduledTime = parseInt(d.scheduledTime, 10);
                else if (typeof d.scheduledTime === "number")
                    m.scheduledTime = d.scheduledTime;
                else if (typeof d.scheduledTime === "object")
                    m.scheduledTime = new $util.LongBits(d.scheduledTime.low >>> 0, d.scheduledTime.high >>> 0).toNumber(true);
            }
            return m;
        };

        ScheduledMessageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.revealKeyID != null && m.hasOwnProperty("revealKeyID")) {
                d.revealKeyID = m.revealKeyID;
                if (o.oneofs)
                    d._revealKeyID = "revealKeyID";
            }
            if (m.revealKey != null && m.hasOwnProperty("revealKey")) {
                d.revealKey = o.bytes === String ? $util.base64.encode(m.revealKey, 0, m.revealKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.revealKey) : m.revealKey;
                if (o.oneofs)
                    d._revealKey = "revealKey";
            }
            if (m.scheduledTime != null && m.hasOwnProperty("scheduledTime")) {
                if (typeof m.scheduledTime === "number")
                    d.scheduledTime = o.longs === String ? String(m.scheduledTime) : m.scheduledTime;
                else
                    d.scheduledTime = o.longs === String ? $util.Long.prototype.toString.call(m.scheduledTime) : o.longs === Number ? new $util.LongBits(m.scheduledTime.low >>> 0, m.scheduledTime.high >>> 0).toNumber(true) : m.scheduledTime;
                if (o.oneofs)
                    d._scheduledTime = "scheduledTime";
            }
            return d;
        };

        ScheduledMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ScheduledMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAWeb.ScheduledMessageMetadata";
        };

        return ScheduledMessageMetadata;
    })();

    return WAWeb;
})();

export const WAE2E = WAE2E = (() => {

    const WAE2E = {};

    WAE2E.PollType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "POLL"] = 0;
        values[valuesById[1] = "QUIZ"] = 1;
        return values;
    })();

    WAE2E.PollContentType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "TEXT"] = 1;
        values[valuesById[2] = "IMAGE"] = 2;
        return values;
    })();

    WAE2E.InsightDeliveryState = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "SENT"] = 0;
        values[valuesById[1] = "DELIVERED"] = 1;
        values[valuesById[2] = "READ"] = 2;
        values[valuesById[3] = "REPLIED"] = 3;
        values[valuesById[4] = "QUICK_REPLIED"] = 4;
        return values;
    })();

    WAE2E.PeerDataOperationRequestType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UPLOAD_STICKER"] = 0;
        values[valuesById[1] = "SEND_RECENT_STICKER_BOOTSTRAP"] = 1;
        values[valuesById[2] = "GENERATE_LINK_PREVIEW"] = 2;
        values[valuesById[3] = "HISTORY_SYNC_ON_DEMAND"] = 3;
        values[valuesById[4] = "PLACEHOLDER_MESSAGE_RESEND"] = 4;
        values[valuesById[5] = "WAFFLE_LINKING_NONCE_FETCH"] = 5;
        values[valuesById[6] = "FULL_HISTORY_SYNC_ON_DEMAND"] = 6;
        values[valuesById[7] = "COMPANION_META_NONCE_FETCH"] = 7;
        values[valuesById[8] = "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY"] = 8;
        values[valuesById[9] = "COMPANION_CANONICAL_USER_NONCE_FETCH"] = 9;
        values[valuesById[10] = "HISTORY_SYNC_CHUNK_RETRY"] = 10;
        values[valuesById[11] = "GALAXY_FLOW_ACTION"] = 11;
        values[valuesById[12] = "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO"] = 12;
        values[valuesById[13] = "BUSINESS_BROADCAST_INSIGHTS_REFRESH"] = 13;
        return values;
    })();

    WAE2E.HistorySyncType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INITIAL_BOOTSTRAP"] = 0;
        values[valuesById[1] = "INITIAL_STATUS_V3"] = 1;
        values[valuesById[2] = "FULL"] = 2;
        values[valuesById[3] = "RECENT"] = 3;
        values[valuesById[4] = "PUSH_NAME"] = 4;
        values[valuesById[5] = "NON_BLOCKING_DATA"] = 5;
        values[valuesById[6] = "ON_DEMAND"] = 6;
        values[valuesById[7] = "NO_HISTORY"] = 7;
        values[valuesById[8] = "MESSAGE_ACCESS_STATUS"] = 8;
        return values;
    })();

    WAE2E.MediaKeyDomain = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "MEDIA_KEY_DOMAIN_UNKNOWN"] = 0;
        values[valuesById[1] = "MEDIA_KEY_DOMAIN_E2EE"] = 1;
        values[valuesById[2] = "MEDIA_KEY_DOMAIN_NON_E2EE"] = 2;
        return values;
    })();

    WAE2E.WebLinkRenderConfig = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "WEBVIEW"] = 0;
        values[valuesById[1] = "SYSTEM"] = 1;
        return values;
    })();

    WAE2E.KeepType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "KEEP_FOR_ALL"] = 1;
        values[valuesById[2] = "UNDO_KEEP_FOR_ALL"] = 2;
        return values;
    })();

    WAE2E.StickerPackMessage = (function() {

        function StickerPackMessage(p) {
            this.stickers = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StickerPackMessage.prototype.stickerPackID = null;
        StickerPackMessage.prototype.name = null;
        StickerPackMessage.prototype.publisher = null;
        StickerPackMessage.prototype.stickers = $util.emptyArray;
        StickerPackMessage.prototype.fileLength = null;
        StickerPackMessage.prototype.fileSHA256 = null;
        StickerPackMessage.prototype.fileEncSHA256 = null;
        StickerPackMessage.prototype.mediaKey = null;
        StickerPackMessage.prototype.directPath = null;
        StickerPackMessage.prototype.caption = null;
        StickerPackMessage.prototype.contextInfo = null;
        StickerPackMessage.prototype.packDescription = null;
        StickerPackMessage.prototype.mediaKeyTimestamp = null;
        StickerPackMessage.prototype.trayIconFileName = null;
        StickerPackMessage.prototype.thumbnailDirectPath = null;
        StickerPackMessage.prototype.thumbnailSHA256 = null;
        StickerPackMessage.prototype.thumbnailEncSHA256 = null;
        StickerPackMessage.prototype.thumbnailHeight = null;
        StickerPackMessage.prototype.thumbnailWidth = null;
        StickerPackMessage.prototype.imageDataHash = null;
        StickerPackMessage.prototype.stickerPackSize = null;
        StickerPackMessage.prototype.stickerPackOrigin = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_stickerPackID", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_publisher", {
            get: $util.oneOfGetter($oneOfFields = ["publisher"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_packDescription", {
            get: $util.oneOfGetter($oneOfFields = ["packDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_trayIconFileName", {
            get: $util.oneOfGetter($oneOfFields = ["trayIconFileName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_thumbnailHeight", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_thumbnailWidth", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_imageDataHash", {
            get: $util.oneOfGetter($oneOfFields = ["imageDataHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_stickerPackSize", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackSize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerPackMessage.prototype, "_stickerPackOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StickerPackMessage.create = function create(properties) {
            return new StickerPackMessage(properties);
        };

        StickerPackMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stickerPackID != null && Object.hasOwnProperty.call(m, "stickerPackID"))
                w.uint32(10).string(m.stickerPackID);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(18).string(m.name);
            if (m.publisher != null && Object.hasOwnProperty.call(m, "publisher"))
                w.uint32(26).string(m.publisher);
            if (m.stickers != null && m.stickers.length) {
                for (var i = 0; i < m.stickers.length; ++i)
                    WAE2E.StickerPackMessage.Sticker.encode(m.stickers[i], w.uint32(34).fork()).ldelim();
            }
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(40).uint64(m.fileLength);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(50).bytes(m.fileSHA256);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(58).bytes(m.fileEncSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(66).bytes(m.mediaKey);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(74).string(m.directPath);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(82).string(m.caption);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(90).fork()).ldelim();
            if (m.packDescription != null && Object.hasOwnProperty.call(m, "packDescription"))
                w.uint32(98).string(m.packDescription);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(104).int64(m.mediaKeyTimestamp);
            if (m.trayIconFileName != null && Object.hasOwnProperty.call(m, "trayIconFileName"))
                w.uint32(114).string(m.trayIconFileName);
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(122).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(130).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(138).bytes(m.thumbnailEncSHA256);
            if (m.thumbnailHeight != null && Object.hasOwnProperty.call(m, "thumbnailHeight"))
                w.uint32(144).uint32(m.thumbnailHeight);
            if (m.thumbnailWidth != null && Object.hasOwnProperty.call(m, "thumbnailWidth"))
                w.uint32(152).uint32(m.thumbnailWidth);
            if (m.imageDataHash != null && Object.hasOwnProperty.call(m, "imageDataHash"))
                w.uint32(162).string(m.imageDataHash);
            if (m.stickerPackSize != null && Object.hasOwnProperty.call(m, "stickerPackSize"))
                w.uint32(168).uint64(m.stickerPackSize);
            if (m.stickerPackOrigin != null && Object.hasOwnProperty.call(m, "stickerPackOrigin"))
                w.uint32(176).int32(m.stickerPackOrigin);
            return w;
        };

        StickerPackMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StickerPackMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stickerPackID = r.string();
                        break;
                    }
                case 2: {
                        m.name = r.string();
                        break;
                    }
                case 3: {
                        m.publisher = r.string();
                        break;
                    }
                case 4: {
                        if (!(m.stickers && m.stickers.length))
                            m.stickers = [];
                        m.stickers.push(WAE2E.StickerPackMessage.Sticker.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 5: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 6: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 7: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 8: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 9: {
                        m.directPath = r.string();
                        break;
                    }
                case 10: {
                        m.caption = r.string();
                        break;
                    }
                case 11: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 12: {
                        m.packDescription = r.string();
                        break;
                    }
                case 13: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 14: {
                        m.trayIconFileName = r.string();
                        break;
                    }
                case 15: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 16: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 17: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 18: {
                        m.thumbnailHeight = r.uint32();
                        break;
                    }
                case 19: {
                        m.thumbnailWidth = r.uint32();
                        break;
                    }
                case 20: {
                        m.imageDataHash = r.string();
                        break;
                    }
                case 21: {
                        m.stickerPackSize = r.uint64();
                        break;
                    }
                case 22: {
                        m.stickerPackOrigin = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StickerPackMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StickerPackMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StickerPackMessage();
            if (d.stickerPackID != null) {
                m.stickerPackID = String(d.stickerPackID);
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.publisher != null) {
                m.publisher = String(d.publisher);
            }
            if (d.stickers) {
                if (!Array.isArray(d.stickers))
                    throw TypeError(".WAE2E.StickerPackMessage.stickers: array expected");
                m.stickers = [];
                for (var i = 0; i < d.stickers.length; ++i) {
                    if (typeof d.stickers[i] !== "object")
                        throw TypeError(".WAE2E.StickerPackMessage.stickers: object expected");
                    m.stickers[i] = WAE2E.StickerPackMessage.Sticker.fromObject(d.stickers[i], n + 1);
                }
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.StickerPackMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.packDescription != null) {
                m.packDescription = String(d.packDescription);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.trayIconFileName != null) {
                m.trayIconFileName = String(d.trayIconFileName);
            }
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.thumbnailHeight != null) {
                m.thumbnailHeight = d.thumbnailHeight >>> 0;
            }
            if (d.thumbnailWidth != null) {
                m.thumbnailWidth = d.thumbnailWidth >>> 0;
            }
            if (d.imageDataHash != null) {
                m.imageDataHash = String(d.imageDataHash);
            }
            if (d.stickerPackSize != null) {
                if ($util.Long)
                    (m.stickerPackSize = $util.Long.fromValue(d.stickerPackSize)).unsigned = true;
                else if (typeof d.stickerPackSize === "string")
                    m.stickerPackSize = parseInt(d.stickerPackSize, 10);
                else if (typeof d.stickerPackSize === "number")
                    m.stickerPackSize = d.stickerPackSize;
                else if (typeof d.stickerPackSize === "object")
                    m.stickerPackSize = new $util.LongBits(d.stickerPackSize.low >>> 0, d.stickerPackSize.high >>> 0).toNumber(true);
            }
            switch (d.stickerPackOrigin) {
            default:
                if (typeof d.stickerPackOrigin === "number") {
                    m.stickerPackOrigin = d.stickerPackOrigin;
                    break;
                }
                break;
            case "FIRST_PARTY":
            case 0:
                m.stickerPackOrigin = 0;
                break;
            case "THIRD_PARTY":
            case 1:
                m.stickerPackOrigin = 1;
                break;
            case "USER_CREATED":
            case 2:
                m.stickerPackOrigin = 2;
                break;
            }
            return m;
        };

        StickerPackMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.stickers = [];
            }
            if (m.stickerPackID != null && m.hasOwnProperty("stickerPackID")) {
                d.stickerPackID = m.stickerPackID;
                if (o.oneofs)
                    d._stickerPackID = "stickerPackID";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.publisher != null && m.hasOwnProperty("publisher")) {
                d.publisher = m.publisher;
                if (o.oneofs)
                    d._publisher = "publisher";
            }
            if (m.stickers && m.stickers.length) {
                d.stickers = [];
                for (var j = 0; j < m.stickers.length; ++j) {
                    d.stickers[j] = WAE2E.StickerPackMessage.Sticker.toObject(m.stickers[j], o);
                }
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.packDescription != null && m.hasOwnProperty("packDescription")) {
                d.packDescription = m.packDescription;
                if (o.oneofs)
                    d._packDescription = "packDescription";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.trayIconFileName != null && m.hasOwnProperty("trayIconFileName")) {
                d.trayIconFileName = m.trayIconFileName;
                if (o.oneofs)
                    d._trayIconFileName = "trayIconFileName";
            }
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.thumbnailHeight != null && m.hasOwnProperty("thumbnailHeight")) {
                d.thumbnailHeight = m.thumbnailHeight;
                if (o.oneofs)
                    d._thumbnailHeight = "thumbnailHeight";
            }
            if (m.thumbnailWidth != null && m.hasOwnProperty("thumbnailWidth")) {
                d.thumbnailWidth = m.thumbnailWidth;
                if (o.oneofs)
                    d._thumbnailWidth = "thumbnailWidth";
            }
            if (m.imageDataHash != null && m.hasOwnProperty("imageDataHash")) {
                d.imageDataHash = m.imageDataHash;
                if (o.oneofs)
                    d._imageDataHash = "imageDataHash";
            }
            if (m.stickerPackSize != null && m.hasOwnProperty("stickerPackSize")) {
                if (typeof m.stickerPackSize === "number")
                    d.stickerPackSize = o.longs === String ? String(m.stickerPackSize) : m.stickerPackSize;
                else
                    d.stickerPackSize = o.longs === String ? $util.Long.prototype.toString.call(m.stickerPackSize) : o.longs === Number ? new $util.LongBits(m.stickerPackSize.low >>> 0, m.stickerPackSize.high >>> 0).toNumber(true) : m.stickerPackSize;
                if (o.oneofs)
                    d._stickerPackSize = "stickerPackSize";
            }
            if (m.stickerPackOrigin != null && m.hasOwnProperty("stickerPackOrigin")) {
                d.stickerPackOrigin = o.enums === String ? WAE2E.StickerPackMessage.StickerPackOrigin[m.stickerPackOrigin] === undefined ? m.stickerPackOrigin : WAE2E.StickerPackMessage.StickerPackOrigin[m.stickerPackOrigin] : m.stickerPackOrigin;
                if (o.oneofs)
                    d._stickerPackOrigin = "stickerPackOrigin";
            }
            return d;
        };

        StickerPackMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StickerPackMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StickerPackMessage";
        };

        StickerPackMessage.StickerPackOrigin = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "FIRST_PARTY"] = 0;
            values[valuesById[1] = "THIRD_PARTY"] = 1;
            values[valuesById[2] = "USER_CREATED"] = 2;
            return values;
        })();

        StickerPackMessage.Sticker = (function() {

            function Sticker(p) {
                this.emojis = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Sticker.prototype.fileName = null;
            Sticker.prototype.isAnimated = null;
            Sticker.prototype.emojis = $util.emptyArray;
            Sticker.prototype.accessibilityLabel = null;
            Sticker.prototype.isLottie = null;
            Sticker.prototype.mimetype = null;
            Sticker.prototype.premium = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_fileName", {
                get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_isAnimated", {
                get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_isLottie", {
                get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Sticker.prototype, "_premium", {
                get: $util.oneOfGetter($oneOfFields = ["premium"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Sticker.create = function create(properties) {
                return new Sticker(properties);
            };

            Sticker.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.fileName != null && Object.hasOwnProperty.call(m, "fileName"))
                    w.uint32(10).string(m.fileName);
                if (m.isAnimated != null && Object.hasOwnProperty.call(m, "isAnimated"))
                    w.uint32(16).bool(m.isAnimated);
                if (m.emojis != null && m.emojis.length) {
                    for (var i = 0; i < m.emojis.length; ++i)
                        w.uint32(26).string(m.emojis[i]);
                }
                if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(34).string(m.accessibilityLabel);
                if (m.isLottie != null && Object.hasOwnProperty.call(m, "isLottie"))
                    w.uint32(40).bool(m.isLottie);
                if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(50).string(m.mimetype);
                if (m.premium != null && Object.hasOwnProperty.call(m, "premium"))
                    w.uint32(56).int32(m.premium);
                return w;
            };

            Sticker.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StickerPackMessage.Sticker();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.fileName = r.string();
                            break;
                        }
                    case 2: {
                            m.isAnimated = r.bool();
                            break;
                        }
                    case 3: {
                            if (!(m.emojis && m.emojis.length))
                                m.emojis = [];
                            m.emojis.push(r.string());
                            break;
                        }
                    case 4: {
                            m.accessibilityLabel = r.string();
                            break;
                        }
                    case 5: {
                            m.isLottie = r.bool();
                            break;
                        }
                    case 6: {
                            m.mimetype = r.string();
                            break;
                        }
                    case 7: {
                            m.premium = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Sticker.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.StickerPackMessage.Sticker)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.StickerPackMessage.Sticker();
                if (d.fileName != null) {
                    m.fileName = String(d.fileName);
                }
                if (d.isAnimated != null) {
                    m.isAnimated = Boolean(d.isAnimated);
                }
                if (d.emojis) {
                    if (!Array.isArray(d.emojis))
                        throw TypeError(".WAE2E.StickerPackMessage.Sticker.emojis: array expected");
                    m.emojis = [];
                    for (var i = 0; i < d.emojis.length; ++i) {
                        m.emojis[i] = String(d.emojis[i]);
                    }
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = String(d.accessibilityLabel);
                }
                if (d.isLottie != null) {
                    m.isLottie = Boolean(d.isLottie);
                }
                if (d.mimetype != null) {
                    m.mimetype = String(d.mimetype);
                }
                if (d.premium != null) {
                    m.premium = d.premium | 0;
                }
                return m;
            };

            Sticker.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.emojis = [];
                }
                if (m.fileName != null && m.hasOwnProperty("fileName")) {
                    d.fileName = m.fileName;
                    if (o.oneofs)
                        d._fileName = "fileName";
                }
                if (m.isAnimated != null && m.hasOwnProperty("isAnimated")) {
                    d.isAnimated = m.isAnimated;
                    if (o.oneofs)
                        d._isAnimated = "isAnimated";
                }
                if (m.emojis && m.emojis.length) {
                    d.emojis = [];
                    for (var j = 0; j < m.emojis.length; ++j) {
                        d.emojis[j] = m.emojis[j];
                    }
                }
                if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                    if (o.oneofs)
                        d._accessibilityLabel = "accessibilityLabel";
                }
                if (m.isLottie != null && m.hasOwnProperty("isLottie")) {
                    d.isLottie = m.isLottie;
                    if (o.oneofs)
                        d._isLottie = "isLottie";
                }
                if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                    d.mimetype = m.mimetype;
                    if (o.oneofs)
                        d._mimetype = "mimetype";
                }
                if (m.premium != null && m.hasOwnProperty("premium")) {
                    d.premium = m.premium;
                    if (o.oneofs)
                        d._premium = "premium";
                }
                return d;
            };

            Sticker.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Sticker.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.StickerPackMessage.Sticker";
            };

            return Sticker;
        })();

        return StickerPackMessage;
    })();

    WAE2E.PlaceholderMessage = (function() {

        function PlaceholderMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PlaceholderMessage.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PlaceholderMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PlaceholderMessage.create = function create(properties) {
            return new PlaceholderMessage(properties);
        };

        PlaceholderMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            return w;
        };

        PlaceholderMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PlaceholderMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PlaceholderMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PlaceholderMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PlaceholderMessage();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "MASK_LINKED_DEVICES":
            case 0:
                m.type = 0;
                break;
            }
            return m;
        };

        PlaceholderMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.PlaceholderMessage.PlaceholderType[m.type] === undefined ? m.type : WAE2E.PlaceholderMessage.PlaceholderType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        PlaceholderMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PlaceholderMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PlaceholderMessage";
        };

        PlaceholderMessage.PlaceholderType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "MASK_LINKED_DEVICES"] = 0;
            return values;
        })();

        return PlaceholderMessage;
    })();

    WAE2E.BCallMessage = (function() {

        function BCallMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BCallMessage.prototype.sessionID = null;
        BCallMessage.prototype.mediaType = null;
        BCallMessage.prototype.masterKey = null;
        BCallMessage.prototype.caption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BCallMessage.prototype, "_sessionID", {
            get: $util.oneOfGetter($oneOfFields = ["sessionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BCallMessage.prototype, "_mediaType", {
            get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BCallMessage.prototype, "_masterKey", {
            get: $util.oneOfGetter($oneOfFields = ["masterKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BCallMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BCallMessage.create = function create(properties) {
            return new BCallMessage(properties);
        };

        BCallMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.sessionID != null && Object.hasOwnProperty.call(m, "sessionID"))
                w.uint32(10).string(m.sessionID);
            if (m.mediaType != null && Object.hasOwnProperty.call(m, "mediaType"))
                w.uint32(16).int32(m.mediaType);
            if (m.masterKey != null && Object.hasOwnProperty.call(m, "masterKey"))
                w.uint32(26).bytes(m.masterKey);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(34).string(m.caption);
            return w;
        };

        BCallMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.BCallMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.sessionID = r.string();
                        break;
                    }
                case 2: {
                        m.mediaType = r.int32();
                        break;
                    }
                case 3: {
                        m.masterKey = r.bytes();
                        break;
                    }
                case 4: {
                        m.caption = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BCallMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.BCallMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.BCallMessage();
            if (d.sessionID != null) {
                m.sessionID = String(d.sessionID);
            }
            switch (d.mediaType) {
            default:
                if (typeof d.mediaType === "number") {
                    m.mediaType = d.mediaType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.mediaType = 0;
                break;
            case "AUDIO":
            case 1:
                m.mediaType = 1;
                break;
            case "VIDEO":
            case 2:
                m.mediaType = 2;
                break;
            }
            if (d.masterKey != null) {
                if (typeof d.masterKey === "string")
                    $util.base64.decode(d.masterKey, m.masterKey = $util.newBuffer($util.base64.length(d.masterKey)), 0);
                else if (d.masterKey.length >= 0)
                    m.masterKey = d.masterKey;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            return m;
        };

        BCallMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.sessionID != null && m.hasOwnProperty("sessionID")) {
                d.sessionID = m.sessionID;
                if (o.oneofs)
                    d._sessionID = "sessionID";
            }
            if (m.mediaType != null && m.hasOwnProperty("mediaType")) {
                d.mediaType = o.enums === String ? WAE2E.BCallMessage.MediaType[m.mediaType] === undefined ? m.mediaType : WAE2E.BCallMessage.MediaType[m.mediaType] : m.mediaType;
                if (o.oneofs)
                    d._mediaType = "mediaType";
            }
            if (m.masterKey != null && m.hasOwnProperty("masterKey")) {
                d.masterKey = o.bytes === String ? $util.base64.encode(m.masterKey, 0, m.masterKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.masterKey) : m.masterKey;
                if (o.oneofs)
                    d._masterKey = "masterKey";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            return d;
        };

        BCallMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BCallMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.BCallMessage";
        };

        BCallMessage.MediaType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "AUDIO"] = 1;
            values[valuesById[2] = "VIDEO"] = 2;
            return values;
        })();

        return BCallMessage;
    })();

    WAE2E.CallLogMessage = (function() {

        function CallLogMessage(p) {
            this.participants = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CallLogMessage.prototype.isVideo = null;
        CallLogMessage.prototype.callOutcome = null;
        CallLogMessage.prototype.durationSecs = null;
        CallLogMessage.prototype.callType = null;
        CallLogMessage.prototype.participants = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogMessage.prototype, "_isVideo", {
            get: $util.oneOfGetter($oneOfFields = ["isVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogMessage.prototype, "_callOutcome", {
            get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogMessage.prototype, "_durationSecs", {
            get: $util.oneOfGetter($oneOfFields = ["durationSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CallLogMessage.prototype, "_callType", {
            get: $util.oneOfGetter($oneOfFields = ["callType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CallLogMessage.create = function create(properties) {
            return new CallLogMessage(properties);
        };

        CallLogMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.isVideo != null && Object.hasOwnProperty.call(m, "isVideo"))
                w.uint32(8).bool(m.isVideo);
            if (m.callOutcome != null && Object.hasOwnProperty.call(m, "callOutcome"))
                w.uint32(16).int32(m.callOutcome);
            if (m.durationSecs != null && Object.hasOwnProperty.call(m, "durationSecs"))
                w.uint32(24).int64(m.durationSecs);
            if (m.callType != null && Object.hasOwnProperty.call(m, "callType"))
                w.uint32(32).int32(m.callType);
            if (m.participants != null && m.participants.length) {
                for (var i = 0; i < m.participants.length; ++i)
                    WAE2E.CallLogMessage.CallParticipant.encode(m.participants[i], w.uint32(42).fork()).ldelim();
            }
            return w;
        };

        CallLogMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CallLogMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.isVideo = r.bool();
                        break;
                    }
                case 2: {
                        m.callOutcome = r.int32();
                        break;
                    }
                case 3: {
                        m.durationSecs = r.int64();
                        break;
                    }
                case 4: {
                        m.callType = r.int32();
                        break;
                    }
                case 5: {
                        if (!(m.participants && m.participants.length))
                            m.participants = [];
                        m.participants.push(WAE2E.CallLogMessage.CallParticipant.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CallLogMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.CallLogMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.CallLogMessage();
            if (d.isVideo != null) {
                m.isVideo = Boolean(d.isVideo);
            }
            switch (d.callOutcome) {
            default:
                if (typeof d.callOutcome === "number") {
                    m.callOutcome = d.callOutcome;
                    break;
                }
                break;
            case "CONNECTED":
            case 0:
                m.callOutcome = 0;
                break;
            case "MISSED":
            case 1:
                m.callOutcome = 1;
                break;
            case "FAILED":
            case 2:
                m.callOutcome = 2;
                break;
            case "REJECTED":
            case 3:
                m.callOutcome = 3;
                break;
            case "ACCEPTED_ELSEWHERE":
            case 4:
                m.callOutcome = 4;
                break;
            case "ONGOING":
            case 5:
                m.callOutcome = 5;
                break;
            case "SILENCED_BY_DND":
            case 6:
                m.callOutcome = 6;
                break;
            case "SILENCED_UNKNOWN_CALLER":
            case 7:
                m.callOutcome = 7;
                break;
            }
            if (d.durationSecs != null) {
                if ($util.Long)
                    (m.durationSecs = $util.Long.fromValue(d.durationSecs)).unsigned = false;
                else if (typeof d.durationSecs === "string")
                    m.durationSecs = parseInt(d.durationSecs, 10);
                else if (typeof d.durationSecs === "number")
                    m.durationSecs = d.durationSecs;
                else if (typeof d.durationSecs === "object")
                    m.durationSecs = new $util.LongBits(d.durationSecs.low >>> 0, d.durationSecs.high >>> 0).toNumber();
            }
            switch (d.callType) {
            default:
                if (typeof d.callType === "number") {
                    m.callType = d.callType;
                    break;
                }
                break;
            case "REGULAR":
            case 0:
                m.callType = 0;
                break;
            case "SCHEDULED_CALL":
            case 1:
                m.callType = 1;
                break;
            case "VOICE_CHAT":
            case 2:
                m.callType = 2;
                break;
            }
            if (d.participants) {
                if (!Array.isArray(d.participants))
                    throw TypeError(".WAE2E.CallLogMessage.participants: array expected");
                m.participants = [];
                for (var i = 0; i < d.participants.length; ++i) {
                    if (typeof d.participants[i] !== "object")
                        throw TypeError(".WAE2E.CallLogMessage.participants: object expected");
                    m.participants[i] = WAE2E.CallLogMessage.CallParticipant.fromObject(d.participants[i], n + 1);
                }
            }
            return m;
        };

        CallLogMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.participants = [];
            }
            if (m.isVideo != null && m.hasOwnProperty("isVideo")) {
                d.isVideo = m.isVideo;
                if (o.oneofs)
                    d._isVideo = "isVideo";
            }
            if (m.callOutcome != null && m.hasOwnProperty("callOutcome")) {
                d.callOutcome = o.enums === String ? WAE2E.CallLogMessage.CallOutcome[m.callOutcome] === undefined ? m.callOutcome : WAE2E.CallLogMessage.CallOutcome[m.callOutcome] : m.callOutcome;
                if (o.oneofs)
                    d._callOutcome = "callOutcome";
            }
            if (m.durationSecs != null && m.hasOwnProperty("durationSecs")) {
                if (typeof m.durationSecs === "number")
                    d.durationSecs = o.longs === String ? String(m.durationSecs) : m.durationSecs;
                else
                    d.durationSecs = o.longs === String ? $util.Long.prototype.toString.call(m.durationSecs) : o.longs === Number ? new $util.LongBits(m.durationSecs.low >>> 0, m.durationSecs.high >>> 0).toNumber() : m.durationSecs;
                if (o.oneofs)
                    d._durationSecs = "durationSecs";
            }
            if (m.callType != null && m.hasOwnProperty("callType")) {
                d.callType = o.enums === String ? WAE2E.CallLogMessage.CallType[m.callType] === undefined ? m.callType : WAE2E.CallLogMessage.CallType[m.callType] : m.callType;
                if (o.oneofs)
                    d._callType = "callType";
            }
            if (m.participants && m.participants.length) {
                d.participants = [];
                for (var j = 0; j < m.participants.length; ++j) {
                    d.participants[j] = WAE2E.CallLogMessage.CallParticipant.toObject(m.participants[j], o);
                }
            }
            return d;
        };

        CallLogMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CallLogMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.CallLogMessage";
        };

        CallLogMessage.CallOutcome = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CONNECTED"] = 0;
            values[valuesById[1] = "MISSED"] = 1;
            values[valuesById[2] = "FAILED"] = 2;
            values[valuesById[3] = "REJECTED"] = 3;
            values[valuesById[4] = "ACCEPTED_ELSEWHERE"] = 4;
            values[valuesById[5] = "ONGOING"] = 5;
            values[valuesById[6] = "SILENCED_BY_DND"] = 6;
            values[valuesById[7] = "SILENCED_UNKNOWN_CALLER"] = 7;
            return values;
        })();

        CallLogMessage.CallType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "REGULAR"] = 0;
            values[valuesById[1] = "SCHEDULED_CALL"] = 1;
            values[valuesById[2] = "VOICE_CHAT"] = 2;
            return values;
        })();

        CallLogMessage.CallParticipant = (function() {

            function CallParticipant(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CallParticipant.prototype.JID = null;
            CallParticipant.prototype.callOutcome = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallParticipant.prototype, "_JID", {
                get: $util.oneOfGetter($oneOfFields = ["JID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallParticipant.prototype, "_callOutcome", {
                get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CallParticipant.create = function create(properties) {
                return new CallParticipant(properties);
            };

            CallParticipant.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.JID != null && Object.hasOwnProperty.call(m, "JID"))
                    w.uint32(10).string(m.JID);
                if (m.callOutcome != null && Object.hasOwnProperty.call(m, "callOutcome"))
                    w.uint32(16).int32(m.callOutcome);
                return w;
            };

            CallParticipant.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CallLogMessage.CallParticipant();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.JID = r.string();
                            break;
                        }
                    case 2: {
                            m.callOutcome = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CallParticipant.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.CallLogMessage.CallParticipant)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.CallLogMessage.CallParticipant();
                if (d.JID != null) {
                    m.JID = String(d.JID);
                }
                switch (d.callOutcome) {
                default:
                    if (typeof d.callOutcome === "number") {
                        m.callOutcome = d.callOutcome;
                        break;
                    }
                    break;
                case "CONNECTED":
                case 0:
                    m.callOutcome = 0;
                    break;
                case "MISSED":
                case 1:
                    m.callOutcome = 1;
                    break;
                case "FAILED":
                case 2:
                    m.callOutcome = 2;
                    break;
                case "REJECTED":
                case 3:
                    m.callOutcome = 3;
                    break;
                case "ACCEPTED_ELSEWHERE":
                case 4:
                    m.callOutcome = 4;
                    break;
                case "ONGOING":
                case 5:
                    m.callOutcome = 5;
                    break;
                case "SILENCED_BY_DND":
                case 6:
                    m.callOutcome = 6;
                    break;
                case "SILENCED_UNKNOWN_CALLER":
                case 7:
                    m.callOutcome = 7;
                    break;
                }
                return m;
            };

            CallParticipant.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.JID != null && m.hasOwnProperty("JID")) {
                    d.JID = m.JID;
                    if (o.oneofs)
                        d._JID = "JID";
                }
                if (m.callOutcome != null && m.hasOwnProperty("callOutcome")) {
                    d.callOutcome = o.enums === String ? WAE2E.CallLogMessage.CallOutcome[m.callOutcome] === undefined ? m.callOutcome : WAE2E.CallLogMessage.CallOutcome[m.callOutcome] : m.callOutcome;
                    if (o.oneofs)
                        d._callOutcome = "callOutcome";
                }
                return d;
            };

            CallParticipant.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.CallLogMessage.CallParticipant";
            };

            return CallParticipant;
        })();

        return CallLogMessage;
    })();

    WAE2E.ScheduledCallEditMessage = (function() {

        function ScheduledCallEditMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ScheduledCallEditMessage.prototype.key = null;
        ScheduledCallEditMessage.prototype.editType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledCallEditMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledCallEditMessage.prototype, "_editType", {
            get: $util.oneOfGetter($oneOfFields = ["editType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ScheduledCallEditMessage.create = function create(properties) {
            return new ScheduledCallEditMessage(properties);
        };

        ScheduledCallEditMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.editType != null && Object.hasOwnProperty.call(m, "editType"))
                w.uint32(16).int32(m.editType);
            return w;
        };

        ScheduledCallEditMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ScheduledCallEditMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.editType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ScheduledCallEditMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ScheduledCallEditMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ScheduledCallEditMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.ScheduledCallEditMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            switch (d.editType) {
            default:
                if (typeof d.editType === "number") {
                    m.editType = d.editType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.editType = 0;
                break;
            case "CANCEL":
            case 1:
                m.editType = 1;
                break;
            }
            return m;
        };

        ScheduledCallEditMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.editType != null && m.hasOwnProperty("editType")) {
                d.editType = o.enums === String ? WAE2E.ScheduledCallEditMessage.EditType[m.editType] === undefined ? m.editType : WAE2E.ScheduledCallEditMessage.EditType[m.editType] : m.editType;
                if (o.oneofs)
                    d._editType = "editType";
            }
            return d;
        };

        ScheduledCallEditMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ScheduledCallEditMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ScheduledCallEditMessage";
        };

        ScheduledCallEditMessage.EditType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CANCEL"] = 1;
            return values;
        })();

        return ScheduledCallEditMessage;
    })();

    WAE2E.ScheduledCallCreationMessage = (function() {

        function ScheduledCallCreationMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ScheduledCallCreationMessage.prototype.scheduledTimestampMS = null;
        ScheduledCallCreationMessage.prototype.callType = null;
        ScheduledCallCreationMessage.prototype.title = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledCallCreationMessage.prototype, "_scheduledTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledCallCreationMessage.prototype, "_callType", {
            get: $util.oneOfGetter($oneOfFields = ["callType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ScheduledCallCreationMessage.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ScheduledCallCreationMessage.create = function create(properties) {
            return new ScheduledCallCreationMessage(properties);
        };

        ScheduledCallCreationMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.scheduledTimestampMS != null && Object.hasOwnProperty.call(m, "scheduledTimestampMS"))
                w.uint32(8).int64(m.scheduledTimestampMS);
            if (m.callType != null && Object.hasOwnProperty.call(m, "callType"))
                w.uint32(16).int32(m.callType);
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(26).string(m.title);
            return w;
        };

        ScheduledCallCreationMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ScheduledCallCreationMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.scheduledTimestampMS = r.int64();
                        break;
                    }
                case 2: {
                        m.callType = r.int32();
                        break;
                    }
                case 3: {
                        m.title = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ScheduledCallCreationMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ScheduledCallCreationMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ScheduledCallCreationMessage();
            if (d.scheduledTimestampMS != null) {
                if ($util.Long)
                    (m.scheduledTimestampMS = $util.Long.fromValue(d.scheduledTimestampMS)).unsigned = false;
                else if (typeof d.scheduledTimestampMS === "string")
                    m.scheduledTimestampMS = parseInt(d.scheduledTimestampMS, 10);
                else if (typeof d.scheduledTimestampMS === "number")
                    m.scheduledTimestampMS = d.scheduledTimestampMS;
                else if (typeof d.scheduledTimestampMS === "object")
                    m.scheduledTimestampMS = new $util.LongBits(d.scheduledTimestampMS.low >>> 0, d.scheduledTimestampMS.high >>> 0).toNumber();
            }
            switch (d.callType) {
            default:
                if (typeof d.callType === "number") {
                    m.callType = d.callType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.callType = 0;
                break;
            case "VOICE":
            case 1:
                m.callType = 1;
                break;
            case "VIDEO":
            case 2:
                m.callType = 2;
                break;
            }
            if (d.title != null) {
                m.title = String(d.title);
            }
            return m;
        };

        ScheduledCallCreationMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.scheduledTimestampMS != null && m.hasOwnProperty("scheduledTimestampMS")) {
                if (typeof m.scheduledTimestampMS === "number")
                    d.scheduledTimestampMS = o.longs === String ? String(m.scheduledTimestampMS) : m.scheduledTimestampMS;
                else
                    d.scheduledTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.scheduledTimestampMS) : o.longs === Number ? new $util.LongBits(m.scheduledTimestampMS.low >>> 0, m.scheduledTimestampMS.high >>> 0).toNumber() : m.scheduledTimestampMS;
                if (o.oneofs)
                    d._scheduledTimestampMS = "scheduledTimestampMS";
            }
            if (m.callType != null && m.hasOwnProperty("callType")) {
                d.callType = o.enums === String ? WAE2E.ScheduledCallCreationMessage.CallType[m.callType] === undefined ? m.callType : WAE2E.ScheduledCallCreationMessage.CallType[m.callType] : m.callType;
                if (o.oneofs)
                    d._callType = "callType";
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            return d;
        };

        ScheduledCallCreationMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ScheduledCallCreationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ScheduledCallCreationMessage";
        };

        ScheduledCallCreationMessage.CallType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "VOICE"] = 1;
            values[valuesById[2] = "VIDEO"] = 2;
            return values;
        })();

        return ScheduledCallCreationMessage;
    })();

    WAE2E.EventResponseMessage = (function() {

        function EventResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EventResponseMessage.prototype.response = null;
        EventResponseMessage.prototype.timestampMS = null;
        EventResponseMessage.prototype.extraGuestCount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponseMessage.prototype, "_response", {
            get: $util.oneOfGetter($oneOfFields = ["response"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponseMessage.prototype, "_timestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventResponseMessage.prototype, "_extraGuestCount", {
            get: $util.oneOfGetter($oneOfFields = ["extraGuestCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventResponseMessage.create = function create(properties) {
            return new EventResponseMessage(properties);
        };

        EventResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.response != null && Object.hasOwnProperty.call(m, "response"))
                w.uint32(8).int32(m.response);
            if (m.timestampMS != null && Object.hasOwnProperty.call(m, "timestampMS"))
                w.uint32(16).int64(m.timestampMS);
            if (m.extraGuestCount != null && Object.hasOwnProperty.call(m, "extraGuestCount"))
                w.uint32(24).int32(m.extraGuestCount);
            return w;
        };

        EventResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EventResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.response = r.int32();
                        break;
                    }
                case 2: {
                        m.timestampMS = r.int64();
                        break;
                    }
                case 3: {
                        m.extraGuestCount = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EventResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EventResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EventResponseMessage();
            switch (d.response) {
            default:
                if (typeof d.response === "number") {
                    m.response = d.response;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.response = 0;
                break;
            case "GOING":
            case 1:
                m.response = 1;
                break;
            case "NOT_GOING":
            case 2:
                m.response = 2;
                break;
            case "MAYBE":
            case 3:
                m.response = 3;
                break;
            }
            if (d.timestampMS != null) {
                if ($util.Long)
                    (m.timestampMS = $util.Long.fromValue(d.timestampMS)).unsigned = false;
                else if (typeof d.timestampMS === "string")
                    m.timestampMS = parseInt(d.timestampMS, 10);
                else if (typeof d.timestampMS === "number")
                    m.timestampMS = d.timestampMS;
                else if (typeof d.timestampMS === "object")
                    m.timestampMS = new $util.LongBits(d.timestampMS.low >>> 0, d.timestampMS.high >>> 0).toNumber();
            }
            if (d.extraGuestCount != null) {
                m.extraGuestCount = d.extraGuestCount | 0;
            }
            return m;
        };

        EventResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.response != null && m.hasOwnProperty("response")) {
                d.response = o.enums === String ? WAE2E.EventResponseMessage.EventResponseType[m.response] === undefined ? m.response : WAE2E.EventResponseMessage.EventResponseType[m.response] : m.response;
                if (o.oneofs)
                    d._response = "response";
            }
            if (m.timestampMS != null && m.hasOwnProperty("timestampMS")) {
                if (typeof m.timestampMS === "number")
                    d.timestampMS = o.longs === String ? String(m.timestampMS) : m.timestampMS;
                else
                    d.timestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.timestampMS) : o.longs === Number ? new $util.LongBits(m.timestampMS.low >>> 0, m.timestampMS.high >>> 0).toNumber() : m.timestampMS;
                if (o.oneofs)
                    d._timestampMS = "timestampMS";
            }
            if (m.extraGuestCount != null && m.hasOwnProperty("extraGuestCount")) {
                d.extraGuestCount = m.extraGuestCount;
                if (o.oneofs)
                    d._extraGuestCount = "extraGuestCount";
            }
            return d;
        };

        EventResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EventResponseMessage";
        };

        EventResponseMessage.EventResponseType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "GOING"] = 1;
            values[valuesById[2] = "NOT_GOING"] = 2;
            values[valuesById[3] = "MAYBE"] = 3;
            return values;
        })();

        return EventResponseMessage;
    })();

    WAE2E.PinInChatMessage = (function() {

        function PinInChatMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PinInChatMessage.prototype.key = null;
        PinInChatMessage.prototype.type = null;
        PinInChatMessage.prototype.senderTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChatMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChatMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PinInChatMessage.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PinInChatMessage.create = function create(properties) {
            return new PinInChatMessage(properties);
        };

        PinInChatMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(16).int32(m.type);
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(24).int64(m.senderTimestampMS);
            return w;
        };

        PinInChatMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PinInChatMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.type = r.int32();
                        break;
                    }
                case 3: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PinInChatMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PinInChatMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PinInChatMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.PinInChatMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                m.type = 0;
                break;
            case "PIN_FOR_ALL":
            case 1:
                m.type = 1;
                break;
            case "UNPIN_FOR_ALL":
            case 2:
                m.type = 2;
                break;
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        PinInChatMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.PinInChatMessage.Type[m.type] === undefined ? m.type : WAE2E.PinInChatMessage.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            return d;
        };

        PinInChatMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PinInChatMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PinInChatMessage";
        };

        PinInChatMessage.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "PIN_FOR_ALL"] = 1;
            values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
            return values;
        })();

        return PinInChatMessage;
    })();

    WAE2E.StatusStickerInteractionMessage = (function() {

        function StatusStickerInteractionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusStickerInteractionMessage.prototype.key = null;
        StatusStickerInteractionMessage.prototype.stickerKey = null;
        StatusStickerInteractionMessage.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusStickerInteractionMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusStickerInteractionMessage.prototype, "_stickerKey", {
            get: $util.oneOfGetter($oneOfFields = ["stickerKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusStickerInteractionMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusStickerInteractionMessage.create = function create(properties) {
            return new StatusStickerInteractionMessage(properties);
        };

        StatusStickerInteractionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.stickerKey != null && Object.hasOwnProperty.call(m, "stickerKey"))
                w.uint32(18).string(m.stickerKey);
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(24).int32(m.type);
            return w;
        };

        StatusStickerInteractionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StatusStickerInteractionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.stickerKey = r.string();
                        break;
                    }
                case 3: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusStickerInteractionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StatusStickerInteractionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StatusStickerInteractionMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.StatusStickerInteractionMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.stickerKey != null) {
                m.stickerKey = String(d.stickerKey);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "REACTION":
            case 1:
                m.type = 1;
                break;
            }
            return m;
        };

        StatusStickerInteractionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.stickerKey != null && m.hasOwnProperty("stickerKey")) {
                d.stickerKey = m.stickerKey;
                if (o.oneofs)
                    d._stickerKey = "stickerKey";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.StatusStickerInteractionMessage.StatusStickerType[m.type] === undefined ? m.type : WAE2E.StatusStickerInteractionMessage.StatusStickerType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        StatusStickerInteractionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusStickerInteractionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StatusStickerInteractionMessage";
        };

        StatusStickerInteractionMessage.StatusStickerType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "REACTION"] = 1;
            return values;
        })();

        return StatusStickerInteractionMessage;
    })();

    WAE2E.ButtonsResponseMessage = (function() {

        function ButtonsResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ButtonsResponseMessage.prototype.selectedDisplayText = null;
        ButtonsResponseMessage.prototype.selectedButtonID = null;
        ButtonsResponseMessage.prototype.contextInfo = null;
        ButtonsResponseMessage.prototype.type = null;

        let $oneOfFields;

        Object.defineProperty(ButtonsResponseMessage.prototype, "response", {
            get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsResponseMessage.prototype, "_selectedButtonID", {
            get: $util.oneOfGetter($oneOfFields = ["selectedButtonID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsResponseMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ButtonsResponseMessage.create = function create(properties) {
            return new ButtonsResponseMessage(properties);
        };

        ButtonsResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.selectedButtonID != null && Object.hasOwnProperty.call(m, "selectedButtonID"))
                w.uint32(10).string(m.selectedButtonID);
            if (m.selectedDisplayText != null && Object.hasOwnProperty.call(m, "selectedDisplayText"))
                w.uint32(18).string(m.selectedDisplayText);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(26).fork()).ldelim();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(32).int32(m.type);
            return w;
        };

        ButtonsResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ButtonsResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.selectedDisplayText = r.string();
                        break;
                    }
                case 1: {
                        m.selectedButtonID = r.string();
                        break;
                    }
                case 3: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ButtonsResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ButtonsResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ButtonsResponseMessage();
            if (d.selectedDisplayText != null) {
                m.selectedDisplayText = String(d.selectedDisplayText);
            }
            if (d.selectedButtonID != null) {
                m.selectedButtonID = String(d.selectedButtonID);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ButtonsResponseMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "DISPLAY_TEXT":
            case 1:
                m.type = 1;
                break;
            }
            return m;
        };

        ButtonsResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.selectedButtonID != null && m.hasOwnProperty("selectedButtonID")) {
                d.selectedButtonID = m.selectedButtonID;
                if (o.oneofs)
                    d._selectedButtonID = "selectedButtonID";
            }
            if (m.selectedDisplayText != null && m.hasOwnProperty("selectedDisplayText")) {
                d.selectedDisplayText = m.selectedDisplayText;
                if (o.oneofs)
                    d.response = "selectedDisplayText";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.ButtonsResponseMessage.Type[m.type] === undefined ? m.type : WAE2E.ButtonsResponseMessage.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        ButtonsResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ButtonsResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ButtonsResponseMessage";
        };

        ButtonsResponseMessage.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "DISPLAY_TEXT"] = 1;
            return values;
        })();

        return ButtonsResponseMessage;
    })();

    WAE2E.ButtonsMessage = (function() {

        function ButtonsMessage(p) {
            this.buttons = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ButtonsMessage.prototype.text = null;
        ButtonsMessage.prototype.documentMessage = null;
        ButtonsMessage.prototype.imageMessage = null;
        ButtonsMessage.prototype.videoMessage = null;
        ButtonsMessage.prototype.locationMessage = null;
        ButtonsMessage.prototype.contentText = null;
        ButtonsMessage.prototype.footerText = null;
        ButtonsMessage.prototype.contextInfo = null;
        ButtonsMessage.prototype.buttons = $util.emptyArray;
        ButtonsMessage.prototype.headerType = null;

        let $oneOfFields;

        Object.defineProperty(ButtonsMessage.prototype, "header", {
            get: $util.oneOfGetter($oneOfFields = ["text", "documentMessage", "imageMessage", "videoMessage", "locationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsMessage.prototype, "_contentText", {
            get: $util.oneOfGetter($oneOfFields = ["contentText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsMessage.prototype, "_footerText", {
            get: $util.oneOfGetter($oneOfFields = ["footerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ButtonsMessage.prototype, "_headerType", {
            get: $util.oneOfGetter($oneOfFields = ["headerType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ButtonsMessage.create = function create(properties) {
            return new ButtonsMessage(properties);
        };

        ButtonsMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(10).string(m.text);
            if (m.documentMessage != null && Object.hasOwnProperty.call(m, "documentMessage"))
                WAE2E.DocumentMessage.encode(m.documentMessage, w.uint32(18).fork()).ldelim();
            if (m.imageMessage != null && Object.hasOwnProperty.call(m, "imageMessage"))
                WAE2E.ImageMessage.encode(m.imageMessage, w.uint32(26).fork()).ldelim();
            if (m.videoMessage != null && Object.hasOwnProperty.call(m, "videoMessage"))
                WAE2E.VideoMessage.encode(m.videoMessage, w.uint32(34).fork()).ldelim();
            if (m.locationMessage != null && Object.hasOwnProperty.call(m, "locationMessage"))
                WAE2E.LocationMessage.encode(m.locationMessage, w.uint32(42).fork()).ldelim();
            if (m.contentText != null && Object.hasOwnProperty.call(m, "contentText"))
                w.uint32(50).string(m.contentText);
            if (m.footerText != null && Object.hasOwnProperty.call(m, "footerText"))
                w.uint32(58).string(m.footerText);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(66).fork()).ldelim();
            if (m.buttons != null && m.buttons.length) {
                for (var i = 0; i < m.buttons.length; ++i)
                    WAE2E.ButtonsMessage.Button.encode(m.buttons[i], w.uint32(74).fork()).ldelim();
            }
            if (m.headerType != null && Object.hasOwnProperty.call(m, "headerType"))
                w.uint32(80).int32(m.headerType);
            return w;
        };

        ButtonsMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ButtonsMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.text = r.string();
                        break;
                    }
                case 2: {
                        m.documentMessage = WAE2E.DocumentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.imageMessage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.videoMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.locationMessage = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.contentText = r.string();
                        break;
                    }
                case 7: {
                        m.footerText = r.string();
                        break;
                    }
                case 8: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        if (!(m.buttons && m.buttons.length))
                            m.buttons = [];
                        m.buttons.push(WAE2E.ButtonsMessage.Button.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 10: {
                        m.headerType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ButtonsMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ButtonsMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ButtonsMessage();
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.documentMessage != null) {
                if (typeof d.documentMessage !== "object")
                    throw TypeError(".WAE2E.ButtonsMessage.documentMessage: object expected");
                m.documentMessage = WAE2E.DocumentMessage.fromObject(d.documentMessage, n + 1);
            }
            if (d.imageMessage != null) {
                if (typeof d.imageMessage !== "object")
                    throw TypeError(".WAE2E.ButtonsMessage.imageMessage: object expected");
                m.imageMessage = WAE2E.ImageMessage.fromObject(d.imageMessage, n + 1);
            }
            if (d.videoMessage != null) {
                if (typeof d.videoMessage !== "object")
                    throw TypeError(".WAE2E.ButtonsMessage.videoMessage: object expected");
                m.videoMessage = WAE2E.VideoMessage.fromObject(d.videoMessage, n + 1);
            }
            if (d.locationMessage != null) {
                if (typeof d.locationMessage !== "object")
                    throw TypeError(".WAE2E.ButtonsMessage.locationMessage: object expected");
                m.locationMessage = WAE2E.LocationMessage.fromObject(d.locationMessage, n + 1);
            }
            if (d.contentText != null) {
                m.contentText = String(d.contentText);
            }
            if (d.footerText != null) {
                m.footerText = String(d.footerText);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ButtonsMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.buttons) {
                if (!Array.isArray(d.buttons))
                    throw TypeError(".WAE2E.ButtonsMessage.buttons: array expected");
                m.buttons = [];
                for (var i = 0; i < d.buttons.length; ++i) {
                    if (typeof d.buttons[i] !== "object")
                        throw TypeError(".WAE2E.ButtonsMessage.buttons: object expected");
                    m.buttons[i] = WAE2E.ButtonsMessage.Button.fromObject(d.buttons[i], n + 1);
                }
            }
            switch (d.headerType) {
            default:
                if (typeof d.headerType === "number") {
                    m.headerType = d.headerType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.headerType = 0;
                break;
            case "EMPTY":
            case 1:
                m.headerType = 1;
                break;
            case "TEXT":
            case 2:
                m.headerType = 2;
                break;
            case "DOCUMENT":
            case 3:
                m.headerType = 3;
                break;
            case "IMAGE":
            case 4:
                m.headerType = 4;
                break;
            case "VIDEO":
            case 5:
                m.headerType = 5;
                break;
            case "LOCATION":
            case 6:
                m.headerType = 6;
                break;
            }
            return m;
        };

        ButtonsMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.buttons = [];
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d.header = "text";
            }
            if (m.documentMessage != null && m.hasOwnProperty("documentMessage")) {
                d.documentMessage = WAE2E.DocumentMessage.toObject(m.documentMessage, o);
                if (o.oneofs)
                    d.header = "documentMessage";
            }
            if (m.imageMessage != null && m.hasOwnProperty("imageMessage")) {
                d.imageMessage = WAE2E.ImageMessage.toObject(m.imageMessage, o);
                if (o.oneofs)
                    d.header = "imageMessage";
            }
            if (m.videoMessage != null && m.hasOwnProperty("videoMessage")) {
                d.videoMessage = WAE2E.VideoMessage.toObject(m.videoMessage, o);
                if (o.oneofs)
                    d.header = "videoMessage";
            }
            if (m.locationMessage != null && m.hasOwnProperty("locationMessage")) {
                d.locationMessage = WAE2E.LocationMessage.toObject(m.locationMessage, o);
                if (o.oneofs)
                    d.header = "locationMessage";
            }
            if (m.contentText != null && m.hasOwnProperty("contentText")) {
                d.contentText = m.contentText;
                if (o.oneofs)
                    d._contentText = "contentText";
            }
            if (m.footerText != null && m.hasOwnProperty("footerText")) {
                d.footerText = m.footerText;
                if (o.oneofs)
                    d._footerText = "footerText";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.buttons && m.buttons.length) {
                d.buttons = [];
                for (var j = 0; j < m.buttons.length; ++j) {
                    d.buttons[j] = WAE2E.ButtonsMessage.Button.toObject(m.buttons[j], o);
                }
            }
            if (m.headerType != null && m.hasOwnProperty("headerType")) {
                d.headerType = o.enums === String ? WAE2E.ButtonsMessage.HeaderType[m.headerType] === undefined ? m.headerType : WAE2E.ButtonsMessage.HeaderType[m.headerType] : m.headerType;
                if (o.oneofs)
                    d._headerType = "headerType";
            }
            return d;
        };

        ButtonsMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ButtonsMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ButtonsMessage";
        };

        ButtonsMessage.HeaderType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "EMPTY"] = 1;
            values[valuesById[2] = "TEXT"] = 2;
            values[valuesById[3] = "DOCUMENT"] = 3;
            values[valuesById[4] = "IMAGE"] = 4;
            values[valuesById[5] = "VIDEO"] = 5;
            values[valuesById[6] = "LOCATION"] = 6;
            return values;
        })();

        ButtonsMessage.Button = (function() {

            function Button(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Button.prototype.buttonID = null;
            Button.prototype.buttonText = null;
            Button.prototype.type = null;
            Button.prototype.nativeFlowInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Button.prototype, "_buttonID", {
                get: $util.oneOfGetter($oneOfFields = ["buttonID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Button.prototype, "_buttonText", {
                get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Button.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Button.prototype, "_nativeFlowInfo", {
                get: $util.oneOfGetter($oneOfFields = ["nativeFlowInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Button.create = function create(properties) {
                return new Button(properties);
            };

            Button.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.buttonID != null && Object.hasOwnProperty.call(m, "buttonID"))
                    w.uint32(10).string(m.buttonID);
                if (m.buttonText != null && Object.hasOwnProperty.call(m, "buttonText"))
                    WAE2E.ButtonsMessage.Button.ButtonText.encode(m.buttonText, w.uint32(18).fork()).ldelim();
                if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                    w.uint32(24).int32(m.type);
                if (m.nativeFlowInfo != null && Object.hasOwnProperty.call(m, "nativeFlowInfo"))
                    WAE2E.ButtonsMessage.Button.NativeFlowInfo.encode(m.nativeFlowInfo, w.uint32(34).fork()).ldelim();
                return w;
            };

            Button.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ButtonsMessage.Button();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.buttonID = r.string();
                            break;
                        }
                    case 2: {
                            m.buttonText = WAE2E.ButtonsMessage.Button.ButtonText.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.type = r.int32();
                            break;
                        }
                    case 4: {
                            m.nativeFlowInfo = WAE2E.ButtonsMessage.Button.NativeFlowInfo.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Button.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ButtonsMessage.Button)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ButtonsMessage.Button();
                if (d.buttonID != null) {
                    m.buttonID = String(d.buttonID);
                }
                if (d.buttonText != null) {
                    if (typeof d.buttonText !== "object")
                        throw TypeError(".WAE2E.ButtonsMessage.Button.buttonText: object expected");
                    m.buttonText = WAE2E.ButtonsMessage.Button.ButtonText.fromObject(d.buttonText, n + 1);
                }
                switch (d.type) {
                default:
                    if (typeof d.type === "number") {
                        m.type = d.type;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "RESPONSE":
                case 1:
                    m.type = 1;
                    break;
                case "NATIVE_FLOW":
                case 2:
                    m.type = 2;
                    break;
                }
                if (d.nativeFlowInfo != null) {
                    if (typeof d.nativeFlowInfo !== "object")
                        throw TypeError(".WAE2E.ButtonsMessage.Button.nativeFlowInfo: object expected");
                    m.nativeFlowInfo = WAE2E.ButtonsMessage.Button.NativeFlowInfo.fromObject(d.nativeFlowInfo, n + 1);
                }
                return m;
            };

            Button.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.buttonID != null && m.hasOwnProperty("buttonID")) {
                    d.buttonID = m.buttonID;
                    if (o.oneofs)
                        d._buttonID = "buttonID";
                }
                if (m.buttonText != null && m.hasOwnProperty("buttonText")) {
                    d.buttonText = WAE2E.ButtonsMessage.Button.ButtonText.toObject(m.buttonText, o);
                    if (o.oneofs)
                        d._buttonText = "buttonText";
                }
                if (m.type != null && m.hasOwnProperty("type")) {
                    d.type = o.enums === String ? WAE2E.ButtonsMessage.Button.Type[m.type] === undefined ? m.type : WAE2E.ButtonsMessage.Button.Type[m.type] : m.type;
                    if (o.oneofs)
                        d._type = "type";
                }
                if (m.nativeFlowInfo != null && m.hasOwnProperty("nativeFlowInfo")) {
                    d.nativeFlowInfo = WAE2E.ButtonsMessage.Button.NativeFlowInfo.toObject(m.nativeFlowInfo, o);
                    if (o.oneofs)
                        d._nativeFlowInfo = "nativeFlowInfo";
                }
                return d;
            };

            Button.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Button.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ButtonsMessage.Button";
            };

            Button.Type = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "RESPONSE"] = 1;
                values[valuesById[2] = "NATIVE_FLOW"] = 2;
                return values;
            })();

            Button.NativeFlowInfo = (function() {

                function NativeFlowInfo(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                NativeFlowInfo.prototype.name = null;
                NativeFlowInfo.prototype.paramsJSON = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowInfo.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowInfo.prototype, "_paramsJSON", {
                    get: $util.oneOfGetter($oneOfFields = ["paramsJSON"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                NativeFlowInfo.create = function create(properties) {
                    return new NativeFlowInfo(properties);
                };

                NativeFlowInfo.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                        w.uint32(10).string(m.name);
                    if (m.paramsJSON != null && Object.hasOwnProperty.call(m, "paramsJSON"))
                        w.uint32(18).string(m.paramsJSON);
                    return w;
                };

                NativeFlowInfo.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ButtonsMessage.Button.NativeFlowInfo();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.name = r.string();
                                break;
                            }
                        case 2: {
                                m.paramsJSON = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                NativeFlowInfo.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.ButtonsMessage.Button.NativeFlowInfo)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.ButtonsMessage.Button.NativeFlowInfo();
                    if (d.name != null) {
                        m.name = String(d.name);
                    }
                    if (d.paramsJSON != null) {
                        m.paramsJSON = String(d.paramsJSON);
                    }
                    return m;
                };

                NativeFlowInfo.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.name != null && m.hasOwnProperty("name")) {
                        d.name = m.name;
                        if (o.oneofs)
                            d._name = "name";
                    }
                    if (m.paramsJSON != null && m.hasOwnProperty("paramsJSON")) {
                        d.paramsJSON = m.paramsJSON;
                        if (o.oneofs)
                            d._paramsJSON = "paramsJSON";
                    }
                    return d;
                };

                NativeFlowInfo.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                NativeFlowInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.ButtonsMessage.Button.NativeFlowInfo";
                };

                return NativeFlowInfo;
            })();

            Button.ButtonText = (function() {

                function ButtonText(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                ButtonText.prototype.displayText = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(ButtonText.prototype, "_displayText", {
                    get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ButtonText.create = function create(properties) {
                    return new ButtonText(properties);
                };

                ButtonText.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                        w.uint32(10).string(m.displayText);
                    return w;
                };

                ButtonText.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ButtonsMessage.Button.ButtonText();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.displayText = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                ButtonText.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.ButtonsMessage.Button.ButtonText)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.ButtonsMessage.Button.ButtonText();
                    if (d.displayText != null) {
                        m.displayText = String(d.displayText);
                    }
                    return m;
                };

                ButtonText.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.displayText != null && m.hasOwnProperty("displayText")) {
                        d.displayText = m.displayText;
                        if (o.oneofs)
                            d._displayText = "displayText";
                    }
                    return d;
                };

                ButtonText.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                ButtonText.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.ButtonsMessage.Button.ButtonText";
                };

                return ButtonText;
            })();

            return Button;
        })();

        return ButtonsMessage;
    })();

    WAE2E.ConditionalRevealMessage = (function() {

        function ConditionalRevealMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ConditionalRevealMessage.prototype.encPayload = null;
        ConditionalRevealMessage.prototype.encIV = null;
        ConditionalRevealMessage.prototype.conditionalRevealMessageType = null;
        ConditionalRevealMessage.prototype.revealKeyID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ConditionalRevealMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ConditionalRevealMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ConditionalRevealMessage.prototype, "_conditionalRevealMessageType", {
            get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ConditionalRevealMessage.prototype, "_revealKeyID", {
            get: $util.oneOfGetter($oneOfFields = ["revealKeyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ConditionalRevealMessage.create = function create(properties) {
            return new ConditionalRevealMessage(properties);
        };

        ConditionalRevealMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(10).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(18).bytes(m.encIV);
            if (m.conditionalRevealMessageType != null && Object.hasOwnProperty.call(m, "conditionalRevealMessageType"))
                w.uint32(24).int32(m.conditionalRevealMessageType);
            if (m.revealKeyID != null && Object.hasOwnProperty.call(m, "revealKeyID"))
                w.uint32(34).string(m.revealKeyID);
            return w;
        };

        ConditionalRevealMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ConditionalRevealMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 2: {
                        m.encIV = r.bytes();
                        break;
                    }
                case 3: {
                        m.conditionalRevealMessageType = r.int32();
                        break;
                    }
                case 4: {
                        m.revealKeyID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ConditionalRevealMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ConditionalRevealMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ConditionalRevealMessage();
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            switch (d.conditionalRevealMessageType) {
            default:
                if (typeof d.conditionalRevealMessageType === "number") {
                    m.conditionalRevealMessageType = d.conditionalRevealMessageType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.conditionalRevealMessageType = 0;
                break;
            case "SCHEDULED_MESSAGE":
            case 1:
                m.conditionalRevealMessageType = 1;
                break;
            }
            if (d.revealKeyID != null) {
                m.revealKeyID = String(d.revealKeyID);
            }
            return m;
        };

        ConditionalRevealMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            if (m.conditionalRevealMessageType != null && m.hasOwnProperty("conditionalRevealMessageType")) {
                d.conditionalRevealMessageType = o.enums === String ? WAE2E.ConditionalRevealMessage.ConditionalRevealMessageType[m.conditionalRevealMessageType] === undefined ? m.conditionalRevealMessageType : WAE2E.ConditionalRevealMessage.ConditionalRevealMessageType[m.conditionalRevealMessageType] : m.conditionalRevealMessageType;
                if (o.oneofs)
                    d._conditionalRevealMessageType = "conditionalRevealMessageType";
            }
            if (m.revealKeyID != null && m.hasOwnProperty("revealKeyID")) {
                d.revealKeyID = m.revealKeyID;
                if (o.oneofs)
                    d._revealKeyID = "revealKeyID";
            }
            return d;
        };

        ConditionalRevealMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ConditionalRevealMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ConditionalRevealMessage";
        };

        ConditionalRevealMessage.ConditionalRevealMessageType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "SCHEDULED_MESSAGE"] = 1;
            return values;
        })();

        return ConditionalRevealMessage;
    })();

    WAE2E.SecretEncryptedMessage = (function() {

        function SecretEncryptedMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SecretEncryptedMessage.prototype.targetMessageKey = null;
        SecretEncryptedMessage.prototype.encPayload = null;
        SecretEncryptedMessage.prototype.encIV = null;
        SecretEncryptedMessage.prototype.secretEncType = null;
        SecretEncryptedMessage.prototype.remoteKeyID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SecretEncryptedMessage.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SecretEncryptedMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SecretEncryptedMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SecretEncryptedMessage.prototype, "_secretEncType", {
            get: $util.oneOfGetter($oneOfFields = ["secretEncType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SecretEncryptedMessage.prototype, "_remoteKeyID", {
            get: $util.oneOfGetter($oneOfFields = ["remoteKeyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SecretEncryptedMessage.create = function create(properties) {
            return new SecretEncryptedMessage(properties);
        };

        SecretEncryptedMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.targetMessageKey != null && Object.hasOwnProperty.call(m, "targetMessageKey"))
                WACommon.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork()).ldelim();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(18).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(26).bytes(m.encIV);
            if (m.secretEncType != null && Object.hasOwnProperty.call(m, "secretEncType"))
                w.uint32(32).int32(m.secretEncType);
            if (m.remoteKeyID != null && Object.hasOwnProperty.call(m, "remoteKeyID"))
                w.uint32(42).string(m.remoteKeyID);
            return w;
        };

        SecretEncryptedMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.SecretEncryptedMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.targetMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 3: {
                        m.encIV = r.bytes();
                        break;
                    }
                case 4: {
                        m.secretEncType = r.int32();
                        break;
                    }
                case 5: {
                        m.remoteKeyID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SecretEncryptedMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.SecretEncryptedMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.SecretEncryptedMessage();
            if (d.targetMessageKey != null) {
                if (typeof d.targetMessageKey !== "object")
                    throw TypeError(".WAE2E.SecretEncryptedMessage.targetMessageKey: object expected");
                m.targetMessageKey = WACommon.MessageKey.fromObject(d.targetMessageKey, n + 1);
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            switch (d.secretEncType) {
            default:
                if (typeof d.secretEncType === "number") {
                    m.secretEncType = d.secretEncType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.secretEncType = 0;
                break;
            case "EVENT_EDIT":
            case 1:
                m.secretEncType = 1;
                break;
            case "MESSAGE_EDIT":
            case 2:
                m.secretEncType = 2;
                break;
            case "MESSAGE_SCHEDULE":
            case 3:
                m.secretEncType = 3;
                break;
            case "POLL_EDIT":
            case 4:
                m.secretEncType = 4;
                break;
            case "POLL_ADD_OPTION":
            case 5:
                m.secretEncType = 5;
                break;
            }
            if (d.remoteKeyID != null) {
                m.remoteKeyID = String(d.remoteKeyID);
            }
            return m;
        };

        SecretEncryptedMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.targetMessageKey != null && m.hasOwnProperty("targetMessageKey")) {
                d.targetMessageKey = WACommon.MessageKey.toObject(m.targetMessageKey, o);
                if (o.oneofs)
                    d._targetMessageKey = "targetMessageKey";
            }
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            if (m.secretEncType != null && m.hasOwnProperty("secretEncType")) {
                d.secretEncType = o.enums === String ? WAE2E.SecretEncryptedMessage.SecretEncType[m.secretEncType] === undefined ? m.secretEncType : WAE2E.SecretEncryptedMessage.SecretEncType[m.secretEncType] : m.secretEncType;
                if (o.oneofs)
                    d._secretEncType = "secretEncType";
            }
            if (m.remoteKeyID != null && m.hasOwnProperty("remoteKeyID")) {
                d.remoteKeyID = m.remoteKeyID;
                if (o.oneofs)
                    d._remoteKeyID = "remoteKeyID";
            }
            return d;
        };

        SecretEncryptedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SecretEncryptedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.SecretEncryptedMessage";
        };

        SecretEncryptedMessage.SecretEncType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "EVENT_EDIT"] = 1;
            values[valuesById[2] = "MESSAGE_EDIT"] = 2;
            values[valuesById[3] = "MESSAGE_SCHEDULE"] = 3;
            values[valuesById[4] = "POLL_EDIT"] = 4;
            values[valuesById[5] = "POLL_ADD_OPTION"] = 5;
            return values;
        })();

        return SecretEncryptedMessage;
    })();

    WAE2E.GroupInviteMessage = (function() {

        function GroupInviteMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupInviteMessage.prototype.groupJID = null;
        GroupInviteMessage.prototype.inviteCode = null;
        GroupInviteMessage.prototype.inviteExpiration = null;
        GroupInviteMessage.prototype.groupName = null;
        GroupInviteMessage.prototype.JPEGThumbnail = null;
        GroupInviteMessage.prototype.caption = null;
        GroupInviteMessage.prototype.contextInfo = null;
        GroupInviteMessage.prototype.groupType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_groupJID", {
            get: $util.oneOfGetter($oneOfFields = ["groupJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_inviteCode", {
            get: $util.oneOfGetter($oneOfFields = ["inviteCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_inviteExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_groupName", {
            get: $util.oneOfGetter($oneOfFields = ["groupName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupInviteMessage.prototype, "_groupType", {
            get: $util.oneOfGetter($oneOfFields = ["groupType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupInviteMessage.create = function create(properties) {
            return new GroupInviteMessage(properties);
        };

        GroupInviteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.groupJID != null && Object.hasOwnProperty.call(m, "groupJID"))
                w.uint32(10).string(m.groupJID);
            if (m.inviteCode != null && Object.hasOwnProperty.call(m, "inviteCode"))
                w.uint32(18).string(m.inviteCode);
            if (m.inviteExpiration != null && Object.hasOwnProperty.call(m, "inviteExpiration"))
                w.uint32(24).int64(m.inviteExpiration);
            if (m.groupName != null && Object.hasOwnProperty.call(m, "groupName"))
                w.uint32(34).string(m.groupName);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(42).bytes(m.JPEGThumbnail);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(50).string(m.caption);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(58).fork()).ldelim();
            if (m.groupType != null && Object.hasOwnProperty.call(m, "groupType"))
                w.uint32(64).int32(m.groupType);
            return w;
        };

        GroupInviteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.GroupInviteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.groupJID = r.string();
                        break;
                    }
                case 2: {
                        m.inviteCode = r.string();
                        break;
                    }
                case 3: {
                        m.inviteExpiration = r.int64();
                        break;
                    }
                case 4: {
                        m.groupName = r.string();
                        break;
                    }
                case 5: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 6: {
                        m.caption = r.string();
                        break;
                    }
                case 7: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.groupType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupInviteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.GroupInviteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.GroupInviteMessage();
            if (d.groupJID != null) {
                m.groupJID = String(d.groupJID);
            }
            if (d.inviteCode != null) {
                m.inviteCode = String(d.inviteCode);
            }
            if (d.inviteExpiration != null) {
                if ($util.Long)
                    (m.inviteExpiration = $util.Long.fromValue(d.inviteExpiration)).unsigned = false;
                else if (typeof d.inviteExpiration === "string")
                    m.inviteExpiration = parseInt(d.inviteExpiration, 10);
                else if (typeof d.inviteExpiration === "number")
                    m.inviteExpiration = d.inviteExpiration;
                else if (typeof d.inviteExpiration === "object")
                    m.inviteExpiration = new $util.LongBits(d.inviteExpiration.low >>> 0, d.inviteExpiration.high >>> 0).toNumber();
            }
            if (d.groupName != null) {
                m.groupName = String(d.groupName);
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.GroupInviteMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            switch (d.groupType) {
            default:
                if (typeof d.groupType === "number") {
                    m.groupType = d.groupType;
                    break;
                }
                break;
            case "DEFAULT":
            case 0:
                m.groupType = 0;
                break;
            case "PARENT":
            case 1:
                m.groupType = 1;
                break;
            }
            return m;
        };

        GroupInviteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.groupJID != null && m.hasOwnProperty("groupJID")) {
                d.groupJID = m.groupJID;
                if (o.oneofs)
                    d._groupJID = "groupJID";
            }
            if (m.inviteCode != null && m.hasOwnProperty("inviteCode")) {
                d.inviteCode = m.inviteCode;
                if (o.oneofs)
                    d._inviteCode = "inviteCode";
            }
            if (m.inviteExpiration != null && m.hasOwnProperty("inviteExpiration")) {
                if (typeof m.inviteExpiration === "number")
                    d.inviteExpiration = o.longs === String ? String(m.inviteExpiration) : m.inviteExpiration;
                else
                    d.inviteExpiration = o.longs === String ? $util.Long.prototype.toString.call(m.inviteExpiration) : o.longs === Number ? new $util.LongBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0).toNumber() : m.inviteExpiration;
                if (o.oneofs)
                    d._inviteExpiration = "inviteExpiration";
            }
            if (m.groupName != null && m.hasOwnProperty("groupName")) {
                d.groupName = m.groupName;
                if (o.oneofs)
                    d._groupName = "groupName";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.groupType != null && m.hasOwnProperty("groupType")) {
                d.groupType = o.enums === String ? WAE2E.GroupInviteMessage.GroupType[m.groupType] === undefined ? m.groupType : WAE2E.GroupInviteMessage.GroupType[m.groupType] : m.groupType;
                if (o.oneofs)
                    d._groupType = "groupType";
            }
            return d;
        };

        GroupInviteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.GroupInviteMessage";
        };

        GroupInviteMessage.GroupType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "PARENT"] = 1;
            return values;
        })();

        return GroupInviteMessage;
    })();

    WAE2E.InteractiveResponseMessage = (function() {

        function InteractiveResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InteractiveResponseMessage.prototype.nativeFlowResponseMessage = null;
        InteractiveResponseMessage.prototype.body = null;
        InteractiveResponseMessage.prototype.contextInfo = null;

        let $oneOfFields;

        Object.defineProperty(InteractiveResponseMessage.prototype, "interactiveResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["nativeFlowResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveResponseMessage.prototype, "_body", {
            get: $util.oneOfGetter($oneOfFields = ["body"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveResponseMessage.create = function create(properties) {
            return new InteractiveResponseMessage(properties);
        };

        InteractiveResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.body != null && Object.hasOwnProperty.call(m, "body"))
                WAE2E.InteractiveResponseMessage.Body.encode(m.body, w.uint32(10).fork()).ldelim();
            if (m.nativeFlowResponseMessage != null && Object.hasOwnProperty.call(m, "nativeFlowResponseMessage"))
                WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage.encode(m.nativeFlowResponseMessage, w.uint32(18).fork()).ldelim();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(122).fork()).ldelim();
            return w;
        };

        InteractiveResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.nativeFlowResponseMessage = WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        m.body = WAE2E.InteractiveResponseMessage.Body.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InteractiveResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.InteractiveResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.InteractiveResponseMessage();
            if (d.nativeFlowResponseMessage != null) {
                if (typeof d.nativeFlowResponseMessage !== "object")
                    throw TypeError(".WAE2E.InteractiveResponseMessage.nativeFlowResponseMessage: object expected");
                m.nativeFlowResponseMessage = WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage.fromObject(d.nativeFlowResponseMessage, n + 1);
            }
            if (d.body != null) {
                if (typeof d.body !== "object")
                    throw TypeError(".WAE2E.InteractiveResponseMessage.body: object expected");
                m.body = WAE2E.InteractiveResponseMessage.Body.fromObject(d.body, n + 1);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.InteractiveResponseMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        InteractiveResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.body != null && m.hasOwnProperty("body")) {
                d.body = WAE2E.InteractiveResponseMessage.Body.toObject(m.body, o);
                if (o.oneofs)
                    d._body = "body";
            }
            if (m.nativeFlowResponseMessage != null && m.hasOwnProperty("nativeFlowResponseMessage")) {
                d.nativeFlowResponseMessage = WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage.toObject(m.nativeFlowResponseMessage, o);
                if (o.oneofs)
                    d.interactiveResponseMessage = "nativeFlowResponseMessage";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        InteractiveResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.InteractiveResponseMessage";
        };

        InteractiveResponseMessage.Body = (function() {

            function Body(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Body.prototype.text = null;
            Body.prototype.format = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Body.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Body.prototype, "_format", {
                get: $util.oneOfGetter($oneOfFields = ["format"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Body.create = function create(properties) {
                return new Body(properties);
            };

            Body.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.format != null && Object.hasOwnProperty.call(m, "format"))
                    w.uint32(16).int32(m.format);
                return w;
            };

            Body.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveResponseMessage.Body();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.text = r.string();
                            break;
                        }
                    case 2: {
                            m.format = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Body.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveResponseMessage.Body)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveResponseMessage.Body();
                if (d.text != null) {
                    m.text = String(d.text);
                }
                switch (d.format) {
                default:
                    if (typeof d.format === "number") {
                        m.format = d.format;
                        break;
                    }
                    break;
                case "DEFAULT":
                case 0:
                    m.format = 0;
                    break;
                case "EXTENSIONS_1":
                case 1:
                    m.format = 1;
                    break;
                }
                return m;
            };

            Body.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.text != null && m.hasOwnProperty("text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d._text = "text";
                }
                if (m.format != null && m.hasOwnProperty("format")) {
                    d.format = o.enums === String ? WAE2E.InteractiveResponseMessage.Body.Format[m.format] === undefined ? m.format : WAE2E.InteractiveResponseMessage.Body.Format[m.format] : m.format;
                    if (o.oneofs)
                        d._format = "format";
                }
                return d;
            };

            Body.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Body.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveResponseMessage.Body";
            };

            Body.Format = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "EXTENSIONS_1"] = 1;
                return values;
            })();

            return Body;
        })();

        InteractiveResponseMessage.NativeFlowResponseMessage = (function() {

            function NativeFlowResponseMessage(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            NativeFlowResponseMessage.prototype.name = null;
            NativeFlowResponseMessage.prototype.paramsJSON = null;
            NativeFlowResponseMessage.prototype.version = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NativeFlowResponseMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NativeFlowResponseMessage.prototype, "_paramsJSON", {
                get: $util.oneOfGetter($oneOfFields = ["paramsJSON"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NativeFlowResponseMessage.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NativeFlowResponseMessage.create = function create(properties) {
                return new NativeFlowResponseMessage(properties);
            };

            NativeFlowResponseMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.paramsJSON != null && Object.hasOwnProperty.call(m, "paramsJSON"))
                    w.uint32(18).string(m.paramsJSON);
                if (m.version != null && Object.hasOwnProperty.call(m, "version"))
                    w.uint32(24).int32(m.version);
                return w;
            };

            NativeFlowResponseMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.name = r.string();
                            break;
                        }
                    case 2: {
                            m.paramsJSON = r.string();
                            break;
                        }
                    case 3: {
                            m.version = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            NativeFlowResponseMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage();
                if (d.name != null) {
                    m.name = String(d.name);
                }
                if (d.paramsJSON != null) {
                    m.paramsJSON = String(d.paramsJSON);
                }
                if (d.version != null) {
                    m.version = d.version | 0;
                }
                return m;
            };

            NativeFlowResponseMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.name != null && m.hasOwnProperty("name")) {
                    d.name = m.name;
                    if (o.oneofs)
                        d._name = "name";
                }
                if (m.paramsJSON != null && m.hasOwnProperty("paramsJSON")) {
                    d.paramsJSON = m.paramsJSON;
                    if (o.oneofs)
                        d._paramsJSON = "paramsJSON";
                }
                if (m.version != null && m.hasOwnProperty("version")) {
                    d.version = m.version;
                    if (o.oneofs)
                        d._version = "version";
                }
                return d;
            };

            NativeFlowResponseMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            NativeFlowResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveResponseMessage.NativeFlowResponseMessage";
            };

            return NativeFlowResponseMessage;
        })();

        return InteractiveResponseMessage;
    })();

    WAE2E.InteractiveMessage = (function() {

        function InteractiveMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InteractiveMessage.prototype.shopStorefrontMessage = null;
        InteractiveMessage.prototype.collectionMessage = null;
        InteractiveMessage.prototype.nativeFlowMessage = null;
        InteractiveMessage.prototype.carouselMessage = null;
        InteractiveMessage.prototype.header = null;
        InteractiveMessage.prototype.body = null;
        InteractiveMessage.prototype.footer = null;
        InteractiveMessage.prototype.bloksWidget = null;
        InteractiveMessage.prototype.contextInfo = null;
        InteractiveMessage.prototype.urlTrackingMap = null;

        let $oneOfFields;

        Object.defineProperty(InteractiveMessage.prototype, "interactiveMessage", {
            get: $util.oneOfGetter($oneOfFields = ["shopStorefrontMessage", "collectionMessage", "nativeFlowMessage", "carouselMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_header", {
            get: $util.oneOfGetter($oneOfFields = ["header"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_body", {
            get: $util.oneOfGetter($oneOfFields = ["body"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_footer", {
            get: $util.oneOfGetter($oneOfFields = ["footer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_bloksWidget", {
            get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveMessage.prototype, "_urlTrackingMap", {
            get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveMessage.create = function create(properties) {
            return new InteractiveMessage(properties);
        };

        InteractiveMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.header != null && Object.hasOwnProperty.call(m, "header"))
                WAE2E.InteractiveMessage.Header.encode(m.header, w.uint32(10).fork()).ldelim();
            if (m.body != null && Object.hasOwnProperty.call(m, "body"))
                WAE2E.InteractiveMessage.Body.encode(m.body, w.uint32(18).fork()).ldelim();
            if (m.footer != null && Object.hasOwnProperty.call(m, "footer"))
                WAE2E.InteractiveMessage.Footer.encode(m.footer, w.uint32(26).fork()).ldelim();
            if (m.shopStorefrontMessage != null && Object.hasOwnProperty.call(m, "shopStorefrontMessage"))
                WAE2E.InteractiveMessage.ShopMessage.encode(m.shopStorefrontMessage, w.uint32(34).fork()).ldelim();
            if (m.collectionMessage != null && Object.hasOwnProperty.call(m, "collectionMessage"))
                WAE2E.InteractiveMessage.CollectionMessage.encode(m.collectionMessage, w.uint32(42).fork()).ldelim();
            if (m.nativeFlowMessage != null && Object.hasOwnProperty.call(m, "nativeFlowMessage"))
                WAE2E.InteractiveMessage.NativeFlowMessage.encode(m.nativeFlowMessage, w.uint32(50).fork()).ldelim();
            if (m.carouselMessage != null && Object.hasOwnProperty.call(m, "carouselMessage"))
                WAE2E.InteractiveMessage.CarouselMessage.encode(m.carouselMessage, w.uint32(58).fork()).ldelim();
            if (m.bloksWidget != null && Object.hasOwnProperty.call(m, "bloksWidget"))
                WAE2E.InteractiveMessage.BloksWidget.encode(m.bloksWidget, w.uint32(66).fork()).ldelim();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(122).fork()).ldelim();
            if (m.urlTrackingMap != null && Object.hasOwnProperty.call(m, "urlTrackingMap"))
                WAE2E.UrlTrackingMap.encode(m.urlTrackingMap, w.uint32(130).fork()).ldelim();
            return w;
        };

        InteractiveMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 4: {
                        m.shopStorefrontMessage = WAE2E.InteractiveMessage.ShopMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.collectionMessage = WAE2E.InteractiveMessage.CollectionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.nativeFlowMessage = WAE2E.InteractiveMessage.NativeFlowMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.carouselMessage = WAE2E.InteractiveMessage.CarouselMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        m.header = WAE2E.InteractiveMessage.Header.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.body = WAE2E.InteractiveMessage.Body.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.footer = WAE2E.InteractiveMessage.Footer.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 16: {
                        m.urlTrackingMap = WAE2E.UrlTrackingMap.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InteractiveMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.InteractiveMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.InteractiveMessage();
            if (d.shopStorefrontMessage != null) {
                if (typeof d.shopStorefrontMessage !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.shopStorefrontMessage: object expected");
                m.shopStorefrontMessage = WAE2E.InteractiveMessage.ShopMessage.fromObject(d.shopStorefrontMessage, n + 1);
            }
            if (d.collectionMessage != null) {
                if (typeof d.collectionMessage !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.collectionMessage: object expected");
                m.collectionMessage = WAE2E.InteractiveMessage.CollectionMessage.fromObject(d.collectionMessage, n + 1);
            }
            if (d.nativeFlowMessage != null) {
                if (typeof d.nativeFlowMessage !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.nativeFlowMessage: object expected");
                m.nativeFlowMessage = WAE2E.InteractiveMessage.NativeFlowMessage.fromObject(d.nativeFlowMessage, n + 1);
            }
            if (d.carouselMessage != null) {
                if (typeof d.carouselMessage !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.carouselMessage: object expected");
                m.carouselMessage = WAE2E.InteractiveMessage.CarouselMessage.fromObject(d.carouselMessage, n + 1);
            }
            if (d.header != null) {
                if (typeof d.header !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.header: object expected");
                m.header = WAE2E.InteractiveMessage.Header.fromObject(d.header, n + 1);
            }
            if (d.body != null) {
                if (typeof d.body !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.body: object expected");
                m.body = WAE2E.InteractiveMessage.Body.fromObject(d.body, n + 1);
            }
            if (d.footer != null) {
                if (typeof d.footer !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.footer: object expected");
                m.footer = WAE2E.InteractiveMessage.Footer.fromObject(d.footer, n + 1);
            }
            if (d.bloksWidget != null) {
                if (typeof d.bloksWidget !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.bloksWidget: object expected");
                m.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.fromObject(d.bloksWidget, n + 1);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.urlTrackingMap != null) {
                if (typeof d.urlTrackingMap !== "object")
                    throw TypeError(".WAE2E.InteractiveMessage.urlTrackingMap: object expected");
                m.urlTrackingMap = WAE2E.UrlTrackingMap.fromObject(d.urlTrackingMap, n + 1);
            }
            return m;
        };

        InteractiveMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.header != null && m.hasOwnProperty("header")) {
                d.header = WAE2E.InteractiveMessage.Header.toObject(m.header, o);
                if (o.oneofs)
                    d._header = "header";
            }
            if (m.body != null && m.hasOwnProperty("body")) {
                d.body = WAE2E.InteractiveMessage.Body.toObject(m.body, o);
                if (o.oneofs)
                    d._body = "body";
            }
            if (m.footer != null && m.hasOwnProperty("footer")) {
                d.footer = WAE2E.InteractiveMessage.Footer.toObject(m.footer, o);
                if (o.oneofs)
                    d._footer = "footer";
            }
            if (m.shopStorefrontMessage != null && m.hasOwnProperty("shopStorefrontMessage")) {
                d.shopStorefrontMessage = WAE2E.InteractiveMessage.ShopMessage.toObject(m.shopStorefrontMessage, o);
                if (o.oneofs)
                    d.interactiveMessage = "shopStorefrontMessage";
            }
            if (m.collectionMessage != null && m.hasOwnProperty("collectionMessage")) {
                d.collectionMessage = WAE2E.InteractiveMessage.CollectionMessage.toObject(m.collectionMessage, o);
                if (o.oneofs)
                    d.interactiveMessage = "collectionMessage";
            }
            if (m.nativeFlowMessage != null && m.hasOwnProperty("nativeFlowMessage")) {
                d.nativeFlowMessage = WAE2E.InteractiveMessage.NativeFlowMessage.toObject(m.nativeFlowMessage, o);
                if (o.oneofs)
                    d.interactiveMessage = "nativeFlowMessage";
            }
            if (m.carouselMessage != null && m.hasOwnProperty("carouselMessage")) {
                d.carouselMessage = WAE2E.InteractiveMessage.CarouselMessage.toObject(m.carouselMessage, o);
                if (o.oneofs)
                    d.interactiveMessage = "carouselMessage";
            }
            if (m.bloksWidget != null && m.hasOwnProperty("bloksWidget")) {
                d.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.toObject(m.bloksWidget, o);
                if (o.oneofs)
                    d._bloksWidget = "bloksWidget";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.urlTrackingMap != null && m.hasOwnProperty("urlTrackingMap")) {
                d.urlTrackingMap = WAE2E.UrlTrackingMap.toObject(m.urlTrackingMap, o);
                if (o.oneofs)
                    d._urlTrackingMap = "urlTrackingMap";
            }
            return d;
        };

        InteractiveMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.InteractiveMessage";
        };

        InteractiveMessage.CarouselMessage = (function() {

            function CarouselMessage(p) {
                this.cards = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CarouselMessage.prototype.cards = $util.emptyArray;
            CarouselMessage.prototype.messageVersion = null;
            CarouselMessage.prototype.carouselCardType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CarouselMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CarouselMessage.prototype, "_carouselCardType", {
                get: $util.oneOfGetter($oneOfFields = ["carouselCardType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CarouselMessage.create = function create(properties) {
                return new CarouselMessage(properties);
            };

            CarouselMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.cards != null && m.cards.length) {
                    for (var i = 0; i < m.cards.length; ++i)
                        WAE2E.InteractiveMessage.encode(m.cards[i], w.uint32(10).fork()).ldelim();
                }
                if (m.messageVersion != null && Object.hasOwnProperty.call(m, "messageVersion"))
                    w.uint32(16).int32(m.messageVersion);
                if (m.carouselCardType != null && Object.hasOwnProperty.call(m, "carouselCardType"))
                    w.uint32(24).int32(m.carouselCardType);
                return w;
            };

            CarouselMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.CarouselMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            if (!(m.cards && m.cards.length))
                                m.cards = [];
                            m.cards.push(WAE2E.InteractiveMessage.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 2: {
                            m.messageVersion = r.int32();
                            break;
                        }
                    case 3: {
                            m.carouselCardType = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CarouselMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.CarouselMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.CarouselMessage();
                if (d.cards) {
                    if (!Array.isArray(d.cards))
                        throw TypeError(".WAE2E.InteractiveMessage.CarouselMessage.cards: array expected");
                    m.cards = [];
                    for (var i = 0; i < d.cards.length; ++i) {
                        if (typeof d.cards[i] !== "object")
                            throw TypeError(".WAE2E.InteractiveMessage.CarouselMessage.cards: object expected");
                        m.cards[i] = WAE2E.InteractiveMessage.fromObject(d.cards[i], n + 1);
                    }
                }
                if (d.messageVersion != null) {
                    m.messageVersion = d.messageVersion | 0;
                }
                switch (d.carouselCardType) {
                default:
                    if (typeof d.carouselCardType === "number") {
                        m.carouselCardType = d.carouselCardType;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.carouselCardType = 0;
                    break;
                case "HSCROLL_CARDS":
                case 1:
                    m.carouselCardType = 1;
                    break;
                case "ALBUM_IMAGE":
                case 2:
                    m.carouselCardType = 2;
                    break;
                }
                return m;
            };

            CarouselMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.cards = [];
                }
                if (m.cards && m.cards.length) {
                    d.cards = [];
                    for (var j = 0; j < m.cards.length; ++j) {
                        d.cards[j] = WAE2E.InteractiveMessage.toObject(m.cards[j], o);
                    }
                }
                if (m.messageVersion != null && m.hasOwnProperty("messageVersion")) {
                    d.messageVersion = m.messageVersion;
                    if (o.oneofs)
                        d._messageVersion = "messageVersion";
                }
                if (m.carouselCardType != null && m.hasOwnProperty("carouselCardType")) {
                    d.carouselCardType = o.enums === String ? WAE2E.InteractiveMessage.CarouselMessage.CarouselCardType[m.carouselCardType] === undefined ? m.carouselCardType : WAE2E.InteractiveMessage.CarouselMessage.CarouselCardType[m.carouselCardType] : m.carouselCardType;
                    if (o.oneofs)
                        d._carouselCardType = "carouselCardType";
                }
                return d;
            };

            CarouselMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CarouselMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.CarouselMessage";
            };

            CarouselMessage.CarouselCardType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "HSCROLL_CARDS"] = 1;
                values[valuesById[2] = "ALBUM_IMAGE"] = 2;
                return values;
            })();

            return CarouselMessage;
        })();

        InteractiveMessage.ShopMessage = (function() {

            function ShopMessage(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ShopMessage.prototype.ID = null;
            ShopMessage.prototype.surface = null;
            ShopMessage.prototype.messageVersion = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ShopMessage.prototype, "_ID", {
                get: $util.oneOfGetter($oneOfFields = ["ID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ShopMessage.prototype, "_surface", {
                get: $util.oneOfGetter($oneOfFields = ["surface"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ShopMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ShopMessage.create = function create(properties) {
                return new ShopMessage(properties);
            };

            ShopMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                    w.uint32(10).string(m.ID);
                if (m.surface != null && Object.hasOwnProperty.call(m, "surface"))
                    w.uint32(16).int32(m.surface);
                if (m.messageVersion != null && Object.hasOwnProperty.call(m, "messageVersion"))
                    w.uint32(24).int32(m.messageVersion);
                return w;
            };

            ShopMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.ShopMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.ID = r.string();
                            break;
                        }
                    case 2: {
                            m.surface = r.int32();
                            break;
                        }
                    case 3: {
                            m.messageVersion = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ShopMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.ShopMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.ShopMessage();
                if (d.ID != null) {
                    m.ID = String(d.ID);
                }
                switch (d.surface) {
                default:
                    if (typeof d.surface === "number") {
                        m.surface = d.surface;
                        break;
                    }
                    break;
                case "UNKNOWN_SURFACE":
                case 0:
                    m.surface = 0;
                    break;
                case "FB":
                case 1:
                    m.surface = 1;
                    break;
                case "IG":
                case 2:
                    m.surface = 2;
                    break;
                case "WA":
                case 3:
                    m.surface = 3;
                    break;
                }
                if (d.messageVersion != null) {
                    m.messageVersion = d.messageVersion | 0;
                }
                return m;
            };

            ShopMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.ID != null && m.hasOwnProperty("ID")) {
                    d.ID = m.ID;
                    if (o.oneofs)
                        d._ID = "ID";
                }
                if (m.surface != null && m.hasOwnProperty("surface")) {
                    d.surface = o.enums === String ? WAE2E.InteractiveMessage.ShopMessage.Surface[m.surface] === undefined ? m.surface : WAE2E.InteractiveMessage.ShopMessage.Surface[m.surface] : m.surface;
                    if (o.oneofs)
                        d._surface = "surface";
                }
                if (m.messageVersion != null && m.hasOwnProperty("messageVersion")) {
                    d.messageVersion = m.messageVersion;
                    if (o.oneofs)
                        d._messageVersion = "messageVersion";
                }
                return d;
            };

            ShopMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ShopMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.ShopMessage";
            };

            ShopMessage.Surface = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_SURFACE"] = 0;
                values[valuesById[1] = "FB"] = 1;
                values[valuesById[2] = "IG"] = 2;
                values[valuesById[3] = "WA"] = 3;
                return values;
            })();

            return ShopMessage;
        })();

        InteractiveMessage.NativeFlowMessage = (function() {

            function NativeFlowMessage(p) {
                this.buttons = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            NativeFlowMessage.prototype.buttons = $util.emptyArray;
            NativeFlowMessage.prototype.messageParamsJSON = null;
            NativeFlowMessage.prototype.messageVersion = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NativeFlowMessage.prototype, "_messageParamsJSON", {
                get: $util.oneOfGetter($oneOfFields = ["messageParamsJSON"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(NativeFlowMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NativeFlowMessage.create = function create(properties) {
                return new NativeFlowMessage(properties);
            };

            NativeFlowMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.buttons != null && m.buttons.length) {
                    for (var i = 0; i < m.buttons.length; ++i)
                        WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton.encode(m.buttons[i], w.uint32(10).fork()).ldelim();
                }
                if (m.messageParamsJSON != null && Object.hasOwnProperty.call(m, "messageParamsJSON"))
                    w.uint32(18).string(m.messageParamsJSON);
                if (m.messageVersion != null && Object.hasOwnProperty.call(m, "messageVersion"))
                    w.uint32(24).int32(m.messageVersion);
                return w;
            };

            NativeFlowMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.NativeFlowMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            if (!(m.buttons && m.buttons.length))
                                m.buttons = [];
                            m.buttons.push(WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 2: {
                            m.messageParamsJSON = r.string();
                            break;
                        }
                    case 3: {
                            m.messageVersion = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            NativeFlowMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.NativeFlowMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.NativeFlowMessage();
                if (d.buttons) {
                    if (!Array.isArray(d.buttons))
                        throw TypeError(".WAE2E.InteractiveMessage.NativeFlowMessage.buttons: array expected");
                    m.buttons = [];
                    for (var i = 0; i < d.buttons.length; ++i) {
                        if (typeof d.buttons[i] !== "object")
                            throw TypeError(".WAE2E.InteractiveMessage.NativeFlowMessage.buttons: object expected");
                        m.buttons[i] = WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton.fromObject(d.buttons[i], n + 1);
                    }
                }
                if (d.messageParamsJSON != null) {
                    m.messageParamsJSON = String(d.messageParamsJSON);
                }
                if (d.messageVersion != null) {
                    m.messageVersion = d.messageVersion | 0;
                }
                return m;
            };

            NativeFlowMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.buttons = [];
                }
                if (m.buttons && m.buttons.length) {
                    d.buttons = [];
                    for (var j = 0; j < m.buttons.length; ++j) {
                        d.buttons[j] = WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton.toObject(m.buttons[j], o);
                    }
                }
                if (m.messageParamsJSON != null && m.hasOwnProperty("messageParamsJSON")) {
                    d.messageParamsJSON = m.messageParamsJSON;
                    if (o.oneofs)
                        d._messageParamsJSON = "messageParamsJSON";
                }
                if (m.messageVersion != null && m.hasOwnProperty("messageVersion")) {
                    d.messageVersion = m.messageVersion;
                    if (o.oneofs)
                        d._messageVersion = "messageVersion";
                }
                return d;
            };

            NativeFlowMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            NativeFlowMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.NativeFlowMessage";
            };

            NativeFlowMessage.NativeFlowButton = (function() {

                function NativeFlowButton(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                NativeFlowButton.prototype.name = null;
                NativeFlowButton.prototype.buttonParamsJSON = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowButton.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(NativeFlowButton.prototype, "_buttonParamsJSON", {
                    get: $util.oneOfGetter($oneOfFields = ["buttonParamsJSON"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                NativeFlowButton.create = function create(properties) {
                    return new NativeFlowButton(properties);
                };

                NativeFlowButton.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                        w.uint32(10).string(m.name);
                    if (m.buttonParamsJSON != null && Object.hasOwnProperty.call(m, "buttonParamsJSON"))
                        w.uint32(18).string(m.buttonParamsJSON);
                    return w;
                };

                NativeFlowButton.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.name = r.string();
                                break;
                            }
                        case 2: {
                                m.buttonParamsJSON = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                NativeFlowButton.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton();
                    if (d.name != null) {
                        m.name = String(d.name);
                    }
                    if (d.buttonParamsJSON != null) {
                        m.buttonParamsJSON = String(d.buttonParamsJSON);
                    }
                    return m;
                };

                NativeFlowButton.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.name != null && m.hasOwnProperty("name")) {
                        d.name = m.name;
                        if (o.oneofs)
                            d._name = "name";
                    }
                    if (m.buttonParamsJSON != null && m.hasOwnProperty("buttonParamsJSON")) {
                        d.buttonParamsJSON = m.buttonParamsJSON;
                        if (o.oneofs)
                            d._buttonParamsJSON = "buttonParamsJSON";
                    }
                    return d;
                };

                NativeFlowButton.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                NativeFlowButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.InteractiveMessage.NativeFlowMessage.NativeFlowButton";
                };

                return NativeFlowButton;
            })();

            return NativeFlowMessage;
        })();

        InteractiveMessage.CollectionMessage = (function() {

            function CollectionMessage(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CollectionMessage.prototype.bizJID = null;
            CollectionMessage.prototype.ID = null;
            CollectionMessage.prototype.messageVersion = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CollectionMessage.prototype, "_bizJID", {
                get: $util.oneOfGetter($oneOfFields = ["bizJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CollectionMessage.prototype, "_ID", {
                get: $util.oneOfGetter($oneOfFields = ["ID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CollectionMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CollectionMessage.create = function create(properties) {
                return new CollectionMessage(properties);
            };

            CollectionMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.bizJID != null && Object.hasOwnProperty.call(m, "bizJID"))
                    w.uint32(10).string(m.bizJID);
                if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                    w.uint32(18).string(m.ID);
                if (m.messageVersion != null && Object.hasOwnProperty.call(m, "messageVersion"))
                    w.uint32(24).int32(m.messageVersion);
                return w;
            };

            CollectionMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.CollectionMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.bizJID = r.string();
                            break;
                        }
                    case 2: {
                            m.ID = r.string();
                            break;
                        }
                    case 3: {
                            m.messageVersion = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CollectionMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.CollectionMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.CollectionMessage();
                if (d.bizJID != null) {
                    m.bizJID = String(d.bizJID);
                }
                if (d.ID != null) {
                    m.ID = String(d.ID);
                }
                if (d.messageVersion != null) {
                    m.messageVersion = d.messageVersion | 0;
                }
                return m;
            };

            CollectionMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.bizJID != null && m.hasOwnProperty("bizJID")) {
                    d.bizJID = m.bizJID;
                    if (o.oneofs)
                        d._bizJID = "bizJID";
                }
                if (m.ID != null && m.hasOwnProperty("ID")) {
                    d.ID = m.ID;
                    if (o.oneofs)
                        d._ID = "ID";
                }
                if (m.messageVersion != null && m.hasOwnProperty("messageVersion")) {
                    d.messageVersion = m.messageVersion;
                    if (o.oneofs)
                        d._messageVersion = "messageVersion";
                }
                return d;
            };

            CollectionMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CollectionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.CollectionMessage";
            };

            return CollectionMessage;
        })();

        InteractiveMessage.BloksWidget = (function() {

            function BloksWidget(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BloksWidget.prototype.uuid = null;
            BloksWidget.prototype.data = null;
            BloksWidget.prototype.type = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BloksWidget.prototype, "_uuid", {
                get: $util.oneOfGetter($oneOfFields = ["uuid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BloksWidget.prototype, "_data", {
                get: $util.oneOfGetter($oneOfFields = ["data"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BloksWidget.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BloksWidget.create = function create(properties) {
                return new BloksWidget(properties);
            };

            BloksWidget.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.uuid != null && Object.hasOwnProperty.call(m, "uuid"))
                    w.uint32(10).string(m.uuid);
                if (m.data != null && Object.hasOwnProperty.call(m, "data"))
                    w.uint32(18).string(m.data);
                if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                    w.uint32(26).string(m.type);
                return w;
            };

            BloksWidget.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.BloksWidget();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.uuid = r.string();
                            break;
                        }
                    case 2: {
                            m.data = r.string();
                            break;
                        }
                    case 3: {
                            m.type = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BloksWidget.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.BloksWidget)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.BloksWidget();
                if (d.uuid != null) {
                    m.uuid = String(d.uuid);
                }
                if (d.data != null) {
                    m.data = String(d.data);
                }
                if (d.type != null) {
                    m.type = String(d.type);
                }
                return m;
            };

            BloksWidget.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.uuid != null && m.hasOwnProperty("uuid")) {
                    d.uuid = m.uuid;
                    if (o.oneofs)
                        d._uuid = "uuid";
                }
                if (m.data != null && m.hasOwnProperty("data")) {
                    d.data = m.data;
                    if (o.oneofs)
                        d._data = "data";
                }
                if (m.type != null && m.hasOwnProperty("type")) {
                    d.type = m.type;
                    if (o.oneofs)
                        d._type = "type";
                }
                return d;
            };

            BloksWidget.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BloksWidget.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.BloksWidget";
            };

            return BloksWidget;
        })();

        InteractiveMessage.Footer = (function() {

            function Footer(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Footer.prototype.audioMessage = null;
            Footer.prototype.text = null;
            Footer.prototype.hasMediaAttachment = null;

            let $oneOfFields;

            Object.defineProperty(Footer.prototype, "media", {
                get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Footer.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Footer.prototype, "_hasMediaAttachment", {
                get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Footer.create = function create(properties) {
                return new Footer(properties);
            };

            Footer.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.audioMessage != null && Object.hasOwnProperty.call(m, "audioMessage"))
                    WAE2E.AudioMessage.encode(m.audioMessage, w.uint32(18).fork()).ldelim();
                if (m.hasMediaAttachment != null && Object.hasOwnProperty.call(m, "hasMediaAttachment"))
                    w.uint32(24).bool(m.hasMediaAttachment);
                return w;
            };

            Footer.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.Footer();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 2: {
                            m.audioMessage = WAE2E.AudioMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 1: {
                            m.text = r.string();
                            break;
                        }
                    case 3: {
                            m.hasMediaAttachment = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Footer.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.Footer)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.Footer();
                if (d.audioMessage != null) {
                    if (typeof d.audioMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Footer.audioMessage: object expected");
                    m.audioMessage = WAE2E.AudioMessage.fromObject(d.audioMessage, n + 1);
                }
                if (d.text != null) {
                    m.text = String(d.text);
                }
                if (d.hasMediaAttachment != null) {
                    m.hasMediaAttachment = Boolean(d.hasMediaAttachment);
                }
                return m;
            };

            Footer.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.text != null && m.hasOwnProperty("text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d._text = "text";
                }
                if (m.audioMessage != null && m.hasOwnProperty("audioMessage")) {
                    d.audioMessage = WAE2E.AudioMessage.toObject(m.audioMessage, o);
                    if (o.oneofs)
                        d.media = "audioMessage";
                }
                if (m.hasMediaAttachment != null && m.hasOwnProperty("hasMediaAttachment")) {
                    d.hasMediaAttachment = m.hasMediaAttachment;
                    if (o.oneofs)
                        d._hasMediaAttachment = "hasMediaAttachment";
                }
                return d;
            };

            Footer.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Footer.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.Footer";
            };

            return Footer;
        })();

        InteractiveMessage.Body = (function() {

            function Body(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Body.prototype.text = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Body.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Body.create = function create(properties) {
                return new Body(properties);
            };

            Body.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                return w;
            };

            Body.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.Body();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.text = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Body.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.Body)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.Body();
                if (d.text != null) {
                    m.text = String(d.text);
                }
                return m;
            };

            Body.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.text != null && m.hasOwnProperty("text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d._text = "text";
                }
                return d;
            };

            Body.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Body.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.Body";
            };

            return Body;
        })();

        InteractiveMessage.Header = (function() {

            function Header(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Header.prototype.documentMessage = null;
            Header.prototype.imageMessage = null;
            Header.prototype.JPEGThumbnail = null;
            Header.prototype.videoMessage = null;
            Header.prototype.locationMessage = null;
            Header.prototype.productMessage = null;
            Header.prototype.title = null;
            Header.prototype.subtitle = null;
            Header.prototype.hasMediaAttachment = null;
            Header.prototype.bloksWidget = null;

            let $oneOfFields;

            Object.defineProperty(Header.prototype, "media", {
                get: $util.oneOfGetter($oneOfFields = ["documentMessage", "imageMessage", "JPEGThumbnail", "videoMessage", "locationMessage", "productMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Header.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Header.prototype, "_subtitle", {
                get: $util.oneOfGetter($oneOfFields = ["subtitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Header.prototype, "_hasMediaAttachment", {
                get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Header.prototype, "_bloksWidget", {
                get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Header.create = function create(properties) {
                return new Header(properties);
            };

            Header.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.subtitle != null && Object.hasOwnProperty.call(m, "subtitle"))
                    w.uint32(18).string(m.subtitle);
                if (m.documentMessage != null && Object.hasOwnProperty.call(m, "documentMessage"))
                    WAE2E.DocumentMessage.encode(m.documentMessage, w.uint32(26).fork()).ldelim();
                if (m.imageMessage != null && Object.hasOwnProperty.call(m, "imageMessage"))
                    WAE2E.ImageMessage.encode(m.imageMessage, w.uint32(34).fork()).ldelim();
                if (m.hasMediaAttachment != null && Object.hasOwnProperty.call(m, "hasMediaAttachment"))
                    w.uint32(40).bool(m.hasMediaAttachment);
                if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                    w.uint32(50).bytes(m.JPEGThumbnail);
                if (m.videoMessage != null && Object.hasOwnProperty.call(m, "videoMessage"))
                    WAE2E.VideoMessage.encode(m.videoMessage, w.uint32(58).fork()).ldelim();
                if (m.locationMessage != null && Object.hasOwnProperty.call(m, "locationMessage"))
                    WAE2E.LocationMessage.encode(m.locationMessage, w.uint32(66).fork()).ldelim();
                if (m.productMessage != null && Object.hasOwnProperty.call(m, "productMessage"))
                    WAE2E.ProductMessage.encode(m.productMessage, w.uint32(74).fork()).ldelim();
                if (m.bloksWidget != null && Object.hasOwnProperty.call(m, "bloksWidget"))
                    WAE2E.InteractiveMessage.BloksWidget.encode(m.bloksWidget, w.uint32(82).fork()).ldelim();
                return w;
            };

            Header.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveMessage.Header();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 3: {
                            m.documentMessage = WAE2E.DocumentMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 4: {
                            m.imageMessage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 6: {
                            m.JPEGThumbnail = r.bytes();
                            break;
                        }
                    case 7: {
                            m.videoMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 8: {
                            m.locationMessage = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 9: {
                            m.productMessage = WAE2E.ProductMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            m.subtitle = r.string();
                            break;
                        }
                    case 5: {
                            m.hasMediaAttachment = r.bool();
                            break;
                        }
                    case 10: {
                            m.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Header.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.InteractiveMessage.Header)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.InteractiveMessage.Header();
                if (d.documentMessage != null) {
                    if (typeof d.documentMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.documentMessage: object expected");
                    m.documentMessage = WAE2E.DocumentMessage.fromObject(d.documentMessage, n + 1);
                }
                if (d.imageMessage != null) {
                    if (typeof d.imageMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.imageMessage: object expected");
                    m.imageMessage = WAE2E.ImageMessage.fromObject(d.imageMessage, n + 1);
                }
                if (d.JPEGThumbnail != null) {
                    if (typeof d.JPEGThumbnail === "string")
                        $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                    else if (d.JPEGThumbnail.length >= 0)
                        m.JPEGThumbnail = d.JPEGThumbnail;
                }
                if (d.videoMessage != null) {
                    if (typeof d.videoMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.videoMessage: object expected");
                    m.videoMessage = WAE2E.VideoMessage.fromObject(d.videoMessage, n + 1);
                }
                if (d.locationMessage != null) {
                    if (typeof d.locationMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.locationMessage: object expected");
                    m.locationMessage = WAE2E.LocationMessage.fromObject(d.locationMessage, n + 1);
                }
                if (d.productMessage != null) {
                    if (typeof d.productMessage !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.productMessage: object expected");
                    m.productMessage = WAE2E.ProductMessage.fromObject(d.productMessage, n + 1);
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.subtitle != null) {
                    m.subtitle = String(d.subtitle);
                }
                if (d.hasMediaAttachment != null) {
                    m.hasMediaAttachment = Boolean(d.hasMediaAttachment);
                }
                if (d.bloksWidget != null) {
                    if (typeof d.bloksWidget !== "object")
                        throw TypeError(".WAE2E.InteractiveMessage.Header.bloksWidget: object expected");
                    m.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.fromObject(d.bloksWidget, n + 1);
                }
                return m;
            };

            Header.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.subtitle != null && m.hasOwnProperty("subtitle")) {
                    d.subtitle = m.subtitle;
                    if (o.oneofs)
                        d._subtitle = "subtitle";
                }
                if (m.documentMessage != null && m.hasOwnProperty("documentMessage")) {
                    d.documentMessage = WAE2E.DocumentMessage.toObject(m.documentMessage, o);
                    if (o.oneofs)
                        d.media = "documentMessage";
                }
                if (m.imageMessage != null && m.hasOwnProperty("imageMessage")) {
                    d.imageMessage = WAE2E.ImageMessage.toObject(m.imageMessage, o);
                    if (o.oneofs)
                        d.media = "imageMessage";
                }
                if (m.hasMediaAttachment != null && m.hasOwnProperty("hasMediaAttachment")) {
                    d.hasMediaAttachment = m.hasMediaAttachment;
                    if (o.oneofs)
                        d._hasMediaAttachment = "hasMediaAttachment";
                }
                if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                    d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                    if (o.oneofs)
                        d.media = "JPEGThumbnail";
                }
                if (m.videoMessage != null && m.hasOwnProperty("videoMessage")) {
                    d.videoMessage = WAE2E.VideoMessage.toObject(m.videoMessage, o);
                    if (o.oneofs)
                        d.media = "videoMessage";
                }
                if (m.locationMessage != null && m.hasOwnProperty("locationMessage")) {
                    d.locationMessage = WAE2E.LocationMessage.toObject(m.locationMessage, o);
                    if (o.oneofs)
                        d.media = "locationMessage";
                }
                if (m.productMessage != null && m.hasOwnProperty("productMessage")) {
                    d.productMessage = WAE2E.ProductMessage.toObject(m.productMessage, o);
                    if (o.oneofs)
                        d.media = "productMessage";
                }
                if (m.bloksWidget != null && m.hasOwnProperty("bloksWidget")) {
                    d.bloksWidget = WAE2E.InteractiveMessage.BloksWidget.toObject(m.bloksWidget, o);
                    if (o.oneofs)
                        d._bloksWidget = "bloksWidget";
                }
                return d;
            };

            Header.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Header.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.InteractiveMessage.Header";
            };

            return Header;
        })();

        return InteractiveMessage;
    })();

    WAE2E.ListResponseMessage = (function() {

        function ListResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ListResponseMessage.prototype.title = null;
        ListResponseMessage.prototype.listType = null;
        ListResponseMessage.prototype.singleSelectReply = null;
        ListResponseMessage.prototype.contextInfo = null;
        ListResponseMessage.prototype.description = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListResponseMessage.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListResponseMessage.prototype, "_listType", {
            get: $util.oneOfGetter($oneOfFields = ["listType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListResponseMessage.prototype, "_singleSelectReply", {
            get: $util.oneOfGetter($oneOfFields = ["singleSelectReply"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListResponseMessage.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ListResponseMessage.create = function create(properties) {
            return new ListResponseMessage(properties);
        };

        ListResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(10).string(m.title);
            if (m.listType != null && Object.hasOwnProperty.call(m, "listType"))
                w.uint32(16).int32(m.listType);
            if (m.singleSelectReply != null && Object.hasOwnProperty.call(m, "singleSelectReply"))
                WAE2E.ListResponseMessage.SingleSelectReply.encode(m.singleSelectReply, w.uint32(26).fork()).ldelim();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(34).fork()).ldelim();
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(42).string(m.description);
            return w;
        };

        ListResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.title = r.string();
                        break;
                    }
                case 2: {
                        m.listType = r.int32();
                        break;
                    }
                case 3: {
                        m.singleSelectReply = WAE2E.ListResponseMessage.SingleSelectReply.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.description = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ListResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ListResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ListResponseMessage();
            if (d.title != null) {
                m.title = String(d.title);
            }
            switch (d.listType) {
            default:
                if (typeof d.listType === "number") {
                    m.listType = d.listType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.listType = 0;
                break;
            case "SINGLE_SELECT":
            case 1:
                m.listType = 1;
                break;
            }
            if (d.singleSelectReply != null) {
                if (typeof d.singleSelectReply !== "object")
                    throw TypeError(".WAE2E.ListResponseMessage.singleSelectReply: object expected");
                m.singleSelectReply = WAE2E.ListResponseMessage.SingleSelectReply.fromObject(d.singleSelectReply, n + 1);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ListResponseMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            return m;
        };

        ListResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.listType != null && m.hasOwnProperty("listType")) {
                d.listType = o.enums === String ? WAE2E.ListResponseMessage.ListType[m.listType] === undefined ? m.listType : WAE2E.ListResponseMessage.ListType[m.listType] : m.listType;
                if (o.oneofs)
                    d._listType = "listType";
            }
            if (m.singleSelectReply != null && m.hasOwnProperty("singleSelectReply")) {
                d.singleSelectReply = WAE2E.ListResponseMessage.SingleSelectReply.toObject(m.singleSelectReply, o);
                if (o.oneofs)
                    d._singleSelectReply = "singleSelectReply";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            return d;
        };

        ListResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ListResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ListResponseMessage";
        };

        ListResponseMessage.ListType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "SINGLE_SELECT"] = 1;
            return values;
        })();

        ListResponseMessage.SingleSelectReply = (function() {

            function SingleSelectReply(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            SingleSelectReply.prototype.selectedRowID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SingleSelectReply.prototype, "_selectedRowID", {
                get: $util.oneOfGetter($oneOfFields = ["selectedRowID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SingleSelectReply.create = function create(properties) {
                return new SingleSelectReply(properties);
            };

            SingleSelectReply.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.selectedRowID != null && Object.hasOwnProperty.call(m, "selectedRowID"))
                    w.uint32(10).string(m.selectedRowID);
                return w;
            };

            SingleSelectReply.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListResponseMessage.SingleSelectReply();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.selectedRowID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            SingleSelectReply.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListResponseMessage.SingleSelectReply)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListResponseMessage.SingleSelectReply();
                if (d.selectedRowID != null) {
                    m.selectedRowID = String(d.selectedRowID);
                }
                return m;
            };

            SingleSelectReply.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.selectedRowID != null && m.hasOwnProperty("selectedRowID")) {
                    d.selectedRowID = m.selectedRowID;
                    if (o.oneofs)
                        d._selectedRowID = "selectedRowID";
                }
                return d;
            };

            SingleSelectReply.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SingleSelectReply.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListResponseMessage.SingleSelectReply";
            };

            return SingleSelectReply;
        })();

        return ListResponseMessage;
    })();

    WAE2E.ListMessage = (function() {

        function ListMessage(p) {
            this.sections = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ListMessage.prototype.title = null;
        ListMessage.prototype.description = null;
        ListMessage.prototype.buttonText = null;
        ListMessage.prototype.listType = null;
        ListMessage.prototype.sections = $util.emptyArray;
        ListMessage.prototype.productListInfo = null;
        ListMessage.prototype.footerText = null;
        ListMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_buttonText", {
            get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_listType", {
            get: $util.oneOfGetter($oneOfFields = ["listType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_productListInfo", {
            get: $util.oneOfGetter($oneOfFields = ["productListInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_footerText", {
            get: $util.oneOfGetter($oneOfFields = ["footerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ListMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ListMessage.create = function create(properties) {
            return new ListMessage(properties);
        };

        ListMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(10).string(m.title);
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(18).string(m.description);
            if (m.buttonText != null && Object.hasOwnProperty.call(m, "buttonText"))
                w.uint32(26).string(m.buttonText);
            if (m.listType != null && Object.hasOwnProperty.call(m, "listType"))
                w.uint32(32).int32(m.listType);
            if (m.sections != null && m.sections.length) {
                for (var i = 0; i < m.sections.length; ++i)
                    WAE2E.ListMessage.Section.encode(m.sections[i], w.uint32(42).fork()).ldelim();
            }
            if (m.productListInfo != null && Object.hasOwnProperty.call(m, "productListInfo"))
                WAE2E.ListMessage.ProductListInfo.encode(m.productListInfo, w.uint32(50).fork()).ldelim();
            if (m.footerText != null && Object.hasOwnProperty.call(m, "footerText"))
                w.uint32(58).string(m.footerText);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(66).fork()).ldelim();
            return w;
        };

        ListMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.title = r.string();
                        break;
                    }
                case 2: {
                        m.description = r.string();
                        break;
                    }
                case 3: {
                        m.buttonText = r.string();
                        break;
                    }
                case 4: {
                        m.listType = r.int32();
                        break;
                    }
                case 5: {
                        if (!(m.sections && m.sections.length))
                            m.sections = [];
                        m.sections.push(WAE2E.ListMessage.Section.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 6: {
                        m.productListInfo = WAE2E.ListMessage.ProductListInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.footerText = r.string();
                        break;
                    }
                case 8: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ListMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ListMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ListMessage();
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            if (d.buttonText != null) {
                m.buttonText = String(d.buttonText);
            }
            switch (d.listType) {
            default:
                if (typeof d.listType === "number") {
                    m.listType = d.listType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.listType = 0;
                break;
            case "SINGLE_SELECT":
            case 1:
                m.listType = 1;
                break;
            case "PRODUCT_LIST":
            case 2:
                m.listType = 2;
                break;
            }
            if (d.sections) {
                if (!Array.isArray(d.sections))
                    throw TypeError(".WAE2E.ListMessage.sections: array expected");
                m.sections = [];
                for (var i = 0; i < d.sections.length; ++i) {
                    if (typeof d.sections[i] !== "object")
                        throw TypeError(".WAE2E.ListMessage.sections: object expected");
                    m.sections[i] = WAE2E.ListMessage.Section.fromObject(d.sections[i], n + 1);
                }
            }
            if (d.productListInfo != null) {
                if (typeof d.productListInfo !== "object")
                    throw TypeError(".WAE2E.ListMessage.productListInfo: object expected");
                m.productListInfo = WAE2E.ListMessage.ProductListInfo.fromObject(d.productListInfo, n + 1);
            }
            if (d.footerText != null) {
                m.footerText = String(d.footerText);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ListMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        ListMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.sections = [];
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            if (m.buttonText != null && m.hasOwnProperty("buttonText")) {
                d.buttonText = m.buttonText;
                if (o.oneofs)
                    d._buttonText = "buttonText";
            }
            if (m.listType != null && m.hasOwnProperty("listType")) {
                d.listType = o.enums === String ? WAE2E.ListMessage.ListType[m.listType] === undefined ? m.listType : WAE2E.ListMessage.ListType[m.listType] : m.listType;
                if (o.oneofs)
                    d._listType = "listType";
            }
            if (m.sections && m.sections.length) {
                d.sections = [];
                for (var j = 0; j < m.sections.length; ++j) {
                    d.sections[j] = WAE2E.ListMessage.Section.toObject(m.sections[j], o);
                }
            }
            if (m.productListInfo != null && m.hasOwnProperty("productListInfo")) {
                d.productListInfo = WAE2E.ListMessage.ProductListInfo.toObject(m.productListInfo, o);
                if (o.oneofs)
                    d._productListInfo = "productListInfo";
            }
            if (m.footerText != null && m.hasOwnProperty("footerText")) {
                d.footerText = m.footerText;
                if (o.oneofs)
                    d._footerText = "footerText";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        ListMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ListMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ListMessage";
        };

        ListMessage.ListType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "SINGLE_SELECT"] = 1;
            values[valuesById[2] = "PRODUCT_LIST"] = 2;
            return values;
        })();

        ListMessage.ProductListInfo = (function() {

            function ProductListInfo(p) {
                this.productSections = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ProductListInfo.prototype.productSections = $util.emptyArray;
            ProductListInfo.prototype.headerImage = null;
            ProductListInfo.prototype.businessOwnerJID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductListInfo.prototype, "_headerImage", {
                get: $util.oneOfGetter($oneOfFields = ["headerImage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductListInfo.prototype, "_businessOwnerJID", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProductListInfo.create = function create(properties) {
                return new ProductListInfo(properties);
            };

            ProductListInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.productSections != null && m.productSections.length) {
                    for (var i = 0; i < m.productSections.length; ++i)
                        WAE2E.ListMessage.ProductSection.encode(m.productSections[i], w.uint32(10).fork()).ldelim();
                }
                if (m.headerImage != null && Object.hasOwnProperty.call(m, "headerImage"))
                    WAE2E.ListMessage.ProductListHeaderImage.encode(m.headerImage, w.uint32(18).fork()).ldelim();
                if (m.businessOwnerJID != null && Object.hasOwnProperty.call(m, "businessOwnerJID"))
                    w.uint32(26).string(m.businessOwnerJID);
                return w;
            };

            ProductListInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.ProductListInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            if (!(m.productSections && m.productSections.length))
                                m.productSections = [];
                            m.productSections.push(WAE2E.ListMessage.ProductSection.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 2: {
                            m.headerImage = WAE2E.ListMessage.ProductListHeaderImage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.businessOwnerJID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ProductListInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.ProductListInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.ProductListInfo();
                if (d.productSections) {
                    if (!Array.isArray(d.productSections))
                        throw TypeError(".WAE2E.ListMessage.ProductListInfo.productSections: array expected");
                    m.productSections = [];
                    for (var i = 0; i < d.productSections.length; ++i) {
                        if (typeof d.productSections[i] !== "object")
                            throw TypeError(".WAE2E.ListMessage.ProductListInfo.productSections: object expected");
                        m.productSections[i] = WAE2E.ListMessage.ProductSection.fromObject(d.productSections[i], n + 1);
                    }
                }
                if (d.headerImage != null) {
                    if (typeof d.headerImage !== "object")
                        throw TypeError(".WAE2E.ListMessage.ProductListInfo.headerImage: object expected");
                    m.headerImage = WAE2E.ListMessage.ProductListHeaderImage.fromObject(d.headerImage, n + 1);
                }
                if (d.businessOwnerJID != null) {
                    m.businessOwnerJID = String(d.businessOwnerJID);
                }
                return m;
            };

            ProductListInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.productSections = [];
                }
                if (m.productSections && m.productSections.length) {
                    d.productSections = [];
                    for (var j = 0; j < m.productSections.length; ++j) {
                        d.productSections[j] = WAE2E.ListMessage.ProductSection.toObject(m.productSections[j], o);
                    }
                }
                if (m.headerImage != null && m.hasOwnProperty("headerImage")) {
                    d.headerImage = WAE2E.ListMessage.ProductListHeaderImage.toObject(m.headerImage, o);
                    if (o.oneofs)
                        d._headerImage = "headerImage";
                }
                if (m.businessOwnerJID != null && m.hasOwnProperty("businessOwnerJID")) {
                    d.businessOwnerJID = m.businessOwnerJID;
                    if (o.oneofs)
                        d._businessOwnerJID = "businessOwnerJID";
                }
                return d;
            };

            ProductListInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProductListInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.ProductListInfo";
            };

            return ProductListInfo;
        })();

        ListMessage.ProductListHeaderImage = (function() {

            function ProductListHeaderImage(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ProductListHeaderImage.prototype.productID = null;
            ProductListHeaderImage.prototype.JPEGThumbnail = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductListHeaderImage.prototype, "_productID", {
                get: $util.oneOfGetter($oneOfFields = ["productID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductListHeaderImage.prototype, "_JPEGThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProductListHeaderImage.create = function create(properties) {
                return new ProductListHeaderImage(properties);
            };

            ProductListHeaderImage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.productID != null && Object.hasOwnProperty.call(m, "productID"))
                    w.uint32(10).string(m.productID);
                if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                    w.uint32(18).bytes(m.JPEGThumbnail);
                return w;
            };

            ProductListHeaderImage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.ProductListHeaderImage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.productID = r.string();
                            break;
                        }
                    case 2: {
                            m.JPEGThumbnail = r.bytes();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ProductListHeaderImage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.ProductListHeaderImage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.ProductListHeaderImage();
                if (d.productID != null) {
                    m.productID = String(d.productID);
                }
                if (d.JPEGThumbnail != null) {
                    if (typeof d.JPEGThumbnail === "string")
                        $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                    else if (d.JPEGThumbnail.length >= 0)
                        m.JPEGThumbnail = d.JPEGThumbnail;
                }
                return m;
            };

            ProductListHeaderImage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.productID != null && m.hasOwnProperty("productID")) {
                    d.productID = m.productID;
                    if (o.oneofs)
                        d._productID = "productID";
                }
                if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                    d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                    if (o.oneofs)
                        d._JPEGThumbnail = "JPEGThumbnail";
                }
                return d;
            };

            ProductListHeaderImage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProductListHeaderImage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.ProductListHeaderImage";
            };

            return ProductListHeaderImage;
        })();

        ListMessage.ProductSection = (function() {

            function ProductSection(p) {
                this.products = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ProductSection.prototype.title = null;
            ProductSection.prototype.products = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSection.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProductSection.create = function create(properties) {
                return new ProductSection(properties);
            };

            ProductSection.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.products != null && m.products.length) {
                    for (var i = 0; i < m.products.length; ++i)
                        WAE2E.ListMessage.Product.encode(m.products[i], w.uint32(18).fork()).ldelim();
                }
                return w;
            };

            ProductSection.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.ProductSection();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            if (!(m.products && m.products.length))
                                m.products = [];
                            m.products.push(WAE2E.ListMessage.Product.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ProductSection.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.ProductSection)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.ProductSection();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.products) {
                    if (!Array.isArray(d.products))
                        throw TypeError(".WAE2E.ListMessage.ProductSection.products: array expected");
                    m.products = [];
                    for (var i = 0; i < d.products.length; ++i) {
                        if (typeof d.products[i] !== "object")
                            throw TypeError(".WAE2E.ListMessage.ProductSection.products: object expected");
                        m.products[i] = WAE2E.ListMessage.Product.fromObject(d.products[i], n + 1);
                    }
                }
                return m;
            };

            ProductSection.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.products = [];
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.products && m.products.length) {
                    d.products = [];
                    for (var j = 0; j < m.products.length; ++j) {
                        d.products[j] = WAE2E.ListMessage.Product.toObject(m.products[j], o);
                    }
                }
                return d;
            };

            ProductSection.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProductSection.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.ProductSection";
            };

            return ProductSection;
        })();

        ListMessage.Product = (function() {

            function Product(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Product.prototype.productID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Product.prototype, "_productID", {
                get: $util.oneOfGetter($oneOfFields = ["productID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Product.create = function create(properties) {
                return new Product(properties);
            };

            Product.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.productID != null && Object.hasOwnProperty.call(m, "productID"))
                    w.uint32(10).string(m.productID);
                return w;
            };

            Product.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.Product();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.productID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Product.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.Product)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.Product();
                if (d.productID != null) {
                    m.productID = String(d.productID);
                }
                return m;
            };

            Product.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.productID != null && m.hasOwnProperty("productID")) {
                    d.productID = m.productID;
                    if (o.oneofs)
                        d._productID = "productID";
                }
                return d;
            };

            Product.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Product.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.Product";
            };

            return Product;
        })();

        ListMessage.Section = (function() {

            function Section(p) {
                this.rows = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Section.prototype.title = null;
            Section.prototype.rows = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Section.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Section.create = function create(properties) {
                return new Section(properties);
            };

            Section.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.rows != null && m.rows.length) {
                    for (var i = 0; i < m.rows.length; ++i)
                        WAE2E.ListMessage.Row.encode(m.rows[i], w.uint32(18).fork()).ldelim();
                }
                return w;
            };

            Section.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.Section();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            if (!(m.rows && m.rows.length))
                                m.rows = [];
                            m.rows.push(WAE2E.ListMessage.Row.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Section.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.Section)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.Section();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.rows) {
                    if (!Array.isArray(d.rows))
                        throw TypeError(".WAE2E.ListMessage.Section.rows: array expected");
                    m.rows = [];
                    for (var i = 0; i < d.rows.length; ++i) {
                        if (typeof d.rows[i] !== "object")
                            throw TypeError(".WAE2E.ListMessage.Section.rows: object expected");
                        m.rows[i] = WAE2E.ListMessage.Row.fromObject(d.rows[i], n + 1);
                    }
                }
                return m;
            };

            Section.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.rows = [];
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.rows && m.rows.length) {
                    d.rows = [];
                    for (var j = 0; j < m.rows.length; ++j) {
                        d.rows[j] = WAE2E.ListMessage.Row.toObject(m.rows[j], o);
                    }
                }
                return d;
            };

            Section.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Section.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.Section";
            };

            return Section;
        })();

        ListMessage.Row = (function() {

            function Row(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Row.prototype.title = null;
            Row.prototype.description = null;
            Row.prototype.rowID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Row.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Row.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Row.prototype, "_rowID", {
                get: $util.oneOfGetter($oneOfFields = ["rowID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Row.create = function create(properties) {
                return new Row(properties);
            };

            Row.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                    w.uint32(18).string(m.description);
                if (m.rowID != null && Object.hasOwnProperty.call(m, "rowID"))
                    w.uint32(26).string(m.rowID);
                return w;
            };

            Row.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ListMessage.Row();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            m.description = r.string();
                            break;
                        }
                    case 3: {
                            m.rowID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Row.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ListMessage.Row)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ListMessage.Row();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.description != null) {
                    m.description = String(d.description);
                }
                if (d.rowID != null) {
                    m.rowID = String(d.rowID);
                }
                return m;
            };

            Row.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.description != null && m.hasOwnProperty("description")) {
                    d.description = m.description;
                    if (o.oneofs)
                        d._description = "description";
                }
                if (m.rowID != null && m.hasOwnProperty("rowID")) {
                    d.rowID = m.rowID;
                    if (o.oneofs)
                        d._rowID = "rowID";
                }
                return d;
            };

            Row.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Row.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ListMessage.Row";
            };

            return Row;
        })();

        return ListMessage;
    })();

    WAE2E.OrderMessage = (function() {

        function OrderMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        OrderMessage.prototype.orderID = null;
        OrderMessage.prototype.thumbnail = null;
        OrderMessage.prototype.itemCount = null;
        OrderMessage.prototype.status = null;
        OrderMessage.prototype.surface = null;
        OrderMessage.prototype.message = null;
        OrderMessage.prototype.orderTitle = null;
        OrderMessage.prototype.sellerJID = null;
        OrderMessage.prototype.token = null;
        OrderMessage.prototype.totalAmount1000 = null;
        OrderMessage.prototype.totalCurrencyCode = null;
        OrderMessage.prototype.contextInfo = null;
        OrderMessage.prototype.messageVersion = null;
        OrderMessage.prototype.orderRequestMessageID = null;
        OrderMessage.prototype.catalogType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_orderID", {
            get: $util.oneOfGetter($oneOfFields = ["orderID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_thumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_itemCount", {
            get: $util.oneOfGetter($oneOfFields = ["itemCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_surface", {
            get: $util.oneOfGetter($oneOfFields = ["surface"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_orderTitle", {
            get: $util.oneOfGetter($oneOfFields = ["orderTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_sellerJID", {
            get: $util.oneOfGetter($oneOfFields = ["sellerJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_token", {
            get: $util.oneOfGetter($oneOfFields = ["token"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_totalAmount1000", {
            get: $util.oneOfGetter($oneOfFields = ["totalAmount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_totalCurrencyCode", {
            get: $util.oneOfGetter($oneOfFields = ["totalCurrencyCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_messageVersion", {
            get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_orderRequestMessageID", {
            get: $util.oneOfGetter($oneOfFields = ["orderRequestMessageID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(OrderMessage.prototype, "_catalogType", {
            get: $util.oneOfGetter($oneOfFields = ["catalogType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        OrderMessage.create = function create(properties) {
            return new OrderMessage(properties);
        };

        OrderMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.orderID != null && Object.hasOwnProperty.call(m, "orderID"))
                w.uint32(10).string(m.orderID);
            if (m.thumbnail != null && Object.hasOwnProperty.call(m, "thumbnail"))
                w.uint32(18).bytes(m.thumbnail);
            if (m.itemCount != null && Object.hasOwnProperty.call(m, "itemCount"))
                w.uint32(24).int32(m.itemCount);
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(32).int32(m.status);
            if (m.surface != null && Object.hasOwnProperty.call(m, "surface"))
                w.uint32(40).int32(m.surface);
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                w.uint32(50).string(m.message);
            if (m.orderTitle != null && Object.hasOwnProperty.call(m, "orderTitle"))
                w.uint32(58).string(m.orderTitle);
            if (m.sellerJID != null && Object.hasOwnProperty.call(m, "sellerJID"))
                w.uint32(66).string(m.sellerJID);
            if (m.token != null && Object.hasOwnProperty.call(m, "token"))
                w.uint32(74).string(m.token);
            if (m.totalAmount1000 != null && Object.hasOwnProperty.call(m, "totalAmount1000"))
                w.uint32(80).int64(m.totalAmount1000);
            if (m.totalCurrencyCode != null && Object.hasOwnProperty.call(m, "totalCurrencyCode"))
                w.uint32(90).string(m.totalCurrencyCode);
            if (m.messageVersion != null && Object.hasOwnProperty.call(m, "messageVersion"))
                w.uint32(96).int32(m.messageVersion);
            if (m.orderRequestMessageID != null && Object.hasOwnProperty.call(m, "orderRequestMessageID"))
                WACommon.MessageKey.encode(m.orderRequestMessageID, w.uint32(106).fork()).ldelim();
            if (m.catalogType != null && Object.hasOwnProperty.call(m, "catalogType"))
                w.uint32(122).string(m.catalogType);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        OrderMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.OrderMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.orderID = r.string();
                        break;
                    }
                case 2: {
                        m.thumbnail = r.bytes();
                        break;
                    }
                case 3: {
                        m.itemCount = r.int32();
                        break;
                    }
                case 4: {
                        m.status = r.int32();
                        break;
                    }
                case 5: {
                        m.surface = r.int32();
                        break;
                    }
                case 6: {
                        m.message = r.string();
                        break;
                    }
                case 7: {
                        m.orderTitle = r.string();
                        break;
                    }
                case 8: {
                        m.sellerJID = r.string();
                        break;
                    }
                case 9: {
                        m.token = r.string();
                        break;
                    }
                case 10: {
                        m.totalAmount1000 = r.int64();
                        break;
                    }
                case 11: {
                        m.totalCurrencyCode = r.string();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 12: {
                        m.messageVersion = r.int32();
                        break;
                    }
                case 13: {
                        m.orderRequestMessageID = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.catalogType = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        OrderMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.OrderMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.OrderMessage();
            if (d.orderID != null) {
                m.orderID = String(d.orderID);
            }
            if (d.thumbnail != null) {
                if (typeof d.thumbnail === "string")
                    $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                else if (d.thumbnail.length >= 0)
                    m.thumbnail = d.thumbnail;
            }
            if (d.itemCount != null) {
                m.itemCount = d.itemCount | 0;
            }
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "INQUIRY":
            case 1:
                m.status = 1;
                break;
            case "ACCEPTED":
            case 2:
                m.status = 2;
                break;
            case "DECLINED":
            case 3:
                m.status = 3;
                break;
            }
            switch (d.surface) {
            default:
                if (typeof d.surface === "number") {
                    m.surface = d.surface;
                    break;
                }
                break;
            case "CATALOG":
            case 1:
                m.surface = 1;
                break;
            }
            if (d.message != null) {
                m.message = String(d.message);
            }
            if (d.orderTitle != null) {
                m.orderTitle = String(d.orderTitle);
            }
            if (d.sellerJID != null) {
                m.sellerJID = String(d.sellerJID);
            }
            if (d.token != null) {
                m.token = String(d.token);
            }
            if (d.totalAmount1000 != null) {
                if ($util.Long)
                    (m.totalAmount1000 = $util.Long.fromValue(d.totalAmount1000)).unsigned = false;
                else if (typeof d.totalAmount1000 === "string")
                    m.totalAmount1000 = parseInt(d.totalAmount1000, 10);
                else if (typeof d.totalAmount1000 === "number")
                    m.totalAmount1000 = d.totalAmount1000;
                else if (typeof d.totalAmount1000 === "object")
                    m.totalAmount1000 = new $util.LongBits(d.totalAmount1000.low >>> 0, d.totalAmount1000.high >>> 0).toNumber();
            }
            if (d.totalCurrencyCode != null) {
                m.totalCurrencyCode = String(d.totalCurrencyCode);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.OrderMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.messageVersion != null) {
                m.messageVersion = d.messageVersion | 0;
            }
            if (d.orderRequestMessageID != null) {
                if (typeof d.orderRequestMessageID !== "object")
                    throw TypeError(".WAE2E.OrderMessage.orderRequestMessageID: object expected");
                m.orderRequestMessageID = WACommon.MessageKey.fromObject(d.orderRequestMessageID, n + 1);
            }
            if (d.catalogType != null) {
                m.catalogType = String(d.catalogType);
            }
            return m;
        };

        OrderMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.orderID != null && m.hasOwnProperty("orderID")) {
                d.orderID = m.orderID;
                if (o.oneofs)
                    d._orderID = "orderID";
            }
            if (m.thumbnail != null && m.hasOwnProperty("thumbnail")) {
                d.thumbnail = o.bytes === String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                if (o.oneofs)
                    d._thumbnail = "thumbnail";
            }
            if (m.itemCount != null && m.hasOwnProperty("itemCount")) {
                d.itemCount = m.itemCount;
                if (o.oneofs)
                    d._itemCount = "itemCount";
            }
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? WAE2E.OrderMessage.OrderStatus[m.status] === undefined ? m.status : WAE2E.OrderMessage.OrderStatus[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            if (m.surface != null && m.hasOwnProperty("surface")) {
                d.surface = o.enums === String ? WAE2E.OrderMessage.OrderSurface[m.surface] === undefined ? m.surface : WAE2E.OrderMessage.OrderSurface[m.surface] : m.surface;
                if (o.oneofs)
                    d._surface = "surface";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = m.message;
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.orderTitle != null && m.hasOwnProperty("orderTitle")) {
                d.orderTitle = m.orderTitle;
                if (o.oneofs)
                    d._orderTitle = "orderTitle";
            }
            if (m.sellerJID != null && m.hasOwnProperty("sellerJID")) {
                d.sellerJID = m.sellerJID;
                if (o.oneofs)
                    d._sellerJID = "sellerJID";
            }
            if (m.token != null && m.hasOwnProperty("token")) {
                d.token = m.token;
                if (o.oneofs)
                    d._token = "token";
            }
            if (m.totalAmount1000 != null && m.hasOwnProperty("totalAmount1000")) {
                if (typeof m.totalAmount1000 === "number")
                    d.totalAmount1000 = o.longs === String ? String(m.totalAmount1000) : m.totalAmount1000;
                else
                    d.totalAmount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.totalAmount1000) : o.longs === Number ? new $util.LongBits(m.totalAmount1000.low >>> 0, m.totalAmount1000.high >>> 0).toNumber() : m.totalAmount1000;
                if (o.oneofs)
                    d._totalAmount1000 = "totalAmount1000";
            }
            if (m.totalCurrencyCode != null && m.hasOwnProperty("totalCurrencyCode")) {
                d.totalCurrencyCode = m.totalCurrencyCode;
                if (o.oneofs)
                    d._totalCurrencyCode = "totalCurrencyCode";
            }
            if (m.messageVersion != null && m.hasOwnProperty("messageVersion")) {
                d.messageVersion = m.messageVersion;
                if (o.oneofs)
                    d._messageVersion = "messageVersion";
            }
            if (m.orderRequestMessageID != null && m.hasOwnProperty("orderRequestMessageID")) {
                d.orderRequestMessageID = WACommon.MessageKey.toObject(m.orderRequestMessageID, o);
                if (o.oneofs)
                    d._orderRequestMessageID = "orderRequestMessageID";
            }
            if (m.catalogType != null && m.hasOwnProperty("catalogType")) {
                d.catalogType = m.catalogType;
                if (o.oneofs)
                    d._catalogType = "catalogType";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        OrderMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        OrderMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.OrderMessage";
        };

        OrderMessage.OrderSurface = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "CATALOG"] = 1;
            return values;
        })();

        OrderMessage.OrderStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "INQUIRY"] = 1;
            values[valuesById[2] = "ACCEPTED"] = 2;
            values[valuesById[3] = "DECLINED"] = 3;
            return values;
        })();

        return OrderMessage;
    })();

    WAE2E.StatusQuotedMessage = (function() {

        function StatusQuotedMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusQuotedMessage.prototype.type = null;
        StatusQuotedMessage.prototype.text = null;
        StatusQuotedMessage.prototype.thumbnail = null;
        StatusQuotedMessage.prototype.originalStatusID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuotedMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuotedMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuotedMessage.prototype, "_thumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuotedMessage.prototype, "_originalStatusID", {
            get: $util.oneOfGetter($oneOfFields = ["originalStatusID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusQuotedMessage.create = function create(properties) {
            return new StatusQuotedMessage(properties);
        };

        StatusQuotedMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            if (m.thumbnail != null && Object.hasOwnProperty.call(m, "thumbnail"))
                w.uint32(26).bytes(m.thumbnail);
            if (m.originalStatusID != null && Object.hasOwnProperty.call(m, "originalStatusID"))
                WACommon.MessageKey.encode(m.originalStatusID, w.uint32(34).fork()).ldelim();
            return w;
        };

        StatusQuotedMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StatusQuotedMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                case 2: {
                        m.text = r.string();
                        break;
                    }
                case 3: {
                        m.thumbnail = r.bytes();
                        break;
                    }
                case 4: {
                        m.originalStatusID = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusQuotedMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StatusQuotedMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StatusQuotedMessage();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "QUESTION_ANSWER":
            case 1:
                m.type = 1;
                break;
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.thumbnail != null) {
                if (typeof d.thumbnail === "string")
                    $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                else if (d.thumbnail.length >= 0)
                    m.thumbnail = d.thumbnail;
            }
            if (d.originalStatusID != null) {
                if (typeof d.originalStatusID !== "object")
                    throw TypeError(".WAE2E.StatusQuotedMessage.originalStatusID: object expected");
                m.originalStatusID = WACommon.MessageKey.fromObject(d.originalStatusID, n + 1);
            }
            return m;
        };

        StatusQuotedMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.StatusQuotedMessage.StatusQuotedMessageType[m.type] === undefined ? m.type : WAE2E.StatusQuotedMessage.StatusQuotedMessageType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.thumbnail != null && m.hasOwnProperty("thumbnail")) {
                d.thumbnail = o.bytes === String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                if (o.oneofs)
                    d._thumbnail = "thumbnail";
            }
            if (m.originalStatusID != null && m.hasOwnProperty("originalStatusID")) {
                d.originalStatusID = WACommon.MessageKey.toObject(m.originalStatusID, o);
                if (o.oneofs)
                    d._originalStatusID = "originalStatusID";
            }
            return d;
        };

        StatusQuotedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusQuotedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StatusQuotedMessage";
        };

        StatusQuotedMessage.StatusQuotedMessageType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "QUESTION_ANSWER"] = 1;
            return values;
        })();

        return StatusQuotedMessage;
    })();

    WAE2E.SplitPaymentParticipant = (function() {

        function SplitPaymentParticipant(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SplitPaymentParticipant.prototype.JID = null;
        SplitPaymentParticipant.prototype.amount = null;
        SplitPaymentParticipant.prototype.status = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentParticipant.prototype, "_JID", {
            get: $util.oneOfGetter($oneOfFields = ["JID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentParticipant.prototype, "_amount", {
            get: $util.oneOfGetter($oneOfFields = ["amount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentParticipant.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SplitPaymentParticipant.create = function create(properties) {
            return new SplitPaymentParticipant(properties);
        };

        SplitPaymentParticipant.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.JID != null && Object.hasOwnProperty.call(m, "JID"))
                w.uint32(10).string(m.JID);
            if (m.amount != null && Object.hasOwnProperty.call(m, "amount"))
                WAE2E.Money.encode(m.amount, w.uint32(18).fork()).ldelim();
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(24).int32(m.status);
            return w;
        };

        SplitPaymentParticipant.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.SplitPaymentParticipant();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.JID = r.string();
                        break;
                    }
                case 2: {
                        m.amount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.status = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SplitPaymentParticipant.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.SplitPaymentParticipant)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.SplitPaymentParticipant();
            if (d.JID != null) {
                m.JID = String(d.JID);
            }
            if (d.amount != null) {
                if (typeof d.amount !== "object")
                    throw TypeError(".WAE2E.SplitPaymentParticipant.amount: object expected");
                m.amount = WAE2E.Money.fromObject(d.amount, n + 1);
            }
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "PENDING":
            case 0:
                m.status = 0;
                break;
            case "PAID":
            case 1:
                m.status = 1;
                break;
            }
            return m;
        };

        SplitPaymentParticipant.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.JID != null && m.hasOwnProperty("JID")) {
                d.JID = m.JID;
                if (o.oneofs)
                    d._JID = "JID";
            }
            if (m.amount != null && m.hasOwnProperty("amount")) {
                d.amount = WAE2E.Money.toObject(m.amount, o);
                if (o.oneofs)
                    d._amount = "amount";
            }
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? WAE2E.SplitPaymentParticipant.SplitPaymentStatus[m.status] === undefined ? m.status : WAE2E.SplitPaymentParticipant.SplitPaymentStatus[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            return d;
        };

        SplitPaymentParticipant.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SplitPaymentParticipant.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.SplitPaymentParticipant";
        };

        SplitPaymentParticipant.SplitPaymentStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "PENDING"] = 0;
            values[valuesById[1] = "PAID"] = 1;
            return values;
        })();

        return SplitPaymentParticipant;
    })();

    WAE2E.P2PPaymentReminderNotification = (function() {

        function P2PPaymentReminderNotification(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        P2PPaymentReminderNotification.prototype.reminderID = null;
        P2PPaymentReminderNotification.prototype.amount = null;
        P2PPaymentReminderNotification.prototype.frequency = null;
        P2PPaymentReminderNotification.prototype.nextReminderTimestamp = null;
        P2PPaymentReminderNotification.prototype.expiryTimestamp = null;
        P2PPaymentReminderNotification.prototype.state = null;
        P2PPaymentReminderNotification.prototype.description = null;
        P2PPaymentReminderNotification.prototype.creatorJID = null;
        P2PPaymentReminderNotification.prototype.receiverJID = null;
        P2PPaymentReminderNotification.prototype.upiID = null;
        P2PPaymentReminderNotification.prototype.createdTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_reminderID", {
            get: $util.oneOfGetter($oneOfFields = ["reminderID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_amount", {
            get: $util.oneOfGetter($oneOfFields = ["amount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_frequency", {
            get: $util.oneOfGetter($oneOfFields = ["frequency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_nextReminderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["nextReminderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_state", {
            get: $util.oneOfGetter($oneOfFields = ["state"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_creatorJID", {
            get: $util.oneOfGetter($oneOfFields = ["creatorJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_receiverJID", {
            get: $util.oneOfGetter($oneOfFields = ["receiverJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_upiID", {
            get: $util.oneOfGetter($oneOfFields = ["upiID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(P2PPaymentReminderNotification.prototype, "_createdTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["createdTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        P2PPaymentReminderNotification.create = function create(properties) {
            return new P2PPaymentReminderNotification(properties);
        };

        P2PPaymentReminderNotification.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.reminderID != null && Object.hasOwnProperty.call(m, "reminderID"))
                w.uint32(10).string(m.reminderID);
            if (m.amount != null && Object.hasOwnProperty.call(m, "amount"))
                WAE2E.Money.encode(m.amount, w.uint32(18).fork()).ldelim();
            if (m.frequency != null && Object.hasOwnProperty.call(m, "frequency"))
                w.uint32(24).int32(m.frequency);
            if (m.nextReminderTimestamp != null && Object.hasOwnProperty.call(m, "nextReminderTimestamp"))
                w.uint32(32).int64(m.nextReminderTimestamp);
            if (m.expiryTimestamp != null && Object.hasOwnProperty.call(m, "expiryTimestamp"))
                w.uint32(40).int64(m.expiryTimestamp);
            if (m.state != null && Object.hasOwnProperty.call(m, "state"))
                w.uint32(48).int32(m.state);
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(58).string(m.description);
            if (m.creatorJID != null && Object.hasOwnProperty.call(m, "creatorJID"))
                w.uint32(66).string(m.creatorJID);
            if (m.receiverJID != null && Object.hasOwnProperty.call(m, "receiverJID"))
                w.uint32(74).string(m.receiverJID);
            if (m.upiID != null && Object.hasOwnProperty.call(m, "upiID"))
                w.uint32(82).string(m.upiID);
            if (m.createdTimestamp != null && Object.hasOwnProperty.call(m, "createdTimestamp"))
                w.uint32(88).int64(m.createdTimestamp);
            return w;
        };

        P2PPaymentReminderNotification.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.P2PPaymentReminderNotification();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.reminderID = r.string();
                        break;
                    }
                case 2: {
                        m.amount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.frequency = r.int32();
                        break;
                    }
                case 4: {
                        m.nextReminderTimestamp = r.int64();
                        break;
                    }
                case 5: {
                        m.expiryTimestamp = r.int64();
                        break;
                    }
                case 6: {
                        m.state = r.int32();
                        break;
                    }
                case 7: {
                        m.description = r.string();
                        break;
                    }
                case 8: {
                        m.creatorJID = r.string();
                        break;
                    }
                case 9: {
                        m.receiverJID = r.string();
                        break;
                    }
                case 10: {
                        m.upiID = r.string();
                        break;
                    }
                case 11: {
                        m.createdTimestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        P2PPaymentReminderNotification.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.P2PPaymentReminderNotification)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.P2PPaymentReminderNotification();
            if (d.reminderID != null) {
                m.reminderID = String(d.reminderID);
            }
            if (d.amount != null) {
                if (typeof d.amount !== "object")
                    throw TypeError(".WAE2E.P2PPaymentReminderNotification.amount: object expected");
                m.amount = WAE2E.Money.fromObject(d.amount, n + 1);
            }
            switch (d.frequency) {
            default:
                if (typeof d.frequency === "number") {
                    m.frequency = d.frequency;
                    break;
                }
                break;
            case "UNKNOWN_FREQUENCY":
            case 0:
                m.frequency = 0;
                break;
            case "WEEKLY":
            case 1:
                m.frequency = 1;
                break;
            case "BIWEEKLY":
            case 2:
                m.frequency = 2;
                break;
            case "MONTHLY":
            case 3:
                m.frequency = 3;
                break;
            case "CUSTOM":
            case 4:
                m.frequency = 4;
                break;
            }
            if (d.nextReminderTimestamp != null) {
                if ($util.Long)
                    (m.nextReminderTimestamp = $util.Long.fromValue(d.nextReminderTimestamp)).unsigned = false;
                else if (typeof d.nextReminderTimestamp === "string")
                    m.nextReminderTimestamp = parseInt(d.nextReminderTimestamp, 10);
                else if (typeof d.nextReminderTimestamp === "number")
                    m.nextReminderTimestamp = d.nextReminderTimestamp;
                else if (typeof d.nextReminderTimestamp === "object")
                    m.nextReminderTimestamp = new $util.LongBits(d.nextReminderTimestamp.low >>> 0, d.nextReminderTimestamp.high >>> 0).toNumber();
            }
            if (d.expiryTimestamp != null) {
                if ($util.Long)
                    (m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp)).unsigned = false;
                else if (typeof d.expiryTimestamp === "string")
                    m.expiryTimestamp = parseInt(d.expiryTimestamp, 10);
                else if (typeof d.expiryTimestamp === "number")
                    m.expiryTimestamp = d.expiryTimestamp;
                else if (typeof d.expiryTimestamp === "object")
                    m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber();
            }
            switch (d.state) {
            default:
                if (typeof d.state === "number") {
                    m.state = d.state;
                    break;
                }
                break;
            case "UNKNOWN_STATE":
            case 0:
                m.state = 0;
                break;
            case "ACTIVE":
            case 1:
                m.state = 1;
                break;
            case "PAUSED":
            case 2:
                m.state = 2;
                break;
            case "STOPPED":
            case 3:
                m.state = 3;
                break;
            case "EXPIRED":
            case 4:
                m.state = 4;
                break;
            case "CANCELLED":
            case 5:
                m.state = 5;
                break;
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            if (d.creatorJID != null) {
                m.creatorJID = String(d.creatorJID);
            }
            if (d.receiverJID != null) {
                m.receiverJID = String(d.receiverJID);
            }
            if (d.upiID != null) {
                m.upiID = String(d.upiID);
            }
            if (d.createdTimestamp != null) {
                if ($util.Long)
                    (m.createdTimestamp = $util.Long.fromValue(d.createdTimestamp)).unsigned = false;
                else if (typeof d.createdTimestamp === "string")
                    m.createdTimestamp = parseInt(d.createdTimestamp, 10);
                else if (typeof d.createdTimestamp === "number")
                    m.createdTimestamp = d.createdTimestamp;
                else if (typeof d.createdTimestamp === "object")
                    m.createdTimestamp = new $util.LongBits(d.createdTimestamp.low >>> 0, d.createdTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        P2PPaymentReminderNotification.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.reminderID != null && m.hasOwnProperty("reminderID")) {
                d.reminderID = m.reminderID;
                if (o.oneofs)
                    d._reminderID = "reminderID";
            }
            if (m.amount != null && m.hasOwnProperty("amount")) {
                d.amount = WAE2E.Money.toObject(m.amount, o);
                if (o.oneofs)
                    d._amount = "amount";
            }
            if (m.frequency != null && m.hasOwnProperty("frequency")) {
                d.frequency = o.enums === String ? WAE2E.P2PPaymentReminderNotification.ReminderFrequency[m.frequency] === undefined ? m.frequency : WAE2E.P2PPaymentReminderNotification.ReminderFrequency[m.frequency] : m.frequency;
                if (o.oneofs)
                    d._frequency = "frequency";
            }
            if (m.nextReminderTimestamp != null && m.hasOwnProperty("nextReminderTimestamp")) {
                if (typeof m.nextReminderTimestamp === "number")
                    d.nextReminderTimestamp = o.longs === String ? String(m.nextReminderTimestamp) : m.nextReminderTimestamp;
                else
                    d.nextReminderTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.nextReminderTimestamp) : o.longs === Number ? new $util.LongBits(m.nextReminderTimestamp.low >>> 0, m.nextReminderTimestamp.high >>> 0).toNumber() : m.nextReminderTimestamp;
                if (o.oneofs)
                    d._nextReminderTimestamp = "nextReminderTimestamp";
            }
            if (m.expiryTimestamp != null && m.hasOwnProperty("expiryTimestamp")) {
                if (typeof m.expiryTimestamp === "number")
                    d.expiryTimestamp = o.longs === String ? String(m.expiryTimestamp) : m.expiryTimestamp;
                else
                    d.expiryTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber() : m.expiryTimestamp;
                if (o.oneofs)
                    d._expiryTimestamp = "expiryTimestamp";
            }
            if (m.state != null && m.hasOwnProperty("state")) {
                d.state = o.enums === String ? WAE2E.P2PPaymentReminderNotification.ReminderState[m.state] === undefined ? m.state : WAE2E.P2PPaymentReminderNotification.ReminderState[m.state] : m.state;
                if (o.oneofs)
                    d._state = "state";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            if (m.creatorJID != null && m.hasOwnProperty("creatorJID")) {
                d.creatorJID = m.creatorJID;
                if (o.oneofs)
                    d._creatorJID = "creatorJID";
            }
            if (m.receiverJID != null && m.hasOwnProperty("receiverJID")) {
                d.receiverJID = m.receiverJID;
                if (o.oneofs)
                    d._receiverJID = "receiverJID";
            }
            if (m.upiID != null && m.hasOwnProperty("upiID")) {
                d.upiID = m.upiID;
                if (o.oneofs)
                    d._upiID = "upiID";
            }
            if (m.createdTimestamp != null && m.hasOwnProperty("createdTimestamp")) {
                if (typeof m.createdTimestamp === "number")
                    d.createdTimestamp = o.longs === String ? String(m.createdTimestamp) : m.createdTimestamp;
                else
                    d.createdTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.createdTimestamp) : o.longs === Number ? new $util.LongBits(m.createdTimestamp.low >>> 0, m.createdTimestamp.high >>> 0).toNumber() : m.createdTimestamp;
                if (o.oneofs)
                    d._createdTimestamp = "createdTimestamp";
            }
            return d;
        };

        P2PPaymentReminderNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        P2PPaymentReminderNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.P2PPaymentReminderNotification";
        };

        P2PPaymentReminderNotification.ReminderState = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATE"] = 0;
            values[valuesById[1] = "ACTIVE"] = 1;
            values[valuesById[2] = "PAUSED"] = 2;
            values[valuesById[3] = "STOPPED"] = 3;
            values[valuesById[4] = "EXPIRED"] = 4;
            values[valuesById[5] = "CANCELLED"] = 5;
            return values;
        })();

        P2PPaymentReminderNotification.ReminderFrequency = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_FREQUENCY"] = 0;
            values[valuesById[1] = "WEEKLY"] = 1;
            values[valuesById[2] = "BIWEEKLY"] = 2;
            values[valuesById[3] = "MONTHLY"] = 3;
            values[valuesById[4] = "CUSTOM"] = 4;
            return values;
        })();

        return P2PPaymentReminderNotification;
    })();

    WAE2E.PaymentInviteMessage = (function() {

        function PaymentInviteMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PaymentInviteMessage.prototype.serviceType = null;
        PaymentInviteMessage.prototype.expiryTimestamp = null;
        PaymentInviteMessage.prototype.incentiveEligible = null;
        PaymentInviteMessage.prototype.referralID = null;
        PaymentInviteMessage.prototype.inviteType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInviteMessage.prototype, "_serviceType", {
            get: $util.oneOfGetter($oneOfFields = ["serviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInviteMessage.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInviteMessage.prototype, "_incentiveEligible", {
            get: $util.oneOfGetter($oneOfFields = ["incentiveEligible"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInviteMessage.prototype, "_referralID", {
            get: $util.oneOfGetter($oneOfFields = ["referralID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentInviteMessage.prototype, "_inviteType", {
            get: $util.oneOfGetter($oneOfFields = ["inviteType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentInviteMessage.create = function create(properties) {
            return new PaymentInviteMessage(properties);
        };

        PaymentInviteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.serviceType != null && Object.hasOwnProperty.call(m, "serviceType"))
                w.uint32(8).int32(m.serviceType);
            if (m.expiryTimestamp != null && Object.hasOwnProperty.call(m, "expiryTimestamp"))
                w.uint32(16).int64(m.expiryTimestamp);
            if (m.incentiveEligible != null && Object.hasOwnProperty.call(m, "incentiveEligible"))
                w.uint32(24).bool(m.incentiveEligible);
            if (m.referralID != null && Object.hasOwnProperty.call(m, "referralID"))
                w.uint32(34).string(m.referralID);
            if (m.inviteType != null && Object.hasOwnProperty.call(m, "inviteType"))
                w.uint32(40).int32(m.inviteType);
            return w;
        };

        PaymentInviteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentInviteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.serviceType = r.int32();
                        break;
                    }
                case 2: {
                        m.expiryTimestamp = r.int64();
                        break;
                    }
                case 3: {
                        m.incentiveEligible = r.bool();
                        break;
                    }
                case 4: {
                        m.referralID = r.string();
                        break;
                    }
                case 5: {
                        m.inviteType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PaymentInviteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PaymentInviteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PaymentInviteMessage();
            switch (d.serviceType) {
            default:
                if (typeof d.serviceType === "number") {
                    m.serviceType = d.serviceType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.serviceType = 0;
                break;
            case "FBPAY":
            case 1:
                m.serviceType = 1;
                break;
            case "NOVI":
            case 2:
                m.serviceType = 2;
                break;
            case "UPI":
            case 3:
                m.serviceType = 3;
                break;
            }
            if (d.expiryTimestamp != null) {
                if ($util.Long)
                    (m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp)).unsigned = false;
                else if (typeof d.expiryTimestamp === "string")
                    m.expiryTimestamp = parseInt(d.expiryTimestamp, 10);
                else if (typeof d.expiryTimestamp === "number")
                    m.expiryTimestamp = d.expiryTimestamp;
                else if (typeof d.expiryTimestamp === "object")
                    m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber();
            }
            if (d.incentiveEligible != null) {
                m.incentiveEligible = Boolean(d.incentiveEligible);
            }
            if (d.referralID != null) {
                m.referralID = String(d.referralID);
            }
            switch (d.inviteType) {
            default:
                if (typeof d.inviteType === "number") {
                    m.inviteType = d.inviteType;
                    break;
                }
                break;
            case "DEFAULT":
            case 0:
                m.inviteType = 0;
                break;
            case "MAPPER":
            case 1:
                m.inviteType = 1;
                break;
            }
            return m;
        };

        PaymentInviteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.serviceType != null && m.hasOwnProperty("serviceType")) {
                d.serviceType = o.enums === String ? WAE2E.PaymentInviteMessage.ServiceType[m.serviceType] === undefined ? m.serviceType : WAE2E.PaymentInviteMessage.ServiceType[m.serviceType] : m.serviceType;
                if (o.oneofs)
                    d._serviceType = "serviceType";
            }
            if (m.expiryTimestamp != null && m.hasOwnProperty("expiryTimestamp")) {
                if (typeof m.expiryTimestamp === "number")
                    d.expiryTimestamp = o.longs === String ? String(m.expiryTimestamp) : m.expiryTimestamp;
                else
                    d.expiryTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber() : m.expiryTimestamp;
                if (o.oneofs)
                    d._expiryTimestamp = "expiryTimestamp";
            }
            if (m.incentiveEligible != null && m.hasOwnProperty("incentiveEligible")) {
                d.incentiveEligible = m.incentiveEligible;
                if (o.oneofs)
                    d._incentiveEligible = "incentiveEligible";
            }
            if (m.referralID != null && m.hasOwnProperty("referralID")) {
                d.referralID = m.referralID;
                if (o.oneofs)
                    d._referralID = "referralID";
            }
            if (m.inviteType != null && m.hasOwnProperty("inviteType")) {
                d.inviteType = o.enums === String ? WAE2E.PaymentInviteMessage.InviteType[m.inviteType] === undefined ? m.inviteType : WAE2E.PaymentInviteMessage.InviteType[m.inviteType] : m.inviteType;
                if (o.oneofs)
                    d._inviteType = "inviteType";
            }
            return d;
        };

        PaymentInviteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PaymentInviteMessage";
        };

        PaymentInviteMessage.InviteType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "MAPPER"] = 1;
            return values;
        })();

        PaymentInviteMessage.ServiceType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "FBPAY"] = 1;
            values[valuesById[2] = "NOVI"] = 2;
            values[valuesById[3] = "UPI"] = 3;
            return values;
        })();

        return PaymentInviteMessage;
    })();

    WAE2E.HighlyStructuredMessage = (function() {

        function HighlyStructuredMessage(p) {
            this.params = [];
            this.localizableParams = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        HighlyStructuredMessage.prototype.namespace = null;
        HighlyStructuredMessage.prototype.elementName = null;
        HighlyStructuredMessage.prototype.params = $util.emptyArray;
        HighlyStructuredMessage.prototype.fallbackLg = null;
        HighlyStructuredMessage.prototype.fallbackLc = null;
        HighlyStructuredMessage.prototype.localizableParams = $util.emptyArray;
        HighlyStructuredMessage.prototype.deterministicLg = null;
        HighlyStructuredMessage.prototype.deterministicLc = null;
        HighlyStructuredMessage.prototype.hydratedHsm = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_namespace", {
            get: $util.oneOfGetter($oneOfFields = ["namespace"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_elementName", {
            get: $util.oneOfGetter($oneOfFields = ["elementName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLg", {
            get: $util.oneOfGetter($oneOfFields = ["fallbackLg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLc", {
            get: $util.oneOfGetter($oneOfFields = ["fallbackLc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLg", {
            get: $util.oneOfGetter($oneOfFields = ["deterministicLg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLc", {
            get: $util.oneOfGetter($oneOfFields = ["deterministicLc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HighlyStructuredMessage.prototype, "_hydratedHsm", {
            get: $util.oneOfGetter($oneOfFields = ["hydratedHsm"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HighlyStructuredMessage.create = function create(properties) {
            return new HighlyStructuredMessage(properties);
        };

        HighlyStructuredMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.namespace != null && Object.hasOwnProperty.call(m, "namespace"))
                w.uint32(10).string(m.namespace);
            if (m.elementName != null && Object.hasOwnProperty.call(m, "elementName"))
                w.uint32(18).string(m.elementName);
            if (m.params != null && m.params.length) {
                for (var i = 0; i < m.params.length; ++i)
                    w.uint32(26).string(m.params[i]);
            }
            if (m.fallbackLg != null && Object.hasOwnProperty.call(m, "fallbackLg"))
                w.uint32(34).string(m.fallbackLg);
            if (m.fallbackLc != null && Object.hasOwnProperty.call(m, "fallbackLc"))
                w.uint32(42).string(m.fallbackLc);
            if (m.localizableParams != null && m.localizableParams.length) {
                for (var i = 0; i < m.localizableParams.length; ++i)
                    WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.encode(m.localizableParams[i], w.uint32(50).fork()).ldelim();
            }
            if (m.deterministicLg != null && Object.hasOwnProperty.call(m, "deterministicLg"))
                w.uint32(58).string(m.deterministicLg);
            if (m.deterministicLc != null && Object.hasOwnProperty.call(m, "deterministicLc"))
                w.uint32(66).string(m.deterministicLc);
            if (m.hydratedHsm != null && Object.hasOwnProperty.call(m, "hydratedHsm"))
                WAE2E.TemplateMessage.encode(m.hydratedHsm, w.uint32(74).fork()).ldelim();
            return w;
        };

        HighlyStructuredMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.namespace = r.string();
                        break;
                    }
                case 2: {
                        m.elementName = r.string();
                        break;
                    }
                case 3: {
                        if (!(m.params && m.params.length))
                            m.params = [];
                        m.params.push(r.string());
                        break;
                    }
                case 4: {
                        m.fallbackLg = r.string();
                        break;
                    }
                case 5: {
                        m.fallbackLc = r.string();
                        break;
                    }
                case 6: {
                        if (!(m.localizableParams && m.localizableParams.length))
                            m.localizableParams = [];
                        m.localizableParams.push(WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 7: {
                        m.deterministicLg = r.string();
                        break;
                    }
                case 8: {
                        m.deterministicLc = r.string();
                        break;
                    }
                case 9: {
                        m.hydratedHsm = WAE2E.TemplateMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        HighlyStructuredMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.HighlyStructuredMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.HighlyStructuredMessage();
            if (d.namespace != null) {
                m.namespace = String(d.namespace);
            }
            if (d.elementName != null) {
                m.elementName = String(d.elementName);
            }
            if (d.params) {
                if (!Array.isArray(d.params))
                    throw TypeError(".WAE2E.HighlyStructuredMessage.params: array expected");
                m.params = [];
                for (var i = 0; i < d.params.length; ++i) {
                    m.params[i] = String(d.params[i]);
                }
            }
            if (d.fallbackLg != null) {
                m.fallbackLg = String(d.fallbackLg);
            }
            if (d.fallbackLc != null) {
                m.fallbackLc = String(d.fallbackLc);
            }
            if (d.localizableParams) {
                if (!Array.isArray(d.localizableParams))
                    throw TypeError(".WAE2E.HighlyStructuredMessage.localizableParams: array expected");
                m.localizableParams = [];
                for (var i = 0; i < d.localizableParams.length; ++i) {
                    if (typeof d.localizableParams[i] !== "object")
                        throw TypeError(".WAE2E.HighlyStructuredMessage.localizableParams: object expected");
                    m.localizableParams[i] = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.fromObject(d.localizableParams[i], n + 1);
                }
            }
            if (d.deterministicLg != null) {
                m.deterministicLg = String(d.deterministicLg);
            }
            if (d.deterministicLc != null) {
                m.deterministicLc = String(d.deterministicLc);
            }
            if (d.hydratedHsm != null) {
                if (typeof d.hydratedHsm !== "object")
                    throw TypeError(".WAE2E.HighlyStructuredMessage.hydratedHsm: object expected");
                m.hydratedHsm = WAE2E.TemplateMessage.fromObject(d.hydratedHsm, n + 1);
            }
            return m;
        };

        HighlyStructuredMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.params = [];
                d.localizableParams = [];
            }
            if (m.namespace != null && m.hasOwnProperty("namespace")) {
                d.namespace = m.namespace;
                if (o.oneofs)
                    d._namespace = "namespace";
            }
            if (m.elementName != null && m.hasOwnProperty("elementName")) {
                d.elementName = m.elementName;
                if (o.oneofs)
                    d._elementName = "elementName";
            }
            if (m.params && m.params.length) {
                d.params = [];
                for (var j = 0; j < m.params.length; ++j) {
                    d.params[j] = m.params[j];
                }
            }
            if (m.fallbackLg != null && m.hasOwnProperty("fallbackLg")) {
                d.fallbackLg = m.fallbackLg;
                if (o.oneofs)
                    d._fallbackLg = "fallbackLg";
            }
            if (m.fallbackLc != null && m.hasOwnProperty("fallbackLc")) {
                d.fallbackLc = m.fallbackLc;
                if (o.oneofs)
                    d._fallbackLc = "fallbackLc";
            }
            if (m.localizableParams && m.localizableParams.length) {
                d.localizableParams = [];
                for (var j = 0; j < m.localizableParams.length; ++j) {
                    d.localizableParams[j] = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.toObject(m.localizableParams[j], o);
                }
            }
            if (m.deterministicLg != null && m.hasOwnProperty("deterministicLg")) {
                d.deterministicLg = m.deterministicLg;
                if (o.oneofs)
                    d._deterministicLg = "deterministicLg";
            }
            if (m.deterministicLc != null && m.hasOwnProperty("deterministicLc")) {
                d.deterministicLc = m.deterministicLc;
                if (o.oneofs)
                    d._deterministicLc = "deterministicLc";
            }
            if (m.hydratedHsm != null && m.hasOwnProperty("hydratedHsm")) {
                d.hydratedHsm = WAE2E.TemplateMessage.toObject(m.hydratedHsm, o);
                if (o.oneofs)
                    d._hydratedHsm = "hydratedHsm";
            }
            return d;
        };

        HighlyStructuredMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HighlyStructuredMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage";
        };

        HighlyStructuredMessage.HSMLocalizableParameter = (function() {

            function HSMLocalizableParameter(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HSMLocalizableParameter.prototype.currency = null;
            HSMLocalizableParameter.prototype.dateTime = null;
            HSMLocalizableParameter.prototype["default"] = null;

            let $oneOfFields;

            Object.defineProperty(HSMLocalizableParameter.prototype, "paramOneof", {
                get: $util.oneOfGetter($oneOfFields = ["currency", "dateTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HSMLocalizableParameter.prototype, "_default", {
                get: $util.oneOfGetter($oneOfFields = ["default"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HSMLocalizableParameter.create = function create(properties) {
                return new HSMLocalizableParameter(properties);
            };

            HSMLocalizableParameter.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m["default"] != null && Object.hasOwnProperty.call(m, "default"))
                    w.uint32(10).string(m["default"]);
                if (m.currency != null && Object.hasOwnProperty.call(m, "currency"))
                    WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.encode(m.currency, w.uint32(18).fork()).ldelim();
                if (m.dateTime != null && Object.hasOwnProperty.call(m, "dateTime"))
                    WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.encode(m.dateTime, w.uint32(26).fork()).ldelim();
                return w;
            };

            HSMLocalizableParameter.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 2: {
                            m.currency = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.dateTime = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 1: {
                            m["default"] = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HSMLocalizableParameter.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.HighlyStructuredMessage.HSMLocalizableParameter)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter();
                if (d.currency != null) {
                    if (typeof d.currency !== "object")
                        throw TypeError(".WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.currency: object expected");
                    m.currency = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.fromObject(d.currency, n + 1);
                }
                if (d.dateTime != null) {
                    if (typeof d.dateTime !== "object")
                        throw TypeError(".WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.dateTime: object expected");
                    m.dateTime = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.fromObject(d.dateTime, n + 1);
                }
                if (d["default"] != null) {
                    m["default"] = String(d["default"]);
                }
                return m;
            };

            HSMLocalizableParameter.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m["default"] != null && m.hasOwnProperty("default")) {
                    d["default"] = m["default"];
                    if (o.oneofs)
                        d._default = "default";
                }
                if (m.currency != null && m.hasOwnProperty("currency")) {
                    d.currency = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.toObject(m.currency, o);
                    if (o.oneofs)
                        d.paramOneof = "currency";
                }
                if (m.dateTime != null && m.hasOwnProperty("dateTime")) {
                    d.dateTime = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.toObject(m.dateTime, o);
                    if (o.oneofs)
                        d.paramOneof = "dateTime";
                }
                return d;
            };

            HSMLocalizableParameter.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HSMLocalizableParameter.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage.HSMLocalizableParameter";
            };

            HSMLocalizableParameter.HSMDateTime = (function() {

                function HSMDateTime(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                HSMDateTime.prototype.component = null;
                HSMDateTime.prototype.unixEpoch = null;

                let $oneOfFields;

                Object.defineProperty(HSMDateTime.prototype, "datetimeOneof", {
                    get: $util.oneOfGetter($oneOfFields = ["component", "unixEpoch"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HSMDateTime.create = function create(properties) {
                    return new HSMDateTime(properties);
                };

                HSMDateTime.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.component != null && Object.hasOwnProperty.call(m, "component"))
                        WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.encode(m.component, w.uint32(10).fork()).ldelim();
                    if (m.unixEpoch != null && Object.hasOwnProperty.call(m, "unixEpoch"))
                        WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.encode(m.unixEpoch, w.uint32(18).fork()).ldelim();
                    return w;
                };

                HSMDateTime.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.component = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 2: {
                                m.unixEpoch = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                HSMDateTime.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime();
                    if (d.component != null) {
                        if (typeof d.component !== "object")
                            throw TypeError(".WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.component: object expected");
                        m.component = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.fromObject(d.component, n + 1);
                    }
                    if (d.unixEpoch != null) {
                        if (typeof d.unixEpoch !== "object")
                            throw TypeError(".WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.unixEpoch: object expected");
                        m.unixEpoch = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.fromObject(d.unixEpoch, n + 1);
                    }
                    return m;
                };

                HSMDateTime.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.component != null && m.hasOwnProperty("component")) {
                        d.component = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.toObject(m.component, o);
                        if (o.oneofs)
                            d.datetimeOneof = "component";
                    }
                    if (m.unixEpoch != null && m.hasOwnProperty("unixEpoch")) {
                        d.unixEpoch = WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.toObject(m.unixEpoch, o);
                        if (o.oneofs)
                            d.datetimeOneof = "unixEpoch";
                    }
                    return d;
                };

                HSMDateTime.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                HSMDateTime.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime";
                };

                HSMDateTime.HSMDateTimeComponent = (function() {

                    function HSMDateTimeComponent(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    HSMDateTimeComponent.prototype.dayOfWeek = null;
                    HSMDateTimeComponent.prototype.year = null;
                    HSMDateTimeComponent.prototype.month = null;
                    HSMDateTimeComponent.prototype.dayOfMonth = null;
                    HSMDateTimeComponent.prototype.hour = null;
                    HSMDateTimeComponent.prototype.minute = null;
                    HSMDateTimeComponent.prototype.calendar = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfWeek", {
                        get: $util.oneOfGetter($oneOfFields = ["dayOfWeek"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_year", {
                        get: $util.oneOfGetter($oneOfFields = ["year"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_month", {
                        get: $util.oneOfGetter($oneOfFields = ["month"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfMonth", {
                        get: $util.oneOfGetter($oneOfFields = ["dayOfMonth"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_hour", {
                        get: $util.oneOfGetter($oneOfFields = ["hour"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_minute", {
                        get: $util.oneOfGetter($oneOfFields = ["minute"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeComponent.prototype, "_calendar", {
                        get: $util.oneOfGetter($oneOfFields = ["calendar"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    HSMDateTimeComponent.create = function create(properties) {
                        return new HSMDateTimeComponent(properties);
                    };

                    HSMDateTimeComponent.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.dayOfWeek != null && Object.hasOwnProperty.call(m, "dayOfWeek"))
                            w.uint32(8).int32(m.dayOfWeek);
                        if (m.year != null && Object.hasOwnProperty.call(m, "year"))
                            w.uint32(16).uint32(m.year);
                        if (m.month != null && Object.hasOwnProperty.call(m, "month"))
                            w.uint32(24).uint32(m.month);
                        if (m.dayOfMonth != null && Object.hasOwnProperty.call(m, "dayOfMonth"))
                            w.uint32(32).uint32(m.dayOfMonth);
                        if (m.hour != null && Object.hasOwnProperty.call(m, "hour"))
                            w.uint32(40).uint32(m.hour);
                        if (m.minute != null && Object.hasOwnProperty.call(m, "minute"))
                            w.uint32(48).uint32(m.minute);
                        if (m.calendar != null && Object.hasOwnProperty.call(m, "calendar"))
                            w.uint32(56).int32(m.calendar);
                        return w;
                    };

                    HSMDateTimeComponent.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.dayOfWeek = r.int32();
                                    break;
                                }
                            case 2: {
                                    m.year = r.uint32();
                                    break;
                                }
                            case 3: {
                                    m.month = r.uint32();
                                    break;
                                }
                            case 4: {
                                    m.dayOfMonth = r.uint32();
                                    break;
                                }
                            case 5: {
                                    m.hour = r.uint32();
                                    break;
                                }
                            case 6: {
                                    m.minute = r.uint32();
                                    break;
                                }
                            case 7: {
                                    m.calendar = r.int32();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    HSMDateTimeComponent.fromObject = function fromObject(d, n) {
                        if (d instanceof WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent();
                        switch (d.dayOfWeek) {
                        default:
                            if (typeof d.dayOfWeek === "number") {
                                m.dayOfWeek = d.dayOfWeek;
                                break;
                            }
                            break;
                        case "MONDAY":
                        case 1:
                            m.dayOfWeek = 1;
                            break;
                        case "TUESDAY":
                        case 2:
                            m.dayOfWeek = 2;
                            break;
                        case "WEDNESDAY":
                        case 3:
                            m.dayOfWeek = 3;
                            break;
                        case "THURSDAY":
                        case 4:
                            m.dayOfWeek = 4;
                            break;
                        case "FRIDAY":
                        case 5:
                            m.dayOfWeek = 5;
                            break;
                        case "SATURDAY":
                        case 6:
                            m.dayOfWeek = 6;
                            break;
                        case "SUNDAY":
                        case 7:
                            m.dayOfWeek = 7;
                            break;
                        }
                        if (d.year != null) {
                            m.year = d.year >>> 0;
                        }
                        if (d.month != null) {
                            m.month = d.month >>> 0;
                        }
                        if (d.dayOfMonth != null) {
                            m.dayOfMonth = d.dayOfMonth >>> 0;
                        }
                        if (d.hour != null) {
                            m.hour = d.hour >>> 0;
                        }
                        if (d.minute != null) {
                            m.minute = d.minute >>> 0;
                        }
                        switch (d.calendar) {
                        default:
                            if (typeof d.calendar === "number") {
                                m.calendar = d.calendar;
                                break;
                            }
                            break;
                        case "GREGORIAN":
                        case 1:
                            m.calendar = 1;
                            break;
                        case "SOLAR_HIJRI":
                        case 2:
                            m.calendar = 2;
                            break;
                        }
                        return m;
                    };

                    HSMDateTimeComponent.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.dayOfWeek != null && m.hasOwnProperty("dayOfWeek")) {
                            d.dayOfWeek = o.enums === String ? WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[m.dayOfWeek] === undefined ? m.dayOfWeek : WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[m.dayOfWeek] : m.dayOfWeek;
                            if (o.oneofs)
                                d._dayOfWeek = "dayOfWeek";
                        }
                        if (m.year != null && m.hasOwnProperty("year")) {
                            d.year = m.year;
                            if (o.oneofs)
                                d._year = "year";
                        }
                        if (m.month != null && m.hasOwnProperty("month")) {
                            d.month = m.month;
                            if (o.oneofs)
                                d._month = "month";
                        }
                        if (m.dayOfMonth != null && m.hasOwnProperty("dayOfMonth")) {
                            d.dayOfMonth = m.dayOfMonth;
                            if (o.oneofs)
                                d._dayOfMonth = "dayOfMonth";
                        }
                        if (m.hour != null && m.hasOwnProperty("hour")) {
                            d.hour = m.hour;
                            if (o.oneofs)
                                d._hour = "hour";
                        }
                        if (m.minute != null && m.hasOwnProperty("minute")) {
                            d.minute = m.minute;
                            if (o.oneofs)
                                d._minute = "minute";
                        }
                        if (m.calendar != null && m.hasOwnProperty("calendar")) {
                            d.calendar = o.enums === String ? WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[m.calendar] === undefined ? m.calendar : WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[m.calendar] : m.calendar;
                            if (o.oneofs)
                                d._calendar = "calendar";
                        }
                        return d;
                    };

                    HSMDateTimeComponent.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    HSMDateTimeComponent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent";
                    };

                    HSMDateTimeComponent.CalendarType = (function() {
                        const valuesById = {}, values = Object.create(valuesById);
                        values[valuesById[1] = "GREGORIAN"] = 1;
                        values[valuesById[2] = "SOLAR_HIJRI"] = 2;
                        return values;
                    })();

                    HSMDateTimeComponent.DayOfWeekType = (function() {
                        const valuesById = {}, values = Object.create(valuesById);
                        values[valuesById[1] = "MONDAY"] = 1;
                        values[valuesById[2] = "TUESDAY"] = 2;
                        values[valuesById[3] = "WEDNESDAY"] = 3;
                        values[valuesById[4] = "THURSDAY"] = 4;
                        values[valuesById[5] = "FRIDAY"] = 5;
                        values[valuesById[6] = "SATURDAY"] = 6;
                        values[valuesById[7] = "SUNDAY"] = 7;
                        return values;
                    })();

                    return HSMDateTimeComponent;
                })();

                HSMDateTime.HSMDateTimeUnixEpoch = (function() {

                    function HSMDateTimeUnixEpoch(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    HSMDateTimeUnixEpoch.prototype.timestamp = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(HSMDateTimeUnixEpoch.prototype, "_timestamp", {
                        get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    HSMDateTimeUnixEpoch.create = function create(properties) {
                        return new HSMDateTimeUnixEpoch(properties);
                    };

                    HSMDateTimeUnixEpoch.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                            w.uint32(8).int64(m.timestamp);
                        return w;
                    };

                    HSMDateTimeUnixEpoch.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.timestamp = r.int64();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    HSMDateTimeUnixEpoch.fromObject = function fromObject(d, n) {
                        if (d instanceof WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch();
                        if (d.timestamp != null) {
                            if ($util.Long)
                                (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = false;
                            else if (typeof d.timestamp === "string")
                                m.timestamp = parseInt(d.timestamp, 10);
                            else if (typeof d.timestamp === "number")
                                m.timestamp = d.timestamp;
                            else if (typeof d.timestamp === "object")
                                m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                        }
                        return m;
                    };

                    HSMDateTimeUnixEpoch.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                            if (typeof m.timestamp === "number")
                                d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                            else
                                d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                            if (o.oneofs)
                                d._timestamp = "timestamp";
                        }
                        return d;
                    };

                    HSMDateTimeUnixEpoch.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    HSMDateTimeUnixEpoch.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch";
                    };

                    return HSMDateTimeUnixEpoch;
                })();

                return HSMDateTime;
            })();

            HSMLocalizableParameter.HSMCurrency = (function() {

                function HSMCurrency(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                HSMCurrency.prototype.currencyCode = null;
                HSMCurrency.prototype.amount1000 = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HSMCurrency.prototype, "_currencyCode", {
                    get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HSMCurrency.prototype, "_amount1000", {
                    get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HSMCurrency.create = function create(properties) {
                    return new HSMCurrency(properties);
                };

                HSMCurrency.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.currencyCode != null && Object.hasOwnProperty.call(m, "currencyCode"))
                        w.uint32(10).string(m.currencyCode);
                    if (m.amount1000 != null && Object.hasOwnProperty.call(m, "amount1000"))
                        w.uint32(16).int64(m.amount1000);
                    return w;
                };

                HSMCurrency.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.currencyCode = r.string();
                                break;
                            }
                        case 2: {
                                m.amount1000 = r.int64();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                HSMCurrency.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency();
                    if (d.currencyCode != null) {
                        m.currencyCode = String(d.currencyCode);
                    }
                    if (d.amount1000 != null) {
                        if ($util.Long)
                            (m.amount1000 = $util.Long.fromValue(d.amount1000)).unsigned = false;
                        else if (typeof d.amount1000 === "string")
                            m.amount1000 = parseInt(d.amount1000, 10);
                        else if (typeof d.amount1000 === "number")
                            m.amount1000 = d.amount1000;
                        else if (typeof d.amount1000 === "object")
                            m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber();
                    }
                    return m;
                };

                HSMCurrency.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.currencyCode != null && m.hasOwnProperty("currencyCode")) {
                        d.currencyCode = m.currencyCode;
                        if (o.oneofs)
                            d._currencyCode = "currencyCode";
                    }
                    if (m.amount1000 != null && m.hasOwnProperty("amount1000")) {
                        if (typeof m.amount1000 === "number")
                            d.amount1000 = o.longs === String ? String(m.amount1000) : m.amount1000;
                        else
                            d.amount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber() : m.amount1000;
                        if (o.oneofs)
                            d._amount1000 = "amount1000";
                    }
                    return d;
                };

                HSMCurrency.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                HSMCurrency.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency";
                };

                return HSMCurrency;
            })();

            return HSMLocalizableParameter;
        })();

        return HighlyStructuredMessage;
    })();

    WAE2E.PeerDataOperationRequestResponseMessage = (function() {

        function PeerDataOperationRequestResponseMessage(p) {
            this.peerDataOperationResult = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PeerDataOperationRequestResponseMessage.prototype.peerDataOperationRequestType = null;
        PeerDataOperationRequestResponseMessage.prototype.stanzaID = null;
        PeerDataOperationRequestResponseMessage.prototype.peerDataOperationResult = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_peerDataOperationRequestType", {
            get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_stanzaID", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PeerDataOperationRequestResponseMessage.create = function create(properties) {
            return new PeerDataOperationRequestResponseMessage(properties);
        };

        PeerDataOperationRequestResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.peerDataOperationRequestType != null && Object.hasOwnProperty.call(m, "peerDataOperationRequestType"))
                w.uint32(8).int32(m.peerDataOperationRequestType);
            if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                w.uint32(18).string(m.stanzaID);
            if (m.peerDataOperationResult != null && m.peerDataOperationResult.length) {
                for (var i = 0; i < m.peerDataOperationResult.length; ++i)
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.encode(m.peerDataOperationResult[i], w.uint32(26).fork()).ldelim();
            }
            return w;
        };

        PeerDataOperationRequestResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.peerDataOperationRequestType = r.int32();
                        break;
                    }
                case 2: {
                        m.stanzaID = r.string();
                        break;
                    }
                case 3: {
                        if (!(m.peerDataOperationResult && m.peerDataOperationResult.length))
                            m.peerDataOperationResult = [];
                        m.peerDataOperationResult.push(WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PeerDataOperationRequestResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PeerDataOperationRequestResponseMessage();
            switch (d.peerDataOperationRequestType) {
            default:
                if (typeof d.peerDataOperationRequestType === "number") {
                    m.peerDataOperationRequestType = d.peerDataOperationRequestType;
                    break;
                }
                break;
            case "UPLOAD_STICKER":
            case 0:
                m.peerDataOperationRequestType = 0;
                break;
            case "SEND_RECENT_STICKER_BOOTSTRAP":
            case 1:
                m.peerDataOperationRequestType = 1;
                break;
            case "GENERATE_LINK_PREVIEW":
            case 2:
                m.peerDataOperationRequestType = 2;
                break;
            case "HISTORY_SYNC_ON_DEMAND":
            case 3:
                m.peerDataOperationRequestType = 3;
                break;
            case "PLACEHOLDER_MESSAGE_RESEND":
            case 4:
                m.peerDataOperationRequestType = 4;
                break;
            case "WAFFLE_LINKING_NONCE_FETCH":
            case 5:
                m.peerDataOperationRequestType = 5;
                break;
            case "FULL_HISTORY_SYNC_ON_DEMAND":
            case 6:
                m.peerDataOperationRequestType = 6;
                break;
            case "COMPANION_META_NONCE_FETCH":
            case 7:
                m.peerDataOperationRequestType = 7;
                break;
            case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
            case 8:
                m.peerDataOperationRequestType = 8;
                break;
            case "COMPANION_CANONICAL_USER_NONCE_FETCH":
            case 9:
                m.peerDataOperationRequestType = 9;
                break;
            case "HISTORY_SYNC_CHUNK_RETRY":
            case 10:
                m.peerDataOperationRequestType = 10;
                break;
            case "GALAXY_FLOW_ACTION":
            case 11:
                m.peerDataOperationRequestType = 11;
                break;
            case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
            case 12:
                m.peerDataOperationRequestType = 12;
                break;
            case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
            case 13:
                m.peerDataOperationRequestType = 13;
                break;
            }
            if (d.stanzaID != null) {
                m.stanzaID = String(d.stanzaID);
            }
            if (d.peerDataOperationResult) {
                if (!Array.isArray(d.peerDataOperationResult))
                    throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.peerDataOperationResult: array expected");
                m.peerDataOperationResult = [];
                for (var i = 0; i < d.peerDataOperationResult.length; ++i) {
                    if (typeof d.peerDataOperationResult[i] !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.peerDataOperationResult: object expected");
                    m.peerDataOperationResult[i] = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fromObject(d.peerDataOperationResult[i], n + 1);
                }
            }
            return m;
        };

        PeerDataOperationRequestResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.peerDataOperationResult = [];
            }
            if (m.peerDataOperationRequestType != null && m.hasOwnProperty("peerDataOperationRequestType")) {
                d.peerDataOperationRequestType = o.enums === String ? WAE2E.PeerDataOperationRequestType[m.peerDataOperationRequestType] === undefined ? m.peerDataOperationRequestType : WAE2E.PeerDataOperationRequestType[m.peerDataOperationRequestType] : m.peerDataOperationRequestType;
                if (o.oneofs)
                    d._peerDataOperationRequestType = "peerDataOperationRequestType";
            }
            if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                d.stanzaID = m.stanzaID;
                if (o.oneofs)
                    d._stanzaID = "stanzaID";
            }
            if (m.peerDataOperationResult && m.peerDataOperationResult.length) {
                d.peerDataOperationResult = [];
                for (var j = 0; j < m.peerDataOperationResult.length; ++j) {
                    d.peerDataOperationResult[j] = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.toObject(m.peerDataOperationResult[j], o);
                }
            }
            return d;
        };

        PeerDataOperationRequestResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PeerDataOperationRequestResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage";
        };

        PeerDataOperationRequestResponseMessage.PeerDataOperationResult = (function() {

            function PeerDataOperationResult(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PeerDataOperationResult.prototype.mediaUploadResult = null;
            PeerDataOperationResult.prototype.stickerMessage = null;
            PeerDataOperationResult.prototype.linkPreviewResponse = null;
            PeerDataOperationResult.prototype.placeholderMessageResendResponse = null;
            PeerDataOperationResult.prototype.waffleNonceFetchRequestResponse = null;
            PeerDataOperationResult.prototype.fullHistorySyncOnDemandRequestResponse = null;
            PeerDataOperationResult.prototype.companionMetaNonceFetchRequestResponse = null;
            PeerDataOperationResult.prototype.syncdSnapshotFatalRecoveryResponse = null;
            PeerDataOperationResult.prototype.companionCanonicalUserNonceFetchRequestResponse = null;
            PeerDataOperationResult.prototype.historySyncChunkRetryResponse = null;
            PeerDataOperationResult.prototype.flowResponsesCsvBundle = null;
            PeerDataOperationResult.prototype.bizBroadcastInsightsContactListResponse = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_mediaUploadResult", {
                get: $util.oneOfGetter($oneOfFields = ["mediaUploadResult"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_stickerMessage", {
                get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_linkPreviewResponse", {
                get: $util.oneOfGetter($oneOfFields = ["linkPreviewResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_placeholderMessageResendResponse", {
                get: $util.oneOfGetter($oneOfFields = ["placeholderMessageResendResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_waffleNonceFetchRequestResponse", {
                get: $util.oneOfGetter($oneOfFields = ["waffleNonceFetchRequestResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_fullHistorySyncOnDemandRequestResponse", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_companionMetaNonceFetchRequestResponse", {
                get: $util.oneOfGetter($oneOfFields = ["companionMetaNonceFetchRequestResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_syncdSnapshotFatalRecoveryResponse", {
                get: $util.oneOfGetter($oneOfFields = ["syncdSnapshotFatalRecoveryResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_companionCanonicalUserNonceFetchRequestResponse", {
                get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequestResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_historySyncChunkRetryResponse", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_flowResponsesCsvBundle", {
                get: $util.oneOfGetter($oneOfFields = ["flowResponsesCsvBundle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PeerDataOperationResult.prototype, "_bizBroadcastInsightsContactListResponse", {
                get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PeerDataOperationResult.create = function create(properties) {
                return new PeerDataOperationResult(properties);
            };

            PeerDataOperationResult.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.mediaUploadResult != null && Object.hasOwnProperty.call(m, "mediaUploadResult"))
                    w.uint32(8).int32(m.mediaUploadResult);
                if (m.stickerMessage != null && Object.hasOwnProperty.call(m, "stickerMessage"))
                    WAE2E.StickerMessage.encode(m.stickerMessage, w.uint32(18).fork()).ldelim();
                if (m.linkPreviewResponse != null && Object.hasOwnProperty.call(m, "linkPreviewResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.encode(m.linkPreviewResponse, w.uint32(26).fork()).ldelim();
                if (m.placeholderMessageResendResponse != null && Object.hasOwnProperty.call(m, "placeholderMessageResendResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.encode(m.placeholderMessageResendResponse, w.uint32(34).fork()).ldelim();
                if (m.waffleNonceFetchRequestResponse != null && Object.hasOwnProperty.call(m, "waffleNonceFetchRequestResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.encode(m.waffleNonceFetchRequestResponse, w.uint32(42).fork()).ldelim();
                if (m.fullHistorySyncOnDemandRequestResponse != null && Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.encode(m.fullHistorySyncOnDemandRequestResponse, w.uint32(50).fork()).ldelim();
                if (m.companionMetaNonceFetchRequestResponse != null && Object.hasOwnProperty.call(m, "companionMetaNonceFetchRequestResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.encode(m.companionMetaNonceFetchRequestResponse, w.uint32(58).fork()).ldelim();
                if (m.syncdSnapshotFatalRecoveryResponse != null && Object.hasOwnProperty.call(m, "syncdSnapshotFatalRecoveryResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.encode(m.syncdSnapshotFatalRecoveryResponse, w.uint32(66).fork()).ldelim();
                if (m.companionCanonicalUserNonceFetchRequestResponse != null && Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequestResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.encode(m.companionCanonicalUserNonceFetchRequestResponse, w.uint32(74).fork()).ldelim();
                if (m.historySyncChunkRetryResponse != null && Object.hasOwnProperty.call(m, "historySyncChunkRetryResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.encode(m.historySyncChunkRetryResponse, w.uint32(82).fork()).ldelim();
                if (m.flowResponsesCsvBundle != null && Object.hasOwnProperty.call(m, "flowResponsesCsvBundle"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.encode(m.flowResponsesCsvBundle, w.uint32(90).fork()).ldelim();
                if (m.bizBroadcastInsightsContactListResponse != null && Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListResponse"))
                    WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.encode(m.bizBroadcastInsightsContactListResponse, w.uint32(98).fork()).ldelim();
                return w;
            };

            PeerDataOperationResult.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.mediaUploadResult = r.int32();
                            break;
                        }
                    case 2: {
                            m.stickerMessage = WAE2E.StickerMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.linkPreviewResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 4: {
                            m.placeholderMessageResendResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 5: {
                            m.waffleNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 6: {
                            m.fullHistorySyncOnDemandRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 7: {
                            m.companionMetaNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 8: {
                            m.syncdSnapshotFatalRecoveryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 9: {
                            m.companionCanonicalUserNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 10: {
                            m.historySyncChunkRetryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 11: {
                            m.flowResponsesCsvBundle = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 12: {
                            m.bizBroadcastInsightsContactListResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PeerDataOperationResult.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult();
                switch (d.mediaUploadResult) {
                default:
                    if (typeof d.mediaUploadResult === "number") {
                        m.mediaUploadResult = d.mediaUploadResult;
                        break;
                    }
                    break;
                case "GENERAL_ERROR":
                case 0:
                    m.mediaUploadResult = 0;
                    break;
                case "SUCCESS":
                case 1:
                    m.mediaUploadResult = 1;
                    break;
                case "NOT_FOUND":
                case 2:
                    m.mediaUploadResult = 2;
                    break;
                case "DECRYPTION_ERROR":
                case 3:
                    m.mediaUploadResult = 3;
                    break;
                }
                if (d.stickerMessage != null) {
                    if (typeof d.stickerMessage !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.stickerMessage: object expected");
                    m.stickerMessage = WAE2E.StickerMessage.fromObject(d.stickerMessage, n + 1);
                }
                if (d.linkPreviewResponse != null) {
                    if (typeof d.linkPreviewResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.linkPreviewResponse: object expected");
                    m.linkPreviewResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.fromObject(d.linkPreviewResponse, n + 1);
                }
                if (d.placeholderMessageResendResponse != null) {
                    if (typeof d.placeholderMessageResendResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.placeholderMessageResendResponse: object expected");
                    m.placeholderMessageResendResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.fromObject(d.placeholderMessageResendResponse, n + 1);
                }
                if (d.waffleNonceFetchRequestResponse != null) {
                    if (typeof d.waffleNonceFetchRequestResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.waffleNonceFetchRequestResponse: object expected");
                    m.waffleNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.fromObject(d.waffleNonceFetchRequestResponse, n + 1);
                }
                if (d.fullHistorySyncOnDemandRequestResponse != null) {
                    if (typeof d.fullHistorySyncOnDemandRequestResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fullHistorySyncOnDemandRequestResponse: object expected");
                    m.fullHistorySyncOnDemandRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.fromObject(d.fullHistorySyncOnDemandRequestResponse, n + 1);
                }
                if (d.companionMetaNonceFetchRequestResponse != null) {
                    if (typeof d.companionMetaNonceFetchRequestResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionMetaNonceFetchRequestResponse: object expected");
                    m.companionMetaNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.fromObject(d.companionMetaNonceFetchRequestResponse, n + 1);
                }
                if (d.syncdSnapshotFatalRecoveryResponse != null) {
                    if (typeof d.syncdSnapshotFatalRecoveryResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.syncdSnapshotFatalRecoveryResponse: object expected");
                    m.syncdSnapshotFatalRecoveryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.fromObject(d.syncdSnapshotFatalRecoveryResponse, n + 1);
                }
                if (d.companionCanonicalUserNonceFetchRequestResponse != null) {
                    if (typeof d.companionCanonicalUserNonceFetchRequestResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionCanonicalUserNonceFetchRequestResponse: object expected");
                    m.companionCanonicalUserNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.fromObject(d.companionCanonicalUserNonceFetchRequestResponse, n + 1);
                }
                if (d.historySyncChunkRetryResponse != null) {
                    if (typeof d.historySyncChunkRetryResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.historySyncChunkRetryResponse: object expected");
                    m.historySyncChunkRetryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.fromObject(d.historySyncChunkRetryResponse, n + 1);
                }
                if (d.flowResponsesCsvBundle != null) {
                    if (typeof d.flowResponsesCsvBundle !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.flowResponsesCsvBundle: object expected");
                    m.flowResponsesCsvBundle = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.fromObject(d.flowResponsesCsvBundle, n + 1);
                }
                if (d.bizBroadcastInsightsContactListResponse != null) {
                    if (typeof d.bizBroadcastInsightsContactListResponse !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.bizBroadcastInsightsContactListResponse: object expected");
                    m.bizBroadcastInsightsContactListResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.fromObject(d.bizBroadcastInsightsContactListResponse, n + 1);
                }
                return m;
            };

            PeerDataOperationResult.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.mediaUploadResult != null && m.hasOwnProperty("mediaUploadResult")) {
                    d.mediaUploadResult = o.enums === String ? $root.WAMmsRetry.MediaRetryNotification.ResultType[m.mediaUploadResult] === undefined ? m.mediaUploadResult : $root.WAMmsRetry.MediaRetryNotification.ResultType[m.mediaUploadResult] : m.mediaUploadResult;
                    if (o.oneofs)
                        d._mediaUploadResult = "mediaUploadResult";
                }
                if (m.stickerMessage != null && m.hasOwnProperty("stickerMessage")) {
                    d.stickerMessage = WAE2E.StickerMessage.toObject(m.stickerMessage, o);
                    if (o.oneofs)
                        d._stickerMessage = "stickerMessage";
                }
                if (m.linkPreviewResponse != null && m.hasOwnProperty("linkPreviewResponse")) {
                    d.linkPreviewResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.toObject(m.linkPreviewResponse, o);
                    if (o.oneofs)
                        d._linkPreviewResponse = "linkPreviewResponse";
                }
                if (m.placeholderMessageResendResponse != null && m.hasOwnProperty("placeholderMessageResendResponse")) {
                    d.placeholderMessageResendResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.toObject(m.placeholderMessageResendResponse, o);
                    if (o.oneofs)
                        d._placeholderMessageResendResponse = "placeholderMessageResendResponse";
                }
                if (m.waffleNonceFetchRequestResponse != null && m.hasOwnProperty("waffleNonceFetchRequestResponse")) {
                    d.waffleNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.toObject(m.waffleNonceFetchRequestResponse, o);
                    if (o.oneofs)
                        d._waffleNonceFetchRequestResponse = "waffleNonceFetchRequestResponse";
                }
                if (m.fullHistorySyncOnDemandRequestResponse != null && m.hasOwnProperty("fullHistorySyncOnDemandRequestResponse")) {
                    d.fullHistorySyncOnDemandRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.toObject(m.fullHistorySyncOnDemandRequestResponse, o);
                    if (o.oneofs)
                        d._fullHistorySyncOnDemandRequestResponse = "fullHistorySyncOnDemandRequestResponse";
                }
                if (m.companionMetaNonceFetchRequestResponse != null && m.hasOwnProperty("companionMetaNonceFetchRequestResponse")) {
                    d.companionMetaNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.toObject(m.companionMetaNonceFetchRequestResponse, o);
                    if (o.oneofs)
                        d._companionMetaNonceFetchRequestResponse = "companionMetaNonceFetchRequestResponse";
                }
                if (m.syncdSnapshotFatalRecoveryResponse != null && m.hasOwnProperty("syncdSnapshotFatalRecoveryResponse")) {
                    d.syncdSnapshotFatalRecoveryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.toObject(m.syncdSnapshotFatalRecoveryResponse, o);
                    if (o.oneofs)
                        d._syncdSnapshotFatalRecoveryResponse = "syncdSnapshotFatalRecoveryResponse";
                }
                if (m.companionCanonicalUserNonceFetchRequestResponse != null && m.hasOwnProperty("companionCanonicalUserNonceFetchRequestResponse")) {
                    d.companionCanonicalUserNonceFetchRequestResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.toObject(m.companionCanonicalUserNonceFetchRequestResponse, o);
                    if (o.oneofs)
                        d._companionCanonicalUserNonceFetchRequestResponse = "companionCanonicalUserNonceFetchRequestResponse";
                }
                if (m.historySyncChunkRetryResponse != null && m.hasOwnProperty("historySyncChunkRetryResponse")) {
                    d.historySyncChunkRetryResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.toObject(m.historySyncChunkRetryResponse, o);
                    if (o.oneofs)
                        d._historySyncChunkRetryResponse = "historySyncChunkRetryResponse";
                }
                if (m.flowResponsesCsvBundle != null && m.hasOwnProperty("flowResponsesCsvBundle")) {
                    d.flowResponsesCsvBundle = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.toObject(m.flowResponsesCsvBundle, o);
                    if (o.oneofs)
                        d._flowResponsesCsvBundle = "flowResponsesCsvBundle";
                }
                if (m.bizBroadcastInsightsContactListResponse != null && m.hasOwnProperty("bizBroadcastInsightsContactListResponse")) {
                    d.bizBroadcastInsightsContactListResponse = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.toObject(m.bizBroadcastInsightsContactListResponse, o);
                    if (o.oneofs)
                        d._bizBroadcastInsightsContactListResponse = "bizBroadcastInsightsContactListResponse";
                }
                return d;
            };

            PeerDataOperationResult.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PeerDataOperationResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult";
            };

            PeerDataOperationResult.HistorySyncChunkRetryResponseCode = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "GENERATION_ERROR"] = 1;
                values[valuesById[2] = "CHUNK_CONSUMED"] = 2;
                values[valuesById[3] = "TIMEOUT"] = 3;
                values[valuesById[4] = "SESSION_EXHAUSTED"] = 4;
                values[valuesById[5] = "CHUNK_EXHAUSTED"] = 5;
                values[valuesById[6] = "DUPLICATED_REQUEST"] = 6;
                return values;
            })();

            PeerDataOperationResult.FullHistorySyncOnDemandResponseCode = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "REQUEST_SUCCESS"] = 0;
                values[valuesById[1] = "REQUEST_TIME_EXPIRED"] = 1;
                values[valuesById[2] = "DECLINED_SHARING_HISTORY"] = 2;
                values[valuesById[3] = "GENERIC_ERROR"] = 3;
                values[valuesById[4] = "ERROR_REQUEST_ON_NON_SMB_PRIMARY"] = 4;
                values[valuesById[5] = "ERROR_HOSTED_DEVICE_NOT_CONNECTED"] = 5;
                values[valuesById[6] = "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET"] = 6;
                values[valuesById[7] = "ERROR_MULTI_PROVIDER_NOT_CONFIGURED"] = 7;
                return values;
            })();

            PeerDataOperationResult.FlowResponsesCsvBundle = (function() {

                function FlowResponsesCsvBundle(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                FlowResponsesCsvBundle.prototype.flowID = null;
                FlowResponsesCsvBundle.prototype.galaxyFlowDownloadRequestID = null;
                FlowResponsesCsvBundle.prototype.fileName = null;
                FlowResponsesCsvBundle.prototype.mimetype = null;
                FlowResponsesCsvBundle.prototype.fileSHA256 = null;
                FlowResponsesCsvBundle.prototype.mediaKey = null;
                FlowResponsesCsvBundle.prototype.fileEncSHA256 = null;
                FlowResponsesCsvBundle.prototype.directPath = null;
                FlowResponsesCsvBundle.prototype.mediaKeyTimestamp = null;
                FlowResponsesCsvBundle.prototype.fileLength = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_flowID", {
                    get: $util.oneOfGetter($oneOfFields = ["flowID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_galaxyFlowDownloadRequestID", {
                    get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileName", {
                    get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mimetype", {
                    get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileSHA256", {
                    get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKey", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileEncSHA256", {
                    get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_directPath", {
                    get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKeyTimestamp", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileLength", {
                    get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                FlowResponsesCsvBundle.create = function create(properties) {
                    return new FlowResponsesCsvBundle(properties);
                };

                FlowResponsesCsvBundle.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.flowID != null && Object.hasOwnProperty.call(m, "flowID"))
                        w.uint32(10).string(m.flowID);
                    if (m.galaxyFlowDownloadRequestID != null && Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestID"))
                        w.uint32(18).string(m.galaxyFlowDownloadRequestID);
                    if (m.fileName != null && Object.hasOwnProperty.call(m, "fileName"))
                        w.uint32(26).string(m.fileName);
                    if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                        w.uint32(34).string(m.mimetype);
                    if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                        w.uint32(42).bytes(m.fileSHA256);
                    if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                        w.uint32(50).bytes(m.mediaKey);
                    if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                        w.uint32(58).bytes(m.fileEncSHA256);
                    if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                        w.uint32(66).string(m.directPath);
                    if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                        w.uint32(72).int64(m.mediaKeyTimestamp);
                    if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                        w.uint32(80).uint64(m.fileLength);
                    return w;
                };

                FlowResponsesCsvBundle.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.flowID = r.string();
                                break;
                            }
                        case 2: {
                                m.galaxyFlowDownloadRequestID = r.string();
                                break;
                            }
                        case 3: {
                                m.fileName = r.string();
                                break;
                            }
                        case 4: {
                                m.mimetype = r.string();
                                break;
                            }
                        case 5: {
                                m.fileSHA256 = r.bytes();
                                break;
                            }
                        case 6: {
                                m.mediaKey = r.bytes();
                                break;
                            }
                        case 7: {
                                m.fileEncSHA256 = r.bytes();
                                break;
                            }
                        case 8: {
                                m.directPath = r.string();
                                break;
                            }
                        case 9: {
                                m.mediaKeyTimestamp = r.int64();
                                break;
                            }
                        case 10: {
                                m.fileLength = r.uint64();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                FlowResponsesCsvBundle.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle();
                    if (d.flowID != null) {
                        m.flowID = String(d.flowID);
                    }
                    if (d.galaxyFlowDownloadRequestID != null) {
                        m.galaxyFlowDownloadRequestID = String(d.galaxyFlowDownloadRequestID);
                    }
                    if (d.fileName != null) {
                        m.fileName = String(d.fileName);
                    }
                    if (d.mimetype != null) {
                        m.mimetype = String(d.mimetype);
                    }
                    if (d.fileSHA256 != null) {
                        if (typeof d.fileSHA256 === "string")
                            $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                        else if (d.fileSHA256.length >= 0)
                            m.fileSHA256 = d.fileSHA256;
                    }
                    if (d.mediaKey != null) {
                        if (typeof d.mediaKey === "string")
                            $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                        else if (d.mediaKey.length >= 0)
                            m.mediaKey = d.mediaKey;
                    }
                    if (d.fileEncSHA256 != null) {
                        if (typeof d.fileEncSHA256 === "string")
                            $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                        else if (d.fileEncSHA256.length >= 0)
                            m.fileEncSHA256 = d.fileEncSHA256;
                    }
                    if (d.directPath != null) {
                        m.directPath = String(d.directPath);
                    }
                    if (d.mediaKeyTimestamp != null) {
                        if ($util.Long)
                            (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                        else if (typeof d.mediaKeyTimestamp === "string")
                            m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                        else if (typeof d.mediaKeyTimestamp === "number")
                            m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                        else if (typeof d.mediaKeyTimestamp === "object")
                            m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                    }
                    if (d.fileLength != null) {
                        if ($util.Long)
                            (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                        else if (typeof d.fileLength === "string")
                            m.fileLength = parseInt(d.fileLength, 10);
                        else if (typeof d.fileLength === "number")
                            m.fileLength = d.fileLength;
                        else if (typeof d.fileLength === "object")
                            m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                    }
                    return m;
                };

                FlowResponsesCsvBundle.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.flowID != null && m.hasOwnProperty("flowID")) {
                        d.flowID = m.flowID;
                        if (o.oneofs)
                            d._flowID = "flowID";
                    }
                    if (m.galaxyFlowDownloadRequestID != null && m.hasOwnProperty("galaxyFlowDownloadRequestID")) {
                        d.galaxyFlowDownloadRequestID = m.galaxyFlowDownloadRequestID;
                        if (o.oneofs)
                            d._galaxyFlowDownloadRequestID = "galaxyFlowDownloadRequestID";
                    }
                    if (m.fileName != null && m.hasOwnProperty("fileName")) {
                        d.fileName = m.fileName;
                        if (o.oneofs)
                            d._fileName = "fileName";
                    }
                    if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                        d.mimetype = m.mimetype;
                        if (o.oneofs)
                            d._mimetype = "mimetype";
                    }
                    if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                        d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                        if (o.oneofs)
                            d._fileSHA256 = "fileSHA256";
                    }
                    if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                        d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                        if (o.oneofs)
                            d._mediaKey = "mediaKey";
                    }
                    if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                        d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                        if (o.oneofs)
                            d._fileEncSHA256 = "fileEncSHA256";
                    }
                    if (m.directPath != null && m.hasOwnProperty("directPath")) {
                        d.directPath = m.directPath;
                        if (o.oneofs)
                            d._directPath = "directPath";
                    }
                    if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                        if (typeof m.mediaKeyTimestamp === "number")
                            d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                        else
                            d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                        if (o.oneofs)
                            d._mediaKeyTimestamp = "mediaKeyTimestamp";
                    }
                    if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                        if (typeof m.fileLength === "number")
                            d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                        else
                            d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                        if (o.oneofs)
                            d._fileLength = "fileLength";
                    }
                    return d;
                };

                FlowResponsesCsvBundle.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                FlowResponsesCsvBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle";
                };

                return FlowResponsesCsvBundle;
            })();

            PeerDataOperationResult.BizBroadcastInsightsContactListResponse = (function() {

                function BizBroadcastInsightsContactListResponse(p) {
                    this.contacts = [];
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                BizBroadcastInsightsContactListResponse.prototype.campaignID = null;
                BizBroadcastInsightsContactListResponse.prototype.timestampMS = null;
                BizBroadcastInsightsContactListResponse.prototype.contacts = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_campaignID", {
                    get: $util.oneOfGetter($oneOfFields = ["campaignID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_timestampMS", {
                    get: $util.oneOfGetter($oneOfFields = ["timestampMS"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BizBroadcastInsightsContactListResponse.create = function create(properties) {
                    return new BizBroadcastInsightsContactListResponse(properties);
                };

                BizBroadcastInsightsContactListResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.campaignID != null && Object.hasOwnProperty.call(m, "campaignID"))
                        w.uint32(10).string(m.campaignID);
                    if (m.timestampMS != null && Object.hasOwnProperty.call(m, "timestampMS"))
                        w.uint32(16).int64(m.timestampMS);
                    if (m.contacts != null && m.contacts.length) {
                        for (var i = 0; i < m.contacts.length; ++i)
                            WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.encode(m.contacts[i], w.uint32(26).fork()).ldelim();
                    }
                    return w;
                };

                BizBroadcastInsightsContactListResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.campaignID = r.string();
                                break;
                            }
                        case 2: {
                                m.timestampMS = r.int64();
                                break;
                            }
                        case 3: {
                                if (!(m.contacts && m.contacts.length))
                                    m.contacts = [];
                                m.contacts.push(WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.decode(r, r.uint32(), undefined, n + 1));
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                BizBroadcastInsightsContactListResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse();
                    if (d.campaignID != null) {
                        m.campaignID = String(d.campaignID);
                    }
                    if (d.timestampMS != null) {
                        if ($util.Long)
                            (m.timestampMS = $util.Long.fromValue(d.timestampMS)).unsigned = false;
                        else if (typeof d.timestampMS === "string")
                            m.timestampMS = parseInt(d.timestampMS, 10);
                        else if (typeof d.timestampMS === "number")
                            m.timestampMS = d.timestampMS;
                        else if (typeof d.timestampMS === "object")
                            m.timestampMS = new $util.LongBits(d.timestampMS.low >>> 0, d.timestampMS.high >>> 0).toNumber();
                    }
                    if (d.contacts) {
                        if (!Array.isArray(d.contacts))
                            throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: array expected");
                        m.contacts = [];
                        for (var i = 0; i < d.contacts.length; ++i) {
                            if (typeof d.contacts[i] !== "object")
                                throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: object expected");
                            m.contacts[i] = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.fromObject(d.contacts[i], n + 1);
                        }
                    }
                    return m;
                };

                BizBroadcastInsightsContactListResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.contacts = [];
                    }
                    if (m.campaignID != null && m.hasOwnProperty("campaignID")) {
                        d.campaignID = m.campaignID;
                        if (o.oneofs)
                            d._campaignID = "campaignID";
                    }
                    if (m.timestampMS != null && m.hasOwnProperty("timestampMS")) {
                        if (typeof m.timestampMS === "number")
                            d.timestampMS = o.longs === String ? String(m.timestampMS) : m.timestampMS;
                        else
                            d.timestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.timestampMS) : o.longs === Number ? new $util.LongBits(m.timestampMS.low >>> 0, m.timestampMS.high >>> 0).toNumber() : m.timestampMS;
                        if (o.oneofs)
                            d._timestampMS = "timestampMS";
                    }
                    if (m.contacts && m.contacts.length) {
                        d.contacts = [];
                        for (var j = 0; j < m.contacts.length; ++j) {
                            d.contacts[j] = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.toObject(m.contacts[j], o);
                        }
                    }
                    return d;
                };

                BizBroadcastInsightsContactListResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                BizBroadcastInsightsContactListResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse";
                };

                return BizBroadcastInsightsContactListResponse;
            })();

            PeerDataOperationResult.BizBroadcastInsightsContactState = (function() {

                function BizBroadcastInsightsContactState(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                BizBroadcastInsightsContactState.prototype.contactJID = null;
                BizBroadcastInsightsContactState.prototype.state = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_contactJID", {
                    get: $util.oneOfGetter($oneOfFields = ["contactJID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_state", {
                    get: $util.oneOfGetter($oneOfFields = ["state"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BizBroadcastInsightsContactState.create = function create(properties) {
                    return new BizBroadcastInsightsContactState(properties);
                };

                BizBroadcastInsightsContactState.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.contactJID != null && Object.hasOwnProperty.call(m, "contactJID"))
                        w.uint32(10).string(m.contactJID);
                    if (m.state != null && Object.hasOwnProperty.call(m, "state"))
                        w.uint32(16).int32(m.state);
                    return w;
                };

                BizBroadcastInsightsContactState.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.contactJID = r.string();
                                break;
                            }
                        case 2: {
                                m.state = r.int32();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                BizBroadcastInsightsContactState.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState();
                    if (d.contactJID != null) {
                        m.contactJID = String(d.contactJID);
                    }
                    switch (d.state) {
                    default:
                        if (typeof d.state === "number") {
                            m.state = d.state;
                            break;
                        }
                        break;
                    case "SENT":
                    case 0:
                        m.state = 0;
                        break;
                    case "DELIVERED":
                    case 1:
                        m.state = 1;
                        break;
                    case "READ":
                    case 2:
                        m.state = 2;
                        break;
                    case "REPLIED":
                    case 3:
                        m.state = 3;
                        break;
                    case "QUICK_REPLIED":
                    case 4:
                        m.state = 4;
                        break;
                    }
                    return m;
                };

                BizBroadcastInsightsContactState.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.contactJID != null && m.hasOwnProperty("contactJID")) {
                        d.contactJID = m.contactJID;
                        if (o.oneofs)
                            d._contactJID = "contactJID";
                    }
                    if (m.state != null && m.hasOwnProperty("state")) {
                        d.state = o.enums === String ? WAE2E.InsightDeliveryState[m.state] === undefined ? m.state : WAE2E.InsightDeliveryState[m.state] : m.state;
                        if (o.oneofs)
                            d._state = "state";
                    }
                    return d;
                };

                BizBroadcastInsightsContactState.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                BizBroadcastInsightsContactState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState";
                };

                return BizBroadcastInsightsContactState;
            })();

            PeerDataOperationResult.HistorySyncChunkRetryResponse = (function() {

                function HistorySyncChunkRetryResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                HistorySyncChunkRetryResponse.prototype.syncType = null;
                HistorySyncChunkRetryResponse.prototype.chunkOrder = null;
                HistorySyncChunkRetryResponse.prototype.requestID = null;
                HistorySyncChunkRetryResponse.prototype.responseCode = null;
                HistorySyncChunkRetryResponse.prototype.canRecover = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_syncType", {
                    get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_chunkOrder", {
                    get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_requestID", {
                    get: $util.oneOfGetter($oneOfFields = ["requestID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_responseCode", {
                    get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_canRecover", {
                    get: $util.oneOfGetter($oneOfFields = ["canRecover"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HistorySyncChunkRetryResponse.create = function create(properties) {
                    return new HistorySyncChunkRetryResponse(properties);
                };

                HistorySyncChunkRetryResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.syncType != null && Object.hasOwnProperty.call(m, "syncType"))
                        w.uint32(8).int32(m.syncType);
                    if (m.chunkOrder != null && Object.hasOwnProperty.call(m, "chunkOrder"))
                        w.uint32(16).uint32(m.chunkOrder);
                    if (m.requestID != null && Object.hasOwnProperty.call(m, "requestID"))
                        w.uint32(26).string(m.requestID);
                    if (m.responseCode != null && Object.hasOwnProperty.call(m, "responseCode"))
                        w.uint32(32).int32(m.responseCode);
                    if (m.canRecover != null && Object.hasOwnProperty.call(m, "canRecover"))
                        w.uint32(40).bool(m.canRecover);
                    return w;
                };

                HistorySyncChunkRetryResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.syncType = r.int32();
                                break;
                            }
                        case 2: {
                                m.chunkOrder = r.uint32();
                                break;
                            }
                        case 3: {
                                m.requestID = r.string();
                                break;
                            }
                        case 4: {
                                m.responseCode = r.int32();
                                break;
                            }
                        case 5: {
                                m.canRecover = r.bool();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                HistorySyncChunkRetryResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse();
                    switch (d.syncType) {
                    default:
                        if (typeof d.syncType === "number") {
                            m.syncType = d.syncType;
                            break;
                        }
                        break;
                    case "INITIAL_BOOTSTRAP":
                    case 0:
                        m.syncType = 0;
                        break;
                    case "INITIAL_STATUS_V3":
                    case 1:
                        m.syncType = 1;
                        break;
                    case "FULL":
                    case 2:
                        m.syncType = 2;
                        break;
                    case "RECENT":
                    case 3:
                        m.syncType = 3;
                        break;
                    case "PUSH_NAME":
                    case 4:
                        m.syncType = 4;
                        break;
                    case "NON_BLOCKING_DATA":
                    case 5:
                        m.syncType = 5;
                        break;
                    case "ON_DEMAND":
                    case 6:
                        m.syncType = 6;
                        break;
                    case "NO_HISTORY":
                    case 7:
                        m.syncType = 7;
                        break;
                    case "MESSAGE_ACCESS_STATUS":
                    case 8:
                        m.syncType = 8;
                        break;
                    }
                    if (d.chunkOrder != null) {
                        m.chunkOrder = d.chunkOrder >>> 0;
                    }
                    if (d.requestID != null) {
                        m.requestID = String(d.requestID);
                    }
                    switch (d.responseCode) {
                    default:
                        if (typeof d.responseCode === "number") {
                            m.responseCode = d.responseCode;
                            break;
                        }
                        break;
                    case "GENERATION_ERROR":
                    case 1:
                        m.responseCode = 1;
                        break;
                    case "CHUNK_CONSUMED":
                    case 2:
                        m.responseCode = 2;
                        break;
                    case "TIMEOUT":
                    case 3:
                        m.responseCode = 3;
                        break;
                    case "SESSION_EXHAUSTED":
                    case 4:
                        m.responseCode = 4;
                        break;
                    case "CHUNK_EXHAUSTED":
                    case 5:
                        m.responseCode = 5;
                        break;
                    case "DUPLICATED_REQUEST":
                    case 6:
                        m.responseCode = 6;
                        break;
                    }
                    if (d.canRecover != null) {
                        m.canRecover = Boolean(d.canRecover);
                    }
                    return m;
                };

                HistorySyncChunkRetryResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.syncType != null && m.hasOwnProperty("syncType")) {
                        d.syncType = o.enums === String ? WAE2E.HistorySyncType[m.syncType] === undefined ? m.syncType : WAE2E.HistorySyncType[m.syncType] : m.syncType;
                        if (o.oneofs)
                            d._syncType = "syncType";
                    }
                    if (m.chunkOrder != null && m.hasOwnProperty("chunkOrder")) {
                        d.chunkOrder = m.chunkOrder;
                        if (o.oneofs)
                            d._chunkOrder = "chunkOrder";
                    }
                    if (m.requestID != null && m.hasOwnProperty("requestID")) {
                        d.requestID = m.requestID;
                        if (o.oneofs)
                            d._requestID = "requestID";
                    }
                    if (m.responseCode != null && m.hasOwnProperty("responseCode")) {
                        d.responseCode = o.enums === String ? WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[m.responseCode] === undefined ? m.responseCode : WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[m.responseCode] : m.responseCode;
                        if (o.oneofs)
                            d._responseCode = "responseCode";
                    }
                    if (m.canRecover != null && m.hasOwnProperty("canRecover")) {
                        d.canRecover = m.canRecover;
                        if (o.oneofs)
                            d._canRecover = "canRecover";
                    }
                    return d;
                };

                HistorySyncChunkRetryResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                HistorySyncChunkRetryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse";
                };

                return HistorySyncChunkRetryResponse;
            })();

            PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse = (function() {

                function SyncDSnapshotFatalRecoveryResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                SyncDSnapshotFatalRecoveryResponse.prototype.collectionSnapshot = null;
                SyncDSnapshotFatalRecoveryResponse.prototype.isCompressed = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_collectionSnapshot", {
                    get: $util.oneOfGetter($oneOfFields = ["collectionSnapshot"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_isCompressed", {
                    get: $util.oneOfGetter($oneOfFields = ["isCompressed"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SyncDSnapshotFatalRecoveryResponse.create = function create(properties) {
                    return new SyncDSnapshotFatalRecoveryResponse(properties);
                };

                SyncDSnapshotFatalRecoveryResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.collectionSnapshot != null && Object.hasOwnProperty.call(m, "collectionSnapshot"))
                        w.uint32(10).bytes(m.collectionSnapshot);
                    if (m.isCompressed != null && Object.hasOwnProperty.call(m, "isCompressed"))
                        w.uint32(16).bool(m.isCompressed);
                    return w;
                };

                SyncDSnapshotFatalRecoveryResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.collectionSnapshot = r.bytes();
                                break;
                            }
                        case 2: {
                                m.isCompressed = r.bool();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                SyncDSnapshotFatalRecoveryResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse();
                    if (d.collectionSnapshot != null) {
                        if (typeof d.collectionSnapshot === "string")
                            $util.base64.decode(d.collectionSnapshot, m.collectionSnapshot = $util.newBuffer($util.base64.length(d.collectionSnapshot)), 0);
                        else if (d.collectionSnapshot.length >= 0)
                            m.collectionSnapshot = d.collectionSnapshot;
                    }
                    if (d.isCompressed != null) {
                        m.isCompressed = Boolean(d.isCompressed);
                    }
                    return m;
                };

                SyncDSnapshotFatalRecoveryResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.collectionSnapshot != null && m.hasOwnProperty("collectionSnapshot")) {
                        d.collectionSnapshot = o.bytes === String ? $util.base64.encode(m.collectionSnapshot, 0, m.collectionSnapshot.length) : o.bytes === Array ? Array.prototype.slice.call(m.collectionSnapshot) : m.collectionSnapshot;
                        if (o.oneofs)
                            d._collectionSnapshot = "collectionSnapshot";
                    }
                    if (m.isCompressed != null && m.hasOwnProperty("isCompressed")) {
                        d.isCompressed = m.isCompressed;
                        if (o.oneofs)
                            d._isCompressed = "isCompressed";
                    }
                    return d;
                };

                SyncDSnapshotFatalRecoveryResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                SyncDSnapshotFatalRecoveryResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse";
                };

                return SyncDSnapshotFatalRecoveryResponse;
            })();

            PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse = (function() {

                function CompanionCanonicalUserNonceFetchResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                CompanionCanonicalUserNonceFetchResponse.prototype.nonce = null;
                CompanionCanonicalUserNonceFetchResponse.prototype.waFbid = null;
                CompanionCanonicalUserNonceFetchResponse.prototype.forceRefresh = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_nonce", {
                    get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_waFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["waFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_forceRefresh", {
                    get: $util.oneOfGetter($oneOfFields = ["forceRefresh"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CompanionCanonicalUserNonceFetchResponse.create = function create(properties) {
                    return new CompanionCanonicalUserNonceFetchResponse(properties);
                };

                CompanionCanonicalUserNonceFetchResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.nonce != null && Object.hasOwnProperty.call(m, "nonce"))
                        w.uint32(10).string(m.nonce);
                    if (m.waFbid != null && Object.hasOwnProperty.call(m, "waFbid"))
                        w.uint32(18).string(m.waFbid);
                    if (m.forceRefresh != null && Object.hasOwnProperty.call(m, "forceRefresh"))
                        w.uint32(24).bool(m.forceRefresh);
                    return w;
                };

                CompanionCanonicalUserNonceFetchResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.nonce = r.string();
                                break;
                            }
                        case 2: {
                                m.waFbid = r.string();
                                break;
                            }
                        case 3: {
                                m.forceRefresh = r.bool();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                CompanionCanonicalUserNonceFetchResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse();
                    if (d.nonce != null) {
                        m.nonce = String(d.nonce);
                    }
                    if (d.waFbid != null) {
                        m.waFbid = String(d.waFbid);
                    }
                    if (d.forceRefresh != null) {
                        m.forceRefresh = Boolean(d.forceRefresh);
                    }
                    return m;
                };

                CompanionCanonicalUserNonceFetchResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.nonce != null && m.hasOwnProperty("nonce")) {
                        d.nonce = m.nonce;
                        if (o.oneofs)
                            d._nonce = "nonce";
                    }
                    if (m.waFbid != null && m.hasOwnProperty("waFbid")) {
                        d.waFbid = m.waFbid;
                        if (o.oneofs)
                            d._waFbid = "waFbid";
                    }
                    if (m.forceRefresh != null && m.hasOwnProperty("forceRefresh")) {
                        d.forceRefresh = m.forceRefresh;
                        if (o.oneofs)
                            d._forceRefresh = "forceRefresh";
                    }
                    return d;
                };

                CompanionCanonicalUserNonceFetchResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                CompanionCanonicalUserNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse";
                };

                return CompanionCanonicalUserNonceFetchResponse;
            })();

            PeerDataOperationResult.CompanionMetaNonceFetchResponse = (function() {

                function CompanionMetaNonceFetchResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                CompanionMetaNonceFetchResponse.prototype.nonce = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(CompanionMetaNonceFetchResponse.prototype, "_nonce", {
                    get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CompanionMetaNonceFetchResponse.create = function create(properties) {
                    return new CompanionMetaNonceFetchResponse(properties);
                };

                CompanionMetaNonceFetchResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.nonce != null && Object.hasOwnProperty.call(m, "nonce"))
                        w.uint32(10).string(m.nonce);
                    return w;
                };

                CompanionMetaNonceFetchResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.nonce = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                CompanionMetaNonceFetchResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse();
                    if (d.nonce != null) {
                        m.nonce = String(d.nonce);
                    }
                    return m;
                };

                CompanionMetaNonceFetchResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.nonce != null && m.hasOwnProperty("nonce")) {
                        d.nonce = m.nonce;
                        if (o.oneofs)
                            d._nonce = "nonce";
                    }
                    return d;
                };

                CompanionMetaNonceFetchResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                CompanionMetaNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse";
                };

                return CompanionMetaNonceFetchResponse;
            })();

            PeerDataOperationResult.WaffleNonceFetchResponse = (function() {

                function WaffleNonceFetchResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                WaffleNonceFetchResponse.prototype.nonce = null;
                WaffleNonceFetchResponse.prototype.waEntFbid = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WaffleNonceFetchResponse.prototype, "_nonce", {
                    get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(WaffleNonceFetchResponse.prototype, "_waEntFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["waEntFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                WaffleNonceFetchResponse.create = function create(properties) {
                    return new WaffleNonceFetchResponse(properties);
                };

                WaffleNonceFetchResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.nonce != null && Object.hasOwnProperty.call(m, "nonce"))
                        w.uint32(10).string(m.nonce);
                    if (m.waEntFbid != null && Object.hasOwnProperty.call(m, "waEntFbid"))
                        w.uint32(18).string(m.waEntFbid);
                    return w;
                };

                WaffleNonceFetchResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.nonce = r.string();
                                break;
                            }
                        case 2: {
                                m.waEntFbid = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                WaffleNonceFetchResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse();
                    if (d.nonce != null) {
                        m.nonce = String(d.nonce);
                    }
                    if (d.waEntFbid != null) {
                        m.waEntFbid = String(d.waEntFbid);
                    }
                    return m;
                };

                WaffleNonceFetchResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.nonce != null && m.hasOwnProperty("nonce")) {
                        d.nonce = m.nonce;
                        if (o.oneofs)
                            d._nonce = "nonce";
                    }
                    if (m.waEntFbid != null && m.hasOwnProperty("waEntFbid")) {
                        d.waEntFbid = m.waEntFbid;
                        if (o.oneofs)
                            d._waEntFbid = "waEntFbid";
                    }
                    return d;
                };

                WaffleNonceFetchResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                WaffleNonceFetchResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse";
                };

                return WaffleNonceFetchResponse;
            })();

            PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse = (function() {

                function FullHistorySyncOnDemandRequestResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                FullHistorySyncOnDemandRequestResponse.prototype.requestMetadata = null;
                FullHistorySyncOnDemandRequestResponse.prototype.responseCode = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_requestMetadata", {
                    get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_responseCode", {
                    get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                FullHistorySyncOnDemandRequestResponse.create = function create(properties) {
                    return new FullHistorySyncOnDemandRequestResponse(properties);
                };

                FullHistorySyncOnDemandRequestResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.requestMetadata != null && Object.hasOwnProperty.call(m, "requestMetadata"))
                        WAE2E.FullHistorySyncOnDemandRequestMetadata.encode(m.requestMetadata, w.uint32(10).fork()).ldelim();
                    if (m.responseCode != null && Object.hasOwnProperty.call(m, "responseCode"))
                        w.uint32(16).int32(m.responseCode);
                    return w;
                };

                FullHistorySyncOnDemandRequestResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 2: {
                                m.responseCode = r.int32();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                FullHistorySyncOnDemandRequestResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse();
                    if (d.requestMetadata != null) {
                        if (typeof d.requestMetadata !== "object")
                            throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.requestMetadata: object expected");
                        m.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.fromObject(d.requestMetadata, n + 1);
                    }
                    switch (d.responseCode) {
                    default:
                        if (typeof d.responseCode === "number") {
                            m.responseCode = d.responseCode;
                            break;
                        }
                        break;
                    case "REQUEST_SUCCESS":
                    case 0:
                        m.responseCode = 0;
                        break;
                    case "REQUEST_TIME_EXPIRED":
                    case 1:
                        m.responseCode = 1;
                        break;
                    case "DECLINED_SHARING_HISTORY":
                    case 2:
                        m.responseCode = 2;
                        break;
                    case "GENERIC_ERROR":
                    case 3:
                        m.responseCode = 3;
                        break;
                    case "ERROR_REQUEST_ON_NON_SMB_PRIMARY":
                    case 4:
                        m.responseCode = 4;
                        break;
                    case "ERROR_HOSTED_DEVICE_NOT_CONNECTED":
                    case 5:
                        m.responseCode = 5;
                        break;
                    case "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET":
                    case 6:
                        m.responseCode = 6;
                        break;
                    case "ERROR_MULTI_PROVIDER_NOT_CONFIGURED":
                    case 7:
                        m.responseCode = 7;
                        break;
                    }
                    return m;
                };

                FullHistorySyncOnDemandRequestResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.requestMetadata != null && m.hasOwnProperty("requestMetadata")) {
                        d.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.toObject(m.requestMetadata, o);
                        if (o.oneofs)
                            d._requestMetadata = "requestMetadata";
                    }
                    if (m.responseCode != null && m.hasOwnProperty("responseCode")) {
                        d.responseCode = o.enums === String ? WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[m.responseCode] === undefined ? m.responseCode : WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[m.responseCode] : m.responseCode;
                        if (o.oneofs)
                            d._responseCode = "responseCode";
                    }
                    return d;
                };

                FullHistorySyncOnDemandRequestResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                FullHistorySyncOnDemandRequestResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse";
                };

                return FullHistorySyncOnDemandRequestResponse;
            })();

            PeerDataOperationResult.PlaceholderMessageResendResponse = (function() {

                function PlaceholderMessageResendResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                PlaceholderMessageResendResponse.prototype.webMessageInfoBytes = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(PlaceholderMessageResendResponse.prototype, "_webMessageInfoBytes", {
                    get: $util.oneOfGetter($oneOfFields = ["webMessageInfoBytes"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PlaceholderMessageResendResponse.create = function create(properties) {
                    return new PlaceholderMessageResendResponse(properties);
                };

                PlaceholderMessageResendResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.webMessageInfoBytes != null && Object.hasOwnProperty.call(m, "webMessageInfoBytes"))
                        w.uint32(10).bytes(m.webMessageInfoBytes);
                    return w;
                };

                PlaceholderMessageResendResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.webMessageInfoBytes = r.bytes();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                PlaceholderMessageResendResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse();
                    if (d.webMessageInfoBytes != null) {
                        if (typeof d.webMessageInfoBytes === "string")
                            $util.base64.decode(d.webMessageInfoBytes, m.webMessageInfoBytes = $util.newBuffer($util.base64.length(d.webMessageInfoBytes)), 0);
                        else if (d.webMessageInfoBytes.length >= 0)
                            m.webMessageInfoBytes = d.webMessageInfoBytes;
                    }
                    return m;
                };

                PlaceholderMessageResendResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.webMessageInfoBytes != null && m.hasOwnProperty("webMessageInfoBytes")) {
                        d.webMessageInfoBytes = o.bytes === String ? $util.base64.encode(m.webMessageInfoBytes, 0, m.webMessageInfoBytes.length) : o.bytes === Array ? Array.prototype.slice.call(m.webMessageInfoBytes) : m.webMessageInfoBytes;
                        if (o.oneofs)
                            d._webMessageInfoBytes = "webMessageInfoBytes";
                    }
                    return d;
                };

                PlaceholderMessageResendResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                PlaceholderMessageResendResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse";
                };

                return PlaceholderMessageResendResponse;
            })();

            PeerDataOperationResult.LinkPreviewResponse = (function() {

                function LinkPreviewResponse(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                LinkPreviewResponse.prototype.URL = null;
                LinkPreviewResponse.prototype.title = null;
                LinkPreviewResponse.prototype.description = null;
                LinkPreviewResponse.prototype.thumbData = null;
                LinkPreviewResponse.prototype.matchText = null;
                LinkPreviewResponse.prototype.previewType = null;
                LinkPreviewResponse.prototype.hqThumbnail = null;
                LinkPreviewResponse.prototype.previewMetadata = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_URL", {
                    get: $util.oneOfGetter($oneOfFields = ["URL"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_thumbData", {
                    get: $util.oneOfGetter($oneOfFields = ["thumbData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_matchText", {
                    get: $util.oneOfGetter($oneOfFields = ["matchText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_previewType", {
                    get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_hqThumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["hqThumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(LinkPreviewResponse.prototype, "_previewMetadata", {
                    get: $util.oneOfGetter($oneOfFields = ["previewMetadata"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                LinkPreviewResponse.create = function create(properties) {
                    return new LinkPreviewResponse(properties);
                };

                LinkPreviewResponse.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                        w.uint32(10).string(m.URL);
                    if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                        w.uint32(18).string(m.title);
                    if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                        w.uint32(26).string(m.description);
                    if (m.thumbData != null && Object.hasOwnProperty.call(m, "thumbData"))
                        w.uint32(34).bytes(m.thumbData);
                    if (m.matchText != null && Object.hasOwnProperty.call(m, "matchText"))
                        w.uint32(50).string(m.matchText);
                    if (m.previewType != null && Object.hasOwnProperty.call(m, "previewType"))
                        w.uint32(58).string(m.previewType);
                    if (m.hqThumbnail != null && Object.hasOwnProperty.call(m, "hqThumbnail"))
                        WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.encode(m.hqThumbnail, w.uint32(66).fork()).ldelim();
                    if (m.previewMetadata != null && Object.hasOwnProperty.call(m, "previewMetadata"))
                        WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.encode(m.previewMetadata, w.uint32(74).fork()).ldelim();
                    return w;
                };

                LinkPreviewResponse.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.URL = r.string();
                                break;
                            }
                        case 2: {
                                m.title = r.string();
                                break;
                            }
                        case 3: {
                                m.description = r.string();
                                break;
                            }
                        case 4: {
                                m.thumbData = r.bytes();
                                break;
                            }
                        case 6: {
                                m.matchText = r.string();
                                break;
                            }
                        case 7: {
                                m.previewType = r.string();
                                break;
                            }
                        case 8: {
                                m.hqThumbnail = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 9: {
                                m.previewMetadata = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                LinkPreviewResponse.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse();
                    if (d.URL != null) {
                        m.URL = String(d.URL);
                    }
                    if (d.title != null) {
                        m.title = String(d.title);
                    }
                    if (d.description != null) {
                        m.description = String(d.description);
                    }
                    if (d.thumbData != null) {
                        if (typeof d.thumbData === "string")
                            $util.base64.decode(d.thumbData, m.thumbData = $util.newBuffer($util.base64.length(d.thumbData)), 0);
                        else if (d.thumbData.length >= 0)
                            m.thumbData = d.thumbData;
                    }
                    if (d.matchText != null) {
                        m.matchText = String(d.matchText);
                    }
                    if (d.previewType != null) {
                        m.previewType = String(d.previewType);
                    }
                    if (d.hqThumbnail != null) {
                        if (typeof d.hqThumbnail !== "object")
                            throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.hqThumbnail: object expected");
                        m.hqThumbnail = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.fromObject(d.hqThumbnail, n + 1);
                    }
                    if (d.previewMetadata != null) {
                        if (typeof d.previewMetadata !== "object")
                            throw TypeError(".WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.previewMetadata: object expected");
                        m.previewMetadata = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.fromObject(d.previewMetadata, n + 1);
                    }
                    return m;
                };

                LinkPreviewResponse.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.URL != null && m.hasOwnProperty("URL")) {
                        d.URL = m.URL;
                        if (o.oneofs)
                            d._URL = "URL";
                    }
                    if (m.title != null && m.hasOwnProperty("title")) {
                        d.title = m.title;
                        if (o.oneofs)
                            d._title = "title";
                    }
                    if (m.description != null && m.hasOwnProperty("description")) {
                        d.description = m.description;
                        if (o.oneofs)
                            d._description = "description";
                    }
                    if (m.thumbData != null && m.hasOwnProperty("thumbData")) {
                        d.thumbData = o.bytes === String ? $util.base64.encode(m.thumbData, 0, m.thumbData.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbData) : m.thumbData;
                        if (o.oneofs)
                            d._thumbData = "thumbData";
                    }
                    if (m.matchText != null && m.hasOwnProperty("matchText")) {
                        d.matchText = m.matchText;
                        if (o.oneofs)
                            d._matchText = "matchText";
                    }
                    if (m.previewType != null && m.hasOwnProperty("previewType")) {
                        d.previewType = m.previewType;
                        if (o.oneofs)
                            d._previewType = "previewType";
                    }
                    if (m.hqThumbnail != null && m.hasOwnProperty("hqThumbnail")) {
                        d.hqThumbnail = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.toObject(m.hqThumbnail, o);
                        if (o.oneofs)
                            d._hqThumbnail = "hqThumbnail";
                    }
                    if (m.previewMetadata != null && m.hasOwnProperty("previewMetadata")) {
                        d.previewMetadata = WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.toObject(m.previewMetadata, o);
                        if (o.oneofs)
                            d._previewMetadata = "previewMetadata";
                    }
                    return d;
                };

                LinkPreviewResponse.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                LinkPreviewResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse";
                };

                LinkPreviewResponse.PaymentLinkPreviewMetadata = (function() {

                    function PaymentLinkPreviewMetadata(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    PaymentLinkPreviewMetadata.prototype.isBusinessVerified = null;
                    PaymentLinkPreviewMetadata.prototype.providerName = null;
                    PaymentLinkPreviewMetadata.prototype.amount = null;
                    PaymentLinkPreviewMetadata.prototype.offset = null;
                    PaymentLinkPreviewMetadata.prototype.currency = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_isBusinessVerified", {
                        get: $util.oneOfGetter($oneOfFields = ["isBusinessVerified"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_providerName", {
                        get: $util.oneOfGetter($oneOfFields = ["providerName"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_amount", {
                        get: $util.oneOfGetter($oneOfFields = ["amount"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_offset", {
                        get: $util.oneOfGetter($oneOfFields = ["offset"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_currency", {
                        get: $util.oneOfGetter($oneOfFields = ["currency"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    PaymentLinkPreviewMetadata.create = function create(properties) {
                        return new PaymentLinkPreviewMetadata(properties);
                    };

                    PaymentLinkPreviewMetadata.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.isBusinessVerified != null && Object.hasOwnProperty.call(m, "isBusinessVerified"))
                            w.uint32(8).bool(m.isBusinessVerified);
                        if (m.providerName != null && Object.hasOwnProperty.call(m, "providerName"))
                            w.uint32(18).string(m.providerName);
                        if (m.amount != null && Object.hasOwnProperty.call(m, "amount"))
                            w.uint32(26).string(m.amount);
                        if (m.offset != null && Object.hasOwnProperty.call(m, "offset"))
                            w.uint32(34).string(m.offset);
                        if (m.currency != null && Object.hasOwnProperty.call(m, "currency"))
                            w.uint32(42).string(m.currency);
                        return w;
                    };

                    PaymentLinkPreviewMetadata.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.isBusinessVerified = r.bool();
                                    break;
                                }
                            case 2: {
                                    m.providerName = r.string();
                                    break;
                                }
                            case 3: {
                                    m.amount = r.string();
                                    break;
                                }
                            case 4: {
                                    m.offset = r.string();
                                    break;
                                }
                            case 5: {
                                    m.currency = r.string();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    PaymentLinkPreviewMetadata.fromObject = function fromObject(d, n) {
                        if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata();
                        if (d.isBusinessVerified != null) {
                            m.isBusinessVerified = Boolean(d.isBusinessVerified);
                        }
                        if (d.providerName != null) {
                            m.providerName = String(d.providerName);
                        }
                        if (d.amount != null) {
                            m.amount = String(d.amount);
                        }
                        if (d.offset != null) {
                            m.offset = String(d.offset);
                        }
                        if (d.currency != null) {
                            m.currency = String(d.currency);
                        }
                        return m;
                    };

                    PaymentLinkPreviewMetadata.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.isBusinessVerified != null && m.hasOwnProperty("isBusinessVerified")) {
                            d.isBusinessVerified = m.isBusinessVerified;
                            if (o.oneofs)
                                d._isBusinessVerified = "isBusinessVerified";
                        }
                        if (m.providerName != null && m.hasOwnProperty("providerName")) {
                            d.providerName = m.providerName;
                            if (o.oneofs)
                                d._providerName = "providerName";
                        }
                        if (m.amount != null && m.hasOwnProperty("amount")) {
                            d.amount = m.amount;
                            if (o.oneofs)
                                d._amount = "amount";
                        }
                        if (m.offset != null && m.hasOwnProperty("offset")) {
                            d.offset = m.offset;
                            if (o.oneofs)
                                d._offset = "offset";
                        }
                        if (m.currency != null && m.hasOwnProperty("currency")) {
                            d.currency = m.currency;
                            if (o.oneofs)
                                d._currency = "currency";
                        }
                        return d;
                    };

                    PaymentLinkPreviewMetadata.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    PaymentLinkPreviewMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata";
                    };

                    return PaymentLinkPreviewMetadata;
                })();

                LinkPreviewResponse.LinkPreviewHighQualityThumbnail = (function() {

                    function LinkPreviewHighQualityThumbnail(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    LinkPreviewHighQualityThumbnail.prototype.directPath = null;
                    LinkPreviewHighQualityThumbnail.prototype.thumbHash = null;
                    LinkPreviewHighQualityThumbnail.prototype.encThumbHash = null;
                    LinkPreviewHighQualityThumbnail.prototype.mediaKey = null;
                    LinkPreviewHighQualityThumbnail.prototype.mediaKeyTimestampMS = null;
                    LinkPreviewHighQualityThumbnail.prototype.thumbWidth = null;
                    LinkPreviewHighQualityThumbnail.prototype.thumbHeight = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_directPath", {
                        get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHash", {
                        get: $util.oneOfGetter($oneOfFields = ["thumbHash"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_encThumbHash", {
                        get: $util.oneOfGetter($oneOfFields = ["encThumbHash"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKey", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKeyTimestampMS", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestampMS"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbWidth", {
                        get: $util.oneOfGetter($oneOfFields = ["thumbWidth"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHeight", {
                        get: $util.oneOfGetter($oneOfFields = ["thumbHeight"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    LinkPreviewHighQualityThumbnail.create = function create(properties) {
                        return new LinkPreviewHighQualityThumbnail(properties);
                    };

                    LinkPreviewHighQualityThumbnail.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                            w.uint32(10).string(m.directPath);
                        if (m.thumbHash != null && Object.hasOwnProperty.call(m, "thumbHash"))
                            w.uint32(18).string(m.thumbHash);
                        if (m.encThumbHash != null && Object.hasOwnProperty.call(m, "encThumbHash"))
                            w.uint32(26).string(m.encThumbHash);
                        if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                            w.uint32(34).bytes(m.mediaKey);
                        if (m.mediaKeyTimestampMS != null && Object.hasOwnProperty.call(m, "mediaKeyTimestampMS"))
                            w.uint32(40).int64(m.mediaKeyTimestampMS);
                        if (m.thumbWidth != null && Object.hasOwnProperty.call(m, "thumbWidth"))
                            w.uint32(48).int32(m.thumbWidth);
                        if (m.thumbHeight != null && Object.hasOwnProperty.call(m, "thumbHeight"))
                            w.uint32(56).int32(m.thumbHeight);
                        return w;
                    };

                    LinkPreviewHighQualityThumbnail.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.directPath = r.string();
                                    break;
                                }
                            case 2: {
                                    m.thumbHash = r.string();
                                    break;
                                }
                            case 3: {
                                    m.encThumbHash = r.string();
                                    break;
                                }
                            case 4: {
                                    m.mediaKey = r.bytes();
                                    break;
                                }
                            case 5: {
                                    m.mediaKeyTimestampMS = r.int64();
                                    break;
                                }
                            case 6: {
                                    m.thumbWidth = r.int32();
                                    break;
                                }
                            case 7: {
                                    m.thumbHeight = r.int32();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    LinkPreviewHighQualityThumbnail.fromObject = function fromObject(d, n) {
                        if (d instanceof WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail();
                        if (d.directPath != null) {
                            m.directPath = String(d.directPath);
                        }
                        if (d.thumbHash != null) {
                            m.thumbHash = String(d.thumbHash);
                        }
                        if (d.encThumbHash != null) {
                            m.encThumbHash = String(d.encThumbHash);
                        }
                        if (d.mediaKey != null) {
                            if (typeof d.mediaKey === "string")
                                $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                            else if (d.mediaKey.length >= 0)
                                m.mediaKey = d.mediaKey;
                        }
                        if (d.mediaKeyTimestampMS != null) {
                            if ($util.Long)
                                (m.mediaKeyTimestampMS = $util.Long.fromValue(d.mediaKeyTimestampMS)).unsigned = false;
                            else if (typeof d.mediaKeyTimestampMS === "string")
                                m.mediaKeyTimestampMS = parseInt(d.mediaKeyTimestampMS, 10);
                            else if (typeof d.mediaKeyTimestampMS === "number")
                                m.mediaKeyTimestampMS = d.mediaKeyTimestampMS;
                            else if (typeof d.mediaKeyTimestampMS === "object")
                                m.mediaKeyTimestampMS = new $util.LongBits(d.mediaKeyTimestampMS.low >>> 0, d.mediaKeyTimestampMS.high >>> 0).toNumber();
                        }
                        if (d.thumbWidth != null) {
                            m.thumbWidth = d.thumbWidth | 0;
                        }
                        if (d.thumbHeight != null) {
                            m.thumbHeight = d.thumbHeight | 0;
                        }
                        return m;
                    };

                    LinkPreviewHighQualityThumbnail.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.directPath != null && m.hasOwnProperty("directPath")) {
                            d.directPath = m.directPath;
                            if (o.oneofs)
                                d._directPath = "directPath";
                        }
                        if (m.thumbHash != null && m.hasOwnProperty("thumbHash")) {
                            d.thumbHash = m.thumbHash;
                            if (o.oneofs)
                                d._thumbHash = "thumbHash";
                        }
                        if (m.encThumbHash != null && m.hasOwnProperty("encThumbHash")) {
                            d.encThumbHash = m.encThumbHash;
                            if (o.oneofs)
                                d._encThumbHash = "encThumbHash";
                        }
                        if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                            d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                            if (o.oneofs)
                                d._mediaKey = "mediaKey";
                        }
                        if (m.mediaKeyTimestampMS != null && m.hasOwnProperty("mediaKeyTimestampMS")) {
                            if (typeof m.mediaKeyTimestampMS === "number")
                                d.mediaKeyTimestampMS = o.longs === String ? String(m.mediaKeyTimestampMS) : m.mediaKeyTimestampMS;
                            else
                                d.mediaKeyTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestampMS) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestampMS.low >>> 0, m.mediaKeyTimestampMS.high >>> 0).toNumber() : m.mediaKeyTimestampMS;
                            if (o.oneofs)
                                d._mediaKeyTimestampMS = "mediaKeyTimestampMS";
                        }
                        if (m.thumbWidth != null && m.hasOwnProperty("thumbWidth")) {
                            d.thumbWidth = m.thumbWidth;
                            if (o.oneofs)
                                d._thumbWidth = "thumbWidth";
                        }
                        if (m.thumbHeight != null && m.hasOwnProperty("thumbHeight")) {
                            d.thumbHeight = m.thumbHeight;
                            if (o.oneofs)
                                d._thumbHeight = "thumbHeight";
                        }
                        return d;
                    };

                    LinkPreviewHighQualityThumbnail.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    LinkPreviewHighQualityThumbnail.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail";
                    };

                    return LinkPreviewHighQualityThumbnail;
                })();

                return LinkPreviewResponse;
            })();

            return PeerDataOperationResult;
        })();

        return PeerDataOperationRequestResponseMessage;
    })();

    WAE2E.PeerDataOperationRequestMessage = (function() {

        function PeerDataOperationRequestMessage(p) {
            this.requestStickerReupload = [];
            this.requestURLPreview = [];
            this.placeholderMessageResendRequest = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PeerDataOperationRequestMessage.prototype.peerDataOperationRequestType = null;
        PeerDataOperationRequestMessage.prototype.requestStickerReupload = $util.emptyArray;
        PeerDataOperationRequestMessage.prototype.requestURLPreview = $util.emptyArray;
        PeerDataOperationRequestMessage.prototype.historySyncOnDemandRequest = null;
        PeerDataOperationRequestMessage.prototype.placeholderMessageResendRequest = $util.emptyArray;
        PeerDataOperationRequestMessage.prototype.fullHistorySyncOnDemandRequest = null;
        PeerDataOperationRequestMessage.prototype.syncdCollectionFatalRecoveryRequest = null;
        PeerDataOperationRequestMessage.prototype.historySyncChunkRetryRequest = null;
        PeerDataOperationRequestMessage.prototype.galaxyFlowAction = null;
        PeerDataOperationRequestMessage.prototype.companionCanonicalUserNonceFetchRequest = null;
        PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsContactListRequest = null;
        PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsRefreshRequest = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_peerDataOperationRequestType", {
            get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncOnDemandRequest", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncOnDemandRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_fullHistorySyncOnDemandRequest", {
            get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_syncdCollectionFatalRecoveryRequest", {
            get: $util.oneOfGetter($oneOfFields = ["syncdCollectionFatalRecoveryRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncChunkRetryRequest", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_galaxyFlowAction", {
            get: $util.oneOfGetter($oneOfFields = ["galaxyFlowAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_companionCanonicalUserNonceFetchRequest", {
            get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsContactListRequest", {
            get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsRefreshRequest", {
            get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsRefreshRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PeerDataOperationRequestMessage.create = function create(properties) {
            return new PeerDataOperationRequestMessage(properties);
        };

        PeerDataOperationRequestMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.peerDataOperationRequestType != null && Object.hasOwnProperty.call(m, "peerDataOperationRequestType"))
                w.uint32(8).int32(m.peerDataOperationRequestType);
            if (m.requestStickerReupload != null && m.requestStickerReupload.length) {
                for (var i = 0; i < m.requestStickerReupload.length; ++i)
                    WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload.encode(m.requestStickerReupload[i], w.uint32(18).fork()).ldelim();
            }
            if (m.requestURLPreview != null && m.requestURLPreview.length) {
                for (var i = 0; i < m.requestURLPreview.length; ++i)
                    WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview.encode(m.requestURLPreview[i], w.uint32(26).fork()).ldelim();
            }
            if (m.historySyncOnDemandRequest != null && Object.hasOwnProperty.call(m, "historySyncOnDemandRequest"))
                WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.encode(m.historySyncOnDemandRequest, w.uint32(34).fork()).ldelim();
            if (m.placeholderMessageResendRequest != null && m.placeholderMessageResendRequest.length) {
                for (var i = 0; i < m.placeholderMessageResendRequest.length; ++i)
                    WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.encode(m.placeholderMessageResendRequest[i], w.uint32(42).fork()).ldelim();
            }
            if (m.fullHistorySyncOnDemandRequest != null && Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequest"))
                WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.encode(m.fullHistorySyncOnDemandRequest, w.uint32(50).fork()).ldelim();
            if (m.syncdCollectionFatalRecoveryRequest != null && Object.hasOwnProperty.call(m, "syncdCollectionFatalRecoveryRequest"))
                WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.encode(m.syncdCollectionFatalRecoveryRequest, w.uint32(58).fork()).ldelim();
            if (m.historySyncChunkRetryRequest != null && Object.hasOwnProperty.call(m, "historySyncChunkRetryRequest"))
                WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.encode(m.historySyncChunkRetryRequest, w.uint32(66).fork()).ldelim();
            if (m.galaxyFlowAction != null && Object.hasOwnProperty.call(m, "galaxyFlowAction"))
                WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.encode(m.galaxyFlowAction, w.uint32(74).fork()).ldelim();
            if (m.companionCanonicalUserNonceFetchRequest != null && Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequest"))
                WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.encode(m.companionCanonicalUserNonceFetchRequest, w.uint32(82).fork()).ldelim();
            if (m.bizBroadcastInsightsContactListRequest != null && Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListRequest"))
                WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.encode(m.bizBroadcastInsightsContactListRequest, w.uint32(90).fork()).ldelim();
            if (m.bizBroadcastInsightsRefreshRequest != null && Object.hasOwnProperty.call(m, "bizBroadcastInsightsRefreshRequest"))
                WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.encode(m.bizBroadcastInsightsRefreshRequest, w.uint32(98).fork()).ldelim();
            return w;
        };

        PeerDataOperationRequestMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.peerDataOperationRequestType = r.int32();
                        break;
                    }
                case 2: {
                        if (!(m.requestStickerReupload && m.requestStickerReupload.length))
                            m.requestStickerReupload = [];
                        m.requestStickerReupload.push(WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        if (!(m.requestURLPreview && m.requestURLPreview.length))
                            m.requestURLPreview = [];
                        m.requestURLPreview.push(WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 4: {
                        m.historySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        if (!(m.placeholderMessageResendRequest && m.placeholderMessageResendRequest.length))
                            m.placeholderMessageResendRequest = [];
                        m.placeholderMessageResendRequest.push(WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 6: {
                        m.fullHistorySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.syncdCollectionFatalRecoveryRequest = WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.historySyncChunkRetryRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.galaxyFlowAction = WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.companionCanonicalUserNonceFetchRequest = WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.bizBroadcastInsightsContactListRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 12: {
                        m.bizBroadcastInsightsRefreshRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PeerDataOperationRequestMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PeerDataOperationRequestMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PeerDataOperationRequestMessage();
            switch (d.peerDataOperationRequestType) {
            default:
                if (typeof d.peerDataOperationRequestType === "number") {
                    m.peerDataOperationRequestType = d.peerDataOperationRequestType;
                    break;
                }
                break;
            case "UPLOAD_STICKER":
            case 0:
                m.peerDataOperationRequestType = 0;
                break;
            case "SEND_RECENT_STICKER_BOOTSTRAP":
            case 1:
                m.peerDataOperationRequestType = 1;
                break;
            case "GENERATE_LINK_PREVIEW":
            case 2:
                m.peerDataOperationRequestType = 2;
                break;
            case "HISTORY_SYNC_ON_DEMAND":
            case 3:
                m.peerDataOperationRequestType = 3;
                break;
            case "PLACEHOLDER_MESSAGE_RESEND":
            case 4:
                m.peerDataOperationRequestType = 4;
                break;
            case "WAFFLE_LINKING_NONCE_FETCH":
            case 5:
                m.peerDataOperationRequestType = 5;
                break;
            case "FULL_HISTORY_SYNC_ON_DEMAND":
            case 6:
                m.peerDataOperationRequestType = 6;
                break;
            case "COMPANION_META_NONCE_FETCH":
            case 7:
                m.peerDataOperationRequestType = 7;
                break;
            case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
            case 8:
                m.peerDataOperationRequestType = 8;
                break;
            case "COMPANION_CANONICAL_USER_NONCE_FETCH":
            case 9:
                m.peerDataOperationRequestType = 9;
                break;
            case "HISTORY_SYNC_CHUNK_RETRY":
            case 10:
                m.peerDataOperationRequestType = 10;
                break;
            case "GALAXY_FLOW_ACTION":
            case 11:
                m.peerDataOperationRequestType = 11;
                break;
            case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
            case 12:
                m.peerDataOperationRequestType = 12;
                break;
            case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
            case 13:
                m.peerDataOperationRequestType = 13;
                break;
            }
            if (d.requestStickerReupload) {
                if (!Array.isArray(d.requestStickerReupload))
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.requestStickerReupload: array expected");
                m.requestStickerReupload = [];
                for (var i = 0; i < d.requestStickerReupload.length; ++i) {
                    if (typeof d.requestStickerReupload[i] !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.requestStickerReupload: object expected");
                    m.requestStickerReupload[i] = WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload.fromObject(d.requestStickerReupload[i], n + 1);
                }
            }
            if (d.requestURLPreview) {
                if (!Array.isArray(d.requestURLPreview))
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.requestURLPreview: array expected");
                m.requestURLPreview = [];
                for (var i = 0; i < d.requestURLPreview.length; ++i) {
                    if (typeof d.requestURLPreview[i] !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.requestURLPreview: object expected");
                    m.requestURLPreview[i] = WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview.fromObject(d.requestURLPreview[i], n + 1);
                }
            }
            if (d.historySyncOnDemandRequest != null) {
                if (typeof d.historySyncOnDemandRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.historySyncOnDemandRequest: object expected");
                m.historySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.fromObject(d.historySyncOnDemandRequest, n + 1);
            }
            if (d.placeholderMessageResendRequest) {
                if (!Array.isArray(d.placeholderMessageResendRequest))
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.placeholderMessageResendRequest: array expected");
                m.placeholderMessageResendRequest = [];
                for (var i = 0; i < d.placeholderMessageResendRequest.length; ++i) {
                    if (typeof d.placeholderMessageResendRequest[i] !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.placeholderMessageResendRequest: object expected");
                    m.placeholderMessageResendRequest[i] = WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.fromObject(d.placeholderMessageResendRequest[i], n + 1);
                }
            }
            if (d.fullHistorySyncOnDemandRequest != null) {
                if (typeof d.fullHistorySyncOnDemandRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.fullHistorySyncOnDemandRequest: object expected");
                m.fullHistorySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fromObject(d.fullHistorySyncOnDemandRequest, n + 1);
            }
            if (d.syncdCollectionFatalRecoveryRequest != null) {
                if (typeof d.syncdCollectionFatalRecoveryRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.syncdCollectionFatalRecoveryRequest: object expected");
                m.syncdCollectionFatalRecoveryRequest = WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.fromObject(d.syncdCollectionFatalRecoveryRequest, n + 1);
            }
            if (d.historySyncChunkRetryRequest != null) {
                if (typeof d.historySyncChunkRetryRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.historySyncChunkRetryRequest: object expected");
                m.historySyncChunkRetryRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.fromObject(d.historySyncChunkRetryRequest, n + 1);
            }
            if (d.galaxyFlowAction != null) {
                if (typeof d.galaxyFlowAction !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.galaxyFlowAction: object expected");
                m.galaxyFlowAction = WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.fromObject(d.galaxyFlowAction, n + 1);
            }
            if (d.companionCanonicalUserNonceFetchRequest != null) {
                if (typeof d.companionCanonicalUserNonceFetchRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.companionCanonicalUserNonceFetchRequest: object expected");
                m.companionCanonicalUserNonceFetchRequest = WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.fromObject(d.companionCanonicalUserNonceFetchRequest, n + 1);
            }
            if (d.bizBroadcastInsightsContactListRequest != null) {
                if (typeof d.bizBroadcastInsightsContactListRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.bizBroadcastInsightsContactListRequest: object expected");
                m.bizBroadcastInsightsContactListRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.fromObject(d.bizBroadcastInsightsContactListRequest, n + 1);
            }
            if (d.bizBroadcastInsightsRefreshRequest != null) {
                if (typeof d.bizBroadcastInsightsRefreshRequest !== "object")
                    throw TypeError(".WAE2E.PeerDataOperationRequestMessage.bizBroadcastInsightsRefreshRequest: object expected");
                m.bizBroadcastInsightsRefreshRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.fromObject(d.bizBroadcastInsightsRefreshRequest, n + 1);
            }
            return m;
        };

        PeerDataOperationRequestMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.requestStickerReupload = [];
                d.requestURLPreview = [];
                d.placeholderMessageResendRequest = [];
            }
            if (m.peerDataOperationRequestType != null && m.hasOwnProperty("peerDataOperationRequestType")) {
                d.peerDataOperationRequestType = o.enums === String ? WAE2E.PeerDataOperationRequestType[m.peerDataOperationRequestType] === undefined ? m.peerDataOperationRequestType : WAE2E.PeerDataOperationRequestType[m.peerDataOperationRequestType] : m.peerDataOperationRequestType;
                if (o.oneofs)
                    d._peerDataOperationRequestType = "peerDataOperationRequestType";
            }
            if (m.requestStickerReupload && m.requestStickerReupload.length) {
                d.requestStickerReupload = [];
                for (var j = 0; j < m.requestStickerReupload.length; ++j) {
                    d.requestStickerReupload[j] = WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload.toObject(m.requestStickerReupload[j], o);
                }
            }
            if (m.requestURLPreview && m.requestURLPreview.length) {
                d.requestURLPreview = [];
                for (var j = 0; j < m.requestURLPreview.length; ++j) {
                    d.requestURLPreview[j] = WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview.toObject(m.requestURLPreview[j], o);
                }
            }
            if (m.historySyncOnDemandRequest != null && m.hasOwnProperty("historySyncOnDemandRequest")) {
                d.historySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.toObject(m.historySyncOnDemandRequest, o);
                if (o.oneofs)
                    d._historySyncOnDemandRequest = "historySyncOnDemandRequest";
            }
            if (m.placeholderMessageResendRequest && m.placeholderMessageResendRequest.length) {
                d.placeholderMessageResendRequest = [];
                for (var j = 0; j < m.placeholderMessageResendRequest.length; ++j) {
                    d.placeholderMessageResendRequest[j] = WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.toObject(m.placeholderMessageResendRequest[j], o);
                }
            }
            if (m.fullHistorySyncOnDemandRequest != null && m.hasOwnProperty("fullHistorySyncOnDemandRequest")) {
                d.fullHistorySyncOnDemandRequest = WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.toObject(m.fullHistorySyncOnDemandRequest, o);
                if (o.oneofs)
                    d._fullHistorySyncOnDemandRequest = "fullHistorySyncOnDemandRequest";
            }
            if (m.syncdCollectionFatalRecoveryRequest != null && m.hasOwnProperty("syncdCollectionFatalRecoveryRequest")) {
                d.syncdCollectionFatalRecoveryRequest = WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.toObject(m.syncdCollectionFatalRecoveryRequest, o);
                if (o.oneofs)
                    d._syncdCollectionFatalRecoveryRequest = "syncdCollectionFatalRecoveryRequest";
            }
            if (m.historySyncChunkRetryRequest != null && m.hasOwnProperty("historySyncChunkRetryRequest")) {
                d.historySyncChunkRetryRequest = WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.toObject(m.historySyncChunkRetryRequest, o);
                if (o.oneofs)
                    d._historySyncChunkRetryRequest = "historySyncChunkRetryRequest";
            }
            if (m.galaxyFlowAction != null && m.hasOwnProperty("galaxyFlowAction")) {
                d.galaxyFlowAction = WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.toObject(m.galaxyFlowAction, o);
                if (o.oneofs)
                    d._galaxyFlowAction = "galaxyFlowAction";
            }
            if (m.companionCanonicalUserNonceFetchRequest != null && m.hasOwnProperty("companionCanonicalUserNonceFetchRequest")) {
                d.companionCanonicalUserNonceFetchRequest = WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.toObject(m.companionCanonicalUserNonceFetchRequest, o);
                if (o.oneofs)
                    d._companionCanonicalUserNonceFetchRequest = "companionCanonicalUserNonceFetchRequest";
            }
            if (m.bizBroadcastInsightsContactListRequest != null && m.hasOwnProperty("bizBroadcastInsightsContactListRequest")) {
                d.bizBroadcastInsightsContactListRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.toObject(m.bizBroadcastInsightsContactListRequest, o);
                if (o.oneofs)
                    d._bizBroadcastInsightsContactListRequest = "bizBroadcastInsightsContactListRequest";
            }
            if (m.bizBroadcastInsightsRefreshRequest != null && m.hasOwnProperty("bizBroadcastInsightsRefreshRequest")) {
                d.bizBroadcastInsightsRefreshRequest = WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.toObject(m.bizBroadcastInsightsRefreshRequest, o);
                if (o.oneofs)
                    d._bizBroadcastInsightsRefreshRequest = "bizBroadcastInsightsRefreshRequest";
            }
            return d;
        };

        PeerDataOperationRequestMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PeerDataOperationRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage";
        };

        PeerDataOperationRequestMessage.GalaxyFlowAction = (function() {

            function GalaxyFlowAction(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            GalaxyFlowAction.prototype.type = null;
            GalaxyFlowAction.prototype.flowID = null;
            GalaxyFlowAction.prototype.stanzaID = null;
            GalaxyFlowAction.prototype.galaxyFlowDownloadRequestID = null;
            GalaxyFlowAction.prototype.agmID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GalaxyFlowAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GalaxyFlowAction.prototype, "_flowID", {
                get: $util.oneOfGetter($oneOfFields = ["flowID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GalaxyFlowAction.prototype, "_stanzaID", {
                get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GalaxyFlowAction.prototype, "_galaxyFlowDownloadRequestID", {
                get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GalaxyFlowAction.prototype, "_agmID", {
                get: $util.oneOfGetter($oneOfFields = ["agmID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            GalaxyFlowAction.create = function create(properties) {
                return new GalaxyFlowAction(properties);
            };

            GalaxyFlowAction.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.flowID != null && Object.hasOwnProperty.call(m, "flowID"))
                    w.uint32(18).string(m.flowID);
                if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                    w.uint32(26).string(m.stanzaID);
                if (m.galaxyFlowDownloadRequestID != null && Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestID"))
                    w.uint32(34).string(m.galaxyFlowDownloadRequestID);
                if (m.agmID != null && Object.hasOwnProperty.call(m, "agmID"))
                    w.uint32(42).string(m.agmID);
                return w;
            };

            GalaxyFlowAction.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.type = r.int32();
                            break;
                        }
                    case 2: {
                            m.flowID = r.string();
                            break;
                        }
                    case 3: {
                            m.stanzaID = r.string();
                            break;
                        }
                    case 4: {
                            m.galaxyFlowDownloadRequestID = r.string();
                            break;
                        }
                    case 5: {
                            m.agmID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            GalaxyFlowAction.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction();
                switch (d.type) {
                default:
                    if (typeof d.type === "number") {
                        m.type = d.type;
                        break;
                    }
                    break;
                case "NOTIFY_LAUNCH":
                case 1:
                    m.type = 1;
                    break;
                case "DOWNLOAD_RESPONSES":
                case 2:
                    m.type = 2;
                    break;
                }
                if (d.flowID != null) {
                    m.flowID = String(d.flowID);
                }
                if (d.stanzaID != null) {
                    m.stanzaID = String(d.stanzaID);
                }
                if (d.galaxyFlowDownloadRequestID != null) {
                    m.galaxyFlowDownloadRequestID = String(d.galaxyFlowDownloadRequestID);
                }
                if (d.agmID != null) {
                    m.agmID = String(d.agmID);
                }
                return m;
            };

            GalaxyFlowAction.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.type != null && m.hasOwnProperty("type")) {
                    d.type = o.enums === String ? WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[m.type] === undefined ? m.type : WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[m.type] : m.type;
                    if (o.oneofs)
                        d._type = "type";
                }
                if (m.flowID != null && m.hasOwnProperty("flowID")) {
                    d.flowID = m.flowID;
                    if (o.oneofs)
                        d._flowID = "flowID";
                }
                if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                    d.stanzaID = m.stanzaID;
                    if (o.oneofs)
                        d._stanzaID = "stanzaID";
                }
                if (m.galaxyFlowDownloadRequestID != null && m.hasOwnProperty("galaxyFlowDownloadRequestID")) {
                    d.galaxyFlowDownloadRequestID = m.galaxyFlowDownloadRequestID;
                    if (o.oneofs)
                        d._galaxyFlowDownloadRequestID = "galaxyFlowDownloadRequestID";
                }
                if (m.agmID != null && m.hasOwnProperty("agmID")) {
                    d.agmID = m.agmID;
                    if (o.oneofs)
                        d._agmID = "agmID";
                }
                return d;
            };

            GalaxyFlowAction.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GalaxyFlowAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.GalaxyFlowAction";
            };

            GalaxyFlowAction.GalaxyFlowActionType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "NOTIFY_LAUNCH"] = 1;
                values[valuesById[2] = "DOWNLOAD_RESPONSES"] = 2;
                return values;
            })();

            return GalaxyFlowAction;
        })();

        PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest = (function() {

            function BizBroadcastInsightsRefreshRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BizBroadcastInsightsRefreshRequest.prototype.campaignID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BizBroadcastInsightsRefreshRequest.prototype, "_campaignID", {
                get: $util.oneOfGetter($oneOfFields = ["campaignID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BizBroadcastInsightsRefreshRequest.create = function create(properties) {
                return new BizBroadcastInsightsRefreshRequest(properties);
            };

            BizBroadcastInsightsRefreshRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.campaignID != null && Object.hasOwnProperty.call(m, "campaignID"))
                    w.uint32(10).string(m.campaignID);
                return w;
            };

            BizBroadcastInsightsRefreshRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.campaignID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BizBroadcastInsightsRefreshRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest();
                if (d.campaignID != null) {
                    m.campaignID = String(d.campaignID);
                }
                return m;
            };

            BizBroadcastInsightsRefreshRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.campaignID != null && m.hasOwnProperty("campaignID")) {
                    d.campaignID = m.campaignID;
                    if (o.oneofs)
                        d._campaignID = "campaignID";
                }
                return d;
            };

            BizBroadcastInsightsRefreshRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BizBroadcastInsightsRefreshRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest";
            };

            return BizBroadcastInsightsRefreshRequest;
        })();

        PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest = (function() {

            function BizBroadcastInsightsContactListRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BizBroadcastInsightsContactListRequest.prototype.campaignID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BizBroadcastInsightsContactListRequest.prototype, "_campaignID", {
                get: $util.oneOfGetter($oneOfFields = ["campaignID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BizBroadcastInsightsContactListRequest.create = function create(properties) {
                return new BizBroadcastInsightsContactListRequest(properties);
            };

            BizBroadcastInsightsContactListRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.campaignID != null && Object.hasOwnProperty.call(m, "campaignID"))
                    w.uint32(10).string(m.campaignID);
                return w;
            };

            BizBroadcastInsightsContactListRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.campaignID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BizBroadcastInsightsContactListRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest();
                if (d.campaignID != null) {
                    m.campaignID = String(d.campaignID);
                }
                return m;
            };

            BizBroadcastInsightsContactListRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.campaignID != null && m.hasOwnProperty("campaignID")) {
                    d.campaignID = m.campaignID;
                    if (o.oneofs)
                        d._campaignID = "campaignID";
                }
                return d;
            };

            BizBroadcastInsightsContactListRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BizBroadcastInsightsContactListRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest";
            };

            return BizBroadcastInsightsContactListRequest;
        })();

        PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest = (function() {

            function CompanionCanonicalUserNonceFetchRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CompanionCanonicalUserNonceFetchRequest.prototype.registrationTraceID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CompanionCanonicalUserNonceFetchRequest.prototype, "_registrationTraceID", {
                get: $util.oneOfGetter($oneOfFields = ["registrationTraceID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CompanionCanonicalUserNonceFetchRequest.create = function create(properties) {
                return new CompanionCanonicalUserNonceFetchRequest(properties);
            };

            CompanionCanonicalUserNonceFetchRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.registrationTraceID != null && Object.hasOwnProperty.call(m, "registrationTraceID"))
                    w.uint32(10).string(m.registrationTraceID);
                return w;
            };

            CompanionCanonicalUserNonceFetchRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.registrationTraceID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CompanionCanonicalUserNonceFetchRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest();
                if (d.registrationTraceID != null) {
                    m.registrationTraceID = String(d.registrationTraceID);
                }
                return m;
            };

            CompanionCanonicalUserNonceFetchRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.registrationTraceID != null && m.hasOwnProperty("registrationTraceID")) {
                    d.registrationTraceID = m.registrationTraceID;
                    if (o.oneofs)
                        d._registrationTraceID = "registrationTraceID";
                }
                return d;
            };

            CompanionCanonicalUserNonceFetchRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CompanionCanonicalUserNonceFetchRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest";
            };

            return CompanionCanonicalUserNonceFetchRequest;
        })();

        PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest = (function() {

            function HistorySyncChunkRetryRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HistorySyncChunkRetryRequest.prototype.syncType = null;
            HistorySyncChunkRetryRequest.prototype.chunkOrder = null;
            HistorySyncChunkRetryRequest.prototype.chunkNotificationID = null;
            HistorySyncChunkRetryRequest.prototype.regenerateChunk = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_syncType", {
                get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkOrder", {
                get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkNotificationID", {
                get: $util.oneOfGetter($oneOfFields = ["chunkNotificationID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_regenerateChunk", {
                get: $util.oneOfGetter($oneOfFields = ["regenerateChunk"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncChunkRetryRequest.create = function create(properties) {
                return new HistorySyncChunkRetryRequest(properties);
            };

            HistorySyncChunkRetryRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.syncType != null && Object.hasOwnProperty.call(m, "syncType"))
                    w.uint32(8).int32(m.syncType);
                if (m.chunkOrder != null && Object.hasOwnProperty.call(m, "chunkOrder"))
                    w.uint32(16).uint32(m.chunkOrder);
                if (m.chunkNotificationID != null && Object.hasOwnProperty.call(m, "chunkNotificationID"))
                    w.uint32(26).string(m.chunkNotificationID);
                if (m.regenerateChunk != null && Object.hasOwnProperty.call(m, "regenerateChunk"))
                    w.uint32(32).bool(m.regenerateChunk);
                return w;
            };

            HistorySyncChunkRetryRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.syncType = r.int32();
                            break;
                        }
                    case 2: {
                            m.chunkOrder = r.uint32();
                            break;
                        }
                    case 3: {
                            m.chunkNotificationID = r.string();
                            break;
                        }
                    case 4: {
                            m.regenerateChunk = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HistorySyncChunkRetryRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest();
                switch (d.syncType) {
                default:
                    if (typeof d.syncType === "number") {
                        m.syncType = d.syncType;
                        break;
                    }
                    break;
                case "INITIAL_BOOTSTRAP":
                case 0:
                    m.syncType = 0;
                    break;
                case "INITIAL_STATUS_V3":
                case 1:
                    m.syncType = 1;
                    break;
                case "FULL":
                case 2:
                    m.syncType = 2;
                    break;
                case "RECENT":
                case 3:
                    m.syncType = 3;
                    break;
                case "PUSH_NAME":
                case 4:
                    m.syncType = 4;
                    break;
                case "NON_BLOCKING_DATA":
                case 5:
                    m.syncType = 5;
                    break;
                case "ON_DEMAND":
                case 6:
                    m.syncType = 6;
                    break;
                case "NO_HISTORY":
                case 7:
                    m.syncType = 7;
                    break;
                case "MESSAGE_ACCESS_STATUS":
                case 8:
                    m.syncType = 8;
                    break;
                }
                if (d.chunkOrder != null) {
                    m.chunkOrder = d.chunkOrder >>> 0;
                }
                if (d.chunkNotificationID != null) {
                    m.chunkNotificationID = String(d.chunkNotificationID);
                }
                if (d.regenerateChunk != null) {
                    m.regenerateChunk = Boolean(d.regenerateChunk);
                }
                return m;
            };

            HistorySyncChunkRetryRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.syncType != null && m.hasOwnProperty("syncType")) {
                    d.syncType = o.enums === String ? WAE2E.HistorySyncType[m.syncType] === undefined ? m.syncType : WAE2E.HistorySyncType[m.syncType] : m.syncType;
                    if (o.oneofs)
                        d._syncType = "syncType";
                }
                if (m.chunkOrder != null && m.hasOwnProperty("chunkOrder")) {
                    d.chunkOrder = m.chunkOrder;
                    if (o.oneofs)
                        d._chunkOrder = "chunkOrder";
                }
                if (m.chunkNotificationID != null && m.hasOwnProperty("chunkNotificationID")) {
                    d.chunkNotificationID = m.chunkNotificationID;
                    if (o.oneofs)
                        d._chunkNotificationID = "chunkNotificationID";
                }
                if (m.regenerateChunk != null && m.hasOwnProperty("regenerateChunk")) {
                    d.regenerateChunk = m.regenerateChunk;
                    if (o.oneofs)
                        d._regenerateChunk = "regenerateChunk";
                }
                return d;
            };

            HistorySyncChunkRetryRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncChunkRetryRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest";
            };

            return HistorySyncChunkRetryRequest;
        })();

        PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest = (function() {

            function SyncDCollectionFatalRecoveryRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            SyncDCollectionFatalRecoveryRequest.prototype.collectionName = null;
            SyncDCollectionFatalRecoveryRequest.prototype.timestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_collectionName", {
                get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SyncDCollectionFatalRecoveryRequest.create = function create(properties) {
                return new SyncDCollectionFatalRecoveryRequest(properties);
            };

            SyncDCollectionFatalRecoveryRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.collectionName != null && Object.hasOwnProperty.call(m, "collectionName"))
                    w.uint32(10).string(m.collectionName);
                if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                    w.uint32(16).int64(m.timestamp);
                return w;
            };

            SyncDCollectionFatalRecoveryRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.collectionName = r.string();
                            break;
                        }
                    case 2: {
                            m.timestamp = r.int64();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            SyncDCollectionFatalRecoveryRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest();
                if (d.collectionName != null) {
                    m.collectionName = String(d.collectionName);
                }
                if (d.timestamp != null) {
                    if ($util.Long)
                        (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = false;
                    else if (typeof d.timestamp === "string")
                        m.timestamp = parseInt(d.timestamp, 10);
                    else if (typeof d.timestamp === "number")
                        m.timestamp = d.timestamp;
                    else if (typeof d.timestamp === "object")
                        m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                }
                return m;
            };

            SyncDCollectionFatalRecoveryRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.collectionName != null && m.hasOwnProperty("collectionName")) {
                    d.collectionName = m.collectionName;
                    if (o.oneofs)
                        d._collectionName = "collectionName";
                }
                if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                    if (typeof m.timestamp === "number")
                        d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                    else
                        d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                    if (o.oneofs)
                        d._timestamp = "timestamp";
                }
                return d;
            };

            SyncDCollectionFatalRecoveryRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SyncDCollectionFatalRecoveryRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest";
            };

            return SyncDCollectionFatalRecoveryRequest;
        })();

        PeerDataOperationRequestMessage.PlaceholderMessageResendRequest = (function() {

            function PlaceholderMessageResendRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PlaceholderMessageResendRequest.prototype.messageKey = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PlaceholderMessageResendRequest.prototype, "_messageKey", {
                get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PlaceholderMessageResendRequest.create = function create(properties) {
                return new PlaceholderMessageResendRequest(properties);
            };

            PlaceholderMessageResendRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.messageKey != null && Object.hasOwnProperty.call(m, "messageKey"))
                    WACommon.MessageKey.encode(m.messageKey, w.uint32(10).fork()).ldelim();
                return w;
            };

            PlaceholderMessageResendRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.messageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PlaceholderMessageResendRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest();
                if (d.messageKey != null) {
                    if (typeof d.messageKey !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.messageKey: object expected");
                    m.messageKey = WACommon.MessageKey.fromObject(d.messageKey, n + 1);
                }
                return m;
            };

            PlaceholderMessageResendRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.messageKey != null && m.hasOwnProperty("messageKey")) {
                    d.messageKey = WACommon.MessageKey.toObject(m.messageKey, o);
                    if (o.oneofs)
                        d._messageKey = "messageKey";
                }
                return d;
            };

            PlaceholderMessageResendRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlaceholderMessageResendRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest";
            };

            return PlaceholderMessageResendRequest;
        })();

        PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest = (function() {

            function FullHistorySyncOnDemandRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            FullHistorySyncOnDemandRequest.prototype.requestMetadata = null;
            FullHistorySyncOnDemandRequest.prototype.historySyncConfig = null;
            FullHistorySyncOnDemandRequest.prototype.fullHistorySyncOnDemandConfig = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_requestMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_historySyncConfig", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_fullHistorySyncOnDemandConfig", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandConfig"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FullHistorySyncOnDemandRequest.create = function create(properties) {
                return new FullHistorySyncOnDemandRequest(properties);
            };

            FullHistorySyncOnDemandRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.requestMetadata != null && Object.hasOwnProperty.call(m, "requestMetadata"))
                    WAE2E.FullHistorySyncOnDemandRequestMetadata.encode(m.requestMetadata, w.uint32(10).fork()).ldelim();
                if (m.historySyncConfig != null && Object.hasOwnProperty.call(m, "historySyncConfig"))
                    $root.WACompanionReg.DeviceProps.HistorySyncConfig.encode(m.historySyncConfig, w.uint32(18).fork()).ldelim();
                if (m.fullHistorySyncOnDemandConfig != null && Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandConfig"))
                    WAE2E.FullHistorySyncOnDemandConfig.encode(m.fullHistorySyncOnDemandConfig, w.uint32(26).fork()).ldelim();
                return w;
            };

            FullHistorySyncOnDemandRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.fullHistorySyncOnDemandConfig = WAE2E.FullHistorySyncOnDemandConfig.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            FullHistorySyncOnDemandRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest();
                if (d.requestMetadata != null) {
                    if (typeof d.requestMetadata !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.requestMetadata: object expected");
                    m.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.fromObject(d.requestMetadata, n + 1);
                }
                if (d.historySyncConfig != null) {
                    if (typeof d.historySyncConfig !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.historySyncConfig: object expected");
                    m.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.fromObject(d.historySyncConfig, n + 1);
                }
                if (d.fullHistorySyncOnDemandConfig != null) {
                    if (typeof d.fullHistorySyncOnDemandConfig !== "object")
                        throw TypeError(".WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fullHistorySyncOnDemandConfig: object expected");
                    m.fullHistorySyncOnDemandConfig = WAE2E.FullHistorySyncOnDemandConfig.fromObject(d.fullHistorySyncOnDemandConfig, n + 1);
                }
                return m;
            };

            FullHistorySyncOnDemandRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.requestMetadata != null && m.hasOwnProperty("requestMetadata")) {
                    d.requestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.toObject(m.requestMetadata, o);
                    if (o.oneofs)
                        d._requestMetadata = "requestMetadata";
                }
                if (m.historySyncConfig != null && m.hasOwnProperty("historySyncConfig")) {
                    d.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.toObject(m.historySyncConfig, o);
                    if (o.oneofs)
                        d._historySyncConfig = "historySyncConfig";
                }
                if (m.fullHistorySyncOnDemandConfig != null && m.hasOwnProperty("fullHistorySyncOnDemandConfig")) {
                    d.fullHistorySyncOnDemandConfig = WAE2E.FullHistorySyncOnDemandConfig.toObject(m.fullHistorySyncOnDemandConfig, o);
                    if (o.oneofs)
                        d._fullHistorySyncOnDemandConfig = "fullHistorySyncOnDemandConfig";
                }
                return d;
            };

            FullHistorySyncOnDemandRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FullHistorySyncOnDemandRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest";
            };

            return FullHistorySyncOnDemandRequest;
        })();

        PeerDataOperationRequestMessage.HistorySyncOnDemandRequest = (function() {

            function HistorySyncOnDemandRequest(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HistorySyncOnDemandRequest.prototype.chatJID = null;
            HistorySyncOnDemandRequest.prototype.oldestMsgID = null;
            HistorySyncOnDemandRequest.prototype.oldestMsgFromMe = null;
            HistorySyncOnDemandRequest.prototype.onDemandMsgCount = null;
            HistorySyncOnDemandRequest.prototype.oldestMsgTimestampMS = null;
            HistorySyncOnDemandRequest.prototype.accountLid = null;
            HistorySyncOnDemandRequest.prototype.supportInlineResponse = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_chatJID", {
                get: $util.oneOfGetter($oneOfFields = ["chatJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgID", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMsgID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgFromMe", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMsgFromMe"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_onDemandMsgCount", {
                get: $util.oneOfGetter($oneOfFields = ["onDemandMsgCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgTimestampMS", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMsgTimestampMS"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_accountLid", {
                get: $util.oneOfGetter($oneOfFields = ["accountLid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_supportInlineResponse", {
                get: $util.oneOfGetter($oneOfFields = ["supportInlineResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncOnDemandRequest.create = function create(properties) {
                return new HistorySyncOnDemandRequest(properties);
            };

            HistorySyncOnDemandRequest.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.chatJID != null && Object.hasOwnProperty.call(m, "chatJID"))
                    w.uint32(10).string(m.chatJID);
                if (m.oldestMsgID != null && Object.hasOwnProperty.call(m, "oldestMsgID"))
                    w.uint32(18).string(m.oldestMsgID);
                if (m.oldestMsgFromMe != null && Object.hasOwnProperty.call(m, "oldestMsgFromMe"))
                    w.uint32(24).bool(m.oldestMsgFromMe);
                if (m.onDemandMsgCount != null && Object.hasOwnProperty.call(m, "onDemandMsgCount"))
                    w.uint32(32).int32(m.onDemandMsgCount);
                if (m.oldestMsgTimestampMS != null && Object.hasOwnProperty.call(m, "oldestMsgTimestampMS"))
                    w.uint32(40).int64(m.oldestMsgTimestampMS);
                if (m.accountLid != null && Object.hasOwnProperty.call(m, "accountLid"))
                    w.uint32(50).string(m.accountLid);
                if (m.supportInlineResponse != null && Object.hasOwnProperty.call(m, "supportInlineResponse"))
                    w.uint32(56).bool(m.supportInlineResponse);
                return w;
            };

            HistorySyncOnDemandRequest.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.chatJID = r.string();
                            break;
                        }
                    case 2: {
                            m.oldestMsgID = r.string();
                            break;
                        }
                    case 3: {
                            m.oldestMsgFromMe = r.bool();
                            break;
                        }
                    case 4: {
                            m.onDemandMsgCount = r.int32();
                            break;
                        }
                    case 5: {
                            m.oldestMsgTimestampMS = r.int64();
                            break;
                        }
                    case 6: {
                            m.accountLid = r.string();
                            break;
                        }
                    case 7: {
                            m.supportInlineResponse = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HistorySyncOnDemandRequest.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest();
                if (d.chatJID != null) {
                    m.chatJID = String(d.chatJID);
                }
                if (d.oldestMsgID != null) {
                    m.oldestMsgID = String(d.oldestMsgID);
                }
                if (d.oldestMsgFromMe != null) {
                    m.oldestMsgFromMe = Boolean(d.oldestMsgFromMe);
                }
                if (d.onDemandMsgCount != null) {
                    m.onDemandMsgCount = d.onDemandMsgCount | 0;
                }
                if (d.oldestMsgTimestampMS != null) {
                    if ($util.Long)
                        (m.oldestMsgTimestampMS = $util.Long.fromValue(d.oldestMsgTimestampMS)).unsigned = false;
                    else if (typeof d.oldestMsgTimestampMS === "string")
                        m.oldestMsgTimestampMS = parseInt(d.oldestMsgTimestampMS, 10);
                    else if (typeof d.oldestMsgTimestampMS === "number")
                        m.oldestMsgTimestampMS = d.oldestMsgTimestampMS;
                    else if (typeof d.oldestMsgTimestampMS === "object")
                        m.oldestMsgTimestampMS = new $util.LongBits(d.oldestMsgTimestampMS.low >>> 0, d.oldestMsgTimestampMS.high >>> 0).toNumber();
                }
                if (d.accountLid != null) {
                    m.accountLid = String(d.accountLid);
                }
                if (d.supportInlineResponse != null) {
                    m.supportInlineResponse = Boolean(d.supportInlineResponse);
                }
                return m;
            };

            HistorySyncOnDemandRequest.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.chatJID != null && m.hasOwnProperty("chatJID")) {
                    d.chatJID = m.chatJID;
                    if (o.oneofs)
                        d._chatJID = "chatJID";
                }
                if (m.oldestMsgID != null && m.hasOwnProperty("oldestMsgID")) {
                    d.oldestMsgID = m.oldestMsgID;
                    if (o.oneofs)
                        d._oldestMsgID = "oldestMsgID";
                }
                if (m.oldestMsgFromMe != null && m.hasOwnProperty("oldestMsgFromMe")) {
                    d.oldestMsgFromMe = m.oldestMsgFromMe;
                    if (o.oneofs)
                        d._oldestMsgFromMe = "oldestMsgFromMe";
                }
                if (m.onDemandMsgCount != null && m.hasOwnProperty("onDemandMsgCount")) {
                    d.onDemandMsgCount = m.onDemandMsgCount;
                    if (o.oneofs)
                        d._onDemandMsgCount = "onDemandMsgCount";
                }
                if (m.oldestMsgTimestampMS != null && m.hasOwnProperty("oldestMsgTimestampMS")) {
                    if (typeof m.oldestMsgTimestampMS === "number")
                        d.oldestMsgTimestampMS = o.longs === String ? String(m.oldestMsgTimestampMS) : m.oldestMsgTimestampMS;
                    else
                        d.oldestMsgTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.oldestMsgTimestampMS) : o.longs === Number ? new $util.LongBits(m.oldestMsgTimestampMS.low >>> 0, m.oldestMsgTimestampMS.high >>> 0).toNumber() : m.oldestMsgTimestampMS;
                    if (o.oneofs)
                        d._oldestMsgTimestampMS = "oldestMsgTimestampMS";
                }
                if (m.accountLid != null && m.hasOwnProperty("accountLid")) {
                    d.accountLid = m.accountLid;
                    if (o.oneofs)
                        d._accountLid = "accountLid";
                }
                if (m.supportInlineResponse != null && m.hasOwnProperty("supportInlineResponse")) {
                    d.supportInlineResponse = m.supportInlineResponse;
                    if (o.oneofs)
                        d._supportInlineResponse = "supportInlineResponse";
                }
                return d;
            };

            HistorySyncOnDemandRequest.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncOnDemandRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest";
            };

            return HistorySyncOnDemandRequest;
        })();

        PeerDataOperationRequestMessage.RequestUrlPreview = (function() {

            function RequestUrlPreview(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            RequestUrlPreview.prototype.URL = null;
            RequestUrlPreview.prototype.includeHqThumbnail = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestUrlPreview.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestUrlPreview.prototype, "_includeHqThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["includeHqThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RequestUrlPreview.create = function create(properties) {
                return new RequestUrlPreview(properties);
            };

            RequestUrlPreview.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    w.uint32(10).string(m.URL);
                if (m.includeHqThumbnail != null && Object.hasOwnProperty.call(m, "includeHqThumbnail"))
                    w.uint32(16).bool(m.includeHqThumbnail);
                return w;
            };

            RequestUrlPreview.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.URL = r.string();
                            break;
                        }
                    case 2: {
                            m.includeHqThumbnail = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            RequestUrlPreview.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview();
                if (d.URL != null) {
                    m.URL = String(d.URL);
                }
                if (d.includeHqThumbnail != null) {
                    m.includeHqThumbnail = Boolean(d.includeHqThumbnail);
                }
                return m;
            };

            RequestUrlPreview.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = m.URL;
                    if (o.oneofs)
                        d._URL = "URL";
                }
                if (m.includeHqThumbnail != null && m.hasOwnProperty("includeHqThumbnail")) {
                    d.includeHqThumbnail = m.includeHqThumbnail;
                    if (o.oneofs)
                        d._includeHqThumbnail = "includeHqThumbnail";
                }
                return d;
            };

            RequestUrlPreview.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RequestUrlPreview.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.RequestUrlPreview";
            };

            return RequestUrlPreview;
        })();

        PeerDataOperationRequestMessage.RequestStickerReupload = (function() {

            function RequestStickerReupload(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            RequestStickerReupload.prototype.fileSHA256 = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RequestStickerReupload.prototype, "_fileSHA256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RequestStickerReupload.create = function create(properties) {
                return new RequestStickerReupload(properties);
            };

            RequestStickerReupload.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                    w.uint32(10).string(m.fileSHA256);
                return w;
            };

            RequestStickerReupload.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.fileSHA256 = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            RequestStickerReupload.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload();
                if (d.fileSHA256 != null) {
                    m.fileSHA256 = String(d.fileSHA256);
                }
                return m;
            };

            RequestStickerReupload.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                    d.fileSHA256 = m.fileSHA256;
                    if (o.oneofs)
                        d._fileSHA256 = "fileSHA256";
                }
                return d;
            };

            RequestStickerReupload.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RequestStickerReupload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PeerDataOperationRequestMessage.RequestStickerReupload";
            };

            return RequestStickerReupload;
        })();

        return PeerDataOperationRequestMessage;
    })();

    WAE2E.RequestWelcomeMessageMetadata = (function() {

        function RequestWelcomeMessageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        RequestWelcomeMessageMetadata.prototype.localChatState = null;
        RequestWelcomeMessageMetadata.prototype.welcomeTrigger = null;
        RequestWelcomeMessageMetadata.prototype.botAgentMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_localChatState", {
            get: $util.oneOfGetter($oneOfFields = ["localChatState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_welcomeTrigger", {
            get: $util.oneOfGetter($oneOfFields = ["welcomeTrigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_botAgentMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botAgentMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RequestWelcomeMessageMetadata.create = function create(properties) {
            return new RequestWelcomeMessageMetadata(properties);
        };

        RequestWelcomeMessageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.localChatState != null && Object.hasOwnProperty.call(m, "localChatState"))
                w.uint32(8).int32(m.localChatState);
            if (m.welcomeTrigger != null && Object.hasOwnProperty.call(m, "welcomeTrigger"))
                w.uint32(16).int32(m.welcomeTrigger);
            if (m.botAgentMetadata != null && Object.hasOwnProperty.call(m, "botAgentMetadata"))
                $root.WAAICommon.BotAgentMetadata.encode(m.botAgentMetadata, w.uint32(26).fork()).ldelim();
            return w;
        };

        RequestWelcomeMessageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.RequestWelcomeMessageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.localChatState = r.int32();
                        break;
                    }
                case 2: {
                        m.welcomeTrigger = r.int32();
                        break;
                    }
                case 3: {
                        m.botAgentMetadata = $root.WAAICommon.BotAgentMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        RequestWelcomeMessageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.RequestWelcomeMessageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.RequestWelcomeMessageMetadata();
            switch (d.localChatState) {
            default:
                if (typeof d.localChatState === "number") {
                    m.localChatState = d.localChatState;
                    break;
                }
                break;
            case "EMPTY":
            case 0:
                m.localChatState = 0;
                break;
            case "NON_EMPTY":
            case 1:
                m.localChatState = 1;
                break;
            }
            switch (d.welcomeTrigger) {
            default:
                if (typeof d.welcomeTrigger === "number") {
                    m.welcomeTrigger = d.welcomeTrigger;
                    break;
                }
                break;
            case "CHAT_OPEN":
            case 0:
                m.welcomeTrigger = 0;
                break;
            case "COMPANION_PAIRING":
            case 1:
                m.welcomeTrigger = 1;
                break;
            }
            if (d.botAgentMetadata != null) {
                if (typeof d.botAgentMetadata !== "object")
                    throw TypeError(".WAE2E.RequestWelcomeMessageMetadata.botAgentMetadata: object expected");
                m.botAgentMetadata = $root.WAAICommon.BotAgentMetadata.fromObject(d.botAgentMetadata, n + 1);
            }
            return m;
        };

        RequestWelcomeMessageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.localChatState != null && m.hasOwnProperty("localChatState")) {
                d.localChatState = o.enums === String ? WAE2E.RequestWelcomeMessageMetadata.LocalChatState[m.localChatState] === undefined ? m.localChatState : WAE2E.RequestWelcomeMessageMetadata.LocalChatState[m.localChatState] : m.localChatState;
                if (o.oneofs)
                    d._localChatState = "localChatState";
            }
            if (m.welcomeTrigger != null && m.hasOwnProperty("welcomeTrigger")) {
                d.welcomeTrigger = o.enums === String ? WAE2E.RequestWelcomeMessageMetadata.WelcomeTrigger[m.welcomeTrigger] === undefined ? m.welcomeTrigger : WAE2E.RequestWelcomeMessageMetadata.WelcomeTrigger[m.welcomeTrigger] : m.welcomeTrigger;
                if (o.oneofs)
                    d._welcomeTrigger = "welcomeTrigger";
            }
            if (m.botAgentMetadata != null && m.hasOwnProperty("botAgentMetadata")) {
                d.botAgentMetadata = $root.WAAICommon.BotAgentMetadata.toObject(m.botAgentMetadata, o);
                if (o.oneofs)
                    d._botAgentMetadata = "botAgentMetadata";
            }
            return d;
        };

        RequestWelcomeMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RequestWelcomeMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.RequestWelcomeMessageMetadata";
        };

        RequestWelcomeMessageMetadata.WelcomeTrigger = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CHAT_OPEN"] = 0;
            values[valuesById[1] = "COMPANION_PAIRING"] = 1;
            return values;
        })();

        RequestWelcomeMessageMetadata.LocalChatState = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "EMPTY"] = 0;
            values[valuesById[1] = "NON_EMPTY"] = 1;
            return values;
        })();

        return RequestWelcomeMessageMetadata;
    })();

    WAE2E.ProtocolMessage = (function() {

        function ProtocolMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ProtocolMessage.prototype.key = null;
        ProtocolMessage.prototype.type = null;
        ProtocolMessage.prototype.ephemeralExpiration = null;
        ProtocolMessage.prototype.ephemeralSettingTimestamp = null;
        ProtocolMessage.prototype.historySyncNotification = null;
        ProtocolMessage.prototype.appStateSyncKeyShare = null;
        ProtocolMessage.prototype.appStateSyncKeyRequest = null;
        ProtocolMessage.prototype.initialSecurityNotificationSettingSync = null;
        ProtocolMessage.prototype.appStateFatalExceptionNotification = null;
        ProtocolMessage.prototype.disappearingMode = null;
        ProtocolMessage.prototype.editedMessage = null;
        ProtocolMessage.prototype.timestampMS = null;
        ProtocolMessage.prototype.peerDataOperationRequestMessage = null;
        ProtocolMessage.prototype.peerDataOperationRequestResponseMessage = null;
        ProtocolMessage.prototype.botFeedbackMessage = null;
        ProtocolMessage.prototype.invokerJID = null;
        ProtocolMessage.prototype.requestWelcomeMessageMetadata = null;
        ProtocolMessage.prototype.mediaNotifyMessage = null;
        ProtocolMessage.prototype.cloudApiThreadControlNotification = null;
        ProtocolMessage.prototype.lidMigrationMappingSyncMessage = null;
        ProtocolMessage.prototype.limitSharing = null;
        ProtocolMessage.prototype.aiPsiMetadata = null;
        ProtocolMessage.prototype.aiQueryFanout = null;
        ProtocolMessage.prototype.memberLabel = null;
        ProtocolMessage.prototype.aiMediaCollectionMessage = null;
        ProtocolMessage.prototype.afterReadDuration = null;
        ProtocolMessage.prototype.chatThemeSetting = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_ephemeralExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_historySyncNotification", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncNotification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyShare", {
            get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyShare"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyRequest", {
            get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyRequest"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_initialSecurityNotificationSettingSync", {
            get: $util.oneOfGetter($oneOfFields = ["initialSecurityNotificationSettingSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_appStateFatalExceptionNotification", {
            get: $util.oneOfGetter($oneOfFields = ["appStateFatalExceptionNotification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_editedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_timestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_botFeedbackMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botFeedbackMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_invokerJID", {
            get: $util.oneOfGetter($oneOfFields = ["invokerJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_requestWelcomeMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["requestWelcomeMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_mediaNotifyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["mediaNotifyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_cloudApiThreadControlNotification", {
            get: $util.oneOfGetter($oneOfFields = ["cloudApiThreadControlNotification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_lidMigrationMappingSyncMessage", {
            get: $util.oneOfGetter($oneOfFields = ["lidMigrationMappingSyncMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_aiPsiMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["aiPsiMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_aiQueryFanout", {
            get: $util.oneOfGetter($oneOfFields = ["aiQueryFanout"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_aiMediaCollectionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProtocolMessage.prototype, "_chatThemeSetting", {
            get: $util.oneOfGetter($oneOfFields = ["chatThemeSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProtocolMessage.create = function create(properties) {
            return new ProtocolMessage(properties);
        };

        ProtocolMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(16).int32(m.type);
            if (m.ephemeralExpiration != null && Object.hasOwnProperty.call(m, "ephemeralExpiration"))
                w.uint32(32).uint32(m.ephemeralExpiration);
            if (m.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp"))
                w.uint32(40).int64(m.ephemeralSettingTimestamp);
            if (m.historySyncNotification != null && Object.hasOwnProperty.call(m, "historySyncNotification"))
                WAE2E.HistorySyncNotification.encode(m.historySyncNotification, w.uint32(50).fork()).ldelim();
            if (m.appStateSyncKeyShare != null && Object.hasOwnProperty.call(m, "appStateSyncKeyShare"))
                WAE2E.AppStateSyncKeyShare.encode(m.appStateSyncKeyShare, w.uint32(58).fork()).ldelim();
            if (m.appStateSyncKeyRequest != null && Object.hasOwnProperty.call(m, "appStateSyncKeyRequest"))
                WAE2E.AppStateSyncKeyRequest.encode(m.appStateSyncKeyRequest, w.uint32(66).fork()).ldelim();
            if (m.initialSecurityNotificationSettingSync != null && Object.hasOwnProperty.call(m, "initialSecurityNotificationSettingSync"))
                WAE2E.InitialSecurityNotificationSettingSync.encode(m.initialSecurityNotificationSettingSync, w.uint32(74).fork()).ldelim();
            if (m.appStateFatalExceptionNotification != null && Object.hasOwnProperty.call(m, "appStateFatalExceptionNotification"))
                WAE2E.AppStateFatalExceptionNotification.encode(m.appStateFatalExceptionNotification, w.uint32(82).fork()).ldelim();
            if (m.disappearingMode != null && Object.hasOwnProperty.call(m, "disappearingMode"))
                WAE2E.DisappearingMode.encode(m.disappearingMode, w.uint32(90).fork()).ldelim();
            if (m.editedMessage != null && Object.hasOwnProperty.call(m, "editedMessage"))
                WAE2E.Message.encode(m.editedMessage, w.uint32(114).fork()).ldelim();
            if (m.timestampMS != null && Object.hasOwnProperty.call(m, "timestampMS"))
                w.uint32(120).int64(m.timestampMS);
            if (m.peerDataOperationRequestMessage != null && Object.hasOwnProperty.call(m, "peerDataOperationRequestMessage"))
                WAE2E.PeerDataOperationRequestMessage.encode(m.peerDataOperationRequestMessage, w.uint32(130).fork()).ldelim();
            if (m.peerDataOperationRequestResponseMessage != null && Object.hasOwnProperty.call(m, "peerDataOperationRequestResponseMessage"))
                WAE2E.PeerDataOperationRequestResponseMessage.encode(m.peerDataOperationRequestResponseMessage, w.uint32(138).fork()).ldelim();
            if (m.botFeedbackMessage != null && Object.hasOwnProperty.call(m, "botFeedbackMessage"))
                $root.WAAICommon.BotFeedbackMessage.encode(m.botFeedbackMessage, w.uint32(146).fork()).ldelim();
            if (m.invokerJID != null && Object.hasOwnProperty.call(m, "invokerJID"))
                w.uint32(154).string(m.invokerJID);
            if (m.requestWelcomeMessageMetadata != null && Object.hasOwnProperty.call(m, "requestWelcomeMessageMetadata"))
                WAE2E.RequestWelcomeMessageMetadata.encode(m.requestWelcomeMessageMetadata, w.uint32(162).fork()).ldelim();
            if (m.mediaNotifyMessage != null && Object.hasOwnProperty.call(m, "mediaNotifyMessage"))
                WAE2E.MediaNotifyMessage.encode(m.mediaNotifyMessage, w.uint32(170).fork()).ldelim();
            if (m.cloudApiThreadControlNotification != null && Object.hasOwnProperty.call(m, "cloudApiThreadControlNotification"))
                WAE2E.CloudAPIThreadControlNotification.encode(m.cloudApiThreadControlNotification, w.uint32(178).fork()).ldelim();
            if (m.lidMigrationMappingSyncMessage != null && Object.hasOwnProperty.call(m, "lidMigrationMappingSyncMessage"))
                WAE2E.LIDMigrationMappingSyncMessage.encode(m.lidMigrationMappingSyncMessage, w.uint32(186).fork()).ldelim();
            if (m.limitSharing != null && Object.hasOwnProperty.call(m, "limitSharing"))
                WACommon.LimitSharing.encode(m.limitSharing, w.uint32(194).fork()).ldelim();
            if (m.aiPsiMetadata != null && Object.hasOwnProperty.call(m, "aiPsiMetadata"))
                w.uint32(202).bytes(m.aiPsiMetadata);
            if (m.aiQueryFanout != null && Object.hasOwnProperty.call(m, "aiQueryFanout"))
                WAE2E.AIQueryFanout.encode(m.aiQueryFanout, w.uint32(210).fork()).ldelim();
            if (m.memberLabel != null && Object.hasOwnProperty.call(m, "memberLabel"))
                WAE2E.MemberLabel.encode(m.memberLabel, w.uint32(218).fork()).ldelim();
            if (m.aiMediaCollectionMessage != null && Object.hasOwnProperty.call(m, "aiMediaCollectionMessage"))
                $root.WAAICommon.AIMediaCollectionMessage.encode(m.aiMediaCollectionMessage, w.uint32(226).fork()).ldelim();
            if (m.afterReadDuration != null && Object.hasOwnProperty.call(m, "afterReadDuration"))
                w.uint32(232).uint32(m.afterReadDuration);
            if (m.chatThemeSetting != null && Object.hasOwnProperty.call(m, "chatThemeSetting"))
                WAE2E.ChatThemeSetting.encode(m.chatThemeSetting, w.uint32(242).fork()).ldelim();
            return w;
        };

        ProtocolMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ProtocolMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.type = r.int32();
                        break;
                    }
                case 4: {
                        m.ephemeralExpiration = r.uint32();
                        break;
                    }
                case 5: {
                        m.ephemeralSettingTimestamp = r.int64();
                        break;
                    }
                case 6: {
                        m.historySyncNotification = WAE2E.HistorySyncNotification.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.appStateSyncKeyShare = WAE2E.AppStateSyncKeyShare.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.appStateSyncKeyRequest = WAE2E.AppStateSyncKeyRequest.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.initialSecurityNotificationSettingSync = WAE2E.InitialSecurityNotificationSettingSync.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.appStateFatalExceptionNotification = WAE2E.AppStateFatalExceptionNotification.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.disappearingMode = WAE2E.DisappearingMode.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 14: {
                        m.editedMessage = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.timestampMS = r.int64();
                        break;
                    }
                case 16: {
                        m.peerDataOperationRequestMessage = WAE2E.PeerDataOperationRequestMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 17: {
                        m.peerDataOperationRequestResponseMessage = WAE2E.PeerDataOperationRequestResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.botFeedbackMessage = $root.WAAICommon.BotFeedbackMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 19: {
                        m.invokerJID = r.string();
                        break;
                    }
                case 20: {
                        m.requestWelcomeMessageMetadata = WAE2E.RequestWelcomeMessageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 21: {
                        m.mediaNotifyMessage = WAE2E.MediaNotifyMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 22: {
                        m.cloudApiThreadControlNotification = WAE2E.CloudAPIThreadControlNotification.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 23: {
                        m.lidMigrationMappingSyncMessage = WAE2E.LIDMigrationMappingSyncMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 24: {
                        m.limitSharing = WACommon.LimitSharing.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 25: {
                        m.aiPsiMetadata = r.bytes();
                        break;
                    }
                case 26: {
                        m.aiQueryFanout = WAE2E.AIQueryFanout.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 27: {
                        m.memberLabel = WAE2E.MemberLabel.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 28: {
                        m.aiMediaCollectionMessage = $root.WAAICommon.AIMediaCollectionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 29: {
                        m.afterReadDuration = r.uint32();
                        break;
                    }
                case 30: {
                        m.chatThemeSetting = WAE2E.ChatThemeSetting.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ProtocolMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ProtocolMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ProtocolMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "REVOKE":
            case 0:
                m.type = 0;
                break;
            case "EPHEMERAL_SETTING":
            case 3:
                m.type = 3;
                break;
            case "EPHEMERAL_SYNC_RESPONSE":
            case 4:
                m.type = 4;
                break;
            case "HISTORY_SYNC_NOTIFICATION":
            case 5:
                m.type = 5;
                break;
            case "APP_STATE_SYNC_KEY_SHARE":
            case 6:
                m.type = 6;
                break;
            case "APP_STATE_SYNC_KEY_REQUEST":
            case 7:
                m.type = 7;
                break;
            case "MSG_FANOUT_BACKFILL_REQUEST":
            case 8:
                m.type = 8;
                break;
            case "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC":
            case 9:
                m.type = 9;
                break;
            case "APP_STATE_FATAL_EXCEPTION_NOTIFICATION":
            case 10:
                m.type = 10;
                break;
            case "SHARE_PHONE_NUMBER":
            case 11:
                m.type = 11;
                break;
            case "MESSAGE_EDIT":
            case 14:
                m.type = 14;
                break;
            case "PEER_DATA_OPERATION_REQUEST_MESSAGE":
            case 16:
                m.type = 16;
                break;
            case "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE":
            case 17:
                m.type = 17;
                break;
            case "REQUEST_WELCOME_MESSAGE":
            case 18:
                m.type = 18;
                break;
            case "BOT_FEEDBACK_MESSAGE":
            case 19:
                m.type = 19;
                break;
            case "MEDIA_NOTIFY_MESSAGE":
            case 20:
                m.type = 20;
                break;
            case "CLOUD_API_THREAD_CONTROL_NOTIFICATION":
            case 21:
                m.type = 21;
                break;
            case "LID_MIGRATION_MAPPING_SYNC":
            case 22:
                m.type = 22;
                break;
            case "REMINDER_MESSAGE":
            case 23:
                m.type = 23;
                break;
            case "BOT_MEMU_ONBOARDING_MESSAGE":
            case 24:
                m.type = 24;
                break;
            case "STATUS_MENTION_MESSAGE":
            case 25:
                m.type = 25;
                break;
            case "STOP_GENERATION_MESSAGE":
            case 26:
                m.type = 26;
                break;
            case "LIMIT_SHARING":
            case 27:
                m.type = 27;
                break;
            case "AI_PSI_METADATA":
            case 28:
                m.type = 28;
                break;
            case "AI_QUERY_FANOUT":
            case 29:
                m.type = 29;
                break;
            case "GROUP_MEMBER_LABEL_CHANGE":
            case 30:
                m.type = 30;
                break;
            case "AI_MEDIA_COLLECTION_MESSAGE":
            case 31:
                m.type = 31;
                break;
            case "MESSAGE_UNSCHEDULE":
            case 32:
                m.type = 32;
                break;
            case "CHAT_THEME_SETTING":
            case 34:
                m.type = 34;
                break;
            }
            if (d.ephemeralExpiration != null) {
                m.ephemeralExpiration = d.ephemeralExpiration >>> 0;
            }
            if (d.ephemeralSettingTimestamp != null) {
                if ($util.Long)
                    (m.ephemeralSettingTimestamp = $util.Long.fromValue(d.ephemeralSettingTimestamp)).unsigned = false;
                else if (typeof d.ephemeralSettingTimestamp === "string")
                    m.ephemeralSettingTimestamp = parseInt(d.ephemeralSettingTimestamp, 10);
                else if (typeof d.ephemeralSettingTimestamp === "number")
                    m.ephemeralSettingTimestamp = d.ephemeralSettingTimestamp;
                else if (typeof d.ephemeralSettingTimestamp === "object")
                    m.ephemeralSettingTimestamp = new $util.LongBits(d.ephemeralSettingTimestamp.low >>> 0, d.ephemeralSettingTimestamp.high >>> 0).toNumber();
            }
            if (d.historySyncNotification != null) {
                if (typeof d.historySyncNotification !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.historySyncNotification: object expected");
                m.historySyncNotification = WAE2E.HistorySyncNotification.fromObject(d.historySyncNotification, n + 1);
            }
            if (d.appStateSyncKeyShare != null) {
                if (typeof d.appStateSyncKeyShare !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.appStateSyncKeyShare: object expected");
                m.appStateSyncKeyShare = WAE2E.AppStateSyncKeyShare.fromObject(d.appStateSyncKeyShare, n + 1);
            }
            if (d.appStateSyncKeyRequest != null) {
                if (typeof d.appStateSyncKeyRequest !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.appStateSyncKeyRequest: object expected");
                m.appStateSyncKeyRequest = WAE2E.AppStateSyncKeyRequest.fromObject(d.appStateSyncKeyRequest, n + 1);
            }
            if (d.initialSecurityNotificationSettingSync != null) {
                if (typeof d.initialSecurityNotificationSettingSync !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.initialSecurityNotificationSettingSync: object expected");
                m.initialSecurityNotificationSettingSync = WAE2E.InitialSecurityNotificationSettingSync.fromObject(d.initialSecurityNotificationSettingSync, n + 1);
            }
            if (d.appStateFatalExceptionNotification != null) {
                if (typeof d.appStateFatalExceptionNotification !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.appStateFatalExceptionNotification: object expected");
                m.appStateFatalExceptionNotification = WAE2E.AppStateFatalExceptionNotification.fromObject(d.appStateFatalExceptionNotification, n + 1);
            }
            if (d.disappearingMode != null) {
                if (typeof d.disappearingMode !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.disappearingMode: object expected");
                m.disappearingMode = WAE2E.DisappearingMode.fromObject(d.disappearingMode, n + 1);
            }
            if (d.editedMessage != null) {
                if (typeof d.editedMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.editedMessage: object expected");
                m.editedMessage = WAE2E.Message.fromObject(d.editedMessage, n + 1);
            }
            if (d.timestampMS != null) {
                if ($util.Long)
                    (m.timestampMS = $util.Long.fromValue(d.timestampMS)).unsigned = false;
                else if (typeof d.timestampMS === "string")
                    m.timestampMS = parseInt(d.timestampMS, 10);
                else if (typeof d.timestampMS === "number")
                    m.timestampMS = d.timestampMS;
                else if (typeof d.timestampMS === "object")
                    m.timestampMS = new $util.LongBits(d.timestampMS.low >>> 0, d.timestampMS.high >>> 0).toNumber();
            }
            if (d.peerDataOperationRequestMessage != null) {
                if (typeof d.peerDataOperationRequestMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.peerDataOperationRequestMessage: object expected");
                m.peerDataOperationRequestMessage = WAE2E.PeerDataOperationRequestMessage.fromObject(d.peerDataOperationRequestMessage, n + 1);
            }
            if (d.peerDataOperationRequestResponseMessage != null) {
                if (typeof d.peerDataOperationRequestResponseMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.peerDataOperationRequestResponseMessage: object expected");
                m.peerDataOperationRequestResponseMessage = WAE2E.PeerDataOperationRequestResponseMessage.fromObject(d.peerDataOperationRequestResponseMessage, n + 1);
            }
            if (d.botFeedbackMessage != null) {
                if (typeof d.botFeedbackMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.botFeedbackMessage: object expected");
                m.botFeedbackMessage = $root.WAAICommon.BotFeedbackMessage.fromObject(d.botFeedbackMessage, n + 1);
            }
            if (d.invokerJID != null) {
                m.invokerJID = String(d.invokerJID);
            }
            if (d.requestWelcomeMessageMetadata != null) {
                if (typeof d.requestWelcomeMessageMetadata !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.requestWelcomeMessageMetadata: object expected");
                m.requestWelcomeMessageMetadata = WAE2E.RequestWelcomeMessageMetadata.fromObject(d.requestWelcomeMessageMetadata, n + 1);
            }
            if (d.mediaNotifyMessage != null) {
                if (typeof d.mediaNotifyMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.mediaNotifyMessage: object expected");
                m.mediaNotifyMessage = WAE2E.MediaNotifyMessage.fromObject(d.mediaNotifyMessage, n + 1);
            }
            if (d.cloudApiThreadControlNotification != null) {
                if (typeof d.cloudApiThreadControlNotification !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.cloudApiThreadControlNotification: object expected");
                m.cloudApiThreadControlNotification = WAE2E.CloudAPIThreadControlNotification.fromObject(d.cloudApiThreadControlNotification, n + 1);
            }
            if (d.lidMigrationMappingSyncMessage != null) {
                if (typeof d.lidMigrationMappingSyncMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.lidMigrationMappingSyncMessage: object expected");
                m.lidMigrationMappingSyncMessage = WAE2E.LIDMigrationMappingSyncMessage.fromObject(d.lidMigrationMappingSyncMessage, n + 1);
            }
            if (d.limitSharing != null) {
                if (typeof d.limitSharing !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.limitSharing: object expected");
                m.limitSharing = WACommon.LimitSharing.fromObject(d.limitSharing, n + 1);
            }
            if (d.aiPsiMetadata != null) {
                if (typeof d.aiPsiMetadata === "string")
                    $util.base64.decode(d.aiPsiMetadata, m.aiPsiMetadata = $util.newBuffer($util.base64.length(d.aiPsiMetadata)), 0);
                else if (d.aiPsiMetadata.length >= 0)
                    m.aiPsiMetadata = d.aiPsiMetadata;
            }
            if (d.aiQueryFanout != null) {
                if (typeof d.aiQueryFanout !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.aiQueryFanout: object expected");
                m.aiQueryFanout = WAE2E.AIQueryFanout.fromObject(d.aiQueryFanout, n + 1);
            }
            if (d.memberLabel != null) {
                if (typeof d.memberLabel !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.memberLabel: object expected");
                m.memberLabel = WAE2E.MemberLabel.fromObject(d.memberLabel, n + 1);
            }
            if (d.aiMediaCollectionMessage != null) {
                if (typeof d.aiMediaCollectionMessage !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.aiMediaCollectionMessage: object expected");
                m.aiMediaCollectionMessage = $root.WAAICommon.AIMediaCollectionMessage.fromObject(d.aiMediaCollectionMessage, n + 1);
            }
            if (d.afterReadDuration != null) {
                m.afterReadDuration = d.afterReadDuration >>> 0;
            }
            if (d.chatThemeSetting != null) {
                if (typeof d.chatThemeSetting !== "object")
                    throw TypeError(".WAE2E.ProtocolMessage.chatThemeSetting: object expected");
                m.chatThemeSetting = WAE2E.ChatThemeSetting.fromObject(d.chatThemeSetting, n + 1);
            }
            return m;
        };

        ProtocolMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.ProtocolMessage.Type[m.type] === undefined ? m.type : WAE2E.ProtocolMessage.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.ephemeralExpiration != null && m.hasOwnProperty("ephemeralExpiration")) {
                d.ephemeralExpiration = m.ephemeralExpiration;
                if (o.oneofs)
                    d._ephemeralExpiration = "ephemeralExpiration";
            }
            if (m.ephemeralSettingTimestamp != null && m.hasOwnProperty("ephemeralSettingTimestamp")) {
                if (typeof m.ephemeralSettingTimestamp === "number")
                    d.ephemeralSettingTimestamp = o.longs === String ? String(m.ephemeralSettingTimestamp) : m.ephemeralSettingTimestamp;
                else
                    d.ephemeralSettingTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.ephemeralSettingTimestamp) : o.longs === Number ? new $util.LongBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0).toNumber() : m.ephemeralSettingTimestamp;
                if (o.oneofs)
                    d._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
            }
            if (m.historySyncNotification != null && m.hasOwnProperty("historySyncNotification")) {
                d.historySyncNotification = WAE2E.HistorySyncNotification.toObject(m.historySyncNotification, o);
                if (o.oneofs)
                    d._historySyncNotification = "historySyncNotification";
            }
            if (m.appStateSyncKeyShare != null && m.hasOwnProperty("appStateSyncKeyShare")) {
                d.appStateSyncKeyShare = WAE2E.AppStateSyncKeyShare.toObject(m.appStateSyncKeyShare, o);
                if (o.oneofs)
                    d._appStateSyncKeyShare = "appStateSyncKeyShare";
            }
            if (m.appStateSyncKeyRequest != null && m.hasOwnProperty("appStateSyncKeyRequest")) {
                d.appStateSyncKeyRequest = WAE2E.AppStateSyncKeyRequest.toObject(m.appStateSyncKeyRequest, o);
                if (o.oneofs)
                    d._appStateSyncKeyRequest = "appStateSyncKeyRequest";
            }
            if (m.initialSecurityNotificationSettingSync != null && m.hasOwnProperty("initialSecurityNotificationSettingSync")) {
                d.initialSecurityNotificationSettingSync = WAE2E.InitialSecurityNotificationSettingSync.toObject(m.initialSecurityNotificationSettingSync, o);
                if (o.oneofs)
                    d._initialSecurityNotificationSettingSync = "initialSecurityNotificationSettingSync";
            }
            if (m.appStateFatalExceptionNotification != null && m.hasOwnProperty("appStateFatalExceptionNotification")) {
                d.appStateFatalExceptionNotification = WAE2E.AppStateFatalExceptionNotification.toObject(m.appStateFatalExceptionNotification, o);
                if (o.oneofs)
                    d._appStateFatalExceptionNotification = "appStateFatalExceptionNotification";
            }
            if (m.disappearingMode != null && m.hasOwnProperty("disappearingMode")) {
                d.disappearingMode = WAE2E.DisappearingMode.toObject(m.disappearingMode, o);
                if (o.oneofs)
                    d._disappearingMode = "disappearingMode";
            }
            if (m.editedMessage != null && m.hasOwnProperty("editedMessage")) {
                d.editedMessage = WAE2E.Message.toObject(m.editedMessage, o);
                if (o.oneofs)
                    d._editedMessage = "editedMessage";
            }
            if (m.timestampMS != null && m.hasOwnProperty("timestampMS")) {
                if (typeof m.timestampMS === "number")
                    d.timestampMS = o.longs === String ? String(m.timestampMS) : m.timestampMS;
                else
                    d.timestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.timestampMS) : o.longs === Number ? new $util.LongBits(m.timestampMS.low >>> 0, m.timestampMS.high >>> 0).toNumber() : m.timestampMS;
                if (o.oneofs)
                    d._timestampMS = "timestampMS";
            }
            if (m.peerDataOperationRequestMessage != null && m.hasOwnProperty("peerDataOperationRequestMessage")) {
                d.peerDataOperationRequestMessage = WAE2E.PeerDataOperationRequestMessage.toObject(m.peerDataOperationRequestMessage, o);
                if (o.oneofs)
                    d._peerDataOperationRequestMessage = "peerDataOperationRequestMessage";
            }
            if (m.peerDataOperationRequestResponseMessage != null && m.hasOwnProperty("peerDataOperationRequestResponseMessage")) {
                d.peerDataOperationRequestResponseMessage = WAE2E.PeerDataOperationRequestResponseMessage.toObject(m.peerDataOperationRequestResponseMessage, o);
                if (o.oneofs)
                    d._peerDataOperationRequestResponseMessage = "peerDataOperationRequestResponseMessage";
            }
            if (m.botFeedbackMessage != null && m.hasOwnProperty("botFeedbackMessage")) {
                d.botFeedbackMessage = $root.WAAICommon.BotFeedbackMessage.toObject(m.botFeedbackMessage, o);
                if (o.oneofs)
                    d._botFeedbackMessage = "botFeedbackMessage";
            }
            if (m.invokerJID != null && m.hasOwnProperty("invokerJID")) {
                d.invokerJID = m.invokerJID;
                if (o.oneofs)
                    d._invokerJID = "invokerJID";
            }
            if (m.requestWelcomeMessageMetadata != null && m.hasOwnProperty("requestWelcomeMessageMetadata")) {
                d.requestWelcomeMessageMetadata = WAE2E.RequestWelcomeMessageMetadata.toObject(m.requestWelcomeMessageMetadata, o);
                if (o.oneofs)
                    d._requestWelcomeMessageMetadata = "requestWelcomeMessageMetadata";
            }
            if (m.mediaNotifyMessage != null && m.hasOwnProperty("mediaNotifyMessage")) {
                d.mediaNotifyMessage = WAE2E.MediaNotifyMessage.toObject(m.mediaNotifyMessage, o);
                if (o.oneofs)
                    d._mediaNotifyMessage = "mediaNotifyMessage";
            }
            if (m.cloudApiThreadControlNotification != null && m.hasOwnProperty("cloudApiThreadControlNotification")) {
                d.cloudApiThreadControlNotification = WAE2E.CloudAPIThreadControlNotification.toObject(m.cloudApiThreadControlNotification, o);
                if (o.oneofs)
                    d._cloudApiThreadControlNotification = "cloudApiThreadControlNotification";
            }
            if (m.lidMigrationMappingSyncMessage != null && m.hasOwnProperty("lidMigrationMappingSyncMessage")) {
                d.lidMigrationMappingSyncMessage = WAE2E.LIDMigrationMappingSyncMessage.toObject(m.lidMigrationMappingSyncMessage, o);
                if (o.oneofs)
                    d._lidMigrationMappingSyncMessage = "lidMigrationMappingSyncMessage";
            }
            if (m.limitSharing != null && m.hasOwnProperty("limitSharing")) {
                d.limitSharing = WACommon.LimitSharing.toObject(m.limitSharing, o);
                if (o.oneofs)
                    d._limitSharing = "limitSharing";
            }
            if (m.aiPsiMetadata != null && m.hasOwnProperty("aiPsiMetadata")) {
                d.aiPsiMetadata = o.bytes === String ? $util.base64.encode(m.aiPsiMetadata, 0, m.aiPsiMetadata.length) : o.bytes === Array ? Array.prototype.slice.call(m.aiPsiMetadata) : m.aiPsiMetadata;
                if (o.oneofs)
                    d._aiPsiMetadata = "aiPsiMetadata";
            }
            if (m.aiQueryFanout != null && m.hasOwnProperty("aiQueryFanout")) {
                d.aiQueryFanout = WAE2E.AIQueryFanout.toObject(m.aiQueryFanout, o);
                if (o.oneofs)
                    d._aiQueryFanout = "aiQueryFanout";
            }
            if (m.memberLabel != null && m.hasOwnProperty("memberLabel")) {
                d.memberLabel = WAE2E.MemberLabel.toObject(m.memberLabel, o);
                if (o.oneofs)
                    d._memberLabel = "memberLabel";
            }
            if (m.aiMediaCollectionMessage != null && m.hasOwnProperty("aiMediaCollectionMessage")) {
                d.aiMediaCollectionMessage = $root.WAAICommon.AIMediaCollectionMessage.toObject(m.aiMediaCollectionMessage, o);
                if (o.oneofs)
                    d._aiMediaCollectionMessage = "aiMediaCollectionMessage";
            }
            if (m.afterReadDuration != null && m.hasOwnProperty("afterReadDuration")) {
                d.afterReadDuration = m.afterReadDuration;
                if (o.oneofs)
                    d._afterReadDuration = "afterReadDuration";
            }
            if (m.chatThemeSetting != null && m.hasOwnProperty("chatThemeSetting")) {
                d.chatThemeSetting = WAE2E.ChatThemeSetting.toObject(m.chatThemeSetting, o);
                if (o.oneofs)
                    d._chatThemeSetting = "chatThemeSetting";
            }
            return d;
        };

        ProtocolMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProtocolMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ProtocolMessage";
        };

        ProtocolMessage.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "REVOKE"] = 0;
            values[valuesById[3] = "EPHEMERAL_SETTING"] = 3;
            values[valuesById[4] = "EPHEMERAL_SYNC_RESPONSE"] = 4;
            values[valuesById[5] = "HISTORY_SYNC_NOTIFICATION"] = 5;
            values[valuesById[6] = "APP_STATE_SYNC_KEY_SHARE"] = 6;
            values[valuesById[7] = "APP_STATE_SYNC_KEY_REQUEST"] = 7;
            values[valuesById[8] = "MSG_FANOUT_BACKFILL_REQUEST"] = 8;
            values[valuesById[9] = "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC"] = 9;
            values[valuesById[10] = "APP_STATE_FATAL_EXCEPTION_NOTIFICATION"] = 10;
            values[valuesById[11] = "SHARE_PHONE_NUMBER"] = 11;
            values[valuesById[14] = "MESSAGE_EDIT"] = 14;
            values[valuesById[16] = "PEER_DATA_OPERATION_REQUEST_MESSAGE"] = 16;
            values[valuesById[17] = "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE"] = 17;
            values[valuesById[18] = "REQUEST_WELCOME_MESSAGE"] = 18;
            values[valuesById[19] = "BOT_FEEDBACK_MESSAGE"] = 19;
            values[valuesById[20] = "MEDIA_NOTIFY_MESSAGE"] = 20;
            values[valuesById[21] = "CLOUD_API_THREAD_CONTROL_NOTIFICATION"] = 21;
            values[valuesById[22] = "LID_MIGRATION_MAPPING_SYNC"] = 22;
            values[valuesById[23] = "REMINDER_MESSAGE"] = 23;
            values[valuesById[24] = "BOT_MEMU_ONBOARDING_MESSAGE"] = 24;
            values[valuesById[25] = "STATUS_MENTION_MESSAGE"] = 25;
            values[valuesById[26] = "STOP_GENERATION_MESSAGE"] = 26;
            values[valuesById[27] = "LIMIT_SHARING"] = 27;
            values[valuesById[28] = "AI_PSI_METADATA"] = 28;
            values[valuesById[29] = "AI_QUERY_FANOUT"] = 29;
            values[valuesById[30] = "GROUP_MEMBER_LABEL_CHANGE"] = 30;
            values[valuesById[31] = "AI_MEDIA_COLLECTION_MESSAGE"] = 31;
            values[valuesById[32] = "MESSAGE_UNSCHEDULE"] = 32;
            values[valuesById[34] = "CHAT_THEME_SETTING"] = 34;
            return values;
        })();

        return ProtocolMessage;
    })();

    WAE2E.CloudAPIThreadControlNotification = (function() {

        function CloudAPIThreadControlNotification(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CloudAPIThreadControlNotification.prototype.status = null;
        CloudAPIThreadControlNotification.prototype.senderNotificationTimestampMS = null;
        CloudAPIThreadControlNotification.prototype.consumerLid = null;
        CloudAPIThreadControlNotification.prototype.consumerPhoneNumber = null;
        CloudAPIThreadControlNotification.prototype.notificationContent = null;
        CloudAPIThreadControlNotification.prototype.shouldSuppressNotification = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_senderNotificationTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderNotificationTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerLid", {
            get: $util.oneOfGetter($oneOfFields = ["consumerLid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerPhoneNumber", {
            get: $util.oneOfGetter($oneOfFields = ["consumerPhoneNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_notificationContent", {
            get: $util.oneOfGetter($oneOfFields = ["notificationContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_shouldSuppressNotification", {
            get: $util.oneOfGetter($oneOfFields = ["shouldSuppressNotification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CloudAPIThreadControlNotification.create = function create(properties) {
            return new CloudAPIThreadControlNotification(properties);
        };

        CloudAPIThreadControlNotification.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                w.uint32(8).int32(m.status);
            if (m.senderNotificationTimestampMS != null && Object.hasOwnProperty.call(m, "senderNotificationTimestampMS"))
                w.uint32(16).int64(m.senderNotificationTimestampMS);
            if (m.consumerLid != null && Object.hasOwnProperty.call(m, "consumerLid"))
                w.uint32(26).string(m.consumerLid);
            if (m.consumerPhoneNumber != null && Object.hasOwnProperty.call(m, "consumerPhoneNumber"))
                w.uint32(34).string(m.consumerPhoneNumber);
            if (m.notificationContent != null && Object.hasOwnProperty.call(m, "notificationContent"))
                WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.encode(m.notificationContent, w.uint32(42).fork()).ldelim();
            if (m.shouldSuppressNotification != null && Object.hasOwnProperty.call(m, "shouldSuppressNotification"))
                w.uint32(48).bool(m.shouldSuppressNotification);
            return w;
        };

        CloudAPIThreadControlNotification.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CloudAPIThreadControlNotification();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.status = r.int32();
                        break;
                    }
                case 2: {
                        m.senderNotificationTimestampMS = r.int64();
                        break;
                    }
                case 3: {
                        m.consumerLid = r.string();
                        break;
                    }
                case 4: {
                        m.consumerPhoneNumber = r.string();
                        break;
                    }
                case 5: {
                        m.notificationContent = WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.shouldSuppressNotification = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CloudAPIThreadControlNotification.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.CloudAPIThreadControlNotification)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.CloudAPIThreadControlNotification();
            switch (d.status) {
            default:
                if (typeof d.status === "number") {
                    m.status = d.status;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.status = 0;
                break;
            case "CONTROL_PASSED":
            case 1:
                m.status = 1;
                break;
            case "CONTROL_TAKEN":
            case 2:
                m.status = 2;
                break;
            case "INFO":
            case 3:
                m.status = 3;
                break;
            }
            if (d.senderNotificationTimestampMS != null) {
                if ($util.Long)
                    (m.senderNotificationTimestampMS = $util.Long.fromValue(d.senderNotificationTimestampMS)).unsigned = false;
                else if (typeof d.senderNotificationTimestampMS === "string")
                    m.senderNotificationTimestampMS = parseInt(d.senderNotificationTimestampMS, 10);
                else if (typeof d.senderNotificationTimestampMS === "number")
                    m.senderNotificationTimestampMS = d.senderNotificationTimestampMS;
                else if (typeof d.senderNotificationTimestampMS === "object")
                    m.senderNotificationTimestampMS = new $util.LongBits(d.senderNotificationTimestampMS.low >>> 0, d.senderNotificationTimestampMS.high >>> 0).toNumber();
            }
            if (d.consumerLid != null) {
                m.consumerLid = String(d.consumerLid);
            }
            if (d.consumerPhoneNumber != null) {
                m.consumerPhoneNumber = String(d.consumerPhoneNumber);
            }
            if (d.notificationContent != null) {
                if (typeof d.notificationContent !== "object")
                    throw TypeError(".WAE2E.CloudAPIThreadControlNotification.notificationContent: object expected");
                m.notificationContent = WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.fromObject(d.notificationContent, n + 1);
            }
            if (d.shouldSuppressNotification != null) {
                m.shouldSuppressNotification = Boolean(d.shouldSuppressNotification);
            }
            return m;
        };

        CloudAPIThreadControlNotification.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.status != null && m.hasOwnProperty("status")) {
                d.status = o.enums === String ? WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControl[m.status] === undefined ? m.status : WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControl[m.status] : m.status;
                if (o.oneofs)
                    d._status = "status";
            }
            if (m.senderNotificationTimestampMS != null && m.hasOwnProperty("senderNotificationTimestampMS")) {
                if (typeof m.senderNotificationTimestampMS === "number")
                    d.senderNotificationTimestampMS = o.longs === String ? String(m.senderNotificationTimestampMS) : m.senderNotificationTimestampMS;
                else
                    d.senderNotificationTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderNotificationTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderNotificationTimestampMS.low >>> 0, m.senderNotificationTimestampMS.high >>> 0).toNumber() : m.senderNotificationTimestampMS;
                if (o.oneofs)
                    d._senderNotificationTimestampMS = "senderNotificationTimestampMS";
            }
            if (m.consumerLid != null && m.hasOwnProperty("consumerLid")) {
                d.consumerLid = m.consumerLid;
                if (o.oneofs)
                    d._consumerLid = "consumerLid";
            }
            if (m.consumerPhoneNumber != null && m.hasOwnProperty("consumerPhoneNumber")) {
                d.consumerPhoneNumber = m.consumerPhoneNumber;
                if (o.oneofs)
                    d._consumerPhoneNumber = "consumerPhoneNumber";
            }
            if (m.notificationContent != null && m.hasOwnProperty("notificationContent")) {
                d.notificationContent = WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.toObject(m.notificationContent, o);
                if (o.oneofs)
                    d._notificationContent = "notificationContent";
            }
            if (m.shouldSuppressNotification != null && m.hasOwnProperty("shouldSuppressNotification")) {
                d.shouldSuppressNotification = m.shouldSuppressNotification;
                if (o.oneofs)
                    d._shouldSuppressNotification = "shouldSuppressNotification";
            }
            return d;
        };

        CloudAPIThreadControlNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CloudAPIThreadControlNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.CloudAPIThreadControlNotification";
        };

        CloudAPIThreadControlNotification.CloudAPIThreadControl = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CONTROL_PASSED"] = 1;
            values[valuesById[2] = "CONTROL_TAKEN"] = 2;
            values[valuesById[3] = "INFO"] = 3;
            return values;
        })();

        CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent = (function() {

            function CloudAPIThreadControlNotificationContent(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CloudAPIThreadControlNotificationContent.prototype.handoffNotificationText = null;
            CloudAPIThreadControlNotificationContent.prototype.extraJSON = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_handoffNotificationText", {
                get: $util.oneOfGetter($oneOfFields = ["handoffNotificationText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_extraJSON", {
                get: $util.oneOfGetter($oneOfFields = ["extraJSON"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CloudAPIThreadControlNotificationContent.create = function create(properties) {
                return new CloudAPIThreadControlNotificationContent(properties);
            };

            CloudAPIThreadControlNotificationContent.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.handoffNotificationText != null && Object.hasOwnProperty.call(m, "handoffNotificationText"))
                    w.uint32(10).string(m.handoffNotificationText);
                if (m.extraJSON != null && Object.hasOwnProperty.call(m, "extraJSON"))
                    w.uint32(18).string(m.extraJSON);
                return w;
            };

            CloudAPIThreadControlNotificationContent.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.handoffNotificationText = r.string();
                            break;
                        }
                    case 2: {
                            m.extraJSON = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CloudAPIThreadControlNotificationContent.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent();
                if (d.handoffNotificationText != null) {
                    m.handoffNotificationText = String(d.handoffNotificationText);
                }
                if (d.extraJSON != null) {
                    m.extraJSON = String(d.extraJSON);
                }
                return m;
            };

            CloudAPIThreadControlNotificationContent.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.handoffNotificationText != null && m.hasOwnProperty("handoffNotificationText")) {
                    d.handoffNotificationText = m.handoffNotificationText;
                    if (o.oneofs)
                        d._handoffNotificationText = "handoffNotificationText";
                }
                if (m.extraJSON != null && m.hasOwnProperty("extraJSON")) {
                    d.extraJSON = m.extraJSON;
                    if (o.oneofs)
                        d._extraJSON = "extraJSON";
                }
                return d;
            };

            CloudAPIThreadControlNotificationContent.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CloudAPIThreadControlNotificationContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent";
            };

            return CloudAPIThreadControlNotificationContent;
        })();

        return CloudAPIThreadControlNotification;
    })();

    WAE2E.VideoMessage = (function() {

        function VideoMessage(p) {
            this.interactiveAnnotations = [];
            this.annotations = [];
            this.processedVideos = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        VideoMessage.prototype.URL = null;
        VideoMessage.prototype.mimetype = null;
        VideoMessage.prototype.fileSHA256 = null;
        VideoMessage.prototype.fileLength = null;
        VideoMessage.prototype.seconds = null;
        VideoMessage.prototype.mediaKey = null;
        VideoMessage.prototype.caption = null;
        VideoMessage.prototype.gifPlayback = null;
        VideoMessage.prototype.height = null;
        VideoMessage.prototype.width = null;
        VideoMessage.prototype.fileEncSHA256 = null;
        VideoMessage.prototype.interactiveAnnotations = $util.emptyArray;
        VideoMessage.prototype.directPath = null;
        VideoMessage.prototype.mediaKeyTimestamp = null;
        VideoMessage.prototype.JPEGThumbnail = null;
        VideoMessage.prototype.contextInfo = null;
        VideoMessage.prototype.streamingSidecar = null;
        VideoMessage.prototype.gifAttribution = null;
        VideoMessage.prototype.viewOnce = null;
        VideoMessage.prototype.thumbnailDirectPath = null;
        VideoMessage.prototype.thumbnailSHA256 = null;
        VideoMessage.prototype.thumbnailEncSHA256 = null;
        VideoMessage.prototype.staticURL = null;
        VideoMessage.prototype.annotations = $util.emptyArray;
        VideoMessage.prototype.accessibilityLabel = null;
        VideoMessage.prototype.processedVideos = $util.emptyArray;
        VideoMessage.prototype.externalShareFullVideoDurationInSeconds = null;
        VideoMessage.prototype.motionPhotoPresentationOffsetMS = null;
        VideoMessage.prototype.metadataURL = null;
        VideoMessage.prototype.videoSourceType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_seconds", {
            get: $util.oneOfGetter($oneOfFields = ["seconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_gifPlayback", {
            get: $util.oneOfGetter($oneOfFields = ["gifPlayback"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_streamingSidecar", {
            get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_gifAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["gifAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_viewOnce", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_staticURL", {
            get: $util.oneOfGetter($oneOfFields = ["staticURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_accessibilityLabel", {
            get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_externalShareFullVideoDurationInSeconds", {
            get: $util.oneOfGetter($oneOfFields = ["externalShareFullVideoDurationInSeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_motionPhotoPresentationOffsetMS", {
            get: $util.oneOfGetter($oneOfFields = ["motionPhotoPresentationOffsetMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_metadataURL", {
            get: $util.oneOfGetter($oneOfFields = ["metadataURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoMessage.prototype, "_videoSourceType", {
            get: $util.oneOfGetter($oneOfFields = ["videoSourceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        VideoMessage.create = function create(properties) {
            return new VideoMessage(properties);
        };

        VideoMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(18).string(m.mimetype);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(26).bytes(m.fileSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(32).uint64(m.fileLength);
            if (m.seconds != null && Object.hasOwnProperty.call(m, "seconds"))
                w.uint32(40).uint32(m.seconds);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(50).bytes(m.mediaKey);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(58).string(m.caption);
            if (m.gifPlayback != null && Object.hasOwnProperty.call(m, "gifPlayback"))
                w.uint32(64).bool(m.gifPlayback);
            if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                w.uint32(72).uint32(m.height);
            if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                w.uint32(80).uint32(m.width);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(90).bytes(m.fileEncSHA256);
            if (m.interactiveAnnotations != null && m.interactiveAnnotations.length) {
                for (var i = 0; i < m.interactiveAnnotations.length; ++i)
                    WAE2E.InteractiveAnnotation.encode(m.interactiveAnnotations[i], w.uint32(98).fork()).ldelim();
            }
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(106).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(112).int64(m.mediaKeyTimestamp);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.streamingSidecar != null && Object.hasOwnProperty.call(m, "streamingSidecar"))
                w.uint32(146).bytes(m.streamingSidecar);
            if (m.gifAttribution != null && Object.hasOwnProperty.call(m, "gifAttribution"))
                w.uint32(152).int32(m.gifAttribution);
            if (m.viewOnce != null && Object.hasOwnProperty.call(m, "viewOnce"))
                w.uint32(160).bool(m.viewOnce);
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(170).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(178).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(186).bytes(m.thumbnailEncSHA256);
            if (m.staticURL != null && Object.hasOwnProperty.call(m, "staticURL"))
                w.uint32(194).string(m.staticURL);
            if (m.annotations != null && m.annotations.length) {
                for (var i = 0; i < m.annotations.length; ++i)
                    WAE2E.InteractiveAnnotation.encode(m.annotations[i], w.uint32(202).fork()).ldelim();
            }
            if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                w.uint32(210).string(m.accessibilityLabel);
            if (m.processedVideos != null && m.processedVideos.length) {
                for (var i = 0; i < m.processedVideos.length; ++i)
                    WAE2E.ProcessedVideo.encode(m.processedVideos[i], w.uint32(218).fork()).ldelim();
            }
            if (m.externalShareFullVideoDurationInSeconds != null && Object.hasOwnProperty.call(m, "externalShareFullVideoDurationInSeconds"))
                w.uint32(224).uint32(m.externalShareFullVideoDurationInSeconds);
            if (m.motionPhotoPresentationOffsetMS != null && Object.hasOwnProperty.call(m, "motionPhotoPresentationOffsetMS"))
                w.uint32(232).uint64(m.motionPhotoPresentationOffsetMS);
            if (m.metadataURL != null && Object.hasOwnProperty.call(m, "metadataURL"))
                w.uint32(242).string(m.metadataURL);
            if (m.videoSourceType != null && Object.hasOwnProperty.call(m, "videoSourceType"))
                w.uint32(248).int32(m.videoSourceType);
            return w;
        };

        VideoMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.VideoMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.mimetype = r.string();
                        break;
                    }
                case 3: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 4: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 5: {
                        m.seconds = r.uint32();
                        break;
                    }
                case 6: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 7: {
                        m.caption = r.string();
                        break;
                    }
                case 8: {
                        m.gifPlayback = r.bool();
                        break;
                    }
                case 9: {
                        m.height = r.uint32();
                        break;
                    }
                case 10: {
                        m.width = r.uint32();
                        break;
                    }
                case 11: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 12: {
                        if (!(m.interactiveAnnotations && m.interactiveAnnotations.length))
                            m.interactiveAnnotations = [];
                        m.interactiveAnnotations.push(WAE2E.InteractiveAnnotation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 13: {
                        m.directPath = r.string();
                        break;
                    }
                case 14: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.streamingSidecar = r.bytes();
                        break;
                    }
                case 19: {
                        m.gifAttribution = r.int32();
                        break;
                    }
                case 20: {
                        m.viewOnce = r.bool();
                        break;
                    }
                case 21: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 22: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 23: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 24: {
                        m.staticURL = r.string();
                        break;
                    }
                case 25: {
                        if (!(m.annotations && m.annotations.length))
                            m.annotations = [];
                        m.annotations.push(WAE2E.InteractiveAnnotation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 26: {
                        m.accessibilityLabel = r.string();
                        break;
                    }
                case 27: {
                        if (!(m.processedVideos && m.processedVideos.length))
                            m.processedVideos = [];
                        m.processedVideos.push(WAE2E.ProcessedVideo.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 28: {
                        m.externalShareFullVideoDurationInSeconds = r.uint32();
                        break;
                    }
                case 29: {
                        m.motionPhotoPresentationOffsetMS = r.uint64();
                        break;
                    }
                case 30: {
                        m.metadataURL = r.string();
                        break;
                    }
                case 31: {
                        m.videoSourceType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        VideoMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.VideoMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.VideoMessage();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.seconds != null) {
                m.seconds = d.seconds >>> 0;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.gifPlayback != null) {
                m.gifPlayback = Boolean(d.gifPlayback);
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.interactiveAnnotations) {
                if (!Array.isArray(d.interactiveAnnotations))
                    throw TypeError(".WAE2E.VideoMessage.interactiveAnnotations: array expected");
                m.interactiveAnnotations = [];
                for (var i = 0; i < d.interactiveAnnotations.length; ++i) {
                    if (typeof d.interactiveAnnotations[i] !== "object")
                        throw TypeError(".WAE2E.VideoMessage.interactiveAnnotations: object expected");
                    m.interactiveAnnotations[i] = WAE2E.InteractiveAnnotation.fromObject(d.interactiveAnnotations[i], n + 1);
                }
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.VideoMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.streamingSidecar != null) {
                if (typeof d.streamingSidecar === "string")
                    $util.base64.decode(d.streamingSidecar, m.streamingSidecar = $util.newBuffer($util.base64.length(d.streamingSidecar)), 0);
                else if (d.streamingSidecar.length >= 0)
                    m.streamingSidecar = d.streamingSidecar;
            }
            switch (d.gifAttribution) {
            default:
                if (typeof d.gifAttribution === "number") {
                    m.gifAttribution = d.gifAttribution;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.gifAttribution = 0;
                break;
            case "GIPHY":
            case 1:
                m.gifAttribution = 1;
                break;
            case "TENOR":
            case 2:
                m.gifAttribution = 2;
                break;
            case "KLIPY":
            case 3:
                m.gifAttribution = 3;
                break;
            }
            if (d.viewOnce != null) {
                m.viewOnce = Boolean(d.viewOnce);
            }
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.staticURL != null) {
                m.staticURL = String(d.staticURL);
            }
            if (d.annotations) {
                if (!Array.isArray(d.annotations))
                    throw TypeError(".WAE2E.VideoMessage.annotations: array expected");
                m.annotations = [];
                for (var i = 0; i < d.annotations.length; ++i) {
                    if (typeof d.annotations[i] !== "object")
                        throw TypeError(".WAE2E.VideoMessage.annotations: object expected");
                    m.annotations[i] = WAE2E.InteractiveAnnotation.fromObject(d.annotations[i], n + 1);
                }
            }
            if (d.accessibilityLabel != null) {
                m.accessibilityLabel = String(d.accessibilityLabel);
            }
            if (d.processedVideos) {
                if (!Array.isArray(d.processedVideos))
                    throw TypeError(".WAE2E.VideoMessage.processedVideos: array expected");
                m.processedVideos = [];
                for (var i = 0; i < d.processedVideos.length; ++i) {
                    if (typeof d.processedVideos[i] !== "object")
                        throw TypeError(".WAE2E.VideoMessage.processedVideos: object expected");
                    m.processedVideos[i] = WAE2E.ProcessedVideo.fromObject(d.processedVideos[i], n + 1);
                }
            }
            if (d.externalShareFullVideoDurationInSeconds != null) {
                m.externalShareFullVideoDurationInSeconds = d.externalShareFullVideoDurationInSeconds >>> 0;
            }
            if (d.motionPhotoPresentationOffsetMS != null) {
                if ($util.Long)
                    (m.motionPhotoPresentationOffsetMS = $util.Long.fromValue(d.motionPhotoPresentationOffsetMS)).unsigned = true;
                else if (typeof d.motionPhotoPresentationOffsetMS === "string")
                    m.motionPhotoPresentationOffsetMS = parseInt(d.motionPhotoPresentationOffsetMS, 10);
                else if (typeof d.motionPhotoPresentationOffsetMS === "number")
                    m.motionPhotoPresentationOffsetMS = d.motionPhotoPresentationOffsetMS;
                else if (typeof d.motionPhotoPresentationOffsetMS === "object")
                    m.motionPhotoPresentationOffsetMS = new $util.LongBits(d.motionPhotoPresentationOffsetMS.low >>> 0, d.motionPhotoPresentationOffsetMS.high >>> 0).toNumber(true);
            }
            if (d.metadataURL != null) {
                m.metadataURL = String(d.metadataURL);
            }
            switch (d.videoSourceType) {
            default:
                if (typeof d.videoSourceType === "number") {
                    m.videoSourceType = d.videoSourceType;
                    break;
                }
                break;
            case "USER_VIDEO":
            case 0:
                m.videoSourceType = 0;
                break;
            case "AI_GENERATED":
            case 1:
                m.videoSourceType = 1;
                break;
            }
            return m;
        };

        VideoMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.interactiveAnnotations = [];
                d.annotations = [];
                d.processedVideos = [];
            }
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.seconds != null && m.hasOwnProperty("seconds")) {
                d.seconds = m.seconds;
                if (o.oneofs)
                    d._seconds = "seconds";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.gifPlayback != null && m.hasOwnProperty("gifPlayback")) {
                d.gifPlayback = m.gifPlayback;
                if (o.oneofs)
                    d._gifPlayback = "gifPlayback";
            }
            if (m.height != null && m.hasOwnProperty("height")) {
                d.height = m.height;
                if (o.oneofs)
                    d._height = "height";
            }
            if (m.width != null && m.hasOwnProperty("width")) {
                d.width = m.width;
                if (o.oneofs)
                    d._width = "width";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.interactiveAnnotations && m.interactiveAnnotations.length) {
                d.interactiveAnnotations = [];
                for (var j = 0; j < m.interactiveAnnotations.length; ++j) {
                    d.interactiveAnnotations[j] = WAE2E.InteractiveAnnotation.toObject(m.interactiveAnnotations[j], o);
                }
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.streamingSidecar != null && m.hasOwnProperty("streamingSidecar")) {
                d.streamingSidecar = o.bytes === String ? $util.base64.encode(m.streamingSidecar, 0, m.streamingSidecar.length) : o.bytes === Array ? Array.prototype.slice.call(m.streamingSidecar) : m.streamingSidecar;
                if (o.oneofs)
                    d._streamingSidecar = "streamingSidecar";
            }
            if (m.gifAttribution != null && m.hasOwnProperty("gifAttribution")) {
                d.gifAttribution = o.enums === String ? WAE2E.VideoMessage.Attribution[m.gifAttribution] === undefined ? m.gifAttribution : WAE2E.VideoMessage.Attribution[m.gifAttribution] : m.gifAttribution;
                if (o.oneofs)
                    d._gifAttribution = "gifAttribution";
            }
            if (m.viewOnce != null && m.hasOwnProperty("viewOnce")) {
                d.viewOnce = m.viewOnce;
                if (o.oneofs)
                    d._viewOnce = "viewOnce";
            }
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.staticURL != null && m.hasOwnProperty("staticURL")) {
                d.staticURL = m.staticURL;
                if (o.oneofs)
                    d._staticURL = "staticURL";
            }
            if (m.annotations && m.annotations.length) {
                d.annotations = [];
                for (var j = 0; j < m.annotations.length; ++j) {
                    d.annotations[j] = WAE2E.InteractiveAnnotation.toObject(m.annotations[j], o);
                }
            }
            if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                d.accessibilityLabel = m.accessibilityLabel;
                if (o.oneofs)
                    d._accessibilityLabel = "accessibilityLabel";
            }
            if (m.processedVideos && m.processedVideos.length) {
                d.processedVideos = [];
                for (var j = 0; j < m.processedVideos.length; ++j) {
                    d.processedVideos[j] = WAE2E.ProcessedVideo.toObject(m.processedVideos[j], o);
                }
            }
            if (m.externalShareFullVideoDurationInSeconds != null && m.hasOwnProperty("externalShareFullVideoDurationInSeconds")) {
                d.externalShareFullVideoDurationInSeconds = m.externalShareFullVideoDurationInSeconds;
                if (o.oneofs)
                    d._externalShareFullVideoDurationInSeconds = "externalShareFullVideoDurationInSeconds";
            }
            if (m.motionPhotoPresentationOffsetMS != null && m.hasOwnProperty("motionPhotoPresentationOffsetMS")) {
                if (typeof m.motionPhotoPresentationOffsetMS === "number")
                    d.motionPhotoPresentationOffsetMS = o.longs === String ? String(m.motionPhotoPresentationOffsetMS) : m.motionPhotoPresentationOffsetMS;
                else
                    d.motionPhotoPresentationOffsetMS = o.longs === String ? $util.Long.prototype.toString.call(m.motionPhotoPresentationOffsetMS) : o.longs === Number ? new $util.LongBits(m.motionPhotoPresentationOffsetMS.low >>> 0, m.motionPhotoPresentationOffsetMS.high >>> 0).toNumber(true) : m.motionPhotoPresentationOffsetMS;
                if (o.oneofs)
                    d._motionPhotoPresentationOffsetMS = "motionPhotoPresentationOffsetMS";
            }
            if (m.metadataURL != null && m.hasOwnProperty("metadataURL")) {
                d.metadataURL = m.metadataURL;
                if (o.oneofs)
                    d._metadataURL = "metadataURL";
            }
            if (m.videoSourceType != null && m.hasOwnProperty("videoSourceType")) {
                d.videoSourceType = o.enums === String ? WAE2E.VideoMessage.VideoSourceType[m.videoSourceType] === undefined ? m.videoSourceType : WAE2E.VideoMessage.VideoSourceType[m.videoSourceType] : m.videoSourceType;
                if (o.oneofs)
                    d._videoSourceType = "videoSourceType";
            }
            return d;
        };

        VideoMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VideoMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.VideoMessage";
        };

        VideoMessage.VideoSourceType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "USER_VIDEO"] = 0;
            values[valuesById[1] = "AI_GENERATED"] = 1;
            return values;
        })();

        VideoMessage.Attribution = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "GIPHY"] = 1;
            values[valuesById[2] = "TENOR"] = 2;
            values[valuesById[3] = "KLIPY"] = 3;
            return values;
        })();

        return VideoMessage;
    })();

    WAE2E.ExtendedTextMessage = (function() {

        function ExtendedTextMessage(p) {
            this.endCardTiles = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ExtendedTextMessage.prototype.text = null;
        ExtendedTextMessage.prototype.matchedText = null;
        ExtendedTextMessage.prototype.description = null;
        ExtendedTextMessage.prototype.title = null;
        ExtendedTextMessage.prototype.textArgb = null;
        ExtendedTextMessage.prototype.backgroundArgb = null;
        ExtendedTextMessage.prototype.font = null;
        ExtendedTextMessage.prototype.previewType = null;
        ExtendedTextMessage.prototype.JPEGThumbnail = null;
        ExtendedTextMessage.prototype.contextInfo = null;
        ExtendedTextMessage.prototype.doNotPlayInline = null;
        ExtendedTextMessage.prototype.thumbnailDirectPath = null;
        ExtendedTextMessage.prototype.thumbnailSHA256 = null;
        ExtendedTextMessage.prototype.thumbnailEncSHA256 = null;
        ExtendedTextMessage.prototype.mediaKey = null;
        ExtendedTextMessage.prototype.mediaKeyTimestamp = null;
        ExtendedTextMessage.prototype.thumbnailHeight = null;
        ExtendedTextMessage.prototype.thumbnailWidth = null;
        ExtendedTextMessage.prototype.inviteLinkGroupType = null;
        ExtendedTextMessage.prototype.inviteLinkParentGroupSubjectV2 = null;
        ExtendedTextMessage.prototype.inviteLinkParentGroupThumbnailV2 = null;
        ExtendedTextMessage.prototype.inviteLinkGroupTypeV2 = null;
        ExtendedTextMessage.prototype.viewOnce = null;
        ExtendedTextMessage.prototype.videoHeight = null;
        ExtendedTextMessage.prototype.videoWidth = null;
        ExtendedTextMessage.prototype.faviconMMSMetadata = null;
        ExtendedTextMessage.prototype.linkPreviewMetadata = null;
        ExtendedTextMessage.prototype.paymentLinkMetadata = null;
        ExtendedTextMessage.prototype.endCardTiles = $util.emptyArray;
        ExtendedTextMessage.prototype.videoContentURL = null;
        ExtendedTextMessage.prototype.musicMetadata = null;
        ExtendedTextMessage.prototype.paymentExtendedMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_matchedText", {
            get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_textArgb", {
            get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_backgroundArgb", {
            get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_font", {
            get: $util.oneOfGetter($oneOfFields = ["font"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_previewType", {
            get: $util.oneOfGetter($oneOfFields = ["previewType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_doNotPlayInline", {
            get: $util.oneOfGetter($oneOfFields = ["doNotPlayInline"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailHeight", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailWidth", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupType", {
            get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupSubjectV2", {
            get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupSubjectV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupThumbnailV2", {
            get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupThumbnailV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupTypeV2", {
            get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupTypeV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_viewOnce", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_videoHeight", {
            get: $util.oneOfGetter($oneOfFields = ["videoHeight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_videoWidth", {
            get: $util.oneOfGetter($oneOfFields = ["videoWidth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_faviconMMSMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["faviconMMSMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_linkPreviewMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["linkPreviewMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_paymentLinkMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_videoContentURL", {
            get: $util.oneOfGetter($oneOfFields = ["videoContentURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_musicMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ExtendedTextMessage.prototype, "_paymentExtendedMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["paymentExtendedMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ExtendedTextMessage.create = function create(properties) {
            return new ExtendedTextMessage(properties);
        };

        ExtendedTextMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(10).string(m.text);
            if (m.matchedText != null && Object.hasOwnProperty.call(m, "matchedText"))
                w.uint32(18).string(m.matchedText);
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(42).string(m.description);
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(50).string(m.title);
            if (m.textArgb != null && Object.hasOwnProperty.call(m, "textArgb"))
                w.uint32(61).fixed32(m.textArgb);
            if (m.backgroundArgb != null && Object.hasOwnProperty.call(m, "backgroundArgb"))
                w.uint32(69).fixed32(m.backgroundArgb);
            if (m.font != null && Object.hasOwnProperty.call(m, "font"))
                w.uint32(72).int32(m.font);
            if (m.previewType != null && Object.hasOwnProperty.call(m, "previewType"))
                w.uint32(80).int32(m.previewType);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.doNotPlayInline != null && Object.hasOwnProperty.call(m, "doNotPlayInline"))
                w.uint32(144).bool(m.doNotPlayInline);
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(154).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(162).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(170).bytes(m.thumbnailEncSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(178).bytes(m.mediaKey);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(184).int64(m.mediaKeyTimestamp);
            if (m.thumbnailHeight != null && Object.hasOwnProperty.call(m, "thumbnailHeight"))
                w.uint32(192).uint32(m.thumbnailHeight);
            if (m.thumbnailWidth != null && Object.hasOwnProperty.call(m, "thumbnailWidth"))
                w.uint32(200).uint32(m.thumbnailWidth);
            if (m.inviteLinkGroupType != null && Object.hasOwnProperty.call(m, "inviteLinkGroupType"))
                w.uint32(208).int32(m.inviteLinkGroupType);
            if (m.inviteLinkParentGroupSubjectV2 != null && Object.hasOwnProperty.call(m, "inviteLinkParentGroupSubjectV2"))
                w.uint32(218).string(m.inviteLinkParentGroupSubjectV2);
            if (m.inviteLinkParentGroupThumbnailV2 != null && Object.hasOwnProperty.call(m, "inviteLinkParentGroupThumbnailV2"))
                w.uint32(226).bytes(m.inviteLinkParentGroupThumbnailV2);
            if (m.inviteLinkGroupTypeV2 != null && Object.hasOwnProperty.call(m, "inviteLinkGroupTypeV2"))
                w.uint32(232).int32(m.inviteLinkGroupTypeV2);
            if (m.viewOnce != null && Object.hasOwnProperty.call(m, "viewOnce"))
                w.uint32(240).bool(m.viewOnce);
            if (m.videoHeight != null && Object.hasOwnProperty.call(m, "videoHeight"))
                w.uint32(248).uint32(m.videoHeight);
            if (m.videoWidth != null && Object.hasOwnProperty.call(m, "videoWidth"))
                w.uint32(256).uint32(m.videoWidth);
            if (m.faviconMMSMetadata != null && Object.hasOwnProperty.call(m, "faviconMMSMetadata"))
                WAE2E.MMSThumbnailMetadata.encode(m.faviconMMSMetadata, w.uint32(266).fork()).ldelim();
            if (m.linkPreviewMetadata != null && Object.hasOwnProperty.call(m, "linkPreviewMetadata"))
                WAE2E.LinkPreviewMetadata.encode(m.linkPreviewMetadata, w.uint32(274).fork()).ldelim();
            if (m.paymentLinkMetadata != null && Object.hasOwnProperty.call(m, "paymentLinkMetadata"))
                WAE2E.PaymentLinkMetadata.encode(m.paymentLinkMetadata, w.uint32(282).fork()).ldelim();
            if (m.endCardTiles != null && m.endCardTiles.length) {
                for (var i = 0; i < m.endCardTiles.length; ++i)
                    WAE2E.VideoEndCard.encode(m.endCardTiles[i], w.uint32(290).fork()).ldelim();
            }
            if (m.videoContentURL != null && Object.hasOwnProperty.call(m, "videoContentURL"))
                w.uint32(298).string(m.videoContentURL);
            if (m.musicMetadata != null && Object.hasOwnProperty.call(m, "musicMetadata"))
                WAE2E.EmbeddedMusic.encode(m.musicMetadata, w.uint32(306).fork()).ldelim();
            if (m.paymentExtendedMetadata != null && Object.hasOwnProperty.call(m, "paymentExtendedMetadata"))
                WAE2E.PaymentExtendedMetadata.encode(m.paymentExtendedMetadata, w.uint32(314).fork()).ldelim();
            return w;
        };

        ExtendedTextMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ExtendedTextMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.text = r.string();
                        break;
                    }
                case 2: {
                        m.matchedText = r.string();
                        break;
                    }
                case 5: {
                        m.description = r.string();
                        break;
                    }
                case 6: {
                        m.title = r.string();
                        break;
                    }
                case 7: {
                        m.textArgb = r.fixed32();
                        break;
                    }
                case 8: {
                        m.backgroundArgb = r.fixed32();
                        break;
                    }
                case 9: {
                        m.font = r.int32();
                        break;
                    }
                case 10: {
                        m.previewType = r.int32();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.doNotPlayInline = r.bool();
                        break;
                    }
                case 19: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 20: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 21: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 22: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 23: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 24: {
                        m.thumbnailHeight = r.uint32();
                        break;
                    }
                case 25: {
                        m.thumbnailWidth = r.uint32();
                        break;
                    }
                case 26: {
                        m.inviteLinkGroupType = r.int32();
                        break;
                    }
                case 27: {
                        m.inviteLinkParentGroupSubjectV2 = r.string();
                        break;
                    }
                case 28: {
                        m.inviteLinkParentGroupThumbnailV2 = r.bytes();
                        break;
                    }
                case 29: {
                        m.inviteLinkGroupTypeV2 = r.int32();
                        break;
                    }
                case 30: {
                        m.viewOnce = r.bool();
                        break;
                    }
                case 31: {
                        m.videoHeight = r.uint32();
                        break;
                    }
                case 32: {
                        m.videoWidth = r.uint32();
                        break;
                    }
                case 33: {
                        m.faviconMMSMetadata = WAE2E.MMSThumbnailMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 34: {
                        m.linkPreviewMetadata = WAE2E.LinkPreviewMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 35: {
                        m.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 36: {
                        if (!(m.endCardTiles && m.endCardTiles.length))
                            m.endCardTiles = [];
                        m.endCardTiles.push(WAE2E.VideoEndCard.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 37: {
                        m.videoContentURL = r.string();
                        break;
                    }
                case 38: {
                        m.musicMetadata = WAE2E.EmbeddedMusic.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 39: {
                        m.paymentExtendedMetadata = WAE2E.PaymentExtendedMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ExtendedTextMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ExtendedTextMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ExtendedTextMessage();
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.matchedText != null) {
                m.matchedText = String(d.matchedText);
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.textArgb != null) {
                m.textArgb = d.textArgb >>> 0;
            }
            if (d.backgroundArgb != null) {
                m.backgroundArgb = d.backgroundArgb >>> 0;
            }
            switch (d.font) {
            default:
                if (typeof d.font === "number") {
                    m.font = d.font;
                    break;
                }
                break;
            case "SYSTEM":
            case 0:
                m.font = 0;
                break;
            case "SYSTEM_TEXT":
            case 1:
                m.font = 1;
                break;
            case "FB_SCRIPT":
            case 2:
                m.font = 2;
                break;
            case "SYSTEM_BOLD":
            case 6:
                m.font = 6;
                break;
            case "MORNINGBREEZE_REGULAR":
            case 7:
                m.font = 7;
                break;
            case "CALISTOGA_REGULAR":
            case 8:
                m.font = 8;
                break;
            case "EXO2_EXTRABOLD":
            case 9:
                m.font = 9;
                break;
            case "COURIERPRIME_BOLD":
            case 10:
                m.font = 10;
                break;
            }
            switch (d.previewType) {
            default:
                if (typeof d.previewType === "number") {
                    m.previewType = d.previewType;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.previewType = 0;
                break;
            case "VIDEO":
            case 1:
                m.previewType = 1;
                break;
            case "PLACEHOLDER":
            case 4:
                m.previewType = 4;
                break;
            case "IMAGE":
            case 5:
                m.previewType = 5;
                break;
            case "PAYMENT_LINKS":
            case 6:
                m.previewType = 6;
                break;
            case "PROFILE":
            case 7:
                m.previewType = 7;
                break;
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.doNotPlayInline != null) {
                m.doNotPlayInline = Boolean(d.doNotPlayInline);
            }
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.thumbnailHeight != null) {
                m.thumbnailHeight = d.thumbnailHeight >>> 0;
            }
            if (d.thumbnailWidth != null) {
                m.thumbnailWidth = d.thumbnailWidth >>> 0;
            }
            switch (d.inviteLinkGroupType) {
            default:
                if (typeof d.inviteLinkGroupType === "number") {
                    m.inviteLinkGroupType = d.inviteLinkGroupType;
                    break;
                }
                break;
            case "DEFAULT":
            case 0:
                m.inviteLinkGroupType = 0;
                break;
            case "PARENT":
            case 1:
                m.inviteLinkGroupType = 1;
                break;
            case "SUB":
            case 2:
                m.inviteLinkGroupType = 2;
                break;
            case "DEFAULT_SUB":
            case 3:
                m.inviteLinkGroupType = 3;
                break;
            }
            if (d.inviteLinkParentGroupSubjectV2 != null) {
                m.inviteLinkParentGroupSubjectV2 = String(d.inviteLinkParentGroupSubjectV2);
            }
            if (d.inviteLinkParentGroupThumbnailV2 != null) {
                if (typeof d.inviteLinkParentGroupThumbnailV2 === "string")
                    $util.base64.decode(d.inviteLinkParentGroupThumbnailV2, m.inviteLinkParentGroupThumbnailV2 = $util.newBuffer($util.base64.length(d.inviteLinkParentGroupThumbnailV2)), 0);
                else if (d.inviteLinkParentGroupThumbnailV2.length >= 0)
                    m.inviteLinkParentGroupThumbnailV2 = d.inviteLinkParentGroupThumbnailV2;
            }
            switch (d.inviteLinkGroupTypeV2) {
            default:
                if (typeof d.inviteLinkGroupTypeV2 === "number") {
                    m.inviteLinkGroupTypeV2 = d.inviteLinkGroupTypeV2;
                    break;
                }
                break;
            case "DEFAULT":
            case 0:
                m.inviteLinkGroupTypeV2 = 0;
                break;
            case "PARENT":
            case 1:
                m.inviteLinkGroupTypeV2 = 1;
                break;
            case "SUB":
            case 2:
                m.inviteLinkGroupTypeV2 = 2;
                break;
            case "DEFAULT_SUB":
            case 3:
                m.inviteLinkGroupTypeV2 = 3;
                break;
            }
            if (d.viewOnce != null) {
                m.viewOnce = Boolean(d.viewOnce);
            }
            if (d.videoHeight != null) {
                m.videoHeight = d.videoHeight >>> 0;
            }
            if (d.videoWidth != null) {
                m.videoWidth = d.videoWidth >>> 0;
            }
            if (d.faviconMMSMetadata != null) {
                if (typeof d.faviconMMSMetadata !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.faviconMMSMetadata: object expected");
                m.faviconMMSMetadata = WAE2E.MMSThumbnailMetadata.fromObject(d.faviconMMSMetadata, n + 1);
            }
            if (d.linkPreviewMetadata != null) {
                if (typeof d.linkPreviewMetadata !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.linkPreviewMetadata: object expected");
                m.linkPreviewMetadata = WAE2E.LinkPreviewMetadata.fromObject(d.linkPreviewMetadata, n + 1);
            }
            if (d.paymentLinkMetadata != null) {
                if (typeof d.paymentLinkMetadata !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.paymentLinkMetadata: object expected");
                m.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.fromObject(d.paymentLinkMetadata, n + 1);
            }
            if (d.endCardTiles) {
                if (!Array.isArray(d.endCardTiles))
                    throw TypeError(".WAE2E.ExtendedTextMessage.endCardTiles: array expected");
                m.endCardTiles = [];
                for (var i = 0; i < d.endCardTiles.length; ++i) {
                    if (typeof d.endCardTiles[i] !== "object")
                        throw TypeError(".WAE2E.ExtendedTextMessage.endCardTiles: object expected");
                    m.endCardTiles[i] = WAE2E.VideoEndCard.fromObject(d.endCardTiles[i], n + 1);
                }
            }
            if (d.videoContentURL != null) {
                m.videoContentURL = String(d.videoContentURL);
            }
            if (d.musicMetadata != null) {
                if (typeof d.musicMetadata !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.musicMetadata: object expected");
                m.musicMetadata = WAE2E.EmbeddedMusic.fromObject(d.musicMetadata, n + 1);
            }
            if (d.paymentExtendedMetadata != null) {
                if (typeof d.paymentExtendedMetadata !== "object")
                    throw TypeError(".WAE2E.ExtendedTextMessage.paymentExtendedMetadata: object expected");
                m.paymentExtendedMetadata = WAE2E.PaymentExtendedMetadata.fromObject(d.paymentExtendedMetadata, n + 1);
            }
            return m;
        };

        ExtendedTextMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.endCardTiles = [];
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.matchedText != null && m.hasOwnProperty("matchedText")) {
                d.matchedText = m.matchedText;
                if (o.oneofs)
                    d._matchedText = "matchedText";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.textArgb != null && m.hasOwnProperty("textArgb")) {
                d.textArgb = m.textArgb;
                if (o.oneofs)
                    d._textArgb = "textArgb";
            }
            if (m.backgroundArgb != null && m.hasOwnProperty("backgroundArgb")) {
                d.backgroundArgb = m.backgroundArgb;
                if (o.oneofs)
                    d._backgroundArgb = "backgroundArgb";
            }
            if (m.font != null && m.hasOwnProperty("font")) {
                d.font = o.enums === String ? WAE2E.ExtendedTextMessage.FontType[m.font] === undefined ? m.font : WAE2E.ExtendedTextMessage.FontType[m.font] : m.font;
                if (o.oneofs)
                    d._font = "font";
            }
            if (m.previewType != null && m.hasOwnProperty("previewType")) {
                d.previewType = o.enums === String ? WAE2E.ExtendedTextMessage.PreviewType[m.previewType] === undefined ? m.previewType : WAE2E.ExtendedTextMessage.PreviewType[m.previewType] : m.previewType;
                if (o.oneofs)
                    d._previewType = "previewType";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.doNotPlayInline != null && m.hasOwnProperty("doNotPlayInline")) {
                d.doNotPlayInline = m.doNotPlayInline;
                if (o.oneofs)
                    d._doNotPlayInline = "doNotPlayInline";
            }
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.thumbnailHeight != null && m.hasOwnProperty("thumbnailHeight")) {
                d.thumbnailHeight = m.thumbnailHeight;
                if (o.oneofs)
                    d._thumbnailHeight = "thumbnailHeight";
            }
            if (m.thumbnailWidth != null && m.hasOwnProperty("thumbnailWidth")) {
                d.thumbnailWidth = m.thumbnailWidth;
                if (o.oneofs)
                    d._thumbnailWidth = "thumbnailWidth";
            }
            if (m.inviteLinkGroupType != null && m.hasOwnProperty("inviteLinkGroupType")) {
                d.inviteLinkGroupType = o.enums === String ? WAE2E.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupType] === undefined ? m.inviteLinkGroupType : WAE2E.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupType] : m.inviteLinkGroupType;
                if (o.oneofs)
                    d._inviteLinkGroupType = "inviteLinkGroupType";
            }
            if (m.inviteLinkParentGroupSubjectV2 != null && m.hasOwnProperty("inviteLinkParentGroupSubjectV2")) {
                d.inviteLinkParentGroupSubjectV2 = m.inviteLinkParentGroupSubjectV2;
                if (o.oneofs)
                    d._inviteLinkParentGroupSubjectV2 = "inviteLinkParentGroupSubjectV2";
            }
            if (m.inviteLinkParentGroupThumbnailV2 != null && m.hasOwnProperty("inviteLinkParentGroupThumbnailV2")) {
                d.inviteLinkParentGroupThumbnailV2 = o.bytes === String ? $util.base64.encode(m.inviteLinkParentGroupThumbnailV2, 0, m.inviteLinkParentGroupThumbnailV2.length) : o.bytes === Array ? Array.prototype.slice.call(m.inviteLinkParentGroupThumbnailV2) : m.inviteLinkParentGroupThumbnailV2;
                if (o.oneofs)
                    d._inviteLinkParentGroupThumbnailV2 = "inviteLinkParentGroupThumbnailV2";
            }
            if (m.inviteLinkGroupTypeV2 != null && m.hasOwnProperty("inviteLinkGroupTypeV2")) {
                d.inviteLinkGroupTypeV2 = o.enums === String ? WAE2E.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupTypeV2] === undefined ? m.inviteLinkGroupTypeV2 : WAE2E.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupTypeV2] : m.inviteLinkGroupTypeV2;
                if (o.oneofs)
                    d._inviteLinkGroupTypeV2 = "inviteLinkGroupTypeV2";
            }
            if (m.viewOnce != null && m.hasOwnProperty("viewOnce")) {
                d.viewOnce = m.viewOnce;
                if (o.oneofs)
                    d._viewOnce = "viewOnce";
            }
            if (m.videoHeight != null && m.hasOwnProperty("videoHeight")) {
                d.videoHeight = m.videoHeight;
                if (o.oneofs)
                    d._videoHeight = "videoHeight";
            }
            if (m.videoWidth != null && m.hasOwnProperty("videoWidth")) {
                d.videoWidth = m.videoWidth;
                if (o.oneofs)
                    d._videoWidth = "videoWidth";
            }
            if (m.faviconMMSMetadata != null && m.hasOwnProperty("faviconMMSMetadata")) {
                d.faviconMMSMetadata = WAE2E.MMSThumbnailMetadata.toObject(m.faviconMMSMetadata, o);
                if (o.oneofs)
                    d._faviconMMSMetadata = "faviconMMSMetadata";
            }
            if (m.linkPreviewMetadata != null && m.hasOwnProperty("linkPreviewMetadata")) {
                d.linkPreviewMetadata = WAE2E.LinkPreviewMetadata.toObject(m.linkPreviewMetadata, o);
                if (o.oneofs)
                    d._linkPreviewMetadata = "linkPreviewMetadata";
            }
            if (m.paymentLinkMetadata != null && m.hasOwnProperty("paymentLinkMetadata")) {
                d.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.toObject(m.paymentLinkMetadata, o);
                if (o.oneofs)
                    d._paymentLinkMetadata = "paymentLinkMetadata";
            }
            if (m.endCardTiles && m.endCardTiles.length) {
                d.endCardTiles = [];
                for (var j = 0; j < m.endCardTiles.length; ++j) {
                    d.endCardTiles[j] = WAE2E.VideoEndCard.toObject(m.endCardTiles[j], o);
                }
            }
            if (m.videoContentURL != null && m.hasOwnProperty("videoContentURL")) {
                d.videoContentURL = m.videoContentURL;
                if (o.oneofs)
                    d._videoContentURL = "videoContentURL";
            }
            if (m.musicMetadata != null && m.hasOwnProperty("musicMetadata")) {
                d.musicMetadata = WAE2E.EmbeddedMusic.toObject(m.musicMetadata, o);
                if (o.oneofs)
                    d._musicMetadata = "musicMetadata";
            }
            if (m.paymentExtendedMetadata != null && m.hasOwnProperty("paymentExtendedMetadata")) {
                d.paymentExtendedMetadata = WAE2E.PaymentExtendedMetadata.toObject(m.paymentExtendedMetadata, o);
                if (o.oneofs)
                    d._paymentExtendedMetadata = "paymentExtendedMetadata";
            }
            return d;
        };

        ExtendedTextMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExtendedTextMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ExtendedTextMessage";
        };

        ExtendedTextMessage.InviteLinkGroupType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "PARENT"] = 1;
            values[valuesById[2] = "SUB"] = 2;
            values[valuesById[3] = "DEFAULT_SUB"] = 3;
            return values;
        })();

        ExtendedTextMessage.PreviewType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "VIDEO"] = 1;
            values[valuesById[4] = "PLACEHOLDER"] = 4;
            values[valuesById[5] = "IMAGE"] = 5;
            values[valuesById[6] = "PAYMENT_LINKS"] = 6;
            values[valuesById[7] = "PROFILE"] = 7;
            return values;
        })();

        ExtendedTextMessage.FontType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SYSTEM"] = 0;
            values[valuesById[1] = "SYSTEM_TEXT"] = 1;
            values[valuesById[2] = "FB_SCRIPT"] = 2;
            values[valuesById[6] = "SYSTEM_BOLD"] = 6;
            values[valuesById[7] = "MORNINGBREEZE_REGULAR"] = 7;
            values[valuesById[8] = "CALISTOGA_REGULAR"] = 8;
            values[valuesById[9] = "EXO2_EXTRABOLD"] = 9;
            values[valuesById[10] = "COURIERPRIME_BOLD"] = 10;
            return values;
        })();

        return ExtendedTextMessage;
    })();

    WAE2E.LinkPreviewMetadata = (function() {

        function LinkPreviewMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LinkPreviewMetadata.prototype.paymentLinkMetadata = null;
        LinkPreviewMetadata.prototype.urlMetadata = null;
        LinkPreviewMetadata.prototype.fbExperimentID = null;
        LinkPreviewMetadata.prototype.linkMediaDuration = null;
        LinkPreviewMetadata.prototype.socialMediaPostType = null;
        LinkPreviewMetadata.prototype.linkInlineVideoMuted = null;
        LinkPreviewMetadata.prototype.videoContentURL = null;
        LinkPreviewMetadata.prototype.musicMetadata = null;
        LinkPreviewMetadata.prototype.videoContentCaption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_paymentLinkMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_urlMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["urlMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_fbExperimentID", {
            get: $util.oneOfGetter($oneOfFields = ["fbExperimentID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_linkMediaDuration", {
            get: $util.oneOfGetter($oneOfFields = ["linkMediaDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_socialMediaPostType", {
            get: $util.oneOfGetter($oneOfFields = ["socialMediaPostType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_linkInlineVideoMuted", {
            get: $util.oneOfGetter($oneOfFields = ["linkInlineVideoMuted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentURL", {
            get: $util.oneOfGetter($oneOfFields = ["videoContentURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_musicMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentCaption", {
            get: $util.oneOfGetter($oneOfFields = ["videoContentCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LinkPreviewMetadata.create = function create(properties) {
            return new LinkPreviewMetadata(properties);
        };

        LinkPreviewMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.paymentLinkMetadata != null && Object.hasOwnProperty.call(m, "paymentLinkMetadata"))
                WAE2E.PaymentLinkMetadata.encode(m.paymentLinkMetadata, w.uint32(10).fork()).ldelim();
            if (m.urlMetadata != null && Object.hasOwnProperty.call(m, "urlMetadata"))
                WAE2E.URLMetadata.encode(m.urlMetadata, w.uint32(18).fork()).ldelim();
            if (m.fbExperimentID != null && Object.hasOwnProperty.call(m, "fbExperimentID"))
                w.uint32(24).uint32(m.fbExperimentID);
            if (m.linkMediaDuration != null && Object.hasOwnProperty.call(m, "linkMediaDuration"))
                w.uint32(32).uint32(m.linkMediaDuration);
            if (m.socialMediaPostType != null && Object.hasOwnProperty.call(m, "socialMediaPostType"))
                w.uint32(40).int32(m.socialMediaPostType);
            if (m.linkInlineVideoMuted != null && Object.hasOwnProperty.call(m, "linkInlineVideoMuted"))
                w.uint32(48).bool(m.linkInlineVideoMuted);
            if (m.videoContentURL != null && Object.hasOwnProperty.call(m, "videoContentURL"))
                w.uint32(58).string(m.videoContentURL);
            if (m.musicMetadata != null && Object.hasOwnProperty.call(m, "musicMetadata"))
                WAE2E.EmbeddedMusic.encode(m.musicMetadata, w.uint32(66).fork()).ldelim();
            if (m.videoContentCaption != null && Object.hasOwnProperty.call(m, "videoContentCaption"))
                w.uint32(74).string(m.videoContentCaption);
            return w;
        };

        LinkPreviewMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.LinkPreviewMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.urlMetadata = WAE2E.URLMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.fbExperimentID = r.uint32();
                        break;
                    }
                case 4: {
                        m.linkMediaDuration = r.uint32();
                        break;
                    }
                case 5: {
                        m.socialMediaPostType = r.int32();
                        break;
                    }
                case 6: {
                        m.linkInlineVideoMuted = r.bool();
                        break;
                    }
                case 7: {
                        m.videoContentURL = r.string();
                        break;
                    }
                case 8: {
                        m.musicMetadata = WAE2E.EmbeddedMusic.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.videoContentCaption = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LinkPreviewMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.LinkPreviewMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.LinkPreviewMetadata();
            if (d.paymentLinkMetadata != null) {
                if (typeof d.paymentLinkMetadata !== "object")
                    throw TypeError(".WAE2E.LinkPreviewMetadata.paymentLinkMetadata: object expected");
                m.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.fromObject(d.paymentLinkMetadata, n + 1);
            }
            if (d.urlMetadata != null) {
                if (typeof d.urlMetadata !== "object")
                    throw TypeError(".WAE2E.LinkPreviewMetadata.urlMetadata: object expected");
                m.urlMetadata = WAE2E.URLMetadata.fromObject(d.urlMetadata, n + 1);
            }
            if (d.fbExperimentID != null) {
                m.fbExperimentID = d.fbExperimentID >>> 0;
            }
            if (d.linkMediaDuration != null) {
                m.linkMediaDuration = d.linkMediaDuration >>> 0;
            }
            switch (d.socialMediaPostType) {
            default:
                if (typeof d.socialMediaPostType === "number") {
                    m.socialMediaPostType = d.socialMediaPostType;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.socialMediaPostType = 0;
                break;
            case "REEL":
            case 1:
                m.socialMediaPostType = 1;
                break;
            case "LIVE_VIDEO":
            case 2:
                m.socialMediaPostType = 2;
                break;
            case "LONG_VIDEO":
            case 3:
                m.socialMediaPostType = 3;
                break;
            case "SINGLE_IMAGE":
            case 4:
                m.socialMediaPostType = 4;
                break;
            case "CAROUSEL":
            case 5:
                m.socialMediaPostType = 5;
                break;
            }
            if (d.linkInlineVideoMuted != null) {
                m.linkInlineVideoMuted = Boolean(d.linkInlineVideoMuted);
            }
            if (d.videoContentURL != null) {
                m.videoContentURL = String(d.videoContentURL);
            }
            if (d.musicMetadata != null) {
                if (typeof d.musicMetadata !== "object")
                    throw TypeError(".WAE2E.LinkPreviewMetadata.musicMetadata: object expected");
                m.musicMetadata = WAE2E.EmbeddedMusic.fromObject(d.musicMetadata, n + 1);
            }
            if (d.videoContentCaption != null) {
                m.videoContentCaption = String(d.videoContentCaption);
            }
            return m;
        };

        LinkPreviewMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.paymentLinkMetadata != null && m.hasOwnProperty("paymentLinkMetadata")) {
                d.paymentLinkMetadata = WAE2E.PaymentLinkMetadata.toObject(m.paymentLinkMetadata, o);
                if (o.oneofs)
                    d._paymentLinkMetadata = "paymentLinkMetadata";
            }
            if (m.urlMetadata != null && m.hasOwnProperty("urlMetadata")) {
                d.urlMetadata = WAE2E.URLMetadata.toObject(m.urlMetadata, o);
                if (o.oneofs)
                    d._urlMetadata = "urlMetadata";
            }
            if (m.fbExperimentID != null && m.hasOwnProperty("fbExperimentID")) {
                d.fbExperimentID = m.fbExperimentID;
                if (o.oneofs)
                    d._fbExperimentID = "fbExperimentID";
            }
            if (m.linkMediaDuration != null && m.hasOwnProperty("linkMediaDuration")) {
                d.linkMediaDuration = m.linkMediaDuration;
                if (o.oneofs)
                    d._linkMediaDuration = "linkMediaDuration";
            }
            if (m.socialMediaPostType != null && m.hasOwnProperty("socialMediaPostType")) {
                d.socialMediaPostType = o.enums === String ? WAE2E.LinkPreviewMetadata.SocialMediaPostType[m.socialMediaPostType] === undefined ? m.socialMediaPostType : WAE2E.LinkPreviewMetadata.SocialMediaPostType[m.socialMediaPostType] : m.socialMediaPostType;
                if (o.oneofs)
                    d._socialMediaPostType = "socialMediaPostType";
            }
            if (m.linkInlineVideoMuted != null && m.hasOwnProperty("linkInlineVideoMuted")) {
                d.linkInlineVideoMuted = m.linkInlineVideoMuted;
                if (o.oneofs)
                    d._linkInlineVideoMuted = "linkInlineVideoMuted";
            }
            if (m.videoContentURL != null && m.hasOwnProperty("videoContentURL")) {
                d.videoContentURL = m.videoContentURL;
                if (o.oneofs)
                    d._videoContentURL = "videoContentURL";
            }
            if (m.musicMetadata != null && m.hasOwnProperty("musicMetadata")) {
                d.musicMetadata = WAE2E.EmbeddedMusic.toObject(m.musicMetadata, o);
                if (o.oneofs)
                    d._musicMetadata = "musicMetadata";
            }
            if (m.videoContentCaption != null && m.hasOwnProperty("videoContentCaption")) {
                d.videoContentCaption = m.videoContentCaption;
                if (o.oneofs)
                    d._videoContentCaption = "videoContentCaption";
            }
            return d;
        };

        LinkPreviewMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LinkPreviewMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.LinkPreviewMetadata";
        };

        LinkPreviewMetadata.SocialMediaPostType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "REEL"] = 1;
            values[valuesById[2] = "LIVE_VIDEO"] = 2;
            values[valuesById[3] = "LONG_VIDEO"] = 3;
            values[valuesById[4] = "SINGLE_IMAGE"] = 4;
            values[valuesById[5] = "CAROUSEL"] = 5;
            return values;
        })();

        return LinkPreviewMetadata;
    })();

    WAE2E.PaymentLinkMetadata = (function() {

        function PaymentLinkMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PaymentLinkMetadata.prototype.button = null;
        PaymentLinkMetadata.prototype.header = null;
        PaymentLinkMetadata.prototype.provider = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentLinkMetadata.prototype, "_button", {
            get: $util.oneOfGetter($oneOfFields = ["button"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentLinkMetadata.prototype, "_header", {
            get: $util.oneOfGetter($oneOfFields = ["header"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentLinkMetadata.prototype, "_provider", {
            get: $util.oneOfGetter($oneOfFields = ["provider"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentLinkMetadata.create = function create(properties) {
            return new PaymentLinkMetadata(properties);
        };

        PaymentLinkMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.button != null && Object.hasOwnProperty.call(m, "button"))
                WAE2E.PaymentLinkMetadata.PaymentLinkButton.encode(m.button, w.uint32(10).fork()).ldelim();
            if (m.header != null && Object.hasOwnProperty.call(m, "header"))
                WAE2E.PaymentLinkMetadata.PaymentLinkHeader.encode(m.header, w.uint32(18).fork()).ldelim();
            if (m.provider != null && Object.hasOwnProperty.call(m, "provider"))
                WAE2E.PaymentLinkMetadata.PaymentLinkProvider.encode(m.provider, w.uint32(26).fork()).ldelim();
            return w;
        };

        PaymentLinkMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentLinkMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.button = WAE2E.PaymentLinkMetadata.PaymentLinkButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.header = WAE2E.PaymentLinkMetadata.PaymentLinkHeader.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.provider = WAE2E.PaymentLinkMetadata.PaymentLinkProvider.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PaymentLinkMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PaymentLinkMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PaymentLinkMetadata();
            if (d.button != null) {
                if (typeof d.button !== "object")
                    throw TypeError(".WAE2E.PaymentLinkMetadata.button: object expected");
                m.button = WAE2E.PaymentLinkMetadata.PaymentLinkButton.fromObject(d.button, n + 1);
            }
            if (d.header != null) {
                if (typeof d.header !== "object")
                    throw TypeError(".WAE2E.PaymentLinkMetadata.header: object expected");
                m.header = WAE2E.PaymentLinkMetadata.PaymentLinkHeader.fromObject(d.header, n + 1);
            }
            if (d.provider != null) {
                if (typeof d.provider !== "object")
                    throw TypeError(".WAE2E.PaymentLinkMetadata.provider: object expected");
                m.provider = WAE2E.PaymentLinkMetadata.PaymentLinkProvider.fromObject(d.provider, n + 1);
            }
            return m;
        };

        PaymentLinkMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.button != null && m.hasOwnProperty("button")) {
                d.button = WAE2E.PaymentLinkMetadata.PaymentLinkButton.toObject(m.button, o);
                if (o.oneofs)
                    d._button = "button";
            }
            if (m.header != null && m.hasOwnProperty("header")) {
                d.header = WAE2E.PaymentLinkMetadata.PaymentLinkHeader.toObject(m.header, o);
                if (o.oneofs)
                    d._header = "header";
            }
            if (m.provider != null && m.hasOwnProperty("provider")) {
                d.provider = WAE2E.PaymentLinkMetadata.PaymentLinkProvider.toObject(m.provider, o);
                if (o.oneofs)
                    d._provider = "provider";
            }
            return d;
        };

        PaymentLinkMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentLinkMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PaymentLinkMetadata";
        };

        PaymentLinkMetadata.PaymentLinkHeader = (function() {

            function PaymentLinkHeader(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PaymentLinkHeader.prototype.headerType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkHeader.prototype, "_headerType", {
                get: $util.oneOfGetter($oneOfFields = ["headerType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentLinkHeader.create = function create(properties) {
                return new PaymentLinkHeader(properties);
            };

            PaymentLinkHeader.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.headerType != null && Object.hasOwnProperty.call(m, "headerType"))
                    w.uint32(8).int32(m.headerType);
                return w;
            };

            PaymentLinkHeader.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentLinkMetadata.PaymentLinkHeader();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.headerType = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PaymentLinkHeader.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PaymentLinkMetadata.PaymentLinkHeader)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PaymentLinkMetadata.PaymentLinkHeader();
                switch (d.headerType) {
                default:
                    if (typeof d.headerType === "number") {
                        m.headerType = d.headerType;
                        break;
                    }
                    break;
                case "LINK_PREVIEW":
                case 0:
                    m.headerType = 0;
                    break;
                case "ORDER":
                case 1:
                    m.headerType = 1;
                    break;
                }
                return m;
            };

            PaymentLinkHeader.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.headerType != null && m.hasOwnProperty("headerType")) {
                    d.headerType = o.enums === String ? WAE2E.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[m.headerType] === undefined ? m.headerType : WAE2E.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[m.headerType] : m.headerType;
                    if (o.oneofs)
                        d._headerType = "headerType";
                }
                return d;
            };

            PaymentLinkHeader.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentLinkHeader.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PaymentLinkMetadata.PaymentLinkHeader";
            };

            PaymentLinkHeader.PaymentLinkHeaderType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "LINK_PREVIEW"] = 0;
                values[valuesById[1] = "ORDER"] = 1;
                return values;
            })();

            return PaymentLinkHeader;
        })();

        PaymentLinkMetadata.PaymentLinkProvider = (function() {

            function PaymentLinkProvider(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PaymentLinkProvider.prototype.paramsJSON = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkProvider.prototype, "_paramsJSON", {
                get: $util.oneOfGetter($oneOfFields = ["paramsJSON"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentLinkProvider.create = function create(properties) {
                return new PaymentLinkProvider(properties);
            };

            PaymentLinkProvider.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.paramsJSON != null && Object.hasOwnProperty.call(m, "paramsJSON"))
                    w.uint32(10).string(m.paramsJSON);
                return w;
            };

            PaymentLinkProvider.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentLinkMetadata.PaymentLinkProvider();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.paramsJSON = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PaymentLinkProvider.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PaymentLinkMetadata.PaymentLinkProvider)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PaymentLinkMetadata.PaymentLinkProvider();
                if (d.paramsJSON != null) {
                    m.paramsJSON = String(d.paramsJSON);
                }
                return m;
            };

            PaymentLinkProvider.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.paramsJSON != null && m.hasOwnProperty("paramsJSON")) {
                    d.paramsJSON = m.paramsJSON;
                    if (o.oneofs)
                        d._paramsJSON = "paramsJSON";
                }
                return d;
            };

            PaymentLinkProvider.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentLinkProvider.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PaymentLinkMetadata.PaymentLinkProvider";
            };

            return PaymentLinkProvider;
        })();

        PaymentLinkMetadata.PaymentLinkButton = (function() {

            function PaymentLinkButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PaymentLinkButton.prototype.displayText = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PaymentLinkButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentLinkButton.create = function create(properties) {
                return new PaymentLinkButton(properties);
            };

            PaymentLinkButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                return w;
            };

            PaymentLinkButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentLinkMetadata.PaymentLinkButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PaymentLinkButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PaymentLinkMetadata.PaymentLinkButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PaymentLinkMetadata.PaymentLinkButton();
                if (d.displayText != null) {
                    m.displayText = String(d.displayText);
                }
                return m;
            };

            PaymentLinkButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = m.displayText;
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                return d;
            };

            PaymentLinkButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentLinkButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PaymentLinkMetadata.PaymentLinkButton";
            };

            return PaymentLinkButton;
        })();

        return PaymentLinkMetadata;
    })();

    WAE2E.StatusNotificationMessage = (function() {

        function StatusNotificationMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusNotificationMessage.prototype.responseMessageKey = null;
        StatusNotificationMessage.prototype.originalMessageKey = null;
        StatusNotificationMessage.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusNotificationMessage.prototype, "_responseMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["responseMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusNotificationMessage.prototype, "_originalMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["originalMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusNotificationMessage.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusNotificationMessage.create = function create(properties) {
            return new StatusNotificationMessage(properties);
        };

        StatusNotificationMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.responseMessageKey != null && Object.hasOwnProperty.call(m, "responseMessageKey"))
                WACommon.MessageKey.encode(m.responseMessageKey, w.uint32(10).fork()).ldelim();
            if (m.originalMessageKey != null && Object.hasOwnProperty.call(m, "originalMessageKey"))
                WACommon.MessageKey.encode(m.originalMessageKey, w.uint32(18).fork()).ldelim();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(24).int32(m.type);
            return w;
        };

        StatusNotificationMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StatusNotificationMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.responseMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.originalMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusNotificationMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StatusNotificationMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StatusNotificationMessage();
            if (d.responseMessageKey != null) {
                if (typeof d.responseMessageKey !== "object")
                    throw TypeError(".WAE2E.StatusNotificationMessage.responseMessageKey: object expected");
                m.responseMessageKey = WACommon.MessageKey.fromObject(d.responseMessageKey, n + 1);
            }
            if (d.originalMessageKey != null) {
                if (typeof d.originalMessageKey !== "object")
                    throw TypeError(".WAE2E.StatusNotificationMessage.originalMessageKey: object expected");
                m.originalMessageKey = WACommon.MessageKey.fromObject(d.originalMessageKey, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "STATUS_ADD_YOURS":
            case 1:
                m.type = 1;
                break;
            case "STATUS_RESHARE":
            case 2:
                m.type = 2;
                break;
            case "STATUS_QUESTION_ANSWER_RESHARE":
            case 3:
                m.type = 3;
                break;
            }
            return m;
        };

        StatusNotificationMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.responseMessageKey != null && m.hasOwnProperty("responseMessageKey")) {
                d.responseMessageKey = WACommon.MessageKey.toObject(m.responseMessageKey, o);
                if (o.oneofs)
                    d._responseMessageKey = "responseMessageKey";
            }
            if (m.originalMessageKey != null && m.hasOwnProperty("originalMessageKey")) {
                d.originalMessageKey = WACommon.MessageKey.toObject(m.originalMessageKey, o);
                if (o.oneofs)
                    d._originalMessageKey = "originalMessageKey";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.StatusNotificationMessage.StatusNotificationType[m.type] === undefined ? m.type : WAE2E.StatusNotificationMessage.StatusNotificationType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        StatusNotificationMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusNotificationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StatusNotificationMessage";
        };

        StatusNotificationMessage.StatusNotificationType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "STATUS_ADD_YOURS"] = 1;
            values[valuesById[2] = "STATUS_RESHARE"] = 2;
            values[valuesById[3] = "STATUS_QUESTION_ANSWER_RESHARE"] = 3;
            return values;
        })();

        return StatusNotificationMessage;
    })();

    WAE2E.InvoiceMessage = (function() {

        function InvoiceMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InvoiceMessage.prototype.note = null;
        InvoiceMessage.prototype.token = null;
        InvoiceMessage.prototype.attachmentType = null;
        InvoiceMessage.prototype.attachmentMimetype = null;
        InvoiceMessage.prototype.attachmentMediaKey = null;
        InvoiceMessage.prototype.attachmentMediaKeyTimestamp = null;
        InvoiceMessage.prototype.attachmentFileSHA256 = null;
        InvoiceMessage.prototype.attachmentFileEncSHA256 = null;
        InvoiceMessage.prototype.attachmentDirectPath = null;
        InvoiceMessage.prototype.attachmentJPEGThumbnail = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_note", {
            get: $util.oneOfGetter($oneOfFields = ["note"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_token", {
            get: $util.oneOfGetter($oneOfFields = ["token"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentType", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentMimetype", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentMimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentFileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentFileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InvoiceMessage.prototype, "_attachmentJPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["attachmentJPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InvoiceMessage.create = function create(properties) {
            return new InvoiceMessage(properties);
        };

        InvoiceMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.note != null && Object.hasOwnProperty.call(m, "note"))
                w.uint32(10).string(m.note);
            if (m.token != null && Object.hasOwnProperty.call(m, "token"))
                w.uint32(18).string(m.token);
            if (m.attachmentType != null && Object.hasOwnProperty.call(m, "attachmentType"))
                w.uint32(24).int32(m.attachmentType);
            if (m.attachmentMimetype != null && Object.hasOwnProperty.call(m, "attachmentMimetype"))
                w.uint32(34).string(m.attachmentMimetype);
            if (m.attachmentMediaKey != null && Object.hasOwnProperty.call(m, "attachmentMediaKey"))
                w.uint32(42).bytes(m.attachmentMediaKey);
            if (m.attachmentMediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "attachmentMediaKeyTimestamp"))
                w.uint32(48).int64(m.attachmentMediaKeyTimestamp);
            if (m.attachmentFileSHA256 != null && Object.hasOwnProperty.call(m, "attachmentFileSHA256"))
                w.uint32(58).bytes(m.attachmentFileSHA256);
            if (m.attachmentFileEncSHA256 != null && Object.hasOwnProperty.call(m, "attachmentFileEncSHA256"))
                w.uint32(66).bytes(m.attachmentFileEncSHA256);
            if (m.attachmentDirectPath != null && Object.hasOwnProperty.call(m, "attachmentDirectPath"))
                w.uint32(74).string(m.attachmentDirectPath);
            if (m.attachmentJPEGThumbnail != null && Object.hasOwnProperty.call(m, "attachmentJPEGThumbnail"))
                w.uint32(82).bytes(m.attachmentJPEGThumbnail);
            return w;
        };

        InvoiceMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InvoiceMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.note = r.string();
                        break;
                    }
                case 2: {
                        m.token = r.string();
                        break;
                    }
                case 3: {
                        m.attachmentType = r.int32();
                        break;
                    }
                case 4: {
                        m.attachmentMimetype = r.string();
                        break;
                    }
                case 5: {
                        m.attachmentMediaKey = r.bytes();
                        break;
                    }
                case 6: {
                        m.attachmentMediaKeyTimestamp = r.int64();
                        break;
                    }
                case 7: {
                        m.attachmentFileSHA256 = r.bytes();
                        break;
                    }
                case 8: {
                        m.attachmentFileEncSHA256 = r.bytes();
                        break;
                    }
                case 9: {
                        m.attachmentDirectPath = r.string();
                        break;
                    }
                case 10: {
                        m.attachmentJPEGThumbnail = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InvoiceMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.InvoiceMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.InvoiceMessage();
            if (d.note != null) {
                m.note = String(d.note);
            }
            if (d.token != null) {
                m.token = String(d.token);
            }
            switch (d.attachmentType) {
            default:
                if (typeof d.attachmentType === "number") {
                    m.attachmentType = d.attachmentType;
                    break;
                }
                break;
            case "IMAGE":
            case 0:
                m.attachmentType = 0;
                break;
            case "PDF":
            case 1:
                m.attachmentType = 1;
                break;
            }
            if (d.attachmentMimetype != null) {
                m.attachmentMimetype = String(d.attachmentMimetype);
            }
            if (d.attachmentMediaKey != null) {
                if (typeof d.attachmentMediaKey === "string")
                    $util.base64.decode(d.attachmentMediaKey, m.attachmentMediaKey = $util.newBuffer($util.base64.length(d.attachmentMediaKey)), 0);
                else if (d.attachmentMediaKey.length >= 0)
                    m.attachmentMediaKey = d.attachmentMediaKey;
            }
            if (d.attachmentMediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.attachmentMediaKeyTimestamp = $util.Long.fromValue(d.attachmentMediaKeyTimestamp)).unsigned = false;
                else if (typeof d.attachmentMediaKeyTimestamp === "string")
                    m.attachmentMediaKeyTimestamp = parseInt(d.attachmentMediaKeyTimestamp, 10);
                else if (typeof d.attachmentMediaKeyTimestamp === "number")
                    m.attachmentMediaKeyTimestamp = d.attachmentMediaKeyTimestamp;
                else if (typeof d.attachmentMediaKeyTimestamp === "object")
                    m.attachmentMediaKeyTimestamp = new $util.LongBits(d.attachmentMediaKeyTimestamp.low >>> 0, d.attachmentMediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.attachmentFileSHA256 != null) {
                if (typeof d.attachmentFileSHA256 === "string")
                    $util.base64.decode(d.attachmentFileSHA256, m.attachmentFileSHA256 = $util.newBuffer($util.base64.length(d.attachmentFileSHA256)), 0);
                else if (d.attachmentFileSHA256.length >= 0)
                    m.attachmentFileSHA256 = d.attachmentFileSHA256;
            }
            if (d.attachmentFileEncSHA256 != null) {
                if (typeof d.attachmentFileEncSHA256 === "string")
                    $util.base64.decode(d.attachmentFileEncSHA256, m.attachmentFileEncSHA256 = $util.newBuffer($util.base64.length(d.attachmentFileEncSHA256)), 0);
                else if (d.attachmentFileEncSHA256.length >= 0)
                    m.attachmentFileEncSHA256 = d.attachmentFileEncSHA256;
            }
            if (d.attachmentDirectPath != null) {
                m.attachmentDirectPath = String(d.attachmentDirectPath);
            }
            if (d.attachmentJPEGThumbnail != null) {
                if (typeof d.attachmentJPEGThumbnail === "string")
                    $util.base64.decode(d.attachmentJPEGThumbnail, m.attachmentJPEGThumbnail = $util.newBuffer($util.base64.length(d.attachmentJPEGThumbnail)), 0);
                else if (d.attachmentJPEGThumbnail.length >= 0)
                    m.attachmentJPEGThumbnail = d.attachmentJPEGThumbnail;
            }
            return m;
        };

        InvoiceMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.note != null && m.hasOwnProperty("note")) {
                d.note = m.note;
                if (o.oneofs)
                    d._note = "note";
            }
            if (m.token != null && m.hasOwnProperty("token")) {
                d.token = m.token;
                if (o.oneofs)
                    d._token = "token";
            }
            if (m.attachmentType != null && m.hasOwnProperty("attachmentType")) {
                d.attachmentType = o.enums === String ? WAE2E.InvoiceMessage.AttachmentType[m.attachmentType] === undefined ? m.attachmentType : WAE2E.InvoiceMessage.AttachmentType[m.attachmentType] : m.attachmentType;
                if (o.oneofs)
                    d._attachmentType = "attachmentType";
            }
            if (m.attachmentMimetype != null && m.hasOwnProperty("attachmentMimetype")) {
                d.attachmentMimetype = m.attachmentMimetype;
                if (o.oneofs)
                    d._attachmentMimetype = "attachmentMimetype";
            }
            if (m.attachmentMediaKey != null && m.hasOwnProperty("attachmentMediaKey")) {
                d.attachmentMediaKey = o.bytes === String ? $util.base64.encode(m.attachmentMediaKey, 0, m.attachmentMediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.attachmentMediaKey) : m.attachmentMediaKey;
                if (o.oneofs)
                    d._attachmentMediaKey = "attachmentMediaKey";
            }
            if (m.attachmentMediaKeyTimestamp != null && m.hasOwnProperty("attachmentMediaKeyTimestamp")) {
                if (typeof m.attachmentMediaKeyTimestamp === "number")
                    d.attachmentMediaKeyTimestamp = o.longs === String ? String(m.attachmentMediaKeyTimestamp) : m.attachmentMediaKeyTimestamp;
                else
                    d.attachmentMediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.attachmentMediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.attachmentMediaKeyTimestamp.low >>> 0, m.attachmentMediaKeyTimestamp.high >>> 0).toNumber() : m.attachmentMediaKeyTimestamp;
                if (o.oneofs)
                    d._attachmentMediaKeyTimestamp = "attachmentMediaKeyTimestamp";
            }
            if (m.attachmentFileSHA256 != null && m.hasOwnProperty("attachmentFileSHA256")) {
                d.attachmentFileSHA256 = o.bytes === String ? $util.base64.encode(m.attachmentFileSHA256, 0, m.attachmentFileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.attachmentFileSHA256) : m.attachmentFileSHA256;
                if (o.oneofs)
                    d._attachmentFileSHA256 = "attachmentFileSHA256";
            }
            if (m.attachmentFileEncSHA256 != null && m.hasOwnProperty("attachmentFileEncSHA256")) {
                d.attachmentFileEncSHA256 = o.bytes === String ? $util.base64.encode(m.attachmentFileEncSHA256, 0, m.attachmentFileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.attachmentFileEncSHA256) : m.attachmentFileEncSHA256;
                if (o.oneofs)
                    d._attachmentFileEncSHA256 = "attachmentFileEncSHA256";
            }
            if (m.attachmentDirectPath != null && m.hasOwnProperty("attachmentDirectPath")) {
                d.attachmentDirectPath = m.attachmentDirectPath;
                if (o.oneofs)
                    d._attachmentDirectPath = "attachmentDirectPath";
            }
            if (m.attachmentJPEGThumbnail != null && m.hasOwnProperty("attachmentJPEGThumbnail")) {
                d.attachmentJPEGThumbnail = o.bytes === String ? $util.base64.encode(m.attachmentJPEGThumbnail, 0, m.attachmentJPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.attachmentJPEGThumbnail) : m.attachmentJPEGThumbnail;
                if (o.oneofs)
                    d._attachmentJPEGThumbnail = "attachmentJPEGThumbnail";
            }
            return d;
        };

        InvoiceMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InvoiceMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.InvoiceMessage";
        };

        InvoiceMessage.AttachmentType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IMAGE"] = 0;
            values[valuesById[1] = "PDF"] = 1;
            return values;
        })();

        return InvoiceMessage;
    })();

    WAE2E.ImageMessage = (function() {

        function ImageMessage(p) {
            this.interactiveAnnotations = [];
            this.scanLengths = [];
            this.annotations = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ImageMessage.prototype.URL = null;
        ImageMessage.prototype.mimetype = null;
        ImageMessage.prototype.caption = null;
        ImageMessage.prototype.fileSHA256 = null;
        ImageMessage.prototype.fileLength = null;
        ImageMessage.prototype.height = null;
        ImageMessage.prototype.width = null;
        ImageMessage.prototype.mediaKey = null;
        ImageMessage.prototype.fileEncSHA256 = null;
        ImageMessage.prototype.interactiveAnnotations = $util.emptyArray;
        ImageMessage.prototype.directPath = null;
        ImageMessage.prototype.mediaKeyTimestamp = null;
        ImageMessage.prototype.JPEGThumbnail = null;
        ImageMessage.prototype.contextInfo = null;
        ImageMessage.prototype.firstScanSidecar = null;
        ImageMessage.prototype.firstScanLength = null;
        ImageMessage.prototype.experimentGroupID = null;
        ImageMessage.prototype.scansSidecar = null;
        ImageMessage.prototype.scanLengths = $util.emptyArray;
        ImageMessage.prototype.midQualityFileSHA256 = null;
        ImageMessage.prototype.midQualityFileEncSHA256 = null;
        ImageMessage.prototype.viewOnce = null;
        ImageMessage.prototype.thumbnailDirectPath = null;
        ImageMessage.prototype.thumbnailSHA256 = null;
        ImageMessage.prototype.thumbnailEncSHA256 = null;
        ImageMessage.prototype.staticURL = null;
        ImageMessage.prototype.annotations = $util.emptyArray;
        ImageMessage.prototype.imageSourceType = null;
        ImageMessage.prototype.accessibilityLabel = null;
        ImageMessage.prototype.qrURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_firstScanSidecar", {
            get: $util.oneOfGetter($oneOfFields = ["firstScanSidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_firstScanLength", {
            get: $util.oneOfGetter($oneOfFields = ["firstScanLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_experimentGroupID", {
            get: $util.oneOfGetter($oneOfFields = ["experimentGroupID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_scansSidecar", {
            get: $util.oneOfGetter($oneOfFields = ["scansSidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_midQualityFileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["midQualityFileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_midQualityFileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["midQualityFileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_viewOnce", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_staticURL", {
            get: $util.oneOfGetter($oneOfFields = ["staticURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_imageSourceType", {
            get: $util.oneOfGetter($oneOfFields = ["imageSourceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_accessibilityLabel", {
            get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ImageMessage.prototype, "_qrURL", {
            get: $util.oneOfGetter($oneOfFields = ["qrURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ImageMessage.create = function create(properties) {
            return new ImageMessage(properties);
        };

        ImageMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(18).string(m.mimetype);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(26).string(m.caption);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(34).bytes(m.fileSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(40).uint64(m.fileLength);
            if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                w.uint32(48).uint32(m.height);
            if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                w.uint32(56).uint32(m.width);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(66).bytes(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(74).bytes(m.fileEncSHA256);
            if (m.interactiveAnnotations != null && m.interactiveAnnotations.length) {
                for (var i = 0; i < m.interactiveAnnotations.length; ++i)
                    WAE2E.InteractiveAnnotation.encode(m.interactiveAnnotations[i], w.uint32(82).fork()).ldelim();
            }
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(90).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(96).int64(m.mediaKeyTimestamp);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.firstScanSidecar != null && Object.hasOwnProperty.call(m, "firstScanSidecar"))
                w.uint32(146).bytes(m.firstScanSidecar);
            if (m.firstScanLength != null && Object.hasOwnProperty.call(m, "firstScanLength"))
                w.uint32(152).uint32(m.firstScanLength);
            if (m.experimentGroupID != null && Object.hasOwnProperty.call(m, "experimentGroupID"))
                w.uint32(160).uint32(m.experimentGroupID);
            if (m.scansSidecar != null && Object.hasOwnProperty.call(m, "scansSidecar"))
                w.uint32(170).bytes(m.scansSidecar);
            if (m.scanLengths != null && m.scanLengths.length) {
                w.uint32(178).fork();
                for (var i = 0; i < m.scanLengths.length; ++i)
                    w.uint32(m.scanLengths[i]);
                w.ldelim();
            }
            if (m.midQualityFileSHA256 != null && Object.hasOwnProperty.call(m, "midQualityFileSHA256"))
                w.uint32(186).bytes(m.midQualityFileSHA256);
            if (m.midQualityFileEncSHA256 != null && Object.hasOwnProperty.call(m, "midQualityFileEncSHA256"))
                w.uint32(194).bytes(m.midQualityFileEncSHA256);
            if (m.viewOnce != null && Object.hasOwnProperty.call(m, "viewOnce"))
                w.uint32(200).bool(m.viewOnce);
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(210).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(218).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(226).bytes(m.thumbnailEncSHA256);
            if (m.staticURL != null && Object.hasOwnProperty.call(m, "staticURL"))
                w.uint32(234).string(m.staticURL);
            if (m.annotations != null && m.annotations.length) {
                for (var i = 0; i < m.annotations.length; ++i)
                    WAE2E.InteractiveAnnotation.encode(m.annotations[i], w.uint32(242).fork()).ldelim();
            }
            if (m.imageSourceType != null && Object.hasOwnProperty.call(m, "imageSourceType"))
                w.uint32(248).int32(m.imageSourceType);
            if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                w.uint32(258).string(m.accessibilityLabel);
            if (m.qrURL != null && Object.hasOwnProperty.call(m, "qrURL"))
                w.uint32(274).string(m.qrURL);
            return w;
        };

        ImageMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ImageMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.mimetype = r.string();
                        break;
                    }
                case 3: {
                        m.caption = r.string();
                        break;
                    }
                case 4: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 5: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 6: {
                        m.height = r.uint32();
                        break;
                    }
                case 7: {
                        m.width = r.uint32();
                        break;
                    }
                case 8: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 9: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 10: {
                        if (!(m.interactiveAnnotations && m.interactiveAnnotations.length))
                            m.interactiveAnnotations = [];
                        m.interactiveAnnotations.push(WAE2E.InteractiveAnnotation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 11: {
                        m.directPath = r.string();
                        break;
                    }
                case 12: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.firstScanSidecar = r.bytes();
                        break;
                    }
                case 19: {
                        m.firstScanLength = r.uint32();
                        break;
                    }
                case 20: {
                        m.experimentGroupID = r.uint32();
                        break;
                    }
                case 21: {
                        m.scansSidecar = r.bytes();
                        break;
                    }
                case 22: {
                        if (!(m.scanLengths && m.scanLengths.length))
                            m.scanLengths = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.scanLengths.push(r.uint32());
                        } else
                            m.scanLengths.push(r.uint32());
                        break;
                    }
                case 23: {
                        m.midQualityFileSHA256 = r.bytes();
                        break;
                    }
                case 24: {
                        m.midQualityFileEncSHA256 = r.bytes();
                        break;
                    }
                case 25: {
                        m.viewOnce = r.bool();
                        break;
                    }
                case 26: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 27: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 28: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 29: {
                        m.staticURL = r.string();
                        break;
                    }
                case 30: {
                        if (!(m.annotations && m.annotations.length))
                            m.annotations = [];
                        m.annotations.push(WAE2E.InteractiveAnnotation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 31: {
                        m.imageSourceType = r.int32();
                        break;
                    }
                case 32: {
                        m.accessibilityLabel = r.string();
                        break;
                    }
                case 34: {
                        m.qrURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ImageMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ImageMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ImageMessage();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.interactiveAnnotations) {
                if (!Array.isArray(d.interactiveAnnotations))
                    throw TypeError(".WAE2E.ImageMessage.interactiveAnnotations: array expected");
                m.interactiveAnnotations = [];
                for (var i = 0; i < d.interactiveAnnotations.length; ++i) {
                    if (typeof d.interactiveAnnotations[i] !== "object")
                        throw TypeError(".WAE2E.ImageMessage.interactiveAnnotations: object expected");
                    m.interactiveAnnotations[i] = WAE2E.InteractiveAnnotation.fromObject(d.interactiveAnnotations[i], n + 1);
                }
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ImageMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.firstScanSidecar != null) {
                if (typeof d.firstScanSidecar === "string")
                    $util.base64.decode(d.firstScanSidecar, m.firstScanSidecar = $util.newBuffer($util.base64.length(d.firstScanSidecar)), 0);
                else if (d.firstScanSidecar.length >= 0)
                    m.firstScanSidecar = d.firstScanSidecar;
            }
            if (d.firstScanLength != null) {
                m.firstScanLength = d.firstScanLength >>> 0;
            }
            if (d.experimentGroupID != null) {
                m.experimentGroupID = d.experimentGroupID >>> 0;
            }
            if (d.scansSidecar != null) {
                if (typeof d.scansSidecar === "string")
                    $util.base64.decode(d.scansSidecar, m.scansSidecar = $util.newBuffer($util.base64.length(d.scansSidecar)), 0);
                else if (d.scansSidecar.length >= 0)
                    m.scansSidecar = d.scansSidecar;
            }
            if (d.scanLengths) {
                if (!Array.isArray(d.scanLengths))
                    throw TypeError(".WAE2E.ImageMessage.scanLengths: array expected");
                m.scanLengths = [];
                for (var i = 0; i < d.scanLengths.length; ++i) {
                    m.scanLengths[i] = d.scanLengths[i] >>> 0;
                }
            }
            if (d.midQualityFileSHA256 != null) {
                if (typeof d.midQualityFileSHA256 === "string")
                    $util.base64.decode(d.midQualityFileSHA256, m.midQualityFileSHA256 = $util.newBuffer($util.base64.length(d.midQualityFileSHA256)), 0);
                else if (d.midQualityFileSHA256.length >= 0)
                    m.midQualityFileSHA256 = d.midQualityFileSHA256;
            }
            if (d.midQualityFileEncSHA256 != null) {
                if (typeof d.midQualityFileEncSHA256 === "string")
                    $util.base64.decode(d.midQualityFileEncSHA256, m.midQualityFileEncSHA256 = $util.newBuffer($util.base64.length(d.midQualityFileEncSHA256)), 0);
                else if (d.midQualityFileEncSHA256.length >= 0)
                    m.midQualityFileEncSHA256 = d.midQualityFileEncSHA256;
            }
            if (d.viewOnce != null) {
                m.viewOnce = Boolean(d.viewOnce);
            }
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.staticURL != null) {
                m.staticURL = String(d.staticURL);
            }
            if (d.annotations) {
                if (!Array.isArray(d.annotations))
                    throw TypeError(".WAE2E.ImageMessage.annotations: array expected");
                m.annotations = [];
                for (var i = 0; i < d.annotations.length; ++i) {
                    if (typeof d.annotations[i] !== "object")
                        throw TypeError(".WAE2E.ImageMessage.annotations: object expected");
                    m.annotations[i] = WAE2E.InteractiveAnnotation.fromObject(d.annotations[i], n + 1);
                }
            }
            switch (d.imageSourceType) {
            default:
                if (typeof d.imageSourceType === "number") {
                    m.imageSourceType = d.imageSourceType;
                    break;
                }
                break;
            case "USER_IMAGE":
            case 0:
                m.imageSourceType = 0;
                break;
            case "AI_GENERATED":
            case 1:
                m.imageSourceType = 1;
                break;
            case "AI_MODIFIED":
            case 2:
                m.imageSourceType = 2;
                break;
            case "RASTERIZED_TEXT_STATUS":
            case 3:
                m.imageSourceType = 3;
                break;
            }
            if (d.accessibilityLabel != null) {
                m.accessibilityLabel = String(d.accessibilityLabel);
            }
            if (d.qrURL != null) {
                m.qrURL = String(d.qrURL);
            }
            return m;
        };

        ImageMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.interactiveAnnotations = [];
                d.scanLengths = [];
                d.annotations = [];
            }
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.height != null && m.hasOwnProperty("height")) {
                d.height = m.height;
                if (o.oneofs)
                    d._height = "height";
            }
            if (m.width != null && m.hasOwnProperty("width")) {
                d.width = m.width;
                if (o.oneofs)
                    d._width = "width";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.interactiveAnnotations && m.interactiveAnnotations.length) {
                d.interactiveAnnotations = [];
                for (var j = 0; j < m.interactiveAnnotations.length; ++j) {
                    d.interactiveAnnotations[j] = WAE2E.InteractiveAnnotation.toObject(m.interactiveAnnotations[j], o);
                }
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.firstScanSidecar != null && m.hasOwnProperty("firstScanSidecar")) {
                d.firstScanSidecar = o.bytes === String ? $util.base64.encode(m.firstScanSidecar, 0, m.firstScanSidecar.length) : o.bytes === Array ? Array.prototype.slice.call(m.firstScanSidecar) : m.firstScanSidecar;
                if (o.oneofs)
                    d._firstScanSidecar = "firstScanSidecar";
            }
            if (m.firstScanLength != null && m.hasOwnProperty("firstScanLength")) {
                d.firstScanLength = m.firstScanLength;
                if (o.oneofs)
                    d._firstScanLength = "firstScanLength";
            }
            if (m.experimentGroupID != null && m.hasOwnProperty("experimentGroupID")) {
                d.experimentGroupID = m.experimentGroupID;
                if (o.oneofs)
                    d._experimentGroupID = "experimentGroupID";
            }
            if (m.scansSidecar != null && m.hasOwnProperty("scansSidecar")) {
                d.scansSidecar = o.bytes === String ? $util.base64.encode(m.scansSidecar, 0, m.scansSidecar.length) : o.bytes === Array ? Array.prototype.slice.call(m.scansSidecar) : m.scansSidecar;
                if (o.oneofs)
                    d._scansSidecar = "scansSidecar";
            }
            if (m.scanLengths && m.scanLengths.length) {
                d.scanLengths = [];
                for (var j = 0; j < m.scanLengths.length; ++j) {
                    d.scanLengths[j] = m.scanLengths[j];
                }
            }
            if (m.midQualityFileSHA256 != null && m.hasOwnProperty("midQualityFileSHA256")) {
                d.midQualityFileSHA256 = o.bytes === String ? $util.base64.encode(m.midQualityFileSHA256, 0, m.midQualityFileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.midQualityFileSHA256) : m.midQualityFileSHA256;
                if (o.oneofs)
                    d._midQualityFileSHA256 = "midQualityFileSHA256";
            }
            if (m.midQualityFileEncSHA256 != null && m.hasOwnProperty("midQualityFileEncSHA256")) {
                d.midQualityFileEncSHA256 = o.bytes === String ? $util.base64.encode(m.midQualityFileEncSHA256, 0, m.midQualityFileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.midQualityFileEncSHA256) : m.midQualityFileEncSHA256;
                if (o.oneofs)
                    d._midQualityFileEncSHA256 = "midQualityFileEncSHA256";
            }
            if (m.viewOnce != null && m.hasOwnProperty("viewOnce")) {
                d.viewOnce = m.viewOnce;
                if (o.oneofs)
                    d._viewOnce = "viewOnce";
            }
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.staticURL != null && m.hasOwnProperty("staticURL")) {
                d.staticURL = m.staticURL;
                if (o.oneofs)
                    d._staticURL = "staticURL";
            }
            if (m.annotations && m.annotations.length) {
                d.annotations = [];
                for (var j = 0; j < m.annotations.length; ++j) {
                    d.annotations[j] = WAE2E.InteractiveAnnotation.toObject(m.annotations[j], o);
                }
            }
            if (m.imageSourceType != null && m.hasOwnProperty("imageSourceType")) {
                d.imageSourceType = o.enums === String ? WAE2E.ImageMessage.ImageSourceType[m.imageSourceType] === undefined ? m.imageSourceType : WAE2E.ImageMessage.ImageSourceType[m.imageSourceType] : m.imageSourceType;
                if (o.oneofs)
                    d._imageSourceType = "imageSourceType";
            }
            if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                d.accessibilityLabel = m.accessibilityLabel;
                if (o.oneofs)
                    d._accessibilityLabel = "accessibilityLabel";
            }
            if (m.qrURL != null && m.hasOwnProperty("qrURL")) {
                d.qrURL = m.qrURL;
                if (o.oneofs)
                    d._qrURL = "qrURL";
            }
            return d;
        };

        ImageMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ImageMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ImageMessage";
        };

        ImageMessage.ImageSourceType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "USER_IMAGE"] = 0;
            values[valuesById[1] = "AI_GENERATED"] = 1;
            values[valuesById[2] = "AI_MODIFIED"] = 2;
            values[valuesById[3] = "RASTERIZED_TEXT_STATUS"] = 3;
            return values;
        })();

        return ImageMessage;
    })();

    WAE2E.ContextInfo = (function() {

        function ContextInfo(p) {
            this.mentionedJID = [];
            this.groupMentions = [];
            this.statusAttributions = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ContextInfo.prototype.stanzaID = null;
        ContextInfo.prototype.participant = null;
        ContextInfo.prototype.quotedMessage = null;
        ContextInfo.prototype.remoteJID = null;
        ContextInfo.prototype.mentionedJID = $util.emptyArray;
        ContextInfo.prototype.conversionSource = null;
        ContextInfo.prototype.conversionData = null;
        ContextInfo.prototype.conversionDelaySeconds = null;
        ContextInfo.prototype.forwardingScore = null;
        ContextInfo.prototype.isForwarded = null;
        ContextInfo.prototype.quotedAd = null;
        ContextInfo.prototype.placeholderKey = null;
        ContextInfo.prototype.expiration = null;
        ContextInfo.prototype.ephemeralSettingTimestamp = null;
        ContextInfo.prototype.ephemeralSharedSecret = null;
        ContextInfo.prototype.externalAdReply = null;
        ContextInfo.prototype.entryPointConversionSource = null;
        ContextInfo.prototype.entryPointConversionApp = null;
        ContextInfo.prototype.entryPointConversionDelaySeconds = null;
        ContextInfo.prototype.disappearingMode = null;
        ContextInfo.prototype.actionLink = null;
        ContextInfo.prototype.groupSubject = null;
        ContextInfo.prototype.parentGroupJID = null;
        ContextInfo.prototype.trustBannerType = null;
        ContextInfo.prototype.trustBannerAction = null;
        ContextInfo.prototype.isSampled = null;
        ContextInfo.prototype.groupMentions = $util.emptyArray;
        ContextInfo.prototype.utm = null;
        ContextInfo.prototype.forwardedNewsletterMessageInfo = null;
        ContextInfo.prototype.businessMessageForwardInfo = null;
        ContextInfo.prototype.smbClientCampaignID = null;
        ContextInfo.prototype.smbServerCampaignID = null;
        ContextInfo.prototype.dataSharingContext = null;
        ContextInfo.prototype.alwaysShowAdAttribution = null;
        ContextInfo.prototype.featureEligibilities = null;
        ContextInfo.prototype.entryPointConversionExternalSource = null;
        ContextInfo.prototype.entryPointConversionExternalMedium = null;
        ContextInfo.prototype.ctwaSignals = null;
        ContextInfo.prototype.ctwaPayload = null;
        ContextInfo.prototype.forwardedAiBotMessageInfo = null;
        ContextInfo.prototype.statusAttributionType = null;
        ContextInfo.prototype.urlTrackingMap = null;
        ContextInfo.prototype.pairedMediaType = null;
        ContextInfo.prototype.rankingVersion = null;
        ContextInfo.prototype.memberLabel = null;
        ContextInfo.prototype.isQuestion = null;
        ContextInfo.prototype.statusSourceType = null;
        ContextInfo.prototype.statusAttributions = $util.emptyArray;
        ContextInfo.prototype.isGroupStatus = null;
        ContextInfo.prototype.forwardOrigin = null;
        ContextInfo.prototype.questionReplyQuotedMessage = null;
        ContextInfo.prototype.statusAudienceMetadata = null;
        ContextInfo.prototype.nonJIDMentions = null;
        ContextInfo.prototype.quotedType = null;
        ContextInfo.prototype.botMessageSharingInfo = null;
        ContextInfo.prototype.isSpoiler = null;
        ContextInfo.prototype.mediaDomainInfo = null;
        ContextInfo.prototype.partiallySelectedContent = null;
        ContextInfo.prototype.afterReadDuration = null;
        ContextInfo.prototype.crossAppSource = null;
        ContextInfo.prototype.businessInteractionPills = null;
        ContextInfo.prototype.posterStatusID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_stanzaID", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_remoteJID", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionData", {
            get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_conversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardingScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardingScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isForwarded", {
            get: $util.oneOfGetter($oneOfFields = ["isForwarded"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedAd", {
            get: $util.oneOfGetter($oneOfFields = ["quotedAd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_placeholderKey", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_expiration", {
            get: $util.oneOfGetter($oneOfFields = ["expiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ephemeralSharedSecret", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSharedSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_externalAdReply", {
            get: $util.oneOfGetter($oneOfFields = ["externalAdReply"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionApp", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionApp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_actionLink", {
            get: $util.oneOfGetter($oneOfFields = ["actionLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_parentGroupJID", {
            get: $util.oneOfGetter($oneOfFields = ["parentGroupJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_trustBannerType", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_trustBannerAction", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isSampled", {
            get: $util.oneOfGetter($oneOfFields = ["isSampled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_utm", {
            get: $util.oneOfGetter($oneOfFields = ["utm"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardedNewsletterMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedNewsletterMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_businessMessageForwardInfo", {
            get: $util.oneOfGetter($oneOfFields = ["businessMessageForwardInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_smbClientCampaignID", {
            get: $util.oneOfGetter($oneOfFields = ["smbClientCampaignID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_smbServerCampaignID", {
            get: $util.oneOfGetter($oneOfFields = ["smbServerCampaignID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_dataSharingContext", {
            get: $util.oneOfGetter($oneOfFields = ["dataSharingContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_alwaysShowAdAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["alwaysShowAdAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_featureEligibilities", {
            get: $util.oneOfGetter($oneOfFields = ["featureEligibilities"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalMedium", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalMedium"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ctwaSignals", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_ctwaPayload", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardedAiBotMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedAiBotMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusAttributionType", {
            get: $util.oneOfGetter($oneOfFields = ["statusAttributionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_urlTrackingMap", {
            get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_pairedMediaType", {
            get: $util.oneOfGetter($oneOfFields = ["pairedMediaType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_rankingVersion", {
            get: $util.oneOfGetter($oneOfFields = ["rankingVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isQuestion", {
            get: $util.oneOfGetter($oneOfFields = ["isQuestion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusSourceType", {
            get: $util.oneOfGetter($oneOfFields = ["statusSourceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isGroupStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_forwardOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["forwardOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_questionReplyQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_statusAudienceMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["statusAudienceMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_nonJIDMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJIDMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_quotedType", {
            get: $util.oneOfGetter($oneOfFields = ["quotedType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_botMessageSharingInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSharingInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_isSpoiler", {
            get: $util.oneOfGetter($oneOfFields = ["isSpoiler"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_mediaDomainInfo", {
            get: $util.oneOfGetter($oneOfFields = ["mediaDomainInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_partiallySelectedContent", {
            get: $util.oneOfGetter($oneOfFields = ["partiallySelectedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_crossAppSource", {
            get: $util.oneOfGetter($oneOfFields = ["crossAppSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_businessInteractionPills", {
            get: $util.oneOfGetter($oneOfFields = ["businessInteractionPills"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContextInfo.prototype, "_posterStatusID", {
            get: $util.oneOfGetter($oneOfFields = ["posterStatusID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ContextInfo.create = function create(properties) {
            return new ContextInfo(properties);
        };

        ContextInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                w.uint32(10).string(m.stanzaID);
            if (m.participant != null && Object.hasOwnProperty.call(m, "participant"))
                w.uint32(18).string(m.participant);
            if (m.quotedMessage != null && Object.hasOwnProperty.call(m, "quotedMessage"))
                WAE2E.Message.encode(m.quotedMessage, w.uint32(26).fork()).ldelim();
            if (m.remoteJID != null && Object.hasOwnProperty.call(m, "remoteJID"))
                w.uint32(34).string(m.remoteJID);
            if (m.mentionedJID != null && m.mentionedJID.length) {
                for (var i = 0; i < m.mentionedJID.length; ++i)
                    w.uint32(122).string(m.mentionedJID[i]);
            }
            if (m.conversionSource != null && Object.hasOwnProperty.call(m, "conversionSource"))
                w.uint32(146).string(m.conversionSource);
            if (m.conversionData != null && Object.hasOwnProperty.call(m, "conversionData"))
                w.uint32(154).bytes(m.conversionData);
            if (m.conversionDelaySeconds != null && Object.hasOwnProperty.call(m, "conversionDelaySeconds"))
                w.uint32(160).uint32(m.conversionDelaySeconds);
            if (m.forwardingScore != null && Object.hasOwnProperty.call(m, "forwardingScore"))
                w.uint32(168).uint32(m.forwardingScore);
            if (m.isForwarded != null && Object.hasOwnProperty.call(m, "isForwarded"))
                w.uint32(176).bool(m.isForwarded);
            if (m.quotedAd != null && Object.hasOwnProperty.call(m, "quotedAd"))
                WAE2E.ContextInfo.AdReplyInfo.encode(m.quotedAd, w.uint32(186).fork()).ldelim();
            if (m.placeholderKey != null && Object.hasOwnProperty.call(m, "placeholderKey"))
                WACommon.MessageKey.encode(m.placeholderKey, w.uint32(194).fork()).ldelim();
            if (m.expiration != null && Object.hasOwnProperty.call(m, "expiration"))
                w.uint32(200).uint32(m.expiration);
            if (m.ephemeralSettingTimestamp != null && Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp"))
                w.uint32(208).int64(m.ephemeralSettingTimestamp);
            if (m.ephemeralSharedSecret != null && Object.hasOwnProperty.call(m, "ephemeralSharedSecret"))
                w.uint32(218).bytes(m.ephemeralSharedSecret);
            if (m.externalAdReply != null && Object.hasOwnProperty.call(m, "externalAdReply"))
                WAE2E.ContextInfo.ExternalAdReplyInfo.encode(m.externalAdReply, w.uint32(226).fork()).ldelim();
            if (m.entryPointConversionSource != null && Object.hasOwnProperty.call(m, "entryPointConversionSource"))
                w.uint32(234).string(m.entryPointConversionSource);
            if (m.entryPointConversionApp != null && Object.hasOwnProperty.call(m, "entryPointConversionApp"))
                w.uint32(242).string(m.entryPointConversionApp);
            if (m.entryPointConversionDelaySeconds != null && Object.hasOwnProperty.call(m, "entryPointConversionDelaySeconds"))
                w.uint32(248).uint32(m.entryPointConversionDelaySeconds);
            if (m.disappearingMode != null && Object.hasOwnProperty.call(m, "disappearingMode"))
                WAE2E.DisappearingMode.encode(m.disappearingMode, w.uint32(258).fork()).ldelim();
            if (m.actionLink != null && Object.hasOwnProperty.call(m, "actionLink"))
                WAE2E.ActionLink.encode(m.actionLink, w.uint32(266).fork()).ldelim();
            if (m.groupSubject != null && Object.hasOwnProperty.call(m, "groupSubject"))
                w.uint32(274).string(m.groupSubject);
            if (m.parentGroupJID != null && Object.hasOwnProperty.call(m, "parentGroupJID"))
                w.uint32(282).string(m.parentGroupJID);
            if (m.trustBannerType != null && Object.hasOwnProperty.call(m, "trustBannerType"))
                w.uint32(298).string(m.trustBannerType);
            if (m.trustBannerAction != null && Object.hasOwnProperty.call(m, "trustBannerAction"))
                w.uint32(304).uint32(m.trustBannerAction);
            if (m.isSampled != null && Object.hasOwnProperty.call(m, "isSampled"))
                w.uint32(312).bool(m.isSampled);
            if (m.groupMentions != null && m.groupMentions.length) {
                for (var i = 0; i < m.groupMentions.length; ++i)
                    WAE2E.GroupMention.encode(m.groupMentions[i], w.uint32(322).fork()).ldelim();
            }
            if (m.utm != null && Object.hasOwnProperty.call(m, "utm"))
                WAE2E.ContextInfo.UTMInfo.encode(m.utm, w.uint32(330).fork()).ldelim();
            if (m.forwardedNewsletterMessageInfo != null && Object.hasOwnProperty.call(m, "forwardedNewsletterMessageInfo"))
                WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.encode(m.forwardedNewsletterMessageInfo, w.uint32(346).fork()).ldelim();
            if (m.businessMessageForwardInfo != null && Object.hasOwnProperty.call(m, "businessMessageForwardInfo"))
                WAE2E.ContextInfo.BusinessMessageForwardInfo.encode(m.businessMessageForwardInfo, w.uint32(354).fork()).ldelim();
            if (m.smbClientCampaignID != null && Object.hasOwnProperty.call(m, "smbClientCampaignID"))
                w.uint32(362).string(m.smbClientCampaignID);
            if (m.smbServerCampaignID != null && Object.hasOwnProperty.call(m, "smbServerCampaignID"))
                w.uint32(370).string(m.smbServerCampaignID);
            if (m.dataSharingContext != null && Object.hasOwnProperty.call(m, "dataSharingContext"))
                WAE2E.ContextInfo.DataSharingContext.encode(m.dataSharingContext, w.uint32(378).fork()).ldelim();
            if (m.alwaysShowAdAttribution != null && Object.hasOwnProperty.call(m, "alwaysShowAdAttribution"))
                w.uint32(384).bool(m.alwaysShowAdAttribution);
            if (m.featureEligibilities != null && Object.hasOwnProperty.call(m, "featureEligibilities"))
                WAE2E.ContextInfo.FeatureEligibilities.encode(m.featureEligibilities, w.uint32(394).fork()).ldelim();
            if (m.entryPointConversionExternalSource != null && Object.hasOwnProperty.call(m, "entryPointConversionExternalSource"))
                w.uint32(402).string(m.entryPointConversionExternalSource);
            if (m.entryPointConversionExternalMedium != null && Object.hasOwnProperty.call(m, "entryPointConversionExternalMedium"))
                w.uint32(410).string(m.entryPointConversionExternalMedium);
            if (m.ctwaSignals != null && Object.hasOwnProperty.call(m, "ctwaSignals"))
                w.uint32(434).string(m.ctwaSignals);
            if (m.ctwaPayload != null && Object.hasOwnProperty.call(m, "ctwaPayload"))
                w.uint32(442).bytes(m.ctwaPayload);
            if (m.forwardedAiBotMessageInfo != null && Object.hasOwnProperty.call(m, "forwardedAiBotMessageInfo"))
                $root.WAAICommon.ForwardedAIBotMessageInfo.encode(m.forwardedAiBotMessageInfo, w.uint32(450).fork()).ldelim();
            if (m.statusAttributionType != null && Object.hasOwnProperty.call(m, "statusAttributionType"))
                w.uint32(456).int32(m.statusAttributionType);
            if (m.urlTrackingMap != null && Object.hasOwnProperty.call(m, "urlTrackingMap"))
                WAE2E.UrlTrackingMap.encode(m.urlTrackingMap, w.uint32(466).fork()).ldelim();
            if (m.pairedMediaType != null && Object.hasOwnProperty.call(m, "pairedMediaType"))
                w.uint32(472).int32(m.pairedMediaType);
            if (m.rankingVersion != null && Object.hasOwnProperty.call(m, "rankingVersion"))
                w.uint32(480).uint32(m.rankingVersion);
            if (m.memberLabel != null && Object.hasOwnProperty.call(m, "memberLabel"))
                WAE2E.MemberLabel.encode(m.memberLabel, w.uint32(498).fork()).ldelim();
            if (m.isQuestion != null && Object.hasOwnProperty.call(m, "isQuestion"))
                w.uint32(504).bool(m.isQuestion);
            if (m.statusSourceType != null && Object.hasOwnProperty.call(m, "statusSourceType"))
                w.uint32(512).int32(m.statusSourceType);
            if (m.statusAttributions != null && m.statusAttributions.length) {
                for (var i = 0; i < m.statusAttributions.length; ++i)
                    $root.WAStatusAttributions.StatusAttribution.encode(m.statusAttributions[i], w.uint32(522).fork()).ldelim();
            }
            if (m.isGroupStatus != null && Object.hasOwnProperty.call(m, "isGroupStatus"))
                w.uint32(528).bool(m.isGroupStatus);
            if (m.forwardOrigin != null && Object.hasOwnProperty.call(m, "forwardOrigin"))
                w.uint32(536).int32(m.forwardOrigin);
            if (m.questionReplyQuotedMessage != null && Object.hasOwnProperty.call(m, "questionReplyQuotedMessage"))
                WAE2E.ContextInfo.QuestionReplyQuotedMessage.encode(m.questionReplyQuotedMessage, w.uint32(546).fork()).ldelim();
            if (m.statusAudienceMetadata != null && Object.hasOwnProperty.call(m, "statusAudienceMetadata"))
                WAE2E.ContextInfo.StatusAudienceMetadata.encode(m.statusAudienceMetadata, w.uint32(554).fork()).ldelim();
            if (m.nonJIDMentions != null && Object.hasOwnProperty.call(m, "nonJIDMentions"))
                w.uint32(560).uint32(m.nonJIDMentions);
            if (m.quotedType != null && Object.hasOwnProperty.call(m, "quotedType"))
                w.uint32(568).int32(m.quotedType);
            if (m.botMessageSharingInfo != null && Object.hasOwnProperty.call(m, "botMessageSharingInfo"))
                $root.WAAICommon.BotMessageSharingInfo.encode(m.botMessageSharingInfo, w.uint32(578).fork()).ldelim();
            if (m.isSpoiler != null && Object.hasOwnProperty.call(m, "isSpoiler"))
                w.uint32(584).bool(m.isSpoiler);
            if (m.mediaDomainInfo != null && Object.hasOwnProperty.call(m, "mediaDomainInfo"))
                WAE2E.MediaDomainInfo.encode(m.mediaDomainInfo, w.uint32(594).fork()).ldelim();
            if (m.partiallySelectedContent != null && Object.hasOwnProperty.call(m, "partiallySelectedContent"))
                WAE2E.ContextInfo.PartiallySelectedContent.encode(m.partiallySelectedContent, w.uint32(602).fork()).ldelim();
            if (m.afterReadDuration != null && Object.hasOwnProperty.call(m, "afterReadDuration"))
                w.uint32(608).uint32(m.afterReadDuration);
            if (m.crossAppSource != null && Object.hasOwnProperty.call(m, "crossAppSource"))
                w.uint32(616).int32(m.crossAppSource);
            if (m.businessInteractionPills != null && Object.hasOwnProperty.call(m, "businessInteractionPills"))
                WAE2E.ContextInfo.BusinessInteractionPills.encode(m.businessInteractionPills, w.uint32(626).fork()).ldelim();
            if (m.posterStatusID != null && Object.hasOwnProperty.call(m, "posterStatusID"))
                w.uint32(634).string(m.posterStatusID);
            return w;
        };

        ContextInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stanzaID = r.string();
                        break;
                    }
                case 2: {
                        m.participant = r.string();
                        break;
                    }
                case 3: {
                        m.quotedMessage = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.remoteJID = r.string();
                        break;
                    }
                case 15: {
                        if (!(m.mentionedJID && m.mentionedJID.length))
                            m.mentionedJID = [];
                        m.mentionedJID.push(r.string());
                        break;
                    }
                case 18: {
                        m.conversionSource = r.string();
                        break;
                    }
                case 19: {
                        m.conversionData = r.bytes();
                        break;
                    }
                case 20: {
                        m.conversionDelaySeconds = r.uint32();
                        break;
                    }
                case 21: {
                        m.forwardingScore = r.uint32();
                        break;
                    }
                case 22: {
                        m.isForwarded = r.bool();
                        break;
                    }
                case 23: {
                        m.quotedAd = WAE2E.ContextInfo.AdReplyInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 24: {
                        m.placeholderKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 25: {
                        m.expiration = r.uint32();
                        break;
                    }
                case 26: {
                        m.ephemeralSettingTimestamp = r.int64();
                        break;
                    }
                case 27: {
                        m.ephemeralSharedSecret = r.bytes();
                        break;
                    }
                case 28: {
                        m.externalAdReply = WAE2E.ContextInfo.ExternalAdReplyInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 29: {
                        m.entryPointConversionSource = r.string();
                        break;
                    }
                case 30: {
                        m.entryPointConversionApp = r.string();
                        break;
                    }
                case 31: {
                        m.entryPointConversionDelaySeconds = r.uint32();
                        break;
                    }
                case 32: {
                        m.disappearingMode = WAE2E.DisappearingMode.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 33: {
                        m.actionLink = WAE2E.ActionLink.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 34: {
                        m.groupSubject = r.string();
                        break;
                    }
                case 35: {
                        m.parentGroupJID = r.string();
                        break;
                    }
                case 37: {
                        m.trustBannerType = r.string();
                        break;
                    }
                case 38: {
                        m.trustBannerAction = r.uint32();
                        break;
                    }
                case 39: {
                        m.isSampled = r.bool();
                        break;
                    }
                case 40: {
                        if (!(m.groupMentions && m.groupMentions.length))
                            m.groupMentions = [];
                        m.groupMentions.push(WAE2E.GroupMention.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 41: {
                        m.utm = WAE2E.ContextInfo.UTMInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 43: {
                        m.forwardedNewsletterMessageInfo = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 44: {
                        m.businessMessageForwardInfo = WAE2E.ContextInfo.BusinessMessageForwardInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 45: {
                        m.smbClientCampaignID = r.string();
                        break;
                    }
                case 46: {
                        m.smbServerCampaignID = r.string();
                        break;
                    }
                case 47: {
                        m.dataSharingContext = WAE2E.ContextInfo.DataSharingContext.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 48: {
                        m.alwaysShowAdAttribution = r.bool();
                        break;
                    }
                case 49: {
                        m.featureEligibilities = WAE2E.ContextInfo.FeatureEligibilities.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 50: {
                        m.entryPointConversionExternalSource = r.string();
                        break;
                    }
                case 51: {
                        m.entryPointConversionExternalMedium = r.string();
                        break;
                    }
                case 54: {
                        m.ctwaSignals = r.string();
                        break;
                    }
                case 55: {
                        m.ctwaPayload = r.bytes();
                        break;
                    }
                case 56: {
                        m.forwardedAiBotMessageInfo = $root.WAAICommon.ForwardedAIBotMessageInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 57: {
                        m.statusAttributionType = r.int32();
                        break;
                    }
                case 58: {
                        m.urlTrackingMap = WAE2E.UrlTrackingMap.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 59: {
                        m.pairedMediaType = r.int32();
                        break;
                    }
                case 60: {
                        m.rankingVersion = r.uint32();
                        break;
                    }
                case 62: {
                        m.memberLabel = WAE2E.MemberLabel.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 63: {
                        m.isQuestion = r.bool();
                        break;
                    }
                case 64: {
                        m.statusSourceType = r.int32();
                        break;
                    }
                case 65: {
                        if (!(m.statusAttributions && m.statusAttributions.length))
                            m.statusAttributions = [];
                        m.statusAttributions.push($root.WAStatusAttributions.StatusAttribution.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 66: {
                        m.isGroupStatus = r.bool();
                        break;
                    }
                case 67: {
                        m.forwardOrigin = r.int32();
                        break;
                    }
                case 68: {
                        m.questionReplyQuotedMessage = WAE2E.ContextInfo.QuestionReplyQuotedMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 69: {
                        m.statusAudienceMetadata = WAE2E.ContextInfo.StatusAudienceMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 70: {
                        m.nonJIDMentions = r.uint32();
                        break;
                    }
                case 71: {
                        m.quotedType = r.int32();
                        break;
                    }
                case 72: {
                        m.botMessageSharingInfo = $root.WAAICommon.BotMessageSharingInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 73: {
                        m.isSpoiler = r.bool();
                        break;
                    }
                case 74: {
                        m.mediaDomainInfo = WAE2E.MediaDomainInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 75: {
                        m.partiallySelectedContent = WAE2E.ContextInfo.PartiallySelectedContent.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 76: {
                        m.afterReadDuration = r.uint32();
                        break;
                    }
                case 77: {
                        m.crossAppSource = r.int32();
                        break;
                    }
                case 78: {
                        m.businessInteractionPills = WAE2E.ContextInfo.BusinessInteractionPills.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 79: {
                        m.posterStatusID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ContextInfo.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ContextInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ContextInfo();
            if (d.stanzaID != null) {
                m.stanzaID = String(d.stanzaID);
            }
            if (d.participant != null) {
                m.participant = String(d.participant);
            }
            if (d.quotedMessage != null) {
                if (typeof d.quotedMessage !== "object")
                    throw TypeError(".WAE2E.ContextInfo.quotedMessage: object expected");
                m.quotedMessage = WAE2E.Message.fromObject(d.quotedMessage, n + 1);
            }
            if (d.remoteJID != null) {
                m.remoteJID = String(d.remoteJID);
            }
            if (d.mentionedJID) {
                if (!Array.isArray(d.mentionedJID))
                    throw TypeError(".WAE2E.ContextInfo.mentionedJID: array expected");
                m.mentionedJID = [];
                for (var i = 0; i < d.mentionedJID.length; ++i) {
                    m.mentionedJID[i] = String(d.mentionedJID[i]);
                }
            }
            if (d.conversionSource != null) {
                m.conversionSource = String(d.conversionSource);
            }
            if (d.conversionData != null) {
                if (typeof d.conversionData === "string")
                    $util.base64.decode(d.conversionData, m.conversionData = $util.newBuffer($util.base64.length(d.conversionData)), 0);
                else if (d.conversionData.length >= 0)
                    m.conversionData = d.conversionData;
            }
            if (d.conversionDelaySeconds != null) {
                m.conversionDelaySeconds = d.conversionDelaySeconds >>> 0;
            }
            if (d.forwardingScore != null) {
                m.forwardingScore = d.forwardingScore >>> 0;
            }
            if (d.isForwarded != null) {
                m.isForwarded = Boolean(d.isForwarded);
            }
            if (d.quotedAd != null) {
                if (typeof d.quotedAd !== "object")
                    throw TypeError(".WAE2E.ContextInfo.quotedAd: object expected");
                m.quotedAd = WAE2E.ContextInfo.AdReplyInfo.fromObject(d.quotedAd, n + 1);
            }
            if (d.placeholderKey != null) {
                if (typeof d.placeholderKey !== "object")
                    throw TypeError(".WAE2E.ContextInfo.placeholderKey: object expected");
                m.placeholderKey = WACommon.MessageKey.fromObject(d.placeholderKey, n + 1);
            }
            if (d.expiration != null) {
                m.expiration = d.expiration >>> 0;
            }
            if (d.ephemeralSettingTimestamp != null) {
                if ($util.Long)
                    (m.ephemeralSettingTimestamp = $util.Long.fromValue(d.ephemeralSettingTimestamp)).unsigned = false;
                else if (typeof d.ephemeralSettingTimestamp === "string")
                    m.ephemeralSettingTimestamp = parseInt(d.ephemeralSettingTimestamp, 10);
                else if (typeof d.ephemeralSettingTimestamp === "number")
                    m.ephemeralSettingTimestamp = d.ephemeralSettingTimestamp;
                else if (typeof d.ephemeralSettingTimestamp === "object")
                    m.ephemeralSettingTimestamp = new $util.LongBits(d.ephemeralSettingTimestamp.low >>> 0, d.ephemeralSettingTimestamp.high >>> 0).toNumber();
            }
            if (d.ephemeralSharedSecret != null) {
                if (typeof d.ephemeralSharedSecret === "string")
                    $util.base64.decode(d.ephemeralSharedSecret, m.ephemeralSharedSecret = $util.newBuffer($util.base64.length(d.ephemeralSharedSecret)), 0);
                else if (d.ephemeralSharedSecret.length >= 0)
                    m.ephemeralSharedSecret = d.ephemeralSharedSecret;
            }
            if (d.externalAdReply != null) {
                if (typeof d.externalAdReply !== "object")
                    throw TypeError(".WAE2E.ContextInfo.externalAdReply: object expected");
                m.externalAdReply = WAE2E.ContextInfo.ExternalAdReplyInfo.fromObject(d.externalAdReply, n + 1);
            }
            if (d.entryPointConversionSource != null) {
                m.entryPointConversionSource = String(d.entryPointConversionSource);
            }
            if (d.entryPointConversionApp != null) {
                m.entryPointConversionApp = String(d.entryPointConversionApp);
            }
            if (d.entryPointConversionDelaySeconds != null) {
                m.entryPointConversionDelaySeconds = d.entryPointConversionDelaySeconds >>> 0;
            }
            if (d.disappearingMode != null) {
                if (typeof d.disappearingMode !== "object")
                    throw TypeError(".WAE2E.ContextInfo.disappearingMode: object expected");
                m.disappearingMode = WAE2E.DisappearingMode.fromObject(d.disappearingMode, n + 1);
            }
            if (d.actionLink != null) {
                if (typeof d.actionLink !== "object")
                    throw TypeError(".WAE2E.ContextInfo.actionLink: object expected");
                m.actionLink = WAE2E.ActionLink.fromObject(d.actionLink, n + 1);
            }
            if (d.groupSubject != null) {
                m.groupSubject = String(d.groupSubject);
            }
            if (d.parentGroupJID != null) {
                m.parentGroupJID = String(d.parentGroupJID);
            }
            if (d.trustBannerType != null) {
                m.trustBannerType = String(d.trustBannerType);
            }
            if (d.trustBannerAction != null) {
                m.trustBannerAction = d.trustBannerAction >>> 0;
            }
            if (d.isSampled != null) {
                m.isSampled = Boolean(d.isSampled);
            }
            if (d.groupMentions) {
                if (!Array.isArray(d.groupMentions))
                    throw TypeError(".WAE2E.ContextInfo.groupMentions: array expected");
                m.groupMentions = [];
                for (var i = 0; i < d.groupMentions.length; ++i) {
                    if (typeof d.groupMentions[i] !== "object")
                        throw TypeError(".WAE2E.ContextInfo.groupMentions: object expected");
                    m.groupMentions[i] = WAE2E.GroupMention.fromObject(d.groupMentions[i], n + 1);
                }
            }
            if (d.utm != null) {
                if (typeof d.utm !== "object")
                    throw TypeError(".WAE2E.ContextInfo.utm: object expected");
                m.utm = WAE2E.ContextInfo.UTMInfo.fromObject(d.utm, n + 1);
            }
            if (d.forwardedNewsletterMessageInfo != null) {
                if (typeof d.forwardedNewsletterMessageInfo !== "object")
                    throw TypeError(".WAE2E.ContextInfo.forwardedNewsletterMessageInfo: object expected");
                m.forwardedNewsletterMessageInfo = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(d.forwardedNewsletterMessageInfo, n + 1);
            }
            if (d.businessMessageForwardInfo != null) {
                if (typeof d.businessMessageForwardInfo !== "object")
                    throw TypeError(".WAE2E.ContextInfo.businessMessageForwardInfo: object expected");
                m.businessMessageForwardInfo = WAE2E.ContextInfo.BusinessMessageForwardInfo.fromObject(d.businessMessageForwardInfo, n + 1);
            }
            if (d.smbClientCampaignID != null) {
                m.smbClientCampaignID = String(d.smbClientCampaignID);
            }
            if (d.smbServerCampaignID != null) {
                m.smbServerCampaignID = String(d.smbServerCampaignID);
            }
            if (d.dataSharingContext != null) {
                if (typeof d.dataSharingContext !== "object")
                    throw TypeError(".WAE2E.ContextInfo.dataSharingContext: object expected");
                m.dataSharingContext = WAE2E.ContextInfo.DataSharingContext.fromObject(d.dataSharingContext, n + 1);
            }
            if (d.alwaysShowAdAttribution != null) {
                m.alwaysShowAdAttribution = Boolean(d.alwaysShowAdAttribution);
            }
            if (d.featureEligibilities != null) {
                if (typeof d.featureEligibilities !== "object")
                    throw TypeError(".WAE2E.ContextInfo.featureEligibilities: object expected");
                m.featureEligibilities = WAE2E.ContextInfo.FeatureEligibilities.fromObject(d.featureEligibilities, n + 1);
            }
            if (d.entryPointConversionExternalSource != null) {
                m.entryPointConversionExternalSource = String(d.entryPointConversionExternalSource);
            }
            if (d.entryPointConversionExternalMedium != null) {
                m.entryPointConversionExternalMedium = String(d.entryPointConversionExternalMedium);
            }
            if (d.ctwaSignals != null) {
                m.ctwaSignals = String(d.ctwaSignals);
            }
            if (d.ctwaPayload != null) {
                if (typeof d.ctwaPayload === "string")
                    $util.base64.decode(d.ctwaPayload, m.ctwaPayload = $util.newBuffer($util.base64.length(d.ctwaPayload)), 0);
                else if (d.ctwaPayload.length >= 0)
                    m.ctwaPayload = d.ctwaPayload;
            }
            if (d.forwardedAiBotMessageInfo != null) {
                if (typeof d.forwardedAiBotMessageInfo !== "object")
                    throw TypeError(".WAE2E.ContextInfo.forwardedAiBotMessageInfo: object expected");
                m.forwardedAiBotMessageInfo = $root.WAAICommon.ForwardedAIBotMessageInfo.fromObject(d.forwardedAiBotMessageInfo, n + 1);
            }
            switch (d.statusAttributionType) {
            default:
                if (typeof d.statusAttributionType === "number") {
                    m.statusAttributionType = d.statusAttributionType;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.statusAttributionType = 0;
                break;
            case "RESHARED_FROM_MENTION":
            case 1:
                m.statusAttributionType = 1;
                break;
            case "RESHARED_FROM_POST":
            case 2:
                m.statusAttributionType = 2;
                break;
            case "RESHARED_FROM_POST_MANY_TIMES":
            case 3:
                m.statusAttributionType = 3;
                break;
            case "FORWARDED_FROM_STATUS":
            case 4:
                m.statusAttributionType = 4;
                break;
            }
            if (d.urlTrackingMap != null) {
                if (typeof d.urlTrackingMap !== "object")
                    throw TypeError(".WAE2E.ContextInfo.urlTrackingMap: object expected");
                m.urlTrackingMap = WAE2E.UrlTrackingMap.fromObject(d.urlTrackingMap, n + 1);
            }
            switch (d.pairedMediaType) {
            default:
                if (typeof d.pairedMediaType === "number") {
                    m.pairedMediaType = d.pairedMediaType;
                    break;
                }
                break;
            case "NOT_PAIRED_MEDIA":
            case 0:
                m.pairedMediaType = 0;
                break;
            case "SD_VIDEO_PARENT":
            case 1:
                m.pairedMediaType = 1;
                break;
            case "HD_VIDEO_CHILD":
            case 2:
                m.pairedMediaType = 2;
                break;
            case "SD_IMAGE_PARENT":
            case 3:
                m.pairedMediaType = 3;
                break;
            case "HD_IMAGE_CHILD":
            case 4:
                m.pairedMediaType = 4;
                break;
            case "MOTION_PHOTO_PARENT":
            case 5:
                m.pairedMediaType = 5;
                break;
            case "MOTION_PHOTO_CHILD":
            case 6:
                m.pairedMediaType = 6;
                break;
            case "HEVC_VIDEO_PARENT":
            case 7:
                m.pairedMediaType = 7;
                break;
            case "HEVC_VIDEO_CHILD":
            case 8:
                m.pairedMediaType = 8;
                break;
            }
            if (d.rankingVersion != null) {
                m.rankingVersion = d.rankingVersion >>> 0;
            }
            if (d.memberLabel != null) {
                if (typeof d.memberLabel !== "object")
                    throw TypeError(".WAE2E.ContextInfo.memberLabel: object expected");
                m.memberLabel = WAE2E.MemberLabel.fromObject(d.memberLabel, n + 1);
            }
            if (d.isQuestion != null) {
                m.isQuestion = Boolean(d.isQuestion);
            }
            switch (d.statusSourceType) {
            default:
                if (typeof d.statusSourceType === "number") {
                    m.statusSourceType = d.statusSourceType;
                    break;
                }
                break;
            case "IMAGE":
            case 0:
                m.statusSourceType = 0;
                break;
            case "VIDEO":
            case 1:
                m.statusSourceType = 1;
                break;
            case "GIF":
            case 2:
                m.statusSourceType = 2;
                break;
            case "AUDIO":
            case 3:
                m.statusSourceType = 3;
                break;
            case "TEXT":
            case 4:
                m.statusSourceType = 4;
                break;
            case "MUSIC_STANDALONE":
            case 5:
                m.statusSourceType = 5;
                break;
            }
            if (d.statusAttributions) {
                if (!Array.isArray(d.statusAttributions))
                    throw TypeError(".WAE2E.ContextInfo.statusAttributions: array expected");
                m.statusAttributions = [];
                for (var i = 0; i < d.statusAttributions.length; ++i) {
                    if (typeof d.statusAttributions[i] !== "object")
                        throw TypeError(".WAE2E.ContextInfo.statusAttributions: object expected");
                    m.statusAttributions[i] = $root.WAStatusAttributions.StatusAttribution.fromObject(d.statusAttributions[i], n + 1);
                }
            }
            if (d.isGroupStatus != null) {
                m.isGroupStatus = Boolean(d.isGroupStatus);
            }
            switch (d.forwardOrigin) {
            default:
                if (typeof d.forwardOrigin === "number") {
                    m.forwardOrigin = d.forwardOrigin;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.forwardOrigin = 0;
                break;
            case "CHAT":
            case 1:
                m.forwardOrigin = 1;
                break;
            case "STATUS":
            case 2:
                m.forwardOrigin = 2;
                break;
            case "CHANNELS":
            case 3:
                m.forwardOrigin = 3;
                break;
            case "META_AI":
            case 4:
                m.forwardOrigin = 4;
                break;
            case "UGC":
            case 5:
                m.forwardOrigin = 5;
                break;
            }
            if (d.questionReplyQuotedMessage != null) {
                if (typeof d.questionReplyQuotedMessage !== "object")
                    throw TypeError(".WAE2E.ContextInfo.questionReplyQuotedMessage: object expected");
                m.questionReplyQuotedMessage = WAE2E.ContextInfo.QuestionReplyQuotedMessage.fromObject(d.questionReplyQuotedMessage, n + 1);
            }
            if (d.statusAudienceMetadata != null) {
                if (typeof d.statusAudienceMetadata !== "object")
                    throw TypeError(".WAE2E.ContextInfo.statusAudienceMetadata: object expected");
                m.statusAudienceMetadata = WAE2E.ContextInfo.StatusAudienceMetadata.fromObject(d.statusAudienceMetadata, n + 1);
            }
            if (d.nonJIDMentions != null) {
                m.nonJIDMentions = d.nonJIDMentions >>> 0;
            }
            switch (d.quotedType) {
            default:
                if (typeof d.quotedType === "number") {
                    m.quotedType = d.quotedType;
                    break;
                }
                break;
            case "EXPLICIT":
            case 0:
                m.quotedType = 0;
                break;
            case "AUTO":
            case 1:
                m.quotedType = 1;
                break;
            }
            if (d.botMessageSharingInfo != null) {
                if (typeof d.botMessageSharingInfo !== "object")
                    throw TypeError(".WAE2E.ContextInfo.botMessageSharingInfo: object expected");
                m.botMessageSharingInfo = $root.WAAICommon.BotMessageSharingInfo.fromObject(d.botMessageSharingInfo, n + 1);
            }
            if (d.isSpoiler != null) {
                m.isSpoiler = Boolean(d.isSpoiler);
            }
            if (d.mediaDomainInfo != null) {
                if (typeof d.mediaDomainInfo !== "object")
                    throw TypeError(".WAE2E.ContextInfo.mediaDomainInfo: object expected");
                m.mediaDomainInfo = WAE2E.MediaDomainInfo.fromObject(d.mediaDomainInfo, n + 1);
            }
            if (d.partiallySelectedContent != null) {
                if (typeof d.partiallySelectedContent !== "object")
                    throw TypeError(".WAE2E.ContextInfo.partiallySelectedContent: object expected");
                m.partiallySelectedContent = WAE2E.ContextInfo.PartiallySelectedContent.fromObject(d.partiallySelectedContent, n + 1);
            }
            if (d.afterReadDuration != null) {
                m.afterReadDuration = d.afterReadDuration >>> 0;
            }
            switch (d.crossAppSource) {
            default:
                if (typeof d.crossAppSource === "number") {
                    m.crossAppSource = d.crossAppSource;
                    break;
                }
                break;
            case "CROSS_APP_SOURCE_UNKNOWN":
            case 0:
                m.crossAppSource = 0;
                break;
            case "CROSS_APP_SOURCE_INSTAGRAM":
            case 1:
                m.crossAppSource = 1;
                break;
            case "CROSS_APP_SOURCE_FACEBOOK":
            case 2:
                m.crossAppSource = 2;
                break;
            }
            if (d.businessInteractionPills != null) {
                if (typeof d.businessInteractionPills !== "object")
                    throw TypeError(".WAE2E.ContextInfo.businessInteractionPills: object expected");
                m.businessInteractionPills = WAE2E.ContextInfo.BusinessInteractionPills.fromObject(d.businessInteractionPills, n + 1);
            }
            if (d.posterStatusID != null) {
                m.posterStatusID = String(d.posterStatusID);
            }
            return m;
        };

        ContextInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.mentionedJID = [];
                d.groupMentions = [];
                d.statusAttributions = [];
            }
            if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                d.stanzaID = m.stanzaID;
                if (o.oneofs)
                    d._stanzaID = "stanzaID";
            }
            if (m.participant != null && m.hasOwnProperty("participant")) {
                d.participant = m.participant;
                if (o.oneofs)
                    d._participant = "participant";
            }
            if (m.quotedMessage != null && m.hasOwnProperty("quotedMessage")) {
                d.quotedMessage = WAE2E.Message.toObject(m.quotedMessage, o);
                if (o.oneofs)
                    d._quotedMessage = "quotedMessage";
            }
            if (m.remoteJID != null && m.hasOwnProperty("remoteJID")) {
                d.remoteJID = m.remoteJID;
                if (o.oneofs)
                    d._remoteJID = "remoteJID";
            }
            if (m.mentionedJID && m.mentionedJID.length) {
                d.mentionedJID = [];
                for (var j = 0; j < m.mentionedJID.length; ++j) {
                    d.mentionedJID[j] = m.mentionedJID[j];
                }
            }
            if (m.conversionSource != null && m.hasOwnProperty("conversionSource")) {
                d.conversionSource = m.conversionSource;
                if (o.oneofs)
                    d._conversionSource = "conversionSource";
            }
            if (m.conversionData != null && m.hasOwnProperty("conversionData")) {
                d.conversionData = o.bytes === String ? $util.base64.encode(m.conversionData, 0, m.conversionData.length) : o.bytes === Array ? Array.prototype.slice.call(m.conversionData) : m.conversionData;
                if (o.oneofs)
                    d._conversionData = "conversionData";
            }
            if (m.conversionDelaySeconds != null && m.hasOwnProperty("conversionDelaySeconds")) {
                d.conversionDelaySeconds = m.conversionDelaySeconds;
                if (o.oneofs)
                    d._conversionDelaySeconds = "conversionDelaySeconds";
            }
            if (m.forwardingScore != null && m.hasOwnProperty("forwardingScore")) {
                d.forwardingScore = m.forwardingScore;
                if (o.oneofs)
                    d._forwardingScore = "forwardingScore";
            }
            if (m.isForwarded != null && m.hasOwnProperty("isForwarded")) {
                d.isForwarded = m.isForwarded;
                if (o.oneofs)
                    d._isForwarded = "isForwarded";
            }
            if (m.quotedAd != null && m.hasOwnProperty("quotedAd")) {
                d.quotedAd = WAE2E.ContextInfo.AdReplyInfo.toObject(m.quotedAd, o);
                if (o.oneofs)
                    d._quotedAd = "quotedAd";
            }
            if (m.placeholderKey != null && m.hasOwnProperty("placeholderKey")) {
                d.placeholderKey = WACommon.MessageKey.toObject(m.placeholderKey, o);
                if (o.oneofs)
                    d._placeholderKey = "placeholderKey";
            }
            if (m.expiration != null && m.hasOwnProperty("expiration")) {
                d.expiration = m.expiration;
                if (o.oneofs)
                    d._expiration = "expiration";
            }
            if (m.ephemeralSettingTimestamp != null && m.hasOwnProperty("ephemeralSettingTimestamp")) {
                if (typeof m.ephemeralSettingTimestamp === "number")
                    d.ephemeralSettingTimestamp = o.longs === String ? String(m.ephemeralSettingTimestamp) : m.ephemeralSettingTimestamp;
                else
                    d.ephemeralSettingTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.ephemeralSettingTimestamp) : o.longs === Number ? new $util.LongBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0).toNumber() : m.ephemeralSettingTimestamp;
                if (o.oneofs)
                    d._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
            }
            if (m.ephemeralSharedSecret != null && m.hasOwnProperty("ephemeralSharedSecret")) {
                d.ephemeralSharedSecret = o.bytes === String ? $util.base64.encode(m.ephemeralSharedSecret, 0, m.ephemeralSharedSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.ephemeralSharedSecret) : m.ephemeralSharedSecret;
                if (o.oneofs)
                    d._ephemeralSharedSecret = "ephemeralSharedSecret";
            }
            if (m.externalAdReply != null && m.hasOwnProperty("externalAdReply")) {
                d.externalAdReply = WAE2E.ContextInfo.ExternalAdReplyInfo.toObject(m.externalAdReply, o);
                if (o.oneofs)
                    d._externalAdReply = "externalAdReply";
            }
            if (m.entryPointConversionSource != null && m.hasOwnProperty("entryPointConversionSource")) {
                d.entryPointConversionSource = m.entryPointConversionSource;
                if (o.oneofs)
                    d._entryPointConversionSource = "entryPointConversionSource";
            }
            if (m.entryPointConversionApp != null && m.hasOwnProperty("entryPointConversionApp")) {
                d.entryPointConversionApp = m.entryPointConversionApp;
                if (o.oneofs)
                    d._entryPointConversionApp = "entryPointConversionApp";
            }
            if (m.entryPointConversionDelaySeconds != null && m.hasOwnProperty("entryPointConversionDelaySeconds")) {
                d.entryPointConversionDelaySeconds = m.entryPointConversionDelaySeconds;
                if (o.oneofs)
                    d._entryPointConversionDelaySeconds = "entryPointConversionDelaySeconds";
            }
            if (m.disappearingMode != null && m.hasOwnProperty("disappearingMode")) {
                d.disappearingMode = WAE2E.DisappearingMode.toObject(m.disappearingMode, o);
                if (o.oneofs)
                    d._disappearingMode = "disappearingMode";
            }
            if (m.actionLink != null && m.hasOwnProperty("actionLink")) {
                d.actionLink = WAE2E.ActionLink.toObject(m.actionLink, o);
                if (o.oneofs)
                    d._actionLink = "actionLink";
            }
            if (m.groupSubject != null && m.hasOwnProperty("groupSubject")) {
                d.groupSubject = m.groupSubject;
                if (o.oneofs)
                    d._groupSubject = "groupSubject";
            }
            if (m.parentGroupJID != null && m.hasOwnProperty("parentGroupJID")) {
                d.parentGroupJID = m.parentGroupJID;
                if (o.oneofs)
                    d._parentGroupJID = "parentGroupJID";
            }
            if (m.trustBannerType != null && m.hasOwnProperty("trustBannerType")) {
                d.trustBannerType = m.trustBannerType;
                if (o.oneofs)
                    d._trustBannerType = "trustBannerType";
            }
            if (m.trustBannerAction != null && m.hasOwnProperty("trustBannerAction")) {
                d.trustBannerAction = m.trustBannerAction;
                if (o.oneofs)
                    d._trustBannerAction = "trustBannerAction";
            }
            if (m.isSampled != null && m.hasOwnProperty("isSampled")) {
                d.isSampled = m.isSampled;
                if (o.oneofs)
                    d._isSampled = "isSampled";
            }
            if (m.groupMentions && m.groupMentions.length) {
                d.groupMentions = [];
                for (var j = 0; j < m.groupMentions.length; ++j) {
                    d.groupMentions[j] = WAE2E.GroupMention.toObject(m.groupMentions[j], o);
                }
            }
            if (m.utm != null && m.hasOwnProperty("utm")) {
                d.utm = WAE2E.ContextInfo.UTMInfo.toObject(m.utm, o);
                if (o.oneofs)
                    d._utm = "utm";
            }
            if (m.forwardedNewsletterMessageInfo != null && m.hasOwnProperty("forwardedNewsletterMessageInfo")) {
                d.forwardedNewsletterMessageInfo = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.toObject(m.forwardedNewsletterMessageInfo, o);
                if (o.oneofs)
                    d._forwardedNewsletterMessageInfo = "forwardedNewsletterMessageInfo";
            }
            if (m.businessMessageForwardInfo != null && m.hasOwnProperty("businessMessageForwardInfo")) {
                d.businessMessageForwardInfo = WAE2E.ContextInfo.BusinessMessageForwardInfo.toObject(m.businessMessageForwardInfo, o);
                if (o.oneofs)
                    d._businessMessageForwardInfo = "businessMessageForwardInfo";
            }
            if (m.smbClientCampaignID != null && m.hasOwnProperty("smbClientCampaignID")) {
                d.smbClientCampaignID = m.smbClientCampaignID;
                if (o.oneofs)
                    d._smbClientCampaignID = "smbClientCampaignID";
            }
            if (m.smbServerCampaignID != null && m.hasOwnProperty("smbServerCampaignID")) {
                d.smbServerCampaignID = m.smbServerCampaignID;
                if (o.oneofs)
                    d._smbServerCampaignID = "smbServerCampaignID";
            }
            if (m.dataSharingContext != null && m.hasOwnProperty("dataSharingContext")) {
                d.dataSharingContext = WAE2E.ContextInfo.DataSharingContext.toObject(m.dataSharingContext, o);
                if (o.oneofs)
                    d._dataSharingContext = "dataSharingContext";
            }
            if (m.alwaysShowAdAttribution != null && m.hasOwnProperty("alwaysShowAdAttribution")) {
                d.alwaysShowAdAttribution = m.alwaysShowAdAttribution;
                if (o.oneofs)
                    d._alwaysShowAdAttribution = "alwaysShowAdAttribution";
            }
            if (m.featureEligibilities != null && m.hasOwnProperty("featureEligibilities")) {
                d.featureEligibilities = WAE2E.ContextInfo.FeatureEligibilities.toObject(m.featureEligibilities, o);
                if (o.oneofs)
                    d._featureEligibilities = "featureEligibilities";
            }
            if (m.entryPointConversionExternalSource != null && m.hasOwnProperty("entryPointConversionExternalSource")) {
                d.entryPointConversionExternalSource = m.entryPointConversionExternalSource;
                if (o.oneofs)
                    d._entryPointConversionExternalSource = "entryPointConversionExternalSource";
            }
            if (m.entryPointConversionExternalMedium != null && m.hasOwnProperty("entryPointConversionExternalMedium")) {
                d.entryPointConversionExternalMedium = m.entryPointConversionExternalMedium;
                if (o.oneofs)
                    d._entryPointConversionExternalMedium = "entryPointConversionExternalMedium";
            }
            if (m.ctwaSignals != null && m.hasOwnProperty("ctwaSignals")) {
                d.ctwaSignals = m.ctwaSignals;
                if (o.oneofs)
                    d._ctwaSignals = "ctwaSignals";
            }
            if (m.ctwaPayload != null && m.hasOwnProperty("ctwaPayload")) {
                d.ctwaPayload = o.bytes === String ? $util.base64.encode(m.ctwaPayload, 0, m.ctwaPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.ctwaPayload) : m.ctwaPayload;
                if (o.oneofs)
                    d._ctwaPayload = "ctwaPayload";
            }
            if (m.forwardedAiBotMessageInfo != null && m.hasOwnProperty("forwardedAiBotMessageInfo")) {
                d.forwardedAiBotMessageInfo = $root.WAAICommon.ForwardedAIBotMessageInfo.toObject(m.forwardedAiBotMessageInfo, o);
                if (o.oneofs)
                    d._forwardedAiBotMessageInfo = "forwardedAiBotMessageInfo";
            }
            if (m.statusAttributionType != null && m.hasOwnProperty("statusAttributionType")) {
                d.statusAttributionType = o.enums === String ? WAE2E.ContextInfo.StatusAttributionType[m.statusAttributionType] === undefined ? m.statusAttributionType : WAE2E.ContextInfo.StatusAttributionType[m.statusAttributionType] : m.statusAttributionType;
                if (o.oneofs)
                    d._statusAttributionType = "statusAttributionType";
            }
            if (m.urlTrackingMap != null && m.hasOwnProperty("urlTrackingMap")) {
                d.urlTrackingMap = WAE2E.UrlTrackingMap.toObject(m.urlTrackingMap, o);
                if (o.oneofs)
                    d._urlTrackingMap = "urlTrackingMap";
            }
            if (m.pairedMediaType != null && m.hasOwnProperty("pairedMediaType")) {
                d.pairedMediaType = o.enums === String ? WAE2E.ContextInfo.PairedMediaType[m.pairedMediaType] === undefined ? m.pairedMediaType : WAE2E.ContextInfo.PairedMediaType[m.pairedMediaType] : m.pairedMediaType;
                if (o.oneofs)
                    d._pairedMediaType = "pairedMediaType";
            }
            if (m.rankingVersion != null && m.hasOwnProperty("rankingVersion")) {
                d.rankingVersion = m.rankingVersion;
                if (o.oneofs)
                    d._rankingVersion = "rankingVersion";
            }
            if (m.memberLabel != null && m.hasOwnProperty("memberLabel")) {
                d.memberLabel = WAE2E.MemberLabel.toObject(m.memberLabel, o);
                if (o.oneofs)
                    d._memberLabel = "memberLabel";
            }
            if (m.isQuestion != null && m.hasOwnProperty("isQuestion")) {
                d.isQuestion = m.isQuestion;
                if (o.oneofs)
                    d._isQuestion = "isQuestion";
            }
            if (m.statusSourceType != null && m.hasOwnProperty("statusSourceType")) {
                d.statusSourceType = o.enums === String ? WAE2E.ContextInfo.StatusSourceType[m.statusSourceType] === undefined ? m.statusSourceType : WAE2E.ContextInfo.StatusSourceType[m.statusSourceType] : m.statusSourceType;
                if (o.oneofs)
                    d._statusSourceType = "statusSourceType";
            }
            if (m.statusAttributions && m.statusAttributions.length) {
                d.statusAttributions = [];
                for (var j = 0; j < m.statusAttributions.length; ++j) {
                    d.statusAttributions[j] = $root.WAStatusAttributions.StatusAttribution.toObject(m.statusAttributions[j], o);
                }
            }
            if (m.isGroupStatus != null && m.hasOwnProperty("isGroupStatus")) {
                d.isGroupStatus = m.isGroupStatus;
                if (o.oneofs)
                    d._isGroupStatus = "isGroupStatus";
            }
            if (m.forwardOrigin != null && m.hasOwnProperty("forwardOrigin")) {
                d.forwardOrigin = o.enums === String ? WAE2E.ContextInfo.ForwardOrigin[m.forwardOrigin] === undefined ? m.forwardOrigin : WAE2E.ContextInfo.ForwardOrigin[m.forwardOrigin] : m.forwardOrigin;
                if (o.oneofs)
                    d._forwardOrigin = "forwardOrigin";
            }
            if (m.questionReplyQuotedMessage != null && m.hasOwnProperty("questionReplyQuotedMessage")) {
                d.questionReplyQuotedMessage = WAE2E.ContextInfo.QuestionReplyQuotedMessage.toObject(m.questionReplyQuotedMessage, o);
                if (o.oneofs)
                    d._questionReplyQuotedMessage = "questionReplyQuotedMessage";
            }
            if (m.statusAudienceMetadata != null && m.hasOwnProperty("statusAudienceMetadata")) {
                d.statusAudienceMetadata = WAE2E.ContextInfo.StatusAudienceMetadata.toObject(m.statusAudienceMetadata, o);
                if (o.oneofs)
                    d._statusAudienceMetadata = "statusAudienceMetadata";
            }
            if (m.nonJIDMentions != null && m.hasOwnProperty("nonJIDMentions")) {
                d.nonJIDMentions = m.nonJIDMentions;
                if (o.oneofs)
                    d._nonJIDMentions = "nonJIDMentions";
            }
            if (m.quotedType != null && m.hasOwnProperty("quotedType")) {
                d.quotedType = o.enums === String ? WAE2E.ContextInfo.QuotedType[m.quotedType] === undefined ? m.quotedType : WAE2E.ContextInfo.QuotedType[m.quotedType] : m.quotedType;
                if (o.oneofs)
                    d._quotedType = "quotedType";
            }
            if (m.botMessageSharingInfo != null && m.hasOwnProperty("botMessageSharingInfo")) {
                d.botMessageSharingInfo = $root.WAAICommon.BotMessageSharingInfo.toObject(m.botMessageSharingInfo, o);
                if (o.oneofs)
                    d._botMessageSharingInfo = "botMessageSharingInfo";
            }
            if (m.isSpoiler != null && m.hasOwnProperty("isSpoiler")) {
                d.isSpoiler = m.isSpoiler;
                if (o.oneofs)
                    d._isSpoiler = "isSpoiler";
            }
            if (m.mediaDomainInfo != null && m.hasOwnProperty("mediaDomainInfo")) {
                d.mediaDomainInfo = WAE2E.MediaDomainInfo.toObject(m.mediaDomainInfo, o);
                if (o.oneofs)
                    d._mediaDomainInfo = "mediaDomainInfo";
            }
            if (m.partiallySelectedContent != null && m.hasOwnProperty("partiallySelectedContent")) {
                d.partiallySelectedContent = WAE2E.ContextInfo.PartiallySelectedContent.toObject(m.partiallySelectedContent, o);
                if (o.oneofs)
                    d._partiallySelectedContent = "partiallySelectedContent";
            }
            if (m.afterReadDuration != null && m.hasOwnProperty("afterReadDuration")) {
                d.afterReadDuration = m.afterReadDuration;
                if (o.oneofs)
                    d._afterReadDuration = "afterReadDuration";
            }
            if (m.crossAppSource != null && m.hasOwnProperty("crossAppSource")) {
                d.crossAppSource = o.enums === String ? WAE2E.ContextInfo.CrossAppSource[m.crossAppSource] === undefined ? m.crossAppSource : WAE2E.ContextInfo.CrossAppSource[m.crossAppSource] : m.crossAppSource;
                if (o.oneofs)
                    d._crossAppSource = "crossAppSource";
            }
            if (m.businessInteractionPills != null && m.hasOwnProperty("businessInteractionPills")) {
                d.businessInteractionPills = WAE2E.ContextInfo.BusinessInteractionPills.toObject(m.businessInteractionPills, o);
                if (o.oneofs)
                    d._businessInteractionPills = "businessInteractionPills";
            }
            if (m.posterStatusID != null && m.hasOwnProperty("posterStatusID")) {
                d.posterStatusID = m.posterStatusID;
                if (o.oneofs)
                    d._posterStatusID = "posterStatusID";
            }
            return d;
        };

        ContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ContextInfo";
        };

        ContextInfo.CrossAppSource = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CROSS_APP_SOURCE_UNKNOWN"] = 0;
            values[valuesById[1] = "CROSS_APP_SOURCE_INSTAGRAM"] = 1;
            values[valuesById[2] = "CROSS_APP_SOURCE_FACEBOOK"] = 2;
            return values;
        })();

        ContextInfo.QuotedType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "EXPLICIT"] = 0;
            values[valuesById[1] = "AUTO"] = 1;
            return values;
        })();

        ContextInfo.ForwardOrigin = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT"] = 1;
            values[valuesById[2] = "STATUS"] = 2;
            values[valuesById[3] = "CHANNELS"] = 3;
            values[valuesById[4] = "META_AI"] = 4;
            values[valuesById[5] = "UGC"] = 5;
            return values;
        })();

        ContextInfo.StatusSourceType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IMAGE"] = 0;
            values[valuesById[1] = "VIDEO"] = 1;
            values[valuesById[2] = "GIF"] = 2;
            values[valuesById[3] = "AUDIO"] = 3;
            values[valuesById[4] = "TEXT"] = 4;
            values[valuesById[5] = "MUSIC_STANDALONE"] = 5;
            return values;
        })();

        ContextInfo.PairedMediaType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NOT_PAIRED_MEDIA"] = 0;
            values[valuesById[1] = "SD_VIDEO_PARENT"] = 1;
            values[valuesById[2] = "HD_VIDEO_CHILD"] = 2;
            values[valuesById[3] = "SD_IMAGE_PARENT"] = 3;
            values[valuesById[4] = "HD_IMAGE_CHILD"] = 4;
            values[valuesById[5] = "MOTION_PHOTO_PARENT"] = 5;
            values[valuesById[6] = "MOTION_PHOTO_CHILD"] = 6;
            values[valuesById[7] = "HEVC_VIDEO_PARENT"] = 7;
            values[valuesById[8] = "HEVC_VIDEO_CHILD"] = 8;
            return values;
        })();

        ContextInfo.StatusAttributionType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "RESHARED_FROM_MENTION"] = 1;
            values[valuesById[2] = "RESHARED_FROM_POST"] = 2;
            values[valuesById[3] = "RESHARED_FROM_POST_MANY_TIMES"] = 3;
            values[valuesById[4] = "FORWARDED_FROM_STATUS"] = 4;
            return values;
        })();

        ContextInfo.BusinessInteractionPills = (function() {

            function BusinessInteractionPills(p) {
                this.pills = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BusinessInteractionPills.prototype.businessJID = null;
            BusinessInteractionPills.prototype.pills = $util.emptyArray;
            BusinessInteractionPills.prototype.entryPoint = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_businessJID", {
                get: $util.oneOfGetter($oneOfFields = ["businessJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessInteractionPills.prototype, "_entryPoint", {
                get: $util.oneOfGetter($oneOfFields = ["entryPoint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessInteractionPills.create = function create(properties) {
                return new BusinessInteractionPills(properties);
            };

            BusinessInteractionPills.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.businessJID != null && Object.hasOwnProperty.call(m, "businessJID"))
                    w.uint32(10).string(m.businessJID);
                if (m.pills != null && m.pills.length) {
                    for (var i = 0; i < m.pills.length; ++i)
                        WAE2E.ContextInfo.BusinessInteractionPills.Pill.encode(m.pills[i], w.uint32(18).fork()).ldelim();
                }
                if (m.entryPoint != null && Object.hasOwnProperty.call(m, "entryPoint"))
                    w.uint32(24).int32(m.entryPoint);
                return w;
            };

            BusinessInteractionPills.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.BusinessInteractionPills();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.businessJID = r.string();
                            break;
                        }
                    case 2: {
                            if (!(m.pills && m.pills.length))
                                m.pills = [];
                            m.pills.push(WAE2E.ContextInfo.BusinessInteractionPills.Pill.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 3: {
                            m.entryPoint = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BusinessInteractionPills.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.BusinessInteractionPills)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.BusinessInteractionPills();
                if (d.businessJID != null) {
                    m.businessJID = String(d.businessJID);
                }
                if (d.pills) {
                    if (!Array.isArray(d.pills))
                        throw TypeError(".WAE2E.ContextInfo.BusinessInteractionPills.pills: array expected");
                    m.pills = [];
                    for (var i = 0; i < d.pills.length; ++i) {
                        if (typeof d.pills[i] !== "object")
                            throw TypeError(".WAE2E.ContextInfo.BusinessInteractionPills.pills: object expected");
                        m.pills[i] = WAE2E.ContextInfo.BusinessInteractionPills.Pill.fromObject(d.pills[i], n + 1);
                    }
                }
                switch (d.entryPoint) {
                default:
                    if (typeof d.entryPoint === "number") {
                        m.entryPoint = d.entryPoint;
                        break;
                    }
                    break;
                case "ENTRY_POINT_UNKNOWN":
                case 0:
                    m.entryPoint = 0;
                    break;
                case "P2P_LINK_SHARE":
                case 1:
                    m.entryPoint = 1;
                    break;
                case "CONTACT_CARD_SHARING":
                case 2:
                    m.entryPoint = 2;
                    break;
                case "PHONE_NUMBER":
                case 3:
                    m.entryPoint = 3;
                    break;
                case "STATUS":
                case 4:
                    m.entryPoint = 4;
                    break;
                case "IN_THREAD_CONTEXT_CARD":
                case 5:
                    m.entryPoint = 5;
                    break;
                }
                return m;
            };

            BusinessInteractionPills.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.pills = [];
                }
                if (m.businessJID != null && m.hasOwnProperty("businessJID")) {
                    d.businessJID = m.businessJID;
                    if (o.oneofs)
                        d._businessJID = "businessJID";
                }
                if (m.pills && m.pills.length) {
                    d.pills = [];
                    for (var j = 0; j < m.pills.length; ++j) {
                        d.pills[j] = WAE2E.ContextInfo.BusinessInteractionPills.Pill.toObject(m.pills[j], o);
                    }
                }
                if (m.entryPoint != null && m.hasOwnProperty("entryPoint")) {
                    d.entryPoint = o.enums === String ? WAE2E.ContextInfo.BusinessInteractionPills.EntryPoint[m.entryPoint] === undefined ? m.entryPoint : WAE2E.ContextInfo.BusinessInteractionPills.EntryPoint[m.entryPoint] : m.entryPoint;
                    if (o.oneofs)
                        d._entryPoint = "entryPoint";
                }
                return d;
            };

            BusinessInteractionPills.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessInteractionPills.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.BusinessInteractionPills";
            };

            BusinessInteractionPills.EntryPoint = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "ENTRY_POINT_UNKNOWN"] = 0;
                values[valuesById[1] = "P2P_LINK_SHARE"] = 1;
                values[valuesById[2] = "CONTACT_CARD_SHARING"] = 2;
                values[valuesById[3] = "PHONE_NUMBER"] = 3;
                values[valuesById[4] = "STATUS"] = 4;
                values[valuesById[5] = "IN_THREAD_CONTEXT_CARD"] = 5;
                return values;
            })();

            BusinessInteractionPills.PillType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VIEW_BUSINESS"] = 1;
                values[valuesById[2] = "CHAT"] = 2;
                values[valuesById[3] = "CALL"] = 3;
                values[valuesById[4] = "CATALOG"] = 4;
                values[valuesById[5] = "CHANNEL"] = 5;
                values[valuesById[6] = "BOOK_APPOINTMENT"] = 6;
                values[valuesById[7] = "OFFERS"] = 7;
                values[valuesById[8] = "BESTSELLERS"] = 8;
                values[valuesById[9] = "MENU"] = 9;
                values[valuesById[10] = "ABOUT"] = 10;
                return values;
            })();

            BusinessInteractionPills.Pill = (function() {

                function Pill(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                Pill.prototype.pillType = null;
                Pill.prototype.actionURL = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Pill.prototype, "_pillType", {
                    get: $util.oneOfGetter($oneOfFields = ["pillType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Pill.prototype, "_actionURL", {
                    get: $util.oneOfGetter($oneOfFields = ["actionURL"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Pill.create = function create(properties) {
                    return new Pill(properties);
                };

                Pill.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.pillType != null && Object.hasOwnProperty.call(m, "pillType"))
                        w.uint32(8).int32(m.pillType);
                    if (m.actionURL != null && Object.hasOwnProperty.call(m, "actionURL"))
                        w.uint32(18).string(m.actionURL);
                    return w;
                };

                Pill.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.BusinessInteractionPills.Pill();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.pillType = r.int32();
                                break;
                            }
                        case 2: {
                                m.actionURL = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                Pill.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.ContextInfo.BusinessInteractionPills.Pill)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.ContextInfo.BusinessInteractionPills.Pill();
                    switch (d.pillType) {
                    default:
                        if (typeof d.pillType === "number") {
                            m.pillType = d.pillType;
                            break;
                        }
                        break;
                    case "UNKNOWN":
                    case 0:
                        m.pillType = 0;
                        break;
                    case "VIEW_BUSINESS":
                    case 1:
                        m.pillType = 1;
                        break;
                    case "CHAT":
                    case 2:
                        m.pillType = 2;
                        break;
                    case "CALL":
                    case 3:
                        m.pillType = 3;
                        break;
                    case "CATALOG":
                    case 4:
                        m.pillType = 4;
                        break;
                    case "CHANNEL":
                    case 5:
                        m.pillType = 5;
                        break;
                    case "BOOK_APPOINTMENT":
                    case 6:
                        m.pillType = 6;
                        break;
                    case "OFFERS":
                    case 7:
                        m.pillType = 7;
                        break;
                    case "BESTSELLERS":
                    case 8:
                        m.pillType = 8;
                        break;
                    case "MENU":
                    case 9:
                        m.pillType = 9;
                        break;
                    case "ABOUT":
                    case 10:
                        m.pillType = 10;
                        break;
                    }
                    if (d.actionURL != null) {
                        m.actionURL = String(d.actionURL);
                    }
                    return m;
                };

                Pill.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.pillType != null && m.hasOwnProperty("pillType")) {
                        d.pillType = o.enums === String ? WAE2E.ContextInfo.BusinessInteractionPills.PillType[m.pillType] === undefined ? m.pillType : WAE2E.ContextInfo.BusinessInteractionPills.PillType[m.pillType] : m.pillType;
                        if (o.oneofs)
                            d._pillType = "pillType";
                    }
                    if (m.actionURL != null && m.hasOwnProperty("actionURL")) {
                        d.actionURL = m.actionURL;
                        if (o.oneofs)
                            d._actionURL = "actionURL";
                    }
                    return d;
                };

                Pill.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                Pill.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.ContextInfo.BusinessInteractionPills.Pill";
                };

                return Pill;
            })();

            return BusinessInteractionPills;
        })();

        ContextInfo.StatusAudienceMetadata = (function() {

            function StatusAudienceMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            StatusAudienceMetadata.prototype.audienceType = null;
            StatusAudienceMetadata.prototype.listName = null;
            StatusAudienceMetadata.prototype.listEmoji = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_audienceType", {
                get: $util.oneOfGetter($oneOfFields = ["audienceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_listName", {
                get: $util.oneOfGetter($oneOfFields = ["listName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusAudienceMetadata.prototype, "_listEmoji", {
                get: $util.oneOfGetter($oneOfFields = ["listEmoji"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusAudienceMetadata.create = function create(properties) {
                return new StatusAudienceMetadata(properties);
            };

            StatusAudienceMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.audienceType != null && Object.hasOwnProperty.call(m, "audienceType"))
                    w.uint32(8).int32(m.audienceType);
                if (m.listName != null && Object.hasOwnProperty.call(m, "listName"))
                    w.uint32(18).string(m.listName);
                if (m.listEmoji != null && Object.hasOwnProperty.call(m, "listEmoji"))
                    w.uint32(26).string(m.listEmoji);
                return w;
            };

            StatusAudienceMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.StatusAudienceMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.audienceType = r.int32();
                            break;
                        }
                    case 2: {
                            m.listName = r.string();
                            break;
                        }
                    case 3: {
                            m.listEmoji = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            StatusAudienceMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.StatusAudienceMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.StatusAudienceMetadata();
                switch (d.audienceType) {
                default:
                    if (typeof d.audienceType === "number") {
                        m.audienceType = d.audienceType;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.audienceType = 0;
                    break;
                case "CLOSE_FRIENDS":
                case 1:
                    m.audienceType = 1;
                    break;
                }
                if (d.listName != null) {
                    m.listName = String(d.listName);
                }
                if (d.listEmoji != null) {
                    m.listEmoji = String(d.listEmoji);
                }
                return m;
            };

            StatusAudienceMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.audienceType != null && m.hasOwnProperty("audienceType")) {
                    d.audienceType = o.enums === String ? WAE2E.ContextInfo.StatusAudienceMetadata.AudienceType[m.audienceType] === undefined ? m.audienceType : WAE2E.ContextInfo.StatusAudienceMetadata.AudienceType[m.audienceType] : m.audienceType;
                    if (o.oneofs)
                        d._audienceType = "audienceType";
                }
                if (m.listName != null && m.hasOwnProperty("listName")) {
                    d.listName = m.listName;
                    if (o.oneofs)
                        d._listName = "listName";
                }
                if (m.listEmoji != null && m.hasOwnProperty("listEmoji")) {
                    d.listEmoji = m.listEmoji;
                    if (o.oneofs)
                        d._listEmoji = "listEmoji";
                }
                return d;
            };

            StatusAudienceMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusAudienceMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.StatusAudienceMetadata";
            };

            StatusAudienceMetadata.AudienceType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CLOSE_FRIENDS"] = 1;
                return values;
            })();

            return StatusAudienceMetadata;
        })();

        ContextInfo.DataSharingContext = (function() {

            function DataSharingContext(p) {
                this.parameters = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            DataSharingContext.prototype.showMmDisclosure = null;
            DataSharingContext.prototype.encryptedSignalTokenConsented = null;
            DataSharingContext.prototype.parameters = $util.emptyArray;
            DataSharingContext.prototype.dataSharingFlags = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_showMmDisclosure", {
                get: $util.oneOfGetter($oneOfFields = ["showMmDisclosure"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_encryptedSignalTokenConsented", {
                get: $util.oneOfGetter($oneOfFields = ["encryptedSignalTokenConsented"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(DataSharingContext.prototype, "_dataSharingFlags", {
                get: $util.oneOfGetter($oneOfFields = ["dataSharingFlags"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DataSharingContext.create = function create(properties) {
                return new DataSharingContext(properties);
            };

            DataSharingContext.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.showMmDisclosure != null && Object.hasOwnProperty.call(m, "showMmDisclosure"))
                    w.uint32(8).bool(m.showMmDisclosure);
                if (m.encryptedSignalTokenConsented != null && Object.hasOwnProperty.call(m, "encryptedSignalTokenConsented"))
                    w.uint32(18).string(m.encryptedSignalTokenConsented);
                if (m.parameters != null && m.parameters.length) {
                    for (var i = 0; i < m.parameters.length; ++i)
                        WAE2E.ContextInfo.DataSharingContext.Parameters.encode(m.parameters[i], w.uint32(26).fork()).ldelim();
                }
                if (m.dataSharingFlags != null && Object.hasOwnProperty.call(m, "dataSharingFlags"))
                    w.uint32(32).int32(m.dataSharingFlags);
                return w;
            };

            DataSharingContext.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.DataSharingContext();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.showMmDisclosure = r.bool();
                            break;
                        }
                    case 2: {
                            m.encryptedSignalTokenConsented = r.string();
                            break;
                        }
                    case 3: {
                            if (!(m.parameters && m.parameters.length))
                                m.parameters = [];
                            m.parameters.push(WAE2E.ContextInfo.DataSharingContext.Parameters.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 4: {
                            m.dataSharingFlags = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            DataSharingContext.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.DataSharingContext)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.DataSharingContext();
                if (d.showMmDisclosure != null) {
                    m.showMmDisclosure = Boolean(d.showMmDisclosure);
                }
                if (d.encryptedSignalTokenConsented != null) {
                    m.encryptedSignalTokenConsented = String(d.encryptedSignalTokenConsented);
                }
                if (d.parameters) {
                    if (!Array.isArray(d.parameters))
                        throw TypeError(".WAE2E.ContextInfo.DataSharingContext.parameters: array expected");
                    m.parameters = [];
                    for (var i = 0; i < d.parameters.length; ++i) {
                        if (typeof d.parameters[i] !== "object")
                            throw TypeError(".WAE2E.ContextInfo.DataSharingContext.parameters: object expected");
                        m.parameters[i] = WAE2E.ContextInfo.DataSharingContext.Parameters.fromObject(d.parameters[i], n + 1);
                    }
                }
                if (d.dataSharingFlags != null) {
                    m.dataSharingFlags = d.dataSharingFlags | 0;
                }
                return m;
            };

            DataSharingContext.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.parameters = [];
                }
                if (m.showMmDisclosure != null && m.hasOwnProperty("showMmDisclosure")) {
                    d.showMmDisclosure = m.showMmDisclosure;
                    if (o.oneofs)
                        d._showMmDisclosure = "showMmDisclosure";
                }
                if (m.encryptedSignalTokenConsented != null && m.hasOwnProperty("encryptedSignalTokenConsented")) {
                    d.encryptedSignalTokenConsented = m.encryptedSignalTokenConsented;
                    if (o.oneofs)
                        d._encryptedSignalTokenConsented = "encryptedSignalTokenConsented";
                }
                if (m.parameters && m.parameters.length) {
                    d.parameters = [];
                    for (var j = 0; j < m.parameters.length; ++j) {
                        d.parameters[j] = WAE2E.ContextInfo.DataSharingContext.Parameters.toObject(m.parameters[j], o);
                    }
                }
                if (m.dataSharingFlags != null && m.hasOwnProperty("dataSharingFlags")) {
                    d.dataSharingFlags = m.dataSharingFlags;
                    if (o.oneofs)
                        d._dataSharingFlags = "dataSharingFlags";
                }
                return d;
            };

            DataSharingContext.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            DataSharingContext.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.DataSharingContext";
            };

            DataSharingContext.DataSharingFlags = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "SHOW_MM_DISCLOSURE_ON_CLICK"] = 1;
                values[valuesById[2] = "SHOW_MM_DISCLOSURE_ON_READ"] = 2;
                return values;
            })();

            DataSharingContext.Parameters = (function() {

                function Parameters(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                Parameters.prototype.key = null;
                Parameters.prototype.stringData = null;
                Parameters.prototype.intData = null;
                Parameters.prototype.floatData = null;
                Parameters.prototype.contents = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_stringData", {
                    get: $util.oneOfGetter($oneOfFields = ["stringData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_intData", {
                    get: $util.oneOfGetter($oneOfFields = ["intData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_floatData", {
                    get: $util.oneOfGetter($oneOfFields = ["floatData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Parameters.prototype, "_contents", {
                    get: $util.oneOfGetter($oneOfFields = ["contents"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Parameters.create = function create(properties) {
                    return new Parameters(properties);
                };

                Parameters.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                        w.uint32(10).string(m.key);
                    if (m.stringData != null && Object.hasOwnProperty.call(m, "stringData"))
                        w.uint32(18).string(m.stringData);
                    if (m.intData != null && Object.hasOwnProperty.call(m, "intData"))
                        w.uint32(24).int64(m.intData);
                    if (m.floatData != null && Object.hasOwnProperty.call(m, "floatData"))
                        w.uint32(37).float(m.floatData);
                    if (m.contents != null && Object.hasOwnProperty.call(m, "contents"))
                        WAE2E.ContextInfo.DataSharingContext.Parameters.encode(m.contents, w.uint32(42).fork()).ldelim();
                    return w;
                };

                Parameters.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.DataSharingContext.Parameters();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.key = r.string();
                                break;
                            }
                        case 2: {
                                m.stringData = r.string();
                                break;
                            }
                        case 3: {
                                m.intData = r.int64();
                                break;
                            }
                        case 4: {
                                m.floatData = r.float();
                                break;
                            }
                        case 5: {
                                m.contents = WAE2E.ContextInfo.DataSharingContext.Parameters.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                Parameters.fromObject = function fromObject(d, n) {
                    if (d instanceof WAE2E.ContextInfo.DataSharingContext.Parameters)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new WAE2E.ContextInfo.DataSharingContext.Parameters();
                    if (d.key != null) {
                        m.key = String(d.key);
                    }
                    if (d.stringData != null) {
                        m.stringData = String(d.stringData);
                    }
                    if (d.intData != null) {
                        if ($util.Long)
                            (m.intData = $util.Long.fromValue(d.intData)).unsigned = false;
                        else if (typeof d.intData === "string")
                            m.intData = parseInt(d.intData, 10);
                        else if (typeof d.intData === "number")
                            m.intData = d.intData;
                        else if (typeof d.intData === "object")
                            m.intData = new $util.LongBits(d.intData.low >>> 0, d.intData.high >>> 0).toNumber();
                    }
                    if (d.floatData != null) {
                        m.floatData = Number(d.floatData);
                    }
                    if (d.contents != null) {
                        if (typeof d.contents !== "object")
                            throw TypeError(".WAE2E.ContextInfo.DataSharingContext.Parameters.contents: object expected");
                        m.contents = WAE2E.ContextInfo.DataSharingContext.Parameters.fromObject(d.contents, n + 1);
                    }
                    return m;
                };

                Parameters.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.key != null && m.hasOwnProperty("key")) {
                        d.key = m.key;
                        if (o.oneofs)
                            d._key = "key";
                    }
                    if (m.stringData != null && m.hasOwnProperty("stringData")) {
                        d.stringData = m.stringData;
                        if (o.oneofs)
                            d._stringData = "stringData";
                    }
                    if (m.intData != null && m.hasOwnProperty("intData")) {
                        if (typeof m.intData === "number")
                            d.intData = o.longs === String ? String(m.intData) : m.intData;
                        else
                            d.intData = o.longs === String ? $util.Long.prototype.toString.call(m.intData) : o.longs === Number ? new $util.LongBits(m.intData.low >>> 0, m.intData.high >>> 0).toNumber() : m.intData;
                        if (o.oneofs)
                            d._intData = "intData";
                    }
                    if (m.floatData != null && m.hasOwnProperty("floatData")) {
                        d.floatData = o.json && !isFinite(m.floatData) ? String(m.floatData) : m.floatData;
                        if (o.oneofs)
                            d._floatData = "floatData";
                    }
                    if (m.contents != null && m.hasOwnProperty("contents")) {
                        d.contents = WAE2E.ContextInfo.DataSharingContext.Parameters.toObject(m.contents, o);
                        if (o.oneofs)
                            d._contents = "contents";
                    }
                    return d;
                };

                Parameters.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                Parameters.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAE2E.ContextInfo.DataSharingContext.Parameters";
                };

                return Parameters;
            })();

            return DataSharingContext;
        })();

        ContextInfo.ForwardedNewsletterMessageInfo = (function() {

            function ForwardedNewsletterMessageInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ForwardedNewsletterMessageInfo.prototype.newsletterJID = null;
            ForwardedNewsletterMessageInfo.prototype.serverMessageID = null;
            ForwardedNewsletterMessageInfo.prototype.newsletterName = null;
            ForwardedNewsletterMessageInfo.prototype.contentType = null;
            ForwardedNewsletterMessageInfo.prototype.accessibilityText = null;
            ForwardedNewsletterMessageInfo.prototype.profileName = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterJID", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_serverMessageID", {
                get: $util.oneOfGetter($oneOfFields = ["serverMessageID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_contentType", {
                get: $util.oneOfGetter($oneOfFields = ["contentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_accessibilityText", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_profileName", {
                get: $util.oneOfGetter($oneOfFields = ["profileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ForwardedNewsletterMessageInfo.create = function create(properties) {
                return new ForwardedNewsletterMessageInfo(properties);
            };

            ForwardedNewsletterMessageInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.newsletterJID != null && Object.hasOwnProperty.call(m, "newsletterJID"))
                    w.uint32(10).string(m.newsletterJID);
                if (m.serverMessageID != null && Object.hasOwnProperty.call(m, "serverMessageID"))
                    w.uint32(16).int32(m.serverMessageID);
                if (m.newsletterName != null && Object.hasOwnProperty.call(m, "newsletterName"))
                    w.uint32(26).string(m.newsletterName);
                if (m.contentType != null && Object.hasOwnProperty.call(m, "contentType"))
                    w.uint32(32).int32(m.contentType);
                if (m.accessibilityText != null && Object.hasOwnProperty.call(m, "accessibilityText"))
                    w.uint32(42).string(m.accessibilityText);
                if (m.profileName != null && Object.hasOwnProperty.call(m, "profileName"))
                    w.uint32(50).string(m.profileName);
                return w;
            };

            ForwardedNewsletterMessageInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.ForwardedNewsletterMessageInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.newsletterJID = r.string();
                            break;
                        }
                    case 2: {
                            m.serverMessageID = r.int32();
                            break;
                        }
                    case 3: {
                            m.newsletterName = r.string();
                            break;
                        }
                    case 4: {
                            m.contentType = r.int32();
                            break;
                        }
                    case 5: {
                            m.accessibilityText = r.string();
                            break;
                        }
                    case 6: {
                            m.profileName = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ForwardedNewsletterMessageInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.ForwardedNewsletterMessageInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.ForwardedNewsletterMessageInfo();
                if (d.newsletterJID != null) {
                    m.newsletterJID = String(d.newsletterJID);
                }
                if (d.serverMessageID != null) {
                    m.serverMessageID = d.serverMessageID | 0;
                }
                if (d.newsletterName != null) {
                    m.newsletterName = String(d.newsletterName);
                }
                switch (d.contentType) {
                default:
                    if (typeof d.contentType === "number") {
                        m.contentType = d.contentType;
                        break;
                    }
                    break;
                case "UPDATE":
                case 1:
                    m.contentType = 1;
                    break;
                case "UPDATE_CARD":
                case 2:
                    m.contentType = 2;
                    break;
                case "LINK_CARD":
                case 3:
                    m.contentType = 3;
                    break;
                }
                if (d.accessibilityText != null) {
                    m.accessibilityText = String(d.accessibilityText);
                }
                if (d.profileName != null) {
                    m.profileName = String(d.profileName);
                }
                return m;
            };

            ForwardedNewsletterMessageInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.newsletterJID != null && m.hasOwnProperty("newsletterJID")) {
                    d.newsletterJID = m.newsletterJID;
                    if (o.oneofs)
                        d._newsletterJID = "newsletterJID";
                }
                if (m.serverMessageID != null && m.hasOwnProperty("serverMessageID")) {
                    d.serverMessageID = m.serverMessageID;
                    if (o.oneofs)
                        d._serverMessageID = "serverMessageID";
                }
                if (m.newsletterName != null && m.hasOwnProperty("newsletterName")) {
                    d.newsletterName = m.newsletterName;
                    if (o.oneofs)
                        d._newsletterName = "newsletterName";
                }
                if (m.contentType != null && m.hasOwnProperty("contentType")) {
                    d.contentType = o.enums === String ? WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[m.contentType] === undefined ? m.contentType : WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[m.contentType] : m.contentType;
                    if (o.oneofs)
                        d._contentType = "contentType";
                }
                if (m.accessibilityText != null && m.hasOwnProperty("accessibilityText")) {
                    d.accessibilityText = m.accessibilityText;
                    if (o.oneofs)
                        d._accessibilityText = "accessibilityText";
                }
                if (m.profileName != null && m.hasOwnProperty("profileName")) {
                    d.profileName = m.profileName;
                    if (o.oneofs)
                        d._profileName = "profileName";
                }
                return d;
            };

            ForwardedNewsletterMessageInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ForwardedNewsletterMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.ForwardedNewsletterMessageInfo";
            };

            ForwardedNewsletterMessageInfo.ContentType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "UPDATE"] = 1;
                values[valuesById[2] = "UPDATE_CARD"] = 2;
                values[valuesById[3] = "LINK_CARD"] = 3;
                return values;
            })();

            return ForwardedNewsletterMessageInfo;
        })();

        ContextInfo.ExternalAdReplyInfo = (function() {

            function ExternalAdReplyInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ExternalAdReplyInfo.prototype.title = null;
            ExternalAdReplyInfo.prototype.body = null;
            ExternalAdReplyInfo.prototype.mediaType = null;
            ExternalAdReplyInfo.prototype.thumbnailURL = null;
            ExternalAdReplyInfo.prototype.mediaURL = null;
            ExternalAdReplyInfo.prototype.thumbnail = null;
            ExternalAdReplyInfo.prototype.sourceType = null;
            ExternalAdReplyInfo.prototype.sourceID = null;
            ExternalAdReplyInfo.prototype.sourceURL = null;
            ExternalAdReplyInfo.prototype.containsAutoReply = null;
            ExternalAdReplyInfo.prototype.renderLargerThumbnail = null;
            ExternalAdReplyInfo.prototype.showAdAttribution = null;
            ExternalAdReplyInfo.prototype.ctwaClid = null;
            ExternalAdReplyInfo.prototype.ref = null;
            ExternalAdReplyInfo.prototype.clickToWhatsappCall = null;
            ExternalAdReplyInfo.prototype.adContextPreviewDismissed = null;
            ExternalAdReplyInfo.prototype.sourceApp = null;
            ExternalAdReplyInfo.prototype.automatedGreetingMessageShown = null;
            ExternalAdReplyInfo.prototype.greetingMessageBody = null;
            ExternalAdReplyInfo.prototype.ctaPayload = null;
            ExternalAdReplyInfo.prototype.disableNudge = null;
            ExternalAdReplyInfo.prototype.originalImageURL = null;
            ExternalAdReplyInfo.prototype.automatedGreetingMessageCtaType = null;
            ExternalAdReplyInfo.prototype.wtwaAdFormat = null;
            ExternalAdReplyInfo.prototype.adType = null;
            ExternalAdReplyInfo.prototype.wtwaWebsiteURL = null;
            ExternalAdReplyInfo.prototype.adPreviewURL = null;
            ExternalAdReplyInfo.prototype.containsCtwaFlowsAutoReply = null;
            ExternalAdReplyInfo.prototype.agmThumbnailStrategy = null;
            ExternalAdReplyInfo.prototype.agmTitleStrategy = null;
            ExternalAdReplyInfo.prototype.agmSubtitleStrategy = null;
            ExternalAdReplyInfo.prototype.agmHeaderInteractionStrategy = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnailURL", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaURL", {
                get: $util.oneOfGetter($oneOfFields = ["mediaURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceType", {
                get: $util.oneOfGetter($oneOfFields = ["sourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceID", {
                get: $util.oneOfGetter($oneOfFields = ["sourceID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceURL", {
                get: $util.oneOfGetter($oneOfFields = ["sourceURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_renderLargerThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["renderLargerThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_showAdAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["showAdAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctwaClid", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaClid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ref", {
                get: $util.oneOfGetter($oneOfFields = ["ref"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_clickToWhatsappCall", {
                get: $util.oneOfGetter($oneOfFields = ["clickToWhatsappCall"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adContextPreviewDismissed", {
                get: $util.oneOfGetter($oneOfFields = ["adContextPreviewDismissed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceApp", {
                get: $util.oneOfGetter($oneOfFields = ["sourceApp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageShown", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageShown"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_greetingMessageBody", {
                get: $util.oneOfGetter($oneOfFields = ["greetingMessageBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctaPayload", {
                get: $util.oneOfGetter($oneOfFields = ["ctaPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_disableNudge", {
                get: $util.oneOfGetter($oneOfFields = ["disableNudge"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_originalImageURL", {
                get: $util.oneOfGetter($oneOfFields = ["originalImageURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageCtaType", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageCtaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaAdFormat", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaAdFormat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adType", {
                get: $util.oneOfGetter($oneOfFields = ["adType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaWebsiteURL", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaWebsiteURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_adPreviewURL", {
                get: $util.oneOfGetter($oneOfFields = ["adPreviewURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsCtwaFlowsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsCtwaFlowsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmThumbnailStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmThumbnailStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmTitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmTitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmSubtitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmSubtitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmHeaderInteractionStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmHeaderInteractionStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExternalAdReplyInfo.create = function create(properties) {
                return new ExternalAdReplyInfo(properties);
            };

            ExternalAdReplyInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.body != null && Object.hasOwnProperty.call(m, "body"))
                    w.uint32(18).string(m.body);
                if (m.mediaType != null && Object.hasOwnProperty.call(m, "mediaType"))
                    w.uint32(24).int32(m.mediaType);
                if (m.thumbnailURL != null && Object.hasOwnProperty.call(m, "thumbnailURL"))
                    w.uint32(34).string(m.thumbnailURL);
                if (m.mediaURL != null && Object.hasOwnProperty.call(m, "mediaURL"))
                    w.uint32(42).string(m.mediaURL);
                if (m.thumbnail != null && Object.hasOwnProperty.call(m, "thumbnail"))
                    w.uint32(50).bytes(m.thumbnail);
                if (m.sourceType != null && Object.hasOwnProperty.call(m, "sourceType"))
                    w.uint32(58).string(m.sourceType);
                if (m.sourceID != null && Object.hasOwnProperty.call(m, "sourceID"))
                    w.uint32(66).string(m.sourceID);
                if (m.sourceURL != null && Object.hasOwnProperty.call(m, "sourceURL"))
                    w.uint32(74).string(m.sourceURL);
                if (m.containsAutoReply != null && Object.hasOwnProperty.call(m, "containsAutoReply"))
                    w.uint32(80).bool(m.containsAutoReply);
                if (m.renderLargerThumbnail != null && Object.hasOwnProperty.call(m, "renderLargerThumbnail"))
                    w.uint32(88).bool(m.renderLargerThumbnail);
                if (m.showAdAttribution != null && Object.hasOwnProperty.call(m, "showAdAttribution"))
                    w.uint32(96).bool(m.showAdAttribution);
                if (m.ctwaClid != null && Object.hasOwnProperty.call(m, "ctwaClid"))
                    w.uint32(106).string(m.ctwaClid);
                if (m.ref != null && Object.hasOwnProperty.call(m, "ref"))
                    w.uint32(114).string(m.ref);
                if (m.clickToWhatsappCall != null && Object.hasOwnProperty.call(m, "clickToWhatsappCall"))
                    w.uint32(120).bool(m.clickToWhatsappCall);
                if (m.adContextPreviewDismissed != null && Object.hasOwnProperty.call(m, "adContextPreviewDismissed"))
                    w.uint32(128).bool(m.adContextPreviewDismissed);
                if (m.sourceApp != null && Object.hasOwnProperty.call(m, "sourceApp"))
                    w.uint32(138).string(m.sourceApp);
                if (m.automatedGreetingMessageShown != null && Object.hasOwnProperty.call(m, "automatedGreetingMessageShown"))
                    w.uint32(144).bool(m.automatedGreetingMessageShown);
                if (m.greetingMessageBody != null && Object.hasOwnProperty.call(m, "greetingMessageBody"))
                    w.uint32(154).string(m.greetingMessageBody);
                if (m.ctaPayload != null && Object.hasOwnProperty.call(m, "ctaPayload"))
                    w.uint32(162).string(m.ctaPayload);
                if (m.disableNudge != null && Object.hasOwnProperty.call(m, "disableNudge"))
                    w.uint32(168).bool(m.disableNudge);
                if (m.originalImageURL != null && Object.hasOwnProperty.call(m, "originalImageURL"))
                    w.uint32(178).string(m.originalImageURL);
                if (m.automatedGreetingMessageCtaType != null && Object.hasOwnProperty.call(m, "automatedGreetingMessageCtaType"))
                    w.uint32(186).string(m.automatedGreetingMessageCtaType);
                if (m.wtwaAdFormat != null && Object.hasOwnProperty.call(m, "wtwaAdFormat"))
                    w.uint32(192).bool(m.wtwaAdFormat);
                if (m.adType != null && Object.hasOwnProperty.call(m, "adType"))
                    w.uint32(200).int32(m.adType);
                if (m.wtwaWebsiteURL != null && Object.hasOwnProperty.call(m, "wtwaWebsiteURL"))
                    w.uint32(210).string(m.wtwaWebsiteURL);
                if (m.adPreviewURL != null && Object.hasOwnProperty.call(m, "adPreviewURL"))
                    w.uint32(218).string(m.adPreviewURL);
                if (m.containsCtwaFlowsAutoReply != null && Object.hasOwnProperty.call(m, "containsCtwaFlowsAutoReply"))
                    w.uint32(224).bool(m.containsCtwaFlowsAutoReply);
                if (m.agmThumbnailStrategy != null && Object.hasOwnProperty.call(m, "agmThumbnailStrategy"))
                    w.uint32(232).int32(m.agmThumbnailStrategy);
                if (m.agmTitleStrategy != null && Object.hasOwnProperty.call(m, "agmTitleStrategy"))
                    w.uint32(240).int32(m.agmTitleStrategy);
                if (m.agmSubtitleStrategy != null && Object.hasOwnProperty.call(m, "agmSubtitleStrategy"))
                    w.uint32(248).int32(m.agmSubtitleStrategy);
                if (m.agmHeaderInteractionStrategy != null && Object.hasOwnProperty.call(m, "agmHeaderInteractionStrategy"))
                    w.uint32(256).int32(m.agmHeaderInteractionStrategy);
                return w;
            };

            ExternalAdReplyInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.ExternalAdReplyInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            m.body = r.string();
                            break;
                        }
                    case 3: {
                            m.mediaType = r.int32();
                            break;
                        }
                    case 4: {
                            m.thumbnailURL = r.string();
                            break;
                        }
                    case 5: {
                            m.mediaURL = r.string();
                            break;
                        }
                    case 6: {
                            m.thumbnail = r.bytes();
                            break;
                        }
                    case 7: {
                            m.sourceType = r.string();
                            break;
                        }
                    case 8: {
                            m.sourceID = r.string();
                            break;
                        }
                    case 9: {
                            m.sourceURL = r.string();
                            break;
                        }
                    case 10: {
                            m.containsAutoReply = r.bool();
                            break;
                        }
                    case 11: {
                            m.renderLargerThumbnail = r.bool();
                            break;
                        }
                    case 12: {
                            m.showAdAttribution = r.bool();
                            break;
                        }
                    case 13: {
                            m.ctwaClid = r.string();
                            break;
                        }
                    case 14: {
                            m.ref = r.string();
                            break;
                        }
                    case 15: {
                            m.clickToWhatsappCall = r.bool();
                            break;
                        }
                    case 16: {
                            m.adContextPreviewDismissed = r.bool();
                            break;
                        }
                    case 17: {
                            m.sourceApp = r.string();
                            break;
                        }
                    case 18: {
                            m.automatedGreetingMessageShown = r.bool();
                            break;
                        }
                    case 19: {
                            m.greetingMessageBody = r.string();
                            break;
                        }
                    case 20: {
                            m.ctaPayload = r.string();
                            break;
                        }
                    case 21: {
                            m.disableNudge = r.bool();
                            break;
                        }
                    case 22: {
                            m.originalImageURL = r.string();
                            break;
                        }
                    case 23: {
                            m.automatedGreetingMessageCtaType = r.string();
                            break;
                        }
                    case 24: {
                            m.wtwaAdFormat = r.bool();
                            break;
                        }
                    case 25: {
                            m.adType = r.int32();
                            break;
                        }
                    case 26: {
                            m.wtwaWebsiteURL = r.string();
                            break;
                        }
                    case 27: {
                            m.adPreviewURL = r.string();
                            break;
                        }
                    case 28: {
                            m.containsCtwaFlowsAutoReply = r.bool();
                            break;
                        }
                    case 29: {
                            m.agmThumbnailStrategy = r.int32();
                            break;
                        }
                    case 30: {
                            m.agmTitleStrategy = r.int32();
                            break;
                        }
                    case 31: {
                            m.agmSubtitleStrategy = r.int32();
                            break;
                        }
                    case 32: {
                            m.agmHeaderInteractionStrategy = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ExternalAdReplyInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.ExternalAdReplyInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.ExternalAdReplyInfo();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.body != null) {
                    m.body = String(d.body);
                }
                switch (d.mediaType) {
                default:
                    if (typeof d.mediaType === "number") {
                        m.mediaType = d.mediaType;
                        break;
                    }
                    break;
                case "NONE":
                case 0:
                    m.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    m.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.mediaType = 2;
                    break;
                }
                if (d.thumbnailURL != null) {
                    m.thumbnailURL = String(d.thumbnailURL);
                }
                if (d.mediaURL != null) {
                    m.mediaURL = String(d.mediaURL);
                }
                if (d.thumbnail != null) {
                    if (typeof d.thumbnail === "string")
                        $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                    else if (d.thumbnail.length >= 0)
                        m.thumbnail = d.thumbnail;
                }
                if (d.sourceType != null) {
                    m.sourceType = String(d.sourceType);
                }
                if (d.sourceID != null) {
                    m.sourceID = String(d.sourceID);
                }
                if (d.sourceURL != null) {
                    m.sourceURL = String(d.sourceURL);
                }
                if (d.containsAutoReply != null) {
                    m.containsAutoReply = Boolean(d.containsAutoReply);
                }
                if (d.renderLargerThumbnail != null) {
                    m.renderLargerThumbnail = Boolean(d.renderLargerThumbnail);
                }
                if (d.showAdAttribution != null) {
                    m.showAdAttribution = Boolean(d.showAdAttribution);
                }
                if (d.ctwaClid != null) {
                    m.ctwaClid = String(d.ctwaClid);
                }
                if (d.ref != null) {
                    m.ref = String(d.ref);
                }
                if (d.clickToWhatsappCall != null) {
                    m.clickToWhatsappCall = Boolean(d.clickToWhatsappCall);
                }
                if (d.adContextPreviewDismissed != null) {
                    m.adContextPreviewDismissed = Boolean(d.adContextPreviewDismissed);
                }
                if (d.sourceApp != null) {
                    m.sourceApp = String(d.sourceApp);
                }
                if (d.automatedGreetingMessageShown != null) {
                    m.automatedGreetingMessageShown = Boolean(d.automatedGreetingMessageShown);
                }
                if (d.greetingMessageBody != null) {
                    m.greetingMessageBody = String(d.greetingMessageBody);
                }
                if (d.ctaPayload != null) {
                    m.ctaPayload = String(d.ctaPayload);
                }
                if (d.disableNudge != null) {
                    m.disableNudge = Boolean(d.disableNudge);
                }
                if (d.originalImageURL != null) {
                    m.originalImageURL = String(d.originalImageURL);
                }
                if (d.automatedGreetingMessageCtaType != null) {
                    m.automatedGreetingMessageCtaType = String(d.automatedGreetingMessageCtaType);
                }
                if (d.wtwaAdFormat != null) {
                    m.wtwaAdFormat = Boolean(d.wtwaAdFormat);
                }
                switch (d.adType) {
                default:
                    if (typeof d.adType === "number") {
                        m.adType = d.adType;
                        break;
                    }
                    break;
                case "CTWA":
                case 0:
                    m.adType = 0;
                    break;
                case "CAWC":
                case 1:
                    m.adType = 1;
                    break;
                }
                if (d.wtwaWebsiteURL != null) {
                    m.wtwaWebsiteURL = String(d.wtwaWebsiteURL);
                }
                if (d.adPreviewURL != null) {
                    m.adPreviewURL = String(d.adPreviewURL);
                }
                if (d.containsCtwaFlowsAutoReply != null) {
                    m.containsCtwaFlowsAutoReply = Boolean(d.containsCtwaFlowsAutoReply);
                }
                if (d.agmThumbnailStrategy != null) {
                    m.agmThumbnailStrategy = d.agmThumbnailStrategy | 0;
                }
                if (d.agmTitleStrategy != null) {
                    m.agmTitleStrategy = d.agmTitleStrategy | 0;
                }
                if (d.agmSubtitleStrategy != null) {
                    m.agmSubtitleStrategy = d.agmSubtitleStrategy | 0;
                }
                if (d.agmHeaderInteractionStrategy != null) {
                    m.agmHeaderInteractionStrategy = d.agmHeaderInteractionStrategy | 0;
                }
                return m;
            };

            ExternalAdReplyInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.body != null && m.hasOwnProperty("body")) {
                    d.body = m.body;
                    if (o.oneofs)
                        d._body = "body";
                }
                if (m.mediaType != null && m.hasOwnProperty("mediaType")) {
                    d.mediaType = o.enums === String ? WAE2E.ContextInfo.ExternalAdReplyInfo.MediaType[m.mediaType] === undefined ? m.mediaType : WAE2E.ContextInfo.ExternalAdReplyInfo.MediaType[m.mediaType] : m.mediaType;
                    if (o.oneofs)
                        d._mediaType = "mediaType";
                }
                if (m.thumbnailURL != null && m.hasOwnProperty("thumbnailURL")) {
                    d.thumbnailURL = m.thumbnailURL;
                    if (o.oneofs)
                        d._thumbnailURL = "thumbnailURL";
                }
                if (m.mediaURL != null && m.hasOwnProperty("mediaURL")) {
                    d.mediaURL = m.mediaURL;
                    if (o.oneofs)
                        d._mediaURL = "mediaURL";
                }
                if (m.thumbnail != null && m.hasOwnProperty("thumbnail")) {
                    d.thumbnail = o.bytes === String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                    if (o.oneofs)
                        d._thumbnail = "thumbnail";
                }
                if (m.sourceType != null && m.hasOwnProperty("sourceType")) {
                    d.sourceType = m.sourceType;
                    if (o.oneofs)
                        d._sourceType = "sourceType";
                }
                if (m.sourceID != null && m.hasOwnProperty("sourceID")) {
                    d.sourceID = m.sourceID;
                    if (o.oneofs)
                        d._sourceID = "sourceID";
                }
                if (m.sourceURL != null && m.hasOwnProperty("sourceURL")) {
                    d.sourceURL = m.sourceURL;
                    if (o.oneofs)
                        d._sourceURL = "sourceURL";
                }
                if (m.containsAutoReply != null && m.hasOwnProperty("containsAutoReply")) {
                    d.containsAutoReply = m.containsAutoReply;
                    if (o.oneofs)
                        d._containsAutoReply = "containsAutoReply";
                }
                if (m.renderLargerThumbnail != null && m.hasOwnProperty("renderLargerThumbnail")) {
                    d.renderLargerThumbnail = m.renderLargerThumbnail;
                    if (o.oneofs)
                        d._renderLargerThumbnail = "renderLargerThumbnail";
                }
                if (m.showAdAttribution != null && m.hasOwnProperty("showAdAttribution")) {
                    d.showAdAttribution = m.showAdAttribution;
                    if (o.oneofs)
                        d._showAdAttribution = "showAdAttribution";
                }
                if (m.ctwaClid != null && m.hasOwnProperty("ctwaClid")) {
                    d.ctwaClid = m.ctwaClid;
                    if (o.oneofs)
                        d._ctwaClid = "ctwaClid";
                }
                if (m.ref != null && m.hasOwnProperty("ref")) {
                    d.ref = m.ref;
                    if (o.oneofs)
                        d._ref = "ref";
                }
                if (m.clickToWhatsappCall != null && m.hasOwnProperty("clickToWhatsappCall")) {
                    d.clickToWhatsappCall = m.clickToWhatsappCall;
                    if (o.oneofs)
                        d._clickToWhatsappCall = "clickToWhatsappCall";
                }
                if (m.adContextPreviewDismissed != null && m.hasOwnProperty("adContextPreviewDismissed")) {
                    d.adContextPreviewDismissed = m.adContextPreviewDismissed;
                    if (o.oneofs)
                        d._adContextPreviewDismissed = "adContextPreviewDismissed";
                }
                if (m.sourceApp != null && m.hasOwnProperty("sourceApp")) {
                    d.sourceApp = m.sourceApp;
                    if (o.oneofs)
                        d._sourceApp = "sourceApp";
                }
                if (m.automatedGreetingMessageShown != null && m.hasOwnProperty("automatedGreetingMessageShown")) {
                    d.automatedGreetingMessageShown = m.automatedGreetingMessageShown;
                    if (o.oneofs)
                        d._automatedGreetingMessageShown = "automatedGreetingMessageShown";
                }
                if (m.greetingMessageBody != null && m.hasOwnProperty("greetingMessageBody")) {
                    d.greetingMessageBody = m.greetingMessageBody;
                    if (o.oneofs)
                        d._greetingMessageBody = "greetingMessageBody";
                }
                if (m.ctaPayload != null && m.hasOwnProperty("ctaPayload")) {
                    d.ctaPayload = m.ctaPayload;
                    if (o.oneofs)
                        d._ctaPayload = "ctaPayload";
                }
                if (m.disableNudge != null && m.hasOwnProperty("disableNudge")) {
                    d.disableNudge = m.disableNudge;
                    if (o.oneofs)
                        d._disableNudge = "disableNudge";
                }
                if (m.originalImageURL != null && m.hasOwnProperty("originalImageURL")) {
                    d.originalImageURL = m.originalImageURL;
                    if (o.oneofs)
                        d._originalImageURL = "originalImageURL";
                }
                if (m.automatedGreetingMessageCtaType != null && m.hasOwnProperty("automatedGreetingMessageCtaType")) {
                    d.automatedGreetingMessageCtaType = m.automatedGreetingMessageCtaType;
                    if (o.oneofs)
                        d._automatedGreetingMessageCtaType = "automatedGreetingMessageCtaType";
                }
                if (m.wtwaAdFormat != null && m.hasOwnProperty("wtwaAdFormat")) {
                    d.wtwaAdFormat = m.wtwaAdFormat;
                    if (o.oneofs)
                        d._wtwaAdFormat = "wtwaAdFormat";
                }
                if (m.adType != null && m.hasOwnProperty("adType")) {
                    d.adType = o.enums === String ? WAE2E.ContextInfo.ExternalAdReplyInfo.AdType[m.adType] === undefined ? m.adType : WAE2E.ContextInfo.ExternalAdReplyInfo.AdType[m.adType] : m.adType;
                    if (o.oneofs)
                        d._adType = "adType";
                }
                if (m.wtwaWebsiteURL != null && m.hasOwnProperty("wtwaWebsiteURL")) {
                    d.wtwaWebsiteURL = m.wtwaWebsiteURL;
                    if (o.oneofs)
                        d._wtwaWebsiteURL = "wtwaWebsiteURL";
                }
                if (m.adPreviewURL != null && m.hasOwnProperty("adPreviewURL")) {
                    d.adPreviewURL = m.adPreviewURL;
                    if (o.oneofs)
                        d._adPreviewURL = "adPreviewURL";
                }
                if (m.containsCtwaFlowsAutoReply != null && m.hasOwnProperty("containsCtwaFlowsAutoReply")) {
                    d.containsCtwaFlowsAutoReply = m.containsCtwaFlowsAutoReply;
                    if (o.oneofs)
                        d._containsCtwaFlowsAutoReply = "containsCtwaFlowsAutoReply";
                }
                if (m.agmThumbnailStrategy != null && m.hasOwnProperty("agmThumbnailStrategy")) {
                    d.agmThumbnailStrategy = m.agmThumbnailStrategy;
                    if (o.oneofs)
                        d._agmThumbnailStrategy = "agmThumbnailStrategy";
                }
                if (m.agmTitleStrategy != null && m.hasOwnProperty("agmTitleStrategy")) {
                    d.agmTitleStrategy = m.agmTitleStrategy;
                    if (o.oneofs)
                        d._agmTitleStrategy = "agmTitleStrategy";
                }
                if (m.agmSubtitleStrategy != null && m.hasOwnProperty("agmSubtitleStrategy")) {
                    d.agmSubtitleStrategy = m.agmSubtitleStrategy;
                    if (o.oneofs)
                        d._agmSubtitleStrategy = "agmSubtitleStrategy";
                }
                if (m.agmHeaderInteractionStrategy != null && m.hasOwnProperty("agmHeaderInteractionStrategy")) {
                    d.agmHeaderInteractionStrategy = m.agmHeaderInteractionStrategy;
                    if (o.oneofs)
                        d._agmHeaderInteractionStrategy = "agmHeaderInteractionStrategy";
                }
                return d;
            };

            ExternalAdReplyInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExternalAdReplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.ExternalAdReplyInfo";
            };

            ExternalAdReplyInfo.AdType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "CTWA"] = 0;
                values[valuesById[1] = "CAWC"] = 1;
                return values;
            })();

            ExternalAdReplyInfo.MediaType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return ExternalAdReplyInfo;
        })();

        ContextInfo.AdReplyInfo = (function() {

            function AdReplyInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AdReplyInfo.prototype.advertiserName = null;
            AdReplyInfo.prototype.mediaType = null;
            AdReplyInfo.prototype.JPEGThumbnail = null;
            AdReplyInfo.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_advertiserName", {
                get: $util.oneOfGetter($oneOfFields = ["advertiserName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_JPEGThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AdReplyInfo.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AdReplyInfo.create = function create(properties) {
                return new AdReplyInfo(properties);
            };

            AdReplyInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.advertiserName != null && Object.hasOwnProperty.call(m, "advertiserName"))
                    w.uint32(10).string(m.advertiserName);
                if (m.mediaType != null && Object.hasOwnProperty.call(m, "mediaType"))
                    w.uint32(16).int32(m.mediaType);
                if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                    w.uint32(130).bytes(m.JPEGThumbnail);
                if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(138).string(m.caption);
                return w;
            };

            AdReplyInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.AdReplyInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.advertiserName = r.string();
                            break;
                        }
                    case 2: {
                            m.mediaType = r.int32();
                            break;
                        }
                    case 16: {
                            m.JPEGThumbnail = r.bytes();
                            break;
                        }
                    case 17: {
                            m.caption = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AdReplyInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.AdReplyInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.AdReplyInfo();
                if (d.advertiserName != null) {
                    m.advertiserName = String(d.advertiserName);
                }
                switch (d.mediaType) {
                default:
                    if (typeof d.mediaType === "number") {
                        m.mediaType = d.mediaType;
                        break;
                    }
                    break;
                case "NONE":
                case 0:
                    m.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    m.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.mediaType = 2;
                    break;
                }
                if (d.JPEGThumbnail != null) {
                    if (typeof d.JPEGThumbnail === "string")
                        $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                    else if (d.JPEGThumbnail.length >= 0)
                        m.JPEGThumbnail = d.JPEGThumbnail;
                }
                if (d.caption != null) {
                    m.caption = String(d.caption);
                }
                return m;
            };

            AdReplyInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.advertiserName != null && m.hasOwnProperty("advertiserName")) {
                    d.advertiserName = m.advertiserName;
                    if (o.oneofs)
                        d._advertiserName = "advertiserName";
                }
                if (m.mediaType != null && m.hasOwnProperty("mediaType")) {
                    d.mediaType = o.enums === String ? WAE2E.ContextInfo.AdReplyInfo.MediaType[m.mediaType] === undefined ? m.mediaType : WAE2E.ContextInfo.AdReplyInfo.MediaType[m.mediaType] : m.mediaType;
                    if (o.oneofs)
                        d._mediaType = "mediaType";
                }
                if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                    d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                    if (o.oneofs)
                        d._JPEGThumbnail = "JPEGThumbnail";
                }
                if (m.caption != null && m.hasOwnProperty("caption")) {
                    d.caption = m.caption;
                    if (o.oneofs)
                        d._caption = "caption";
                }
                return d;
            };

            AdReplyInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AdReplyInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.AdReplyInfo";
            };

            AdReplyInfo.MediaType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return AdReplyInfo;
        })();

        ContextInfo.PartiallySelectedContent = (function() {

            function PartiallySelectedContent(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PartiallySelectedContent.prototype.text = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PartiallySelectedContent.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PartiallySelectedContent.create = function create(properties) {
                return new PartiallySelectedContent(properties);
            };

            PartiallySelectedContent.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                return w;
            };

            PartiallySelectedContent.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.PartiallySelectedContent();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.text = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PartiallySelectedContent.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.PartiallySelectedContent)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.PartiallySelectedContent();
                if (d.text != null) {
                    m.text = String(d.text);
                }
                return m;
            };

            PartiallySelectedContent.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.text != null && m.hasOwnProperty("text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d._text = "text";
                }
                return d;
            };

            PartiallySelectedContent.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PartiallySelectedContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.PartiallySelectedContent";
            };

            return PartiallySelectedContent;
        })();

        ContextInfo.FeatureEligibilities = (function() {

            function FeatureEligibilities(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            FeatureEligibilities.prototype.cannotBeReactedTo = null;
            FeatureEligibilities.prototype.cannotBeRanked = null;
            FeatureEligibilities.prototype.canRequestFeedback = null;
            FeatureEligibilities.prototype.canBeReshared = null;
            FeatureEligibilities.prototype.canReceiveMultiReact = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeReactedTo", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeReactedTo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeRanked", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeRanked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canRequestFeedback", {
                get: $util.oneOfGetter($oneOfFields = ["canRequestFeedback"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canBeReshared", {
                get: $util.oneOfGetter($oneOfFields = ["canBeReshared"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FeatureEligibilities.prototype, "_canReceiveMultiReact", {
                get: $util.oneOfGetter($oneOfFields = ["canReceiveMultiReact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FeatureEligibilities.create = function create(properties) {
                return new FeatureEligibilities(properties);
            };

            FeatureEligibilities.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.cannotBeReactedTo != null && Object.hasOwnProperty.call(m, "cannotBeReactedTo"))
                    w.uint32(8).bool(m.cannotBeReactedTo);
                if (m.cannotBeRanked != null && Object.hasOwnProperty.call(m, "cannotBeRanked"))
                    w.uint32(16).bool(m.cannotBeRanked);
                if (m.canRequestFeedback != null && Object.hasOwnProperty.call(m, "canRequestFeedback"))
                    w.uint32(24).bool(m.canRequestFeedback);
                if (m.canBeReshared != null && Object.hasOwnProperty.call(m, "canBeReshared"))
                    w.uint32(32).bool(m.canBeReshared);
                if (m.canReceiveMultiReact != null && Object.hasOwnProperty.call(m, "canReceiveMultiReact"))
                    w.uint32(40).bool(m.canReceiveMultiReact);
                return w;
            };

            FeatureEligibilities.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.FeatureEligibilities();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.cannotBeReactedTo = r.bool();
                            break;
                        }
                    case 2: {
                            m.cannotBeRanked = r.bool();
                            break;
                        }
                    case 3: {
                            m.canRequestFeedback = r.bool();
                            break;
                        }
                    case 4: {
                            m.canBeReshared = r.bool();
                            break;
                        }
                    case 5: {
                            m.canReceiveMultiReact = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            FeatureEligibilities.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.FeatureEligibilities)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.FeatureEligibilities();
                if (d.cannotBeReactedTo != null) {
                    m.cannotBeReactedTo = Boolean(d.cannotBeReactedTo);
                }
                if (d.cannotBeRanked != null) {
                    m.cannotBeRanked = Boolean(d.cannotBeRanked);
                }
                if (d.canRequestFeedback != null) {
                    m.canRequestFeedback = Boolean(d.canRequestFeedback);
                }
                if (d.canBeReshared != null) {
                    m.canBeReshared = Boolean(d.canBeReshared);
                }
                if (d.canReceiveMultiReact != null) {
                    m.canReceiveMultiReact = Boolean(d.canReceiveMultiReact);
                }
                return m;
            };

            FeatureEligibilities.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.cannotBeReactedTo != null && m.hasOwnProperty("cannotBeReactedTo")) {
                    d.cannotBeReactedTo = m.cannotBeReactedTo;
                    if (o.oneofs)
                        d._cannotBeReactedTo = "cannotBeReactedTo";
                }
                if (m.cannotBeRanked != null && m.hasOwnProperty("cannotBeRanked")) {
                    d.cannotBeRanked = m.cannotBeRanked;
                    if (o.oneofs)
                        d._cannotBeRanked = "cannotBeRanked";
                }
                if (m.canRequestFeedback != null && m.hasOwnProperty("canRequestFeedback")) {
                    d.canRequestFeedback = m.canRequestFeedback;
                    if (o.oneofs)
                        d._canRequestFeedback = "canRequestFeedback";
                }
                if (m.canBeReshared != null && m.hasOwnProperty("canBeReshared")) {
                    d.canBeReshared = m.canBeReshared;
                    if (o.oneofs)
                        d._canBeReshared = "canBeReshared";
                }
                if (m.canReceiveMultiReact != null && m.hasOwnProperty("canReceiveMultiReact")) {
                    d.canReceiveMultiReact = m.canReceiveMultiReact;
                    if (o.oneofs)
                        d._canReceiveMultiReact = "canReceiveMultiReact";
                }
                return d;
            };

            FeatureEligibilities.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FeatureEligibilities.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.FeatureEligibilities";
            };

            return FeatureEligibilities;
        })();

        ContextInfo.QuestionReplyQuotedMessage = (function() {

            function QuestionReplyQuotedMessage(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            QuestionReplyQuotedMessage.prototype.serverQuestionID = null;
            QuestionReplyQuotedMessage.prototype.quotedQuestion = null;
            QuestionReplyQuotedMessage.prototype.quotedResponse = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_serverQuestionID", {
                get: $util.oneOfGetter($oneOfFields = ["serverQuestionID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedQuestion", {
                get: $util.oneOfGetter($oneOfFields = ["quotedQuestion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedResponse", {
                get: $util.oneOfGetter($oneOfFields = ["quotedResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuestionReplyQuotedMessage.create = function create(properties) {
                return new QuestionReplyQuotedMessage(properties);
            };

            QuestionReplyQuotedMessage.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.serverQuestionID != null && Object.hasOwnProperty.call(m, "serverQuestionID"))
                    w.uint32(8).int32(m.serverQuestionID);
                if (m.quotedQuestion != null && Object.hasOwnProperty.call(m, "quotedQuestion"))
                    WAE2E.Message.encode(m.quotedQuestion, w.uint32(18).fork()).ldelim();
                if (m.quotedResponse != null && Object.hasOwnProperty.call(m, "quotedResponse"))
                    WAE2E.Message.encode(m.quotedResponse, w.uint32(26).fork()).ldelim();
                return w;
            };

            QuestionReplyQuotedMessage.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.QuestionReplyQuotedMessage();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.serverQuestionID = r.int32();
                            break;
                        }
                    case 2: {
                            m.quotedQuestion = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.quotedResponse = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            QuestionReplyQuotedMessage.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.QuestionReplyQuotedMessage)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.QuestionReplyQuotedMessage();
                if (d.serverQuestionID != null) {
                    m.serverQuestionID = d.serverQuestionID | 0;
                }
                if (d.quotedQuestion != null) {
                    if (typeof d.quotedQuestion !== "object")
                        throw TypeError(".WAE2E.ContextInfo.QuestionReplyQuotedMessage.quotedQuestion: object expected");
                    m.quotedQuestion = WAE2E.Message.fromObject(d.quotedQuestion, n + 1);
                }
                if (d.quotedResponse != null) {
                    if (typeof d.quotedResponse !== "object")
                        throw TypeError(".WAE2E.ContextInfo.QuestionReplyQuotedMessage.quotedResponse: object expected");
                    m.quotedResponse = WAE2E.Message.fromObject(d.quotedResponse, n + 1);
                }
                return m;
            };

            QuestionReplyQuotedMessage.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.serverQuestionID != null && m.hasOwnProperty("serverQuestionID")) {
                    d.serverQuestionID = m.serverQuestionID;
                    if (o.oneofs)
                        d._serverQuestionID = "serverQuestionID";
                }
                if (m.quotedQuestion != null && m.hasOwnProperty("quotedQuestion")) {
                    d.quotedQuestion = WAE2E.Message.toObject(m.quotedQuestion, o);
                    if (o.oneofs)
                        d._quotedQuestion = "quotedQuestion";
                }
                if (m.quotedResponse != null && m.hasOwnProperty("quotedResponse")) {
                    d.quotedResponse = WAE2E.Message.toObject(m.quotedResponse, o);
                    if (o.oneofs)
                        d._quotedResponse = "quotedResponse";
                }
                return d;
            };

            QuestionReplyQuotedMessage.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuestionReplyQuotedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.QuestionReplyQuotedMessage";
            };

            return QuestionReplyQuotedMessage;
        })();

        ContextInfo.UTMInfo = (function() {

            function UTMInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            UTMInfo.prototype.utmSource = null;
            UTMInfo.prototype.utmCampaign = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UTMInfo.prototype, "_utmSource", {
                get: $util.oneOfGetter($oneOfFields = ["utmSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UTMInfo.prototype, "_utmCampaign", {
                get: $util.oneOfGetter($oneOfFields = ["utmCampaign"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UTMInfo.create = function create(properties) {
                return new UTMInfo(properties);
            };

            UTMInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.utmSource != null && Object.hasOwnProperty.call(m, "utmSource"))
                    w.uint32(10).string(m.utmSource);
                if (m.utmCampaign != null && Object.hasOwnProperty.call(m, "utmCampaign"))
                    w.uint32(18).string(m.utmCampaign);
                return w;
            };

            UTMInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.UTMInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.utmSource = r.string();
                            break;
                        }
                    case 2: {
                            m.utmCampaign = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            UTMInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.UTMInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.UTMInfo();
                if (d.utmSource != null) {
                    m.utmSource = String(d.utmSource);
                }
                if (d.utmCampaign != null) {
                    m.utmCampaign = String(d.utmCampaign);
                }
                return m;
            };

            UTMInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.utmSource != null && m.hasOwnProperty("utmSource")) {
                    d.utmSource = m.utmSource;
                    if (o.oneofs)
                        d._utmSource = "utmSource";
                }
                if (m.utmCampaign != null && m.hasOwnProperty("utmCampaign")) {
                    d.utmCampaign = m.utmCampaign;
                    if (o.oneofs)
                        d._utmCampaign = "utmCampaign";
                }
                return d;
            };

            UTMInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            UTMInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.UTMInfo";
            };

            return UTMInfo;
        })();

        ContextInfo.BusinessMessageForwardInfo = (function() {

            function BusinessMessageForwardInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BusinessMessageForwardInfo.prototype.businessOwnerJID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BusinessMessageForwardInfo.prototype, "_businessOwnerJID", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessMessageForwardInfo.create = function create(properties) {
                return new BusinessMessageForwardInfo(properties);
            };

            BusinessMessageForwardInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.businessOwnerJID != null && Object.hasOwnProperty.call(m, "businessOwnerJID"))
                    w.uint32(10).string(m.businessOwnerJID);
                return w;
            };

            BusinessMessageForwardInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContextInfo.BusinessMessageForwardInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.businessOwnerJID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BusinessMessageForwardInfo.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ContextInfo.BusinessMessageForwardInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ContextInfo.BusinessMessageForwardInfo();
                if (d.businessOwnerJID != null) {
                    m.businessOwnerJID = String(d.businessOwnerJID);
                }
                return m;
            };

            BusinessMessageForwardInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.businessOwnerJID != null && m.hasOwnProperty("businessOwnerJID")) {
                    d.businessOwnerJID = m.businessOwnerJID;
                    if (o.oneofs)
                        d._businessOwnerJID = "businessOwnerJID";
                }
                return d;
            };

            BusinessMessageForwardInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessMessageForwardInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ContextInfo.BusinessMessageForwardInfo";
            };

            return BusinessMessageForwardInfo;
        })();

        return ContextInfo;
    })();

    WAE2E.MessageAssociation = (function() {

        function MessageAssociation(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageAssociation.prototype.associationType = null;
        MessageAssociation.prototype.parentMessageKey = null;
        MessageAssociation.prototype.messageIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_associationType", {
            get: $util.oneOfGetter($oneOfFields = ["associationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_parentMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageAssociation.prototype, "_messageIndex", {
            get: $util.oneOfGetter($oneOfFields = ["messageIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAssociation.create = function create(properties) {
            return new MessageAssociation(properties);
        };

        MessageAssociation.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.associationType != null && Object.hasOwnProperty.call(m, "associationType"))
                w.uint32(8).int32(m.associationType);
            if (m.parentMessageKey != null && Object.hasOwnProperty.call(m, "parentMessageKey"))
                WACommon.MessageKey.encode(m.parentMessageKey, w.uint32(18).fork()).ldelim();
            if (m.messageIndex != null && Object.hasOwnProperty.call(m, "messageIndex"))
                w.uint32(24).int32(m.messageIndex);
            return w;
        };

        MessageAssociation.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageAssociation();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.associationType = r.int32();
                        break;
                    }
                case 2: {
                        m.parentMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.messageIndex = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageAssociation.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageAssociation)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageAssociation();
            switch (d.associationType) {
            default:
                if (typeof d.associationType === "number") {
                    m.associationType = d.associationType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.associationType = 0;
                break;
            case "MEDIA_ALBUM":
            case 1:
                m.associationType = 1;
                break;
            case "BOT_PLUGIN":
            case 2:
                m.associationType = 2;
                break;
            case "EVENT_COVER_IMAGE":
            case 3:
                m.associationType = 3;
                break;
            case "STATUS_POLL":
            case 4:
                m.associationType = 4;
                break;
            case "HD_VIDEO_DUAL_UPLOAD":
            case 5:
                m.associationType = 5;
                break;
            case "STATUS_EXTERNAL_RESHARE":
            case 6:
                m.associationType = 6;
                break;
            case "MEDIA_POLL":
            case 7:
                m.associationType = 7;
                break;
            case "STATUS_ADD_YOURS":
            case 8:
                m.associationType = 8;
                break;
            case "STATUS_NOTIFICATION":
            case 9:
                m.associationType = 9;
                break;
            case "HD_IMAGE_DUAL_UPLOAD":
            case 10:
                m.associationType = 10;
                break;
            case "STICKER_ANNOTATION":
            case 11:
                m.associationType = 11;
                break;
            case "MOTION_PHOTO":
            case 12:
                m.associationType = 12;
                break;
            case "STATUS_LINK_ACTION":
            case 13:
                m.associationType = 13;
                break;
            case "VIEW_ALL_REPLIES":
            case 14:
                m.associationType = 14;
                break;
            case "STATUS_ADD_YOURS_AI_IMAGINE":
            case 15:
                m.associationType = 15;
                break;
            case "STATUS_QUESTION":
            case 16:
                m.associationType = 16;
                break;
            case "STATUS_ADD_YOURS_DIWALI":
            case 17:
                m.associationType = 17;
                break;
            case "STATUS_REACTION":
            case 18:
                m.associationType = 18;
                break;
            case "HEVC_VIDEO_DUAL_UPLOAD":
            case 19:
                m.associationType = 19;
                break;
            case "POLL_ADD_OPTION":
            case 20:
                m.associationType = 20;
                break;
            }
            if (d.parentMessageKey != null) {
                if (typeof d.parentMessageKey !== "object")
                    throw TypeError(".WAE2E.MessageAssociation.parentMessageKey: object expected");
                m.parentMessageKey = WACommon.MessageKey.fromObject(d.parentMessageKey, n + 1);
            }
            if (d.messageIndex != null) {
                m.messageIndex = d.messageIndex | 0;
            }
            return m;
        };

        MessageAssociation.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.associationType != null && m.hasOwnProperty("associationType")) {
                d.associationType = o.enums === String ? WAE2E.MessageAssociation.AssociationType[m.associationType] === undefined ? m.associationType : WAE2E.MessageAssociation.AssociationType[m.associationType] : m.associationType;
                if (o.oneofs)
                    d._associationType = "associationType";
            }
            if (m.parentMessageKey != null && m.hasOwnProperty("parentMessageKey")) {
                d.parentMessageKey = WACommon.MessageKey.toObject(m.parentMessageKey, o);
                if (o.oneofs)
                    d._parentMessageKey = "parentMessageKey";
            }
            if (m.messageIndex != null && m.hasOwnProperty("messageIndex")) {
                d.messageIndex = m.messageIndex;
                if (o.oneofs)
                    d._messageIndex = "messageIndex";
            }
            return d;
        };

        MessageAssociation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAssociation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageAssociation";
        };

        MessageAssociation.AssociationType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "MEDIA_ALBUM"] = 1;
            values[valuesById[2] = "BOT_PLUGIN"] = 2;
            values[valuesById[3] = "EVENT_COVER_IMAGE"] = 3;
            values[valuesById[4] = "STATUS_POLL"] = 4;
            values[valuesById[5] = "HD_VIDEO_DUAL_UPLOAD"] = 5;
            values[valuesById[6] = "STATUS_EXTERNAL_RESHARE"] = 6;
            values[valuesById[7] = "MEDIA_POLL"] = 7;
            values[valuesById[8] = "STATUS_ADD_YOURS"] = 8;
            values[valuesById[9] = "STATUS_NOTIFICATION"] = 9;
            values[valuesById[10] = "HD_IMAGE_DUAL_UPLOAD"] = 10;
            values[valuesById[11] = "STICKER_ANNOTATION"] = 11;
            values[valuesById[12] = "MOTION_PHOTO"] = 12;
            values[valuesById[13] = "STATUS_LINK_ACTION"] = 13;
            values[valuesById[14] = "VIEW_ALL_REPLIES"] = 14;
            values[valuesById[15] = "STATUS_ADD_YOURS_AI_IMAGINE"] = 15;
            values[valuesById[16] = "STATUS_QUESTION"] = 16;
            values[valuesById[17] = "STATUS_ADD_YOURS_DIWALI"] = 17;
            values[valuesById[18] = "STATUS_REACTION"] = 18;
            values[valuesById[19] = "HEVC_VIDEO_DUAL_UPLOAD"] = 19;
            values[valuesById[20] = "POLL_ADD_OPTION"] = 20;
            return values;
        })();

        return MessageAssociation;
    })();

    WAE2E.ThreadID = (function() {

        function ThreadID(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ThreadID.prototype.threadType = null;
        ThreadID.prototype.threadKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ThreadID.prototype, "_threadType", {
            get: $util.oneOfGetter($oneOfFields = ["threadType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ThreadID.prototype, "_threadKey", {
            get: $util.oneOfGetter($oneOfFields = ["threadKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ThreadID.create = function create(properties) {
            return new ThreadID(properties);
        };

        ThreadID.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.threadType != null && Object.hasOwnProperty.call(m, "threadType"))
                w.uint32(8).int32(m.threadType);
            if (m.threadKey != null && Object.hasOwnProperty.call(m, "threadKey"))
                WACommon.MessageKey.encode(m.threadKey, w.uint32(18).fork()).ldelim();
            return w;
        };

        ThreadID.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ThreadID();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.threadType = r.int32();
                        break;
                    }
                case 2: {
                        m.threadKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ThreadID.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ThreadID)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ThreadID();
            switch (d.threadType) {
            default:
                if (typeof d.threadType === "number") {
                    m.threadType = d.threadType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.threadType = 0;
                break;
            case "VIEW_REPLIES":
            case 1:
                m.threadType = 1;
                break;
            case "AI_THREAD":
            case 2:
                m.threadType = 2;
                break;
            }
            if (d.threadKey != null) {
                if (typeof d.threadKey !== "object")
                    throw TypeError(".WAE2E.ThreadID.threadKey: object expected");
                m.threadKey = WACommon.MessageKey.fromObject(d.threadKey, n + 1);
            }
            return m;
        };

        ThreadID.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.threadType != null && m.hasOwnProperty("threadType")) {
                d.threadType = o.enums === String ? WAE2E.ThreadID.ThreadType[m.threadType] === undefined ? m.threadType : WAE2E.ThreadID.ThreadType[m.threadType] : m.threadType;
                if (o.oneofs)
                    d._threadType = "threadType";
            }
            if (m.threadKey != null && m.hasOwnProperty("threadKey")) {
                d.threadKey = WACommon.MessageKey.toObject(m.threadKey, o);
                if (o.oneofs)
                    d._threadKey = "threadKey";
            }
            return d;
        };

        ThreadID.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ThreadID.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ThreadID";
        };

        ThreadID.ThreadType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "VIEW_REPLIES"] = 1;
            values[valuesById[2] = "AI_THREAD"] = 2;
            return values;
        })();

        return ThreadID;
    })();

    WAE2E.MessageContextInfo = (function() {

        function MessageContextInfo(p) {
            this.threadID = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageContextInfo.prototype.deviceListMetadata = null;
        MessageContextInfo.prototype.deviceListMetadataVersion = null;
        MessageContextInfo.prototype.messageSecret = null;
        MessageContextInfo.prototype.paddingBytes = null;
        MessageContextInfo.prototype.messageAddOnDurationInSecs = null;
        MessageContextInfo.prototype.botMessageSecret = null;
        MessageContextInfo.prototype.botMetadata = null;
        MessageContextInfo.prototype.reportingTokenVersion = null;
        MessageContextInfo.prototype.messageAddOnExpiryType = null;
        MessageContextInfo.prototype.messageAssociation = null;
        MessageContextInfo.prototype.capiCreatedGroup = null;
        MessageContextInfo.prototype.supportPayload = null;
        MessageContextInfo.prototype.limitSharing = null;
        MessageContextInfo.prototype.limitSharingV2 = null;
        MessageContextInfo.prototype.threadID = $util.emptyArray;
        MessageContextInfo.prototype.weblinkRenderConfig = null;
        MessageContextInfo.prototype.teeBotMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadataVersion", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadataVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_paddingBytes", {
            get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_botMessageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_botMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_reportingTokenVersion", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_messageAssociation", {
            get: $util.oneOfGetter($oneOfFields = ["messageAssociation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_capiCreatedGroup", {
            get: $util.oneOfGetter($oneOfFields = ["capiCreatedGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_supportPayload", {
            get: $util.oneOfGetter($oneOfFields = ["supportPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_limitSharingV2", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_weblinkRenderConfig", {
            get: $util.oneOfGetter($oneOfFields = ["weblinkRenderConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageContextInfo.prototype, "_teeBotMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["teeBotMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageContextInfo.create = function create(properties) {
            return new MessageContextInfo(properties);
        };

        MessageContextInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.deviceListMetadata != null && Object.hasOwnProperty.call(m, "deviceListMetadata"))
                WAE2E.DeviceListMetadata.encode(m.deviceListMetadata, w.uint32(10).fork()).ldelim();
            if (m.deviceListMetadataVersion != null && Object.hasOwnProperty.call(m, "deviceListMetadataVersion"))
                w.uint32(16).int32(m.deviceListMetadataVersion);
            if (m.messageSecret != null && Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(26).bytes(m.messageSecret);
            if (m.paddingBytes != null && Object.hasOwnProperty.call(m, "paddingBytes"))
                w.uint32(34).bytes(m.paddingBytes);
            if (m.messageAddOnDurationInSecs != null && Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs"))
                w.uint32(40).uint32(m.messageAddOnDurationInSecs);
            if (m.botMessageSecret != null && Object.hasOwnProperty.call(m, "botMessageSecret"))
                w.uint32(50).bytes(m.botMessageSecret);
            if (m.botMetadata != null && Object.hasOwnProperty.call(m, "botMetadata"))
                $root.WAAICommon.BotMetadata.encode(m.botMetadata, w.uint32(58).fork()).ldelim();
            if (m.reportingTokenVersion != null && Object.hasOwnProperty.call(m, "reportingTokenVersion"))
                w.uint32(64).int32(m.reportingTokenVersion);
            if (m.messageAddOnExpiryType != null && Object.hasOwnProperty.call(m, "messageAddOnExpiryType"))
                w.uint32(72).int32(m.messageAddOnExpiryType);
            if (m.messageAssociation != null && Object.hasOwnProperty.call(m, "messageAssociation"))
                WAE2E.MessageAssociation.encode(m.messageAssociation, w.uint32(82).fork()).ldelim();
            if (m.capiCreatedGroup != null && Object.hasOwnProperty.call(m, "capiCreatedGroup"))
                w.uint32(88).bool(m.capiCreatedGroup);
            if (m.supportPayload != null && Object.hasOwnProperty.call(m, "supportPayload"))
                w.uint32(98).string(m.supportPayload);
            if (m.limitSharing != null && Object.hasOwnProperty.call(m, "limitSharing"))
                WACommon.LimitSharing.encode(m.limitSharing, w.uint32(106).fork()).ldelim();
            if (m.limitSharingV2 != null && Object.hasOwnProperty.call(m, "limitSharingV2"))
                WACommon.LimitSharing.encode(m.limitSharingV2, w.uint32(114).fork()).ldelim();
            if (m.threadID != null && m.threadID.length) {
                for (var i = 0; i < m.threadID.length; ++i)
                    WAE2E.ThreadID.encode(m.threadID[i], w.uint32(122).fork()).ldelim();
            }
            if (m.weblinkRenderConfig != null && Object.hasOwnProperty.call(m, "weblinkRenderConfig"))
                w.uint32(128).int32(m.weblinkRenderConfig);
            if (m.teeBotMetadata != null && Object.hasOwnProperty.call(m, "teeBotMetadata"))
                w.uint32(138).bytes(m.teeBotMetadata);
            return w;
        };

        MessageContextInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageContextInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.deviceListMetadata = WAE2E.DeviceListMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.deviceListMetadataVersion = r.int32();
                        break;
                    }
                case 3: {
                        m.messageSecret = r.bytes();
                        break;
                    }
                case 4: {
                        m.paddingBytes = r.bytes();
                        break;
                    }
                case 5: {
                        m.messageAddOnDurationInSecs = r.uint32();
                        break;
                    }
                case 6: {
                        m.botMessageSecret = r.bytes();
                        break;
                    }
                case 7: {
                        m.botMetadata = $root.WAAICommon.BotMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.reportingTokenVersion = r.int32();
                        break;
                    }
                case 9: {
                        m.messageAddOnExpiryType = r.int32();
                        break;
                    }
                case 10: {
                        m.messageAssociation = WAE2E.MessageAssociation.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.capiCreatedGroup = r.bool();
                        break;
                    }
                case 12: {
                        m.supportPayload = r.string();
                        break;
                    }
                case 13: {
                        m.limitSharing = WACommon.LimitSharing.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 14: {
                        m.limitSharingV2 = WACommon.LimitSharing.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        if (!(m.threadID && m.threadID.length))
                            m.threadID = [];
                        m.threadID.push(WAE2E.ThreadID.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 16: {
                        m.weblinkRenderConfig = r.int32();
                        break;
                    }
                case 17: {
                        m.teeBotMetadata = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageContextInfo.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageContextInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageContextInfo();
            if (d.deviceListMetadata != null) {
                if (typeof d.deviceListMetadata !== "object")
                    throw TypeError(".WAE2E.MessageContextInfo.deviceListMetadata: object expected");
                m.deviceListMetadata = WAE2E.DeviceListMetadata.fromObject(d.deviceListMetadata, n + 1);
            }
            if (d.deviceListMetadataVersion != null) {
                m.deviceListMetadataVersion = d.deviceListMetadataVersion | 0;
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            if (d.paddingBytes != null) {
                if (typeof d.paddingBytes === "string")
                    $util.base64.decode(d.paddingBytes, m.paddingBytes = $util.newBuffer($util.base64.length(d.paddingBytes)), 0);
                else if (d.paddingBytes.length >= 0)
                    m.paddingBytes = d.paddingBytes;
            }
            if (d.messageAddOnDurationInSecs != null) {
                m.messageAddOnDurationInSecs = d.messageAddOnDurationInSecs >>> 0;
            }
            if (d.botMessageSecret != null) {
                if (typeof d.botMessageSecret === "string")
                    $util.base64.decode(d.botMessageSecret, m.botMessageSecret = $util.newBuffer($util.base64.length(d.botMessageSecret)), 0);
                else if (d.botMessageSecret.length >= 0)
                    m.botMessageSecret = d.botMessageSecret;
            }
            if (d.botMetadata != null) {
                if (typeof d.botMetadata !== "object")
                    throw TypeError(".WAE2E.MessageContextInfo.botMetadata: object expected");
                m.botMetadata = $root.WAAICommon.BotMetadata.fromObject(d.botMetadata, n + 1);
            }
            if (d.reportingTokenVersion != null) {
                m.reportingTokenVersion = d.reportingTokenVersion | 0;
            }
            switch (d.messageAddOnExpiryType) {
            default:
                if (typeof d.messageAddOnExpiryType === "number") {
                    m.messageAddOnExpiryType = d.messageAddOnExpiryType;
                    break;
                }
                break;
            case "STATIC":
            case 1:
                m.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                m.messageAddOnExpiryType = 2;
                break;
            }
            if (d.messageAssociation != null) {
                if (typeof d.messageAssociation !== "object")
                    throw TypeError(".WAE2E.MessageContextInfo.messageAssociation: object expected");
                m.messageAssociation = WAE2E.MessageAssociation.fromObject(d.messageAssociation, n + 1);
            }
            if (d.capiCreatedGroup != null) {
                m.capiCreatedGroup = Boolean(d.capiCreatedGroup);
            }
            if (d.supportPayload != null) {
                m.supportPayload = String(d.supportPayload);
            }
            if (d.limitSharing != null) {
                if (typeof d.limitSharing !== "object")
                    throw TypeError(".WAE2E.MessageContextInfo.limitSharing: object expected");
                m.limitSharing = WACommon.LimitSharing.fromObject(d.limitSharing, n + 1);
            }
            if (d.limitSharingV2 != null) {
                if (typeof d.limitSharingV2 !== "object")
                    throw TypeError(".WAE2E.MessageContextInfo.limitSharingV2: object expected");
                m.limitSharingV2 = WACommon.LimitSharing.fromObject(d.limitSharingV2, n + 1);
            }
            if (d.threadID) {
                if (!Array.isArray(d.threadID))
                    throw TypeError(".WAE2E.MessageContextInfo.threadID: array expected");
                m.threadID = [];
                for (var i = 0; i < d.threadID.length; ++i) {
                    if (typeof d.threadID[i] !== "object")
                        throw TypeError(".WAE2E.MessageContextInfo.threadID: object expected");
                    m.threadID[i] = WAE2E.ThreadID.fromObject(d.threadID[i], n + 1);
                }
            }
            switch (d.weblinkRenderConfig) {
            default:
                if (typeof d.weblinkRenderConfig === "number") {
                    m.weblinkRenderConfig = d.weblinkRenderConfig;
                    break;
                }
                break;
            case "WEBVIEW":
            case 0:
                m.weblinkRenderConfig = 0;
                break;
            case "SYSTEM":
            case 1:
                m.weblinkRenderConfig = 1;
                break;
            }
            if (d.teeBotMetadata != null) {
                if (typeof d.teeBotMetadata === "string")
                    $util.base64.decode(d.teeBotMetadata, m.teeBotMetadata = $util.newBuffer($util.base64.length(d.teeBotMetadata)), 0);
                else if (d.teeBotMetadata.length >= 0)
                    m.teeBotMetadata = d.teeBotMetadata;
            }
            return m;
        };

        MessageContextInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.threadID = [];
            }
            if (m.deviceListMetadata != null && m.hasOwnProperty("deviceListMetadata")) {
                d.deviceListMetadata = WAE2E.DeviceListMetadata.toObject(m.deviceListMetadata, o);
                if (o.oneofs)
                    d._deviceListMetadata = "deviceListMetadata";
            }
            if (m.deviceListMetadataVersion != null && m.hasOwnProperty("deviceListMetadataVersion")) {
                d.deviceListMetadataVersion = m.deviceListMetadataVersion;
                if (o.oneofs)
                    d._deviceListMetadataVersion = "deviceListMetadataVersion";
            }
            if (m.messageSecret != null && m.hasOwnProperty("messageSecret")) {
                d.messageSecret = o.bytes === String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
                if (o.oneofs)
                    d._messageSecret = "messageSecret";
            }
            if (m.paddingBytes != null && m.hasOwnProperty("paddingBytes")) {
                d.paddingBytes = o.bytes === String ? $util.base64.encode(m.paddingBytes, 0, m.paddingBytes.length) : o.bytes === Array ? Array.prototype.slice.call(m.paddingBytes) : m.paddingBytes;
                if (o.oneofs)
                    d._paddingBytes = "paddingBytes";
            }
            if (m.messageAddOnDurationInSecs != null && m.hasOwnProperty("messageAddOnDurationInSecs")) {
                d.messageAddOnDurationInSecs = m.messageAddOnDurationInSecs;
                if (o.oneofs)
                    d._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
            }
            if (m.botMessageSecret != null && m.hasOwnProperty("botMessageSecret")) {
                d.botMessageSecret = o.bytes === String ? $util.base64.encode(m.botMessageSecret, 0, m.botMessageSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.botMessageSecret) : m.botMessageSecret;
                if (o.oneofs)
                    d._botMessageSecret = "botMessageSecret";
            }
            if (m.botMetadata != null && m.hasOwnProperty("botMetadata")) {
                d.botMetadata = $root.WAAICommon.BotMetadata.toObject(m.botMetadata, o);
                if (o.oneofs)
                    d._botMetadata = "botMetadata";
            }
            if (m.reportingTokenVersion != null && m.hasOwnProperty("reportingTokenVersion")) {
                d.reportingTokenVersion = m.reportingTokenVersion;
                if (o.oneofs)
                    d._reportingTokenVersion = "reportingTokenVersion";
            }
            if (m.messageAddOnExpiryType != null && m.hasOwnProperty("messageAddOnExpiryType")) {
                d.messageAddOnExpiryType = o.enums === String ? WAE2E.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] === undefined ? m.messageAddOnExpiryType : WAE2E.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] : m.messageAddOnExpiryType;
                if (o.oneofs)
                    d._messageAddOnExpiryType = "messageAddOnExpiryType";
            }
            if (m.messageAssociation != null && m.hasOwnProperty("messageAssociation")) {
                d.messageAssociation = WAE2E.MessageAssociation.toObject(m.messageAssociation, o);
                if (o.oneofs)
                    d._messageAssociation = "messageAssociation";
            }
            if (m.capiCreatedGroup != null && m.hasOwnProperty("capiCreatedGroup")) {
                d.capiCreatedGroup = m.capiCreatedGroup;
                if (o.oneofs)
                    d._capiCreatedGroup = "capiCreatedGroup";
            }
            if (m.supportPayload != null && m.hasOwnProperty("supportPayload")) {
                d.supportPayload = m.supportPayload;
                if (o.oneofs)
                    d._supportPayload = "supportPayload";
            }
            if (m.limitSharing != null && m.hasOwnProperty("limitSharing")) {
                d.limitSharing = WACommon.LimitSharing.toObject(m.limitSharing, o);
                if (o.oneofs)
                    d._limitSharing = "limitSharing";
            }
            if (m.limitSharingV2 != null && m.hasOwnProperty("limitSharingV2")) {
                d.limitSharingV2 = WACommon.LimitSharing.toObject(m.limitSharingV2, o);
                if (o.oneofs)
                    d._limitSharingV2 = "limitSharingV2";
            }
            if (m.threadID && m.threadID.length) {
                d.threadID = [];
                for (var j = 0; j < m.threadID.length; ++j) {
                    d.threadID[j] = WAE2E.ThreadID.toObject(m.threadID[j], o);
                }
            }
            if (m.weblinkRenderConfig != null && m.hasOwnProperty("weblinkRenderConfig")) {
                d.weblinkRenderConfig = o.enums === String ? WAE2E.WebLinkRenderConfig[m.weblinkRenderConfig] === undefined ? m.weblinkRenderConfig : WAE2E.WebLinkRenderConfig[m.weblinkRenderConfig] : m.weblinkRenderConfig;
                if (o.oneofs)
                    d._weblinkRenderConfig = "weblinkRenderConfig";
            }
            if (m.teeBotMetadata != null && m.hasOwnProperty("teeBotMetadata")) {
                d.teeBotMetadata = o.bytes === String ? $util.base64.encode(m.teeBotMetadata, 0, m.teeBotMetadata.length) : o.bytes === Array ? Array.prototype.slice.call(m.teeBotMetadata) : m.teeBotMetadata;
                if (o.oneofs)
                    d._teeBotMetadata = "teeBotMetadata";
            }
            return d;
        };

        MessageContextInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageContextInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageContextInfo";
        };

        MessageContextInfo.MessageAddonExpiryType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "STATIC"] = 1;
            values[valuesById[2] = "DEPENDENT_ON_PARENT"] = 2;
            return values;
        })();

        return MessageContextInfo;
    })();

    WAE2E.InteractiveAnnotation = (function() {

        function InteractiveAnnotation(p) {
            this.polygonVertices = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InteractiveAnnotation.prototype.location = null;
        InteractiveAnnotation.prototype.newsletter = null;
        InteractiveAnnotation.prototype.embeddedAction = null;
        InteractiveAnnotation.prototype.tapAction = null;
        InteractiveAnnotation.prototype.polygonVertices = $util.emptyArray;
        InteractiveAnnotation.prototype.shouldSkipConfirmation = null;
        InteractiveAnnotation.prototype.embeddedContent = null;
        InteractiveAnnotation.prototype.statusLinkType = null;

        let $oneOfFields;

        Object.defineProperty(InteractiveAnnotation.prototype, "action", {
            get: $util.oneOfGetter($oneOfFields = ["location", "newsletter", "embeddedAction", "tapAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_shouldSkipConfirmation", {
            get: $util.oneOfGetter($oneOfFields = ["shouldSkipConfirmation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_embeddedContent", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InteractiveAnnotation.prototype, "_statusLinkType", {
            get: $util.oneOfGetter($oneOfFields = ["statusLinkType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveAnnotation.create = function create(properties) {
            return new InteractiveAnnotation(properties);
        };

        InteractiveAnnotation.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.polygonVertices != null && m.polygonVertices.length) {
                for (var i = 0; i < m.polygonVertices.length; ++i)
                    WAE2E.Point.encode(m.polygonVertices[i], w.uint32(10).fork()).ldelim();
            }
            if (m.location != null && Object.hasOwnProperty.call(m, "location"))
                WAE2E.Location.encode(m.location, w.uint32(18).fork()).ldelim();
            if (m.newsletter != null && Object.hasOwnProperty.call(m, "newsletter"))
                WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.encode(m.newsletter, w.uint32(26).fork()).ldelim();
            if (m.shouldSkipConfirmation != null && Object.hasOwnProperty.call(m, "shouldSkipConfirmation"))
                w.uint32(32).bool(m.shouldSkipConfirmation);
            if (m.embeddedContent != null && Object.hasOwnProperty.call(m, "embeddedContent"))
                WAE2E.EmbeddedContent.encode(m.embeddedContent, w.uint32(42).fork()).ldelim();
            if (m.embeddedAction != null && Object.hasOwnProperty.call(m, "embeddedAction"))
                w.uint32(48).bool(m.embeddedAction);
            if (m.tapAction != null && Object.hasOwnProperty.call(m, "tapAction"))
                WAE2E.TapLinkAction.encode(m.tapAction, w.uint32(58).fork()).ldelim();
            if (m.statusLinkType != null && Object.hasOwnProperty.call(m, "statusLinkType"))
                w.uint32(64).int32(m.statusLinkType);
            return w;
        };

        InteractiveAnnotation.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InteractiveAnnotation();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.location = WAE2E.Location.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.newsletter = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.embeddedAction = r.bool();
                        break;
                    }
                case 7: {
                        m.tapAction = WAE2E.TapLinkAction.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        if (!(m.polygonVertices && m.polygonVertices.length))
                            m.polygonVertices = [];
                        m.polygonVertices.push(WAE2E.Point.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 4: {
                        m.shouldSkipConfirmation = r.bool();
                        break;
                    }
                case 5: {
                        m.embeddedContent = WAE2E.EmbeddedContent.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.statusLinkType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InteractiveAnnotation.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.InteractiveAnnotation)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.InteractiveAnnotation();
            if (d.location != null) {
                if (typeof d.location !== "object")
                    throw TypeError(".WAE2E.InteractiveAnnotation.location: object expected");
                m.location = WAE2E.Location.fromObject(d.location, n + 1);
            }
            if (d.newsletter != null) {
                if (typeof d.newsletter !== "object")
                    throw TypeError(".WAE2E.InteractiveAnnotation.newsletter: object expected");
                m.newsletter = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(d.newsletter, n + 1);
            }
            if (d.embeddedAction != null) {
                m.embeddedAction = Boolean(d.embeddedAction);
            }
            if (d.tapAction != null) {
                if (typeof d.tapAction !== "object")
                    throw TypeError(".WAE2E.InteractiveAnnotation.tapAction: object expected");
                m.tapAction = WAE2E.TapLinkAction.fromObject(d.tapAction, n + 1);
            }
            if (d.polygonVertices) {
                if (!Array.isArray(d.polygonVertices))
                    throw TypeError(".WAE2E.InteractiveAnnotation.polygonVertices: array expected");
                m.polygonVertices = [];
                for (var i = 0; i < d.polygonVertices.length; ++i) {
                    if (typeof d.polygonVertices[i] !== "object")
                        throw TypeError(".WAE2E.InteractiveAnnotation.polygonVertices: object expected");
                    m.polygonVertices[i] = WAE2E.Point.fromObject(d.polygonVertices[i], n + 1);
                }
            }
            if (d.shouldSkipConfirmation != null) {
                m.shouldSkipConfirmation = Boolean(d.shouldSkipConfirmation);
            }
            if (d.embeddedContent != null) {
                if (typeof d.embeddedContent !== "object")
                    throw TypeError(".WAE2E.InteractiveAnnotation.embeddedContent: object expected");
                m.embeddedContent = WAE2E.EmbeddedContent.fromObject(d.embeddedContent, n + 1);
            }
            switch (d.statusLinkType) {
            default:
                if (typeof d.statusLinkType === "number") {
                    m.statusLinkType = d.statusLinkType;
                    break;
                }
                break;
            case "RASTERIZED_LINK_PREVIEW":
            case 1:
                m.statusLinkType = 1;
                break;
            case "RASTERIZED_LINK_TRUNCATED":
            case 2:
                m.statusLinkType = 2;
                break;
            case "RASTERIZED_LINK_FULL_URL":
            case 3:
                m.statusLinkType = 3;
                break;
            }
            return m;
        };

        InteractiveAnnotation.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.polygonVertices = [];
            }
            if (m.polygonVertices && m.polygonVertices.length) {
                d.polygonVertices = [];
                for (var j = 0; j < m.polygonVertices.length; ++j) {
                    d.polygonVertices[j] = WAE2E.Point.toObject(m.polygonVertices[j], o);
                }
            }
            if (m.location != null && m.hasOwnProperty("location")) {
                d.location = WAE2E.Location.toObject(m.location, o);
                if (o.oneofs)
                    d.action = "location";
            }
            if (m.newsletter != null && m.hasOwnProperty("newsletter")) {
                d.newsletter = WAE2E.ContextInfo.ForwardedNewsletterMessageInfo.toObject(m.newsletter, o);
                if (o.oneofs)
                    d.action = "newsletter";
            }
            if (m.shouldSkipConfirmation != null && m.hasOwnProperty("shouldSkipConfirmation")) {
                d.shouldSkipConfirmation = m.shouldSkipConfirmation;
                if (o.oneofs)
                    d._shouldSkipConfirmation = "shouldSkipConfirmation";
            }
            if (m.embeddedContent != null && m.hasOwnProperty("embeddedContent")) {
                d.embeddedContent = WAE2E.EmbeddedContent.toObject(m.embeddedContent, o);
                if (o.oneofs)
                    d._embeddedContent = "embeddedContent";
            }
            if (m.embeddedAction != null && m.hasOwnProperty("embeddedAction")) {
                d.embeddedAction = m.embeddedAction;
                if (o.oneofs)
                    d.action = "embeddedAction";
            }
            if (m.tapAction != null && m.hasOwnProperty("tapAction")) {
                d.tapAction = WAE2E.TapLinkAction.toObject(m.tapAction, o);
                if (o.oneofs)
                    d.action = "tapAction";
            }
            if (m.statusLinkType != null && m.hasOwnProperty("statusLinkType")) {
                d.statusLinkType = o.enums === String ? WAE2E.InteractiveAnnotation.StatusLinkType[m.statusLinkType] === undefined ? m.statusLinkType : WAE2E.InteractiveAnnotation.StatusLinkType[m.statusLinkType] : m.statusLinkType;
                if (o.oneofs)
                    d._statusLinkType = "statusLinkType";
            }
            return d;
        };

        InteractiveAnnotation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveAnnotation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.InteractiveAnnotation";
        };

        InteractiveAnnotation.StatusLinkType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "RASTERIZED_LINK_PREVIEW"] = 1;
            values[valuesById[2] = "RASTERIZED_LINK_TRUNCATED"] = 2;
            values[valuesById[3] = "RASTERIZED_LINK_FULL_URL"] = 3;
            return values;
        })();

        return InteractiveAnnotation;
    })();

    WAE2E.HydratedTemplateButton = (function() {

        function HydratedTemplateButton(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        HydratedTemplateButton.prototype.quickReplyButton = null;
        HydratedTemplateButton.prototype.urlButton = null;
        HydratedTemplateButton.prototype.callButton = null;
        HydratedTemplateButton.prototype.index = null;

        let $oneOfFields;

        Object.defineProperty(HydratedTemplateButton.prototype, "hydratedButton", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HydratedTemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HydratedTemplateButton.create = function create(properties) {
            return new HydratedTemplateButton(properties);
        };

        HydratedTemplateButton.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.quickReplyButton != null && Object.hasOwnProperty.call(m, "quickReplyButton"))
                WAE2E.HydratedTemplateButton.HydratedQuickReplyButton.encode(m.quickReplyButton, w.uint32(10).fork()).ldelim();
            if (m.urlButton != null && Object.hasOwnProperty.call(m, "urlButton"))
                WAE2E.HydratedTemplateButton.HydratedURLButton.encode(m.urlButton, w.uint32(18).fork()).ldelim();
            if (m.callButton != null && Object.hasOwnProperty.call(m, "callButton"))
                WAE2E.HydratedTemplateButton.HydratedCallButton.encode(m.callButton, w.uint32(26).fork()).ldelim();
            if (m.index != null && Object.hasOwnProperty.call(m, "index"))
                w.uint32(32).uint32(m.index);
            return w;
        };

        HydratedTemplateButton.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HydratedTemplateButton();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.quickReplyButton = WAE2E.HydratedTemplateButton.HydratedQuickReplyButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.urlButton = WAE2E.HydratedTemplateButton.HydratedURLButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.callButton = WAE2E.HydratedTemplateButton.HydratedCallButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.index = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        HydratedTemplateButton.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.HydratedTemplateButton)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.HydratedTemplateButton();
            if (d.quickReplyButton != null) {
                if (typeof d.quickReplyButton !== "object")
                    throw TypeError(".WAE2E.HydratedTemplateButton.quickReplyButton: object expected");
                m.quickReplyButton = WAE2E.HydratedTemplateButton.HydratedQuickReplyButton.fromObject(d.quickReplyButton, n + 1);
            }
            if (d.urlButton != null) {
                if (typeof d.urlButton !== "object")
                    throw TypeError(".WAE2E.HydratedTemplateButton.urlButton: object expected");
                m.urlButton = WAE2E.HydratedTemplateButton.HydratedURLButton.fromObject(d.urlButton, n + 1);
            }
            if (d.callButton != null) {
                if (typeof d.callButton !== "object")
                    throw TypeError(".WAE2E.HydratedTemplateButton.callButton: object expected");
                m.callButton = WAE2E.HydratedTemplateButton.HydratedCallButton.fromObject(d.callButton, n + 1);
            }
            if (d.index != null) {
                m.index = d.index >>> 0;
            }
            return m;
        };

        HydratedTemplateButton.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.quickReplyButton != null && m.hasOwnProperty("quickReplyButton")) {
                d.quickReplyButton = WAE2E.HydratedTemplateButton.HydratedQuickReplyButton.toObject(m.quickReplyButton, o);
                if (o.oneofs)
                    d.hydratedButton = "quickReplyButton";
            }
            if (m.urlButton != null && m.hasOwnProperty("urlButton")) {
                d.urlButton = WAE2E.HydratedTemplateButton.HydratedURLButton.toObject(m.urlButton, o);
                if (o.oneofs)
                    d.hydratedButton = "urlButton";
            }
            if (m.callButton != null && m.hasOwnProperty("callButton")) {
                d.callButton = WAE2E.HydratedTemplateButton.HydratedCallButton.toObject(m.callButton, o);
                if (o.oneofs)
                    d.hydratedButton = "callButton";
            }
            if (m.index != null && m.hasOwnProperty("index")) {
                d.index = m.index;
                if (o.oneofs)
                    d._index = "index";
            }
            return d;
        };

        HydratedTemplateButton.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HydratedTemplateButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.HydratedTemplateButton";
        };

        HydratedTemplateButton.HydratedURLButton = (function() {

            function HydratedURLButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HydratedURLButton.prototype.displayText = null;
            HydratedURLButton.prototype.URL = null;
            HydratedURLButton.prototype.consentedUsersURL = null;
            HydratedURLButton.prototype.webviewPresentation = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_consentedUsersURL", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedURLButton.prototype, "_webviewPresentation", {
                get: $util.oneOfGetter($oneOfFields = ["webviewPresentation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedURLButton.create = function create(properties) {
                return new HydratedURLButton(properties);
            };

            HydratedURLButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    w.uint32(18).string(m.URL);
                if (m.consentedUsersURL != null && Object.hasOwnProperty.call(m, "consentedUsersURL"))
                    w.uint32(26).string(m.consentedUsersURL);
                if (m.webviewPresentation != null && Object.hasOwnProperty.call(m, "webviewPresentation"))
                    w.uint32(32).int32(m.webviewPresentation);
                return w;
            };

            HydratedURLButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HydratedTemplateButton.HydratedURLButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = r.string();
                            break;
                        }
                    case 2: {
                            m.URL = r.string();
                            break;
                        }
                    case 3: {
                            m.consentedUsersURL = r.string();
                            break;
                        }
                    case 4: {
                            m.webviewPresentation = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HydratedURLButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.HydratedTemplateButton.HydratedURLButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.HydratedTemplateButton.HydratedURLButton();
                if (d.displayText != null) {
                    m.displayText = String(d.displayText);
                }
                if (d.URL != null) {
                    m.URL = String(d.URL);
                }
                if (d.consentedUsersURL != null) {
                    m.consentedUsersURL = String(d.consentedUsersURL);
                }
                switch (d.webviewPresentation) {
                default:
                    if (typeof d.webviewPresentation === "number") {
                        m.webviewPresentation = d.webviewPresentation;
                        break;
                    }
                    break;
                case "FULL":
                case 1:
                    m.webviewPresentation = 1;
                    break;
                case "TALL":
                case 2:
                    m.webviewPresentation = 2;
                    break;
                case "COMPACT":
                case 3:
                    m.webviewPresentation = 3;
                    break;
                }
                return m;
            };

            HydratedURLButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = m.displayText;
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = m.URL;
                    if (o.oneofs)
                        d._URL = "URL";
                }
                if (m.consentedUsersURL != null && m.hasOwnProperty("consentedUsersURL")) {
                    d.consentedUsersURL = m.consentedUsersURL;
                    if (o.oneofs)
                        d._consentedUsersURL = "consentedUsersURL";
                }
                if (m.webviewPresentation != null && m.hasOwnProperty("webviewPresentation")) {
                    d.webviewPresentation = o.enums === String ? WAE2E.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[m.webviewPresentation] === undefined ? m.webviewPresentation : WAE2E.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[m.webviewPresentation] : m.webviewPresentation;
                    if (o.oneofs)
                        d._webviewPresentation = "webviewPresentation";
                }
                return d;
            };

            HydratedURLButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedURLButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.HydratedTemplateButton.HydratedURLButton";
            };

            HydratedURLButton.WebviewPresentationType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[1] = "FULL"] = 1;
                values[valuesById[2] = "TALL"] = 2;
                values[valuesById[3] = "COMPACT"] = 3;
                return values;
            })();

            return HydratedURLButton;
        })();

        HydratedTemplateButton.HydratedCallButton = (function() {

            function HydratedCallButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HydratedCallButton.prototype.displayText = null;
            HydratedCallButton.prototype.phoneNumber = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedCallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedCallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedCallButton.create = function create(properties) {
                return new HydratedCallButton(properties);
            };

            HydratedCallButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.phoneNumber != null && Object.hasOwnProperty.call(m, "phoneNumber"))
                    w.uint32(18).string(m.phoneNumber);
                return w;
            };

            HydratedCallButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HydratedTemplateButton.HydratedCallButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = r.string();
                            break;
                        }
                    case 2: {
                            m.phoneNumber = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HydratedCallButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.HydratedTemplateButton.HydratedCallButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.HydratedTemplateButton.HydratedCallButton();
                if (d.displayText != null) {
                    m.displayText = String(d.displayText);
                }
                if (d.phoneNumber != null) {
                    m.phoneNumber = String(d.phoneNumber);
                }
                return m;
            };

            HydratedCallButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = m.displayText;
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.phoneNumber != null && m.hasOwnProperty("phoneNumber")) {
                    d.phoneNumber = m.phoneNumber;
                    if (o.oneofs)
                        d._phoneNumber = "phoneNumber";
                }
                return d;
            };

            HydratedCallButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedCallButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.HydratedTemplateButton.HydratedCallButton";
            };

            return HydratedCallButton;
        })();

        HydratedTemplateButton.HydratedQuickReplyButton = (function() {

            function HydratedQuickReplyButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HydratedQuickReplyButton.prototype.displayText = null;
            HydratedQuickReplyButton.prototype.ID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedQuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedQuickReplyButton.prototype, "_ID", {
                get: $util.oneOfGetter($oneOfFields = ["ID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedQuickReplyButton.create = function create(properties) {
                return new HydratedQuickReplyButton(properties);
            };

            HydratedQuickReplyButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                    w.uint32(18).string(m.ID);
                return w;
            };

            HydratedQuickReplyButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HydratedTemplateButton.HydratedQuickReplyButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = r.string();
                            break;
                        }
                    case 2: {
                            m.ID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HydratedQuickReplyButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.HydratedTemplateButton.HydratedQuickReplyButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.HydratedTemplateButton.HydratedQuickReplyButton();
                if (d.displayText != null) {
                    m.displayText = String(d.displayText);
                }
                if (d.ID != null) {
                    m.ID = String(d.ID);
                }
                return m;
            };

            HydratedQuickReplyButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = m.displayText;
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.ID != null && m.hasOwnProperty("ID")) {
                    d.ID = m.ID;
                    if (o.oneofs)
                        d._ID = "ID";
                }
                return d;
            };

            HydratedQuickReplyButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedQuickReplyButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.HydratedTemplateButton.HydratedQuickReplyButton";
            };

            return HydratedQuickReplyButton;
        })();

        return HydratedTemplateButton;
    })();

    WAE2E.PaymentBackground = (function() {

        function PaymentBackground(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PaymentBackground.prototype.ID = null;
        PaymentBackground.prototype.fileLength = null;
        PaymentBackground.prototype.width = null;
        PaymentBackground.prototype.height = null;
        PaymentBackground.prototype.mimetype = null;
        PaymentBackground.prototype.placeholderArgb = null;
        PaymentBackground.prototype.textArgb = null;
        PaymentBackground.prototype.subtextArgb = null;
        PaymentBackground.prototype.mediaData = null;
        PaymentBackground.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_ID", {
            get: $util.oneOfGetter($oneOfFields = ["ID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_placeholderArgb", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_textArgb", {
            get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_subtextArgb", {
            get: $util.oneOfGetter($oneOfFields = ["subtextArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentBackground.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentBackground.create = function create(properties) {
            return new PaymentBackground(properties);
        };

        PaymentBackground.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                w.uint32(10).string(m.ID);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(16).uint64(m.fileLength);
            if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                w.uint32(24).uint32(m.width);
            if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                w.uint32(32).uint32(m.height);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(42).string(m.mimetype);
            if (m.placeholderArgb != null && Object.hasOwnProperty.call(m, "placeholderArgb"))
                w.uint32(53).fixed32(m.placeholderArgb);
            if (m.textArgb != null && Object.hasOwnProperty.call(m, "textArgb"))
                w.uint32(61).fixed32(m.textArgb);
            if (m.subtextArgb != null && Object.hasOwnProperty.call(m, "subtextArgb"))
                w.uint32(69).fixed32(m.subtextArgb);
            if (m.mediaData != null && Object.hasOwnProperty.call(m, "mediaData"))
                WAE2E.PaymentBackground.MediaData.encode(m.mediaData, w.uint32(74).fork()).ldelim();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(80).int32(m.type);
            return w;
        };

        PaymentBackground.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentBackground();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.ID = r.string();
                        break;
                    }
                case 2: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 3: {
                        m.width = r.uint32();
                        break;
                    }
                case 4: {
                        m.height = r.uint32();
                        break;
                    }
                case 5: {
                        m.mimetype = r.string();
                        break;
                    }
                case 6: {
                        m.placeholderArgb = r.fixed32();
                        break;
                    }
                case 7: {
                        m.textArgb = r.fixed32();
                        break;
                    }
                case 8: {
                        m.subtextArgb = r.fixed32();
                        break;
                    }
                case 9: {
                        m.mediaData = WAE2E.PaymentBackground.MediaData.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PaymentBackground.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PaymentBackground)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PaymentBackground();
            if (d.ID != null) {
                m.ID = String(d.ID);
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.placeholderArgb != null) {
                m.placeholderArgb = d.placeholderArgb >>> 0;
            }
            if (d.textArgb != null) {
                m.textArgb = d.textArgb >>> 0;
            }
            if (d.subtextArgb != null) {
                m.subtextArgb = d.subtextArgb >>> 0;
            }
            if (d.mediaData != null) {
                if (typeof d.mediaData !== "object")
                    throw TypeError(".WAE2E.PaymentBackground.mediaData: object expected");
                m.mediaData = WAE2E.PaymentBackground.MediaData.fromObject(d.mediaData, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "DEFAULT":
            case 1:
                m.type = 1;
                break;
            }
            return m;
        };

        PaymentBackground.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.ID != null && m.hasOwnProperty("ID")) {
                d.ID = m.ID;
                if (o.oneofs)
                    d._ID = "ID";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.width != null && m.hasOwnProperty("width")) {
                d.width = m.width;
                if (o.oneofs)
                    d._width = "width";
            }
            if (m.height != null && m.hasOwnProperty("height")) {
                d.height = m.height;
                if (o.oneofs)
                    d._height = "height";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.placeholderArgb != null && m.hasOwnProperty("placeholderArgb")) {
                d.placeholderArgb = m.placeholderArgb;
                if (o.oneofs)
                    d._placeholderArgb = "placeholderArgb";
            }
            if (m.textArgb != null && m.hasOwnProperty("textArgb")) {
                d.textArgb = m.textArgb;
                if (o.oneofs)
                    d._textArgb = "textArgb";
            }
            if (m.subtextArgb != null && m.hasOwnProperty("subtextArgb")) {
                d.subtextArgb = m.subtextArgb;
                if (o.oneofs)
                    d._subtextArgb = "subtextArgb";
            }
            if (m.mediaData != null && m.hasOwnProperty("mediaData")) {
                d.mediaData = WAE2E.PaymentBackground.MediaData.toObject(m.mediaData, o);
                if (o.oneofs)
                    d._mediaData = "mediaData";
            }
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? WAE2E.PaymentBackground.Type[m.type] === undefined ? m.type : WAE2E.PaymentBackground.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        PaymentBackground.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentBackground.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PaymentBackground";
        };

        PaymentBackground.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "DEFAULT"] = 1;
            return values;
        })();

        PaymentBackground.MediaData = (function() {

            function MediaData(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            MediaData.prototype.mediaKey = null;
            MediaData.prototype.mediaKeyTimestamp = null;
            MediaData.prototype.fileSHA256 = null;
            MediaData.prototype.fileEncSHA256 = null;
            MediaData.prototype.directPath = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_fileSHA256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_fileEncSHA256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaData.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MediaData.create = function create(properties) {
                return new MediaData(properties);
            };

            MediaData.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(10).bytes(m.mediaKey);
                if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(16).int64(m.mediaKeyTimestamp);
                if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                    w.uint32(26).bytes(m.fileSHA256);
                if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                    w.uint32(34).bytes(m.fileEncSHA256);
                if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(42).string(m.directPath);
                return w;
            };

            MediaData.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentBackground.MediaData();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.mediaKey = r.bytes();
                            break;
                        }
                    case 2: {
                            m.mediaKeyTimestamp = r.int64();
                            break;
                        }
                    case 3: {
                            m.fileSHA256 = r.bytes();
                            break;
                        }
                    case 4: {
                            m.fileEncSHA256 = r.bytes();
                            break;
                        }
                    case 5: {
                            m.directPath = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            MediaData.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PaymentBackground.MediaData)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PaymentBackground.MediaData();
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.fileSHA256 != null) {
                    if (typeof d.fileSHA256 === "string")
                        $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                    else if (d.fileSHA256.length >= 0)
                        m.fileSHA256 = d.fileSHA256;
                }
                if (d.fileEncSHA256 != null) {
                    if (typeof d.fileEncSHA256 === "string")
                        $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                    else if (d.fileEncSHA256.length >= 0)
                        m.fileEncSHA256 = d.fileEncSHA256;
                }
                if (d.directPath != null) {
                    m.directPath = String(d.directPath);
                }
                return m;
            };

            MediaData.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                    d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                    if (o.oneofs)
                        d._mediaKey = "mediaKey";
                }
                if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                    if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                    if (o.oneofs)
                        d._mediaKeyTimestamp = "mediaKeyTimestamp";
                }
                if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                    d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                    if (o.oneofs)
                        d._fileSHA256 = "fileSHA256";
                }
                if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                    d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                    if (o.oneofs)
                        d._fileEncSHA256 = "fileEncSHA256";
                }
                if (m.directPath != null && m.hasOwnProperty("directPath")) {
                    d.directPath = m.directPath;
                    if (o.oneofs)
                        d._directPath = "directPath";
                }
                return d;
            };

            MediaData.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MediaData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PaymentBackground.MediaData";
            };

            return MediaData;
        })();

        return PaymentBackground;
    })();

    WAE2E.DisappearingMode = (function() {

        function DisappearingMode(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DisappearingMode.prototype.initiator = null;
        DisappearingMode.prototype.trigger = null;
        DisappearingMode.prototype.initiatorDeviceJID = null;
        DisappearingMode.prototype.initiatedByMe = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiator", {
            get: $util.oneOfGetter($oneOfFields = ["initiator"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiatorDeviceJID", {
            get: $util.oneOfGetter($oneOfFields = ["initiatorDeviceJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DisappearingMode.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DisappearingMode.create = function create(properties) {
            return new DisappearingMode(properties);
        };

        DisappearingMode.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.initiator != null && Object.hasOwnProperty.call(m, "initiator"))
                w.uint32(8).int32(m.initiator);
            if (m.trigger != null && Object.hasOwnProperty.call(m, "trigger"))
                w.uint32(16).int32(m.trigger);
            if (m.initiatorDeviceJID != null && Object.hasOwnProperty.call(m, "initiatorDeviceJID"))
                w.uint32(26).string(m.initiatorDeviceJID);
            if (m.initiatedByMe != null && Object.hasOwnProperty.call(m, "initiatedByMe"))
                w.uint32(32).bool(m.initiatedByMe);
            return w;
        };

        DisappearingMode.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.DisappearingMode();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.initiator = r.int32();
                        break;
                    }
                case 2: {
                        m.trigger = r.int32();
                        break;
                    }
                case 3: {
                        m.initiatorDeviceJID = r.string();
                        break;
                    }
                case 4: {
                        m.initiatedByMe = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DisappearingMode.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.DisappearingMode)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.DisappearingMode();
            switch (d.initiator) {
            default:
                if (typeof d.initiator === "number") {
                    m.initiator = d.initiator;
                    break;
                }
                break;
            case "CHANGED_IN_CHAT":
            case 0:
                m.initiator = 0;
                break;
            case "INITIATED_BY_ME":
            case 1:
                m.initiator = 1;
                break;
            case "INITIATED_BY_OTHER":
            case 2:
                m.initiator = 2;
                break;
            case "BIZ_UPGRADE_FB_HOSTING":
            case 3:
                m.initiator = 3;
                break;
            }
            switch (d.trigger) {
            default:
                if (typeof d.trigger === "number") {
                    m.trigger = d.trigger;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                m.trigger = 1;
                break;
            case "ACCOUNT_SETTING":
            case 2:
                m.trigger = 2;
                break;
            case "BULK_CHANGE":
            case 3:
                m.trigger = 3;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 4:
                m.trigger = 4;
                break;
            case "UNKNOWN_GROUPS":
            case 5:
                m.trigger = 5;
                break;
            }
            if (d.initiatorDeviceJID != null) {
                m.initiatorDeviceJID = String(d.initiatorDeviceJID);
            }
            if (d.initiatedByMe != null) {
                m.initiatedByMe = Boolean(d.initiatedByMe);
            }
            return m;
        };

        DisappearingMode.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.initiator != null && m.hasOwnProperty("initiator")) {
                d.initiator = o.enums === String ? WAE2E.DisappearingMode.Initiator[m.initiator] === undefined ? m.initiator : WAE2E.DisappearingMode.Initiator[m.initiator] : m.initiator;
                if (o.oneofs)
                    d._initiator = "initiator";
            }
            if (m.trigger != null && m.hasOwnProperty("trigger")) {
                d.trigger = o.enums === String ? WAE2E.DisappearingMode.Trigger[m.trigger] === undefined ? m.trigger : WAE2E.DisappearingMode.Trigger[m.trigger] : m.trigger;
                if (o.oneofs)
                    d._trigger = "trigger";
            }
            if (m.initiatorDeviceJID != null && m.hasOwnProperty("initiatorDeviceJID")) {
                d.initiatorDeviceJID = m.initiatorDeviceJID;
                if (o.oneofs)
                    d._initiatorDeviceJID = "initiatorDeviceJID";
            }
            if (m.initiatedByMe != null && m.hasOwnProperty("initiatedByMe")) {
                d.initiatedByMe = m.initiatedByMe;
                if (o.oneofs)
                    d._initiatedByMe = "initiatedByMe";
            }
            return d;
        };

        DisappearingMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DisappearingMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.DisappearingMode";
        };

        DisappearingMode.Trigger = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "ACCOUNT_SETTING"] = 2;
            values[valuesById[3] = "BULK_CHANGE"] = 3;
            values[valuesById[4] = "BIZ_SUPPORTS_FB_HOSTING"] = 4;
            values[valuesById[5] = "UNKNOWN_GROUPS"] = 5;
            return values;
        })();

        DisappearingMode.Initiator = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "CHANGED_IN_CHAT"] = 0;
            values[valuesById[1] = "INITIATED_BY_ME"] = 1;
            values[valuesById[2] = "INITIATED_BY_OTHER"] = 2;
            values[valuesById[3] = "BIZ_UPGRADE_FB_HOSTING"] = 3;
            return values;
        })();

        return DisappearingMode;
    })();

    WAE2E.ProcessedVideo = (function() {

        function ProcessedVideo(p) {
            this.capabilities = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ProcessedVideo.prototype.directPath = null;
        ProcessedVideo.prototype.fileSHA256 = null;
        ProcessedVideo.prototype.height = null;
        ProcessedVideo.prototype.width = null;
        ProcessedVideo.prototype.fileLength = null;
        ProcessedVideo.prototype.bitrate = null;
        ProcessedVideo.prototype.quality = null;
        ProcessedVideo.prototype.capabilities = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_bitrate", {
            get: $util.oneOfGetter($oneOfFields = ["bitrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProcessedVideo.prototype, "_quality", {
            get: $util.oneOfGetter($oneOfFields = ["quality"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProcessedVideo.create = function create(properties) {
            return new ProcessedVideo(properties);
        };

        ProcessedVideo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(10).string(m.directPath);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(18).bytes(m.fileSHA256);
            if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                w.uint32(24).uint32(m.height);
            if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                w.uint32(32).uint32(m.width);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(40).uint64(m.fileLength);
            if (m.bitrate != null && Object.hasOwnProperty.call(m, "bitrate"))
                w.uint32(48).uint32(m.bitrate);
            if (m.quality != null && Object.hasOwnProperty.call(m, "quality"))
                w.uint32(56).int32(m.quality);
            if (m.capabilities != null && m.capabilities.length) {
                for (var i = 0; i < m.capabilities.length; ++i)
                    w.uint32(66).string(m.capabilities[i]);
            }
            return w;
        };

        ProcessedVideo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ProcessedVideo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.directPath = r.string();
                        break;
                    }
                case 2: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 3: {
                        m.height = r.uint32();
                        break;
                    }
                case 4: {
                        m.width = r.uint32();
                        break;
                    }
                case 5: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 6: {
                        m.bitrate = r.uint32();
                        break;
                    }
                case 7: {
                        m.quality = r.int32();
                        break;
                    }
                case 8: {
                        if (!(m.capabilities && m.capabilities.length))
                            m.capabilities = [];
                        m.capabilities.push(r.string());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ProcessedVideo.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ProcessedVideo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ProcessedVideo();
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.bitrate != null) {
                m.bitrate = d.bitrate >>> 0;
            }
            switch (d.quality) {
            default:
                if (typeof d.quality === "number") {
                    m.quality = d.quality;
                    break;
                }
                break;
            case "UNDEFINED":
            case 0:
                m.quality = 0;
                break;
            case "LOW":
            case 1:
                m.quality = 1;
                break;
            case "MID":
            case 2:
                m.quality = 2;
                break;
            case "HIGH":
            case 3:
                m.quality = 3;
                break;
            }
            if (d.capabilities) {
                if (!Array.isArray(d.capabilities))
                    throw TypeError(".WAE2E.ProcessedVideo.capabilities: array expected");
                m.capabilities = [];
                for (var i = 0; i < d.capabilities.length; ++i) {
                    m.capabilities[i] = String(d.capabilities[i]);
                }
            }
            return m;
        };

        ProcessedVideo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilities = [];
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.height != null && m.hasOwnProperty("height")) {
                d.height = m.height;
                if (o.oneofs)
                    d._height = "height";
            }
            if (m.width != null && m.hasOwnProperty("width")) {
                d.width = m.width;
                if (o.oneofs)
                    d._width = "width";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.bitrate != null && m.hasOwnProperty("bitrate")) {
                d.bitrate = m.bitrate;
                if (o.oneofs)
                    d._bitrate = "bitrate";
            }
            if (m.quality != null && m.hasOwnProperty("quality")) {
                d.quality = o.enums === String ? WAE2E.ProcessedVideo.VideoQuality[m.quality] === undefined ? m.quality : WAE2E.ProcessedVideo.VideoQuality[m.quality] : m.quality;
                if (o.oneofs)
                    d._quality = "quality";
            }
            if (m.capabilities && m.capabilities.length) {
                d.capabilities = [];
                for (var j = 0; j < m.capabilities.length; ++j) {
                    d.capabilities[j] = m.capabilities[j];
                }
            }
            return d;
        };

        ProcessedVideo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProcessedVideo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ProcessedVideo";
        };

        ProcessedVideo.VideoQuality = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "MID"] = 2;
            values[valuesById[3] = "HIGH"] = 3;
            return values;
        })();

        return ProcessedVideo;
    })();

    WAE2E.Message = (function() {

        function Message(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Message.prototype.conversation = null;
        Message.prototype.senderKeyDistributionMessage = null;
        Message.prototype.imageMessage = null;
        Message.prototype.contactMessage = null;
        Message.prototype.locationMessage = null;
        Message.prototype.extendedTextMessage = null;
        Message.prototype.documentMessage = null;
        Message.prototype.audioMessage = null;
        Message.prototype.videoMessage = null;
        Message.prototype.call = null;
        Message.prototype.chat = null;
        Message.prototype.protocolMessage = null;
        Message.prototype.contactsArrayMessage = null;
        Message.prototype.highlyStructuredMessage = null;
        Message.prototype.fastRatchetKeySenderKeyDistributionMessage = null;
        Message.prototype.sendPaymentMessage = null;
        Message.prototype.liveLocationMessage = null;
        Message.prototype.requestPaymentMessage = null;
        Message.prototype.declinePaymentRequestMessage = null;
        Message.prototype.cancelPaymentRequestMessage = null;
        Message.prototype.templateMessage = null;
        Message.prototype.stickerMessage = null;
        Message.prototype.groupInviteMessage = null;
        Message.prototype.templateButtonReplyMessage = null;
        Message.prototype.productMessage = null;
        Message.prototype.deviceSentMessage = null;
        Message.prototype.messageContextInfo = null;
        Message.prototype.listMessage = null;
        Message.prototype.viewOnceMessage = null;
        Message.prototype.orderMessage = null;
        Message.prototype.listResponseMessage = null;
        Message.prototype.ephemeralMessage = null;
        Message.prototype.invoiceMessage = null;
        Message.prototype.buttonsMessage = null;
        Message.prototype.buttonsResponseMessage = null;
        Message.prototype.paymentInviteMessage = null;
        Message.prototype.interactiveMessage = null;
        Message.prototype.reactionMessage = null;
        Message.prototype.stickerSyncRmrMessage = null;
        Message.prototype.interactiveResponseMessage = null;
        Message.prototype.pollCreationMessage = null;
        Message.prototype.pollUpdateMessage = null;
        Message.prototype.keepInChatMessage = null;
        Message.prototype.documentWithCaptionMessage = null;
        Message.prototype.requestPhoneNumberMessage = null;
        Message.prototype.viewOnceMessageV2 = null;
        Message.prototype.encReactionMessage = null;
        Message.prototype.editedMessage = null;
        Message.prototype.viewOnceMessageV2Extension = null;
        Message.prototype.pollCreationMessageV2 = null;
        Message.prototype.scheduledCallCreationMessage = null;
        Message.prototype.groupMentionedMessage = null;
        Message.prototype.pinInChatMessage = null;
        Message.prototype.pollCreationMessageV3 = null;
        Message.prototype.scheduledCallEditMessage = null;
        Message.prototype.ptvMessage = null;
        Message.prototype.botInvokeMessage = null;
        Message.prototype.callLogMesssage = null;
        Message.prototype.messageHistoryBundle = null;
        Message.prototype.encCommentMessage = null;
        Message.prototype.bcallMessage = null;
        Message.prototype.lottieStickerMessage = null;
        Message.prototype.eventMessage = null;
        Message.prototype.encEventResponseMessage = null;
        Message.prototype.commentMessage = null;
        Message.prototype.newsletterAdminInviteMessage = null;
        Message.prototype.placeholderMessage = null;
        Message.prototype.secretEncryptedMessage = null;
        Message.prototype.albumMessage = null;
        Message.prototype.eventCoverImage = null;
        Message.prototype.stickerPackMessage = null;
        Message.prototype.statusMentionMessage = null;
        Message.prototype.pollResultSnapshotMessage = null;
        Message.prototype.pollCreationOptionImageMessage = null;
        Message.prototype.associatedChildMessage = null;
        Message.prototype.groupStatusMentionMessage = null;
        Message.prototype.pollCreationMessageV4 = null;
        Message.prototype.statusAddYours = null;
        Message.prototype.groupStatusMessage = null;
        Message.prototype.richResponseMessage = null;
        Message.prototype.statusNotificationMessage = null;
        Message.prototype.limitSharingMessage = null;
        Message.prototype.botTaskMessage = null;
        Message.prototype.questionMessage = null;
        Message.prototype.messageHistoryNotice = null;
        Message.prototype.groupStatusMessageV2 = null;
        Message.prototype.botForwardedMessage = null;
        Message.prototype.statusQuestionAnswerMessage = null;
        Message.prototype.questionReplyMessage = null;
        Message.prototype.questionResponseMessage = null;
        Message.prototype.statusQuotedMessage = null;
        Message.prototype.statusStickerInteractionMessage = null;
        Message.prototype.pollCreationMessageV5 = null;
        Message.prototype.newsletterFollowerInviteMessageV2 = null;
        Message.prototype.pollResultSnapshotMessageV3 = null;
        Message.prototype.newsletterAdminProfileMessage = null;
        Message.prototype.newsletterAdminProfileMessageV2 = null;
        Message.prototype.spoilerMessage = null;
        Message.prototype.pollCreationMessageV6 = null;
        Message.prototype.conditionalRevealMessage = null;
        Message.prototype.pollAddOptionMessage = null;
        Message.prototype.eventInviteMessage = null;
        Message.prototype.groupRootKeyShare = null;
        Message.prototype.p2PPaymentReminderNotification = null;
        Message.prototype.splitPaymentMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_conversation", {
            get: $util.oneOfGetter($oneOfFields = ["conversation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_senderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_imageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["imageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_contactMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_locationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["locationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_extendedTextMessage", {
            get: $util.oneOfGetter($oneOfFields = ["extendedTextMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_documentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_audioMessage", {
            get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_videoMessage", {
            get: $util.oneOfGetter($oneOfFields = ["videoMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_call", {
            get: $util.oneOfGetter($oneOfFields = ["call"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_chat", {
            get: $util.oneOfGetter($oneOfFields = ["chat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_protocolMessage", {
            get: $util.oneOfGetter($oneOfFields = ["protocolMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_contactsArrayMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactsArrayMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_highlyStructuredMessage", {
            get: $util.oneOfGetter($oneOfFields = ["highlyStructuredMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_fastRatchetKeySenderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["fastRatchetKeySenderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_sendPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["sendPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_liveLocationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_requestPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_declinePaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["declinePaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_cancelPaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["cancelPaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_templateButtonReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateButtonReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_productMessage", {
            get: $util.oneOfGetter($oneOfFields = ["productMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_deviceSentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_listMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_orderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["orderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_listResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_ephemeralMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_invoiceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["invoiceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_buttonsMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_buttonsResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_paymentInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_interactiveMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_reactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["reactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerSyncRmrMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerSyncRmrMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_interactiveResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollUpdateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_keepInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_documentWithCaptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentWithCaptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_requestPhoneNumberMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPhoneNumberMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encReactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_editedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_viewOnceMessageV2Extension", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2Extension"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_scheduledCallCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupMentionedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupMentionedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pinInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_scheduledCallEditMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallEditMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_ptvMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ptvMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botInvokeMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botInvokeMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_callLogMesssage", {
            get: $util.oneOfGetter($oneOfFields = ["callLogMesssage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encCommentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encCommentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_bcallMessage", {
            get: $util.oneOfGetter($oneOfFields = ["bcallMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_lottieStickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["lottieStickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_encEventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encEventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_commentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["commentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_placeholderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_secretEncryptedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["secretEncryptedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_albumMessage", {
            get: $util.oneOfGetter($oneOfFields = ["albumMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventCoverImage", {
            get: $util.oneOfGetter($oneOfFields = ["eventCoverImage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_stickerPackMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollResultSnapshotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationOptionImageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationOptionImageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_associatedChildMessage", {
            get: $util.oneOfGetter($oneOfFields = ["associatedChildMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV4", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV4"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusAddYours", {
            get: $util.oneOfGetter($oneOfFields = ["statusAddYours"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_richResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusNotificationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusNotificationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_limitSharingMessage", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botTaskMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botTaskMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_messageHistoryNotice", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupStatusMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_botForwardedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botForwardedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusQuestionAnswerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuestionAnswerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_questionResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_statusStickerInteractionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusStickerInteractionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV5", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV5"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterFollowerInviteMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterFollowerInviteMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollResultSnapshotMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminProfileMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_newsletterAdminProfileMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_spoilerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["spoilerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollCreationMessageV6", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV6"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_conditionalRevealMessage", {
            get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_pollAddOptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollAddOptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_eventInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_groupRootKeyShare", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKeyShare"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_p2PPaymentReminderNotification", {
            get: $util.oneOfGetter($oneOfFields = ["p2PPaymentReminderNotification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Message.prototype, "_splitPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["splitPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Message.create = function create(properties) {
            return new Message(properties);
        };

        Message.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.conversation != null && Object.hasOwnProperty.call(m, "conversation"))
                w.uint32(10).string(m.conversation);
            if (m.senderKeyDistributionMessage != null && Object.hasOwnProperty.call(m, "senderKeyDistributionMessage"))
                WAE2E.SenderKeyDistributionMessage.encode(m.senderKeyDistributionMessage, w.uint32(18).fork()).ldelim();
            if (m.imageMessage != null && Object.hasOwnProperty.call(m, "imageMessage"))
                WAE2E.ImageMessage.encode(m.imageMessage, w.uint32(26).fork()).ldelim();
            if (m.contactMessage != null && Object.hasOwnProperty.call(m, "contactMessage"))
                WAE2E.ContactMessage.encode(m.contactMessage, w.uint32(34).fork()).ldelim();
            if (m.locationMessage != null && Object.hasOwnProperty.call(m, "locationMessage"))
                WAE2E.LocationMessage.encode(m.locationMessage, w.uint32(42).fork()).ldelim();
            if (m.extendedTextMessage != null && Object.hasOwnProperty.call(m, "extendedTextMessage"))
                WAE2E.ExtendedTextMessage.encode(m.extendedTextMessage, w.uint32(50).fork()).ldelim();
            if (m.documentMessage != null && Object.hasOwnProperty.call(m, "documentMessage"))
                WAE2E.DocumentMessage.encode(m.documentMessage, w.uint32(58).fork()).ldelim();
            if (m.audioMessage != null && Object.hasOwnProperty.call(m, "audioMessage"))
                WAE2E.AudioMessage.encode(m.audioMessage, w.uint32(66).fork()).ldelim();
            if (m.videoMessage != null && Object.hasOwnProperty.call(m, "videoMessage"))
                WAE2E.VideoMessage.encode(m.videoMessage, w.uint32(74).fork()).ldelim();
            if (m.call != null && Object.hasOwnProperty.call(m, "call"))
                WAE2E.Call.encode(m.call, w.uint32(82).fork()).ldelim();
            if (m.chat != null && Object.hasOwnProperty.call(m, "chat"))
                WAE2E.Chat.encode(m.chat, w.uint32(90).fork()).ldelim();
            if (m.protocolMessage != null && Object.hasOwnProperty.call(m, "protocolMessage"))
                WAE2E.ProtocolMessage.encode(m.protocolMessage, w.uint32(98).fork()).ldelim();
            if (m.contactsArrayMessage != null && Object.hasOwnProperty.call(m, "contactsArrayMessage"))
                WAE2E.ContactsArrayMessage.encode(m.contactsArrayMessage, w.uint32(106).fork()).ldelim();
            if (m.highlyStructuredMessage != null && Object.hasOwnProperty.call(m, "highlyStructuredMessage"))
                WAE2E.HighlyStructuredMessage.encode(m.highlyStructuredMessage, w.uint32(114).fork()).ldelim();
            if (m.fastRatchetKeySenderKeyDistributionMessage != null && Object.hasOwnProperty.call(m, "fastRatchetKeySenderKeyDistributionMessage"))
                WAE2E.SenderKeyDistributionMessage.encode(m.fastRatchetKeySenderKeyDistributionMessage, w.uint32(122).fork()).ldelim();
            if (m.sendPaymentMessage != null && Object.hasOwnProperty.call(m, "sendPaymentMessage"))
                WAE2E.SendPaymentMessage.encode(m.sendPaymentMessage, w.uint32(130).fork()).ldelim();
            if (m.liveLocationMessage != null && Object.hasOwnProperty.call(m, "liveLocationMessage"))
                WAE2E.LiveLocationMessage.encode(m.liveLocationMessage, w.uint32(146).fork()).ldelim();
            if (m.requestPaymentMessage != null && Object.hasOwnProperty.call(m, "requestPaymentMessage"))
                WAE2E.RequestPaymentMessage.encode(m.requestPaymentMessage, w.uint32(178).fork()).ldelim();
            if (m.declinePaymentRequestMessage != null && Object.hasOwnProperty.call(m, "declinePaymentRequestMessage"))
                WAE2E.DeclinePaymentRequestMessage.encode(m.declinePaymentRequestMessage, w.uint32(186).fork()).ldelim();
            if (m.cancelPaymentRequestMessage != null && Object.hasOwnProperty.call(m, "cancelPaymentRequestMessage"))
                WAE2E.CancelPaymentRequestMessage.encode(m.cancelPaymentRequestMessage, w.uint32(194).fork()).ldelim();
            if (m.templateMessage != null && Object.hasOwnProperty.call(m, "templateMessage"))
                WAE2E.TemplateMessage.encode(m.templateMessage, w.uint32(202).fork()).ldelim();
            if (m.stickerMessage != null && Object.hasOwnProperty.call(m, "stickerMessage"))
                WAE2E.StickerMessage.encode(m.stickerMessage, w.uint32(210).fork()).ldelim();
            if (m.groupInviteMessage != null && Object.hasOwnProperty.call(m, "groupInviteMessage"))
                WAE2E.GroupInviteMessage.encode(m.groupInviteMessage, w.uint32(226).fork()).ldelim();
            if (m.templateButtonReplyMessage != null && Object.hasOwnProperty.call(m, "templateButtonReplyMessage"))
                WAE2E.TemplateButtonReplyMessage.encode(m.templateButtonReplyMessage, w.uint32(234).fork()).ldelim();
            if (m.productMessage != null && Object.hasOwnProperty.call(m, "productMessage"))
                WAE2E.ProductMessage.encode(m.productMessage, w.uint32(242).fork()).ldelim();
            if (m.deviceSentMessage != null && Object.hasOwnProperty.call(m, "deviceSentMessage"))
                WAE2E.DeviceSentMessage.encode(m.deviceSentMessage, w.uint32(250).fork()).ldelim();
            if (m.messageContextInfo != null && Object.hasOwnProperty.call(m, "messageContextInfo"))
                WAE2E.MessageContextInfo.encode(m.messageContextInfo, w.uint32(282).fork()).ldelim();
            if (m.listMessage != null && Object.hasOwnProperty.call(m, "listMessage"))
                WAE2E.ListMessage.encode(m.listMessage, w.uint32(290).fork()).ldelim();
            if (m.viewOnceMessage != null && Object.hasOwnProperty.call(m, "viewOnceMessage"))
                WAE2E.FutureProofMessage.encode(m.viewOnceMessage, w.uint32(298).fork()).ldelim();
            if (m.orderMessage != null && Object.hasOwnProperty.call(m, "orderMessage"))
                WAE2E.OrderMessage.encode(m.orderMessage, w.uint32(306).fork()).ldelim();
            if (m.listResponseMessage != null && Object.hasOwnProperty.call(m, "listResponseMessage"))
                WAE2E.ListResponseMessage.encode(m.listResponseMessage, w.uint32(314).fork()).ldelim();
            if (m.ephemeralMessage != null && Object.hasOwnProperty.call(m, "ephemeralMessage"))
                WAE2E.FutureProofMessage.encode(m.ephemeralMessage, w.uint32(322).fork()).ldelim();
            if (m.invoiceMessage != null && Object.hasOwnProperty.call(m, "invoiceMessage"))
                WAE2E.InvoiceMessage.encode(m.invoiceMessage, w.uint32(330).fork()).ldelim();
            if (m.buttonsMessage != null && Object.hasOwnProperty.call(m, "buttonsMessage"))
                WAE2E.ButtonsMessage.encode(m.buttonsMessage, w.uint32(338).fork()).ldelim();
            if (m.buttonsResponseMessage != null && Object.hasOwnProperty.call(m, "buttonsResponseMessage"))
                WAE2E.ButtonsResponseMessage.encode(m.buttonsResponseMessage, w.uint32(346).fork()).ldelim();
            if (m.paymentInviteMessage != null && Object.hasOwnProperty.call(m, "paymentInviteMessage"))
                WAE2E.PaymentInviteMessage.encode(m.paymentInviteMessage, w.uint32(354).fork()).ldelim();
            if (m.interactiveMessage != null && Object.hasOwnProperty.call(m, "interactiveMessage"))
                WAE2E.InteractiveMessage.encode(m.interactiveMessage, w.uint32(362).fork()).ldelim();
            if (m.reactionMessage != null && Object.hasOwnProperty.call(m, "reactionMessage"))
                WAE2E.ReactionMessage.encode(m.reactionMessage, w.uint32(370).fork()).ldelim();
            if (m.stickerSyncRmrMessage != null && Object.hasOwnProperty.call(m, "stickerSyncRmrMessage"))
                WAE2E.StickerSyncRMRMessage.encode(m.stickerSyncRmrMessage, w.uint32(378).fork()).ldelim();
            if (m.interactiveResponseMessage != null && Object.hasOwnProperty.call(m, "interactiveResponseMessage"))
                WAE2E.InteractiveResponseMessage.encode(m.interactiveResponseMessage, w.uint32(386).fork()).ldelim();
            if (m.pollCreationMessage != null && Object.hasOwnProperty.call(m, "pollCreationMessage"))
                WAE2E.PollCreationMessage.encode(m.pollCreationMessage, w.uint32(394).fork()).ldelim();
            if (m.pollUpdateMessage != null && Object.hasOwnProperty.call(m, "pollUpdateMessage"))
                WAE2E.PollUpdateMessage.encode(m.pollUpdateMessage, w.uint32(402).fork()).ldelim();
            if (m.keepInChatMessage != null && Object.hasOwnProperty.call(m, "keepInChatMessage"))
                WAE2E.KeepInChatMessage.encode(m.keepInChatMessage, w.uint32(410).fork()).ldelim();
            if (m.documentWithCaptionMessage != null && Object.hasOwnProperty.call(m, "documentWithCaptionMessage"))
                WAE2E.FutureProofMessage.encode(m.documentWithCaptionMessage, w.uint32(426).fork()).ldelim();
            if (m.requestPhoneNumberMessage != null && Object.hasOwnProperty.call(m, "requestPhoneNumberMessage"))
                WAE2E.RequestPhoneNumberMessage.encode(m.requestPhoneNumberMessage, w.uint32(434).fork()).ldelim();
            if (m.viewOnceMessageV2 != null && Object.hasOwnProperty.call(m, "viewOnceMessageV2"))
                WAE2E.FutureProofMessage.encode(m.viewOnceMessageV2, w.uint32(442).fork()).ldelim();
            if (m.encReactionMessage != null && Object.hasOwnProperty.call(m, "encReactionMessage"))
                WAE2E.EncReactionMessage.encode(m.encReactionMessage, w.uint32(450).fork()).ldelim();
            if (m.editedMessage != null && Object.hasOwnProperty.call(m, "editedMessage"))
                WAE2E.FutureProofMessage.encode(m.editedMessage, w.uint32(466).fork()).ldelim();
            if (m.viewOnceMessageV2Extension != null && Object.hasOwnProperty.call(m, "viewOnceMessageV2Extension"))
                WAE2E.FutureProofMessage.encode(m.viewOnceMessageV2Extension, w.uint32(474).fork()).ldelim();
            if (m.pollCreationMessageV2 != null && Object.hasOwnProperty.call(m, "pollCreationMessageV2"))
                WAE2E.PollCreationMessage.encode(m.pollCreationMessageV2, w.uint32(482).fork()).ldelim();
            if (m.scheduledCallCreationMessage != null && Object.hasOwnProperty.call(m, "scheduledCallCreationMessage"))
                WAE2E.ScheduledCallCreationMessage.encode(m.scheduledCallCreationMessage, w.uint32(490).fork()).ldelim();
            if (m.groupMentionedMessage != null && Object.hasOwnProperty.call(m, "groupMentionedMessage"))
                WAE2E.FutureProofMessage.encode(m.groupMentionedMessage, w.uint32(498).fork()).ldelim();
            if (m.pinInChatMessage != null && Object.hasOwnProperty.call(m, "pinInChatMessage"))
                WAE2E.PinInChatMessage.encode(m.pinInChatMessage, w.uint32(506).fork()).ldelim();
            if (m.pollCreationMessageV3 != null && Object.hasOwnProperty.call(m, "pollCreationMessageV3"))
                WAE2E.PollCreationMessage.encode(m.pollCreationMessageV3, w.uint32(514).fork()).ldelim();
            if (m.scheduledCallEditMessage != null && Object.hasOwnProperty.call(m, "scheduledCallEditMessage"))
                WAE2E.ScheduledCallEditMessage.encode(m.scheduledCallEditMessage, w.uint32(522).fork()).ldelim();
            if (m.ptvMessage != null && Object.hasOwnProperty.call(m, "ptvMessage"))
                WAE2E.VideoMessage.encode(m.ptvMessage, w.uint32(530).fork()).ldelim();
            if (m.botInvokeMessage != null && Object.hasOwnProperty.call(m, "botInvokeMessage"))
                WAE2E.FutureProofMessage.encode(m.botInvokeMessage, w.uint32(538).fork()).ldelim();
            if (m.callLogMesssage != null && Object.hasOwnProperty.call(m, "callLogMesssage"))
                WAE2E.CallLogMessage.encode(m.callLogMesssage, w.uint32(554).fork()).ldelim();
            if (m.messageHistoryBundle != null && Object.hasOwnProperty.call(m, "messageHistoryBundle"))
                WAE2E.MessageHistoryBundle.encode(m.messageHistoryBundle, w.uint32(562).fork()).ldelim();
            if (m.encCommentMessage != null && Object.hasOwnProperty.call(m, "encCommentMessage"))
                WAE2E.EncCommentMessage.encode(m.encCommentMessage, w.uint32(570).fork()).ldelim();
            if (m.bcallMessage != null && Object.hasOwnProperty.call(m, "bcallMessage"))
                WAE2E.BCallMessage.encode(m.bcallMessage, w.uint32(578).fork()).ldelim();
            if (m.lottieStickerMessage != null && Object.hasOwnProperty.call(m, "lottieStickerMessage"))
                WAE2E.FutureProofMessage.encode(m.lottieStickerMessage, w.uint32(594).fork()).ldelim();
            if (m.eventMessage != null && Object.hasOwnProperty.call(m, "eventMessage"))
                WAE2E.EventMessage.encode(m.eventMessage, w.uint32(602).fork()).ldelim();
            if (m.encEventResponseMessage != null && Object.hasOwnProperty.call(m, "encEventResponseMessage"))
                WAE2E.EncEventResponseMessage.encode(m.encEventResponseMessage, w.uint32(610).fork()).ldelim();
            if (m.commentMessage != null && Object.hasOwnProperty.call(m, "commentMessage"))
                WAE2E.CommentMessage.encode(m.commentMessage, w.uint32(618).fork()).ldelim();
            if (m.newsletterAdminInviteMessage != null && Object.hasOwnProperty.call(m, "newsletterAdminInviteMessage"))
                WAE2E.NewsletterAdminInviteMessage.encode(m.newsletterAdminInviteMessage, w.uint32(626).fork()).ldelim();
            if (m.placeholderMessage != null && Object.hasOwnProperty.call(m, "placeholderMessage"))
                WAE2E.PlaceholderMessage.encode(m.placeholderMessage, w.uint32(642).fork()).ldelim();
            if (m.secretEncryptedMessage != null && Object.hasOwnProperty.call(m, "secretEncryptedMessage"))
                WAE2E.SecretEncryptedMessage.encode(m.secretEncryptedMessage, w.uint32(658).fork()).ldelim();
            if (m.albumMessage != null && Object.hasOwnProperty.call(m, "albumMessage"))
                WAE2E.AlbumMessage.encode(m.albumMessage, w.uint32(666).fork()).ldelim();
            if (m.eventCoverImage != null && Object.hasOwnProperty.call(m, "eventCoverImage"))
                WAE2E.FutureProofMessage.encode(m.eventCoverImage, w.uint32(682).fork()).ldelim();
            if (m.stickerPackMessage != null && Object.hasOwnProperty.call(m, "stickerPackMessage"))
                WAE2E.StickerPackMessage.encode(m.stickerPackMessage, w.uint32(690).fork()).ldelim();
            if (m.statusMentionMessage != null && Object.hasOwnProperty.call(m, "statusMentionMessage"))
                WAE2E.FutureProofMessage.encode(m.statusMentionMessage, w.uint32(698).fork()).ldelim();
            if (m.pollResultSnapshotMessage != null && Object.hasOwnProperty.call(m, "pollResultSnapshotMessage"))
                WAE2E.PollResultSnapshotMessage.encode(m.pollResultSnapshotMessage, w.uint32(706).fork()).ldelim();
            if (m.pollCreationOptionImageMessage != null && Object.hasOwnProperty.call(m, "pollCreationOptionImageMessage"))
                WAE2E.FutureProofMessage.encode(m.pollCreationOptionImageMessage, w.uint32(722).fork()).ldelim();
            if (m.associatedChildMessage != null && Object.hasOwnProperty.call(m, "associatedChildMessage"))
                WAE2E.FutureProofMessage.encode(m.associatedChildMessage, w.uint32(730).fork()).ldelim();
            if (m.groupStatusMentionMessage != null && Object.hasOwnProperty.call(m, "groupStatusMentionMessage"))
                WAE2E.FutureProofMessage.encode(m.groupStatusMentionMessage, w.uint32(738).fork()).ldelim();
            if (m.pollCreationMessageV4 != null && Object.hasOwnProperty.call(m, "pollCreationMessageV4"))
                WAE2E.FutureProofMessage.encode(m.pollCreationMessageV4, w.uint32(746).fork()).ldelim();
            if (m.statusAddYours != null && Object.hasOwnProperty.call(m, "statusAddYours"))
                WAE2E.FutureProofMessage.encode(m.statusAddYours, w.uint32(762).fork()).ldelim();
            if (m.groupStatusMessage != null && Object.hasOwnProperty.call(m, "groupStatusMessage"))
                WAE2E.FutureProofMessage.encode(m.groupStatusMessage, w.uint32(770).fork()).ldelim();
            if (m.richResponseMessage != null && Object.hasOwnProperty.call(m, "richResponseMessage"))
                WAE2E.AIRichResponseMessage.encode(m.richResponseMessage, w.uint32(778).fork()).ldelim();
            if (m.statusNotificationMessage != null && Object.hasOwnProperty.call(m, "statusNotificationMessage"))
                WAE2E.StatusNotificationMessage.encode(m.statusNotificationMessage, w.uint32(786).fork()).ldelim();
            if (m.limitSharingMessage != null && Object.hasOwnProperty.call(m, "limitSharingMessage"))
                WAE2E.FutureProofMessage.encode(m.limitSharingMessage, w.uint32(794).fork()).ldelim();
            if (m.botTaskMessage != null && Object.hasOwnProperty.call(m, "botTaskMessage"))
                WAE2E.FutureProofMessage.encode(m.botTaskMessage, w.uint32(802).fork()).ldelim();
            if (m.questionMessage != null && Object.hasOwnProperty.call(m, "questionMessage"))
                WAE2E.FutureProofMessage.encode(m.questionMessage, w.uint32(810).fork()).ldelim();
            if (m.messageHistoryNotice != null && Object.hasOwnProperty.call(m, "messageHistoryNotice"))
                WAE2E.MessageHistoryNotice.encode(m.messageHistoryNotice, w.uint32(818).fork()).ldelim();
            if (m.groupStatusMessageV2 != null && Object.hasOwnProperty.call(m, "groupStatusMessageV2"))
                WAE2E.FutureProofMessage.encode(m.groupStatusMessageV2, w.uint32(826).fork()).ldelim();
            if (m.botForwardedMessage != null && Object.hasOwnProperty.call(m, "botForwardedMessage"))
                WAE2E.FutureProofMessage.encode(m.botForwardedMessage, w.uint32(834).fork()).ldelim();
            if (m.statusQuestionAnswerMessage != null && Object.hasOwnProperty.call(m, "statusQuestionAnswerMessage"))
                WAE2E.StatusQuestionAnswerMessage.encode(m.statusQuestionAnswerMessage, w.uint32(842).fork()).ldelim();
            if (m.questionReplyMessage != null && Object.hasOwnProperty.call(m, "questionReplyMessage"))
                WAE2E.FutureProofMessage.encode(m.questionReplyMessage, w.uint32(850).fork()).ldelim();
            if (m.questionResponseMessage != null && Object.hasOwnProperty.call(m, "questionResponseMessage"))
                WAE2E.QuestionResponseMessage.encode(m.questionResponseMessage, w.uint32(858).fork()).ldelim();
            if (m.statusQuotedMessage != null && Object.hasOwnProperty.call(m, "statusQuotedMessage"))
                WAE2E.StatusQuotedMessage.encode(m.statusQuotedMessage, w.uint32(874).fork()).ldelim();
            if (m.statusStickerInteractionMessage != null && Object.hasOwnProperty.call(m, "statusStickerInteractionMessage"))
                WAE2E.StatusStickerInteractionMessage.encode(m.statusStickerInteractionMessage, w.uint32(882).fork()).ldelim();
            if (m.pollCreationMessageV5 != null && Object.hasOwnProperty.call(m, "pollCreationMessageV5"))
                WAE2E.PollCreationMessage.encode(m.pollCreationMessageV5, w.uint32(890).fork()).ldelim();
            if (m.newsletterFollowerInviteMessageV2 != null && Object.hasOwnProperty.call(m, "newsletterFollowerInviteMessageV2"))
                WAE2E.NewsletterFollowerInviteMessage.encode(m.newsletterFollowerInviteMessageV2, w.uint32(906).fork()).ldelim();
            if (m.pollResultSnapshotMessageV3 != null && Object.hasOwnProperty.call(m, "pollResultSnapshotMessageV3"))
                WAE2E.PollResultSnapshotMessage.encode(m.pollResultSnapshotMessageV3, w.uint32(922).fork()).ldelim();
            if (m.newsletterAdminProfileMessage != null && Object.hasOwnProperty.call(m, "newsletterAdminProfileMessage"))
                WAE2E.FutureProofMessage.encode(m.newsletterAdminProfileMessage, w.uint32(930).fork()).ldelim();
            if (m.newsletterAdminProfileMessageV2 != null && Object.hasOwnProperty.call(m, "newsletterAdminProfileMessageV2"))
                WAE2E.FutureProofMessage.encode(m.newsletterAdminProfileMessageV2, w.uint32(938).fork()).ldelim();
            if (m.spoilerMessage != null && Object.hasOwnProperty.call(m, "spoilerMessage"))
                WAE2E.FutureProofMessage.encode(m.spoilerMessage, w.uint32(946).fork()).ldelim();
            if (m.pollCreationMessageV6 != null && Object.hasOwnProperty.call(m, "pollCreationMessageV6"))
                WAE2E.PollCreationMessage.encode(m.pollCreationMessageV6, w.uint32(954).fork()).ldelim();
            if (m.conditionalRevealMessage != null && Object.hasOwnProperty.call(m, "conditionalRevealMessage"))
                WAE2E.ConditionalRevealMessage.encode(m.conditionalRevealMessage, w.uint32(962).fork()).ldelim();
            if (m.pollAddOptionMessage != null && Object.hasOwnProperty.call(m, "pollAddOptionMessage"))
                WAE2E.PollAddOptionMessage.encode(m.pollAddOptionMessage, w.uint32(970).fork()).ldelim();
            if (m.eventInviteMessage != null && Object.hasOwnProperty.call(m, "eventInviteMessage"))
                WAE2E.EventInviteMessage.encode(m.eventInviteMessage, w.uint32(978).fork()).ldelim();
            if (m.groupRootKeyShare != null && Object.hasOwnProperty.call(m, "groupRootKeyShare"))
                WAE2E.GroupRootKeyShare.encode(m.groupRootKeyShare, w.uint32(986).fork()).ldelim();
            if (m.p2PPaymentReminderNotification != null && Object.hasOwnProperty.call(m, "p2PPaymentReminderNotification"))
                WAE2E.P2PPaymentReminderNotification.encode(m.p2PPaymentReminderNotification, w.uint32(994).fork()).ldelim();
            if (m.splitPaymentMessage != null && Object.hasOwnProperty.call(m, "splitPaymentMessage"))
                WAE2E.SplitPaymentMessage.encode(m.splitPaymentMessage, w.uint32(1002).fork()).ldelim();
            return w;
        };

        Message.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Message();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.conversation = r.string();
                        break;
                    }
                case 2: {
                        m.senderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.imageMessage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.contactMessage = WAE2E.ContactMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.locationMessage = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.extendedTextMessage = WAE2E.ExtendedTextMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.documentMessage = WAE2E.DocumentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.audioMessage = WAE2E.AudioMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.videoMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.call = WAE2E.Call.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.chat = WAE2E.Chat.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 12: {
                        m.protocolMessage = WAE2E.ProtocolMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 13: {
                        m.contactsArrayMessage = WAE2E.ContactsArrayMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 14: {
                        m.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.fastRatchetKeySenderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 16: {
                        m.sendPaymentMessage = WAE2E.SendPaymentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.liveLocationMessage = WAE2E.LiveLocationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 22: {
                        m.requestPaymentMessage = WAE2E.RequestPaymentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 23: {
                        m.declinePaymentRequestMessage = WAE2E.DeclinePaymentRequestMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 24: {
                        m.cancelPaymentRequestMessage = WAE2E.CancelPaymentRequestMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 25: {
                        m.templateMessage = WAE2E.TemplateMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 26: {
                        m.stickerMessage = WAE2E.StickerMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 28: {
                        m.groupInviteMessage = WAE2E.GroupInviteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 29: {
                        m.templateButtonReplyMessage = WAE2E.TemplateButtonReplyMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 30: {
                        m.productMessage = WAE2E.ProductMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 31: {
                        m.deviceSentMessage = WAE2E.DeviceSentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 35: {
                        m.messageContextInfo = WAE2E.MessageContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 36: {
                        m.listMessage = WAE2E.ListMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 37: {
                        m.viewOnceMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 38: {
                        m.orderMessage = WAE2E.OrderMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 39: {
                        m.listResponseMessage = WAE2E.ListResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 40: {
                        m.ephemeralMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 41: {
                        m.invoiceMessage = WAE2E.InvoiceMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 42: {
                        m.buttonsMessage = WAE2E.ButtonsMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 43: {
                        m.buttonsResponseMessage = WAE2E.ButtonsResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 44: {
                        m.paymentInviteMessage = WAE2E.PaymentInviteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 45: {
                        m.interactiveMessage = WAE2E.InteractiveMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 46: {
                        m.reactionMessage = WAE2E.ReactionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 47: {
                        m.stickerSyncRmrMessage = WAE2E.StickerSyncRMRMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 48: {
                        m.interactiveResponseMessage = WAE2E.InteractiveResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 49: {
                        m.pollCreationMessage = WAE2E.PollCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 50: {
                        m.pollUpdateMessage = WAE2E.PollUpdateMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 51: {
                        m.keepInChatMessage = WAE2E.KeepInChatMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 53: {
                        m.documentWithCaptionMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 54: {
                        m.requestPhoneNumberMessage = WAE2E.RequestPhoneNumberMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 55: {
                        m.viewOnceMessageV2 = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 56: {
                        m.encReactionMessage = WAE2E.EncReactionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 58: {
                        m.editedMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 59: {
                        m.viewOnceMessageV2Extension = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 60: {
                        m.pollCreationMessageV2 = WAE2E.PollCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 61: {
                        m.scheduledCallCreationMessage = WAE2E.ScheduledCallCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 62: {
                        m.groupMentionedMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 63: {
                        m.pinInChatMessage = WAE2E.PinInChatMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 64: {
                        m.pollCreationMessageV3 = WAE2E.PollCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 65: {
                        m.scheduledCallEditMessage = WAE2E.ScheduledCallEditMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 66: {
                        m.ptvMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 67: {
                        m.botInvokeMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 69: {
                        m.callLogMesssage = WAE2E.CallLogMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 70: {
                        m.messageHistoryBundle = WAE2E.MessageHistoryBundle.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 71: {
                        m.encCommentMessage = WAE2E.EncCommentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 72: {
                        m.bcallMessage = WAE2E.BCallMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 74: {
                        m.lottieStickerMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 75: {
                        m.eventMessage = WAE2E.EventMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 76: {
                        m.encEventResponseMessage = WAE2E.EncEventResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 77: {
                        m.commentMessage = WAE2E.CommentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 78: {
                        m.newsletterAdminInviteMessage = WAE2E.NewsletterAdminInviteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 80: {
                        m.placeholderMessage = WAE2E.PlaceholderMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 82: {
                        m.secretEncryptedMessage = WAE2E.SecretEncryptedMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 83: {
                        m.albumMessage = WAE2E.AlbumMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 85: {
                        m.eventCoverImage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 86: {
                        m.stickerPackMessage = WAE2E.StickerPackMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 87: {
                        m.statusMentionMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 88: {
                        m.pollResultSnapshotMessage = WAE2E.PollResultSnapshotMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 90: {
                        m.pollCreationOptionImageMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 91: {
                        m.associatedChildMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 92: {
                        m.groupStatusMentionMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 93: {
                        m.pollCreationMessageV4 = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 95: {
                        m.statusAddYours = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 96: {
                        m.groupStatusMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 97: {
                        m.richResponseMessage = WAE2E.AIRichResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 98: {
                        m.statusNotificationMessage = WAE2E.StatusNotificationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 99: {
                        m.limitSharingMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 100: {
                        m.botTaskMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 101: {
                        m.questionMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 102: {
                        m.messageHistoryNotice = WAE2E.MessageHistoryNotice.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 103: {
                        m.groupStatusMessageV2 = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 104: {
                        m.botForwardedMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 105: {
                        m.statusQuestionAnswerMessage = WAE2E.StatusQuestionAnswerMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 106: {
                        m.questionReplyMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 107: {
                        m.questionResponseMessage = WAE2E.QuestionResponseMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 109: {
                        m.statusQuotedMessage = WAE2E.StatusQuotedMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 110: {
                        m.statusStickerInteractionMessage = WAE2E.StatusStickerInteractionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 111: {
                        m.pollCreationMessageV5 = WAE2E.PollCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 113: {
                        m.newsletterFollowerInviteMessageV2 = WAE2E.NewsletterFollowerInviteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 115: {
                        m.pollResultSnapshotMessageV3 = WAE2E.PollResultSnapshotMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 116: {
                        m.newsletterAdminProfileMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 117: {
                        m.newsletterAdminProfileMessageV2 = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 118: {
                        m.spoilerMessage = WAE2E.FutureProofMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 119: {
                        m.pollCreationMessageV6 = WAE2E.PollCreationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 120: {
                        m.conditionalRevealMessage = WAE2E.ConditionalRevealMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 121: {
                        m.pollAddOptionMessage = WAE2E.PollAddOptionMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 122: {
                        m.eventInviteMessage = WAE2E.EventInviteMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 123: {
                        m.groupRootKeyShare = WAE2E.GroupRootKeyShare.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 124: {
                        m.p2PPaymentReminderNotification = WAE2E.P2PPaymentReminderNotification.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 125: {
                        m.splitPaymentMessage = WAE2E.SplitPaymentMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Message.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Message)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Message();
            if (d.conversation != null) {
                m.conversation = String(d.conversation);
            }
            if (d.senderKeyDistributionMessage != null) {
                if (typeof d.senderKeyDistributionMessage !== "object")
                    throw TypeError(".WAE2E.Message.senderKeyDistributionMessage: object expected");
                m.senderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.fromObject(d.senderKeyDistributionMessage, n + 1);
            }
            if (d.imageMessage != null) {
                if (typeof d.imageMessage !== "object")
                    throw TypeError(".WAE2E.Message.imageMessage: object expected");
                m.imageMessage = WAE2E.ImageMessage.fromObject(d.imageMessage, n + 1);
            }
            if (d.contactMessage != null) {
                if (typeof d.contactMessage !== "object")
                    throw TypeError(".WAE2E.Message.contactMessage: object expected");
                m.contactMessage = WAE2E.ContactMessage.fromObject(d.contactMessage, n + 1);
            }
            if (d.locationMessage != null) {
                if (typeof d.locationMessage !== "object")
                    throw TypeError(".WAE2E.Message.locationMessage: object expected");
                m.locationMessage = WAE2E.LocationMessage.fromObject(d.locationMessage, n + 1);
            }
            if (d.extendedTextMessage != null) {
                if (typeof d.extendedTextMessage !== "object")
                    throw TypeError(".WAE2E.Message.extendedTextMessage: object expected");
                m.extendedTextMessage = WAE2E.ExtendedTextMessage.fromObject(d.extendedTextMessage, n + 1);
            }
            if (d.documentMessage != null) {
                if (typeof d.documentMessage !== "object")
                    throw TypeError(".WAE2E.Message.documentMessage: object expected");
                m.documentMessage = WAE2E.DocumentMessage.fromObject(d.documentMessage, n + 1);
            }
            if (d.audioMessage != null) {
                if (typeof d.audioMessage !== "object")
                    throw TypeError(".WAE2E.Message.audioMessage: object expected");
                m.audioMessage = WAE2E.AudioMessage.fromObject(d.audioMessage, n + 1);
            }
            if (d.videoMessage != null) {
                if (typeof d.videoMessage !== "object")
                    throw TypeError(".WAE2E.Message.videoMessage: object expected");
                m.videoMessage = WAE2E.VideoMessage.fromObject(d.videoMessage, n + 1);
            }
            if (d.call != null) {
                if (typeof d.call !== "object")
                    throw TypeError(".WAE2E.Message.call: object expected");
                m.call = WAE2E.Call.fromObject(d.call, n + 1);
            }
            if (d.chat != null) {
                if (typeof d.chat !== "object")
                    throw TypeError(".WAE2E.Message.chat: object expected");
                m.chat = WAE2E.Chat.fromObject(d.chat, n + 1);
            }
            if (d.protocolMessage != null) {
                if (typeof d.protocolMessage !== "object")
                    throw TypeError(".WAE2E.Message.protocolMessage: object expected");
                m.protocolMessage = WAE2E.ProtocolMessage.fromObject(d.protocolMessage, n + 1);
            }
            if (d.contactsArrayMessage != null) {
                if (typeof d.contactsArrayMessage !== "object")
                    throw TypeError(".WAE2E.Message.contactsArrayMessage: object expected");
                m.contactsArrayMessage = WAE2E.ContactsArrayMessage.fromObject(d.contactsArrayMessage, n + 1);
            }
            if (d.highlyStructuredMessage != null) {
                if (typeof d.highlyStructuredMessage !== "object")
                    throw TypeError(".WAE2E.Message.highlyStructuredMessage: object expected");
                m.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.fromObject(d.highlyStructuredMessage, n + 1);
            }
            if (d.fastRatchetKeySenderKeyDistributionMessage != null) {
                if (typeof d.fastRatchetKeySenderKeyDistributionMessage !== "object")
                    throw TypeError(".WAE2E.Message.fastRatchetKeySenderKeyDistributionMessage: object expected");
                m.fastRatchetKeySenderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.fromObject(d.fastRatchetKeySenderKeyDistributionMessage, n + 1);
            }
            if (d.sendPaymentMessage != null) {
                if (typeof d.sendPaymentMessage !== "object")
                    throw TypeError(".WAE2E.Message.sendPaymentMessage: object expected");
                m.sendPaymentMessage = WAE2E.SendPaymentMessage.fromObject(d.sendPaymentMessage, n + 1);
            }
            if (d.liveLocationMessage != null) {
                if (typeof d.liveLocationMessage !== "object")
                    throw TypeError(".WAE2E.Message.liveLocationMessage: object expected");
                m.liveLocationMessage = WAE2E.LiveLocationMessage.fromObject(d.liveLocationMessage, n + 1);
            }
            if (d.requestPaymentMessage != null) {
                if (typeof d.requestPaymentMessage !== "object")
                    throw TypeError(".WAE2E.Message.requestPaymentMessage: object expected");
                m.requestPaymentMessage = WAE2E.RequestPaymentMessage.fromObject(d.requestPaymentMessage, n + 1);
            }
            if (d.declinePaymentRequestMessage != null) {
                if (typeof d.declinePaymentRequestMessage !== "object")
                    throw TypeError(".WAE2E.Message.declinePaymentRequestMessage: object expected");
                m.declinePaymentRequestMessage = WAE2E.DeclinePaymentRequestMessage.fromObject(d.declinePaymentRequestMessage, n + 1);
            }
            if (d.cancelPaymentRequestMessage != null) {
                if (typeof d.cancelPaymentRequestMessage !== "object")
                    throw TypeError(".WAE2E.Message.cancelPaymentRequestMessage: object expected");
                m.cancelPaymentRequestMessage = WAE2E.CancelPaymentRequestMessage.fromObject(d.cancelPaymentRequestMessage, n + 1);
            }
            if (d.templateMessage != null) {
                if (typeof d.templateMessage !== "object")
                    throw TypeError(".WAE2E.Message.templateMessage: object expected");
                m.templateMessage = WAE2E.TemplateMessage.fromObject(d.templateMessage, n + 1);
            }
            if (d.stickerMessage != null) {
                if (typeof d.stickerMessage !== "object")
                    throw TypeError(".WAE2E.Message.stickerMessage: object expected");
                m.stickerMessage = WAE2E.StickerMessage.fromObject(d.stickerMessage, n + 1);
            }
            if (d.groupInviteMessage != null) {
                if (typeof d.groupInviteMessage !== "object")
                    throw TypeError(".WAE2E.Message.groupInviteMessage: object expected");
                m.groupInviteMessage = WAE2E.GroupInviteMessage.fromObject(d.groupInviteMessage, n + 1);
            }
            if (d.templateButtonReplyMessage != null) {
                if (typeof d.templateButtonReplyMessage !== "object")
                    throw TypeError(".WAE2E.Message.templateButtonReplyMessage: object expected");
                m.templateButtonReplyMessage = WAE2E.TemplateButtonReplyMessage.fromObject(d.templateButtonReplyMessage, n + 1);
            }
            if (d.productMessage != null) {
                if (typeof d.productMessage !== "object")
                    throw TypeError(".WAE2E.Message.productMessage: object expected");
                m.productMessage = WAE2E.ProductMessage.fromObject(d.productMessage, n + 1);
            }
            if (d.deviceSentMessage != null) {
                if (typeof d.deviceSentMessage !== "object")
                    throw TypeError(".WAE2E.Message.deviceSentMessage: object expected");
                m.deviceSentMessage = WAE2E.DeviceSentMessage.fromObject(d.deviceSentMessage, n + 1);
            }
            if (d.messageContextInfo != null) {
                if (typeof d.messageContextInfo !== "object")
                    throw TypeError(".WAE2E.Message.messageContextInfo: object expected");
                m.messageContextInfo = WAE2E.MessageContextInfo.fromObject(d.messageContextInfo, n + 1);
            }
            if (d.listMessage != null) {
                if (typeof d.listMessage !== "object")
                    throw TypeError(".WAE2E.Message.listMessage: object expected");
                m.listMessage = WAE2E.ListMessage.fromObject(d.listMessage, n + 1);
            }
            if (d.viewOnceMessage != null) {
                if (typeof d.viewOnceMessage !== "object")
                    throw TypeError(".WAE2E.Message.viewOnceMessage: object expected");
                m.viewOnceMessage = WAE2E.FutureProofMessage.fromObject(d.viewOnceMessage, n + 1);
            }
            if (d.orderMessage != null) {
                if (typeof d.orderMessage !== "object")
                    throw TypeError(".WAE2E.Message.orderMessage: object expected");
                m.orderMessage = WAE2E.OrderMessage.fromObject(d.orderMessage, n + 1);
            }
            if (d.listResponseMessage != null) {
                if (typeof d.listResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.listResponseMessage: object expected");
                m.listResponseMessage = WAE2E.ListResponseMessage.fromObject(d.listResponseMessage, n + 1);
            }
            if (d.ephemeralMessage != null) {
                if (typeof d.ephemeralMessage !== "object")
                    throw TypeError(".WAE2E.Message.ephemeralMessage: object expected");
                m.ephemeralMessage = WAE2E.FutureProofMessage.fromObject(d.ephemeralMessage, n + 1);
            }
            if (d.invoiceMessage != null) {
                if (typeof d.invoiceMessage !== "object")
                    throw TypeError(".WAE2E.Message.invoiceMessage: object expected");
                m.invoiceMessage = WAE2E.InvoiceMessage.fromObject(d.invoiceMessage, n + 1);
            }
            if (d.buttonsMessage != null) {
                if (typeof d.buttonsMessage !== "object")
                    throw TypeError(".WAE2E.Message.buttonsMessage: object expected");
                m.buttonsMessage = WAE2E.ButtonsMessage.fromObject(d.buttonsMessage, n + 1);
            }
            if (d.buttonsResponseMessage != null) {
                if (typeof d.buttonsResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.buttonsResponseMessage: object expected");
                m.buttonsResponseMessage = WAE2E.ButtonsResponseMessage.fromObject(d.buttonsResponseMessage, n + 1);
            }
            if (d.paymentInviteMessage != null) {
                if (typeof d.paymentInviteMessage !== "object")
                    throw TypeError(".WAE2E.Message.paymentInviteMessage: object expected");
                m.paymentInviteMessage = WAE2E.PaymentInviteMessage.fromObject(d.paymentInviteMessage, n + 1);
            }
            if (d.interactiveMessage != null) {
                if (typeof d.interactiveMessage !== "object")
                    throw TypeError(".WAE2E.Message.interactiveMessage: object expected");
                m.interactiveMessage = WAE2E.InteractiveMessage.fromObject(d.interactiveMessage, n + 1);
            }
            if (d.reactionMessage != null) {
                if (typeof d.reactionMessage !== "object")
                    throw TypeError(".WAE2E.Message.reactionMessage: object expected");
                m.reactionMessage = WAE2E.ReactionMessage.fromObject(d.reactionMessage, n + 1);
            }
            if (d.stickerSyncRmrMessage != null) {
                if (typeof d.stickerSyncRmrMessage !== "object")
                    throw TypeError(".WAE2E.Message.stickerSyncRmrMessage: object expected");
                m.stickerSyncRmrMessage = WAE2E.StickerSyncRMRMessage.fromObject(d.stickerSyncRmrMessage, n + 1);
            }
            if (d.interactiveResponseMessage != null) {
                if (typeof d.interactiveResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.interactiveResponseMessage: object expected");
                m.interactiveResponseMessage = WAE2E.InteractiveResponseMessage.fromObject(d.interactiveResponseMessage, n + 1);
            }
            if (d.pollCreationMessage != null) {
                if (typeof d.pollCreationMessage !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessage: object expected");
                m.pollCreationMessage = WAE2E.PollCreationMessage.fromObject(d.pollCreationMessage, n + 1);
            }
            if (d.pollUpdateMessage != null) {
                if (typeof d.pollUpdateMessage !== "object")
                    throw TypeError(".WAE2E.Message.pollUpdateMessage: object expected");
                m.pollUpdateMessage = WAE2E.PollUpdateMessage.fromObject(d.pollUpdateMessage, n + 1);
            }
            if (d.keepInChatMessage != null) {
                if (typeof d.keepInChatMessage !== "object")
                    throw TypeError(".WAE2E.Message.keepInChatMessage: object expected");
                m.keepInChatMessage = WAE2E.KeepInChatMessage.fromObject(d.keepInChatMessage, n + 1);
            }
            if (d.documentWithCaptionMessage != null) {
                if (typeof d.documentWithCaptionMessage !== "object")
                    throw TypeError(".WAE2E.Message.documentWithCaptionMessage: object expected");
                m.documentWithCaptionMessage = WAE2E.FutureProofMessage.fromObject(d.documentWithCaptionMessage, n + 1);
            }
            if (d.requestPhoneNumberMessage != null) {
                if (typeof d.requestPhoneNumberMessage !== "object")
                    throw TypeError(".WAE2E.Message.requestPhoneNumberMessage: object expected");
                m.requestPhoneNumberMessage = WAE2E.RequestPhoneNumberMessage.fromObject(d.requestPhoneNumberMessage, n + 1);
            }
            if (d.viewOnceMessageV2 != null) {
                if (typeof d.viewOnceMessageV2 !== "object")
                    throw TypeError(".WAE2E.Message.viewOnceMessageV2: object expected");
                m.viewOnceMessageV2 = WAE2E.FutureProofMessage.fromObject(d.viewOnceMessageV2, n + 1);
            }
            if (d.encReactionMessage != null) {
                if (typeof d.encReactionMessage !== "object")
                    throw TypeError(".WAE2E.Message.encReactionMessage: object expected");
                m.encReactionMessage = WAE2E.EncReactionMessage.fromObject(d.encReactionMessage, n + 1);
            }
            if (d.editedMessage != null) {
                if (typeof d.editedMessage !== "object")
                    throw TypeError(".WAE2E.Message.editedMessage: object expected");
                m.editedMessage = WAE2E.FutureProofMessage.fromObject(d.editedMessage, n + 1);
            }
            if (d.viewOnceMessageV2Extension != null) {
                if (typeof d.viewOnceMessageV2Extension !== "object")
                    throw TypeError(".WAE2E.Message.viewOnceMessageV2Extension: object expected");
                m.viewOnceMessageV2Extension = WAE2E.FutureProofMessage.fromObject(d.viewOnceMessageV2Extension, n + 1);
            }
            if (d.pollCreationMessageV2 != null) {
                if (typeof d.pollCreationMessageV2 !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessageV2: object expected");
                m.pollCreationMessageV2 = WAE2E.PollCreationMessage.fromObject(d.pollCreationMessageV2, n + 1);
            }
            if (d.scheduledCallCreationMessage != null) {
                if (typeof d.scheduledCallCreationMessage !== "object")
                    throw TypeError(".WAE2E.Message.scheduledCallCreationMessage: object expected");
                m.scheduledCallCreationMessage = WAE2E.ScheduledCallCreationMessage.fromObject(d.scheduledCallCreationMessage, n + 1);
            }
            if (d.groupMentionedMessage != null) {
                if (typeof d.groupMentionedMessage !== "object")
                    throw TypeError(".WAE2E.Message.groupMentionedMessage: object expected");
                m.groupMentionedMessage = WAE2E.FutureProofMessage.fromObject(d.groupMentionedMessage, n + 1);
            }
            if (d.pinInChatMessage != null) {
                if (typeof d.pinInChatMessage !== "object")
                    throw TypeError(".WAE2E.Message.pinInChatMessage: object expected");
                m.pinInChatMessage = WAE2E.PinInChatMessage.fromObject(d.pinInChatMessage, n + 1);
            }
            if (d.pollCreationMessageV3 != null) {
                if (typeof d.pollCreationMessageV3 !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessageV3: object expected");
                m.pollCreationMessageV3 = WAE2E.PollCreationMessage.fromObject(d.pollCreationMessageV3, n + 1);
            }
            if (d.scheduledCallEditMessage != null) {
                if (typeof d.scheduledCallEditMessage !== "object")
                    throw TypeError(".WAE2E.Message.scheduledCallEditMessage: object expected");
                m.scheduledCallEditMessage = WAE2E.ScheduledCallEditMessage.fromObject(d.scheduledCallEditMessage, n + 1);
            }
            if (d.ptvMessage != null) {
                if (typeof d.ptvMessage !== "object")
                    throw TypeError(".WAE2E.Message.ptvMessage: object expected");
                m.ptvMessage = WAE2E.VideoMessage.fromObject(d.ptvMessage, n + 1);
            }
            if (d.botInvokeMessage != null) {
                if (typeof d.botInvokeMessage !== "object")
                    throw TypeError(".WAE2E.Message.botInvokeMessage: object expected");
                m.botInvokeMessage = WAE2E.FutureProofMessage.fromObject(d.botInvokeMessage, n + 1);
            }
            if (d.callLogMesssage != null) {
                if (typeof d.callLogMesssage !== "object")
                    throw TypeError(".WAE2E.Message.callLogMesssage: object expected");
                m.callLogMesssage = WAE2E.CallLogMessage.fromObject(d.callLogMesssage, n + 1);
            }
            if (d.messageHistoryBundle != null) {
                if (typeof d.messageHistoryBundle !== "object")
                    throw TypeError(".WAE2E.Message.messageHistoryBundle: object expected");
                m.messageHistoryBundle = WAE2E.MessageHistoryBundle.fromObject(d.messageHistoryBundle, n + 1);
            }
            if (d.encCommentMessage != null) {
                if (typeof d.encCommentMessage !== "object")
                    throw TypeError(".WAE2E.Message.encCommentMessage: object expected");
                m.encCommentMessage = WAE2E.EncCommentMessage.fromObject(d.encCommentMessage, n + 1);
            }
            if (d.bcallMessage != null) {
                if (typeof d.bcallMessage !== "object")
                    throw TypeError(".WAE2E.Message.bcallMessage: object expected");
                m.bcallMessage = WAE2E.BCallMessage.fromObject(d.bcallMessage, n + 1);
            }
            if (d.lottieStickerMessage != null) {
                if (typeof d.lottieStickerMessage !== "object")
                    throw TypeError(".WAE2E.Message.lottieStickerMessage: object expected");
                m.lottieStickerMessage = WAE2E.FutureProofMessage.fromObject(d.lottieStickerMessage, n + 1);
            }
            if (d.eventMessage != null) {
                if (typeof d.eventMessage !== "object")
                    throw TypeError(".WAE2E.Message.eventMessage: object expected");
                m.eventMessage = WAE2E.EventMessage.fromObject(d.eventMessage, n + 1);
            }
            if (d.encEventResponseMessage != null) {
                if (typeof d.encEventResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.encEventResponseMessage: object expected");
                m.encEventResponseMessage = WAE2E.EncEventResponseMessage.fromObject(d.encEventResponseMessage, n + 1);
            }
            if (d.commentMessage != null) {
                if (typeof d.commentMessage !== "object")
                    throw TypeError(".WAE2E.Message.commentMessage: object expected");
                m.commentMessage = WAE2E.CommentMessage.fromObject(d.commentMessage, n + 1);
            }
            if (d.newsletterAdminInviteMessage != null) {
                if (typeof d.newsletterAdminInviteMessage !== "object")
                    throw TypeError(".WAE2E.Message.newsletterAdminInviteMessage: object expected");
                m.newsletterAdminInviteMessage = WAE2E.NewsletterAdminInviteMessage.fromObject(d.newsletterAdminInviteMessage, n + 1);
            }
            if (d.placeholderMessage != null) {
                if (typeof d.placeholderMessage !== "object")
                    throw TypeError(".WAE2E.Message.placeholderMessage: object expected");
                m.placeholderMessage = WAE2E.PlaceholderMessage.fromObject(d.placeholderMessage, n + 1);
            }
            if (d.secretEncryptedMessage != null) {
                if (typeof d.secretEncryptedMessage !== "object")
                    throw TypeError(".WAE2E.Message.secretEncryptedMessage: object expected");
                m.secretEncryptedMessage = WAE2E.SecretEncryptedMessage.fromObject(d.secretEncryptedMessage, n + 1);
            }
            if (d.albumMessage != null) {
                if (typeof d.albumMessage !== "object")
                    throw TypeError(".WAE2E.Message.albumMessage: object expected");
                m.albumMessage = WAE2E.AlbumMessage.fromObject(d.albumMessage, n + 1);
            }
            if (d.eventCoverImage != null) {
                if (typeof d.eventCoverImage !== "object")
                    throw TypeError(".WAE2E.Message.eventCoverImage: object expected");
                m.eventCoverImage = WAE2E.FutureProofMessage.fromObject(d.eventCoverImage, n + 1);
            }
            if (d.stickerPackMessage != null) {
                if (typeof d.stickerPackMessage !== "object")
                    throw TypeError(".WAE2E.Message.stickerPackMessage: object expected");
                m.stickerPackMessage = WAE2E.StickerPackMessage.fromObject(d.stickerPackMessage, n + 1);
            }
            if (d.statusMentionMessage != null) {
                if (typeof d.statusMentionMessage !== "object")
                    throw TypeError(".WAE2E.Message.statusMentionMessage: object expected");
                m.statusMentionMessage = WAE2E.FutureProofMessage.fromObject(d.statusMentionMessage, n + 1);
            }
            if (d.pollResultSnapshotMessage != null) {
                if (typeof d.pollResultSnapshotMessage !== "object")
                    throw TypeError(".WAE2E.Message.pollResultSnapshotMessage: object expected");
                m.pollResultSnapshotMessage = WAE2E.PollResultSnapshotMessage.fromObject(d.pollResultSnapshotMessage, n + 1);
            }
            if (d.pollCreationOptionImageMessage != null) {
                if (typeof d.pollCreationOptionImageMessage !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationOptionImageMessage: object expected");
                m.pollCreationOptionImageMessage = WAE2E.FutureProofMessage.fromObject(d.pollCreationOptionImageMessage, n + 1);
            }
            if (d.associatedChildMessage != null) {
                if (typeof d.associatedChildMessage !== "object")
                    throw TypeError(".WAE2E.Message.associatedChildMessage: object expected");
                m.associatedChildMessage = WAE2E.FutureProofMessage.fromObject(d.associatedChildMessage, n + 1);
            }
            if (d.groupStatusMentionMessage != null) {
                if (typeof d.groupStatusMentionMessage !== "object")
                    throw TypeError(".WAE2E.Message.groupStatusMentionMessage: object expected");
                m.groupStatusMentionMessage = WAE2E.FutureProofMessage.fromObject(d.groupStatusMentionMessage, n + 1);
            }
            if (d.pollCreationMessageV4 != null) {
                if (typeof d.pollCreationMessageV4 !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessageV4: object expected");
                m.pollCreationMessageV4 = WAE2E.FutureProofMessage.fromObject(d.pollCreationMessageV4, n + 1);
            }
            if (d.statusAddYours != null) {
                if (typeof d.statusAddYours !== "object")
                    throw TypeError(".WAE2E.Message.statusAddYours: object expected");
                m.statusAddYours = WAE2E.FutureProofMessage.fromObject(d.statusAddYours, n + 1);
            }
            if (d.groupStatusMessage != null) {
                if (typeof d.groupStatusMessage !== "object")
                    throw TypeError(".WAE2E.Message.groupStatusMessage: object expected");
                m.groupStatusMessage = WAE2E.FutureProofMessage.fromObject(d.groupStatusMessage, n + 1);
            }
            if (d.richResponseMessage != null) {
                if (typeof d.richResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.richResponseMessage: object expected");
                m.richResponseMessage = WAE2E.AIRichResponseMessage.fromObject(d.richResponseMessage, n + 1);
            }
            if (d.statusNotificationMessage != null) {
                if (typeof d.statusNotificationMessage !== "object")
                    throw TypeError(".WAE2E.Message.statusNotificationMessage: object expected");
                m.statusNotificationMessage = WAE2E.StatusNotificationMessage.fromObject(d.statusNotificationMessage, n + 1);
            }
            if (d.limitSharingMessage != null) {
                if (typeof d.limitSharingMessage !== "object")
                    throw TypeError(".WAE2E.Message.limitSharingMessage: object expected");
                m.limitSharingMessage = WAE2E.FutureProofMessage.fromObject(d.limitSharingMessage, n + 1);
            }
            if (d.botTaskMessage != null) {
                if (typeof d.botTaskMessage !== "object")
                    throw TypeError(".WAE2E.Message.botTaskMessage: object expected");
                m.botTaskMessage = WAE2E.FutureProofMessage.fromObject(d.botTaskMessage, n + 1);
            }
            if (d.questionMessage != null) {
                if (typeof d.questionMessage !== "object")
                    throw TypeError(".WAE2E.Message.questionMessage: object expected");
                m.questionMessage = WAE2E.FutureProofMessage.fromObject(d.questionMessage, n + 1);
            }
            if (d.messageHistoryNotice != null) {
                if (typeof d.messageHistoryNotice !== "object")
                    throw TypeError(".WAE2E.Message.messageHistoryNotice: object expected");
                m.messageHistoryNotice = WAE2E.MessageHistoryNotice.fromObject(d.messageHistoryNotice, n + 1);
            }
            if (d.groupStatusMessageV2 != null) {
                if (typeof d.groupStatusMessageV2 !== "object")
                    throw TypeError(".WAE2E.Message.groupStatusMessageV2: object expected");
                m.groupStatusMessageV2 = WAE2E.FutureProofMessage.fromObject(d.groupStatusMessageV2, n + 1);
            }
            if (d.botForwardedMessage != null) {
                if (typeof d.botForwardedMessage !== "object")
                    throw TypeError(".WAE2E.Message.botForwardedMessage: object expected");
                m.botForwardedMessage = WAE2E.FutureProofMessage.fromObject(d.botForwardedMessage, n + 1);
            }
            if (d.statusQuestionAnswerMessage != null) {
                if (typeof d.statusQuestionAnswerMessage !== "object")
                    throw TypeError(".WAE2E.Message.statusQuestionAnswerMessage: object expected");
                m.statusQuestionAnswerMessage = WAE2E.StatusQuestionAnswerMessage.fromObject(d.statusQuestionAnswerMessage, n + 1);
            }
            if (d.questionReplyMessage != null) {
                if (typeof d.questionReplyMessage !== "object")
                    throw TypeError(".WAE2E.Message.questionReplyMessage: object expected");
                m.questionReplyMessage = WAE2E.FutureProofMessage.fromObject(d.questionReplyMessage, n + 1);
            }
            if (d.questionResponseMessage != null) {
                if (typeof d.questionResponseMessage !== "object")
                    throw TypeError(".WAE2E.Message.questionResponseMessage: object expected");
                m.questionResponseMessage = WAE2E.QuestionResponseMessage.fromObject(d.questionResponseMessage, n + 1);
            }
            if (d.statusQuotedMessage != null) {
                if (typeof d.statusQuotedMessage !== "object")
                    throw TypeError(".WAE2E.Message.statusQuotedMessage: object expected");
                m.statusQuotedMessage = WAE2E.StatusQuotedMessage.fromObject(d.statusQuotedMessage, n + 1);
            }
            if (d.statusStickerInteractionMessage != null) {
                if (typeof d.statusStickerInteractionMessage !== "object")
                    throw TypeError(".WAE2E.Message.statusStickerInteractionMessage: object expected");
                m.statusStickerInteractionMessage = WAE2E.StatusStickerInteractionMessage.fromObject(d.statusStickerInteractionMessage, n + 1);
            }
            if (d.pollCreationMessageV5 != null) {
                if (typeof d.pollCreationMessageV5 !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessageV5: object expected");
                m.pollCreationMessageV5 = WAE2E.PollCreationMessage.fromObject(d.pollCreationMessageV5, n + 1);
            }
            if (d.newsletterFollowerInviteMessageV2 != null) {
                if (typeof d.newsletterFollowerInviteMessageV2 !== "object")
                    throw TypeError(".WAE2E.Message.newsletterFollowerInviteMessageV2: object expected");
                m.newsletterFollowerInviteMessageV2 = WAE2E.NewsletterFollowerInviteMessage.fromObject(d.newsletterFollowerInviteMessageV2, n + 1);
            }
            if (d.pollResultSnapshotMessageV3 != null) {
                if (typeof d.pollResultSnapshotMessageV3 !== "object")
                    throw TypeError(".WAE2E.Message.pollResultSnapshotMessageV3: object expected");
                m.pollResultSnapshotMessageV3 = WAE2E.PollResultSnapshotMessage.fromObject(d.pollResultSnapshotMessageV3, n + 1);
            }
            if (d.newsletterAdminProfileMessage != null) {
                if (typeof d.newsletterAdminProfileMessage !== "object")
                    throw TypeError(".WAE2E.Message.newsletterAdminProfileMessage: object expected");
                m.newsletterAdminProfileMessage = WAE2E.FutureProofMessage.fromObject(d.newsletterAdminProfileMessage, n + 1);
            }
            if (d.newsletterAdminProfileMessageV2 != null) {
                if (typeof d.newsletterAdminProfileMessageV2 !== "object")
                    throw TypeError(".WAE2E.Message.newsletterAdminProfileMessageV2: object expected");
                m.newsletterAdminProfileMessageV2 = WAE2E.FutureProofMessage.fromObject(d.newsletterAdminProfileMessageV2, n + 1);
            }
            if (d.spoilerMessage != null) {
                if (typeof d.spoilerMessage !== "object")
                    throw TypeError(".WAE2E.Message.spoilerMessage: object expected");
                m.spoilerMessage = WAE2E.FutureProofMessage.fromObject(d.spoilerMessage, n + 1);
            }
            if (d.pollCreationMessageV6 != null) {
                if (typeof d.pollCreationMessageV6 !== "object")
                    throw TypeError(".WAE2E.Message.pollCreationMessageV6: object expected");
                m.pollCreationMessageV6 = WAE2E.PollCreationMessage.fromObject(d.pollCreationMessageV6, n + 1);
            }
            if (d.conditionalRevealMessage != null) {
                if (typeof d.conditionalRevealMessage !== "object")
                    throw TypeError(".WAE2E.Message.conditionalRevealMessage: object expected");
                m.conditionalRevealMessage = WAE2E.ConditionalRevealMessage.fromObject(d.conditionalRevealMessage, n + 1);
            }
            if (d.pollAddOptionMessage != null) {
                if (typeof d.pollAddOptionMessage !== "object")
                    throw TypeError(".WAE2E.Message.pollAddOptionMessage: object expected");
                m.pollAddOptionMessage = WAE2E.PollAddOptionMessage.fromObject(d.pollAddOptionMessage, n + 1);
            }
            if (d.eventInviteMessage != null) {
                if (typeof d.eventInviteMessage !== "object")
                    throw TypeError(".WAE2E.Message.eventInviteMessage: object expected");
                m.eventInviteMessage = WAE2E.EventInviteMessage.fromObject(d.eventInviteMessage, n + 1);
            }
            if (d.groupRootKeyShare != null) {
                if (typeof d.groupRootKeyShare !== "object")
                    throw TypeError(".WAE2E.Message.groupRootKeyShare: object expected");
                m.groupRootKeyShare = WAE2E.GroupRootKeyShare.fromObject(d.groupRootKeyShare, n + 1);
            }
            if (d.p2PPaymentReminderNotification != null) {
                if (typeof d.p2PPaymentReminderNotification !== "object")
                    throw TypeError(".WAE2E.Message.p2PPaymentReminderNotification: object expected");
                m.p2PPaymentReminderNotification = WAE2E.P2PPaymentReminderNotification.fromObject(d.p2PPaymentReminderNotification, n + 1);
            }
            if (d.splitPaymentMessage != null) {
                if (typeof d.splitPaymentMessage !== "object")
                    throw TypeError(".WAE2E.Message.splitPaymentMessage: object expected");
                m.splitPaymentMessage = WAE2E.SplitPaymentMessage.fromObject(d.splitPaymentMessage, n + 1);
            }
            return m;
        };

        Message.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.conversation != null && m.hasOwnProperty("conversation")) {
                d.conversation = m.conversation;
                if (o.oneofs)
                    d._conversation = "conversation";
            }
            if (m.senderKeyDistributionMessage != null && m.hasOwnProperty("senderKeyDistributionMessage")) {
                d.senderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.toObject(m.senderKeyDistributionMessage, o);
                if (o.oneofs)
                    d._senderKeyDistributionMessage = "senderKeyDistributionMessage";
            }
            if (m.imageMessage != null && m.hasOwnProperty("imageMessage")) {
                d.imageMessage = WAE2E.ImageMessage.toObject(m.imageMessage, o);
                if (o.oneofs)
                    d._imageMessage = "imageMessage";
            }
            if (m.contactMessage != null && m.hasOwnProperty("contactMessage")) {
                d.contactMessage = WAE2E.ContactMessage.toObject(m.contactMessage, o);
                if (o.oneofs)
                    d._contactMessage = "contactMessage";
            }
            if (m.locationMessage != null && m.hasOwnProperty("locationMessage")) {
                d.locationMessage = WAE2E.LocationMessage.toObject(m.locationMessage, o);
                if (o.oneofs)
                    d._locationMessage = "locationMessage";
            }
            if (m.extendedTextMessage != null && m.hasOwnProperty("extendedTextMessage")) {
                d.extendedTextMessage = WAE2E.ExtendedTextMessage.toObject(m.extendedTextMessage, o);
                if (o.oneofs)
                    d._extendedTextMessage = "extendedTextMessage";
            }
            if (m.documentMessage != null && m.hasOwnProperty("documentMessage")) {
                d.documentMessage = WAE2E.DocumentMessage.toObject(m.documentMessage, o);
                if (o.oneofs)
                    d._documentMessage = "documentMessage";
            }
            if (m.audioMessage != null && m.hasOwnProperty("audioMessage")) {
                d.audioMessage = WAE2E.AudioMessage.toObject(m.audioMessage, o);
                if (o.oneofs)
                    d._audioMessage = "audioMessage";
            }
            if (m.videoMessage != null && m.hasOwnProperty("videoMessage")) {
                d.videoMessage = WAE2E.VideoMessage.toObject(m.videoMessage, o);
                if (o.oneofs)
                    d._videoMessage = "videoMessage";
            }
            if (m.call != null && m.hasOwnProperty("call")) {
                d.call = WAE2E.Call.toObject(m.call, o);
                if (o.oneofs)
                    d._call = "call";
            }
            if (m.chat != null && m.hasOwnProperty("chat")) {
                d.chat = WAE2E.Chat.toObject(m.chat, o);
                if (o.oneofs)
                    d._chat = "chat";
            }
            if (m.protocolMessage != null && m.hasOwnProperty("protocolMessage")) {
                d.protocolMessage = WAE2E.ProtocolMessage.toObject(m.protocolMessage, o);
                if (o.oneofs)
                    d._protocolMessage = "protocolMessage";
            }
            if (m.contactsArrayMessage != null && m.hasOwnProperty("contactsArrayMessage")) {
                d.contactsArrayMessage = WAE2E.ContactsArrayMessage.toObject(m.contactsArrayMessage, o);
                if (o.oneofs)
                    d._contactsArrayMessage = "contactsArrayMessage";
            }
            if (m.highlyStructuredMessage != null && m.hasOwnProperty("highlyStructuredMessage")) {
                d.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.toObject(m.highlyStructuredMessage, o);
                if (o.oneofs)
                    d._highlyStructuredMessage = "highlyStructuredMessage";
            }
            if (m.fastRatchetKeySenderKeyDistributionMessage != null && m.hasOwnProperty("fastRatchetKeySenderKeyDistributionMessage")) {
                d.fastRatchetKeySenderKeyDistributionMessage = WAE2E.SenderKeyDistributionMessage.toObject(m.fastRatchetKeySenderKeyDistributionMessage, o);
                if (o.oneofs)
                    d._fastRatchetKeySenderKeyDistributionMessage = "fastRatchetKeySenderKeyDistributionMessage";
            }
            if (m.sendPaymentMessage != null && m.hasOwnProperty("sendPaymentMessage")) {
                d.sendPaymentMessage = WAE2E.SendPaymentMessage.toObject(m.sendPaymentMessage, o);
                if (o.oneofs)
                    d._sendPaymentMessage = "sendPaymentMessage";
            }
            if (m.liveLocationMessage != null && m.hasOwnProperty("liveLocationMessage")) {
                d.liveLocationMessage = WAE2E.LiveLocationMessage.toObject(m.liveLocationMessage, o);
                if (o.oneofs)
                    d._liveLocationMessage = "liveLocationMessage";
            }
            if (m.requestPaymentMessage != null && m.hasOwnProperty("requestPaymentMessage")) {
                d.requestPaymentMessage = WAE2E.RequestPaymentMessage.toObject(m.requestPaymentMessage, o);
                if (o.oneofs)
                    d._requestPaymentMessage = "requestPaymentMessage";
            }
            if (m.declinePaymentRequestMessage != null && m.hasOwnProperty("declinePaymentRequestMessage")) {
                d.declinePaymentRequestMessage = WAE2E.DeclinePaymentRequestMessage.toObject(m.declinePaymentRequestMessage, o);
                if (o.oneofs)
                    d._declinePaymentRequestMessage = "declinePaymentRequestMessage";
            }
            if (m.cancelPaymentRequestMessage != null && m.hasOwnProperty("cancelPaymentRequestMessage")) {
                d.cancelPaymentRequestMessage = WAE2E.CancelPaymentRequestMessage.toObject(m.cancelPaymentRequestMessage, o);
                if (o.oneofs)
                    d._cancelPaymentRequestMessage = "cancelPaymentRequestMessage";
            }
            if (m.templateMessage != null && m.hasOwnProperty("templateMessage")) {
                d.templateMessage = WAE2E.TemplateMessage.toObject(m.templateMessage, o);
                if (o.oneofs)
                    d._templateMessage = "templateMessage";
            }
            if (m.stickerMessage != null && m.hasOwnProperty("stickerMessage")) {
                d.stickerMessage = WAE2E.StickerMessage.toObject(m.stickerMessage, o);
                if (o.oneofs)
                    d._stickerMessage = "stickerMessage";
            }
            if (m.groupInviteMessage != null && m.hasOwnProperty("groupInviteMessage")) {
                d.groupInviteMessage = WAE2E.GroupInviteMessage.toObject(m.groupInviteMessage, o);
                if (o.oneofs)
                    d._groupInviteMessage = "groupInviteMessage";
            }
            if (m.templateButtonReplyMessage != null && m.hasOwnProperty("templateButtonReplyMessage")) {
                d.templateButtonReplyMessage = WAE2E.TemplateButtonReplyMessage.toObject(m.templateButtonReplyMessage, o);
                if (o.oneofs)
                    d._templateButtonReplyMessage = "templateButtonReplyMessage";
            }
            if (m.productMessage != null && m.hasOwnProperty("productMessage")) {
                d.productMessage = WAE2E.ProductMessage.toObject(m.productMessage, o);
                if (o.oneofs)
                    d._productMessage = "productMessage";
            }
            if (m.deviceSentMessage != null && m.hasOwnProperty("deviceSentMessage")) {
                d.deviceSentMessage = WAE2E.DeviceSentMessage.toObject(m.deviceSentMessage, o);
                if (o.oneofs)
                    d._deviceSentMessage = "deviceSentMessage";
            }
            if (m.messageContextInfo != null && m.hasOwnProperty("messageContextInfo")) {
                d.messageContextInfo = WAE2E.MessageContextInfo.toObject(m.messageContextInfo, o);
                if (o.oneofs)
                    d._messageContextInfo = "messageContextInfo";
            }
            if (m.listMessage != null && m.hasOwnProperty("listMessage")) {
                d.listMessage = WAE2E.ListMessage.toObject(m.listMessage, o);
                if (o.oneofs)
                    d._listMessage = "listMessage";
            }
            if (m.viewOnceMessage != null && m.hasOwnProperty("viewOnceMessage")) {
                d.viewOnceMessage = WAE2E.FutureProofMessage.toObject(m.viewOnceMessage, o);
                if (o.oneofs)
                    d._viewOnceMessage = "viewOnceMessage";
            }
            if (m.orderMessage != null && m.hasOwnProperty("orderMessage")) {
                d.orderMessage = WAE2E.OrderMessage.toObject(m.orderMessage, o);
                if (o.oneofs)
                    d._orderMessage = "orderMessage";
            }
            if (m.listResponseMessage != null && m.hasOwnProperty("listResponseMessage")) {
                d.listResponseMessage = WAE2E.ListResponseMessage.toObject(m.listResponseMessage, o);
                if (o.oneofs)
                    d._listResponseMessage = "listResponseMessage";
            }
            if (m.ephemeralMessage != null && m.hasOwnProperty("ephemeralMessage")) {
                d.ephemeralMessage = WAE2E.FutureProofMessage.toObject(m.ephemeralMessage, o);
                if (o.oneofs)
                    d._ephemeralMessage = "ephemeralMessage";
            }
            if (m.invoiceMessage != null && m.hasOwnProperty("invoiceMessage")) {
                d.invoiceMessage = WAE2E.InvoiceMessage.toObject(m.invoiceMessage, o);
                if (o.oneofs)
                    d._invoiceMessage = "invoiceMessage";
            }
            if (m.buttonsMessage != null && m.hasOwnProperty("buttonsMessage")) {
                d.buttonsMessage = WAE2E.ButtonsMessage.toObject(m.buttonsMessage, o);
                if (o.oneofs)
                    d._buttonsMessage = "buttonsMessage";
            }
            if (m.buttonsResponseMessage != null && m.hasOwnProperty("buttonsResponseMessage")) {
                d.buttonsResponseMessage = WAE2E.ButtonsResponseMessage.toObject(m.buttonsResponseMessage, o);
                if (o.oneofs)
                    d._buttonsResponseMessage = "buttonsResponseMessage";
            }
            if (m.paymentInviteMessage != null && m.hasOwnProperty("paymentInviteMessage")) {
                d.paymentInviteMessage = WAE2E.PaymentInviteMessage.toObject(m.paymentInviteMessage, o);
                if (o.oneofs)
                    d._paymentInviteMessage = "paymentInviteMessage";
            }
            if (m.interactiveMessage != null && m.hasOwnProperty("interactiveMessage")) {
                d.interactiveMessage = WAE2E.InteractiveMessage.toObject(m.interactiveMessage, o);
                if (o.oneofs)
                    d._interactiveMessage = "interactiveMessage";
            }
            if (m.reactionMessage != null && m.hasOwnProperty("reactionMessage")) {
                d.reactionMessage = WAE2E.ReactionMessage.toObject(m.reactionMessage, o);
                if (o.oneofs)
                    d._reactionMessage = "reactionMessage";
            }
            if (m.stickerSyncRmrMessage != null && m.hasOwnProperty("stickerSyncRmrMessage")) {
                d.stickerSyncRmrMessage = WAE2E.StickerSyncRMRMessage.toObject(m.stickerSyncRmrMessage, o);
                if (o.oneofs)
                    d._stickerSyncRmrMessage = "stickerSyncRmrMessage";
            }
            if (m.interactiveResponseMessage != null && m.hasOwnProperty("interactiveResponseMessage")) {
                d.interactiveResponseMessage = WAE2E.InteractiveResponseMessage.toObject(m.interactiveResponseMessage, o);
                if (o.oneofs)
                    d._interactiveResponseMessage = "interactiveResponseMessage";
            }
            if (m.pollCreationMessage != null && m.hasOwnProperty("pollCreationMessage")) {
                d.pollCreationMessage = WAE2E.PollCreationMessage.toObject(m.pollCreationMessage, o);
                if (o.oneofs)
                    d._pollCreationMessage = "pollCreationMessage";
            }
            if (m.pollUpdateMessage != null && m.hasOwnProperty("pollUpdateMessage")) {
                d.pollUpdateMessage = WAE2E.PollUpdateMessage.toObject(m.pollUpdateMessage, o);
                if (o.oneofs)
                    d._pollUpdateMessage = "pollUpdateMessage";
            }
            if (m.keepInChatMessage != null && m.hasOwnProperty("keepInChatMessage")) {
                d.keepInChatMessage = WAE2E.KeepInChatMessage.toObject(m.keepInChatMessage, o);
                if (o.oneofs)
                    d._keepInChatMessage = "keepInChatMessage";
            }
            if (m.documentWithCaptionMessage != null && m.hasOwnProperty("documentWithCaptionMessage")) {
                d.documentWithCaptionMessage = WAE2E.FutureProofMessage.toObject(m.documentWithCaptionMessage, o);
                if (o.oneofs)
                    d._documentWithCaptionMessage = "documentWithCaptionMessage";
            }
            if (m.requestPhoneNumberMessage != null && m.hasOwnProperty("requestPhoneNumberMessage")) {
                d.requestPhoneNumberMessage = WAE2E.RequestPhoneNumberMessage.toObject(m.requestPhoneNumberMessage, o);
                if (o.oneofs)
                    d._requestPhoneNumberMessage = "requestPhoneNumberMessage";
            }
            if (m.viewOnceMessageV2 != null && m.hasOwnProperty("viewOnceMessageV2")) {
                d.viewOnceMessageV2 = WAE2E.FutureProofMessage.toObject(m.viewOnceMessageV2, o);
                if (o.oneofs)
                    d._viewOnceMessageV2 = "viewOnceMessageV2";
            }
            if (m.encReactionMessage != null && m.hasOwnProperty("encReactionMessage")) {
                d.encReactionMessage = WAE2E.EncReactionMessage.toObject(m.encReactionMessage, o);
                if (o.oneofs)
                    d._encReactionMessage = "encReactionMessage";
            }
            if (m.editedMessage != null && m.hasOwnProperty("editedMessage")) {
                d.editedMessage = WAE2E.FutureProofMessage.toObject(m.editedMessage, o);
                if (o.oneofs)
                    d._editedMessage = "editedMessage";
            }
            if (m.viewOnceMessageV2Extension != null && m.hasOwnProperty("viewOnceMessageV2Extension")) {
                d.viewOnceMessageV2Extension = WAE2E.FutureProofMessage.toObject(m.viewOnceMessageV2Extension, o);
                if (o.oneofs)
                    d._viewOnceMessageV2Extension = "viewOnceMessageV2Extension";
            }
            if (m.pollCreationMessageV2 != null && m.hasOwnProperty("pollCreationMessageV2")) {
                d.pollCreationMessageV2 = WAE2E.PollCreationMessage.toObject(m.pollCreationMessageV2, o);
                if (o.oneofs)
                    d._pollCreationMessageV2 = "pollCreationMessageV2";
            }
            if (m.scheduledCallCreationMessage != null && m.hasOwnProperty("scheduledCallCreationMessage")) {
                d.scheduledCallCreationMessage = WAE2E.ScheduledCallCreationMessage.toObject(m.scheduledCallCreationMessage, o);
                if (o.oneofs)
                    d._scheduledCallCreationMessage = "scheduledCallCreationMessage";
            }
            if (m.groupMentionedMessage != null && m.hasOwnProperty("groupMentionedMessage")) {
                d.groupMentionedMessage = WAE2E.FutureProofMessage.toObject(m.groupMentionedMessage, o);
                if (o.oneofs)
                    d._groupMentionedMessage = "groupMentionedMessage";
            }
            if (m.pinInChatMessage != null && m.hasOwnProperty("pinInChatMessage")) {
                d.pinInChatMessage = WAE2E.PinInChatMessage.toObject(m.pinInChatMessage, o);
                if (o.oneofs)
                    d._pinInChatMessage = "pinInChatMessage";
            }
            if (m.pollCreationMessageV3 != null && m.hasOwnProperty("pollCreationMessageV3")) {
                d.pollCreationMessageV3 = WAE2E.PollCreationMessage.toObject(m.pollCreationMessageV3, o);
                if (o.oneofs)
                    d._pollCreationMessageV3 = "pollCreationMessageV3";
            }
            if (m.scheduledCallEditMessage != null && m.hasOwnProperty("scheduledCallEditMessage")) {
                d.scheduledCallEditMessage = WAE2E.ScheduledCallEditMessage.toObject(m.scheduledCallEditMessage, o);
                if (o.oneofs)
                    d._scheduledCallEditMessage = "scheduledCallEditMessage";
            }
            if (m.ptvMessage != null && m.hasOwnProperty("ptvMessage")) {
                d.ptvMessage = WAE2E.VideoMessage.toObject(m.ptvMessage, o);
                if (o.oneofs)
                    d._ptvMessage = "ptvMessage";
            }
            if (m.botInvokeMessage != null && m.hasOwnProperty("botInvokeMessage")) {
                d.botInvokeMessage = WAE2E.FutureProofMessage.toObject(m.botInvokeMessage, o);
                if (o.oneofs)
                    d._botInvokeMessage = "botInvokeMessage";
            }
            if (m.callLogMesssage != null && m.hasOwnProperty("callLogMesssage")) {
                d.callLogMesssage = WAE2E.CallLogMessage.toObject(m.callLogMesssage, o);
                if (o.oneofs)
                    d._callLogMesssage = "callLogMesssage";
            }
            if (m.messageHistoryBundle != null && m.hasOwnProperty("messageHistoryBundle")) {
                d.messageHistoryBundle = WAE2E.MessageHistoryBundle.toObject(m.messageHistoryBundle, o);
                if (o.oneofs)
                    d._messageHistoryBundle = "messageHistoryBundle";
            }
            if (m.encCommentMessage != null && m.hasOwnProperty("encCommentMessage")) {
                d.encCommentMessage = WAE2E.EncCommentMessage.toObject(m.encCommentMessage, o);
                if (o.oneofs)
                    d._encCommentMessage = "encCommentMessage";
            }
            if (m.bcallMessage != null && m.hasOwnProperty("bcallMessage")) {
                d.bcallMessage = WAE2E.BCallMessage.toObject(m.bcallMessage, o);
                if (o.oneofs)
                    d._bcallMessage = "bcallMessage";
            }
            if (m.lottieStickerMessage != null && m.hasOwnProperty("lottieStickerMessage")) {
                d.lottieStickerMessage = WAE2E.FutureProofMessage.toObject(m.lottieStickerMessage, o);
                if (o.oneofs)
                    d._lottieStickerMessage = "lottieStickerMessage";
            }
            if (m.eventMessage != null && m.hasOwnProperty("eventMessage")) {
                d.eventMessage = WAE2E.EventMessage.toObject(m.eventMessage, o);
                if (o.oneofs)
                    d._eventMessage = "eventMessage";
            }
            if (m.encEventResponseMessage != null && m.hasOwnProperty("encEventResponseMessage")) {
                d.encEventResponseMessage = WAE2E.EncEventResponseMessage.toObject(m.encEventResponseMessage, o);
                if (o.oneofs)
                    d._encEventResponseMessage = "encEventResponseMessage";
            }
            if (m.commentMessage != null && m.hasOwnProperty("commentMessage")) {
                d.commentMessage = WAE2E.CommentMessage.toObject(m.commentMessage, o);
                if (o.oneofs)
                    d._commentMessage = "commentMessage";
            }
            if (m.newsletterAdminInviteMessage != null && m.hasOwnProperty("newsletterAdminInviteMessage")) {
                d.newsletterAdminInviteMessage = WAE2E.NewsletterAdminInviteMessage.toObject(m.newsletterAdminInviteMessage, o);
                if (o.oneofs)
                    d._newsletterAdminInviteMessage = "newsletterAdminInviteMessage";
            }
            if (m.placeholderMessage != null && m.hasOwnProperty("placeholderMessage")) {
                d.placeholderMessage = WAE2E.PlaceholderMessage.toObject(m.placeholderMessage, o);
                if (o.oneofs)
                    d._placeholderMessage = "placeholderMessage";
            }
            if (m.secretEncryptedMessage != null && m.hasOwnProperty("secretEncryptedMessage")) {
                d.secretEncryptedMessage = WAE2E.SecretEncryptedMessage.toObject(m.secretEncryptedMessage, o);
                if (o.oneofs)
                    d._secretEncryptedMessage = "secretEncryptedMessage";
            }
            if (m.albumMessage != null && m.hasOwnProperty("albumMessage")) {
                d.albumMessage = WAE2E.AlbumMessage.toObject(m.albumMessage, o);
                if (o.oneofs)
                    d._albumMessage = "albumMessage";
            }
            if (m.eventCoverImage != null && m.hasOwnProperty("eventCoverImage")) {
                d.eventCoverImage = WAE2E.FutureProofMessage.toObject(m.eventCoverImage, o);
                if (o.oneofs)
                    d._eventCoverImage = "eventCoverImage";
            }
            if (m.stickerPackMessage != null && m.hasOwnProperty("stickerPackMessage")) {
                d.stickerPackMessage = WAE2E.StickerPackMessage.toObject(m.stickerPackMessage, o);
                if (o.oneofs)
                    d._stickerPackMessage = "stickerPackMessage";
            }
            if (m.statusMentionMessage != null && m.hasOwnProperty("statusMentionMessage")) {
                d.statusMentionMessage = WAE2E.FutureProofMessage.toObject(m.statusMentionMessage, o);
                if (o.oneofs)
                    d._statusMentionMessage = "statusMentionMessage";
            }
            if (m.pollResultSnapshotMessage != null && m.hasOwnProperty("pollResultSnapshotMessage")) {
                d.pollResultSnapshotMessage = WAE2E.PollResultSnapshotMessage.toObject(m.pollResultSnapshotMessage, o);
                if (o.oneofs)
                    d._pollResultSnapshotMessage = "pollResultSnapshotMessage";
            }
            if (m.pollCreationOptionImageMessage != null && m.hasOwnProperty("pollCreationOptionImageMessage")) {
                d.pollCreationOptionImageMessage = WAE2E.FutureProofMessage.toObject(m.pollCreationOptionImageMessage, o);
                if (o.oneofs)
                    d._pollCreationOptionImageMessage = "pollCreationOptionImageMessage";
            }
            if (m.associatedChildMessage != null && m.hasOwnProperty("associatedChildMessage")) {
                d.associatedChildMessage = WAE2E.FutureProofMessage.toObject(m.associatedChildMessage, o);
                if (o.oneofs)
                    d._associatedChildMessage = "associatedChildMessage";
            }
            if (m.groupStatusMentionMessage != null && m.hasOwnProperty("groupStatusMentionMessage")) {
                d.groupStatusMentionMessage = WAE2E.FutureProofMessage.toObject(m.groupStatusMentionMessage, o);
                if (o.oneofs)
                    d._groupStatusMentionMessage = "groupStatusMentionMessage";
            }
            if (m.pollCreationMessageV4 != null && m.hasOwnProperty("pollCreationMessageV4")) {
                d.pollCreationMessageV4 = WAE2E.FutureProofMessage.toObject(m.pollCreationMessageV4, o);
                if (o.oneofs)
                    d._pollCreationMessageV4 = "pollCreationMessageV4";
            }
            if (m.statusAddYours != null && m.hasOwnProperty("statusAddYours")) {
                d.statusAddYours = WAE2E.FutureProofMessage.toObject(m.statusAddYours, o);
                if (o.oneofs)
                    d._statusAddYours = "statusAddYours";
            }
            if (m.groupStatusMessage != null && m.hasOwnProperty("groupStatusMessage")) {
                d.groupStatusMessage = WAE2E.FutureProofMessage.toObject(m.groupStatusMessage, o);
                if (o.oneofs)
                    d._groupStatusMessage = "groupStatusMessage";
            }
            if (m.richResponseMessage != null && m.hasOwnProperty("richResponseMessage")) {
                d.richResponseMessage = WAE2E.AIRichResponseMessage.toObject(m.richResponseMessage, o);
                if (o.oneofs)
                    d._richResponseMessage = "richResponseMessage";
            }
            if (m.statusNotificationMessage != null && m.hasOwnProperty("statusNotificationMessage")) {
                d.statusNotificationMessage = WAE2E.StatusNotificationMessage.toObject(m.statusNotificationMessage, o);
                if (o.oneofs)
                    d._statusNotificationMessage = "statusNotificationMessage";
            }
            if (m.limitSharingMessage != null && m.hasOwnProperty("limitSharingMessage")) {
                d.limitSharingMessage = WAE2E.FutureProofMessage.toObject(m.limitSharingMessage, o);
                if (o.oneofs)
                    d._limitSharingMessage = "limitSharingMessage";
            }
            if (m.botTaskMessage != null && m.hasOwnProperty("botTaskMessage")) {
                d.botTaskMessage = WAE2E.FutureProofMessage.toObject(m.botTaskMessage, o);
                if (o.oneofs)
                    d._botTaskMessage = "botTaskMessage";
            }
            if (m.questionMessage != null && m.hasOwnProperty("questionMessage")) {
                d.questionMessage = WAE2E.FutureProofMessage.toObject(m.questionMessage, o);
                if (o.oneofs)
                    d._questionMessage = "questionMessage";
            }
            if (m.messageHistoryNotice != null && m.hasOwnProperty("messageHistoryNotice")) {
                d.messageHistoryNotice = WAE2E.MessageHistoryNotice.toObject(m.messageHistoryNotice, o);
                if (o.oneofs)
                    d._messageHistoryNotice = "messageHistoryNotice";
            }
            if (m.groupStatusMessageV2 != null && m.hasOwnProperty("groupStatusMessageV2")) {
                d.groupStatusMessageV2 = WAE2E.FutureProofMessage.toObject(m.groupStatusMessageV2, o);
                if (o.oneofs)
                    d._groupStatusMessageV2 = "groupStatusMessageV2";
            }
            if (m.botForwardedMessage != null && m.hasOwnProperty("botForwardedMessage")) {
                d.botForwardedMessage = WAE2E.FutureProofMessage.toObject(m.botForwardedMessage, o);
                if (o.oneofs)
                    d._botForwardedMessage = "botForwardedMessage";
            }
            if (m.statusQuestionAnswerMessage != null && m.hasOwnProperty("statusQuestionAnswerMessage")) {
                d.statusQuestionAnswerMessage = WAE2E.StatusQuestionAnswerMessage.toObject(m.statusQuestionAnswerMessage, o);
                if (o.oneofs)
                    d._statusQuestionAnswerMessage = "statusQuestionAnswerMessage";
            }
            if (m.questionReplyMessage != null && m.hasOwnProperty("questionReplyMessage")) {
                d.questionReplyMessage = WAE2E.FutureProofMessage.toObject(m.questionReplyMessage, o);
                if (o.oneofs)
                    d._questionReplyMessage = "questionReplyMessage";
            }
            if (m.questionResponseMessage != null && m.hasOwnProperty("questionResponseMessage")) {
                d.questionResponseMessage = WAE2E.QuestionResponseMessage.toObject(m.questionResponseMessage, o);
                if (o.oneofs)
                    d._questionResponseMessage = "questionResponseMessage";
            }
            if (m.statusQuotedMessage != null && m.hasOwnProperty("statusQuotedMessage")) {
                d.statusQuotedMessage = WAE2E.StatusQuotedMessage.toObject(m.statusQuotedMessage, o);
                if (o.oneofs)
                    d._statusQuotedMessage = "statusQuotedMessage";
            }
            if (m.statusStickerInteractionMessage != null && m.hasOwnProperty("statusStickerInteractionMessage")) {
                d.statusStickerInteractionMessage = WAE2E.StatusStickerInteractionMessage.toObject(m.statusStickerInteractionMessage, o);
                if (o.oneofs)
                    d._statusStickerInteractionMessage = "statusStickerInteractionMessage";
            }
            if (m.pollCreationMessageV5 != null && m.hasOwnProperty("pollCreationMessageV5")) {
                d.pollCreationMessageV5 = WAE2E.PollCreationMessage.toObject(m.pollCreationMessageV5, o);
                if (o.oneofs)
                    d._pollCreationMessageV5 = "pollCreationMessageV5";
            }
            if (m.newsletterFollowerInviteMessageV2 != null && m.hasOwnProperty("newsletterFollowerInviteMessageV2")) {
                d.newsletterFollowerInviteMessageV2 = WAE2E.NewsletterFollowerInviteMessage.toObject(m.newsletterFollowerInviteMessageV2, o);
                if (o.oneofs)
                    d._newsletterFollowerInviteMessageV2 = "newsletterFollowerInviteMessageV2";
            }
            if (m.pollResultSnapshotMessageV3 != null && m.hasOwnProperty("pollResultSnapshotMessageV3")) {
                d.pollResultSnapshotMessageV3 = WAE2E.PollResultSnapshotMessage.toObject(m.pollResultSnapshotMessageV3, o);
                if (o.oneofs)
                    d._pollResultSnapshotMessageV3 = "pollResultSnapshotMessageV3";
            }
            if (m.newsletterAdminProfileMessage != null && m.hasOwnProperty("newsletterAdminProfileMessage")) {
                d.newsletterAdminProfileMessage = WAE2E.FutureProofMessage.toObject(m.newsletterAdminProfileMessage, o);
                if (o.oneofs)
                    d._newsletterAdminProfileMessage = "newsletterAdminProfileMessage";
            }
            if (m.newsletterAdminProfileMessageV2 != null && m.hasOwnProperty("newsletterAdminProfileMessageV2")) {
                d.newsletterAdminProfileMessageV2 = WAE2E.FutureProofMessage.toObject(m.newsletterAdminProfileMessageV2, o);
                if (o.oneofs)
                    d._newsletterAdminProfileMessageV2 = "newsletterAdminProfileMessageV2";
            }
            if (m.spoilerMessage != null && m.hasOwnProperty("spoilerMessage")) {
                d.spoilerMessage = WAE2E.FutureProofMessage.toObject(m.spoilerMessage, o);
                if (o.oneofs)
                    d._spoilerMessage = "spoilerMessage";
            }
            if (m.pollCreationMessageV6 != null && m.hasOwnProperty("pollCreationMessageV6")) {
                d.pollCreationMessageV6 = WAE2E.PollCreationMessage.toObject(m.pollCreationMessageV6, o);
                if (o.oneofs)
                    d._pollCreationMessageV6 = "pollCreationMessageV6";
            }
            if (m.conditionalRevealMessage != null && m.hasOwnProperty("conditionalRevealMessage")) {
                d.conditionalRevealMessage = WAE2E.ConditionalRevealMessage.toObject(m.conditionalRevealMessage, o);
                if (o.oneofs)
                    d._conditionalRevealMessage = "conditionalRevealMessage";
            }
            if (m.pollAddOptionMessage != null && m.hasOwnProperty("pollAddOptionMessage")) {
                d.pollAddOptionMessage = WAE2E.PollAddOptionMessage.toObject(m.pollAddOptionMessage, o);
                if (o.oneofs)
                    d._pollAddOptionMessage = "pollAddOptionMessage";
            }
            if (m.eventInviteMessage != null && m.hasOwnProperty("eventInviteMessage")) {
                d.eventInviteMessage = WAE2E.EventInviteMessage.toObject(m.eventInviteMessage, o);
                if (o.oneofs)
                    d._eventInviteMessage = "eventInviteMessage";
            }
            if (m.groupRootKeyShare != null && m.hasOwnProperty("groupRootKeyShare")) {
                d.groupRootKeyShare = WAE2E.GroupRootKeyShare.toObject(m.groupRootKeyShare, o);
                if (o.oneofs)
                    d._groupRootKeyShare = "groupRootKeyShare";
            }
            if (m.p2PPaymentReminderNotification != null && m.hasOwnProperty("p2PPaymentReminderNotification")) {
                d.p2PPaymentReminderNotification = WAE2E.P2PPaymentReminderNotification.toObject(m.p2PPaymentReminderNotification, o);
                if (o.oneofs)
                    d._p2PPaymentReminderNotification = "p2PPaymentReminderNotification";
            }
            if (m.splitPaymentMessage != null && m.hasOwnProperty("splitPaymentMessage")) {
                d.splitPaymentMessage = WAE2E.SplitPaymentMessage.toObject(m.splitPaymentMessage, o);
                if (o.oneofs)
                    d._splitPaymentMessage = "splitPaymentMessage";
            }
            return d;
        };

        Message.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Message.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Message";
        };

        return Message;
    })();

    WAE2E.AlbumMessage = (function() {

        function AlbumMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AlbumMessage.prototype.expectedImageCount = null;
        AlbumMessage.prototype.expectedVideoCount = null;
        AlbumMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AlbumMessage.prototype, "_expectedImageCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedImageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AlbumMessage.prototype, "_expectedVideoCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedVideoCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AlbumMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AlbumMessage.create = function create(properties) {
            return new AlbumMessage(properties);
        };

        AlbumMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.expectedImageCount != null && Object.hasOwnProperty.call(m, "expectedImageCount"))
                w.uint32(16).uint32(m.expectedImageCount);
            if (m.expectedVideoCount != null && Object.hasOwnProperty.call(m, "expectedVideoCount"))
                w.uint32(24).uint32(m.expectedVideoCount);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        AlbumMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AlbumMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.expectedImageCount = r.uint32();
                        break;
                    }
                case 3: {
                        m.expectedVideoCount = r.uint32();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AlbumMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AlbumMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AlbumMessage();
            if (d.expectedImageCount != null) {
                m.expectedImageCount = d.expectedImageCount >>> 0;
            }
            if (d.expectedVideoCount != null) {
                m.expectedVideoCount = d.expectedVideoCount >>> 0;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.AlbumMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        AlbumMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.expectedImageCount != null && m.hasOwnProperty("expectedImageCount")) {
                d.expectedImageCount = m.expectedImageCount;
                if (o.oneofs)
                    d._expectedImageCount = "expectedImageCount";
            }
            if (m.expectedVideoCount != null && m.hasOwnProperty("expectedVideoCount")) {
                d.expectedVideoCount = m.expectedVideoCount;
                if (o.oneofs)
                    d._expectedVideoCount = "expectedVideoCount";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        AlbumMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AlbumMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AlbumMessage";
        };

        return AlbumMessage;
    })();

    WAE2E.MessageHistoryMetadata = (function() {

        function MessageHistoryMetadata(p) {
            this.historyReceivers = [];
            this.nonHistoryReceivers = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageHistoryMetadata.prototype.historyReceivers = $util.emptyArray;
        MessageHistoryMetadata.prototype.oldestMessageTimestampInWindow = null;
        MessageHistoryMetadata.prototype.messageCount = null;
        MessageHistoryMetadata.prototype.nonHistoryReceivers = $util.emptyArray;
        MessageHistoryMetadata.prototype.oldestMessageTimestampInBundle = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInWindow", {
            get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInWindow"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryMetadata.prototype, "_messageCount", {
            get: $util.oneOfGetter($oneOfFields = ["messageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInBundle", {
            get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageHistoryMetadata.create = function create(properties) {
            return new MessageHistoryMetadata(properties);
        };

        MessageHistoryMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.historyReceivers != null && m.historyReceivers.length) {
                for (var i = 0; i < m.historyReceivers.length; ++i)
                    w.uint32(10).string(m.historyReceivers[i]);
            }
            if (m.oldestMessageTimestampInWindow != null && Object.hasOwnProperty.call(m, "oldestMessageTimestampInWindow"))
                w.uint32(16).int64(m.oldestMessageTimestampInWindow);
            if (m.messageCount != null && Object.hasOwnProperty.call(m, "messageCount"))
                w.uint32(24).int64(m.messageCount);
            if (m.nonHistoryReceivers != null && m.nonHistoryReceivers.length) {
                for (var i = 0; i < m.nonHistoryReceivers.length; ++i)
                    w.uint32(34).string(m.nonHistoryReceivers[i]);
            }
            if (m.oldestMessageTimestampInBundle != null && Object.hasOwnProperty.call(m, "oldestMessageTimestampInBundle"))
                w.uint32(40).int64(m.oldestMessageTimestampInBundle);
            return w;
        };

        MessageHistoryMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageHistoryMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.historyReceivers && m.historyReceivers.length))
                            m.historyReceivers = [];
                        m.historyReceivers.push(r.string());
                        break;
                    }
                case 2: {
                        m.oldestMessageTimestampInWindow = r.int64();
                        break;
                    }
                case 3: {
                        m.messageCount = r.int64();
                        break;
                    }
                case 4: {
                        if (!(m.nonHistoryReceivers && m.nonHistoryReceivers.length))
                            m.nonHistoryReceivers = [];
                        m.nonHistoryReceivers.push(r.string());
                        break;
                    }
                case 5: {
                        m.oldestMessageTimestampInBundle = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageHistoryMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageHistoryMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageHistoryMetadata();
            if (d.historyReceivers) {
                if (!Array.isArray(d.historyReceivers))
                    throw TypeError(".WAE2E.MessageHistoryMetadata.historyReceivers: array expected");
                m.historyReceivers = [];
                for (var i = 0; i < d.historyReceivers.length; ++i) {
                    m.historyReceivers[i] = String(d.historyReceivers[i]);
                }
            }
            if (d.oldestMessageTimestampInWindow != null) {
                if ($util.Long)
                    (m.oldestMessageTimestampInWindow = $util.Long.fromValue(d.oldestMessageTimestampInWindow)).unsigned = false;
                else if (typeof d.oldestMessageTimestampInWindow === "string")
                    m.oldestMessageTimestampInWindow = parseInt(d.oldestMessageTimestampInWindow, 10);
                else if (typeof d.oldestMessageTimestampInWindow === "number")
                    m.oldestMessageTimestampInWindow = d.oldestMessageTimestampInWindow;
                else if (typeof d.oldestMessageTimestampInWindow === "object")
                    m.oldestMessageTimestampInWindow = new $util.LongBits(d.oldestMessageTimestampInWindow.low >>> 0, d.oldestMessageTimestampInWindow.high >>> 0).toNumber();
            }
            if (d.messageCount != null) {
                if ($util.Long)
                    (m.messageCount = $util.Long.fromValue(d.messageCount)).unsigned = false;
                else if (typeof d.messageCount === "string")
                    m.messageCount = parseInt(d.messageCount, 10);
                else if (typeof d.messageCount === "number")
                    m.messageCount = d.messageCount;
                else if (typeof d.messageCount === "object")
                    m.messageCount = new $util.LongBits(d.messageCount.low >>> 0, d.messageCount.high >>> 0).toNumber();
            }
            if (d.nonHistoryReceivers) {
                if (!Array.isArray(d.nonHistoryReceivers))
                    throw TypeError(".WAE2E.MessageHistoryMetadata.nonHistoryReceivers: array expected");
                m.nonHistoryReceivers = [];
                for (var i = 0; i < d.nonHistoryReceivers.length; ++i) {
                    m.nonHistoryReceivers[i] = String(d.nonHistoryReceivers[i]);
                }
            }
            if (d.oldestMessageTimestampInBundle != null) {
                if ($util.Long)
                    (m.oldestMessageTimestampInBundle = $util.Long.fromValue(d.oldestMessageTimestampInBundle)).unsigned = false;
                else if (typeof d.oldestMessageTimestampInBundle === "string")
                    m.oldestMessageTimestampInBundle = parseInt(d.oldestMessageTimestampInBundle, 10);
                else if (typeof d.oldestMessageTimestampInBundle === "number")
                    m.oldestMessageTimestampInBundle = d.oldestMessageTimestampInBundle;
                else if (typeof d.oldestMessageTimestampInBundle === "object")
                    m.oldestMessageTimestampInBundle = new $util.LongBits(d.oldestMessageTimestampInBundle.low >>> 0, d.oldestMessageTimestampInBundle.high >>> 0).toNumber();
            }
            return m;
        };

        MessageHistoryMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.historyReceivers = [];
                d.nonHistoryReceivers = [];
            }
            if (m.historyReceivers && m.historyReceivers.length) {
                d.historyReceivers = [];
                for (var j = 0; j < m.historyReceivers.length; ++j) {
                    d.historyReceivers[j] = m.historyReceivers[j];
                }
            }
            if (m.oldestMessageTimestampInWindow != null && m.hasOwnProperty("oldestMessageTimestampInWindow")) {
                if (typeof m.oldestMessageTimestampInWindow === "number")
                    d.oldestMessageTimestampInWindow = o.longs === String ? String(m.oldestMessageTimestampInWindow) : m.oldestMessageTimestampInWindow;
                else
                    d.oldestMessageTimestampInWindow = o.longs === String ? $util.Long.prototype.toString.call(m.oldestMessageTimestampInWindow) : o.longs === Number ? new $util.LongBits(m.oldestMessageTimestampInWindow.low >>> 0, m.oldestMessageTimestampInWindow.high >>> 0).toNumber() : m.oldestMessageTimestampInWindow;
                if (o.oneofs)
                    d._oldestMessageTimestampInWindow = "oldestMessageTimestampInWindow";
            }
            if (m.messageCount != null && m.hasOwnProperty("messageCount")) {
                if (typeof m.messageCount === "number")
                    d.messageCount = o.longs === String ? String(m.messageCount) : m.messageCount;
                else
                    d.messageCount = o.longs === String ? $util.Long.prototype.toString.call(m.messageCount) : o.longs === Number ? new $util.LongBits(m.messageCount.low >>> 0, m.messageCount.high >>> 0).toNumber() : m.messageCount;
                if (o.oneofs)
                    d._messageCount = "messageCount";
            }
            if (m.nonHistoryReceivers && m.nonHistoryReceivers.length) {
                d.nonHistoryReceivers = [];
                for (var j = 0; j < m.nonHistoryReceivers.length; ++j) {
                    d.nonHistoryReceivers[j] = m.nonHistoryReceivers[j];
                }
            }
            if (m.oldestMessageTimestampInBundle != null && m.hasOwnProperty("oldestMessageTimestampInBundle")) {
                if (typeof m.oldestMessageTimestampInBundle === "number")
                    d.oldestMessageTimestampInBundle = o.longs === String ? String(m.oldestMessageTimestampInBundle) : m.oldestMessageTimestampInBundle;
                else
                    d.oldestMessageTimestampInBundle = o.longs === String ? $util.Long.prototype.toString.call(m.oldestMessageTimestampInBundle) : o.longs === Number ? new $util.LongBits(m.oldestMessageTimestampInBundle.low >>> 0, m.oldestMessageTimestampInBundle.high >>> 0).toNumber() : m.oldestMessageTimestampInBundle;
                if (o.oneofs)
                    d._oldestMessageTimestampInBundle = "oldestMessageTimestampInBundle";
            }
            return d;
        };

        MessageHistoryMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageHistoryMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageHistoryMetadata";
        };

        return MessageHistoryMetadata;
    })();

    WAE2E.MessageHistoryNotice = (function() {

        function MessageHistoryNotice(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageHistoryNotice.prototype.contextInfo = null;
        MessageHistoryNotice.prototype.messageHistoryMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryNotice.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryNotice.prototype, "_messageHistoryMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageHistoryNotice.create = function create(properties) {
            return new MessageHistoryNotice(properties);
        };

        MessageHistoryNotice.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(10).fork()).ldelim();
            if (m.messageHistoryMetadata != null && Object.hasOwnProperty.call(m, "messageHistoryMetadata"))
                WAE2E.MessageHistoryMetadata.encode(m.messageHistoryMetadata, w.uint32(18).fork()).ldelim();
            return w;
        };

        MessageHistoryNotice.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageHistoryNotice();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageHistoryNotice.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageHistoryNotice)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageHistoryNotice();
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.MessageHistoryNotice.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.messageHistoryMetadata != null) {
                if (typeof d.messageHistoryMetadata !== "object")
                    throw TypeError(".WAE2E.MessageHistoryNotice.messageHistoryMetadata: object expected");
                m.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.fromObject(d.messageHistoryMetadata, n + 1);
            }
            return m;
        };

        MessageHistoryNotice.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.messageHistoryMetadata != null && m.hasOwnProperty("messageHistoryMetadata")) {
                d.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.toObject(m.messageHistoryMetadata, o);
                if (o.oneofs)
                    d._messageHistoryMetadata = "messageHistoryMetadata";
            }
            return d;
        };

        MessageHistoryNotice.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageHistoryNotice.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageHistoryNotice";
        };

        return MessageHistoryNotice;
    })();

    WAE2E.MessageHistoryBundle = (function() {

        function MessageHistoryBundle(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageHistoryBundle.prototype.mimetype = null;
        MessageHistoryBundle.prototype.fileSHA256 = null;
        MessageHistoryBundle.prototype.mediaKey = null;
        MessageHistoryBundle.prototype.fileEncSHA256 = null;
        MessageHistoryBundle.prototype.directPath = null;
        MessageHistoryBundle.prototype.mediaKeyTimestamp = null;
        MessageHistoryBundle.prototype.contextInfo = null;
        MessageHistoryBundle.prototype.messageHistoryMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageHistoryBundle.prototype, "_messageHistoryMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageHistoryBundle.create = function create(properties) {
            return new MessageHistoryBundle(properties);
        };

        MessageHistoryBundle.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(10).string(m.mimetype);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(18).bytes(m.fileSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(26).bytes(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(34).bytes(m.fileEncSHA256);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(42).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(48).int64(m.mediaKeyTimestamp);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(58).fork()).ldelim();
            if (m.messageHistoryMetadata != null && Object.hasOwnProperty.call(m, "messageHistoryMetadata"))
                WAE2E.MessageHistoryMetadata.encode(m.messageHistoryMetadata, w.uint32(66).fork()).ldelim();
            return w;
        };

        MessageHistoryBundle.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageHistoryBundle();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.mimetype = r.string();
                        break;
                    }
                case 2: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 3: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 4: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 5: {
                        m.directPath = r.string();
                        break;
                    }
                case 6: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 7: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageHistoryBundle.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageHistoryBundle)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageHistoryBundle();
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.MessageHistoryBundle.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.messageHistoryMetadata != null) {
                if (typeof d.messageHistoryMetadata !== "object")
                    throw TypeError(".WAE2E.MessageHistoryBundle.messageHistoryMetadata: object expected");
                m.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.fromObject(d.messageHistoryMetadata, n + 1);
            }
            return m;
        };

        MessageHistoryBundle.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.messageHistoryMetadata != null && m.hasOwnProperty("messageHistoryMetadata")) {
                d.messageHistoryMetadata = WAE2E.MessageHistoryMetadata.toObject(m.messageHistoryMetadata, o);
                if (o.oneofs)
                    d._messageHistoryMetadata = "messageHistoryMetadata";
            }
            return d;
        };

        MessageHistoryBundle.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageHistoryBundle.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageHistoryBundle";
        };

        return MessageHistoryBundle;
    })();

    WAE2E.EncEventResponseMessage = (function() {

        function EncEventResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EncEventResponseMessage.prototype.eventCreationMessageKey = null;
        EncEventResponseMessage.prototype.encPayload = null;
        EncEventResponseMessage.prototype.encIV = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncEventResponseMessage.prototype, "_eventCreationMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["eventCreationMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncEventResponseMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncEventResponseMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncEventResponseMessage.create = function create(properties) {
            return new EncEventResponseMessage(properties);
        };

        EncEventResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.eventCreationMessageKey != null && Object.hasOwnProperty.call(m, "eventCreationMessageKey"))
                WACommon.MessageKey.encode(m.eventCreationMessageKey, w.uint32(10).fork()).ldelim();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(18).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(26).bytes(m.encIV);
            return w;
        };

        EncEventResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EncEventResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.eventCreationMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 3: {
                        m.encIV = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EncEventResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EncEventResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EncEventResponseMessage();
            if (d.eventCreationMessageKey != null) {
                if (typeof d.eventCreationMessageKey !== "object")
                    throw TypeError(".WAE2E.EncEventResponseMessage.eventCreationMessageKey: object expected");
                m.eventCreationMessageKey = WACommon.MessageKey.fromObject(d.eventCreationMessageKey, n + 1);
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            return m;
        };

        EncEventResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.eventCreationMessageKey != null && m.hasOwnProperty("eventCreationMessageKey")) {
                d.eventCreationMessageKey = WACommon.MessageKey.toObject(m.eventCreationMessageKey, o);
                if (o.oneofs)
                    d._eventCreationMessageKey = "eventCreationMessageKey";
            }
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            return d;
        };

        EncEventResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncEventResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EncEventResponseMessage";
        };

        return EncEventResponseMessage;
    })();

    WAE2E.EventMessage = (function() {

        function EventMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EventMessage.prototype.contextInfo = null;
        EventMessage.prototype.isCanceled = null;
        EventMessage.prototype.name = null;
        EventMessage.prototype.description = null;
        EventMessage.prototype.location = null;
        EventMessage.prototype.joinLink = null;
        EventMessage.prototype.startTime = null;
        EventMessage.prototype.endTime = null;
        EventMessage.prototype.extraGuestsAllowed = null;
        EventMessage.prototype.isScheduleCall = null;
        EventMessage.prototype.hasReminder = null;
        EventMessage.prototype.reminderOffsetSec = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_isCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_location", {
            get: $util.oneOfGetter($oneOfFields = ["location"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_joinLink", {
            get: $util.oneOfGetter($oneOfFields = ["joinLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_startTime", {
            get: $util.oneOfGetter($oneOfFields = ["startTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_endTime", {
            get: $util.oneOfGetter($oneOfFields = ["endTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_extraGuestsAllowed", {
            get: $util.oneOfGetter($oneOfFields = ["extraGuestsAllowed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_isScheduleCall", {
            get: $util.oneOfGetter($oneOfFields = ["isScheduleCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_hasReminder", {
            get: $util.oneOfGetter($oneOfFields = ["hasReminder"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventMessage.prototype, "_reminderOffsetSec", {
            get: $util.oneOfGetter($oneOfFields = ["reminderOffsetSec"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventMessage.create = function create(properties) {
            return new EventMessage(properties);
        };

        EventMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(10).fork()).ldelim();
            if (m.isCanceled != null && Object.hasOwnProperty.call(m, "isCanceled"))
                w.uint32(16).bool(m.isCanceled);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(34).string(m.description);
            if (m.location != null && Object.hasOwnProperty.call(m, "location"))
                WAE2E.LocationMessage.encode(m.location, w.uint32(42).fork()).ldelim();
            if (m.joinLink != null && Object.hasOwnProperty.call(m, "joinLink"))
                w.uint32(50).string(m.joinLink);
            if (m.startTime != null && Object.hasOwnProperty.call(m, "startTime"))
                w.uint32(56).int64(m.startTime);
            if (m.endTime != null && Object.hasOwnProperty.call(m, "endTime"))
                w.uint32(64).int64(m.endTime);
            if (m.extraGuestsAllowed != null && Object.hasOwnProperty.call(m, "extraGuestsAllowed"))
                w.uint32(72).bool(m.extraGuestsAllowed);
            if (m.isScheduleCall != null && Object.hasOwnProperty.call(m, "isScheduleCall"))
                w.uint32(80).bool(m.isScheduleCall);
            if (m.hasReminder != null && Object.hasOwnProperty.call(m, "hasReminder"))
                w.uint32(88).bool(m.hasReminder);
            if (m.reminderOffsetSec != null && Object.hasOwnProperty.call(m, "reminderOffsetSec"))
                w.uint32(96).int64(m.reminderOffsetSec);
            return w;
        };

        EventMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EventMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.isCanceled = r.bool();
                        break;
                    }
                case 3: {
                        m.name = r.string();
                        break;
                    }
                case 4: {
                        m.description = r.string();
                        break;
                    }
                case 5: {
                        m.location = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.joinLink = r.string();
                        break;
                    }
                case 7: {
                        m.startTime = r.int64();
                        break;
                    }
                case 8: {
                        m.endTime = r.int64();
                        break;
                    }
                case 9: {
                        m.extraGuestsAllowed = r.bool();
                        break;
                    }
                case 10: {
                        m.isScheduleCall = r.bool();
                        break;
                    }
                case 11: {
                        m.hasReminder = r.bool();
                        break;
                    }
                case 12: {
                        m.reminderOffsetSec = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EventMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EventMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EventMessage();
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.EventMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.isCanceled != null) {
                m.isCanceled = Boolean(d.isCanceled);
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            if (d.location != null) {
                if (typeof d.location !== "object")
                    throw TypeError(".WAE2E.EventMessage.location: object expected");
                m.location = WAE2E.LocationMessage.fromObject(d.location, n + 1);
            }
            if (d.joinLink != null) {
                m.joinLink = String(d.joinLink);
            }
            if (d.startTime != null) {
                if ($util.Long)
                    (m.startTime = $util.Long.fromValue(d.startTime)).unsigned = false;
                else if (typeof d.startTime === "string")
                    m.startTime = parseInt(d.startTime, 10);
                else if (typeof d.startTime === "number")
                    m.startTime = d.startTime;
                else if (typeof d.startTime === "object")
                    m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
            }
            if (d.endTime != null) {
                if ($util.Long)
                    (m.endTime = $util.Long.fromValue(d.endTime)).unsigned = false;
                else if (typeof d.endTime === "string")
                    m.endTime = parseInt(d.endTime, 10);
                else if (typeof d.endTime === "number")
                    m.endTime = d.endTime;
                else if (typeof d.endTime === "object")
                    m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
            }
            if (d.extraGuestsAllowed != null) {
                m.extraGuestsAllowed = Boolean(d.extraGuestsAllowed);
            }
            if (d.isScheduleCall != null) {
                m.isScheduleCall = Boolean(d.isScheduleCall);
            }
            if (d.hasReminder != null) {
                m.hasReminder = Boolean(d.hasReminder);
            }
            if (d.reminderOffsetSec != null) {
                if ($util.Long)
                    (m.reminderOffsetSec = $util.Long.fromValue(d.reminderOffsetSec)).unsigned = false;
                else if (typeof d.reminderOffsetSec === "string")
                    m.reminderOffsetSec = parseInt(d.reminderOffsetSec, 10);
                else if (typeof d.reminderOffsetSec === "number")
                    m.reminderOffsetSec = d.reminderOffsetSec;
                else if (typeof d.reminderOffsetSec === "object")
                    m.reminderOffsetSec = new $util.LongBits(d.reminderOffsetSec.low >>> 0, d.reminderOffsetSec.high >>> 0).toNumber();
            }
            return m;
        };

        EventMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.isCanceled != null && m.hasOwnProperty("isCanceled")) {
                d.isCanceled = m.isCanceled;
                if (o.oneofs)
                    d._isCanceled = "isCanceled";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            if (m.location != null && m.hasOwnProperty("location")) {
                d.location = WAE2E.LocationMessage.toObject(m.location, o);
                if (o.oneofs)
                    d._location = "location";
            }
            if (m.joinLink != null && m.hasOwnProperty("joinLink")) {
                d.joinLink = m.joinLink;
                if (o.oneofs)
                    d._joinLink = "joinLink";
            }
            if (m.startTime != null && m.hasOwnProperty("startTime")) {
                if (typeof m.startTime === "number")
                    d.startTime = o.longs === String ? String(m.startTime) : m.startTime;
                else
                    d.startTime = o.longs === String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
                if (o.oneofs)
                    d._startTime = "startTime";
            }
            if (m.endTime != null && m.hasOwnProperty("endTime")) {
                if (typeof m.endTime === "number")
                    d.endTime = o.longs === String ? String(m.endTime) : m.endTime;
                else
                    d.endTime = o.longs === String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                if (o.oneofs)
                    d._endTime = "endTime";
            }
            if (m.extraGuestsAllowed != null && m.hasOwnProperty("extraGuestsAllowed")) {
                d.extraGuestsAllowed = m.extraGuestsAllowed;
                if (o.oneofs)
                    d._extraGuestsAllowed = "extraGuestsAllowed";
            }
            if (m.isScheduleCall != null && m.hasOwnProperty("isScheduleCall")) {
                d.isScheduleCall = m.isScheduleCall;
                if (o.oneofs)
                    d._isScheduleCall = "isScheduleCall";
            }
            if (m.hasReminder != null && m.hasOwnProperty("hasReminder")) {
                d.hasReminder = m.hasReminder;
                if (o.oneofs)
                    d._hasReminder = "hasReminder";
            }
            if (m.reminderOffsetSec != null && m.hasOwnProperty("reminderOffsetSec")) {
                if (typeof m.reminderOffsetSec === "number")
                    d.reminderOffsetSec = o.longs === String ? String(m.reminderOffsetSec) : m.reminderOffsetSec;
                else
                    d.reminderOffsetSec = o.longs === String ? $util.Long.prototype.toString.call(m.reminderOffsetSec) : o.longs === Number ? new $util.LongBits(m.reminderOffsetSec.low >>> 0, m.reminderOffsetSec.high >>> 0).toNumber() : m.reminderOffsetSec;
                if (o.oneofs)
                    d._reminderOffsetSec = "reminderOffsetSec";
            }
            return d;
        };

        EventMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EventMessage";
        };

        return EventMessage;
    })();

    WAE2E.CommentMessage = (function() {

        function CommentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CommentMessage.prototype.message = null;
        CommentMessage.prototype.targetMessageKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CommentMessage.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CommentMessage.create = function create(properties) {
            return new CommentMessage(properties);
        };

        CommentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(10).fork()).ldelim();
            if (m.targetMessageKey != null && Object.hasOwnProperty.call(m, "targetMessageKey"))
                WACommon.MessageKey.encode(m.targetMessageKey, w.uint32(18).fork()).ldelim();
            return w;
        };

        CommentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CommentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.targetMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CommentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.CommentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.CommentMessage();
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAE2E.CommentMessage.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            if (d.targetMessageKey != null) {
                if (typeof d.targetMessageKey !== "object")
                    throw TypeError(".WAE2E.CommentMessage.targetMessageKey: object expected");
                m.targetMessageKey = WACommon.MessageKey.fromObject(d.targetMessageKey, n + 1);
            }
            return m;
        };

        CommentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.targetMessageKey != null && m.hasOwnProperty("targetMessageKey")) {
                d.targetMessageKey = WACommon.MessageKey.toObject(m.targetMessageKey, o);
                if (o.oneofs)
                    d._targetMessageKey = "targetMessageKey";
            }
            return d;
        };

        CommentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CommentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.CommentMessage";
        };

        return CommentMessage;
    })();

    WAE2E.EncCommentMessage = (function() {

        function EncCommentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EncCommentMessage.prototype.targetMessageKey = null;
        EncCommentMessage.prototype.encPayload = null;
        EncCommentMessage.prototype.encIV = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncCommentMessage.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncCommentMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncCommentMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncCommentMessage.create = function create(properties) {
            return new EncCommentMessage(properties);
        };

        EncCommentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.targetMessageKey != null && Object.hasOwnProperty.call(m, "targetMessageKey"))
                WACommon.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork()).ldelim();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(18).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(26).bytes(m.encIV);
            return w;
        };

        EncCommentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EncCommentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.targetMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 3: {
                        m.encIV = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EncCommentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EncCommentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EncCommentMessage();
            if (d.targetMessageKey != null) {
                if (typeof d.targetMessageKey !== "object")
                    throw TypeError(".WAE2E.EncCommentMessage.targetMessageKey: object expected");
                m.targetMessageKey = WACommon.MessageKey.fromObject(d.targetMessageKey, n + 1);
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            return m;
        };

        EncCommentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.targetMessageKey != null && m.hasOwnProperty("targetMessageKey")) {
                d.targetMessageKey = WACommon.MessageKey.toObject(m.targetMessageKey, o);
                if (o.oneofs)
                    d._targetMessageKey = "targetMessageKey";
            }
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            return d;
        };

        EncCommentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncCommentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EncCommentMessage";
        };

        return EncCommentMessage;
    })();

    WAE2E.EncReactionMessage = (function() {

        function EncReactionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EncReactionMessage.prototype.targetMessageKey = null;
        EncReactionMessage.prototype.encPayload = null;
        EncReactionMessage.prototype.encIV = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncReactionMessage.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncReactionMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncReactionMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncReactionMessage.create = function create(properties) {
            return new EncReactionMessage(properties);
        };

        EncReactionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.targetMessageKey != null && Object.hasOwnProperty.call(m, "targetMessageKey"))
                WACommon.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork()).ldelim();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(18).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(26).bytes(m.encIV);
            return w;
        };

        EncReactionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EncReactionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.targetMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 3: {
                        m.encIV = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EncReactionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EncReactionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EncReactionMessage();
            if (d.targetMessageKey != null) {
                if (typeof d.targetMessageKey !== "object")
                    throw TypeError(".WAE2E.EncReactionMessage.targetMessageKey: object expected");
                m.targetMessageKey = WACommon.MessageKey.fromObject(d.targetMessageKey, n + 1);
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            return m;
        };

        EncReactionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.targetMessageKey != null && m.hasOwnProperty("targetMessageKey")) {
                d.targetMessageKey = WACommon.MessageKey.toObject(m.targetMessageKey, o);
                if (o.oneofs)
                    d._targetMessageKey = "targetMessageKey";
            }
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            return d;
        };

        EncReactionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncReactionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EncReactionMessage";
        };

        return EncReactionMessage;
    })();

    WAE2E.KeepInChatMessage = (function() {

        function KeepInChatMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        KeepInChatMessage.prototype.key = null;
        KeepInChatMessage.prototype.keepType = null;
        KeepInChatMessage.prototype.timestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChatMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChatMessage.prototype, "_keepType", {
            get: $util.oneOfGetter($oneOfFields = ["keepType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(KeepInChatMessage.prototype, "_timestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeepInChatMessage.create = function create(properties) {
            return new KeepInChatMessage(properties);
        };

        KeepInChatMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.keepType != null && Object.hasOwnProperty.call(m, "keepType"))
                w.uint32(16).int32(m.keepType);
            if (m.timestampMS != null && Object.hasOwnProperty.call(m, "timestampMS"))
                w.uint32(24).int64(m.timestampMS);
            return w;
        };

        KeepInChatMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.KeepInChatMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.keepType = r.int32();
                        break;
                    }
                case 3: {
                        m.timestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        KeepInChatMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.KeepInChatMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.KeepInChatMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.KeepInChatMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            switch (d.keepType) {
            default:
                if (typeof d.keepType === "number") {
                    m.keepType = d.keepType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.keepType = 0;
                break;
            case "KEEP_FOR_ALL":
            case 1:
                m.keepType = 1;
                break;
            case "UNDO_KEEP_FOR_ALL":
            case 2:
                m.keepType = 2;
                break;
            }
            if (d.timestampMS != null) {
                if ($util.Long)
                    (m.timestampMS = $util.Long.fromValue(d.timestampMS)).unsigned = false;
                else if (typeof d.timestampMS === "string")
                    m.timestampMS = parseInt(d.timestampMS, 10);
                else if (typeof d.timestampMS === "number")
                    m.timestampMS = d.timestampMS;
                else if (typeof d.timestampMS === "object")
                    m.timestampMS = new $util.LongBits(d.timestampMS.low >>> 0, d.timestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        KeepInChatMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.keepType != null && m.hasOwnProperty("keepType")) {
                d.keepType = o.enums === String ? WAE2E.KeepType[m.keepType] === undefined ? m.keepType : WAE2E.KeepType[m.keepType] : m.keepType;
                if (o.oneofs)
                    d._keepType = "keepType";
            }
            if (m.timestampMS != null && m.hasOwnProperty("timestampMS")) {
                if (typeof m.timestampMS === "number")
                    d.timestampMS = o.longs === String ? String(m.timestampMS) : m.timestampMS;
                else
                    d.timestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.timestampMS) : o.longs === Number ? new $util.LongBits(m.timestampMS.low >>> 0, m.timestampMS.high >>> 0).toNumber() : m.timestampMS;
                if (o.oneofs)
                    d._timestampMS = "timestampMS";
            }
            return d;
        };

        KeepInChatMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        KeepInChatMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.KeepInChatMessage";
        };

        return KeepInChatMessage;
    })();

    WAE2E.QuestionResponseMessage = (function() {

        function QuestionResponseMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        QuestionResponseMessage.prototype.key = null;
        QuestionResponseMessage.prototype.text = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuestionResponseMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(QuestionResponseMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        QuestionResponseMessage.create = function create(properties) {
            return new QuestionResponseMessage(properties);
        };

        QuestionResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            return w;
        };

        QuestionResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.QuestionResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.text = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        QuestionResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.QuestionResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.QuestionResponseMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.QuestionResponseMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            return m;
        };

        QuestionResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            return d;
        };

        QuestionResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuestionResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.QuestionResponseMessage";
        };

        return QuestionResponseMessage;
    })();

    WAE2E.StatusQuestionAnswerMessage = (function() {

        function StatusQuestionAnswerMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusQuestionAnswerMessage.prototype.key = null;
        StatusQuestionAnswerMessage.prototype.text = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusQuestionAnswerMessage.create = function create(properties) {
            return new StatusQuestionAnswerMessage(properties);
        };

        StatusQuestionAnswerMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            return w;
        };

        StatusQuestionAnswerMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StatusQuestionAnswerMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.text = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusQuestionAnswerMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StatusQuestionAnswerMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StatusQuestionAnswerMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.StatusQuestionAnswerMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            return m;
        };

        StatusQuestionAnswerMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            return d;
        };

        StatusQuestionAnswerMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusQuestionAnswerMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StatusQuestionAnswerMessage";
        };

        return StatusQuestionAnswerMessage;
    })();

    WAE2E.PollResultSnapshotMessage = (function() {

        function PollResultSnapshotMessage(p) {
            this.pollVotes = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollResultSnapshotMessage.prototype.name = null;
        PollResultSnapshotMessage.prototype.pollVotes = $util.emptyArray;
        PollResultSnapshotMessage.prototype.contextInfo = null;
        PollResultSnapshotMessage.prototype.pollType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollResultSnapshotMessage.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollResultSnapshotMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollResultSnapshotMessage.prototype, "_pollType", {
            get: $util.oneOfGetter($oneOfFields = ["pollType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollResultSnapshotMessage.create = function create(properties) {
            return new PollResultSnapshotMessage(properties);
        };

        PollResultSnapshotMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(10).string(m.name);
            if (m.pollVotes != null && m.pollVotes.length) {
                for (var i = 0; i < m.pollVotes.length; ++i)
                    WAE2E.PollResultSnapshotMessage.PollVote.encode(m.pollVotes[i], w.uint32(18).fork()).ldelim();
            }
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(26).fork()).ldelim();
            if (m.pollType != null && Object.hasOwnProperty.call(m, "pollType"))
                w.uint32(32).int32(m.pollType);
            return w;
        };

        PollResultSnapshotMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollResultSnapshotMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.name = r.string();
                        break;
                    }
                case 2: {
                        if (!(m.pollVotes && m.pollVotes.length))
                            m.pollVotes = [];
                        m.pollVotes.push(WAE2E.PollResultSnapshotMessage.PollVote.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.pollType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollResultSnapshotMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollResultSnapshotMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollResultSnapshotMessage();
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.pollVotes) {
                if (!Array.isArray(d.pollVotes))
                    throw TypeError(".WAE2E.PollResultSnapshotMessage.pollVotes: array expected");
                m.pollVotes = [];
                for (var i = 0; i < d.pollVotes.length; ++i) {
                    if (typeof d.pollVotes[i] !== "object")
                        throw TypeError(".WAE2E.PollResultSnapshotMessage.pollVotes: object expected");
                    m.pollVotes[i] = WAE2E.PollResultSnapshotMessage.PollVote.fromObject(d.pollVotes[i], n + 1);
                }
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.PollResultSnapshotMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            switch (d.pollType) {
            default:
                if (typeof d.pollType === "number") {
                    m.pollType = d.pollType;
                    break;
                }
                break;
            case "POLL":
            case 0:
                m.pollType = 0;
                break;
            case "QUIZ":
            case 1:
                m.pollType = 1;
                break;
            }
            return m;
        };

        PollResultSnapshotMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.pollVotes = [];
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.pollVotes && m.pollVotes.length) {
                d.pollVotes = [];
                for (var j = 0; j < m.pollVotes.length; ++j) {
                    d.pollVotes[j] = WAE2E.PollResultSnapshotMessage.PollVote.toObject(m.pollVotes[j], o);
                }
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.pollType != null && m.hasOwnProperty("pollType")) {
                d.pollType = o.enums === String ? WAE2E.PollType[m.pollType] === undefined ? m.pollType : WAE2E.PollType[m.pollType] : m.pollType;
                if (o.oneofs)
                    d._pollType = "pollType";
            }
            return d;
        };

        PollResultSnapshotMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollResultSnapshotMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollResultSnapshotMessage";
        };

        PollResultSnapshotMessage.PollVote = (function() {

            function PollVote(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            PollVote.prototype.optionName = null;
            PollVote.prototype.optionVoteCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollVote.prototype, "_optionName", {
                get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(PollVote.prototype, "_optionVoteCount", {
                get: $util.oneOfGetter($oneOfFields = ["optionVoteCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollVote.create = function create(properties) {
                return new PollVote(properties);
            };

            PollVote.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.optionName != null && Object.hasOwnProperty.call(m, "optionName"))
                    w.uint32(10).string(m.optionName);
                if (m.optionVoteCount != null && Object.hasOwnProperty.call(m, "optionVoteCount"))
                    w.uint32(16).int64(m.optionVoteCount);
                return w;
            };

            PollVote.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollResultSnapshotMessage.PollVote();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.optionName = r.string();
                            break;
                        }
                    case 2: {
                            m.optionVoteCount = r.int64();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            PollVote.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PollResultSnapshotMessage.PollVote)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PollResultSnapshotMessage.PollVote();
                if (d.optionName != null) {
                    m.optionName = String(d.optionName);
                }
                if (d.optionVoteCount != null) {
                    if ($util.Long)
                        (m.optionVoteCount = $util.Long.fromValue(d.optionVoteCount)).unsigned = false;
                    else if (typeof d.optionVoteCount === "string")
                        m.optionVoteCount = parseInt(d.optionVoteCount, 10);
                    else if (typeof d.optionVoteCount === "number")
                        m.optionVoteCount = d.optionVoteCount;
                    else if (typeof d.optionVoteCount === "object")
                        m.optionVoteCount = new $util.LongBits(d.optionVoteCount.low >>> 0, d.optionVoteCount.high >>> 0).toNumber();
                }
                return m;
            };

            PollVote.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.optionName != null && m.hasOwnProperty("optionName")) {
                    d.optionName = m.optionName;
                    if (o.oneofs)
                        d._optionName = "optionName";
                }
                if (m.optionVoteCount != null && m.hasOwnProperty("optionVoteCount")) {
                    if (typeof m.optionVoteCount === "number")
                        d.optionVoteCount = o.longs === String ? String(m.optionVoteCount) : m.optionVoteCount;
                    else
                        d.optionVoteCount = o.longs === String ? $util.Long.prototype.toString.call(m.optionVoteCount) : o.longs === Number ? new $util.LongBits(m.optionVoteCount.low >>> 0, m.optionVoteCount.high >>> 0).toNumber() : m.optionVoteCount;
                    if (o.oneofs)
                        d._optionVoteCount = "optionVoteCount";
                }
                return d;
            };

            PollVote.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollVote.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PollResultSnapshotMessage.PollVote";
            };

            return PollVote;
        })();

        return PollResultSnapshotMessage;
    })();

    WAE2E.PollAddOptionMessage = (function() {

        function PollAddOptionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollAddOptionMessage.prototype.pollCreationMessageKey = null;
        PollAddOptionMessage.prototype.addOption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollAddOptionMessage.prototype, "_pollCreationMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollAddOptionMessage.prototype, "_addOption", {
            get: $util.oneOfGetter($oneOfFields = ["addOption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollAddOptionMessage.create = function create(properties) {
            return new PollAddOptionMessage(properties);
        };

        PollAddOptionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.pollCreationMessageKey != null && Object.hasOwnProperty.call(m, "pollCreationMessageKey"))
                WACommon.MessageKey.encode(m.pollCreationMessageKey, w.uint32(10).fork()).ldelim();
            if (m.addOption != null && Object.hasOwnProperty.call(m, "addOption"))
                WAE2E.PollCreationMessage.Option.encode(m.addOption, w.uint32(18).fork()).ldelim();
            return w;
        };

        PollAddOptionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollAddOptionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.pollCreationMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.addOption = WAE2E.PollCreationMessage.Option.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollAddOptionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollAddOptionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollAddOptionMessage();
            if (d.pollCreationMessageKey != null) {
                if (typeof d.pollCreationMessageKey !== "object")
                    throw TypeError(".WAE2E.PollAddOptionMessage.pollCreationMessageKey: object expected");
                m.pollCreationMessageKey = WACommon.MessageKey.fromObject(d.pollCreationMessageKey, n + 1);
            }
            if (d.addOption != null) {
                if (typeof d.addOption !== "object")
                    throw TypeError(".WAE2E.PollAddOptionMessage.addOption: object expected");
                m.addOption = WAE2E.PollCreationMessage.Option.fromObject(d.addOption, n + 1);
            }
            return m;
        };

        PollAddOptionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.pollCreationMessageKey != null && m.hasOwnProperty("pollCreationMessageKey")) {
                d.pollCreationMessageKey = WACommon.MessageKey.toObject(m.pollCreationMessageKey, o);
                if (o.oneofs)
                    d._pollCreationMessageKey = "pollCreationMessageKey";
            }
            if (m.addOption != null && m.hasOwnProperty("addOption")) {
                d.addOption = WAE2E.PollCreationMessage.Option.toObject(m.addOption, o);
                if (o.oneofs)
                    d._addOption = "addOption";
            }
            return d;
        };

        PollAddOptionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollAddOptionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollAddOptionMessage";
        };

        return PollAddOptionMessage;
    })();

    WAE2E.PollVoteMessage = (function() {

        function PollVoteMessage(p) {
            this.selectedOptions = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollVoteMessage.prototype.selectedOptions = $util.emptyArray;

        PollVoteMessage.create = function create(properties) {
            return new PollVoteMessage(properties);
        };

        PollVoteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.selectedOptions != null && m.selectedOptions.length) {
                for (var i = 0; i < m.selectedOptions.length; ++i)
                    w.uint32(10).bytes(m.selectedOptions[i]);
            }
            return w;
        };

        PollVoteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollVoteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.selectedOptions && m.selectedOptions.length))
                            m.selectedOptions = [];
                        m.selectedOptions.push(r.bytes());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollVoteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollVoteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollVoteMessage();
            if (d.selectedOptions) {
                if (!Array.isArray(d.selectedOptions))
                    throw TypeError(".WAE2E.PollVoteMessage.selectedOptions: array expected");
                m.selectedOptions = [];
                for (var i = 0; i < d.selectedOptions.length; ++i) {
                    if (typeof d.selectedOptions[i] === "string")
                        $util.base64.decode(d.selectedOptions[i], m.selectedOptions[i] = $util.newBuffer($util.base64.length(d.selectedOptions[i])), 0);
                    else if (d.selectedOptions[i].length >= 0)
                        m.selectedOptions[i] = d.selectedOptions[i];
                }
            }
            return m;
        };

        PollVoteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.selectedOptions = [];
            }
            if (m.selectedOptions && m.selectedOptions.length) {
                d.selectedOptions = [];
                for (var j = 0; j < m.selectedOptions.length; ++j) {
                    d.selectedOptions[j] = o.bytes === String ? $util.base64.encode(m.selectedOptions[j], 0, m.selectedOptions[j].length) : o.bytes === Array ? Array.prototype.slice.call(m.selectedOptions[j]) : m.selectedOptions[j];
                }
            }
            return d;
        };

        PollVoteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollVoteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollVoteMessage";
        };

        return PollVoteMessage;
    })();

    WAE2E.PollEncValue = (function() {

        function PollEncValue(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollEncValue.prototype.encPayload = null;
        PollEncValue.prototype.encIV = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollEncValue.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollEncValue.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollEncValue.create = function create(properties) {
            return new PollEncValue(properties);
        };

        PollEncValue.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(10).bytes(m.encPayload);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(18).bytes(m.encIV);
            return w;
        };

        PollEncValue.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollEncValue();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.encPayload = r.bytes();
                        break;
                    }
                case 2: {
                        m.encIV = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollEncValue.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollEncValue)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollEncValue();
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            return m;
        };

        PollEncValue.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            return d;
        };

        PollEncValue.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollEncValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollEncValue";
        };

        return PollEncValue;
    })();

    WAE2E.PollUpdateMessageMetadata = (function() {

        function PollUpdateMessageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollUpdateMessageMetadata.create = function create(properties) {
            return new PollUpdateMessageMetadata(properties);
        };

        PollUpdateMessageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            return w;
        };

        PollUpdateMessageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollUpdateMessageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollUpdateMessageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollUpdateMessageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            return new WAE2E.PollUpdateMessageMetadata();
        };

        PollUpdateMessageMetadata.toObject = function toObject() {
            return {};
        };

        PollUpdateMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollUpdateMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollUpdateMessageMetadata";
        };

        return PollUpdateMessageMetadata;
    })();

    WAE2E.PollUpdateMessage = (function() {

        function PollUpdateMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollUpdateMessage.prototype.pollCreationMessageKey = null;
        PollUpdateMessage.prototype.vote = null;
        PollUpdateMessage.prototype.metadata = null;
        PollUpdateMessage.prototype.senderTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdateMessage.prototype, "_pollCreationMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdateMessage.prototype, "_vote", {
            get: $util.oneOfGetter($oneOfFields = ["vote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdateMessage.prototype, "_metadata", {
            get: $util.oneOfGetter($oneOfFields = ["metadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollUpdateMessage.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollUpdateMessage.create = function create(properties) {
            return new PollUpdateMessage(properties);
        };

        PollUpdateMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.pollCreationMessageKey != null && Object.hasOwnProperty.call(m, "pollCreationMessageKey"))
                WACommon.MessageKey.encode(m.pollCreationMessageKey, w.uint32(10).fork()).ldelim();
            if (m.vote != null && Object.hasOwnProperty.call(m, "vote"))
                WAE2E.PollEncValue.encode(m.vote, w.uint32(18).fork()).ldelim();
            if (m.metadata != null && Object.hasOwnProperty.call(m, "metadata"))
                WAE2E.PollUpdateMessageMetadata.encode(m.metadata, w.uint32(26).fork()).ldelim();
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(32).int64(m.senderTimestampMS);
            return w;
        };

        PollUpdateMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollUpdateMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.pollCreationMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.vote = WAE2E.PollEncValue.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.metadata = WAE2E.PollUpdateMessageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollUpdateMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollUpdateMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollUpdateMessage();
            if (d.pollCreationMessageKey != null) {
                if (typeof d.pollCreationMessageKey !== "object")
                    throw TypeError(".WAE2E.PollUpdateMessage.pollCreationMessageKey: object expected");
                m.pollCreationMessageKey = WACommon.MessageKey.fromObject(d.pollCreationMessageKey, n + 1);
            }
            if (d.vote != null) {
                if (typeof d.vote !== "object")
                    throw TypeError(".WAE2E.PollUpdateMessage.vote: object expected");
                m.vote = WAE2E.PollEncValue.fromObject(d.vote, n + 1);
            }
            if (d.metadata != null) {
                if (typeof d.metadata !== "object")
                    throw TypeError(".WAE2E.PollUpdateMessage.metadata: object expected");
                m.metadata = WAE2E.PollUpdateMessageMetadata.fromObject(d.metadata, n + 1);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        PollUpdateMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.pollCreationMessageKey != null && m.hasOwnProperty("pollCreationMessageKey")) {
                d.pollCreationMessageKey = WACommon.MessageKey.toObject(m.pollCreationMessageKey, o);
                if (o.oneofs)
                    d._pollCreationMessageKey = "pollCreationMessageKey";
            }
            if (m.vote != null && m.hasOwnProperty("vote")) {
                d.vote = WAE2E.PollEncValue.toObject(m.vote, o);
                if (o.oneofs)
                    d._vote = "vote";
            }
            if (m.metadata != null && m.hasOwnProperty("metadata")) {
                d.metadata = WAE2E.PollUpdateMessageMetadata.toObject(m.metadata, o);
                if (o.oneofs)
                    d._metadata = "metadata";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            return d;
        };

        PollUpdateMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollUpdateMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollUpdateMessage";
        };

        return PollUpdateMessage;
    })();

    WAE2E.PollCreationMessage = (function() {

        function PollCreationMessage(p) {
            this.options = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PollCreationMessage.prototype.encKey = null;
        PollCreationMessage.prototype.name = null;
        PollCreationMessage.prototype.options = $util.emptyArray;
        PollCreationMessage.prototype.selectableOptionsCount = null;
        PollCreationMessage.prototype.contextInfo = null;
        PollCreationMessage.prototype.pollContentType = null;
        PollCreationMessage.prototype.pollType = null;
        PollCreationMessage.prototype.correctAnswer = null;
        PollCreationMessage.prototype.endTime = null;
        PollCreationMessage.prototype.hideParticipantName = null;
        PollCreationMessage.prototype.allowAddOption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_encKey", {
            get: $util.oneOfGetter($oneOfFields = ["encKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_selectableOptionsCount", {
            get: $util.oneOfGetter($oneOfFields = ["selectableOptionsCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_pollContentType", {
            get: $util.oneOfGetter($oneOfFields = ["pollContentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_pollType", {
            get: $util.oneOfGetter($oneOfFields = ["pollType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_correctAnswer", {
            get: $util.oneOfGetter($oneOfFields = ["correctAnswer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_endTime", {
            get: $util.oneOfGetter($oneOfFields = ["endTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_hideParticipantName", {
            get: $util.oneOfGetter($oneOfFields = ["hideParticipantName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PollCreationMessage.prototype, "_allowAddOption", {
            get: $util.oneOfGetter($oneOfFields = ["allowAddOption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollCreationMessage.create = function create(properties) {
            return new PollCreationMessage(properties);
        };

        PollCreationMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.encKey != null && Object.hasOwnProperty.call(m, "encKey"))
                w.uint32(10).bytes(m.encKey);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(18).string(m.name);
            if (m.options != null && m.options.length) {
                for (var i = 0; i < m.options.length; ++i)
                    WAE2E.PollCreationMessage.Option.encode(m.options[i], w.uint32(26).fork()).ldelim();
            }
            if (m.selectableOptionsCount != null && Object.hasOwnProperty.call(m, "selectableOptionsCount"))
                w.uint32(32).uint32(m.selectableOptionsCount);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(42).fork()).ldelim();
            if (m.pollContentType != null && Object.hasOwnProperty.call(m, "pollContentType"))
                w.uint32(48).int32(m.pollContentType);
            if (m.pollType != null && Object.hasOwnProperty.call(m, "pollType"))
                w.uint32(56).int32(m.pollType);
            if (m.correctAnswer != null && Object.hasOwnProperty.call(m, "correctAnswer"))
                WAE2E.PollCreationMessage.Option.encode(m.correctAnswer, w.uint32(66).fork()).ldelim();
            if (m.endTime != null && Object.hasOwnProperty.call(m, "endTime"))
                w.uint32(72).int64(m.endTime);
            if (m.hideParticipantName != null && Object.hasOwnProperty.call(m, "hideParticipantName"))
                w.uint32(80).bool(m.hideParticipantName);
            if (m.allowAddOption != null && Object.hasOwnProperty.call(m, "allowAddOption"))
                w.uint32(88).bool(m.allowAddOption);
            return w;
        };

        PollCreationMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollCreationMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.encKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.name = r.string();
                        break;
                    }
                case 3: {
                        if (!(m.options && m.options.length))
                            m.options = [];
                        m.options.push(WAE2E.PollCreationMessage.Option.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 4: {
                        m.selectableOptionsCount = r.uint32();
                        break;
                    }
                case 5: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.pollContentType = r.int32();
                        break;
                    }
                case 7: {
                        m.pollType = r.int32();
                        break;
                    }
                case 8: {
                        m.correctAnswer = WAE2E.PollCreationMessage.Option.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.endTime = r.int64();
                        break;
                    }
                case 10: {
                        m.hideParticipantName = r.bool();
                        break;
                    }
                case 11: {
                        m.allowAddOption = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PollCreationMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PollCreationMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PollCreationMessage();
            if (d.encKey != null) {
                if (typeof d.encKey === "string")
                    $util.base64.decode(d.encKey, m.encKey = $util.newBuffer($util.base64.length(d.encKey)), 0);
                else if (d.encKey.length >= 0)
                    m.encKey = d.encKey;
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.options) {
                if (!Array.isArray(d.options))
                    throw TypeError(".WAE2E.PollCreationMessage.options: array expected");
                m.options = [];
                for (var i = 0; i < d.options.length; ++i) {
                    if (typeof d.options[i] !== "object")
                        throw TypeError(".WAE2E.PollCreationMessage.options: object expected");
                    m.options[i] = WAE2E.PollCreationMessage.Option.fromObject(d.options[i], n + 1);
                }
            }
            if (d.selectableOptionsCount != null) {
                m.selectableOptionsCount = d.selectableOptionsCount >>> 0;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.PollCreationMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            switch (d.pollContentType) {
            default:
                if (typeof d.pollContentType === "number") {
                    m.pollContentType = d.pollContentType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.pollContentType = 0;
                break;
            case "TEXT":
            case 1:
                m.pollContentType = 1;
                break;
            case "IMAGE":
            case 2:
                m.pollContentType = 2;
                break;
            }
            switch (d.pollType) {
            default:
                if (typeof d.pollType === "number") {
                    m.pollType = d.pollType;
                    break;
                }
                break;
            case "POLL":
            case 0:
                m.pollType = 0;
                break;
            case "QUIZ":
            case 1:
                m.pollType = 1;
                break;
            }
            if (d.correctAnswer != null) {
                if (typeof d.correctAnswer !== "object")
                    throw TypeError(".WAE2E.PollCreationMessage.correctAnswer: object expected");
                m.correctAnswer = WAE2E.PollCreationMessage.Option.fromObject(d.correctAnswer, n + 1);
            }
            if (d.endTime != null) {
                if ($util.Long)
                    (m.endTime = $util.Long.fromValue(d.endTime)).unsigned = false;
                else if (typeof d.endTime === "string")
                    m.endTime = parseInt(d.endTime, 10);
                else if (typeof d.endTime === "number")
                    m.endTime = d.endTime;
                else if (typeof d.endTime === "object")
                    m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
            }
            if (d.hideParticipantName != null) {
                m.hideParticipantName = Boolean(d.hideParticipantName);
            }
            if (d.allowAddOption != null) {
                m.allowAddOption = Boolean(d.allowAddOption);
            }
            return m;
        };

        PollCreationMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.options = [];
            }
            if (m.encKey != null && m.hasOwnProperty("encKey")) {
                d.encKey = o.bytes === String ? $util.base64.encode(m.encKey, 0, m.encKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.encKey) : m.encKey;
                if (o.oneofs)
                    d._encKey = "encKey";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.options && m.options.length) {
                d.options = [];
                for (var j = 0; j < m.options.length; ++j) {
                    d.options[j] = WAE2E.PollCreationMessage.Option.toObject(m.options[j], o);
                }
            }
            if (m.selectableOptionsCount != null && m.hasOwnProperty("selectableOptionsCount")) {
                d.selectableOptionsCount = m.selectableOptionsCount;
                if (o.oneofs)
                    d._selectableOptionsCount = "selectableOptionsCount";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.pollContentType != null && m.hasOwnProperty("pollContentType")) {
                d.pollContentType = o.enums === String ? WAE2E.PollContentType[m.pollContentType] === undefined ? m.pollContentType : WAE2E.PollContentType[m.pollContentType] : m.pollContentType;
                if (o.oneofs)
                    d._pollContentType = "pollContentType";
            }
            if (m.pollType != null && m.hasOwnProperty("pollType")) {
                d.pollType = o.enums === String ? WAE2E.PollType[m.pollType] === undefined ? m.pollType : WAE2E.PollType[m.pollType] : m.pollType;
                if (o.oneofs)
                    d._pollType = "pollType";
            }
            if (m.correctAnswer != null && m.hasOwnProperty("correctAnswer")) {
                d.correctAnswer = WAE2E.PollCreationMessage.Option.toObject(m.correctAnswer, o);
                if (o.oneofs)
                    d._correctAnswer = "correctAnswer";
            }
            if (m.endTime != null && m.hasOwnProperty("endTime")) {
                if (typeof m.endTime === "number")
                    d.endTime = o.longs === String ? String(m.endTime) : m.endTime;
                else
                    d.endTime = o.longs === String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                if (o.oneofs)
                    d._endTime = "endTime";
            }
            if (m.hideParticipantName != null && m.hasOwnProperty("hideParticipantName")) {
                d.hideParticipantName = m.hideParticipantName;
                if (o.oneofs)
                    d._hideParticipantName = "hideParticipantName";
            }
            if (m.allowAddOption != null && m.hasOwnProperty("allowAddOption")) {
                d.allowAddOption = m.allowAddOption;
                if (o.oneofs)
                    d._allowAddOption = "allowAddOption";
            }
            return d;
        };

        PollCreationMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollCreationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PollCreationMessage";
        };

        PollCreationMessage.Option = (function() {

            function Option(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Option.prototype.optionName = null;
            Option.prototype.optionHash = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Option.prototype, "_optionName", {
                get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Option.prototype, "_optionHash", {
                get: $util.oneOfGetter($oneOfFields = ["optionHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Option.create = function create(properties) {
                return new Option(properties);
            };

            Option.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.optionName != null && Object.hasOwnProperty.call(m, "optionName"))
                    w.uint32(10).string(m.optionName);
                if (m.optionHash != null && Object.hasOwnProperty.call(m, "optionHash"))
                    w.uint32(18).string(m.optionHash);
                return w;
            };

            Option.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PollCreationMessage.Option();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.optionName = r.string();
                            break;
                        }
                    case 2: {
                            m.optionHash = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Option.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.PollCreationMessage.Option)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.PollCreationMessage.Option();
                if (d.optionName != null) {
                    m.optionName = String(d.optionName);
                }
                if (d.optionHash != null) {
                    m.optionHash = String(d.optionHash);
                }
                return m;
            };

            Option.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.optionName != null && m.hasOwnProperty("optionName")) {
                    d.optionName = m.optionName;
                    if (o.oneofs)
                        d._optionName = "optionName";
                }
                if (m.optionHash != null && m.hasOwnProperty("optionHash")) {
                    d.optionHash = m.optionHash;
                    if (o.oneofs)
                        d._optionHash = "optionHash";
                }
                return d;
            };

            Option.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Option.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.PollCreationMessage.Option";
            };

            return Option;
        })();

        return PollCreationMessage;
    })();

    WAE2E.StickerSyncRMRMessage = (function() {

        function StickerSyncRMRMessage(p) {
            this.filehash = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StickerSyncRMRMessage.prototype.filehash = $util.emptyArray;
        StickerSyncRMRMessage.prototype.rmrSource = null;
        StickerSyncRMRMessage.prototype.requestTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerSyncRMRMessage.prototype, "_rmrSource", {
            get: $util.oneOfGetter($oneOfFields = ["rmrSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerSyncRMRMessage.prototype, "_requestTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["requestTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StickerSyncRMRMessage.create = function create(properties) {
            return new StickerSyncRMRMessage(properties);
        };

        StickerSyncRMRMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.filehash != null && m.filehash.length) {
                for (var i = 0; i < m.filehash.length; ++i)
                    w.uint32(10).string(m.filehash[i]);
            }
            if (m.rmrSource != null && Object.hasOwnProperty.call(m, "rmrSource"))
                w.uint32(18).string(m.rmrSource);
            if (m.requestTimestamp != null && Object.hasOwnProperty.call(m, "requestTimestamp"))
                w.uint32(24).int64(m.requestTimestamp);
            return w;
        };

        StickerSyncRMRMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StickerSyncRMRMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.filehash && m.filehash.length))
                            m.filehash = [];
                        m.filehash.push(r.string());
                        break;
                    }
                case 2: {
                        m.rmrSource = r.string();
                        break;
                    }
                case 3: {
                        m.requestTimestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StickerSyncRMRMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StickerSyncRMRMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StickerSyncRMRMessage();
            if (d.filehash) {
                if (!Array.isArray(d.filehash))
                    throw TypeError(".WAE2E.StickerSyncRMRMessage.filehash: array expected");
                m.filehash = [];
                for (var i = 0; i < d.filehash.length; ++i) {
                    m.filehash[i] = String(d.filehash[i]);
                }
            }
            if (d.rmrSource != null) {
                m.rmrSource = String(d.rmrSource);
            }
            if (d.requestTimestamp != null) {
                if ($util.Long)
                    (m.requestTimestamp = $util.Long.fromValue(d.requestTimestamp)).unsigned = false;
                else if (typeof d.requestTimestamp === "string")
                    m.requestTimestamp = parseInt(d.requestTimestamp, 10);
                else if (typeof d.requestTimestamp === "number")
                    m.requestTimestamp = d.requestTimestamp;
                else if (typeof d.requestTimestamp === "object")
                    m.requestTimestamp = new $util.LongBits(d.requestTimestamp.low >>> 0, d.requestTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        StickerSyncRMRMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.filehash = [];
            }
            if (m.filehash && m.filehash.length) {
                d.filehash = [];
                for (var j = 0; j < m.filehash.length; ++j) {
                    d.filehash[j] = m.filehash[j];
                }
            }
            if (m.rmrSource != null && m.hasOwnProperty("rmrSource")) {
                d.rmrSource = m.rmrSource;
                if (o.oneofs)
                    d._rmrSource = "rmrSource";
            }
            if (m.requestTimestamp != null && m.hasOwnProperty("requestTimestamp")) {
                if (typeof m.requestTimestamp === "number")
                    d.requestTimestamp = o.longs === String ? String(m.requestTimestamp) : m.requestTimestamp;
                else
                    d.requestTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.requestTimestamp) : o.longs === Number ? new $util.LongBits(m.requestTimestamp.low >>> 0, m.requestTimestamp.high >>> 0).toNumber() : m.requestTimestamp;
                if (o.oneofs)
                    d._requestTimestamp = "requestTimestamp";
            }
            return d;
        };

        StickerSyncRMRMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StickerSyncRMRMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StickerSyncRMRMessage";
        };

        return StickerSyncRMRMessage;
    })();

    WAE2E.ReactionMessage = (function() {

        function ReactionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ReactionMessage.prototype.key = null;
        ReactionMessage.prototype.text = null;
        ReactionMessage.prototype.groupingKey = null;
        ReactionMessage.prototype.senderTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReactionMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReactionMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReactionMessage.prototype, "_groupingKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ReactionMessage.prototype, "_senderTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ReactionMessage.create = function create(properties) {
            return new ReactionMessage(properties);
        };

        ReactionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            if (m.groupingKey != null && Object.hasOwnProperty.call(m, "groupingKey"))
                w.uint32(26).string(m.groupingKey);
            if (m.senderTimestampMS != null && Object.hasOwnProperty.call(m, "senderTimestampMS"))
                w.uint32(32).int64(m.senderTimestampMS);
            return w;
        };

        ReactionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ReactionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.text = r.string();
                        break;
                    }
                case 3: {
                        m.groupingKey = r.string();
                        break;
                    }
                case 4: {
                        m.senderTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ReactionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ReactionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ReactionMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.ReactionMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.groupingKey != null) {
                m.groupingKey = String(d.groupingKey);
            }
            if (d.senderTimestampMS != null) {
                if ($util.Long)
                    (m.senderTimestampMS = $util.Long.fromValue(d.senderTimestampMS)).unsigned = false;
                else if (typeof d.senderTimestampMS === "string")
                    m.senderTimestampMS = parseInt(d.senderTimestampMS, 10);
                else if (typeof d.senderTimestampMS === "number")
                    m.senderTimestampMS = d.senderTimestampMS;
                else if (typeof d.senderTimestampMS === "object")
                    m.senderTimestampMS = new $util.LongBits(d.senderTimestampMS.low >>> 0, d.senderTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        ReactionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.groupingKey != null && m.hasOwnProperty("groupingKey")) {
                d.groupingKey = m.groupingKey;
                if (o.oneofs)
                    d._groupingKey = "groupingKey";
            }
            if (m.senderTimestampMS != null && m.hasOwnProperty("senderTimestampMS")) {
                if (typeof m.senderTimestampMS === "number")
                    d.senderTimestampMS = o.longs === String ? String(m.senderTimestampMS) : m.senderTimestampMS;
                else
                    d.senderTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestampMS) : o.longs === Number ? new $util.LongBits(m.senderTimestampMS.low >>> 0, m.senderTimestampMS.high >>> 0).toNumber() : m.senderTimestampMS;
                if (o.oneofs)
                    d._senderTimestampMS = "senderTimestampMS";
            }
            return d;
        };

        ReactionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReactionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ReactionMessage";
        };

        return ReactionMessage;
    })();

    WAE2E.FutureProofMessage = (function() {

        function FutureProofMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        FutureProofMessage.prototype.message = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FutureProofMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FutureProofMessage.create = function create(properties) {
            return new FutureProofMessage(properties);
        };

        FutureProofMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(10).fork()).ldelim();
            return w;
        };

        FutureProofMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.FutureProofMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        FutureProofMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.FutureProofMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.FutureProofMessage();
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAE2E.FutureProofMessage.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            return m;
        };

        FutureProofMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            return d;
        };

        FutureProofMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FutureProofMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.FutureProofMessage";
        };

        return FutureProofMessage;
    })();

    WAE2E.DeviceSentMessage = (function() {

        function DeviceSentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DeviceSentMessage.prototype.destinationJID = null;
        DeviceSentMessage.prototype.message = null;
        DeviceSentMessage.prototype.phash = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceSentMessage.prototype, "_destinationJID", {
            get: $util.oneOfGetter($oneOfFields = ["destinationJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceSentMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceSentMessage.prototype, "_phash", {
            get: $util.oneOfGetter($oneOfFields = ["phash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceSentMessage.create = function create(properties) {
            return new DeviceSentMessage(properties);
        };

        DeviceSentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.destinationJID != null && Object.hasOwnProperty.call(m, "destinationJID"))
                w.uint32(10).string(m.destinationJID);
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(18).fork()).ldelim();
            if (m.phash != null && Object.hasOwnProperty.call(m, "phash"))
                w.uint32(26).string(m.phash);
            return w;
        };

        DeviceSentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.DeviceSentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.destinationJID = r.string();
                        break;
                    }
                case 2: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.phash = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DeviceSentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.DeviceSentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.DeviceSentMessage();
            if (d.destinationJID != null) {
                m.destinationJID = String(d.destinationJID);
            }
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAE2E.DeviceSentMessage.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            if (d.phash != null) {
                m.phash = String(d.phash);
            }
            return m;
        };

        DeviceSentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.destinationJID != null && m.hasOwnProperty("destinationJID")) {
                d.destinationJID = m.destinationJID;
                if (o.oneofs)
                    d._destinationJID = "destinationJID";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.phash != null && m.hasOwnProperty("phash")) {
                d.phash = m.phash;
                if (o.oneofs)
                    d._phash = "phash";
            }
            return d;
        };

        DeviceSentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceSentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.DeviceSentMessage";
        };

        return DeviceSentMessage;
    })();

    WAE2E.RequestPhoneNumberMessage = (function() {

        function RequestPhoneNumberMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        RequestPhoneNumberMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPhoneNumberMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RequestPhoneNumberMessage.create = function create(properties) {
            return new RequestPhoneNumberMessage(properties);
        };

        RequestPhoneNumberMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(10).fork()).ldelim();
            return w;
        };

        RequestPhoneNumberMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.RequestPhoneNumberMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        RequestPhoneNumberMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.RequestPhoneNumberMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.RequestPhoneNumberMessage();
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.RequestPhoneNumberMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        RequestPhoneNumberMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        RequestPhoneNumberMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RequestPhoneNumberMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.RequestPhoneNumberMessage";
        };

        return RequestPhoneNumberMessage;
    })();

    WAE2E.EventInviteMessage = (function() {

        function EventInviteMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EventInviteMessage.prototype.contextInfo = null;
        EventInviteMessage.prototype.eventID = null;
        EventInviteMessage.prototype.eventTitle = null;
        EventInviteMessage.prototype.JPEGThumbnail = null;
        EventInviteMessage.prototype.startTime = null;
        EventInviteMessage.prototype.caption = null;
        EventInviteMessage.prototype.isCanceled = null;
        EventInviteMessage.prototype.endTime = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_eventID", {
            get: $util.oneOfGetter($oneOfFields = ["eventID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_eventTitle", {
            get: $util.oneOfGetter($oneOfFields = ["eventTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_startTime", {
            get: $util.oneOfGetter($oneOfFields = ["startTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_isCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EventInviteMessage.prototype, "_endTime", {
            get: $util.oneOfGetter($oneOfFields = ["endTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventInviteMessage.create = function create(properties) {
            return new EventInviteMessage(properties);
        };

        EventInviteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(10).fork()).ldelim();
            if (m.eventID != null && Object.hasOwnProperty.call(m, "eventID"))
                w.uint32(18).string(m.eventID);
            if (m.eventTitle != null && Object.hasOwnProperty.call(m, "eventTitle"))
                w.uint32(26).string(m.eventTitle);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(34).bytes(m.JPEGThumbnail);
            if (m.startTime != null && Object.hasOwnProperty.call(m, "startTime"))
                w.uint32(40).int64(m.startTime);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(50).string(m.caption);
            if (m.isCanceled != null && Object.hasOwnProperty.call(m, "isCanceled"))
                w.uint32(56).bool(m.isCanceled);
            if (m.endTime != null && Object.hasOwnProperty.call(m, "endTime"))
                w.uint32(64).int64(m.endTime);
            return w;
        };

        EventInviteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EventInviteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.eventID = r.string();
                        break;
                    }
                case 3: {
                        m.eventTitle = r.string();
                        break;
                    }
                case 4: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 5: {
                        m.startTime = r.int64();
                        break;
                    }
                case 6: {
                        m.caption = r.string();
                        break;
                    }
                case 7: {
                        m.isCanceled = r.bool();
                        break;
                    }
                case 8: {
                        m.endTime = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EventInviteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EventInviteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EventInviteMessage();
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.EventInviteMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.eventID != null) {
                m.eventID = String(d.eventID);
            }
            if (d.eventTitle != null) {
                m.eventTitle = String(d.eventTitle);
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.startTime != null) {
                if ($util.Long)
                    (m.startTime = $util.Long.fromValue(d.startTime)).unsigned = false;
                else if (typeof d.startTime === "string")
                    m.startTime = parseInt(d.startTime, 10);
                else if (typeof d.startTime === "number")
                    m.startTime = d.startTime;
                else if (typeof d.startTime === "object")
                    m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.isCanceled != null) {
                m.isCanceled = Boolean(d.isCanceled);
            }
            if (d.endTime != null) {
                if ($util.Long)
                    (m.endTime = $util.Long.fromValue(d.endTime)).unsigned = false;
                else if (typeof d.endTime === "string")
                    m.endTime = parseInt(d.endTime, 10);
                else if (typeof d.endTime === "number")
                    m.endTime = d.endTime;
                else if (typeof d.endTime === "object")
                    m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
            }
            return m;
        };

        EventInviteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.eventID != null && m.hasOwnProperty("eventID")) {
                d.eventID = m.eventID;
                if (o.oneofs)
                    d._eventID = "eventID";
            }
            if (m.eventTitle != null && m.hasOwnProperty("eventTitle")) {
                d.eventTitle = m.eventTitle;
                if (o.oneofs)
                    d._eventTitle = "eventTitle";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.startTime != null && m.hasOwnProperty("startTime")) {
                if (typeof m.startTime === "number")
                    d.startTime = o.longs === String ? String(m.startTime) : m.startTime;
                else
                    d.startTime = o.longs === String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
                if (o.oneofs)
                    d._startTime = "startTime";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.isCanceled != null && m.hasOwnProperty("isCanceled")) {
                d.isCanceled = m.isCanceled;
                if (o.oneofs)
                    d._isCanceled = "isCanceled";
            }
            if (m.endTime != null && m.hasOwnProperty("endTime")) {
                if (typeof m.endTime === "number")
                    d.endTime = o.longs === String ? String(m.endTime) : m.endTime;
                else
                    d.endTime = o.longs === String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                if (o.oneofs)
                    d._endTime = "endTime";
            }
            return d;
        };

        EventInviteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EventInviteMessage";
        };

        return EventInviteMessage;
    })();

    WAE2E.NewsletterFollowerInviteMessage = (function() {

        function NewsletterFollowerInviteMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        NewsletterFollowerInviteMessage.prototype.newsletterJID = null;
        NewsletterFollowerInviteMessage.prototype.newsletterName = null;
        NewsletterFollowerInviteMessage.prototype.JPEGThumbnail = null;
        NewsletterFollowerInviteMessage.prototype.caption = null;
        NewsletterFollowerInviteMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterJID", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterName", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NewsletterFollowerInviteMessage.create = function create(properties) {
            return new NewsletterFollowerInviteMessage(properties);
        };

        NewsletterFollowerInviteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.newsletterJID != null && Object.hasOwnProperty.call(m, "newsletterJID"))
                w.uint32(10).string(m.newsletterJID);
            if (m.newsletterName != null && Object.hasOwnProperty.call(m, "newsletterName"))
                w.uint32(18).string(m.newsletterName);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(26).bytes(m.JPEGThumbnail);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(34).string(m.caption);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(42).fork()).ldelim();
            return w;
        };

        NewsletterFollowerInviteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.NewsletterFollowerInviteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.newsletterJID = r.string();
                        break;
                    }
                case 2: {
                        m.newsletterName = r.string();
                        break;
                    }
                case 3: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 4: {
                        m.caption = r.string();
                        break;
                    }
                case 5: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        NewsletterFollowerInviteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.NewsletterFollowerInviteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.NewsletterFollowerInviteMessage();
            if (d.newsletterJID != null) {
                m.newsletterJID = String(d.newsletterJID);
            }
            if (d.newsletterName != null) {
                m.newsletterName = String(d.newsletterName);
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.NewsletterFollowerInviteMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        NewsletterFollowerInviteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.newsletterJID != null && m.hasOwnProperty("newsletterJID")) {
                d.newsletterJID = m.newsletterJID;
                if (o.oneofs)
                    d._newsletterJID = "newsletterJID";
            }
            if (m.newsletterName != null && m.hasOwnProperty("newsletterName")) {
                d.newsletterName = m.newsletterName;
                if (o.oneofs)
                    d._newsletterName = "newsletterName";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        NewsletterFollowerInviteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NewsletterFollowerInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.NewsletterFollowerInviteMessage";
        };

        return NewsletterFollowerInviteMessage;
    })();

    WAE2E.NewsletterAdminInviteMessage = (function() {

        function NewsletterAdminInviteMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        NewsletterAdminInviteMessage.prototype.newsletterJID = null;
        NewsletterAdminInviteMessage.prototype.newsletterName = null;
        NewsletterAdminInviteMessage.prototype.JPEGThumbnail = null;
        NewsletterAdminInviteMessage.prototype.caption = null;
        NewsletterAdminInviteMessage.prototype.inviteExpiration = null;
        NewsletterAdminInviteMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterJID", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterName", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_inviteExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NewsletterAdminInviteMessage.create = function create(properties) {
            return new NewsletterAdminInviteMessage(properties);
        };

        NewsletterAdminInviteMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.newsletterJID != null && Object.hasOwnProperty.call(m, "newsletterJID"))
                w.uint32(10).string(m.newsletterJID);
            if (m.newsletterName != null && Object.hasOwnProperty.call(m, "newsletterName"))
                w.uint32(18).string(m.newsletterName);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(26).bytes(m.JPEGThumbnail);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(34).string(m.caption);
            if (m.inviteExpiration != null && Object.hasOwnProperty.call(m, "inviteExpiration"))
                w.uint32(40).int64(m.inviteExpiration);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(50).fork()).ldelim();
            return w;
        };

        NewsletterAdminInviteMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.NewsletterAdminInviteMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.newsletterJID = r.string();
                        break;
                    }
                case 2: {
                        m.newsletterName = r.string();
                        break;
                    }
                case 3: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 4: {
                        m.caption = r.string();
                        break;
                    }
                case 5: {
                        m.inviteExpiration = r.int64();
                        break;
                    }
                case 6: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        NewsletterAdminInviteMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.NewsletterAdminInviteMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.NewsletterAdminInviteMessage();
            if (d.newsletterJID != null) {
                m.newsletterJID = String(d.newsletterJID);
            }
            if (d.newsletterName != null) {
                m.newsletterName = String(d.newsletterName);
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.inviteExpiration != null) {
                if ($util.Long)
                    (m.inviteExpiration = $util.Long.fromValue(d.inviteExpiration)).unsigned = false;
                else if (typeof d.inviteExpiration === "string")
                    m.inviteExpiration = parseInt(d.inviteExpiration, 10);
                else if (typeof d.inviteExpiration === "number")
                    m.inviteExpiration = d.inviteExpiration;
                else if (typeof d.inviteExpiration === "object")
                    m.inviteExpiration = new $util.LongBits(d.inviteExpiration.low >>> 0, d.inviteExpiration.high >>> 0).toNumber();
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.NewsletterAdminInviteMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        NewsletterAdminInviteMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.newsletterJID != null && m.hasOwnProperty("newsletterJID")) {
                d.newsletterJID = m.newsletterJID;
                if (o.oneofs)
                    d._newsletterJID = "newsletterJID";
            }
            if (m.newsletterName != null && m.hasOwnProperty("newsletterName")) {
                d.newsletterName = m.newsletterName;
                if (o.oneofs)
                    d._newsletterName = "newsletterName";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.inviteExpiration != null && m.hasOwnProperty("inviteExpiration")) {
                if (typeof m.inviteExpiration === "number")
                    d.inviteExpiration = o.longs === String ? String(m.inviteExpiration) : m.inviteExpiration;
                else
                    d.inviteExpiration = o.longs === String ? $util.Long.prototype.toString.call(m.inviteExpiration) : o.longs === Number ? new $util.LongBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0).toNumber() : m.inviteExpiration;
                if (o.oneofs)
                    d._inviteExpiration = "inviteExpiration";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        NewsletterAdminInviteMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        NewsletterAdminInviteMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.NewsletterAdminInviteMessage";
        };

        return NewsletterAdminInviteMessage;
    })();

    WAE2E.ProductMessage = (function() {

        function ProductMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ProductMessage.prototype.product = null;
        ProductMessage.prototype.businessOwnerJID = null;
        ProductMessage.prototype.catalog = null;
        ProductMessage.prototype.body = null;
        ProductMessage.prototype.footer = null;
        ProductMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_product", {
            get: $util.oneOfGetter($oneOfFields = ["product"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_businessOwnerJID", {
            get: $util.oneOfGetter($oneOfFields = ["businessOwnerJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_catalog", {
            get: $util.oneOfGetter($oneOfFields = ["catalog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_body", {
            get: $util.oneOfGetter($oneOfFields = ["body"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_footer", {
            get: $util.oneOfGetter($oneOfFields = ["footer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProductMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProductMessage.create = function create(properties) {
            return new ProductMessage(properties);
        };

        ProductMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.product != null && Object.hasOwnProperty.call(m, "product"))
                WAE2E.ProductMessage.ProductSnapshot.encode(m.product, w.uint32(10).fork()).ldelim();
            if (m.businessOwnerJID != null && Object.hasOwnProperty.call(m, "businessOwnerJID"))
                w.uint32(18).string(m.businessOwnerJID);
            if (m.catalog != null && Object.hasOwnProperty.call(m, "catalog"))
                WAE2E.ProductMessage.CatalogSnapshot.encode(m.catalog, w.uint32(34).fork()).ldelim();
            if (m.body != null && Object.hasOwnProperty.call(m, "body"))
                w.uint32(42).string(m.body);
            if (m.footer != null && Object.hasOwnProperty.call(m, "footer"))
                w.uint32(50).string(m.footer);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        ProductMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ProductMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.product = WAE2E.ProductMessage.ProductSnapshot.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.businessOwnerJID = r.string();
                        break;
                    }
                case 4: {
                        m.catalog = WAE2E.ProductMessage.CatalogSnapshot.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.body = r.string();
                        break;
                    }
                case 6: {
                        m.footer = r.string();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ProductMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ProductMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ProductMessage();
            if (d.product != null) {
                if (typeof d.product !== "object")
                    throw TypeError(".WAE2E.ProductMessage.product: object expected");
                m.product = WAE2E.ProductMessage.ProductSnapshot.fromObject(d.product, n + 1);
            }
            if (d.businessOwnerJID != null) {
                m.businessOwnerJID = String(d.businessOwnerJID);
            }
            if (d.catalog != null) {
                if (typeof d.catalog !== "object")
                    throw TypeError(".WAE2E.ProductMessage.catalog: object expected");
                m.catalog = WAE2E.ProductMessage.CatalogSnapshot.fromObject(d.catalog, n + 1);
            }
            if (d.body != null) {
                m.body = String(d.body);
            }
            if (d.footer != null) {
                m.footer = String(d.footer);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ProductMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        ProductMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.product != null && m.hasOwnProperty("product")) {
                d.product = WAE2E.ProductMessage.ProductSnapshot.toObject(m.product, o);
                if (o.oneofs)
                    d._product = "product";
            }
            if (m.businessOwnerJID != null && m.hasOwnProperty("businessOwnerJID")) {
                d.businessOwnerJID = m.businessOwnerJID;
                if (o.oneofs)
                    d._businessOwnerJID = "businessOwnerJID";
            }
            if (m.catalog != null && m.hasOwnProperty("catalog")) {
                d.catalog = WAE2E.ProductMessage.CatalogSnapshot.toObject(m.catalog, o);
                if (o.oneofs)
                    d._catalog = "catalog";
            }
            if (m.body != null && m.hasOwnProperty("body")) {
                d.body = m.body;
                if (o.oneofs)
                    d._body = "body";
            }
            if (m.footer != null && m.hasOwnProperty("footer")) {
                d.footer = m.footer;
                if (o.oneofs)
                    d._footer = "footer";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        ProductMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProductMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ProductMessage";
        };

        ProductMessage.ProductSnapshot = (function() {

            function ProductSnapshot(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ProductSnapshot.prototype.productImage = null;
            ProductSnapshot.prototype.productID = null;
            ProductSnapshot.prototype.title = null;
            ProductSnapshot.prototype.description = null;
            ProductSnapshot.prototype.currencyCode = null;
            ProductSnapshot.prototype.priceAmount1000 = null;
            ProductSnapshot.prototype.retailerID = null;
            ProductSnapshot.prototype.URL = null;
            ProductSnapshot.prototype.productImageCount = null;
            ProductSnapshot.prototype.firstImageID = null;
            ProductSnapshot.prototype.salePriceAmount1000 = null;
            ProductSnapshot.prototype.signedURL = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_productImage", {
                get: $util.oneOfGetter($oneOfFields = ["productImage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_productID", {
                get: $util.oneOfGetter($oneOfFields = ["productID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_currencyCode", {
                get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_priceAmount1000", {
                get: $util.oneOfGetter($oneOfFields = ["priceAmount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_retailerID", {
                get: $util.oneOfGetter($oneOfFields = ["retailerID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_productImageCount", {
                get: $util.oneOfGetter($oneOfFields = ["productImageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_firstImageID", {
                get: $util.oneOfGetter($oneOfFields = ["firstImageID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_salePriceAmount1000", {
                get: $util.oneOfGetter($oneOfFields = ["salePriceAmount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ProductSnapshot.prototype, "_signedURL", {
                get: $util.oneOfGetter($oneOfFields = ["signedURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProductSnapshot.create = function create(properties) {
                return new ProductSnapshot(properties);
            };

            ProductSnapshot.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.productImage != null && Object.hasOwnProperty.call(m, "productImage"))
                    WAE2E.ImageMessage.encode(m.productImage, w.uint32(10).fork()).ldelim();
                if (m.productID != null && Object.hasOwnProperty.call(m, "productID"))
                    w.uint32(18).string(m.productID);
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(26).string(m.title);
                if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                    w.uint32(34).string(m.description);
                if (m.currencyCode != null && Object.hasOwnProperty.call(m, "currencyCode"))
                    w.uint32(42).string(m.currencyCode);
                if (m.priceAmount1000 != null && Object.hasOwnProperty.call(m, "priceAmount1000"))
                    w.uint32(48).int64(m.priceAmount1000);
                if (m.retailerID != null && Object.hasOwnProperty.call(m, "retailerID"))
                    w.uint32(58).string(m.retailerID);
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    w.uint32(66).string(m.URL);
                if (m.productImageCount != null && Object.hasOwnProperty.call(m, "productImageCount"))
                    w.uint32(72).uint32(m.productImageCount);
                if (m.firstImageID != null && Object.hasOwnProperty.call(m, "firstImageID"))
                    w.uint32(90).string(m.firstImageID);
                if (m.salePriceAmount1000 != null && Object.hasOwnProperty.call(m, "salePriceAmount1000"))
                    w.uint32(96).int64(m.salePriceAmount1000);
                if (m.signedURL != null && Object.hasOwnProperty.call(m, "signedURL"))
                    w.uint32(106).string(m.signedURL);
                return w;
            };

            ProductSnapshot.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ProductMessage.ProductSnapshot();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.productImage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.productID = r.string();
                            break;
                        }
                    case 3: {
                            m.title = r.string();
                            break;
                        }
                    case 4: {
                            m.description = r.string();
                            break;
                        }
                    case 5: {
                            m.currencyCode = r.string();
                            break;
                        }
                    case 6: {
                            m.priceAmount1000 = r.int64();
                            break;
                        }
                    case 7: {
                            m.retailerID = r.string();
                            break;
                        }
                    case 8: {
                            m.URL = r.string();
                            break;
                        }
                    case 9: {
                            m.productImageCount = r.uint32();
                            break;
                        }
                    case 11: {
                            m.firstImageID = r.string();
                            break;
                        }
                    case 12: {
                            m.salePriceAmount1000 = r.int64();
                            break;
                        }
                    case 13: {
                            m.signedURL = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ProductSnapshot.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ProductMessage.ProductSnapshot)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ProductMessage.ProductSnapshot();
                if (d.productImage != null) {
                    if (typeof d.productImage !== "object")
                        throw TypeError(".WAE2E.ProductMessage.ProductSnapshot.productImage: object expected");
                    m.productImage = WAE2E.ImageMessage.fromObject(d.productImage, n + 1);
                }
                if (d.productID != null) {
                    m.productID = String(d.productID);
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.description != null) {
                    m.description = String(d.description);
                }
                if (d.currencyCode != null) {
                    m.currencyCode = String(d.currencyCode);
                }
                if (d.priceAmount1000 != null) {
                    if ($util.Long)
                        (m.priceAmount1000 = $util.Long.fromValue(d.priceAmount1000)).unsigned = false;
                    else if (typeof d.priceAmount1000 === "string")
                        m.priceAmount1000 = parseInt(d.priceAmount1000, 10);
                    else if (typeof d.priceAmount1000 === "number")
                        m.priceAmount1000 = d.priceAmount1000;
                    else if (typeof d.priceAmount1000 === "object")
                        m.priceAmount1000 = new $util.LongBits(d.priceAmount1000.low >>> 0, d.priceAmount1000.high >>> 0).toNumber();
                }
                if (d.retailerID != null) {
                    m.retailerID = String(d.retailerID);
                }
                if (d.URL != null) {
                    m.URL = String(d.URL);
                }
                if (d.productImageCount != null) {
                    m.productImageCount = d.productImageCount >>> 0;
                }
                if (d.firstImageID != null) {
                    m.firstImageID = String(d.firstImageID);
                }
                if (d.salePriceAmount1000 != null) {
                    if ($util.Long)
                        (m.salePriceAmount1000 = $util.Long.fromValue(d.salePriceAmount1000)).unsigned = false;
                    else if (typeof d.salePriceAmount1000 === "string")
                        m.salePriceAmount1000 = parseInt(d.salePriceAmount1000, 10);
                    else if (typeof d.salePriceAmount1000 === "number")
                        m.salePriceAmount1000 = d.salePriceAmount1000;
                    else if (typeof d.salePriceAmount1000 === "object")
                        m.salePriceAmount1000 = new $util.LongBits(d.salePriceAmount1000.low >>> 0, d.salePriceAmount1000.high >>> 0).toNumber();
                }
                if (d.signedURL != null) {
                    m.signedURL = String(d.signedURL);
                }
                return m;
            };

            ProductSnapshot.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.productImage != null && m.hasOwnProperty("productImage")) {
                    d.productImage = WAE2E.ImageMessage.toObject(m.productImage, o);
                    if (o.oneofs)
                        d._productImage = "productImage";
                }
                if (m.productID != null && m.hasOwnProperty("productID")) {
                    d.productID = m.productID;
                    if (o.oneofs)
                        d._productID = "productID";
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.description != null && m.hasOwnProperty("description")) {
                    d.description = m.description;
                    if (o.oneofs)
                        d._description = "description";
                }
                if (m.currencyCode != null && m.hasOwnProperty("currencyCode")) {
                    d.currencyCode = m.currencyCode;
                    if (o.oneofs)
                        d._currencyCode = "currencyCode";
                }
                if (m.priceAmount1000 != null && m.hasOwnProperty("priceAmount1000")) {
                    if (typeof m.priceAmount1000 === "number")
                        d.priceAmount1000 = o.longs === String ? String(m.priceAmount1000) : m.priceAmount1000;
                    else
                        d.priceAmount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.priceAmount1000) : o.longs === Number ? new $util.LongBits(m.priceAmount1000.low >>> 0, m.priceAmount1000.high >>> 0).toNumber() : m.priceAmount1000;
                    if (o.oneofs)
                        d._priceAmount1000 = "priceAmount1000";
                }
                if (m.retailerID != null && m.hasOwnProperty("retailerID")) {
                    d.retailerID = m.retailerID;
                    if (o.oneofs)
                        d._retailerID = "retailerID";
                }
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = m.URL;
                    if (o.oneofs)
                        d._URL = "URL";
                }
                if (m.productImageCount != null && m.hasOwnProperty("productImageCount")) {
                    d.productImageCount = m.productImageCount;
                    if (o.oneofs)
                        d._productImageCount = "productImageCount";
                }
                if (m.firstImageID != null && m.hasOwnProperty("firstImageID")) {
                    d.firstImageID = m.firstImageID;
                    if (o.oneofs)
                        d._firstImageID = "firstImageID";
                }
                if (m.salePriceAmount1000 != null && m.hasOwnProperty("salePriceAmount1000")) {
                    if (typeof m.salePriceAmount1000 === "number")
                        d.salePriceAmount1000 = o.longs === String ? String(m.salePriceAmount1000) : m.salePriceAmount1000;
                    else
                        d.salePriceAmount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.salePriceAmount1000) : o.longs === Number ? new $util.LongBits(m.salePriceAmount1000.low >>> 0, m.salePriceAmount1000.high >>> 0).toNumber() : m.salePriceAmount1000;
                    if (o.oneofs)
                        d._salePriceAmount1000 = "salePriceAmount1000";
                }
                if (m.signedURL != null && m.hasOwnProperty("signedURL")) {
                    d.signedURL = m.signedURL;
                    if (o.oneofs)
                        d._signedURL = "signedURL";
                }
                return d;
            };

            ProductSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProductSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ProductMessage.ProductSnapshot";
            };

            return ProductSnapshot;
        })();

        ProductMessage.CatalogSnapshot = (function() {

            function CatalogSnapshot(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CatalogSnapshot.prototype.catalogImage = null;
            CatalogSnapshot.prototype.title = null;
            CatalogSnapshot.prototype.description = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CatalogSnapshot.prototype, "_catalogImage", {
                get: $util.oneOfGetter($oneOfFields = ["catalogImage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CatalogSnapshot.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CatalogSnapshot.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CatalogSnapshot.create = function create(properties) {
                return new CatalogSnapshot(properties);
            };

            CatalogSnapshot.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.catalogImage != null && Object.hasOwnProperty.call(m, "catalogImage"))
                    WAE2E.ImageMessage.encode(m.catalogImage, w.uint32(10).fork()).ldelim();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(18).string(m.title);
                if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                    w.uint32(26).string(m.description);
                return w;
            };

            CatalogSnapshot.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ProductMessage.CatalogSnapshot();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.catalogImage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.title = r.string();
                            break;
                        }
                    case 3: {
                            m.description = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CatalogSnapshot.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.ProductMessage.CatalogSnapshot)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.ProductMessage.CatalogSnapshot();
                if (d.catalogImage != null) {
                    if (typeof d.catalogImage !== "object")
                        throw TypeError(".WAE2E.ProductMessage.CatalogSnapshot.catalogImage: object expected");
                    m.catalogImage = WAE2E.ImageMessage.fromObject(d.catalogImage, n + 1);
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.description != null) {
                    m.description = String(d.description);
                }
                return m;
            };

            CatalogSnapshot.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.catalogImage != null && m.hasOwnProperty("catalogImage")) {
                    d.catalogImage = WAE2E.ImageMessage.toObject(m.catalogImage, o);
                    if (o.oneofs)
                        d._catalogImage = "catalogImage";
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.description != null && m.hasOwnProperty("description")) {
                    d.description = m.description;
                    if (o.oneofs)
                        d._description = "description";
                }
                return d;
            };

            CatalogSnapshot.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CatalogSnapshot.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.ProductMessage.CatalogSnapshot";
            };

            return CatalogSnapshot;
        })();

        return ProductMessage;
    })();

    WAE2E.TemplateButtonReplyMessage = (function() {

        function TemplateButtonReplyMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        TemplateButtonReplyMessage.prototype.selectedID = null;
        TemplateButtonReplyMessage.prototype.selectedDisplayText = null;
        TemplateButtonReplyMessage.prototype.contextInfo = null;
        TemplateButtonReplyMessage.prototype.selectedIndex = null;
        TemplateButtonReplyMessage.prototype.selectedCarouselCardIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedID", {
            get: $util.oneOfGetter($oneOfFields = ["selectedID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedDisplayText", {
            get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButtonReplyMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedIndex", {
            get: $util.oneOfGetter($oneOfFields = ["selectedIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedCarouselCardIndex", {
            get: $util.oneOfGetter($oneOfFields = ["selectedCarouselCardIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TemplateButtonReplyMessage.create = function create(properties) {
            return new TemplateButtonReplyMessage(properties);
        };

        TemplateButtonReplyMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.selectedID != null && Object.hasOwnProperty.call(m, "selectedID"))
                w.uint32(10).string(m.selectedID);
            if (m.selectedDisplayText != null && Object.hasOwnProperty.call(m, "selectedDisplayText"))
                w.uint32(18).string(m.selectedDisplayText);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(26).fork()).ldelim();
            if (m.selectedIndex != null && Object.hasOwnProperty.call(m, "selectedIndex"))
                w.uint32(32).uint32(m.selectedIndex);
            if (m.selectedCarouselCardIndex != null && Object.hasOwnProperty.call(m, "selectedCarouselCardIndex"))
                w.uint32(40).uint32(m.selectedCarouselCardIndex);
            return w;
        };

        TemplateButtonReplyMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateButtonReplyMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.selectedID = r.string();
                        break;
                    }
                case 2: {
                        m.selectedDisplayText = r.string();
                        break;
                    }
                case 3: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.selectedIndex = r.uint32();
                        break;
                    }
                case 5: {
                        m.selectedCarouselCardIndex = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        TemplateButtonReplyMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.TemplateButtonReplyMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.TemplateButtonReplyMessage();
            if (d.selectedID != null) {
                m.selectedID = String(d.selectedID);
            }
            if (d.selectedDisplayText != null) {
                m.selectedDisplayText = String(d.selectedDisplayText);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.TemplateButtonReplyMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.selectedIndex != null) {
                m.selectedIndex = d.selectedIndex >>> 0;
            }
            if (d.selectedCarouselCardIndex != null) {
                m.selectedCarouselCardIndex = d.selectedCarouselCardIndex >>> 0;
            }
            return m;
        };

        TemplateButtonReplyMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.selectedID != null && m.hasOwnProperty("selectedID")) {
                d.selectedID = m.selectedID;
                if (o.oneofs)
                    d._selectedID = "selectedID";
            }
            if (m.selectedDisplayText != null && m.hasOwnProperty("selectedDisplayText")) {
                d.selectedDisplayText = m.selectedDisplayText;
                if (o.oneofs)
                    d._selectedDisplayText = "selectedDisplayText";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.selectedIndex != null && m.hasOwnProperty("selectedIndex")) {
                d.selectedIndex = m.selectedIndex;
                if (o.oneofs)
                    d._selectedIndex = "selectedIndex";
            }
            if (m.selectedCarouselCardIndex != null && m.hasOwnProperty("selectedCarouselCardIndex")) {
                d.selectedCarouselCardIndex = m.selectedCarouselCardIndex;
                if (o.oneofs)
                    d._selectedCarouselCardIndex = "selectedCarouselCardIndex";
            }
            return d;
        };

        TemplateButtonReplyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TemplateButtonReplyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.TemplateButtonReplyMessage";
        };

        return TemplateButtonReplyMessage;
    })();

    WAE2E.TemplateMessage = (function() {

        function TemplateMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        TemplateMessage.prototype.fourRowTemplate = null;
        TemplateMessage.prototype.hydratedFourRowTemplate = null;
        TemplateMessage.prototype.interactiveMessageTemplate = null;
        TemplateMessage.prototype.contextInfo = null;
        TemplateMessage.prototype.hydratedTemplate = null;
        TemplateMessage.prototype.templateID = null;

        let $oneOfFields;

        Object.defineProperty(TemplateMessage.prototype, "format", {
            get: $util.oneOfGetter($oneOfFields = ["fourRowTemplate", "hydratedFourRowTemplate", "interactiveMessageTemplate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateMessage.prototype, "_hydratedTemplate", {
            get: $util.oneOfGetter($oneOfFields = ["hydratedTemplate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateMessage.prototype, "_templateID", {
            get: $util.oneOfGetter($oneOfFields = ["templateID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TemplateMessage.create = function create(properties) {
            return new TemplateMessage(properties);
        };

        TemplateMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.fourRowTemplate != null && Object.hasOwnProperty.call(m, "fourRowTemplate"))
                WAE2E.TemplateMessage.FourRowTemplate.encode(m.fourRowTemplate, w.uint32(10).fork()).ldelim();
            if (m.hydratedFourRowTemplate != null && Object.hasOwnProperty.call(m, "hydratedFourRowTemplate"))
                WAE2E.TemplateMessage.HydratedFourRowTemplate.encode(m.hydratedFourRowTemplate, w.uint32(18).fork()).ldelim();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(26).fork()).ldelim();
            if (m.hydratedTemplate != null && Object.hasOwnProperty.call(m, "hydratedTemplate"))
                WAE2E.TemplateMessage.HydratedFourRowTemplate.encode(m.hydratedTemplate, w.uint32(34).fork()).ldelim();
            if (m.interactiveMessageTemplate != null && Object.hasOwnProperty.call(m, "interactiveMessageTemplate"))
                WAE2E.InteractiveMessage.encode(m.interactiveMessageTemplate, w.uint32(42).fork()).ldelim();
            if (m.templateID != null && Object.hasOwnProperty.call(m, "templateID"))
                w.uint32(74).string(m.templateID);
            return w;
        };

        TemplateMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.fourRowTemplate = WAE2E.TemplateMessage.FourRowTemplate.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.hydratedFourRowTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.interactiveMessageTemplate = WAE2E.InteractiveMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.hydratedTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.templateID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        TemplateMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.TemplateMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.TemplateMessage();
            if (d.fourRowTemplate != null) {
                if (typeof d.fourRowTemplate !== "object")
                    throw TypeError(".WAE2E.TemplateMessage.fourRowTemplate: object expected");
                m.fourRowTemplate = WAE2E.TemplateMessage.FourRowTemplate.fromObject(d.fourRowTemplate, n + 1);
            }
            if (d.hydratedFourRowTemplate != null) {
                if (typeof d.hydratedFourRowTemplate !== "object")
                    throw TypeError(".WAE2E.TemplateMessage.hydratedFourRowTemplate: object expected");
                m.hydratedFourRowTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.fromObject(d.hydratedFourRowTemplate, n + 1);
            }
            if (d.interactiveMessageTemplate != null) {
                if (typeof d.interactiveMessageTemplate !== "object")
                    throw TypeError(".WAE2E.TemplateMessage.interactiveMessageTemplate: object expected");
                m.interactiveMessageTemplate = WAE2E.InteractiveMessage.fromObject(d.interactiveMessageTemplate, n + 1);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.TemplateMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.hydratedTemplate != null) {
                if (typeof d.hydratedTemplate !== "object")
                    throw TypeError(".WAE2E.TemplateMessage.hydratedTemplate: object expected");
                m.hydratedTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.fromObject(d.hydratedTemplate, n + 1);
            }
            if (d.templateID != null) {
                m.templateID = String(d.templateID);
            }
            return m;
        };

        TemplateMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.fourRowTemplate != null && m.hasOwnProperty("fourRowTemplate")) {
                d.fourRowTemplate = WAE2E.TemplateMessage.FourRowTemplate.toObject(m.fourRowTemplate, o);
                if (o.oneofs)
                    d.format = "fourRowTemplate";
            }
            if (m.hydratedFourRowTemplate != null && m.hasOwnProperty("hydratedFourRowTemplate")) {
                d.hydratedFourRowTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.toObject(m.hydratedFourRowTemplate, o);
                if (o.oneofs)
                    d.format = "hydratedFourRowTemplate";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.hydratedTemplate != null && m.hasOwnProperty("hydratedTemplate")) {
                d.hydratedTemplate = WAE2E.TemplateMessage.HydratedFourRowTemplate.toObject(m.hydratedTemplate, o);
                if (o.oneofs)
                    d._hydratedTemplate = "hydratedTemplate";
            }
            if (m.interactiveMessageTemplate != null && m.hasOwnProperty("interactiveMessageTemplate")) {
                d.interactiveMessageTemplate = WAE2E.InteractiveMessage.toObject(m.interactiveMessageTemplate, o);
                if (o.oneofs)
                    d.format = "interactiveMessageTemplate";
            }
            if (m.templateID != null && m.hasOwnProperty("templateID")) {
                d.templateID = m.templateID;
                if (o.oneofs)
                    d._templateID = "templateID";
            }
            return d;
        };

        TemplateMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TemplateMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.TemplateMessage";
        };

        TemplateMessage.HydratedFourRowTemplate = (function() {

            function HydratedFourRowTemplate(p) {
                this.hydratedButtons = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HydratedFourRowTemplate.prototype.documentMessage = null;
            HydratedFourRowTemplate.prototype.hydratedTitleText = null;
            HydratedFourRowTemplate.prototype.imageMessage = null;
            HydratedFourRowTemplate.prototype.videoMessage = null;
            HydratedFourRowTemplate.prototype.locationMessage = null;
            HydratedFourRowTemplate.prototype.hydratedContentText = null;
            HydratedFourRowTemplate.prototype.hydratedFooterText = null;
            HydratedFourRowTemplate.prototype.hydratedButtons = $util.emptyArray;
            HydratedFourRowTemplate.prototype.templateID = null;
            HydratedFourRowTemplate.prototype.maskLinkedDevices = null;

            let $oneOfFields;

            Object.defineProperty(HydratedFourRowTemplate.prototype, "title", {
                get: $util.oneOfGetter($oneOfFields = ["documentMessage", "hydratedTitleText", "imageMessage", "videoMessage", "locationMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedContentText", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedContentText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedFooterText", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedFooterText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedFourRowTemplate.prototype, "_templateID", {
                get: $util.oneOfGetter($oneOfFields = ["templateID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HydratedFourRowTemplate.prototype, "_maskLinkedDevices", {
                get: $util.oneOfGetter($oneOfFields = ["maskLinkedDevices"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedFourRowTemplate.create = function create(properties) {
                return new HydratedFourRowTemplate(properties);
            };

            HydratedFourRowTemplate.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.documentMessage != null && Object.hasOwnProperty.call(m, "documentMessage"))
                    WAE2E.DocumentMessage.encode(m.documentMessage, w.uint32(10).fork()).ldelim();
                if (m.hydratedTitleText != null && Object.hasOwnProperty.call(m, "hydratedTitleText"))
                    w.uint32(18).string(m.hydratedTitleText);
                if (m.imageMessage != null && Object.hasOwnProperty.call(m, "imageMessage"))
                    WAE2E.ImageMessage.encode(m.imageMessage, w.uint32(26).fork()).ldelim();
                if (m.videoMessage != null && Object.hasOwnProperty.call(m, "videoMessage"))
                    WAE2E.VideoMessage.encode(m.videoMessage, w.uint32(34).fork()).ldelim();
                if (m.locationMessage != null && Object.hasOwnProperty.call(m, "locationMessage"))
                    WAE2E.LocationMessage.encode(m.locationMessage, w.uint32(42).fork()).ldelim();
                if (m.hydratedContentText != null && Object.hasOwnProperty.call(m, "hydratedContentText"))
                    w.uint32(50).string(m.hydratedContentText);
                if (m.hydratedFooterText != null && Object.hasOwnProperty.call(m, "hydratedFooterText"))
                    w.uint32(58).string(m.hydratedFooterText);
                if (m.hydratedButtons != null && m.hydratedButtons.length) {
                    for (var i = 0; i < m.hydratedButtons.length; ++i)
                        WAE2E.HydratedTemplateButton.encode(m.hydratedButtons[i], w.uint32(66).fork()).ldelim();
                }
                if (m.templateID != null && Object.hasOwnProperty.call(m, "templateID"))
                    w.uint32(74).string(m.templateID);
                if (m.maskLinkedDevices != null && Object.hasOwnProperty.call(m, "maskLinkedDevices"))
                    w.uint32(80).bool(m.maskLinkedDevices);
                return w;
            };

            HydratedFourRowTemplate.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateMessage.HydratedFourRowTemplate();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.documentMessage = WAE2E.DocumentMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.hydratedTitleText = r.string();
                            break;
                        }
                    case 3: {
                            m.imageMessage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 4: {
                            m.videoMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 5: {
                            m.locationMessage = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 6: {
                            m.hydratedContentText = r.string();
                            break;
                        }
                    case 7: {
                            m.hydratedFooterText = r.string();
                            break;
                        }
                    case 8: {
                            if (!(m.hydratedButtons && m.hydratedButtons.length))
                                m.hydratedButtons = [];
                            m.hydratedButtons.push(WAE2E.HydratedTemplateButton.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 9: {
                            m.templateID = r.string();
                            break;
                        }
                    case 10: {
                            m.maskLinkedDevices = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HydratedFourRowTemplate.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.TemplateMessage.HydratedFourRowTemplate)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.TemplateMessage.HydratedFourRowTemplate();
                if (d.documentMessage != null) {
                    if (typeof d.documentMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.documentMessage: object expected");
                    m.documentMessage = WAE2E.DocumentMessage.fromObject(d.documentMessage, n + 1);
                }
                if (d.hydratedTitleText != null) {
                    m.hydratedTitleText = String(d.hydratedTitleText);
                }
                if (d.imageMessage != null) {
                    if (typeof d.imageMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.imageMessage: object expected");
                    m.imageMessage = WAE2E.ImageMessage.fromObject(d.imageMessage, n + 1);
                }
                if (d.videoMessage != null) {
                    if (typeof d.videoMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.videoMessage: object expected");
                    m.videoMessage = WAE2E.VideoMessage.fromObject(d.videoMessage, n + 1);
                }
                if (d.locationMessage != null) {
                    if (typeof d.locationMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.locationMessage: object expected");
                    m.locationMessage = WAE2E.LocationMessage.fromObject(d.locationMessage, n + 1);
                }
                if (d.hydratedContentText != null) {
                    m.hydratedContentText = String(d.hydratedContentText);
                }
                if (d.hydratedFooterText != null) {
                    m.hydratedFooterText = String(d.hydratedFooterText);
                }
                if (d.hydratedButtons) {
                    if (!Array.isArray(d.hydratedButtons))
                        throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: array expected");
                    m.hydratedButtons = [];
                    for (var i = 0; i < d.hydratedButtons.length; ++i) {
                        if (typeof d.hydratedButtons[i] !== "object")
                            throw TypeError(".WAE2E.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: object expected");
                        m.hydratedButtons[i] = WAE2E.HydratedTemplateButton.fromObject(d.hydratedButtons[i], n + 1);
                    }
                }
                if (d.templateID != null) {
                    m.templateID = String(d.templateID);
                }
                if (d.maskLinkedDevices != null) {
                    m.maskLinkedDevices = Boolean(d.maskLinkedDevices);
                }
                return m;
            };

            HydratedFourRowTemplate.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.hydratedButtons = [];
                }
                if (m.documentMessage != null && m.hasOwnProperty("documentMessage")) {
                    d.documentMessage = WAE2E.DocumentMessage.toObject(m.documentMessage, o);
                    if (o.oneofs)
                        d.title = "documentMessage";
                }
                if (m.hydratedTitleText != null && m.hasOwnProperty("hydratedTitleText")) {
                    d.hydratedTitleText = m.hydratedTitleText;
                    if (o.oneofs)
                        d.title = "hydratedTitleText";
                }
                if (m.imageMessage != null && m.hasOwnProperty("imageMessage")) {
                    d.imageMessage = WAE2E.ImageMessage.toObject(m.imageMessage, o);
                    if (o.oneofs)
                        d.title = "imageMessage";
                }
                if (m.videoMessage != null && m.hasOwnProperty("videoMessage")) {
                    d.videoMessage = WAE2E.VideoMessage.toObject(m.videoMessage, o);
                    if (o.oneofs)
                        d.title = "videoMessage";
                }
                if (m.locationMessage != null && m.hasOwnProperty("locationMessage")) {
                    d.locationMessage = WAE2E.LocationMessage.toObject(m.locationMessage, o);
                    if (o.oneofs)
                        d.title = "locationMessage";
                }
                if (m.hydratedContentText != null && m.hasOwnProperty("hydratedContentText")) {
                    d.hydratedContentText = m.hydratedContentText;
                    if (o.oneofs)
                        d._hydratedContentText = "hydratedContentText";
                }
                if (m.hydratedFooterText != null && m.hasOwnProperty("hydratedFooterText")) {
                    d.hydratedFooterText = m.hydratedFooterText;
                    if (o.oneofs)
                        d._hydratedFooterText = "hydratedFooterText";
                }
                if (m.hydratedButtons && m.hydratedButtons.length) {
                    d.hydratedButtons = [];
                    for (var j = 0; j < m.hydratedButtons.length; ++j) {
                        d.hydratedButtons[j] = WAE2E.HydratedTemplateButton.toObject(m.hydratedButtons[j], o);
                    }
                }
                if (m.templateID != null && m.hasOwnProperty("templateID")) {
                    d.templateID = m.templateID;
                    if (o.oneofs)
                        d._templateID = "templateID";
                }
                if (m.maskLinkedDevices != null && m.hasOwnProperty("maskLinkedDevices")) {
                    d.maskLinkedDevices = m.maskLinkedDevices;
                    if (o.oneofs)
                        d._maskLinkedDevices = "maskLinkedDevices";
                }
                return d;
            };

            HydratedFourRowTemplate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedFourRowTemplate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.TemplateMessage.HydratedFourRowTemplate";
            };

            return HydratedFourRowTemplate;
        })();

        TemplateMessage.FourRowTemplate = (function() {

            function FourRowTemplate(p) {
                this.buttons = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            FourRowTemplate.prototype.documentMessage = null;
            FourRowTemplate.prototype.highlyStructuredMessage = null;
            FourRowTemplate.prototype.imageMessage = null;
            FourRowTemplate.prototype.videoMessage = null;
            FourRowTemplate.prototype.locationMessage = null;
            FourRowTemplate.prototype.content = null;
            FourRowTemplate.prototype.footer = null;
            FourRowTemplate.prototype.buttons = $util.emptyArray;

            let $oneOfFields;

            Object.defineProperty(FourRowTemplate.prototype, "title", {
                get: $util.oneOfGetter($oneOfFields = ["documentMessage", "highlyStructuredMessage", "imageMessage", "videoMessage", "locationMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FourRowTemplate.prototype, "_content", {
                get: $util.oneOfGetter($oneOfFields = ["content"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(FourRowTemplate.prototype, "_footer", {
                get: $util.oneOfGetter($oneOfFields = ["footer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FourRowTemplate.create = function create(properties) {
                return new FourRowTemplate(properties);
            };

            FourRowTemplate.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.documentMessage != null && Object.hasOwnProperty.call(m, "documentMessage"))
                    WAE2E.DocumentMessage.encode(m.documentMessage, w.uint32(10).fork()).ldelim();
                if (m.highlyStructuredMessage != null && Object.hasOwnProperty.call(m, "highlyStructuredMessage"))
                    WAE2E.HighlyStructuredMessage.encode(m.highlyStructuredMessage, w.uint32(18).fork()).ldelim();
                if (m.imageMessage != null && Object.hasOwnProperty.call(m, "imageMessage"))
                    WAE2E.ImageMessage.encode(m.imageMessage, w.uint32(26).fork()).ldelim();
                if (m.videoMessage != null && Object.hasOwnProperty.call(m, "videoMessage"))
                    WAE2E.VideoMessage.encode(m.videoMessage, w.uint32(34).fork()).ldelim();
                if (m.locationMessage != null && Object.hasOwnProperty.call(m, "locationMessage"))
                    WAE2E.LocationMessage.encode(m.locationMessage, w.uint32(42).fork()).ldelim();
                if (m.content != null && Object.hasOwnProperty.call(m, "content"))
                    WAE2E.HighlyStructuredMessage.encode(m.content, w.uint32(50).fork()).ldelim();
                if (m.footer != null && Object.hasOwnProperty.call(m, "footer"))
                    WAE2E.HighlyStructuredMessage.encode(m.footer, w.uint32(58).fork()).ldelim();
                if (m.buttons != null && m.buttons.length) {
                    for (var i = 0; i < m.buttons.length; ++i)
                        WAE2E.TemplateButton.encode(m.buttons[i], w.uint32(66).fork()).ldelim();
                }
                return w;
            };

            FourRowTemplate.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateMessage.FourRowTemplate();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.documentMessage = WAE2E.DocumentMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.imageMessage = WAE2E.ImageMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 4: {
                            m.videoMessage = WAE2E.VideoMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 5: {
                            m.locationMessage = WAE2E.LocationMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 6: {
                            m.content = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 7: {
                            m.footer = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 8: {
                            if (!(m.buttons && m.buttons.length))
                                m.buttons = [];
                            m.buttons.push(WAE2E.TemplateButton.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            FourRowTemplate.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.TemplateMessage.FourRowTemplate)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.TemplateMessage.FourRowTemplate();
                if (d.documentMessage != null) {
                    if (typeof d.documentMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.documentMessage: object expected");
                    m.documentMessage = WAE2E.DocumentMessage.fromObject(d.documentMessage, n + 1);
                }
                if (d.highlyStructuredMessage != null) {
                    if (typeof d.highlyStructuredMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.highlyStructuredMessage: object expected");
                    m.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.fromObject(d.highlyStructuredMessage, n + 1);
                }
                if (d.imageMessage != null) {
                    if (typeof d.imageMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.imageMessage: object expected");
                    m.imageMessage = WAE2E.ImageMessage.fromObject(d.imageMessage, n + 1);
                }
                if (d.videoMessage != null) {
                    if (typeof d.videoMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.videoMessage: object expected");
                    m.videoMessage = WAE2E.VideoMessage.fromObject(d.videoMessage, n + 1);
                }
                if (d.locationMessage != null) {
                    if (typeof d.locationMessage !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.locationMessage: object expected");
                    m.locationMessage = WAE2E.LocationMessage.fromObject(d.locationMessage, n + 1);
                }
                if (d.content != null) {
                    if (typeof d.content !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.content: object expected");
                    m.content = WAE2E.HighlyStructuredMessage.fromObject(d.content, n + 1);
                }
                if (d.footer != null) {
                    if (typeof d.footer !== "object")
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.footer: object expected");
                    m.footer = WAE2E.HighlyStructuredMessage.fromObject(d.footer, n + 1);
                }
                if (d.buttons) {
                    if (!Array.isArray(d.buttons))
                        throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.buttons: array expected");
                    m.buttons = [];
                    for (var i = 0; i < d.buttons.length; ++i) {
                        if (typeof d.buttons[i] !== "object")
                            throw TypeError(".WAE2E.TemplateMessage.FourRowTemplate.buttons: object expected");
                        m.buttons[i] = WAE2E.TemplateButton.fromObject(d.buttons[i], n + 1);
                    }
                }
                return m;
            };

            FourRowTemplate.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.buttons = [];
                }
                if (m.documentMessage != null && m.hasOwnProperty("documentMessage")) {
                    d.documentMessage = WAE2E.DocumentMessage.toObject(m.documentMessage, o);
                    if (o.oneofs)
                        d.title = "documentMessage";
                }
                if (m.highlyStructuredMessage != null && m.hasOwnProperty("highlyStructuredMessage")) {
                    d.highlyStructuredMessage = WAE2E.HighlyStructuredMessage.toObject(m.highlyStructuredMessage, o);
                    if (o.oneofs)
                        d.title = "highlyStructuredMessage";
                }
                if (m.imageMessage != null && m.hasOwnProperty("imageMessage")) {
                    d.imageMessage = WAE2E.ImageMessage.toObject(m.imageMessage, o);
                    if (o.oneofs)
                        d.title = "imageMessage";
                }
                if (m.videoMessage != null && m.hasOwnProperty("videoMessage")) {
                    d.videoMessage = WAE2E.VideoMessage.toObject(m.videoMessage, o);
                    if (o.oneofs)
                        d.title = "videoMessage";
                }
                if (m.locationMessage != null && m.hasOwnProperty("locationMessage")) {
                    d.locationMessage = WAE2E.LocationMessage.toObject(m.locationMessage, o);
                    if (o.oneofs)
                        d.title = "locationMessage";
                }
                if (m.content != null && m.hasOwnProperty("content")) {
                    d.content = WAE2E.HighlyStructuredMessage.toObject(m.content, o);
                    if (o.oneofs)
                        d._content = "content";
                }
                if (m.footer != null && m.hasOwnProperty("footer")) {
                    d.footer = WAE2E.HighlyStructuredMessage.toObject(m.footer, o);
                    if (o.oneofs)
                        d._footer = "footer";
                }
                if (m.buttons && m.buttons.length) {
                    d.buttons = [];
                    for (var j = 0; j < m.buttons.length; ++j) {
                        d.buttons[j] = WAE2E.TemplateButton.toObject(m.buttons[j], o);
                    }
                }
                return d;
            };

            FourRowTemplate.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            FourRowTemplate.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.TemplateMessage.FourRowTemplate";
            };

            return FourRowTemplate;
        })();

        return TemplateMessage;
    })();

    WAE2E.StickerMessage = (function() {

        function StickerMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StickerMessage.prototype.URL = null;
        StickerMessage.prototype.fileSHA256 = null;
        StickerMessage.prototype.fileEncSHA256 = null;
        StickerMessage.prototype.mediaKey = null;
        StickerMessage.prototype.mimetype = null;
        StickerMessage.prototype.height = null;
        StickerMessage.prototype.width = null;
        StickerMessage.prototype.directPath = null;
        StickerMessage.prototype.fileLength = null;
        StickerMessage.prototype.mediaKeyTimestamp = null;
        StickerMessage.prototype.firstFrameLength = null;
        StickerMessage.prototype.firstFrameSidecar = null;
        StickerMessage.prototype.isAnimated = null;
        StickerMessage.prototype.pngThumbnail = null;
        StickerMessage.prototype.contextInfo = null;
        StickerMessage.prototype.stickerSentTS = null;
        StickerMessage.prototype.isAvatar = null;
        StickerMessage.prototype.isAiSticker = null;
        StickerMessage.prototype.isLottie = null;
        StickerMessage.prototype.accessibilityLabel = null;
        StickerMessage.prototype.premium = null;
        StickerMessage.prototype.emojis = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_firstFrameLength", {
            get: $util.oneOfGetter($oneOfFields = ["firstFrameLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_firstFrameSidecar", {
            get: $util.oneOfGetter($oneOfFields = ["firstFrameSidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_isAnimated", {
            get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_pngThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["pngThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_stickerSentTS", {
            get: $util.oneOfGetter($oneOfFields = ["stickerSentTS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_isAvatar", {
            get: $util.oneOfGetter($oneOfFields = ["isAvatar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_isAiSticker", {
            get: $util.oneOfGetter($oneOfFields = ["isAiSticker"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_isLottie", {
            get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_accessibilityLabel", {
            get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_premium", {
            get: $util.oneOfGetter($oneOfFields = ["premium"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StickerMessage.prototype, "_emojis", {
            get: $util.oneOfGetter($oneOfFields = ["emojis"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StickerMessage.create = function create(properties) {
            return new StickerMessage(properties);
        };

        StickerMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(18).bytes(m.fileSHA256);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(26).bytes(m.fileEncSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(34).bytes(m.mediaKey);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(42).string(m.mimetype);
            if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                w.uint32(48).uint32(m.height);
            if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                w.uint32(56).uint32(m.width);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(66).string(m.directPath);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(72).uint64(m.fileLength);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(80).int64(m.mediaKeyTimestamp);
            if (m.firstFrameLength != null && Object.hasOwnProperty.call(m, "firstFrameLength"))
                w.uint32(88).uint32(m.firstFrameLength);
            if (m.firstFrameSidecar != null && Object.hasOwnProperty.call(m, "firstFrameSidecar"))
                w.uint32(98).bytes(m.firstFrameSidecar);
            if (m.isAnimated != null && Object.hasOwnProperty.call(m, "isAnimated"))
                w.uint32(104).bool(m.isAnimated);
            if (m.pngThumbnail != null && Object.hasOwnProperty.call(m, "pngThumbnail"))
                w.uint32(130).bytes(m.pngThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.stickerSentTS != null && Object.hasOwnProperty.call(m, "stickerSentTS"))
                w.uint32(144).int64(m.stickerSentTS);
            if (m.isAvatar != null && Object.hasOwnProperty.call(m, "isAvatar"))
                w.uint32(152).bool(m.isAvatar);
            if (m.isAiSticker != null && Object.hasOwnProperty.call(m, "isAiSticker"))
                w.uint32(160).bool(m.isAiSticker);
            if (m.isLottie != null && Object.hasOwnProperty.call(m, "isLottie"))
                w.uint32(168).bool(m.isLottie);
            if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                w.uint32(178).string(m.accessibilityLabel);
            if (m.premium != null && Object.hasOwnProperty.call(m, "premium"))
                w.uint32(192).int32(m.premium);
            if (m.emojis != null && Object.hasOwnProperty.call(m, "emojis"))
                w.uint32(202).string(m.emojis);
            return w;
        };

        StickerMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.StickerMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 3: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 4: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 5: {
                        m.mimetype = r.string();
                        break;
                    }
                case 6: {
                        m.height = r.uint32();
                        break;
                    }
                case 7: {
                        m.width = r.uint32();
                        break;
                    }
                case 8: {
                        m.directPath = r.string();
                        break;
                    }
                case 9: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 10: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 11: {
                        m.firstFrameLength = r.uint32();
                        break;
                    }
                case 12: {
                        m.firstFrameSidecar = r.bytes();
                        break;
                    }
                case 13: {
                        m.isAnimated = r.bool();
                        break;
                    }
                case 16: {
                        m.pngThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.stickerSentTS = r.int64();
                        break;
                    }
                case 19: {
                        m.isAvatar = r.bool();
                        break;
                    }
                case 20: {
                        m.isAiSticker = r.bool();
                        break;
                    }
                case 21: {
                        m.isLottie = r.bool();
                        break;
                    }
                case 22: {
                        m.accessibilityLabel = r.string();
                        break;
                    }
                case 24: {
                        m.premium = r.int32();
                        break;
                    }
                case 25: {
                        m.emojis = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StickerMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.StickerMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.StickerMessage();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.firstFrameLength != null) {
                m.firstFrameLength = d.firstFrameLength >>> 0;
            }
            if (d.firstFrameSidecar != null) {
                if (typeof d.firstFrameSidecar === "string")
                    $util.base64.decode(d.firstFrameSidecar, m.firstFrameSidecar = $util.newBuffer($util.base64.length(d.firstFrameSidecar)), 0);
                else if (d.firstFrameSidecar.length >= 0)
                    m.firstFrameSidecar = d.firstFrameSidecar;
            }
            if (d.isAnimated != null) {
                m.isAnimated = Boolean(d.isAnimated);
            }
            if (d.pngThumbnail != null) {
                if (typeof d.pngThumbnail === "string")
                    $util.base64.decode(d.pngThumbnail, m.pngThumbnail = $util.newBuffer($util.base64.length(d.pngThumbnail)), 0);
                else if (d.pngThumbnail.length >= 0)
                    m.pngThumbnail = d.pngThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.StickerMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.stickerSentTS != null) {
                if ($util.Long)
                    (m.stickerSentTS = $util.Long.fromValue(d.stickerSentTS)).unsigned = false;
                else if (typeof d.stickerSentTS === "string")
                    m.stickerSentTS = parseInt(d.stickerSentTS, 10);
                else if (typeof d.stickerSentTS === "number")
                    m.stickerSentTS = d.stickerSentTS;
                else if (typeof d.stickerSentTS === "object")
                    m.stickerSentTS = new $util.LongBits(d.stickerSentTS.low >>> 0, d.stickerSentTS.high >>> 0).toNumber();
            }
            if (d.isAvatar != null) {
                m.isAvatar = Boolean(d.isAvatar);
            }
            if (d.isAiSticker != null) {
                m.isAiSticker = Boolean(d.isAiSticker);
            }
            if (d.isLottie != null) {
                m.isLottie = Boolean(d.isLottie);
            }
            if (d.accessibilityLabel != null) {
                m.accessibilityLabel = String(d.accessibilityLabel);
            }
            if (d.premium != null) {
                m.premium = d.premium | 0;
            }
            if (d.emojis != null) {
                m.emojis = String(d.emojis);
            }
            return m;
        };

        StickerMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.height != null && m.hasOwnProperty("height")) {
                d.height = m.height;
                if (o.oneofs)
                    d._height = "height";
            }
            if (m.width != null && m.hasOwnProperty("width")) {
                d.width = m.width;
                if (o.oneofs)
                    d._width = "width";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.firstFrameLength != null && m.hasOwnProperty("firstFrameLength")) {
                d.firstFrameLength = m.firstFrameLength;
                if (o.oneofs)
                    d._firstFrameLength = "firstFrameLength";
            }
            if (m.firstFrameSidecar != null && m.hasOwnProperty("firstFrameSidecar")) {
                d.firstFrameSidecar = o.bytes === String ? $util.base64.encode(m.firstFrameSidecar, 0, m.firstFrameSidecar.length) : o.bytes === Array ? Array.prototype.slice.call(m.firstFrameSidecar) : m.firstFrameSidecar;
                if (o.oneofs)
                    d._firstFrameSidecar = "firstFrameSidecar";
            }
            if (m.isAnimated != null && m.hasOwnProperty("isAnimated")) {
                d.isAnimated = m.isAnimated;
                if (o.oneofs)
                    d._isAnimated = "isAnimated";
            }
            if (m.pngThumbnail != null && m.hasOwnProperty("pngThumbnail")) {
                d.pngThumbnail = o.bytes === String ? $util.base64.encode(m.pngThumbnail, 0, m.pngThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.pngThumbnail) : m.pngThumbnail;
                if (o.oneofs)
                    d._pngThumbnail = "pngThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.stickerSentTS != null && m.hasOwnProperty("stickerSentTS")) {
                if (typeof m.stickerSentTS === "number")
                    d.stickerSentTS = o.longs === String ? String(m.stickerSentTS) : m.stickerSentTS;
                else
                    d.stickerSentTS = o.longs === String ? $util.Long.prototype.toString.call(m.stickerSentTS) : o.longs === Number ? new $util.LongBits(m.stickerSentTS.low >>> 0, m.stickerSentTS.high >>> 0).toNumber() : m.stickerSentTS;
                if (o.oneofs)
                    d._stickerSentTS = "stickerSentTS";
            }
            if (m.isAvatar != null && m.hasOwnProperty("isAvatar")) {
                d.isAvatar = m.isAvatar;
                if (o.oneofs)
                    d._isAvatar = "isAvatar";
            }
            if (m.isAiSticker != null && m.hasOwnProperty("isAiSticker")) {
                d.isAiSticker = m.isAiSticker;
                if (o.oneofs)
                    d._isAiSticker = "isAiSticker";
            }
            if (m.isLottie != null && m.hasOwnProperty("isLottie")) {
                d.isLottie = m.isLottie;
                if (o.oneofs)
                    d._isLottie = "isLottie";
            }
            if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                d.accessibilityLabel = m.accessibilityLabel;
                if (o.oneofs)
                    d._accessibilityLabel = "accessibilityLabel";
            }
            if (m.premium != null && m.hasOwnProperty("premium")) {
                d.premium = m.premium;
                if (o.oneofs)
                    d._premium = "premium";
            }
            if (m.emojis != null && m.hasOwnProperty("emojis")) {
                d.emojis = m.emojis;
                if (o.oneofs)
                    d._emojis = "emojis";
            }
            return d;
        };

        StickerMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StickerMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.StickerMessage";
        };

        return StickerMessage;
    })();

    WAE2E.LiveLocationMessage = (function() {

        function LiveLocationMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LiveLocationMessage.prototype.degreesLatitude = null;
        LiveLocationMessage.prototype.degreesLongitude = null;
        LiveLocationMessage.prototype.accuracyInMeters = null;
        LiveLocationMessage.prototype.speedInMps = null;
        LiveLocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;
        LiveLocationMessage.prototype.caption = null;
        LiveLocationMessage.prototype.sequenceNumber = null;
        LiveLocationMessage.prototype.timeOffset = null;
        LiveLocationMessage.prototype.JPEGThumbnail = null;
        LiveLocationMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_degreesLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_degreesLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_accuracyInMeters", {
            get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_speedInMps", {
            get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
            get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_sequenceNumber", {
            get: $util.oneOfGetter($oneOfFields = ["sequenceNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_timeOffset", {
            get: $util.oneOfGetter($oneOfFields = ["timeOffset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LiveLocationMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LiveLocationMessage.create = function create(properties) {
            return new LiveLocationMessage(properties);
        };

        LiveLocationMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.degreesLatitude != null && Object.hasOwnProperty.call(m, "degreesLatitude"))
                w.uint32(9).double(m.degreesLatitude);
            if (m.degreesLongitude != null && Object.hasOwnProperty.call(m, "degreesLongitude"))
                w.uint32(17).double(m.degreesLongitude);
            if (m.accuracyInMeters != null && Object.hasOwnProperty.call(m, "accuracyInMeters"))
                w.uint32(24).uint32(m.accuracyInMeters);
            if (m.speedInMps != null && Object.hasOwnProperty.call(m, "speedInMps"))
                w.uint32(37).float(m.speedInMps);
            if (m.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth"))
                w.uint32(40).uint32(m.degreesClockwiseFromMagneticNorth);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(50).string(m.caption);
            if (m.sequenceNumber != null && Object.hasOwnProperty.call(m, "sequenceNumber"))
                w.uint32(56).int64(m.sequenceNumber);
            if (m.timeOffset != null && Object.hasOwnProperty.call(m, "timeOffset"))
                w.uint32(64).uint32(m.timeOffset);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        LiveLocationMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.LiveLocationMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.degreesLatitude = r.double();
                        break;
                    }
                case 2: {
                        m.degreesLongitude = r.double();
                        break;
                    }
                case 3: {
                        m.accuracyInMeters = r.uint32();
                        break;
                    }
                case 4: {
                        m.speedInMps = r.float();
                        break;
                    }
                case 5: {
                        m.degreesClockwiseFromMagneticNorth = r.uint32();
                        break;
                    }
                case 6: {
                        m.caption = r.string();
                        break;
                    }
                case 7: {
                        m.sequenceNumber = r.int64();
                        break;
                    }
                case 8: {
                        m.timeOffset = r.uint32();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LiveLocationMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.LiveLocationMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.LiveLocationMessage();
            if (d.degreesLatitude != null) {
                m.degreesLatitude = Number(d.degreesLatitude);
            }
            if (d.degreesLongitude != null) {
                m.degreesLongitude = Number(d.degreesLongitude);
            }
            if (d.accuracyInMeters != null) {
                m.accuracyInMeters = d.accuracyInMeters >>> 0;
            }
            if (d.speedInMps != null) {
                m.speedInMps = Number(d.speedInMps);
            }
            if (d.degreesClockwiseFromMagneticNorth != null) {
                m.degreesClockwiseFromMagneticNorth = d.degreesClockwiseFromMagneticNorth >>> 0;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.sequenceNumber != null) {
                if ($util.Long)
                    (m.sequenceNumber = $util.Long.fromValue(d.sequenceNumber)).unsigned = false;
                else if (typeof d.sequenceNumber === "string")
                    m.sequenceNumber = parseInt(d.sequenceNumber, 10);
                else if (typeof d.sequenceNumber === "number")
                    m.sequenceNumber = d.sequenceNumber;
                else if (typeof d.sequenceNumber === "object")
                    m.sequenceNumber = new $util.LongBits(d.sequenceNumber.low >>> 0, d.sequenceNumber.high >>> 0).toNumber();
            }
            if (d.timeOffset != null) {
                m.timeOffset = d.timeOffset >>> 0;
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.LiveLocationMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        LiveLocationMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.degreesLatitude != null && m.hasOwnProperty("degreesLatitude")) {
                d.degreesLatitude = o.json && !isFinite(m.degreesLatitude) ? String(m.degreesLatitude) : m.degreesLatitude;
                if (o.oneofs)
                    d._degreesLatitude = "degreesLatitude";
            }
            if (m.degreesLongitude != null && m.hasOwnProperty("degreesLongitude")) {
                d.degreesLongitude = o.json && !isFinite(m.degreesLongitude) ? String(m.degreesLongitude) : m.degreesLongitude;
                if (o.oneofs)
                    d._degreesLongitude = "degreesLongitude";
            }
            if (m.accuracyInMeters != null && m.hasOwnProperty("accuracyInMeters")) {
                d.accuracyInMeters = m.accuracyInMeters;
                if (o.oneofs)
                    d._accuracyInMeters = "accuracyInMeters";
            }
            if (m.speedInMps != null && m.hasOwnProperty("speedInMps")) {
                d.speedInMps = o.json && !isFinite(m.speedInMps) ? String(m.speedInMps) : m.speedInMps;
                if (o.oneofs)
                    d._speedInMps = "speedInMps";
            }
            if (m.degreesClockwiseFromMagneticNorth != null && m.hasOwnProperty("degreesClockwiseFromMagneticNorth")) {
                d.degreesClockwiseFromMagneticNorth = m.degreesClockwiseFromMagneticNorth;
                if (o.oneofs)
                    d._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.sequenceNumber != null && m.hasOwnProperty("sequenceNumber")) {
                if (typeof m.sequenceNumber === "number")
                    d.sequenceNumber = o.longs === String ? String(m.sequenceNumber) : m.sequenceNumber;
                else
                    d.sequenceNumber = o.longs === String ? $util.Long.prototype.toString.call(m.sequenceNumber) : o.longs === Number ? new $util.LongBits(m.sequenceNumber.low >>> 0, m.sequenceNumber.high >>> 0).toNumber() : m.sequenceNumber;
                if (o.oneofs)
                    d._sequenceNumber = "sequenceNumber";
            }
            if (m.timeOffset != null && m.hasOwnProperty("timeOffset")) {
                d.timeOffset = m.timeOffset;
                if (o.oneofs)
                    d._timeOffset = "timeOffset";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        LiveLocationMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LiveLocationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.LiveLocationMessage";
        };

        return LiveLocationMessage;
    })();

    WAE2E.SplitPaymentMessage = (function() {

        function SplitPaymentMessage(p) {
            this.participants = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SplitPaymentMessage.prototype.splitID = null;
        SplitPaymentMessage.prototype.totalAmount = null;
        SplitPaymentMessage.prototype.description = null;
        SplitPaymentMessage.prototype.requesterJID = null;
        SplitPaymentMessage.prototype.participants = $util.emptyArray;
        SplitPaymentMessage.prototype.createdAtMS = null;
        SplitPaymentMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_splitID", {
            get: $util.oneOfGetter($oneOfFields = ["splitID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_totalAmount", {
            get: $util.oneOfGetter($oneOfFields = ["totalAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_requesterJID", {
            get: $util.oneOfGetter($oneOfFields = ["requesterJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_createdAtMS", {
            get: $util.oneOfGetter($oneOfFields = ["createdAtMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SplitPaymentMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SplitPaymentMessage.create = function create(properties) {
            return new SplitPaymentMessage(properties);
        };

        SplitPaymentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.splitID != null && Object.hasOwnProperty.call(m, "splitID"))
                w.uint32(10).string(m.splitID);
            if (m.totalAmount != null && Object.hasOwnProperty.call(m, "totalAmount"))
                WAE2E.Money.encode(m.totalAmount, w.uint32(18).fork()).ldelim();
            if (m.description != null && Object.hasOwnProperty.call(m, "description"))
                w.uint32(26).string(m.description);
            if (m.requesterJID != null && Object.hasOwnProperty.call(m, "requesterJID"))
                w.uint32(34).string(m.requesterJID);
            if (m.participants != null && m.participants.length) {
                for (var i = 0; i < m.participants.length; ++i)
                    WAE2E.SplitPaymentParticipant.encode(m.participants[i], w.uint32(42).fork()).ldelim();
            }
            if (m.createdAtMS != null && Object.hasOwnProperty.call(m, "createdAtMS"))
                w.uint32(48).int64(m.createdAtMS);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        SplitPaymentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.SplitPaymentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.splitID = r.string();
                        break;
                    }
                case 2: {
                        m.totalAmount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.description = r.string();
                        break;
                    }
                case 4: {
                        m.requesterJID = r.string();
                        break;
                    }
                case 5: {
                        if (!(m.participants && m.participants.length))
                            m.participants = [];
                        m.participants.push(WAE2E.SplitPaymentParticipant.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 6: {
                        m.createdAtMS = r.int64();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SplitPaymentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.SplitPaymentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.SplitPaymentMessage();
            if (d.splitID != null) {
                m.splitID = String(d.splitID);
            }
            if (d.totalAmount != null) {
                if (typeof d.totalAmount !== "object")
                    throw TypeError(".WAE2E.SplitPaymentMessage.totalAmount: object expected");
                m.totalAmount = WAE2E.Money.fromObject(d.totalAmount, n + 1);
            }
            if (d.description != null) {
                m.description = String(d.description);
            }
            if (d.requesterJID != null) {
                m.requesterJID = String(d.requesterJID);
            }
            if (d.participants) {
                if (!Array.isArray(d.participants))
                    throw TypeError(".WAE2E.SplitPaymentMessage.participants: array expected");
                m.participants = [];
                for (var i = 0; i < d.participants.length; ++i) {
                    if (typeof d.participants[i] !== "object")
                        throw TypeError(".WAE2E.SplitPaymentMessage.participants: object expected");
                    m.participants[i] = WAE2E.SplitPaymentParticipant.fromObject(d.participants[i], n + 1);
                }
            }
            if (d.createdAtMS != null) {
                if ($util.Long)
                    (m.createdAtMS = $util.Long.fromValue(d.createdAtMS)).unsigned = false;
                else if (typeof d.createdAtMS === "string")
                    m.createdAtMS = parseInt(d.createdAtMS, 10);
                else if (typeof d.createdAtMS === "number")
                    m.createdAtMS = d.createdAtMS;
                else if (typeof d.createdAtMS === "object")
                    m.createdAtMS = new $util.LongBits(d.createdAtMS.low >>> 0, d.createdAtMS.high >>> 0).toNumber();
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.SplitPaymentMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        SplitPaymentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.participants = [];
            }
            if (m.splitID != null && m.hasOwnProperty("splitID")) {
                d.splitID = m.splitID;
                if (o.oneofs)
                    d._splitID = "splitID";
            }
            if (m.totalAmount != null && m.hasOwnProperty("totalAmount")) {
                d.totalAmount = WAE2E.Money.toObject(m.totalAmount, o);
                if (o.oneofs)
                    d._totalAmount = "totalAmount";
            }
            if (m.description != null && m.hasOwnProperty("description")) {
                d.description = m.description;
                if (o.oneofs)
                    d._description = "description";
            }
            if (m.requesterJID != null && m.hasOwnProperty("requesterJID")) {
                d.requesterJID = m.requesterJID;
                if (o.oneofs)
                    d._requesterJID = "requesterJID";
            }
            if (m.participants && m.participants.length) {
                d.participants = [];
                for (var j = 0; j < m.participants.length; ++j) {
                    d.participants[j] = WAE2E.SplitPaymentParticipant.toObject(m.participants[j], o);
                }
            }
            if (m.createdAtMS != null && m.hasOwnProperty("createdAtMS")) {
                if (typeof m.createdAtMS === "number")
                    d.createdAtMS = o.longs === String ? String(m.createdAtMS) : m.createdAtMS;
                else
                    d.createdAtMS = o.longs === String ? $util.Long.prototype.toString.call(m.createdAtMS) : o.longs === Number ? new $util.LongBits(m.createdAtMS.low >>> 0, m.createdAtMS.high >>> 0).toNumber() : m.createdAtMS;
                if (o.oneofs)
                    d._createdAtMS = "createdAtMS";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        SplitPaymentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SplitPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.SplitPaymentMessage";
        };

        return SplitPaymentMessage;
    })();

    WAE2E.CancelPaymentRequestMessage = (function() {

        function CancelPaymentRequestMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CancelPaymentRequestMessage.prototype.key = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CancelPaymentRequestMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CancelPaymentRequestMessage.create = function create(properties) {
            return new CancelPaymentRequestMessage(properties);
        };

        CancelPaymentRequestMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            return w;
        };

        CancelPaymentRequestMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.CancelPaymentRequestMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CancelPaymentRequestMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.CancelPaymentRequestMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.CancelPaymentRequestMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.CancelPaymentRequestMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            return m;
        };

        CancelPaymentRequestMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            return d;
        };

        CancelPaymentRequestMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CancelPaymentRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.CancelPaymentRequestMessage";
        };

        return CancelPaymentRequestMessage;
    })();

    WAE2E.DeclinePaymentRequestMessage = (function() {

        function DeclinePaymentRequestMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DeclinePaymentRequestMessage.prototype.key = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeclinePaymentRequestMessage.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeclinePaymentRequestMessage.create = function create(properties) {
            return new DeclinePaymentRequestMessage(properties);
        };

        DeclinePaymentRequestMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.key != null && Object.hasOwnProperty.call(m, "key"))
                WACommon.MessageKey.encode(m.key, w.uint32(10).fork()).ldelim();
            return w;
        };

        DeclinePaymentRequestMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.DeclinePaymentRequestMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.key = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DeclinePaymentRequestMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.DeclinePaymentRequestMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.DeclinePaymentRequestMessage();
            if (d.key != null) {
                if (typeof d.key !== "object")
                    throw TypeError(".WAE2E.DeclinePaymentRequestMessage.key: object expected");
                m.key = WACommon.MessageKey.fromObject(d.key, n + 1);
            }
            return m;
        };

        DeclinePaymentRequestMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.key != null && m.hasOwnProperty("key")) {
                d.key = WACommon.MessageKey.toObject(m.key, o);
                if (o.oneofs)
                    d._key = "key";
            }
            return d;
        };

        DeclinePaymentRequestMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeclinePaymentRequestMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.DeclinePaymentRequestMessage";
        };

        return DeclinePaymentRequestMessage;
    })();

    WAE2E.RequestPaymentMessage = (function() {

        function RequestPaymentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        RequestPaymentMessage.prototype.noteMessage = null;
        RequestPaymentMessage.prototype.currencyCodeIso4217 = null;
        RequestPaymentMessage.prototype.amount1000 = null;
        RequestPaymentMessage.prototype.requestFrom = null;
        RequestPaymentMessage.prototype.expiryTimestamp = null;
        RequestPaymentMessage.prototype.amount = null;
        RequestPaymentMessage.prototype.background = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_noteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_currencyCodeIso4217", {
            get: $util.oneOfGetter($oneOfFields = ["currencyCodeIso4217"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_amount1000", {
            get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_requestFrom", {
            get: $util.oneOfGetter($oneOfFields = ["requestFrom"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_amount", {
            get: $util.oneOfGetter($oneOfFields = ["amount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(RequestPaymentMessage.prototype, "_background", {
            get: $util.oneOfGetter($oneOfFields = ["background"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RequestPaymentMessage.create = function create(properties) {
            return new RequestPaymentMessage(properties);
        };

        RequestPaymentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.currencyCodeIso4217 != null && Object.hasOwnProperty.call(m, "currencyCodeIso4217"))
                w.uint32(10).string(m.currencyCodeIso4217);
            if (m.amount1000 != null && Object.hasOwnProperty.call(m, "amount1000"))
                w.uint32(16).uint64(m.amount1000);
            if (m.requestFrom != null && Object.hasOwnProperty.call(m, "requestFrom"))
                w.uint32(26).string(m.requestFrom);
            if (m.noteMessage != null && Object.hasOwnProperty.call(m, "noteMessage"))
                WAE2E.Message.encode(m.noteMessage, w.uint32(34).fork()).ldelim();
            if (m.expiryTimestamp != null && Object.hasOwnProperty.call(m, "expiryTimestamp"))
                w.uint32(40).int64(m.expiryTimestamp);
            if (m.amount != null && Object.hasOwnProperty.call(m, "amount"))
                WAE2E.Money.encode(m.amount, w.uint32(50).fork()).ldelim();
            if (m.background != null && Object.hasOwnProperty.call(m, "background"))
                WAE2E.PaymentBackground.encode(m.background, w.uint32(58).fork()).ldelim();
            return w;
        };

        RequestPaymentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.RequestPaymentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 4: {
                        m.noteMessage = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        m.currencyCodeIso4217 = r.string();
                        break;
                    }
                case 2: {
                        m.amount1000 = r.uint64();
                        break;
                    }
                case 3: {
                        m.requestFrom = r.string();
                        break;
                    }
                case 5: {
                        m.expiryTimestamp = r.int64();
                        break;
                    }
                case 6: {
                        m.amount = WAE2E.Money.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.background = WAE2E.PaymentBackground.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        RequestPaymentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.RequestPaymentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.RequestPaymentMessage();
            if (d.noteMessage != null) {
                if (typeof d.noteMessage !== "object")
                    throw TypeError(".WAE2E.RequestPaymentMessage.noteMessage: object expected");
                m.noteMessage = WAE2E.Message.fromObject(d.noteMessage, n + 1);
            }
            if (d.currencyCodeIso4217 != null) {
                m.currencyCodeIso4217 = String(d.currencyCodeIso4217);
            }
            if (d.amount1000 != null) {
                if ($util.Long)
                    (m.amount1000 = $util.Long.fromValue(d.amount1000)).unsigned = true;
                else if (typeof d.amount1000 === "string")
                    m.amount1000 = parseInt(d.amount1000, 10);
                else if (typeof d.amount1000 === "number")
                    m.amount1000 = d.amount1000;
                else if (typeof d.amount1000 === "object")
                    m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber(true);
            }
            if (d.requestFrom != null) {
                m.requestFrom = String(d.requestFrom);
            }
            if (d.expiryTimestamp != null) {
                if ($util.Long)
                    (m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp)).unsigned = false;
                else if (typeof d.expiryTimestamp === "string")
                    m.expiryTimestamp = parseInt(d.expiryTimestamp, 10);
                else if (typeof d.expiryTimestamp === "number")
                    m.expiryTimestamp = d.expiryTimestamp;
                else if (typeof d.expiryTimestamp === "object")
                    m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber();
            }
            if (d.amount != null) {
                if (typeof d.amount !== "object")
                    throw TypeError(".WAE2E.RequestPaymentMessage.amount: object expected");
                m.amount = WAE2E.Money.fromObject(d.amount, n + 1);
            }
            if (d.background != null) {
                if (typeof d.background !== "object")
                    throw TypeError(".WAE2E.RequestPaymentMessage.background: object expected");
                m.background = WAE2E.PaymentBackground.fromObject(d.background, n + 1);
            }
            return m;
        };

        RequestPaymentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.currencyCodeIso4217 != null && m.hasOwnProperty("currencyCodeIso4217")) {
                d.currencyCodeIso4217 = m.currencyCodeIso4217;
                if (o.oneofs)
                    d._currencyCodeIso4217 = "currencyCodeIso4217";
            }
            if (m.amount1000 != null && m.hasOwnProperty("amount1000")) {
                if (typeof m.amount1000 === "number")
                    d.amount1000 = o.longs === String ? String(m.amount1000) : m.amount1000;
                else
                    d.amount1000 = o.longs === String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber(true) : m.amount1000;
                if (o.oneofs)
                    d._amount1000 = "amount1000";
            }
            if (m.requestFrom != null && m.hasOwnProperty("requestFrom")) {
                d.requestFrom = m.requestFrom;
                if (o.oneofs)
                    d._requestFrom = "requestFrom";
            }
            if (m.noteMessage != null && m.hasOwnProperty("noteMessage")) {
                d.noteMessage = WAE2E.Message.toObject(m.noteMessage, o);
                if (o.oneofs)
                    d._noteMessage = "noteMessage";
            }
            if (m.expiryTimestamp != null && m.hasOwnProperty("expiryTimestamp")) {
                if (typeof m.expiryTimestamp === "number")
                    d.expiryTimestamp = o.longs === String ? String(m.expiryTimestamp) : m.expiryTimestamp;
                else
                    d.expiryTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber() : m.expiryTimestamp;
                if (o.oneofs)
                    d._expiryTimestamp = "expiryTimestamp";
            }
            if (m.amount != null && m.hasOwnProperty("amount")) {
                d.amount = WAE2E.Money.toObject(m.amount, o);
                if (o.oneofs)
                    d._amount = "amount";
            }
            if (m.background != null && m.hasOwnProperty("background")) {
                d.background = WAE2E.PaymentBackground.toObject(m.background, o);
                if (o.oneofs)
                    d._background = "background";
            }
            return d;
        };

        RequestPaymentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        RequestPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.RequestPaymentMessage";
        };

        return RequestPaymentMessage;
    })();

    WAE2E.SendPaymentMessage = (function() {

        function SendPaymentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SendPaymentMessage.prototype.noteMessage = null;
        SendPaymentMessage.prototype.requestMessageKey = null;
        SendPaymentMessage.prototype.background = null;
        SendPaymentMessage.prototype.transactionData = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SendPaymentMessage.prototype, "_noteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SendPaymentMessage.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SendPaymentMessage.prototype, "_background", {
            get: $util.oneOfGetter($oneOfFields = ["background"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SendPaymentMessage.prototype, "_transactionData", {
            get: $util.oneOfGetter($oneOfFields = ["transactionData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SendPaymentMessage.create = function create(properties) {
            return new SendPaymentMessage(properties);
        };

        SendPaymentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.noteMessage != null && Object.hasOwnProperty.call(m, "noteMessage"))
                WAE2E.Message.encode(m.noteMessage, w.uint32(18).fork()).ldelim();
            if (m.requestMessageKey != null && Object.hasOwnProperty.call(m, "requestMessageKey"))
                WACommon.MessageKey.encode(m.requestMessageKey, w.uint32(26).fork()).ldelim();
            if (m.background != null && Object.hasOwnProperty.call(m, "background"))
                WAE2E.PaymentBackground.encode(m.background, w.uint32(34).fork()).ldelim();
            if (m.transactionData != null && Object.hasOwnProperty.call(m, "transactionData"))
                w.uint32(42).string(m.transactionData);
            return w;
        };

        SendPaymentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.SendPaymentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.noteMessage = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.requestMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.background = WAE2E.PaymentBackground.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.transactionData = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SendPaymentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.SendPaymentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.SendPaymentMessage();
            if (d.noteMessage != null) {
                if (typeof d.noteMessage !== "object")
                    throw TypeError(".WAE2E.SendPaymentMessage.noteMessage: object expected");
                m.noteMessage = WAE2E.Message.fromObject(d.noteMessage, n + 1);
            }
            if (d.requestMessageKey != null) {
                if (typeof d.requestMessageKey !== "object")
                    throw TypeError(".WAE2E.SendPaymentMessage.requestMessageKey: object expected");
                m.requestMessageKey = WACommon.MessageKey.fromObject(d.requestMessageKey, n + 1);
            }
            if (d.background != null) {
                if (typeof d.background !== "object")
                    throw TypeError(".WAE2E.SendPaymentMessage.background: object expected");
                m.background = WAE2E.PaymentBackground.fromObject(d.background, n + 1);
            }
            if (d.transactionData != null) {
                m.transactionData = String(d.transactionData);
            }
            return m;
        };

        SendPaymentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.noteMessage != null && m.hasOwnProperty("noteMessage")) {
                d.noteMessage = WAE2E.Message.toObject(m.noteMessage, o);
                if (o.oneofs)
                    d._noteMessage = "noteMessage";
            }
            if (m.requestMessageKey != null && m.hasOwnProperty("requestMessageKey")) {
                d.requestMessageKey = WACommon.MessageKey.toObject(m.requestMessageKey, o);
                if (o.oneofs)
                    d._requestMessageKey = "requestMessageKey";
            }
            if (m.background != null && m.hasOwnProperty("background")) {
                d.background = WAE2E.PaymentBackground.toObject(m.background, o);
                if (o.oneofs)
                    d._background = "background";
            }
            if (m.transactionData != null && m.hasOwnProperty("transactionData")) {
                d.transactionData = m.transactionData;
                if (o.oneofs)
                    d._transactionData = "transactionData";
            }
            return d;
        };

        SendPaymentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SendPaymentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.SendPaymentMessage";
        };

        return SendPaymentMessage;
    })();

    WAE2E.ContactsArrayMessage = (function() {

        function ContactsArrayMessage(p) {
            this.contacts = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ContactsArrayMessage.prototype.displayName = null;
        ContactsArrayMessage.prototype.contacts = $util.emptyArray;
        ContactsArrayMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactsArrayMessage.prototype, "_displayName", {
            get: $util.oneOfGetter($oneOfFields = ["displayName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactsArrayMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ContactsArrayMessage.create = function create(properties) {
            return new ContactsArrayMessage(properties);
        };

        ContactsArrayMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.displayName != null && Object.hasOwnProperty.call(m, "displayName"))
                w.uint32(10).string(m.displayName);
            if (m.contacts != null && m.contacts.length) {
                for (var i = 0; i < m.contacts.length; ++i)
                    WAE2E.ContactMessage.encode(m.contacts[i], w.uint32(18).fork()).ldelim();
            }
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        ContactsArrayMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContactsArrayMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.displayName = r.string();
                        break;
                    }
                case 2: {
                        if (!(m.contacts && m.contacts.length))
                            m.contacts = [];
                        m.contacts.push(WAE2E.ContactMessage.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ContactsArrayMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ContactsArrayMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ContactsArrayMessage();
            if (d.displayName != null) {
                m.displayName = String(d.displayName);
            }
            if (d.contacts) {
                if (!Array.isArray(d.contacts))
                    throw TypeError(".WAE2E.ContactsArrayMessage.contacts: array expected");
                m.contacts = [];
                for (var i = 0; i < d.contacts.length; ++i) {
                    if (typeof d.contacts[i] !== "object")
                        throw TypeError(".WAE2E.ContactsArrayMessage.contacts: object expected");
                    m.contacts[i] = WAE2E.ContactMessage.fromObject(d.contacts[i], n + 1);
                }
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ContactsArrayMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        ContactsArrayMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.contacts = [];
            }
            if (m.displayName != null && m.hasOwnProperty("displayName")) {
                d.displayName = m.displayName;
                if (o.oneofs)
                    d._displayName = "displayName";
            }
            if (m.contacts && m.contacts.length) {
                d.contacts = [];
                for (var j = 0; j < m.contacts.length; ++j) {
                    d.contacts[j] = WAE2E.ContactMessage.toObject(m.contacts[j], o);
                }
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        ContactsArrayMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContactsArrayMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ContactsArrayMessage";
        };

        return ContactsArrayMessage;
    })();

    WAE2E.InitialSecurityNotificationSettingSync = (function() {

        function InitialSecurityNotificationSettingSync(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InitialSecurityNotificationSettingSync.prototype.securityNotificationEnabled = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InitialSecurityNotificationSettingSync.prototype, "_securityNotificationEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["securityNotificationEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InitialSecurityNotificationSettingSync.create = function create(properties) {
            return new InitialSecurityNotificationSettingSync(properties);
        };

        InitialSecurityNotificationSettingSync.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.securityNotificationEnabled != null && Object.hasOwnProperty.call(m, "securityNotificationEnabled"))
                w.uint32(8).bool(m.securityNotificationEnabled);
            return w;
        };

        InitialSecurityNotificationSettingSync.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.InitialSecurityNotificationSettingSync();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.securityNotificationEnabled = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InitialSecurityNotificationSettingSync.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.InitialSecurityNotificationSettingSync)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.InitialSecurityNotificationSettingSync();
            if (d.securityNotificationEnabled != null) {
                m.securityNotificationEnabled = Boolean(d.securityNotificationEnabled);
            }
            return m;
        };

        InitialSecurityNotificationSettingSync.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.securityNotificationEnabled != null && m.hasOwnProperty("securityNotificationEnabled")) {
                d.securityNotificationEnabled = m.securityNotificationEnabled;
                if (o.oneofs)
                    d._securityNotificationEnabled = "securityNotificationEnabled";
            }
            return d;
        };

        InitialSecurityNotificationSettingSync.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InitialSecurityNotificationSettingSync.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.InitialSecurityNotificationSettingSync";
        };

        return InitialSecurityNotificationSettingSync;
    })();

    WAE2E.FullHistorySyncOnDemandConfig = (function() {

        function FullHistorySyncOnDemandConfig(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        FullHistorySyncOnDemandConfig.prototype.historyFromTimestamp = null;
        FullHistorySyncOnDemandConfig.prototype.historyDurationDays = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyFromTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["historyFromTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyDurationDays", {
            get: $util.oneOfGetter($oneOfFields = ["historyDurationDays"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FullHistorySyncOnDemandConfig.create = function create(properties) {
            return new FullHistorySyncOnDemandConfig(properties);
        };

        FullHistorySyncOnDemandConfig.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.historyFromTimestamp != null && Object.hasOwnProperty.call(m, "historyFromTimestamp"))
                w.uint32(8).uint64(m.historyFromTimestamp);
            if (m.historyDurationDays != null && Object.hasOwnProperty.call(m, "historyDurationDays"))
                w.uint32(16).uint32(m.historyDurationDays);
            return w;
        };

        FullHistorySyncOnDemandConfig.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.FullHistorySyncOnDemandConfig();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.historyFromTimestamp = r.uint64();
                        break;
                    }
                case 2: {
                        m.historyDurationDays = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        FullHistorySyncOnDemandConfig.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.FullHistorySyncOnDemandConfig)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.FullHistorySyncOnDemandConfig();
            if (d.historyFromTimestamp != null) {
                if ($util.Long)
                    (m.historyFromTimestamp = $util.Long.fromValue(d.historyFromTimestamp)).unsigned = true;
                else if (typeof d.historyFromTimestamp === "string")
                    m.historyFromTimestamp = parseInt(d.historyFromTimestamp, 10);
                else if (typeof d.historyFromTimestamp === "number")
                    m.historyFromTimestamp = d.historyFromTimestamp;
                else if (typeof d.historyFromTimestamp === "object")
                    m.historyFromTimestamp = new $util.LongBits(d.historyFromTimestamp.low >>> 0, d.historyFromTimestamp.high >>> 0).toNumber(true);
            }
            if (d.historyDurationDays != null) {
                m.historyDurationDays = d.historyDurationDays >>> 0;
            }
            return m;
        };

        FullHistorySyncOnDemandConfig.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.historyFromTimestamp != null && m.hasOwnProperty("historyFromTimestamp")) {
                if (typeof m.historyFromTimestamp === "number")
                    d.historyFromTimestamp = o.longs === String ? String(m.historyFromTimestamp) : m.historyFromTimestamp;
                else
                    d.historyFromTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.historyFromTimestamp) : o.longs === Number ? new $util.LongBits(m.historyFromTimestamp.low >>> 0, m.historyFromTimestamp.high >>> 0).toNumber(true) : m.historyFromTimestamp;
                if (o.oneofs)
                    d._historyFromTimestamp = "historyFromTimestamp";
            }
            if (m.historyDurationDays != null && m.hasOwnProperty("historyDurationDays")) {
                d.historyDurationDays = m.historyDurationDays;
                if (o.oneofs)
                    d._historyDurationDays = "historyDurationDays";
            }
            return d;
        };

        FullHistorySyncOnDemandConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FullHistorySyncOnDemandConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.FullHistorySyncOnDemandConfig";
        };

        return FullHistorySyncOnDemandConfig;
    })();

    WAE2E.FullHistorySyncOnDemandRequestMetadata = (function() {

        function FullHistorySyncOnDemandRequestMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        FullHistorySyncOnDemandRequestMetadata.prototype.requestID = null;
        FullHistorySyncOnDemandRequestMetadata.prototype.businessProduct = null;
        FullHistorySyncOnDemandRequestMetadata.prototype.opaqueClientData = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_requestID", {
            get: $util.oneOfGetter($oneOfFields = ["requestID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_businessProduct", {
            get: $util.oneOfGetter($oneOfFields = ["businessProduct"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_opaqueClientData", {
            get: $util.oneOfGetter($oneOfFields = ["opaqueClientData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FullHistorySyncOnDemandRequestMetadata.create = function create(properties) {
            return new FullHistorySyncOnDemandRequestMetadata(properties);
        };

        FullHistorySyncOnDemandRequestMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.requestID != null && Object.hasOwnProperty.call(m, "requestID"))
                w.uint32(10).string(m.requestID);
            if (m.businessProduct != null && Object.hasOwnProperty.call(m, "businessProduct"))
                w.uint32(18).string(m.businessProduct);
            if (m.opaqueClientData != null && Object.hasOwnProperty.call(m, "opaqueClientData"))
                w.uint32(26).bytes(m.opaqueClientData);
            return w;
        };

        FullHistorySyncOnDemandRequestMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.FullHistorySyncOnDemandRequestMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.requestID = r.string();
                        break;
                    }
                case 2: {
                        m.businessProduct = r.string();
                        break;
                    }
                case 3: {
                        m.opaqueClientData = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        FullHistorySyncOnDemandRequestMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.FullHistorySyncOnDemandRequestMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.FullHistorySyncOnDemandRequestMetadata();
            if (d.requestID != null) {
                m.requestID = String(d.requestID);
            }
            if (d.businessProduct != null) {
                m.businessProduct = String(d.businessProduct);
            }
            if (d.opaqueClientData != null) {
                if (typeof d.opaqueClientData === "string")
                    $util.base64.decode(d.opaqueClientData, m.opaqueClientData = $util.newBuffer($util.base64.length(d.opaqueClientData)), 0);
                else if (d.opaqueClientData.length >= 0)
                    m.opaqueClientData = d.opaqueClientData;
            }
            return m;
        };

        FullHistorySyncOnDemandRequestMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.requestID != null && m.hasOwnProperty("requestID")) {
                d.requestID = m.requestID;
                if (o.oneofs)
                    d._requestID = "requestID";
            }
            if (m.businessProduct != null && m.hasOwnProperty("businessProduct")) {
                d.businessProduct = m.businessProduct;
                if (o.oneofs)
                    d._businessProduct = "businessProduct";
            }
            if (m.opaqueClientData != null && m.hasOwnProperty("opaqueClientData")) {
                d.opaqueClientData = o.bytes === String ? $util.base64.encode(m.opaqueClientData, 0, m.opaqueClientData.length) : o.bytes === Array ? Array.prototype.slice.call(m.opaqueClientData) : m.opaqueClientData;
                if (o.oneofs)
                    d._opaqueClientData = "opaqueClientData";
            }
            return d;
        };

        FullHistorySyncOnDemandRequestMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        FullHistorySyncOnDemandRequestMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.FullHistorySyncOnDemandRequestMetadata";
        };

        return FullHistorySyncOnDemandRequestMetadata;
    })();

    WAE2E.AppStateFatalExceptionNotification = (function() {

        function AppStateFatalExceptionNotification(p) {
            this.collectionNames = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateFatalExceptionNotification.prototype.collectionNames = $util.emptyArray;
        AppStateFatalExceptionNotification.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateFatalExceptionNotification.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AppStateFatalExceptionNotification.create = function create(properties) {
            return new AppStateFatalExceptionNotification(properties);
        };

        AppStateFatalExceptionNotification.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.collectionNames != null && m.collectionNames.length) {
                for (var i = 0; i < m.collectionNames.length; ++i)
                    w.uint32(10).string(m.collectionNames[i]);
            }
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).int64(m.timestamp);
            return w;
        };

        AppStateFatalExceptionNotification.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateFatalExceptionNotification();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.collectionNames && m.collectionNames.length))
                            m.collectionNames = [];
                        m.collectionNames.push(r.string());
                        break;
                    }
                case 2: {
                        m.timestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateFatalExceptionNotification.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateFatalExceptionNotification)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateFatalExceptionNotification();
            if (d.collectionNames) {
                if (!Array.isArray(d.collectionNames))
                    throw TypeError(".WAE2E.AppStateFatalExceptionNotification.collectionNames: array expected");
                m.collectionNames = [];
                for (var i = 0; i < d.collectionNames.length; ++i) {
                    m.collectionNames[i] = String(d.collectionNames[i]);
                }
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = false;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            return m;
        };

        AppStateFatalExceptionNotification.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.collectionNames = [];
            }
            if (m.collectionNames && m.collectionNames.length) {
                d.collectionNames = [];
                for (var j = 0; j < m.collectionNames.length; ++j) {
                    d.collectionNames[j] = m.collectionNames[j];
                }
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            return d;
        };

        AppStateFatalExceptionNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateFatalExceptionNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateFatalExceptionNotification";
        };

        return AppStateFatalExceptionNotification;
    })();

    WAE2E.AppStateSyncKeyRequest = (function() {

        function AppStateSyncKeyRequest(p) {
            this.keyIDs = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKeyRequest.prototype.keyIDs = $util.emptyArray;

        AppStateSyncKeyRequest.create = function create(properties) {
            return new AppStateSyncKeyRequest(properties);
        };

        AppStateSyncKeyRequest.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keyIDs != null && m.keyIDs.length) {
                for (var i = 0; i < m.keyIDs.length; ++i)
                    WAE2E.AppStateSyncKeyId.encode(m.keyIDs[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        AppStateSyncKeyRequest.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKeyRequest();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.keyIDs && m.keyIDs.length))
                            m.keyIDs = [];
                        m.keyIDs.push(WAE2E.AppStateSyncKeyId.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKeyRequest.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKeyRequest)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKeyRequest();
            if (d.keyIDs) {
                if (!Array.isArray(d.keyIDs))
                    throw TypeError(".WAE2E.AppStateSyncKeyRequest.keyIDs: array expected");
                m.keyIDs = [];
                for (var i = 0; i < d.keyIDs.length; ++i) {
                    if (typeof d.keyIDs[i] !== "object")
                        throw TypeError(".WAE2E.AppStateSyncKeyRequest.keyIDs: object expected");
                    m.keyIDs[i] = WAE2E.AppStateSyncKeyId.fromObject(d.keyIDs[i], n + 1);
                }
            }
            return m;
        };

        AppStateSyncKeyRequest.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.keyIDs = [];
            }
            if (m.keyIDs && m.keyIDs.length) {
                d.keyIDs = [];
                for (var j = 0; j < m.keyIDs.length; ++j) {
                    d.keyIDs[j] = WAE2E.AppStateSyncKeyId.toObject(m.keyIDs[j], o);
                }
            }
            return d;
        };

        AppStateSyncKeyRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKeyRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKeyRequest";
        };

        return AppStateSyncKeyRequest;
    })();

    WAE2E.AppStateSyncKeyShare = (function() {

        function AppStateSyncKeyShare(p) {
            this.keys = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKeyShare.prototype.keys = $util.emptyArray;

        AppStateSyncKeyShare.create = function create(properties) {
            return new AppStateSyncKeyShare(properties);
        };

        AppStateSyncKeyShare.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keys != null && m.keys.length) {
                for (var i = 0; i < m.keys.length; ++i)
                    WAE2E.AppStateSyncKey.encode(m.keys[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        AppStateSyncKeyShare.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKeyShare();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.keys && m.keys.length))
                            m.keys = [];
                        m.keys.push(WAE2E.AppStateSyncKey.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKeyShare.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKeyShare)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKeyShare();
            if (d.keys) {
                if (!Array.isArray(d.keys))
                    throw TypeError(".WAE2E.AppStateSyncKeyShare.keys: array expected");
                m.keys = [];
                for (var i = 0; i < d.keys.length; ++i) {
                    if (typeof d.keys[i] !== "object")
                        throw TypeError(".WAE2E.AppStateSyncKeyShare.keys: object expected");
                    m.keys[i] = WAE2E.AppStateSyncKey.fromObject(d.keys[i], n + 1);
                }
            }
            return m;
        };

        AppStateSyncKeyShare.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.keys = [];
            }
            if (m.keys && m.keys.length) {
                d.keys = [];
                for (var j = 0; j < m.keys.length; ++j) {
                    d.keys[j] = WAE2E.AppStateSyncKey.toObject(m.keys[j], o);
                }
            }
            return d;
        };

        AppStateSyncKeyShare.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKeyShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKeyShare";
        };

        return AppStateSyncKeyShare;
    })();

    WAE2E.AppStateSyncKeyData = (function() {

        function AppStateSyncKeyData(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKeyData.prototype.keyData = null;
        AppStateSyncKeyData.prototype.fingerprint = null;
        AppStateSyncKeyData.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyData.prototype, "_keyData", {
            get: $util.oneOfGetter($oneOfFields = ["keyData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyData.prototype, "_fingerprint", {
            get: $util.oneOfGetter($oneOfFields = ["fingerprint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyData.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AppStateSyncKeyData.create = function create(properties) {
            return new AppStateSyncKeyData(properties);
        };

        AppStateSyncKeyData.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keyData != null && Object.hasOwnProperty.call(m, "keyData"))
                w.uint32(10).bytes(m.keyData);
            if (m.fingerprint != null && Object.hasOwnProperty.call(m, "fingerprint"))
                WAE2E.AppStateSyncKeyFingerprint.encode(m.fingerprint, w.uint32(18).fork()).ldelim();
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(24).int64(m.timestamp);
            return w;
        };

        AppStateSyncKeyData.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKeyData();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.keyData = r.bytes();
                        break;
                    }
                case 2: {
                        m.fingerprint = WAE2E.AppStateSyncKeyFingerprint.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.timestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKeyData.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKeyData)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKeyData();
            if (d.keyData != null) {
                if (typeof d.keyData === "string")
                    $util.base64.decode(d.keyData, m.keyData = $util.newBuffer($util.base64.length(d.keyData)), 0);
                else if (d.keyData.length >= 0)
                    m.keyData = d.keyData;
            }
            if (d.fingerprint != null) {
                if (typeof d.fingerprint !== "object")
                    throw TypeError(".WAE2E.AppStateSyncKeyData.fingerprint: object expected");
                m.fingerprint = WAE2E.AppStateSyncKeyFingerprint.fromObject(d.fingerprint, n + 1);
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = false;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            return m;
        };

        AppStateSyncKeyData.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.keyData != null && m.hasOwnProperty("keyData")) {
                d.keyData = o.bytes === String ? $util.base64.encode(m.keyData, 0, m.keyData.length) : o.bytes === Array ? Array.prototype.slice.call(m.keyData) : m.keyData;
                if (o.oneofs)
                    d._keyData = "keyData";
            }
            if (m.fingerprint != null && m.hasOwnProperty("fingerprint")) {
                d.fingerprint = WAE2E.AppStateSyncKeyFingerprint.toObject(m.fingerprint, o);
                if (o.oneofs)
                    d._fingerprint = "fingerprint";
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            return d;
        };

        AppStateSyncKeyData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKeyData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKeyData";
        };

        return AppStateSyncKeyData;
    })();

    WAE2E.AppStateSyncKeyFingerprint = (function() {

        function AppStateSyncKeyFingerprint(p) {
            this.deviceIndexes = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKeyFingerprint.prototype.rawID = null;
        AppStateSyncKeyFingerprint.prototype.currentIndex = null;
        AppStateSyncKeyFingerprint.prototype.deviceIndexes = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_rawID", {
            get: $util.oneOfGetter($oneOfFields = ["rawID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_currentIndex", {
            get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AppStateSyncKeyFingerprint.create = function create(properties) {
            return new AppStateSyncKeyFingerprint(properties);
        };

        AppStateSyncKeyFingerprint.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.rawID != null && Object.hasOwnProperty.call(m, "rawID"))
                w.uint32(8).uint32(m.rawID);
            if (m.currentIndex != null && Object.hasOwnProperty.call(m, "currentIndex"))
                w.uint32(16).uint32(m.currentIndex);
            if (m.deviceIndexes != null && m.deviceIndexes.length) {
                w.uint32(26).fork();
                for (var i = 0; i < m.deviceIndexes.length; ++i)
                    w.uint32(m.deviceIndexes[i]);
                w.ldelim();
            }
            return w;
        };

        AppStateSyncKeyFingerprint.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKeyFingerprint();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.rawID = r.uint32();
                        break;
                    }
                case 2: {
                        m.currentIndex = r.uint32();
                        break;
                    }
                case 3: {
                        if (!(m.deviceIndexes && m.deviceIndexes.length))
                            m.deviceIndexes = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.deviceIndexes.push(r.uint32());
                        } else
                            m.deviceIndexes.push(r.uint32());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKeyFingerprint.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKeyFingerprint)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKeyFingerprint();
            if (d.rawID != null) {
                m.rawID = d.rawID >>> 0;
            }
            if (d.currentIndex != null) {
                m.currentIndex = d.currentIndex >>> 0;
            }
            if (d.deviceIndexes) {
                if (!Array.isArray(d.deviceIndexes))
                    throw TypeError(".WAE2E.AppStateSyncKeyFingerprint.deviceIndexes: array expected");
                m.deviceIndexes = [];
                for (var i = 0; i < d.deviceIndexes.length; ++i) {
                    m.deviceIndexes[i] = d.deviceIndexes[i] >>> 0;
                }
            }
            return m;
        };

        AppStateSyncKeyFingerprint.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.deviceIndexes = [];
            }
            if (m.rawID != null && m.hasOwnProperty("rawID")) {
                d.rawID = m.rawID;
                if (o.oneofs)
                    d._rawID = "rawID";
            }
            if (m.currentIndex != null && m.hasOwnProperty("currentIndex")) {
                d.currentIndex = m.currentIndex;
                if (o.oneofs)
                    d._currentIndex = "currentIndex";
            }
            if (m.deviceIndexes && m.deviceIndexes.length) {
                d.deviceIndexes = [];
                for (var j = 0; j < m.deviceIndexes.length; ++j) {
                    d.deviceIndexes[j] = m.deviceIndexes[j];
                }
            }
            return d;
        };

        AppStateSyncKeyFingerprint.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKeyFingerprint.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKeyFingerprint";
        };

        return AppStateSyncKeyFingerprint;
    })();

    WAE2E.AppStateSyncKeyId = (function() {

        function AppStateSyncKeyId(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKeyId.prototype.keyID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKeyId.prototype, "_keyID", {
            get: $util.oneOfGetter($oneOfFields = ["keyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AppStateSyncKeyId.create = function create(properties) {
            return new AppStateSyncKeyId(properties);
        };

        AppStateSyncKeyId.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keyID != null && Object.hasOwnProperty.call(m, "keyID"))
                w.uint32(10).bytes(m.keyID);
            return w;
        };

        AppStateSyncKeyId.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKeyId();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.keyID = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKeyId.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKeyId)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKeyId();
            if (d.keyID != null) {
                if (typeof d.keyID === "string")
                    $util.base64.decode(d.keyID, m.keyID = $util.newBuffer($util.base64.length(d.keyID)), 0);
                else if (d.keyID.length >= 0)
                    m.keyID = d.keyID;
            }
            return m;
        };

        AppStateSyncKeyId.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.keyID != null && m.hasOwnProperty("keyID")) {
                d.keyID = o.bytes === String ? $util.base64.encode(m.keyID, 0, m.keyID.length) : o.bytes === Array ? Array.prototype.slice.call(m.keyID) : m.keyID;
                if (o.oneofs)
                    d._keyID = "keyID";
            }
            return d;
        };

        AppStateSyncKeyId.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKeyId.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKeyId";
        };

        return AppStateSyncKeyId;
    })();

    WAE2E.AppStateSyncKey = (function() {

        function AppStateSyncKey(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AppStateSyncKey.prototype.keyID = null;
        AppStateSyncKey.prototype.keyData = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKey.prototype, "_keyID", {
            get: $util.oneOfGetter($oneOfFields = ["keyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AppStateSyncKey.prototype, "_keyData", {
            get: $util.oneOfGetter($oneOfFields = ["keyData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AppStateSyncKey.create = function create(properties) {
            return new AppStateSyncKey(properties);
        };

        AppStateSyncKey.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keyID != null && Object.hasOwnProperty.call(m, "keyID"))
                WAE2E.AppStateSyncKeyId.encode(m.keyID, w.uint32(10).fork()).ldelim();
            if (m.keyData != null && Object.hasOwnProperty.call(m, "keyData"))
                WAE2E.AppStateSyncKeyData.encode(m.keyData, w.uint32(18).fork()).ldelim();
            return w;
        };

        AppStateSyncKey.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AppStateSyncKey();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.keyID = WAE2E.AppStateSyncKeyId.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.keyData = WAE2E.AppStateSyncKeyData.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AppStateSyncKey.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AppStateSyncKey)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AppStateSyncKey();
            if (d.keyID != null) {
                if (typeof d.keyID !== "object")
                    throw TypeError(".WAE2E.AppStateSyncKey.keyID: object expected");
                m.keyID = WAE2E.AppStateSyncKeyId.fromObject(d.keyID, n + 1);
            }
            if (d.keyData != null) {
                if (typeof d.keyData !== "object")
                    throw TypeError(".WAE2E.AppStateSyncKey.keyData: object expected");
                m.keyData = WAE2E.AppStateSyncKeyData.fromObject(d.keyData, n + 1);
            }
            return m;
        };

        AppStateSyncKey.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.keyID != null && m.hasOwnProperty("keyID")) {
                d.keyID = WAE2E.AppStateSyncKeyId.toObject(m.keyID, o);
                if (o.oneofs)
                    d._keyID = "keyID";
            }
            if (m.keyData != null && m.hasOwnProperty("keyData")) {
                d.keyData = WAE2E.AppStateSyncKeyData.toObject(m.keyData, o);
                if (o.oneofs)
                    d._keyData = "keyData";
            }
            return d;
        };

        AppStateSyncKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AppStateSyncKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AppStateSyncKey";
        };

        return AppStateSyncKey;
    })();

    WAE2E.HistorySyncNotification = (function() {

        function HistorySyncNotification(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        HistorySyncNotification.prototype.fileSHA256 = null;
        HistorySyncNotification.prototype.fileLength = null;
        HistorySyncNotification.prototype.mediaKey = null;
        HistorySyncNotification.prototype.fileEncSHA256 = null;
        HistorySyncNotification.prototype.directPath = null;
        HistorySyncNotification.prototype.syncType = null;
        HistorySyncNotification.prototype.chunkOrder = null;
        HistorySyncNotification.prototype.originalMessageID = null;
        HistorySyncNotification.prototype.progress = null;
        HistorySyncNotification.prototype.oldestMsgInChunkTimestampSec = null;
        HistorySyncNotification.prototype.initialHistBootstrapInlinePayload = null;
        HistorySyncNotification.prototype.peerDataRequestSessionID = null;
        HistorySyncNotification.prototype.fullHistorySyncOnDemandRequestMetadata = null;
        HistorySyncNotification.prototype.encHandle = null;
        HistorySyncNotification.prototype.messageAccessStatus = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_syncType", {
            get: $util.oneOfGetter($oneOfFields = ["syncType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_chunkOrder", {
            get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_originalMessageID", {
            get: $util.oneOfGetter($oneOfFields = ["originalMessageID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_progress", {
            get: $util.oneOfGetter($oneOfFields = ["progress"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_oldestMsgInChunkTimestampSec", {
            get: $util.oneOfGetter($oneOfFields = ["oldestMsgInChunkTimestampSec"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_initialHistBootstrapInlinePayload", {
            get: $util.oneOfGetter($oneOfFields = ["initialHistBootstrapInlinePayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_peerDataRequestSessionID", {
            get: $util.oneOfGetter($oneOfFields = ["peerDataRequestSessionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_fullHistorySyncOnDemandRequestMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_encHandle", {
            get: $util.oneOfGetter($oneOfFields = ["encHandle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncNotification.prototype, "_messageAccessStatus", {
            get: $util.oneOfGetter($oneOfFields = ["messageAccessStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HistorySyncNotification.create = function create(properties) {
            return new HistorySyncNotification(properties);
        };

        HistorySyncNotification.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(10).bytes(m.fileSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(16).uint64(m.fileLength);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(26).bytes(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(34).bytes(m.fileEncSHA256);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(42).string(m.directPath);
            if (m.syncType != null && Object.hasOwnProperty.call(m, "syncType"))
                w.uint32(48).int32(m.syncType);
            if (m.chunkOrder != null && Object.hasOwnProperty.call(m, "chunkOrder"))
                w.uint32(56).uint32(m.chunkOrder);
            if (m.originalMessageID != null && Object.hasOwnProperty.call(m, "originalMessageID"))
                w.uint32(66).string(m.originalMessageID);
            if (m.progress != null && Object.hasOwnProperty.call(m, "progress"))
                w.uint32(72).uint32(m.progress);
            if (m.oldestMsgInChunkTimestampSec != null && Object.hasOwnProperty.call(m, "oldestMsgInChunkTimestampSec"))
                w.uint32(80).int64(m.oldestMsgInChunkTimestampSec);
            if (m.initialHistBootstrapInlinePayload != null && Object.hasOwnProperty.call(m, "initialHistBootstrapInlinePayload"))
                w.uint32(90).bytes(m.initialHistBootstrapInlinePayload);
            if (m.peerDataRequestSessionID != null && Object.hasOwnProperty.call(m, "peerDataRequestSessionID"))
                w.uint32(98).string(m.peerDataRequestSessionID);
            if (m.fullHistorySyncOnDemandRequestMetadata != null && Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestMetadata"))
                WAE2E.FullHistorySyncOnDemandRequestMetadata.encode(m.fullHistorySyncOnDemandRequestMetadata, w.uint32(106).fork()).ldelim();
            if (m.encHandle != null && Object.hasOwnProperty.call(m, "encHandle"))
                w.uint32(114).string(m.encHandle);
            if (m.messageAccessStatus != null && Object.hasOwnProperty.call(m, "messageAccessStatus"))
                WAE2E.HistorySyncMessageAccessStatus.encode(m.messageAccessStatus, w.uint32(122).fork()).ldelim();
            return w;
        };

        HistorySyncNotification.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HistorySyncNotification();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 2: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 3: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 4: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 5: {
                        m.directPath = r.string();
                        break;
                    }
                case 6: {
                        m.syncType = r.int32();
                        break;
                    }
                case 7: {
                        m.chunkOrder = r.uint32();
                        break;
                    }
                case 8: {
                        m.originalMessageID = r.string();
                        break;
                    }
                case 9: {
                        m.progress = r.uint32();
                        break;
                    }
                case 10: {
                        m.oldestMsgInChunkTimestampSec = r.int64();
                        break;
                    }
                case 11: {
                        m.initialHistBootstrapInlinePayload = r.bytes();
                        break;
                    }
                case 12: {
                        m.peerDataRequestSessionID = r.string();
                        break;
                    }
                case 13: {
                        m.fullHistorySyncOnDemandRequestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 14: {
                        m.encHandle = r.string();
                        break;
                    }
                case 15: {
                        m.messageAccessStatus = WAE2E.HistorySyncMessageAccessStatus.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        HistorySyncNotification.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.HistorySyncNotification)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.HistorySyncNotification();
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            switch (d.syncType) {
            default:
                if (typeof d.syncType === "number") {
                    m.syncType = d.syncType;
                    break;
                }
                break;
            case "INITIAL_BOOTSTRAP":
            case 0:
                m.syncType = 0;
                break;
            case "INITIAL_STATUS_V3":
            case 1:
                m.syncType = 1;
                break;
            case "FULL":
            case 2:
                m.syncType = 2;
                break;
            case "RECENT":
            case 3:
                m.syncType = 3;
                break;
            case "PUSH_NAME":
            case 4:
                m.syncType = 4;
                break;
            case "NON_BLOCKING_DATA":
            case 5:
                m.syncType = 5;
                break;
            case "ON_DEMAND":
            case 6:
                m.syncType = 6;
                break;
            case "NO_HISTORY":
            case 7:
                m.syncType = 7;
                break;
            case "MESSAGE_ACCESS_STATUS":
            case 8:
                m.syncType = 8;
                break;
            }
            if (d.chunkOrder != null) {
                m.chunkOrder = d.chunkOrder >>> 0;
            }
            if (d.originalMessageID != null) {
                m.originalMessageID = String(d.originalMessageID);
            }
            if (d.progress != null) {
                m.progress = d.progress >>> 0;
            }
            if (d.oldestMsgInChunkTimestampSec != null) {
                if ($util.Long)
                    (m.oldestMsgInChunkTimestampSec = $util.Long.fromValue(d.oldestMsgInChunkTimestampSec)).unsigned = false;
                else if (typeof d.oldestMsgInChunkTimestampSec === "string")
                    m.oldestMsgInChunkTimestampSec = parseInt(d.oldestMsgInChunkTimestampSec, 10);
                else if (typeof d.oldestMsgInChunkTimestampSec === "number")
                    m.oldestMsgInChunkTimestampSec = d.oldestMsgInChunkTimestampSec;
                else if (typeof d.oldestMsgInChunkTimestampSec === "object")
                    m.oldestMsgInChunkTimestampSec = new $util.LongBits(d.oldestMsgInChunkTimestampSec.low >>> 0, d.oldestMsgInChunkTimestampSec.high >>> 0).toNumber();
            }
            if (d.initialHistBootstrapInlinePayload != null) {
                if (typeof d.initialHistBootstrapInlinePayload === "string")
                    $util.base64.decode(d.initialHistBootstrapInlinePayload, m.initialHistBootstrapInlinePayload = $util.newBuffer($util.base64.length(d.initialHistBootstrapInlinePayload)), 0);
                else if (d.initialHistBootstrapInlinePayload.length >= 0)
                    m.initialHistBootstrapInlinePayload = d.initialHistBootstrapInlinePayload;
            }
            if (d.peerDataRequestSessionID != null) {
                m.peerDataRequestSessionID = String(d.peerDataRequestSessionID);
            }
            if (d.fullHistorySyncOnDemandRequestMetadata != null) {
                if (typeof d.fullHistorySyncOnDemandRequestMetadata !== "object")
                    throw TypeError(".WAE2E.HistorySyncNotification.fullHistorySyncOnDemandRequestMetadata: object expected");
                m.fullHistorySyncOnDemandRequestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.fromObject(d.fullHistorySyncOnDemandRequestMetadata, n + 1);
            }
            if (d.encHandle != null) {
                m.encHandle = String(d.encHandle);
            }
            if (d.messageAccessStatus != null) {
                if (typeof d.messageAccessStatus !== "object")
                    throw TypeError(".WAE2E.HistorySyncNotification.messageAccessStatus: object expected");
                m.messageAccessStatus = WAE2E.HistorySyncMessageAccessStatus.fromObject(d.messageAccessStatus, n + 1);
            }
            return m;
        };

        HistorySyncNotification.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.syncType != null && m.hasOwnProperty("syncType")) {
                d.syncType = o.enums === String ? WAE2E.HistorySyncType[m.syncType] === undefined ? m.syncType : WAE2E.HistorySyncType[m.syncType] : m.syncType;
                if (o.oneofs)
                    d._syncType = "syncType";
            }
            if (m.chunkOrder != null && m.hasOwnProperty("chunkOrder")) {
                d.chunkOrder = m.chunkOrder;
                if (o.oneofs)
                    d._chunkOrder = "chunkOrder";
            }
            if (m.originalMessageID != null && m.hasOwnProperty("originalMessageID")) {
                d.originalMessageID = m.originalMessageID;
                if (o.oneofs)
                    d._originalMessageID = "originalMessageID";
            }
            if (m.progress != null && m.hasOwnProperty("progress")) {
                d.progress = m.progress;
                if (o.oneofs)
                    d._progress = "progress";
            }
            if (m.oldestMsgInChunkTimestampSec != null && m.hasOwnProperty("oldestMsgInChunkTimestampSec")) {
                if (typeof m.oldestMsgInChunkTimestampSec === "number")
                    d.oldestMsgInChunkTimestampSec = o.longs === String ? String(m.oldestMsgInChunkTimestampSec) : m.oldestMsgInChunkTimestampSec;
                else
                    d.oldestMsgInChunkTimestampSec = o.longs === String ? $util.Long.prototype.toString.call(m.oldestMsgInChunkTimestampSec) : o.longs === Number ? new $util.LongBits(m.oldestMsgInChunkTimestampSec.low >>> 0, m.oldestMsgInChunkTimestampSec.high >>> 0).toNumber() : m.oldestMsgInChunkTimestampSec;
                if (o.oneofs)
                    d._oldestMsgInChunkTimestampSec = "oldestMsgInChunkTimestampSec";
            }
            if (m.initialHistBootstrapInlinePayload != null && m.hasOwnProperty("initialHistBootstrapInlinePayload")) {
                d.initialHistBootstrapInlinePayload = o.bytes === String ? $util.base64.encode(m.initialHistBootstrapInlinePayload, 0, m.initialHistBootstrapInlinePayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.initialHistBootstrapInlinePayload) : m.initialHistBootstrapInlinePayload;
                if (o.oneofs)
                    d._initialHistBootstrapInlinePayload = "initialHistBootstrapInlinePayload";
            }
            if (m.peerDataRequestSessionID != null && m.hasOwnProperty("peerDataRequestSessionID")) {
                d.peerDataRequestSessionID = m.peerDataRequestSessionID;
                if (o.oneofs)
                    d._peerDataRequestSessionID = "peerDataRequestSessionID";
            }
            if (m.fullHistorySyncOnDemandRequestMetadata != null && m.hasOwnProperty("fullHistorySyncOnDemandRequestMetadata")) {
                d.fullHistorySyncOnDemandRequestMetadata = WAE2E.FullHistorySyncOnDemandRequestMetadata.toObject(m.fullHistorySyncOnDemandRequestMetadata, o);
                if (o.oneofs)
                    d._fullHistorySyncOnDemandRequestMetadata = "fullHistorySyncOnDemandRequestMetadata";
            }
            if (m.encHandle != null && m.hasOwnProperty("encHandle")) {
                d.encHandle = m.encHandle;
                if (o.oneofs)
                    d._encHandle = "encHandle";
            }
            if (m.messageAccessStatus != null && m.hasOwnProperty("messageAccessStatus")) {
                d.messageAccessStatus = WAE2E.HistorySyncMessageAccessStatus.toObject(m.messageAccessStatus, o);
                if (o.oneofs)
                    d._messageAccessStatus = "messageAccessStatus";
            }
            return d;
        };

        HistorySyncNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HistorySyncNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.HistorySyncNotification";
        };

        return HistorySyncNotification;
    })();

    WAE2E.HistorySyncMessageAccessStatus = (function() {

        function HistorySyncMessageAccessStatus(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        HistorySyncMessageAccessStatus.prototype.completeAccessGranted = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(HistorySyncMessageAccessStatus.prototype, "_completeAccessGranted", {
            get: $util.oneOfGetter($oneOfFields = ["completeAccessGranted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HistorySyncMessageAccessStatus.create = function create(properties) {
            return new HistorySyncMessageAccessStatus(properties);
        };

        HistorySyncMessageAccessStatus.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.completeAccessGranted != null && Object.hasOwnProperty.call(m, "completeAccessGranted"))
                w.uint32(8).bool(m.completeAccessGranted);
            return w;
        };

        HistorySyncMessageAccessStatus.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.HistorySyncMessageAccessStatus();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.completeAccessGranted = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        HistorySyncMessageAccessStatus.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.HistorySyncMessageAccessStatus)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.HistorySyncMessageAccessStatus();
            if (d.completeAccessGranted != null) {
                m.completeAccessGranted = Boolean(d.completeAccessGranted);
            }
            return m;
        };

        HistorySyncMessageAccessStatus.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.completeAccessGranted != null && m.hasOwnProperty("completeAccessGranted")) {
                d.completeAccessGranted = m.completeAccessGranted;
                if (o.oneofs)
                    d._completeAccessGranted = "completeAccessGranted";
            }
            return d;
        };

        HistorySyncMessageAccessStatus.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        HistorySyncMessageAccessStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.HistorySyncMessageAccessStatus";
        };

        return HistorySyncMessageAccessStatus;
    })();

    WAE2E.ChatStockImageWallpaper = (function() {

        function ChatStockImageWallpaper(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ChatStockImageWallpaper.prototype.stockImageID = null;
        ChatStockImageWallpaper.prototype.dimLevel = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatStockImageWallpaper.prototype, "_stockImageID", {
            get: $util.oneOfGetter($oneOfFields = ["stockImageID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatStockImageWallpaper.prototype, "_dimLevel", {
            get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatStockImageWallpaper.create = function create(properties) {
            return new ChatStockImageWallpaper(properties);
        };

        ChatStockImageWallpaper.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stockImageID != null && Object.hasOwnProperty.call(m, "stockImageID"))
                w.uint32(10).string(m.stockImageID);
            if (m.dimLevel != null && Object.hasOwnProperty.call(m, "dimLevel"))
                w.uint32(21).float(m.dimLevel);
            return w;
        };

        ChatStockImageWallpaper.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ChatStockImageWallpaper();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stockImageID = r.string();
                        break;
                    }
                case 2: {
                        m.dimLevel = r.float();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ChatStockImageWallpaper.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ChatStockImageWallpaper)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ChatStockImageWallpaper();
            if (d.stockImageID != null) {
                m.stockImageID = String(d.stockImageID);
            }
            if (d.dimLevel != null) {
                m.dimLevel = Number(d.dimLevel);
            }
            return m;
        };

        ChatStockImageWallpaper.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.stockImageID != null && m.hasOwnProperty("stockImageID")) {
                d.stockImageID = m.stockImageID;
                if (o.oneofs)
                    d._stockImageID = "stockImageID";
            }
            if (m.dimLevel != null && m.hasOwnProperty("dimLevel")) {
                d.dimLevel = o.json && !isFinite(m.dimLevel) ? String(m.dimLevel) : m.dimLevel;
                if (o.oneofs)
                    d._dimLevel = "dimLevel";
            }
            return d;
        };

        ChatStockImageWallpaper.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatStockImageWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ChatStockImageWallpaper";
        };

        return ChatStockImageWallpaper;
    })();

    WAE2E.ChatSolidColorWallpaper = (function() {

        function ChatSolidColorWallpaper(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ChatSolidColorWallpaper.prototype.colorLight = null;
        ChatSolidColorWallpaper.prototype.colorDark = null;
        ChatSolidColorWallpaper.prototype.isDoodleEnabled = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorLight", {
            get: $util.oneOfGetter($oneOfFields = ["colorLight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorDark", {
            get: $util.oneOfGetter($oneOfFields = ["colorDark"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatSolidColorWallpaper.prototype, "_isDoodleEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatSolidColorWallpaper.create = function create(properties) {
            return new ChatSolidColorWallpaper(properties);
        };

        ChatSolidColorWallpaper.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.colorLight != null && Object.hasOwnProperty.call(m, "colorLight"))
                w.uint32(10).string(m.colorLight);
            if (m.colorDark != null && Object.hasOwnProperty.call(m, "colorDark"))
                w.uint32(18).string(m.colorDark);
            if (m.isDoodleEnabled != null && Object.hasOwnProperty.call(m, "isDoodleEnabled"))
                w.uint32(24).bool(m.isDoodleEnabled);
            return w;
        };

        ChatSolidColorWallpaper.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ChatSolidColorWallpaper();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.colorLight = r.string();
                        break;
                    }
                case 2: {
                        m.colorDark = r.string();
                        break;
                    }
                case 3: {
                        m.isDoodleEnabled = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ChatSolidColorWallpaper.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ChatSolidColorWallpaper)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ChatSolidColorWallpaper();
            if (d.colorLight != null) {
                m.colorLight = String(d.colorLight);
            }
            if (d.colorDark != null) {
                m.colorDark = String(d.colorDark);
            }
            if (d.isDoodleEnabled != null) {
                m.isDoodleEnabled = Boolean(d.isDoodleEnabled);
            }
            return m;
        };

        ChatSolidColorWallpaper.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.colorLight != null && m.hasOwnProperty("colorLight")) {
                d.colorLight = m.colorLight;
                if (o.oneofs)
                    d._colorLight = "colorLight";
            }
            if (m.colorDark != null && m.hasOwnProperty("colorDark")) {
                d.colorDark = m.colorDark;
                if (o.oneofs)
                    d._colorDark = "colorDark";
            }
            if (m.isDoodleEnabled != null && m.hasOwnProperty("isDoodleEnabled")) {
                d.isDoodleEnabled = m.isDoodleEnabled;
                if (o.oneofs)
                    d._isDoodleEnabled = "isDoodleEnabled";
            }
            return d;
        };

        ChatSolidColorWallpaper.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatSolidColorWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ChatSolidColorWallpaper";
        };

        return ChatSolidColorWallpaper;
    })();

    WAE2E.ChatDefaultWallpaper = (function() {

        function ChatDefaultWallpaper(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ChatDefaultWallpaper.prototype.isDoodleEnabled = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatDefaultWallpaper.prototype, "_isDoodleEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatDefaultWallpaper.create = function create(properties) {
            return new ChatDefaultWallpaper(properties);
        };

        ChatDefaultWallpaper.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.isDoodleEnabled != null && Object.hasOwnProperty.call(m, "isDoodleEnabled"))
                w.uint32(8).bool(m.isDoodleEnabled);
            return w;
        };

        ChatDefaultWallpaper.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ChatDefaultWallpaper();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.isDoodleEnabled = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ChatDefaultWallpaper.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ChatDefaultWallpaper)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ChatDefaultWallpaper();
            if (d.isDoodleEnabled != null) {
                m.isDoodleEnabled = Boolean(d.isDoodleEnabled);
            }
            return m;
        };

        ChatDefaultWallpaper.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.isDoodleEnabled != null && m.hasOwnProperty("isDoodleEnabled")) {
                d.isDoodleEnabled = m.isDoodleEnabled;
                if (o.oneofs)
                    d._isDoodleEnabled = "isDoodleEnabled";
            }
            return d;
        };

        ChatDefaultWallpaper.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatDefaultWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ChatDefaultWallpaper";
        };

        return ChatDefaultWallpaper;
    })();

    WAE2E.ChatCustomImageWallpaper = (function() {

        function ChatCustomImageWallpaper(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ChatCustomImageWallpaper.prototype.directPath = null;
        ChatCustomImageWallpaper.prototype.mediaKey = null;
        ChatCustomImageWallpaper.prototype.fileEncSHA256 = null;
        ChatCustomImageWallpaper.prototype.fileSHA256 = null;
        ChatCustomImageWallpaper.prototype.dimLevel = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatCustomImageWallpaper.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatCustomImageWallpaper.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatCustomImageWallpaper.prototype, "_dimLevel", {
            get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatCustomImageWallpaper.create = function create(properties) {
            return new ChatCustomImageWallpaper(properties);
        };

        ChatCustomImageWallpaper.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(10).string(m.directPath);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(18).bytes(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(26).bytes(m.fileEncSHA256);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(34).bytes(m.fileSHA256);
            if (m.dimLevel != null && Object.hasOwnProperty.call(m, "dimLevel"))
                w.uint32(45).float(m.dimLevel);
            return w;
        };

        ChatCustomImageWallpaper.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ChatCustomImageWallpaper();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.directPath = r.string();
                        break;
                    }
                case 2: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 3: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 4: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 5: {
                        m.dimLevel = r.float();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ChatCustomImageWallpaper.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ChatCustomImageWallpaper)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ChatCustomImageWallpaper();
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.dimLevel != null) {
                m.dimLevel = Number(d.dimLevel);
            }
            return m;
        };

        ChatCustomImageWallpaper.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.dimLevel != null && m.hasOwnProperty("dimLevel")) {
                d.dimLevel = o.json && !isFinite(m.dimLevel) ? String(m.dimLevel) : m.dimLevel;
                if (o.oneofs)
                    d._dimLevel = "dimLevel";
            }
            return d;
        };

        ChatCustomImageWallpaper.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatCustomImageWallpaper.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ChatCustomImageWallpaper";
        };

        return ChatCustomImageWallpaper;
    })();

    WAE2E.ChatThemeSetting = (function() {

        function ChatThemeSetting(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ChatThemeSetting.prototype.defaultWallpaper = null;
        ChatThemeSetting.prototype.solidColor = null;
        ChatThemeSetting.prototype.stockImage = null;
        ChatThemeSetting.prototype.customImage = null;
        ChatThemeSetting.prototype.settingTimestampMS = null;
        ChatThemeSetting.prototype.clearTheme = null;
        ChatThemeSetting.prototype.colorSchemeID = null;

        let $oneOfFields;

        Object.defineProperty(ChatThemeSetting.prototype, "wallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["defaultWallpaper", "solidColor", "stockImage", "customImage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatThemeSetting.prototype, "_settingTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["settingTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatThemeSetting.prototype, "_clearTheme", {
            get: $util.oneOfGetter($oneOfFields = ["clearTheme"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ChatThemeSetting.prototype, "_colorSchemeID", {
            get: $util.oneOfGetter($oneOfFields = ["colorSchemeID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatThemeSetting.create = function create(properties) {
            return new ChatThemeSetting(properties);
        };

        ChatThemeSetting.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.settingTimestampMS != null && Object.hasOwnProperty.call(m, "settingTimestampMS"))
                w.uint32(8).int64(m.settingTimestampMS);
            if (m.clearTheme != null && Object.hasOwnProperty.call(m, "clearTheme"))
                w.uint32(16).bool(m.clearTheme);
            if (m.colorSchemeID != null && Object.hasOwnProperty.call(m, "colorSchemeID"))
                w.uint32(26).string(m.colorSchemeID);
            if (m.defaultWallpaper != null && Object.hasOwnProperty.call(m, "defaultWallpaper"))
                WAE2E.ChatDefaultWallpaper.encode(m.defaultWallpaper, w.uint32(82).fork()).ldelim();
            if (m.solidColor != null && Object.hasOwnProperty.call(m, "solidColor"))
                WAE2E.ChatSolidColorWallpaper.encode(m.solidColor, w.uint32(90).fork()).ldelim();
            if (m.stockImage != null && Object.hasOwnProperty.call(m, "stockImage"))
                WAE2E.ChatStockImageWallpaper.encode(m.stockImage, w.uint32(98).fork()).ldelim();
            if (m.customImage != null && Object.hasOwnProperty.call(m, "customImage"))
                WAE2E.ChatCustomImageWallpaper.encode(m.customImage, w.uint32(106).fork()).ldelim();
            return w;
        };

        ChatThemeSetting.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ChatThemeSetting();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 10: {
                        m.defaultWallpaper = WAE2E.ChatDefaultWallpaper.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.solidColor = WAE2E.ChatSolidColorWallpaper.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 12: {
                        m.stockImage = WAE2E.ChatStockImageWallpaper.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 13: {
                        m.customImage = WAE2E.ChatCustomImageWallpaper.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        m.settingTimestampMS = r.int64();
                        break;
                    }
                case 2: {
                        m.clearTheme = r.bool();
                        break;
                    }
                case 3: {
                        m.colorSchemeID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ChatThemeSetting.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ChatThemeSetting)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ChatThemeSetting();
            if (d.defaultWallpaper != null) {
                if (typeof d.defaultWallpaper !== "object")
                    throw TypeError(".WAE2E.ChatThemeSetting.defaultWallpaper: object expected");
                m.defaultWallpaper = WAE2E.ChatDefaultWallpaper.fromObject(d.defaultWallpaper, n + 1);
            }
            if (d.solidColor != null) {
                if (typeof d.solidColor !== "object")
                    throw TypeError(".WAE2E.ChatThemeSetting.solidColor: object expected");
                m.solidColor = WAE2E.ChatSolidColorWallpaper.fromObject(d.solidColor, n + 1);
            }
            if (d.stockImage != null) {
                if (typeof d.stockImage !== "object")
                    throw TypeError(".WAE2E.ChatThemeSetting.stockImage: object expected");
                m.stockImage = WAE2E.ChatStockImageWallpaper.fromObject(d.stockImage, n + 1);
            }
            if (d.customImage != null) {
                if (typeof d.customImage !== "object")
                    throw TypeError(".WAE2E.ChatThemeSetting.customImage: object expected");
                m.customImage = WAE2E.ChatCustomImageWallpaper.fromObject(d.customImage, n + 1);
            }
            if (d.settingTimestampMS != null) {
                if ($util.Long)
                    (m.settingTimestampMS = $util.Long.fromValue(d.settingTimestampMS)).unsigned = false;
                else if (typeof d.settingTimestampMS === "string")
                    m.settingTimestampMS = parseInt(d.settingTimestampMS, 10);
                else if (typeof d.settingTimestampMS === "number")
                    m.settingTimestampMS = d.settingTimestampMS;
                else if (typeof d.settingTimestampMS === "object")
                    m.settingTimestampMS = new $util.LongBits(d.settingTimestampMS.low >>> 0, d.settingTimestampMS.high >>> 0).toNumber();
            }
            if (d.clearTheme != null) {
                m.clearTheme = Boolean(d.clearTheme);
            }
            if (d.colorSchemeID != null) {
                m.colorSchemeID = String(d.colorSchemeID);
            }
            return m;
        };

        ChatThemeSetting.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.settingTimestampMS != null && m.hasOwnProperty("settingTimestampMS")) {
                if (typeof m.settingTimestampMS === "number")
                    d.settingTimestampMS = o.longs === String ? String(m.settingTimestampMS) : m.settingTimestampMS;
                else
                    d.settingTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.settingTimestampMS) : o.longs === Number ? new $util.LongBits(m.settingTimestampMS.low >>> 0, m.settingTimestampMS.high >>> 0).toNumber() : m.settingTimestampMS;
                if (o.oneofs)
                    d._settingTimestampMS = "settingTimestampMS";
            }
            if (m.clearTheme != null && m.hasOwnProperty("clearTheme")) {
                d.clearTheme = m.clearTheme;
                if (o.oneofs)
                    d._clearTheme = "clearTheme";
            }
            if (m.colorSchemeID != null && m.hasOwnProperty("colorSchemeID")) {
                d.colorSchemeID = m.colorSchemeID;
                if (o.oneofs)
                    d._colorSchemeID = "colorSchemeID";
            }
            if (m.defaultWallpaper != null && m.hasOwnProperty("defaultWallpaper")) {
                d.defaultWallpaper = WAE2E.ChatDefaultWallpaper.toObject(m.defaultWallpaper, o);
                if (o.oneofs)
                    d.wallpaper = "defaultWallpaper";
            }
            if (m.solidColor != null && m.hasOwnProperty("solidColor")) {
                d.solidColor = WAE2E.ChatSolidColorWallpaper.toObject(m.solidColor, o);
                if (o.oneofs)
                    d.wallpaper = "solidColor";
            }
            if (m.stockImage != null && m.hasOwnProperty("stockImage")) {
                d.stockImage = WAE2E.ChatStockImageWallpaper.toObject(m.stockImage, o);
                if (o.oneofs)
                    d.wallpaper = "stockImage";
            }
            if (m.customImage != null && m.hasOwnProperty("customImage")) {
                d.customImage = WAE2E.ChatCustomImageWallpaper.toObject(m.customImage, o);
                if (o.oneofs)
                    d.wallpaper = "customImage";
            }
            return d;
        };

        ChatThemeSetting.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatThemeSetting.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ChatThemeSetting";
        };

        return ChatThemeSetting;
    })();

    WAE2E.Chat = (function() {

        function Chat(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Chat.prototype.displayName = null;
        Chat.prototype.ID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Chat.prototype, "_displayName", {
            get: $util.oneOfGetter($oneOfFields = ["displayName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Chat.prototype, "_ID", {
            get: $util.oneOfGetter($oneOfFields = ["ID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Chat.create = function create(properties) {
            return new Chat(properties);
        };

        Chat.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.displayName != null && Object.hasOwnProperty.call(m, "displayName"))
                w.uint32(10).string(m.displayName);
            if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                w.uint32(18).string(m.ID);
            return w;
        };

        Chat.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Chat();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.displayName = r.string();
                        break;
                    }
                case 2: {
                        m.ID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Chat.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Chat)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Chat();
            if (d.displayName != null) {
                m.displayName = String(d.displayName);
            }
            if (d.ID != null) {
                m.ID = String(d.ID);
            }
            return m;
        };

        Chat.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.displayName != null && m.hasOwnProperty("displayName")) {
                d.displayName = m.displayName;
                if (o.oneofs)
                    d._displayName = "displayName";
            }
            if (m.ID != null && m.hasOwnProperty("ID")) {
                d.ID = m.ID;
                if (o.oneofs)
                    d._ID = "ID";
            }
            return d;
        };

        Chat.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Chat.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Chat";
        };

        return Chat;
    })();

    WAE2E.Call = (function() {

        function Call(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Call.prototype.callKey = null;
        Call.prototype.conversionSource = null;
        Call.prototype.conversionData = null;
        Call.prototype.conversionDelaySeconds = null;
        Call.prototype.ctwaSignals = null;
        Call.prototype.ctwaPayload = null;
        Call.prototype.contextInfo = null;
        Call.prototype.nativeFlowCallButtonPayload = null;
        Call.prototype.deeplinkPayload = null;
        Call.prototype.messageContextInfo = null;
        Call.prototype.callEntryPoint = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_callKey", {
            get: $util.oneOfGetter($oneOfFields = ["callKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_conversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_conversionData", {
            get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_conversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_ctwaSignals", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_ctwaPayload", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_nativeFlowCallButtonPayload", {
            get: $util.oneOfGetter($oneOfFields = ["nativeFlowCallButtonPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_deeplinkPayload", {
            get: $util.oneOfGetter($oneOfFields = ["deeplinkPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_messageContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Call.prototype, "_callEntryPoint", {
            get: $util.oneOfGetter($oneOfFields = ["callEntryPoint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Call.create = function create(properties) {
            return new Call(properties);
        };

        Call.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.callKey != null && Object.hasOwnProperty.call(m, "callKey"))
                w.uint32(10).bytes(m.callKey);
            if (m.conversionSource != null && Object.hasOwnProperty.call(m, "conversionSource"))
                w.uint32(18).string(m.conversionSource);
            if (m.conversionData != null && Object.hasOwnProperty.call(m, "conversionData"))
                w.uint32(26).bytes(m.conversionData);
            if (m.conversionDelaySeconds != null && Object.hasOwnProperty.call(m, "conversionDelaySeconds"))
                w.uint32(32).uint32(m.conversionDelaySeconds);
            if (m.ctwaSignals != null && Object.hasOwnProperty.call(m, "ctwaSignals"))
                w.uint32(42).string(m.ctwaSignals);
            if (m.ctwaPayload != null && Object.hasOwnProperty.call(m, "ctwaPayload"))
                w.uint32(50).bytes(m.ctwaPayload);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(58).fork()).ldelim();
            if (m.nativeFlowCallButtonPayload != null && Object.hasOwnProperty.call(m, "nativeFlowCallButtonPayload"))
                w.uint32(66).string(m.nativeFlowCallButtonPayload);
            if (m.deeplinkPayload != null && Object.hasOwnProperty.call(m, "deeplinkPayload"))
                w.uint32(74).string(m.deeplinkPayload);
            if (m.messageContextInfo != null && Object.hasOwnProperty.call(m, "messageContextInfo"))
                WAE2E.MessageContextInfo.encode(m.messageContextInfo, w.uint32(82).fork()).ldelim();
            if (m.callEntryPoint != null && Object.hasOwnProperty.call(m, "callEntryPoint"))
                w.uint32(88).uint32(m.callEntryPoint);
            return w;
        };

        Call.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Call();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.callKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.conversionSource = r.string();
                        break;
                    }
                case 3: {
                        m.conversionData = r.bytes();
                        break;
                    }
                case 4: {
                        m.conversionDelaySeconds = r.uint32();
                        break;
                    }
                case 5: {
                        m.ctwaSignals = r.string();
                        break;
                    }
                case 6: {
                        m.ctwaPayload = r.bytes();
                        break;
                    }
                case 7: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.nativeFlowCallButtonPayload = r.string();
                        break;
                    }
                case 9: {
                        m.deeplinkPayload = r.string();
                        break;
                    }
                case 10: {
                        m.messageContextInfo = WAE2E.MessageContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.callEntryPoint = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Call.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Call)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Call();
            if (d.callKey != null) {
                if (typeof d.callKey === "string")
                    $util.base64.decode(d.callKey, m.callKey = $util.newBuffer($util.base64.length(d.callKey)), 0);
                else if (d.callKey.length >= 0)
                    m.callKey = d.callKey;
            }
            if (d.conversionSource != null) {
                m.conversionSource = String(d.conversionSource);
            }
            if (d.conversionData != null) {
                if (typeof d.conversionData === "string")
                    $util.base64.decode(d.conversionData, m.conversionData = $util.newBuffer($util.base64.length(d.conversionData)), 0);
                else if (d.conversionData.length >= 0)
                    m.conversionData = d.conversionData;
            }
            if (d.conversionDelaySeconds != null) {
                m.conversionDelaySeconds = d.conversionDelaySeconds >>> 0;
            }
            if (d.ctwaSignals != null) {
                m.ctwaSignals = String(d.ctwaSignals);
            }
            if (d.ctwaPayload != null) {
                if (typeof d.ctwaPayload === "string")
                    $util.base64.decode(d.ctwaPayload, m.ctwaPayload = $util.newBuffer($util.base64.length(d.ctwaPayload)), 0);
                else if (d.ctwaPayload.length >= 0)
                    m.ctwaPayload = d.ctwaPayload;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.Call.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.nativeFlowCallButtonPayload != null) {
                m.nativeFlowCallButtonPayload = String(d.nativeFlowCallButtonPayload);
            }
            if (d.deeplinkPayload != null) {
                m.deeplinkPayload = String(d.deeplinkPayload);
            }
            if (d.messageContextInfo != null) {
                if (typeof d.messageContextInfo !== "object")
                    throw TypeError(".WAE2E.Call.messageContextInfo: object expected");
                m.messageContextInfo = WAE2E.MessageContextInfo.fromObject(d.messageContextInfo, n + 1);
            }
            if (d.callEntryPoint != null) {
                m.callEntryPoint = d.callEntryPoint >>> 0;
            }
            return m;
        };

        Call.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.callKey != null && m.hasOwnProperty("callKey")) {
                d.callKey = o.bytes === String ? $util.base64.encode(m.callKey, 0, m.callKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.callKey) : m.callKey;
                if (o.oneofs)
                    d._callKey = "callKey";
            }
            if (m.conversionSource != null && m.hasOwnProperty("conversionSource")) {
                d.conversionSource = m.conversionSource;
                if (o.oneofs)
                    d._conversionSource = "conversionSource";
            }
            if (m.conversionData != null && m.hasOwnProperty("conversionData")) {
                d.conversionData = o.bytes === String ? $util.base64.encode(m.conversionData, 0, m.conversionData.length) : o.bytes === Array ? Array.prototype.slice.call(m.conversionData) : m.conversionData;
                if (o.oneofs)
                    d._conversionData = "conversionData";
            }
            if (m.conversionDelaySeconds != null && m.hasOwnProperty("conversionDelaySeconds")) {
                d.conversionDelaySeconds = m.conversionDelaySeconds;
                if (o.oneofs)
                    d._conversionDelaySeconds = "conversionDelaySeconds";
            }
            if (m.ctwaSignals != null && m.hasOwnProperty("ctwaSignals")) {
                d.ctwaSignals = m.ctwaSignals;
                if (o.oneofs)
                    d._ctwaSignals = "ctwaSignals";
            }
            if (m.ctwaPayload != null && m.hasOwnProperty("ctwaPayload")) {
                d.ctwaPayload = o.bytes === String ? $util.base64.encode(m.ctwaPayload, 0, m.ctwaPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.ctwaPayload) : m.ctwaPayload;
                if (o.oneofs)
                    d._ctwaPayload = "ctwaPayload";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.nativeFlowCallButtonPayload != null && m.hasOwnProperty("nativeFlowCallButtonPayload")) {
                d.nativeFlowCallButtonPayload = m.nativeFlowCallButtonPayload;
                if (o.oneofs)
                    d._nativeFlowCallButtonPayload = "nativeFlowCallButtonPayload";
            }
            if (m.deeplinkPayload != null && m.hasOwnProperty("deeplinkPayload")) {
                d.deeplinkPayload = m.deeplinkPayload;
                if (o.oneofs)
                    d._deeplinkPayload = "deeplinkPayload";
            }
            if (m.messageContextInfo != null && m.hasOwnProperty("messageContextInfo")) {
                d.messageContextInfo = WAE2E.MessageContextInfo.toObject(m.messageContextInfo, o);
                if (o.oneofs)
                    d._messageContextInfo = "messageContextInfo";
            }
            if (m.callEntryPoint != null && m.hasOwnProperty("callEntryPoint")) {
                d.callEntryPoint = m.callEntryPoint;
                if (o.oneofs)
                    d._callEntryPoint = "callEntryPoint";
            }
            return d;
        };

        Call.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Call.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Call";
        };

        return Call;
    })();

    WAE2E.AudioMessage = (function() {

        function AudioMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AudioMessage.prototype.URL = null;
        AudioMessage.prototype.mimetype = null;
        AudioMessage.prototype.fileSHA256 = null;
        AudioMessage.prototype.fileLength = null;
        AudioMessage.prototype.seconds = null;
        AudioMessage.prototype.PTT = null;
        AudioMessage.prototype.mediaKey = null;
        AudioMessage.prototype.fileEncSHA256 = null;
        AudioMessage.prototype.directPath = null;
        AudioMessage.prototype.mediaKeyTimestamp = null;
        AudioMessage.prototype.contextInfo = null;
        AudioMessage.prototype.streamingSidecar = null;
        AudioMessage.prototype.waveform = null;
        AudioMessage.prototype.backgroundArgb = null;
        AudioMessage.prototype.viewOnce = null;
        AudioMessage.prototype.accessibilityLabel = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_seconds", {
            get: $util.oneOfGetter($oneOfFields = ["seconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_PTT", {
            get: $util.oneOfGetter($oneOfFields = ["PTT"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_streamingSidecar", {
            get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_waveform", {
            get: $util.oneOfGetter($oneOfFields = ["waveform"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_backgroundArgb", {
            get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_viewOnce", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AudioMessage.prototype, "_accessibilityLabel", {
            get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AudioMessage.create = function create(properties) {
            return new AudioMessage(properties);
        };

        AudioMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(18).string(m.mimetype);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(26).bytes(m.fileSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(32).uint64(m.fileLength);
            if (m.seconds != null && Object.hasOwnProperty.call(m, "seconds"))
                w.uint32(40).uint32(m.seconds);
            if (m.PTT != null && Object.hasOwnProperty.call(m, "PTT"))
                w.uint32(48).bool(m.PTT);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(58).bytes(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(66).bytes(m.fileEncSHA256);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(74).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(80).int64(m.mediaKeyTimestamp);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.streamingSidecar != null && Object.hasOwnProperty.call(m, "streamingSidecar"))
                w.uint32(146).bytes(m.streamingSidecar);
            if (m.waveform != null && Object.hasOwnProperty.call(m, "waveform"))
                w.uint32(154).bytes(m.waveform);
            if (m.backgroundArgb != null && Object.hasOwnProperty.call(m, "backgroundArgb"))
                w.uint32(165).fixed32(m.backgroundArgb);
            if (m.viewOnce != null && Object.hasOwnProperty.call(m, "viewOnce"))
                w.uint32(168).bool(m.viewOnce);
            if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                w.uint32(178).string(m.accessibilityLabel);
            return w;
        };

        AudioMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AudioMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.mimetype = r.string();
                        break;
                    }
                case 3: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 4: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 5: {
                        m.seconds = r.uint32();
                        break;
                    }
                case 6: {
                        m.PTT = r.bool();
                        break;
                    }
                case 7: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 8: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 9: {
                        m.directPath = r.string();
                        break;
                    }
                case 10: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.streamingSidecar = r.bytes();
                        break;
                    }
                case 19: {
                        m.waveform = r.bytes();
                        break;
                    }
                case 20: {
                        m.backgroundArgb = r.fixed32();
                        break;
                    }
                case 21: {
                        m.viewOnce = r.bool();
                        break;
                    }
                case 22: {
                        m.accessibilityLabel = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AudioMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AudioMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AudioMessage();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.seconds != null) {
                m.seconds = d.seconds >>> 0;
            }
            if (d.PTT != null) {
                m.PTT = Boolean(d.PTT);
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.AudioMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.streamingSidecar != null) {
                if (typeof d.streamingSidecar === "string")
                    $util.base64.decode(d.streamingSidecar, m.streamingSidecar = $util.newBuffer($util.base64.length(d.streamingSidecar)), 0);
                else if (d.streamingSidecar.length >= 0)
                    m.streamingSidecar = d.streamingSidecar;
            }
            if (d.waveform != null) {
                if (typeof d.waveform === "string")
                    $util.base64.decode(d.waveform, m.waveform = $util.newBuffer($util.base64.length(d.waveform)), 0);
                else if (d.waveform.length >= 0)
                    m.waveform = d.waveform;
            }
            if (d.backgroundArgb != null) {
                m.backgroundArgb = d.backgroundArgb >>> 0;
            }
            if (d.viewOnce != null) {
                m.viewOnce = Boolean(d.viewOnce);
            }
            if (d.accessibilityLabel != null) {
                m.accessibilityLabel = String(d.accessibilityLabel);
            }
            return m;
        };

        AudioMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.seconds != null && m.hasOwnProperty("seconds")) {
                d.seconds = m.seconds;
                if (o.oneofs)
                    d._seconds = "seconds";
            }
            if (m.PTT != null && m.hasOwnProperty("PTT")) {
                d.PTT = m.PTT;
                if (o.oneofs)
                    d._PTT = "PTT";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.streamingSidecar != null && m.hasOwnProperty("streamingSidecar")) {
                d.streamingSidecar = o.bytes === String ? $util.base64.encode(m.streamingSidecar, 0, m.streamingSidecar.length) : o.bytes === Array ? Array.prototype.slice.call(m.streamingSidecar) : m.streamingSidecar;
                if (o.oneofs)
                    d._streamingSidecar = "streamingSidecar";
            }
            if (m.waveform != null && m.hasOwnProperty("waveform")) {
                d.waveform = o.bytes === String ? $util.base64.encode(m.waveform, 0, m.waveform.length) : o.bytes === Array ? Array.prototype.slice.call(m.waveform) : m.waveform;
                if (o.oneofs)
                    d._waveform = "waveform";
            }
            if (m.backgroundArgb != null && m.hasOwnProperty("backgroundArgb")) {
                d.backgroundArgb = m.backgroundArgb;
                if (o.oneofs)
                    d._backgroundArgb = "backgroundArgb";
            }
            if (m.viewOnce != null && m.hasOwnProperty("viewOnce")) {
                d.viewOnce = m.viewOnce;
                if (o.oneofs)
                    d._viewOnce = "viewOnce";
            }
            if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                d.accessibilityLabel = m.accessibilityLabel;
                if (o.oneofs)
                    d._accessibilityLabel = "accessibilityLabel";
            }
            return d;
        };

        AudioMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AudioMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AudioMessage";
        };

        return AudioMessage;
    })();

    WAE2E.DocumentMessage = (function() {

        function DocumentMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DocumentMessage.prototype.URL = null;
        DocumentMessage.prototype.mimetype = null;
        DocumentMessage.prototype.title = null;
        DocumentMessage.prototype.fileSHA256 = null;
        DocumentMessage.prototype.fileLength = null;
        DocumentMessage.prototype.pageCount = null;
        DocumentMessage.prototype.mediaKey = null;
        DocumentMessage.prototype.fileName = null;
        DocumentMessage.prototype.fileEncSHA256 = null;
        DocumentMessage.prototype.directPath = null;
        DocumentMessage.prototype.mediaKeyTimestamp = null;
        DocumentMessage.prototype.contactVcard = null;
        DocumentMessage.prototype.thumbnailDirectPath = null;
        DocumentMessage.prototype.thumbnailSHA256 = null;
        DocumentMessage.prototype.thumbnailEncSHA256 = null;
        DocumentMessage.prototype.JPEGThumbnail = null;
        DocumentMessage.prototype.contextInfo = null;
        DocumentMessage.prototype.thumbnailHeight = null;
        DocumentMessage.prototype.thumbnailWidth = null;
        DocumentMessage.prototype.caption = null;
        DocumentMessage.prototype.accessibilityLabel = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_pageCount", {
            get: $util.oneOfGetter($oneOfFields = ["pageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_fileName", {
            get: $util.oneOfGetter($oneOfFields = ["fileName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_contactVcard", {
            get: $util.oneOfGetter($oneOfFields = ["contactVcard"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_thumbnailHeight", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_thumbnailWidth", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DocumentMessage.prototype, "_accessibilityLabel", {
            get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DocumentMessage.create = function create(properties) {
            return new DocumentMessage(properties);
        };

        DocumentMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(18).string(m.mimetype);
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(26).string(m.title);
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(34).bytes(m.fileSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(40).uint64(m.fileLength);
            if (m.pageCount != null && Object.hasOwnProperty.call(m, "pageCount"))
                w.uint32(48).uint32(m.pageCount);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(58).bytes(m.mediaKey);
            if (m.fileName != null && Object.hasOwnProperty.call(m, "fileName"))
                w.uint32(66).string(m.fileName);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(74).bytes(m.fileEncSHA256);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(82).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(88).int64(m.mediaKeyTimestamp);
            if (m.contactVcard != null && Object.hasOwnProperty.call(m, "contactVcard"))
                w.uint32(96).bool(m.contactVcard);
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(106).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(114).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(122).bytes(m.thumbnailEncSHA256);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.thumbnailHeight != null && Object.hasOwnProperty.call(m, "thumbnailHeight"))
                w.uint32(144).uint32(m.thumbnailHeight);
            if (m.thumbnailWidth != null && Object.hasOwnProperty.call(m, "thumbnailWidth"))
                w.uint32(152).uint32(m.thumbnailWidth);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(162).string(m.caption);
            if (m.accessibilityLabel != null && Object.hasOwnProperty.call(m, "accessibilityLabel"))
                w.uint32(170).string(m.accessibilityLabel);
            return w;
        };

        DocumentMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.DocumentMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.mimetype = r.string();
                        break;
                    }
                case 3: {
                        m.title = r.string();
                        break;
                    }
                case 4: {
                        m.fileSHA256 = r.bytes();
                        break;
                    }
                case 5: {
                        m.fileLength = r.uint64();
                        break;
                    }
                case 6: {
                        m.pageCount = r.uint32();
                        break;
                    }
                case 7: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 8: {
                        m.fileName = r.string();
                        break;
                    }
                case 9: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 10: {
                        m.directPath = r.string();
                        break;
                    }
                case 11: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 12: {
                        m.contactVcard = r.bool();
                        break;
                    }
                case 13: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 14: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 15: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.thumbnailHeight = r.uint32();
                        break;
                    }
                case 19: {
                        m.thumbnailWidth = r.uint32();
                        break;
                    }
                case 20: {
                        m.caption = r.string();
                        break;
                    }
                case 21: {
                        m.accessibilityLabel = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DocumentMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.DocumentMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.DocumentMessage();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.fileSHA256 != null) {
                if (typeof d.fileSHA256 === "string")
                    $util.base64.decode(d.fileSHA256, m.fileSHA256 = $util.newBuffer($util.base64.length(d.fileSHA256)), 0);
                else if (d.fileSHA256.length >= 0)
                    m.fileSHA256 = d.fileSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.pageCount != null) {
                m.pageCount = d.pageCount >>> 0;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileName != null) {
                m.fileName = String(d.fileName);
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.contactVcard != null) {
                m.contactVcard = Boolean(d.contactVcard);
            }
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.DocumentMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.thumbnailHeight != null) {
                m.thumbnailHeight = d.thumbnailHeight >>> 0;
            }
            if (d.thumbnailWidth != null) {
                m.thumbnailWidth = d.thumbnailWidth >>> 0;
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.accessibilityLabel != null) {
                m.accessibilityLabel = String(d.accessibilityLabel);
            }
            return m;
        };

        DocumentMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = o.bytes === String ? $util.base64.encode(m.fileSHA256, 0, m.fileSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileSHA256) : m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            if (m.pageCount != null && m.hasOwnProperty("pageCount")) {
                d.pageCount = m.pageCount;
                if (o.oneofs)
                    d._pageCount = "pageCount";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileName != null && m.hasOwnProperty("fileName")) {
                d.fileName = m.fileName;
                if (o.oneofs)
                    d._fileName = "fileName";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.contactVcard != null && m.hasOwnProperty("contactVcard")) {
                d.contactVcard = m.contactVcard;
                if (o.oneofs)
                    d._contactVcard = "contactVcard";
            }
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.thumbnailHeight != null && m.hasOwnProperty("thumbnailHeight")) {
                d.thumbnailHeight = m.thumbnailHeight;
                if (o.oneofs)
                    d._thumbnailHeight = "thumbnailHeight";
            }
            if (m.thumbnailWidth != null && m.hasOwnProperty("thumbnailWidth")) {
                d.thumbnailWidth = m.thumbnailWidth;
                if (o.oneofs)
                    d._thumbnailWidth = "thumbnailWidth";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.accessibilityLabel != null && m.hasOwnProperty("accessibilityLabel")) {
                d.accessibilityLabel = m.accessibilityLabel;
                if (o.oneofs)
                    d._accessibilityLabel = "accessibilityLabel";
            }
            return d;
        };

        DocumentMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DocumentMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.DocumentMessage";
        };

        return DocumentMessage;
    })();

    WAE2E.URLMetadata = (function() {

        function URLMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        URLMetadata.prototype.fbExperimentID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(URLMetadata.prototype, "_fbExperimentID", {
            get: $util.oneOfGetter($oneOfFields = ["fbExperimentID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        URLMetadata.create = function create(properties) {
            return new URLMetadata(properties);
        };

        URLMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.fbExperimentID != null && Object.hasOwnProperty.call(m, "fbExperimentID"))
                w.uint32(8).uint32(m.fbExperimentID);
            return w;
        };

        URLMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.URLMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.fbExperimentID = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        URLMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.URLMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.URLMetadata();
            if (d.fbExperimentID != null) {
                m.fbExperimentID = d.fbExperimentID >>> 0;
            }
            return m;
        };

        URLMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.fbExperimentID != null && m.hasOwnProperty("fbExperimentID")) {
                d.fbExperimentID = m.fbExperimentID;
                if (o.oneofs)
                    d._fbExperimentID = "fbExperimentID";
            }
            return d;
        };

        URLMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        URLMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.URLMetadata";
        };

        return URLMetadata;
    })();

    WAE2E.PaymentExtendedMetadata = (function() {

        function PaymentExtendedMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PaymentExtendedMetadata.prototype.type = null;
        PaymentExtendedMetadata.prototype.platform = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentExtendedMetadata.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PaymentExtendedMetadata.prototype, "_platform", {
            get: $util.oneOfGetter($oneOfFields = ["platform"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentExtendedMetadata.create = function create(properties) {
            return new PaymentExtendedMetadata(properties);
        };

        PaymentExtendedMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).uint32(m.type);
            if (m.platform != null && Object.hasOwnProperty.call(m, "platform"))
                w.uint32(18).string(m.platform);
            return w;
        };

        PaymentExtendedMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.PaymentExtendedMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.uint32();
                        break;
                    }
                case 2: {
                        m.platform = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PaymentExtendedMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.PaymentExtendedMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.PaymentExtendedMetadata();
            if (d.type != null) {
                m.type = d.type >>> 0;
            }
            if (d.platform != null) {
                m.platform = String(d.platform);
            }
            return m;
        };

        PaymentExtendedMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.platform != null && m.hasOwnProperty("platform")) {
                d.platform = m.platform;
                if (o.oneofs)
                    d._platform = "platform";
            }
            return d;
        };

        PaymentExtendedMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentExtendedMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.PaymentExtendedMetadata";
        };

        return PaymentExtendedMetadata;
    })();

    WAE2E.MMSThumbnailMetadata = (function() {

        function MMSThumbnailMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MMSThumbnailMetadata.prototype.thumbnailDirectPath = null;
        MMSThumbnailMetadata.prototype.thumbnailSHA256 = null;
        MMSThumbnailMetadata.prototype.thumbnailEncSHA256 = null;
        MMSThumbnailMetadata.prototype.mediaKey = null;
        MMSThumbnailMetadata.prototype.mediaKeyTimestamp = null;
        MMSThumbnailMetadata.prototype.thumbnailHeight = null;
        MMSThumbnailMetadata.prototype.thumbnailWidth = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailHeight", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailWidth", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MMSThumbnailMetadata.create = function create(properties) {
            return new MMSThumbnailMetadata(properties);
        };

        MMSThumbnailMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.thumbnailDirectPath != null && Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                w.uint32(10).string(m.thumbnailDirectPath);
            if (m.thumbnailSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailSHA256"))
                w.uint32(18).bytes(m.thumbnailSHA256);
            if (m.thumbnailEncSHA256 != null && Object.hasOwnProperty.call(m, "thumbnailEncSHA256"))
                w.uint32(26).bytes(m.thumbnailEncSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(34).bytes(m.mediaKey);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(40).int64(m.mediaKeyTimestamp);
            if (m.thumbnailHeight != null && Object.hasOwnProperty.call(m, "thumbnailHeight"))
                w.uint32(48).uint32(m.thumbnailHeight);
            if (m.thumbnailWidth != null && Object.hasOwnProperty.call(m, "thumbnailWidth"))
                w.uint32(56).uint32(m.thumbnailWidth);
            return w;
        };

        MMSThumbnailMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MMSThumbnailMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.thumbnailDirectPath = r.string();
                        break;
                    }
                case 2: {
                        m.thumbnailSHA256 = r.bytes();
                        break;
                    }
                case 3: {
                        m.thumbnailEncSHA256 = r.bytes();
                        break;
                    }
                case 4: {
                        m.mediaKey = r.bytes();
                        break;
                    }
                case 5: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 6: {
                        m.thumbnailHeight = r.uint32();
                        break;
                    }
                case 7: {
                        m.thumbnailWidth = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MMSThumbnailMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MMSThumbnailMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MMSThumbnailMetadata();
            if (d.thumbnailDirectPath != null) {
                m.thumbnailDirectPath = String(d.thumbnailDirectPath);
            }
            if (d.thumbnailSHA256 != null) {
                if (typeof d.thumbnailSHA256 === "string")
                    $util.base64.decode(d.thumbnailSHA256, m.thumbnailSHA256 = $util.newBuffer($util.base64.length(d.thumbnailSHA256)), 0);
                else if (d.thumbnailSHA256.length >= 0)
                    m.thumbnailSHA256 = d.thumbnailSHA256;
            }
            if (d.thumbnailEncSHA256 != null) {
                if (typeof d.thumbnailEncSHA256 === "string")
                    $util.base64.decode(d.thumbnailEncSHA256, m.thumbnailEncSHA256 = $util.newBuffer($util.base64.length(d.thumbnailEncSHA256)), 0);
                else if (d.thumbnailEncSHA256.length >= 0)
                    m.thumbnailEncSHA256 = d.thumbnailEncSHA256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.thumbnailHeight != null) {
                m.thumbnailHeight = d.thumbnailHeight >>> 0;
            }
            if (d.thumbnailWidth != null) {
                m.thumbnailWidth = d.thumbnailWidth >>> 0;
            }
            return m;
        };

        MMSThumbnailMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.thumbnailDirectPath != null && m.hasOwnProperty("thumbnailDirectPath")) {
                d.thumbnailDirectPath = m.thumbnailDirectPath;
                if (o.oneofs)
                    d._thumbnailDirectPath = "thumbnailDirectPath";
            }
            if (m.thumbnailSHA256 != null && m.hasOwnProperty("thumbnailSHA256")) {
                d.thumbnailSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailSHA256, 0, m.thumbnailSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailSHA256) : m.thumbnailSHA256;
                if (o.oneofs)
                    d._thumbnailSHA256 = "thumbnailSHA256";
            }
            if (m.thumbnailEncSHA256 != null && m.hasOwnProperty("thumbnailEncSHA256")) {
                d.thumbnailEncSHA256 = o.bytes === String ? $util.base64.encode(m.thumbnailEncSHA256, 0, m.thumbnailEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.thumbnailEncSHA256) : m.thumbnailEncSHA256;
                if (o.oneofs)
                    d._thumbnailEncSHA256 = "thumbnailEncSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = o.bytes === String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.thumbnailHeight != null && m.hasOwnProperty("thumbnailHeight")) {
                d.thumbnailHeight = m.thumbnailHeight;
                if (o.oneofs)
                    d._thumbnailHeight = "thumbnailHeight";
            }
            if (m.thumbnailWidth != null && m.hasOwnProperty("thumbnailWidth")) {
                d.thumbnailWidth = m.thumbnailWidth;
                if (o.oneofs)
                    d._thumbnailWidth = "thumbnailWidth";
            }
            return d;
        };

        MMSThumbnailMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MMSThumbnailMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MMSThumbnailMetadata";
        };

        return MMSThumbnailMetadata;
    })();

    WAE2E.LocationMessage = (function() {

        function LocationMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LocationMessage.prototype.degreesLatitude = null;
        LocationMessage.prototype.degreesLongitude = null;
        LocationMessage.prototype.name = null;
        LocationMessage.prototype.address = null;
        LocationMessage.prototype.URL = null;
        LocationMessage.prototype.isLive = null;
        LocationMessage.prototype.accuracyInMeters = null;
        LocationMessage.prototype.speedInMps = null;
        LocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;
        LocationMessage.prototype.comment = null;
        LocationMessage.prototype.JPEGThumbnail = null;
        LocationMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_degreesLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_degreesLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_address", {
            get: $util.oneOfGetter($oneOfFields = ["address"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_isLive", {
            get: $util.oneOfGetter($oneOfFields = ["isLive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_accuracyInMeters", {
            get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_speedInMps", {
            get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
            get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_comment", {
            get: $util.oneOfGetter($oneOfFields = ["comment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_JPEGThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["JPEGThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LocationMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LocationMessage.create = function create(properties) {
            return new LocationMessage(properties);
        };

        LocationMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.degreesLatitude != null && Object.hasOwnProperty.call(m, "degreesLatitude"))
                w.uint32(9).double(m.degreesLatitude);
            if (m.degreesLongitude != null && Object.hasOwnProperty.call(m, "degreesLongitude"))
                w.uint32(17).double(m.degreesLongitude);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            if (m.address != null && Object.hasOwnProperty.call(m, "address"))
                w.uint32(34).string(m.address);
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(42).string(m.URL);
            if (m.isLive != null && Object.hasOwnProperty.call(m, "isLive"))
                w.uint32(48).bool(m.isLive);
            if (m.accuracyInMeters != null && Object.hasOwnProperty.call(m, "accuracyInMeters"))
                w.uint32(56).uint32(m.accuracyInMeters);
            if (m.speedInMps != null && Object.hasOwnProperty.call(m, "speedInMps"))
                w.uint32(69).float(m.speedInMps);
            if (m.degreesClockwiseFromMagneticNorth != null && Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth"))
                w.uint32(72).uint32(m.degreesClockwiseFromMagneticNorth);
            if (m.comment != null && Object.hasOwnProperty.call(m, "comment"))
                w.uint32(90).string(m.comment);
            if (m.JPEGThumbnail != null && Object.hasOwnProperty.call(m, "JPEGThumbnail"))
                w.uint32(130).bytes(m.JPEGThumbnail);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            return w;
        };

        LocationMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.LocationMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.degreesLatitude = r.double();
                        break;
                    }
                case 2: {
                        m.degreesLongitude = r.double();
                        break;
                    }
                case 3: {
                        m.name = r.string();
                        break;
                    }
                case 4: {
                        m.address = r.string();
                        break;
                    }
                case 5: {
                        m.URL = r.string();
                        break;
                    }
                case 6: {
                        m.isLive = r.bool();
                        break;
                    }
                case 7: {
                        m.accuracyInMeters = r.uint32();
                        break;
                    }
                case 8: {
                        m.speedInMps = r.float();
                        break;
                    }
                case 9: {
                        m.degreesClockwiseFromMagneticNorth = r.uint32();
                        break;
                    }
                case 11: {
                        m.comment = r.string();
                        break;
                    }
                case 16: {
                        m.JPEGThumbnail = r.bytes();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LocationMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.LocationMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.LocationMessage();
            if (d.degreesLatitude != null) {
                m.degreesLatitude = Number(d.degreesLatitude);
            }
            if (d.degreesLongitude != null) {
                m.degreesLongitude = Number(d.degreesLongitude);
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.address != null) {
                m.address = String(d.address);
            }
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.isLive != null) {
                m.isLive = Boolean(d.isLive);
            }
            if (d.accuracyInMeters != null) {
                m.accuracyInMeters = d.accuracyInMeters >>> 0;
            }
            if (d.speedInMps != null) {
                m.speedInMps = Number(d.speedInMps);
            }
            if (d.degreesClockwiseFromMagneticNorth != null) {
                m.degreesClockwiseFromMagneticNorth = d.degreesClockwiseFromMagneticNorth >>> 0;
            }
            if (d.comment != null) {
                m.comment = String(d.comment);
            }
            if (d.JPEGThumbnail != null) {
                if (typeof d.JPEGThumbnail === "string")
                    $util.base64.decode(d.JPEGThumbnail, m.JPEGThumbnail = $util.newBuffer($util.base64.length(d.JPEGThumbnail)), 0);
                else if (d.JPEGThumbnail.length >= 0)
                    m.JPEGThumbnail = d.JPEGThumbnail;
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.LocationMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        LocationMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.degreesLatitude != null && m.hasOwnProperty("degreesLatitude")) {
                d.degreesLatitude = o.json && !isFinite(m.degreesLatitude) ? String(m.degreesLatitude) : m.degreesLatitude;
                if (o.oneofs)
                    d._degreesLatitude = "degreesLatitude";
            }
            if (m.degreesLongitude != null && m.hasOwnProperty("degreesLongitude")) {
                d.degreesLongitude = o.json && !isFinite(m.degreesLongitude) ? String(m.degreesLongitude) : m.degreesLongitude;
                if (o.oneofs)
                    d._degreesLongitude = "degreesLongitude";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.address != null && m.hasOwnProperty("address")) {
                d.address = m.address;
                if (o.oneofs)
                    d._address = "address";
            }
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.isLive != null && m.hasOwnProperty("isLive")) {
                d.isLive = m.isLive;
                if (o.oneofs)
                    d._isLive = "isLive";
            }
            if (m.accuracyInMeters != null && m.hasOwnProperty("accuracyInMeters")) {
                d.accuracyInMeters = m.accuracyInMeters;
                if (o.oneofs)
                    d._accuracyInMeters = "accuracyInMeters";
            }
            if (m.speedInMps != null && m.hasOwnProperty("speedInMps")) {
                d.speedInMps = o.json && !isFinite(m.speedInMps) ? String(m.speedInMps) : m.speedInMps;
                if (o.oneofs)
                    d._speedInMps = "speedInMps";
            }
            if (m.degreesClockwiseFromMagneticNorth != null && m.hasOwnProperty("degreesClockwiseFromMagneticNorth")) {
                d.degreesClockwiseFromMagneticNorth = m.degreesClockwiseFromMagneticNorth;
                if (o.oneofs)
                    d._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
            }
            if (m.comment != null && m.hasOwnProperty("comment")) {
                d.comment = m.comment;
                if (o.oneofs)
                    d._comment = "comment";
            }
            if (m.JPEGThumbnail != null && m.hasOwnProperty("JPEGThumbnail")) {
                d.JPEGThumbnail = o.bytes === String ? $util.base64.encode(m.JPEGThumbnail, 0, m.JPEGThumbnail.length) : o.bytes === Array ? Array.prototype.slice.call(m.JPEGThumbnail) : m.JPEGThumbnail;
                if (o.oneofs)
                    d._JPEGThumbnail = "JPEGThumbnail";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        LocationMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LocationMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.LocationMessage";
        };

        return LocationMessage;
    })();

    WAE2E.ContactMessage = (function() {

        function ContactMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ContactMessage.prototype.displayName = null;
        ContactMessage.prototype.vcard = null;
        ContactMessage.prototype.contextInfo = null;
        ContactMessage.prototype.isSelfContact = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactMessage.prototype, "_displayName", {
            get: $util.oneOfGetter($oneOfFields = ["displayName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactMessage.prototype, "_vcard", {
            get: $util.oneOfGetter($oneOfFields = ["vcard"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ContactMessage.prototype, "_isSelfContact", {
            get: $util.oneOfGetter($oneOfFields = ["isSelfContact"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ContactMessage.create = function create(properties) {
            return new ContactMessage(properties);
        };

        ContactMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.displayName != null && Object.hasOwnProperty.call(m, "displayName"))
                w.uint32(10).string(m.displayName);
            if (m.vcard != null && Object.hasOwnProperty.call(m, "vcard"))
                w.uint32(130).string(m.vcard);
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(138).fork()).ldelim();
            if (m.isSelfContact != null && Object.hasOwnProperty.call(m, "isSelfContact"))
                w.uint32(144).bool(m.isSelfContact);
            return w;
        };

        ContactMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ContactMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.displayName = r.string();
                        break;
                    }
                case 16: {
                        m.vcard = r.string();
                        break;
                    }
                case 17: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.isSelfContact = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ContactMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ContactMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ContactMessage();
            if (d.displayName != null) {
                m.displayName = String(d.displayName);
            }
            if (d.vcard != null) {
                m.vcard = String(d.vcard);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.ContactMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            if (d.isSelfContact != null) {
                m.isSelfContact = Boolean(d.isSelfContact);
            }
            return m;
        };

        ContactMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.displayName != null && m.hasOwnProperty("displayName")) {
                d.displayName = m.displayName;
                if (o.oneofs)
                    d._displayName = "displayName";
            }
            if (m.vcard != null && m.hasOwnProperty("vcard")) {
                d.vcard = m.vcard;
                if (o.oneofs)
                    d._vcard = "vcard";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            if (m.isSelfContact != null && m.hasOwnProperty("isSelfContact")) {
                d.isSelfContact = m.isSelfContact;
                if (o.oneofs)
                    d._isSelfContact = "isSelfContact";
            }
            return d;
        };

        ContactMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContactMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ContactMessage";
        };

        return ContactMessage;
    })();

    WAE2E.SenderKeyDistributionMessage = (function() {

        function SenderKeyDistributionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SenderKeyDistributionMessage.prototype.groupID = null;
        SenderKeyDistributionMessage.prototype.axolotlSenderKeyDistributionMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_groupID", {
            get: $util.oneOfGetter($oneOfFields = ["groupID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SenderKeyDistributionMessage.prototype, "_axolotlSenderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["axolotlSenderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SenderKeyDistributionMessage.create = function create(properties) {
            return new SenderKeyDistributionMessage(properties);
        };

        SenderKeyDistributionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.groupID != null && Object.hasOwnProperty.call(m, "groupID"))
                w.uint32(10).string(m.groupID);
            if (m.axolotlSenderKeyDistributionMessage != null && Object.hasOwnProperty.call(m, "axolotlSenderKeyDistributionMessage"))
                w.uint32(18).bytes(m.axolotlSenderKeyDistributionMessage);
            return w;
        };

        SenderKeyDistributionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.SenderKeyDistributionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.groupID = r.string();
                        break;
                    }
                case 2: {
                        m.axolotlSenderKeyDistributionMessage = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SenderKeyDistributionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.SenderKeyDistributionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.SenderKeyDistributionMessage();
            if (d.groupID != null) {
                m.groupID = String(d.groupID);
            }
            if (d.axolotlSenderKeyDistributionMessage != null) {
                if (typeof d.axolotlSenderKeyDistributionMessage === "string")
                    $util.base64.decode(d.axolotlSenderKeyDistributionMessage, m.axolotlSenderKeyDistributionMessage = $util.newBuffer($util.base64.length(d.axolotlSenderKeyDistributionMessage)), 0);
                else if (d.axolotlSenderKeyDistributionMessage.length >= 0)
                    m.axolotlSenderKeyDistributionMessage = d.axolotlSenderKeyDistributionMessage;
            }
            return m;
        };

        SenderKeyDistributionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.groupID != null && m.hasOwnProperty("groupID")) {
                d.groupID = m.groupID;
                if (o.oneofs)
                    d._groupID = "groupID";
            }
            if (m.axolotlSenderKeyDistributionMessage != null && m.hasOwnProperty("axolotlSenderKeyDistributionMessage")) {
                d.axolotlSenderKeyDistributionMessage = o.bytes === String ? $util.base64.encode(m.axolotlSenderKeyDistributionMessage, 0, m.axolotlSenderKeyDistributionMessage.length) : o.bytes === Array ? Array.prototype.slice.call(m.axolotlSenderKeyDistributionMessage) : m.axolotlSenderKeyDistributionMessage;
                if (o.oneofs)
                    d._axolotlSenderKeyDistributionMessage = "axolotlSenderKeyDistributionMessage";
            }
            return d;
        };

        SenderKeyDistributionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SenderKeyDistributionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.SenderKeyDistributionMessage";
        };

        return SenderKeyDistributionMessage;
    })();

    WAE2E.VideoEndCard = (function() {

        function VideoEndCard(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        VideoEndCard.prototype.username = null;
        VideoEndCard.prototype.caption = null;
        VideoEndCard.prototype.thumbnailImageURL = null;
        VideoEndCard.prototype.profilePictureURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoEndCard.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoEndCard.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoEndCard.prototype, "_thumbnailImageURL", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailImageURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(VideoEndCard.prototype, "_profilePictureURL", {
            get: $util.oneOfGetter($oneOfFields = ["profilePictureURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        VideoEndCard.create = function create(properties) {
            return new VideoEndCard(properties);
        };

        VideoEndCard.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.username != null && Object.hasOwnProperty.call(m, "username"))
                w.uint32(10).string(m.username);
            if (m.caption != null && Object.hasOwnProperty.call(m, "caption"))
                w.uint32(18).string(m.caption);
            if (m.thumbnailImageURL != null && Object.hasOwnProperty.call(m, "thumbnailImageURL"))
                w.uint32(26).string(m.thumbnailImageURL);
            if (m.profilePictureURL != null && Object.hasOwnProperty.call(m, "profilePictureURL"))
                w.uint32(34).string(m.profilePictureURL);
            return w;
        };

        VideoEndCard.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.VideoEndCard();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.username = r.string();
                        break;
                    }
                case 2: {
                        m.caption = r.string();
                        break;
                    }
                case 3: {
                        m.thumbnailImageURL = r.string();
                        break;
                    }
                case 4: {
                        m.profilePictureURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        VideoEndCard.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.VideoEndCard)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.VideoEndCard();
            if (d.username != null) {
                m.username = String(d.username);
            }
            if (d.caption != null) {
                m.caption = String(d.caption);
            }
            if (d.thumbnailImageURL != null) {
                m.thumbnailImageURL = String(d.thumbnailImageURL);
            }
            if (d.profilePictureURL != null) {
                m.profilePictureURL = String(d.profilePictureURL);
            }
            return m;
        };

        VideoEndCard.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.username != null && m.hasOwnProperty("username")) {
                d.username = m.username;
                if (o.oneofs)
                    d._username = "username";
            }
            if (m.caption != null && m.hasOwnProperty("caption")) {
                d.caption = m.caption;
                if (o.oneofs)
                    d._caption = "caption";
            }
            if (m.thumbnailImageURL != null && m.hasOwnProperty("thumbnailImageURL")) {
                d.thumbnailImageURL = m.thumbnailImageURL;
                if (o.oneofs)
                    d._thumbnailImageURL = "thumbnailImageURL";
            }
            if (m.profilePictureURL != null && m.hasOwnProperty("profilePictureURL")) {
                d.profilePictureURL = m.profilePictureURL;
                if (o.oneofs)
                    d._profilePictureURL = "profilePictureURL";
            }
            return d;
        };

        VideoEndCard.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        VideoEndCard.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.VideoEndCard";
        };

        return VideoEndCard;
    })();

    WAE2E.MediaDomainInfo = (function() {

        function MediaDomainInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MediaDomainInfo.prototype.mediaKeyDomain = null;
        MediaDomainInfo.prototype.e2EeMediaKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaDomainInfo.prototype, "_mediaKeyDomain", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyDomain"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaDomainInfo.prototype, "_e2EeMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["e2EeMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaDomainInfo.create = function create(properties) {
            return new MediaDomainInfo(properties);
        };

        MediaDomainInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.mediaKeyDomain != null && Object.hasOwnProperty.call(m, "mediaKeyDomain"))
                w.uint32(8).int32(m.mediaKeyDomain);
            if (m.e2EeMediaKey != null && Object.hasOwnProperty.call(m, "e2EeMediaKey"))
                w.uint32(18).bytes(m.e2EeMediaKey);
            return w;
        };

        MediaDomainInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MediaDomainInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.mediaKeyDomain = r.int32();
                        break;
                    }
                case 2: {
                        m.e2EeMediaKey = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MediaDomainInfo.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MediaDomainInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MediaDomainInfo();
            switch (d.mediaKeyDomain) {
            default:
                if (typeof d.mediaKeyDomain === "number") {
                    m.mediaKeyDomain = d.mediaKeyDomain;
                    break;
                }
                break;
            case "MEDIA_KEY_DOMAIN_UNKNOWN":
            case 0:
                m.mediaKeyDomain = 0;
                break;
            case "MEDIA_KEY_DOMAIN_E2EE":
            case 1:
                m.mediaKeyDomain = 1;
                break;
            case "MEDIA_KEY_DOMAIN_NON_E2EE":
            case 2:
                m.mediaKeyDomain = 2;
                break;
            }
            if (d.e2EeMediaKey != null) {
                if (typeof d.e2EeMediaKey === "string")
                    $util.base64.decode(d.e2EeMediaKey, m.e2EeMediaKey = $util.newBuffer($util.base64.length(d.e2EeMediaKey)), 0);
                else if (d.e2EeMediaKey.length >= 0)
                    m.e2EeMediaKey = d.e2EeMediaKey;
            }
            return m;
        };

        MediaDomainInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.mediaKeyDomain != null && m.hasOwnProperty("mediaKeyDomain")) {
                d.mediaKeyDomain = o.enums === String ? WAE2E.MediaKeyDomain[m.mediaKeyDomain] === undefined ? m.mediaKeyDomain : WAE2E.MediaKeyDomain[m.mediaKeyDomain] : m.mediaKeyDomain;
                if (o.oneofs)
                    d._mediaKeyDomain = "mediaKeyDomain";
            }
            if (m.e2EeMediaKey != null && m.hasOwnProperty("e2EeMediaKey")) {
                d.e2EeMediaKey = o.bytes === String ? $util.base64.encode(m.e2EeMediaKey, 0, m.e2EeMediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.e2EeMediaKey) : m.e2EeMediaKey;
                if (o.oneofs)
                    d._e2EeMediaKey = "e2EeMediaKey";
            }
            return d;
        };

        MediaDomainInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaDomainInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MediaDomainInfo";
        };

        return MediaDomainInfo;
    })();

    WAE2E.DeviceListMetadata = (function() {

        function DeviceListMetadata(p) {
            this.senderKeyIndexes = [];
            this.recipientKeyIndexes = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DeviceListMetadata.prototype.senderKeyHash = null;
        DeviceListMetadata.prototype.senderTimestamp = null;
        DeviceListMetadata.prototype.senderKeyIndexes = $util.emptyArray;
        DeviceListMetadata.prototype.senderAccountType = null;
        DeviceListMetadata.prototype.receiverAccountType = null;
        DeviceListMetadata.prototype.recipientKeyHash = null;
        DeviceListMetadata.prototype.recipientTimestamp = null;
        DeviceListMetadata.prototype.recipientKeyIndexes = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_senderAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["senderAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_receiverAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["receiverAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_recipientKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["recipientKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceListMetadata.prototype, "_recipientTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["recipientTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceListMetadata.create = function create(properties) {
            return new DeviceListMetadata(properties);
        };

        DeviceListMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.senderKeyHash != null && Object.hasOwnProperty.call(m, "senderKeyHash"))
                w.uint32(10).bytes(m.senderKeyHash);
            if (m.senderTimestamp != null && Object.hasOwnProperty.call(m, "senderTimestamp"))
                w.uint32(16).uint64(m.senderTimestamp);
            if (m.senderKeyIndexes != null && m.senderKeyIndexes.length) {
                w.uint32(26).fork();
                for (var i = 0; i < m.senderKeyIndexes.length; ++i)
                    w.uint32(m.senderKeyIndexes[i]);
                w.ldelim();
            }
            if (m.senderAccountType != null && Object.hasOwnProperty.call(m, "senderAccountType"))
                w.uint32(32).int32(m.senderAccountType);
            if (m.receiverAccountType != null && Object.hasOwnProperty.call(m, "receiverAccountType"))
                w.uint32(40).int32(m.receiverAccountType);
            if (m.recipientKeyHash != null && Object.hasOwnProperty.call(m, "recipientKeyHash"))
                w.uint32(66).bytes(m.recipientKeyHash);
            if (m.recipientTimestamp != null && Object.hasOwnProperty.call(m, "recipientTimestamp"))
                w.uint32(72).uint64(m.recipientTimestamp);
            if (m.recipientKeyIndexes != null && m.recipientKeyIndexes.length) {
                w.uint32(82).fork();
                for (var i = 0; i < m.recipientKeyIndexes.length; ++i)
                    w.uint32(m.recipientKeyIndexes[i]);
                w.ldelim();
            }
            return w;
        };

        DeviceListMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.DeviceListMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.senderKeyHash = r.bytes();
                        break;
                    }
                case 2: {
                        m.senderTimestamp = r.uint64();
                        break;
                    }
                case 3: {
                        if (!(m.senderKeyIndexes && m.senderKeyIndexes.length))
                            m.senderKeyIndexes = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.senderKeyIndexes.push(r.uint32());
                        } else
                            m.senderKeyIndexes.push(r.uint32());
                        break;
                    }
                case 4: {
                        m.senderAccountType = r.int32();
                        break;
                    }
                case 5: {
                        m.receiverAccountType = r.int32();
                        break;
                    }
                case 8: {
                        m.recipientKeyHash = r.bytes();
                        break;
                    }
                case 9: {
                        m.recipientTimestamp = r.uint64();
                        break;
                    }
                case 10: {
                        if (!(m.recipientKeyIndexes && m.recipientKeyIndexes.length))
                            m.recipientKeyIndexes = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.recipientKeyIndexes.push(r.uint32());
                        } else
                            m.recipientKeyIndexes.push(r.uint32());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DeviceListMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.DeviceListMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.DeviceListMetadata();
            if (d.senderKeyHash != null) {
                if (typeof d.senderKeyHash === "string")
                    $util.base64.decode(d.senderKeyHash, m.senderKeyHash = $util.newBuffer($util.base64.length(d.senderKeyHash)), 0);
                else if (d.senderKeyHash.length >= 0)
                    m.senderKeyHash = d.senderKeyHash;
            }
            if (d.senderTimestamp != null) {
                if ($util.Long)
                    (m.senderTimestamp = $util.Long.fromValue(d.senderTimestamp)).unsigned = true;
                else if (typeof d.senderTimestamp === "string")
                    m.senderTimestamp = parseInt(d.senderTimestamp, 10);
                else if (typeof d.senderTimestamp === "number")
                    m.senderTimestamp = d.senderTimestamp;
                else if (typeof d.senderTimestamp === "object")
                    m.senderTimestamp = new $util.LongBits(d.senderTimestamp.low >>> 0, d.senderTimestamp.high >>> 0).toNumber(true);
            }
            if (d.senderKeyIndexes) {
                if (!Array.isArray(d.senderKeyIndexes))
                    throw TypeError(".WAE2E.DeviceListMetadata.senderKeyIndexes: array expected");
                m.senderKeyIndexes = [];
                for (var i = 0; i < d.senderKeyIndexes.length; ++i) {
                    m.senderKeyIndexes[i] = d.senderKeyIndexes[i] >>> 0;
                }
            }
            switch (d.senderAccountType) {
            default:
                if (typeof d.senderAccountType === "number") {
                    m.senderAccountType = d.senderAccountType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.senderAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.senderAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.senderAccountType = 2;
                break;
            }
            switch (d.receiverAccountType) {
            default:
                if (typeof d.receiverAccountType === "number") {
                    m.receiverAccountType = d.receiverAccountType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.receiverAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.receiverAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.receiverAccountType = 2;
                break;
            }
            if (d.recipientKeyHash != null) {
                if (typeof d.recipientKeyHash === "string")
                    $util.base64.decode(d.recipientKeyHash, m.recipientKeyHash = $util.newBuffer($util.base64.length(d.recipientKeyHash)), 0);
                else if (d.recipientKeyHash.length >= 0)
                    m.recipientKeyHash = d.recipientKeyHash;
            }
            if (d.recipientTimestamp != null) {
                if ($util.Long)
                    (m.recipientTimestamp = $util.Long.fromValue(d.recipientTimestamp)).unsigned = true;
                else if (typeof d.recipientTimestamp === "string")
                    m.recipientTimestamp = parseInt(d.recipientTimestamp, 10);
                else if (typeof d.recipientTimestamp === "number")
                    m.recipientTimestamp = d.recipientTimestamp;
                else if (typeof d.recipientTimestamp === "object")
                    m.recipientTimestamp = new $util.LongBits(d.recipientTimestamp.low >>> 0, d.recipientTimestamp.high >>> 0).toNumber(true);
            }
            if (d.recipientKeyIndexes) {
                if (!Array.isArray(d.recipientKeyIndexes))
                    throw TypeError(".WAE2E.DeviceListMetadata.recipientKeyIndexes: array expected");
                m.recipientKeyIndexes = [];
                for (var i = 0; i < d.recipientKeyIndexes.length; ++i) {
                    m.recipientKeyIndexes[i] = d.recipientKeyIndexes[i] >>> 0;
                }
            }
            return m;
        };

        DeviceListMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.senderKeyIndexes = [];
                d.recipientKeyIndexes = [];
            }
            if (m.senderKeyHash != null && m.hasOwnProperty("senderKeyHash")) {
                d.senderKeyHash = o.bytes === String ? $util.base64.encode(m.senderKeyHash, 0, m.senderKeyHash.length) : o.bytes === Array ? Array.prototype.slice.call(m.senderKeyHash) : m.senderKeyHash;
                if (o.oneofs)
                    d._senderKeyHash = "senderKeyHash";
            }
            if (m.senderTimestamp != null && m.hasOwnProperty("senderTimestamp")) {
                if (typeof m.senderTimestamp === "number")
                    d.senderTimestamp = o.longs === String ? String(m.senderTimestamp) : m.senderTimestamp;
                else
                    d.senderTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.senderTimestamp) : o.longs === Number ? new $util.LongBits(m.senderTimestamp.low >>> 0, m.senderTimestamp.high >>> 0).toNumber(true) : m.senderTimestamp;
                if (o.oneofs)
                    d._senderTimestamp = "senderTimestamp";
            }
            if (m.senderKeyIndexes && m.senderKeyIndexes.length) {
                d.senderKeyIndexes = [];
                for (var j = 0; j < m.senderKeyIndexes.length; ++j) {
                    d.senderKeyIndexes[j] = m.senderKeyIndexes[j];
                }
            }
            if (m.senderAccountType != null && m.hasOwnProperty("senderAccountType")) {
                d.senderAccountType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.senderAccountType] === undefined ? m.senderAccountType : $root.WAAdv.ADVEncryptionType[m.senderAccountType] : m.senderAccountType;
                if (o.oneofs)
                    d._senderAccountType = "senderAccountType";
            }
            if (m.receiverAccountType != null && m.hasOwnProperty("receiverAccountType")) {
                d.receiverAccountType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.receiverAccountType] === undefined ? m.receiverAccountType : $root.WAAdv.ADVEncryptionType[m.receiverAccountType] : m.receiverAccountType;
                if (o.oneofs)
                    d._receiverAccountType = "receiverAccountType";
            }
            if (m.recipientKeyHash != null && m.hasOwnProperty("recipientKeyHash")) {
                d.recipientKeyHash = o.bytes === String ? $util.base64.encode(m.recipientKeyHash, 0, m.recipientKeyHash.length) : o.bytes === Array ? Array.prototype.slice.call(m.recipientKeyHash) : m.recipientKeyHash;
                if (o.oneofs)
                    d._recipientKeyHash = "recipientKeyHash";
            }
            if (m.recipientTimestamp != null && m.hasOwnProperty("recipientTimestamp")) {
                if (typeof m.recipientTimestamp === "number")
                    d.recipientTimestamp = o.longs === String ? String(m.recipientTimestamp) : m.recipientTimestamp;
                else
                    d.recipientTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.recipientTimestamp) : o.longs === Number ? new $util.LongBits(m.recipientTimestamp.low >>> 0, m.recipientTimestamp.high >>> 0).toNumber(true) : m.recipientTimestamp;
                if (o.oneofs)
                    d._recipientTimestamp = "recipientTimestamp";
            }
            if (m.recipientKeyIndexes && m.recipientKeyIndexes.length) {
                d.recipientKeyIndexes = [];
                for (var j = 0; j < m.recipientKeyIndexes.length; ++j) {
                    d.recipientKeyIndexes[j] = m.recipientKeyIndexes[j];
                }
            }
            return d;
        };

        DeviceListMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceListMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.DeviceListMetadata";
        };

        return DeviceListMetadata;
    })();

    WAE2E.EmbeddedMessage = (function() {

        function EmbeddedMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EmbeddedMessage.prototype.stanzaID = null;
        EmbeddedMessage.prototype.message = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMessage.prototype, "_stanzaID", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedMessage.create = function create(properties) {
            return new EmbeddedMessage(properties);
        };

        EmbeddedMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                w.uint32(10).string(m.stanzaID);
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(18).fork()).ldelim();
            return w;
        };

        EmbeddedMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EmbeddedMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stanzaID = r.string();
                        break;
                    }
                case 2: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EmbeddedMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EmbeddedMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EmbeddedMessage();
            if (d.stanzaID != null) {
                m.stanzaID = String(d.stanzaID);
            }
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAE2E.EmbeddedMessage.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            return m;
        };

        EmbeddedMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                d.stanzaID = m.stanzaID;
                if (o.oneofs)
                    d._stanzaID = "stanzaID";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            return d;
        };

        EmbeddedMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EmbeddedMessage";
        };

        return EmbeddedMessage;
    })();

    WAE2E.EmbeddedMusic = (function() {

        function EmbeddedMusic(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EmbeddedMusic.prototype.musicContentMediaID = null;
        EmbeddedMusic.prototype.songID = null;
        EmbeddedMusic.prototype.author = null;
        EmbeddedMusic.prototype.title = null;
        EmbeddedMusic.prototype.artworkDirectPath = null;
        EmbeddedMusic.prototype.artworkSHA256 = null;
        EmbeddedMusic.prototype.artworkEncSHA256 = null;
        EmbeddedMusic.prototype.artistAttribution = null;
        EmbeddedMusic.prototype.countryBlocklist = null;
        EmbeddedMusic.prototype.isExplicit = null;
        EmbeddedMusic.prototype.artworkMediaKey = null;
        EmbeddedMusic.prototype.musicSongStartTimeInMS = null;
        EmbeddedMusic.prototype.derivedContentStartTimeInMS = null;
        EmbeddedMusic.prototype.overlapDurationInMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_musicContentMediaID", {
            get: $util.oneOfGetter($oneOfFields = ["musicContentMediaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_songID", {
            get: $util.oneOfGetter($oneOfFields = ["songID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_author", {
            get: $util.oneOfGetter($oneOfFields = ["author"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["artworkDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artistAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_countryBlocklist", {
            get: $util.oneOfGetter($oneOfFields = ["countryBlocklist"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_isExplicit", {
            get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_artworkMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["artworkMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_musicSongStartTimeInMS", {
            get: $util.oneOfGetter($oneOfFields = ["musicSongStartTimeInMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_derivedContentStartTimeInMS", {
            get: $util.oneOfGetter($oneOfFields = ["derivedContentStartTimeInMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EmbeddedMusic.prototype, "_overlapDurationInMS", {
            get: $util.oneOfGetter($oneOfFields = ["overlapDurationInMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedMusic.create = function create(properties) {
            return new EmbeddedMusic(properties);
        };

        EmbeddedMusic.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.musicContentMediaID != null && Object.hasOwnProperty.call(m, "musicContentMediaID"))
                w.uint32(10).string(m.musicContentMediaID);
            if (m.songID != null && Object.hasOwnProperty.call(m, "songID"))
                w.uint32(18).string(m.songID);
            if (m.author != null && Object.hasOwnProperty.call(m, "author"))
                w.uint32(26).string(m.author);
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(34).string(m.title);
            if (m.artworkDirectPath != null && Object.hasOwnProperty.call(m, "artworkDirectPath"))
                w.uint32(42).string(m.artworkDirectPath);
            if (m.artworkSHA256 != null && Object.hasOwnProperty.call(m, "artworkSHA256"))
                w.uint32(50).bytes(m.artworkSHA256);
            if (m.artworkEncSHA256 != null && Object.hasOwnProperty.call(m, "artworkEncSHA256"))
                w.uint32(58).bytes(m.artworkEncSHA256);
            if (m.artistAttribution != null && Object.hasOwnProperty.call(m, "artistAttribution"))
                w.uint32(66).string(m.artistAttribution);
            if (m.countryBlocklist != null && Object.hasOwnProperty.call(m, "countryBlocklist"))
                w.uint32(74).bytes(m.countryBlocklist);
            if (m.isExplicit != null && Object.hasOwnProperty.call(m, "isExplicit"))
                w.uint32(80).bool(m.isExplicit);
            if (m.artworkMediaKey != null && Object.hasOwnProperty.call(m, "artworkMediaKey"))
                w.uint32(90).bytes(m.artworkMediaKey);
            if (m.musicSongStartTimeInMS != null && Object.hasOwnProperty.call(m, "musicSongStartTimeInMS"))
                w.uint32(96).int64(m.musicSongStartTimeInMS);
            if (m.derivedContentStartTimeInMS != null && Object.hasOwnProperty.call(m, "derivedContentStartTimeInMS"))
                w.uint32(104).int64(m.derivedContentStartTimeInMS);
            if (m.overlapDurationInMS != null && Object.hasOwnProperty.call(m, "overlapDurationInMS"))
                w.uint32(112).int64(m.overlapDurationInMS);
            return w;
        };

        EmbeddedMusic.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EmbeddedMusic();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.musicContentMediaID = r.string();
                        break;
                    }
                case 2: {
                        m.songID = r.string();
                        break;
                    }
                case 3: {
                        m.author = r.string();
                        break;
                    }
                case 4: {
                        m.title = r.string();
                        break;
                    }
                case 5: {
                        m.artworkDirectPath = r.string();
                        break;
                    }
                case 6: {
                        m.artworkSHA256 = r.bytes();
                        break;
                    }
                case 7: {
                        m.artworkEncSHA256 = r.bytes();
                        break;
                    }
                case 8: {
                        m.artistAttribution = r.string();
                        break;
                    }
                case 9: {
                        m.countryBlocklist = r.bytes();
                        break;
                    }
                case 10: {
                        m.isExplicit = r.bool();
                        break;
                    }
                case 11: {
                        m.artworkMediaKey = r.bytes();
                        break;
                    }
                case 12: {
                        m.musicSongStartTimeInMS = r.int64();
                        break;
                    }
                case 13: {
                        m.derivedContentStartTimeInMS = r.int64();
                        break;
                    }
                case 14: {
                        m.overlapDurationInMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EmbeddedMusic.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EmbeddedMusic)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EmbeddedMusic();
            if (d.musicContentMediaID != null) {
                m.musicContentMediaID = String(d.musicContentMediaID);
            }
            if (d.songID != null) {
                m.songID = String(d.songID);
            }
            if (d.author != null) {
                m.author = String(d.author);
            }
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.artworkDirectPath != null) {
                m.artworkDirectPath = String(d.artworkDirectPath);
            }
            if (d.artworkSHA256 != null) {
                if (typeof d.artworkSHA256 === "string")
                    $util.base64.decode(d.artworkSHA256, m.artworkSHA256 = $util.newBuffer($util.base64.length(d.artworkSHA256)), 0);
                else if (d.artworkSHA256.length >= 0)
                    m.artworkSHA256 = d.artworkSHA256;
            }
            if (d.artworkEncSHA256 != null) {
                if (typeof d.artworkEncSHA256 === "string")
                    $util.base64.decode(d.artworkEncSHA256, m.artworkEncSHA256 = $util.newBuffer($util.base64.length(d.artworkEncSHA256)), 0);
                else if (d.artworkEncSHA256.length >= 0)
                    m.artworkEncSHA256 = d.artworkEncSHA256;
            }
            if (d.artistAttribution != null) {
                m.artistAttribution = String(d.artistAttribution);
            }
            if (d.countryBlocklist != null) {
                if (typeof d.countryBlocklist === "string")
                    $util.base64.decode(d.countryBlocklist, m.countryBlocklist = $util.newBuffer($util.base64.length(d.countryBlocklist)), 0);
                else if (d.countryBlocklist.length >= 0)
                    m.countryBlocklist = d.countryBlocklist;
            }
            if (d.isExplicit != null) {
                m.isExplicit = Boolean(d.isExplicit);
            }
            if (d.artworkMediaKey != null) {
                if (typeof d.artworkMediaKey === "string")
                    $util.base64.decode(d.artworkMediaKey, m.artworkMediaKey = $util.newBuffer($util.base64.length(d.artworkMediaKey)), 0);
                else if (d.artworkMediaKey.length >= 0)
                    m.artworkMediaKey = d.artworkMediaKey;
            }
            if (d.musicSongStartTimeInMS != null) {
                if ($util.Long)
                    (m.musicSongStartTimeInMS = $util.Long.fromValue(d.musicSongStartTimeInMS)).unsigned = false;
                else if (typeof d.musicSongStartTimeInMS === "string")
                    m.musicSongStartTimeInMS = parseInt(d.musicSongStartTimeInMS, 10);
                else if (typeof d.musicSongStartTimeInMS === "number")
                    m.musicSongStartTimeInMS = d.musicSongStartTimeInMS;
                else if (typeof d.musicSongStartTimeInMS === "object")
                    m.musicSongStartTimeInMS = new $util.LongBits(d.musicSongStartTimeInMS.low >>> 0, d.musicSongStartTimeInMS.high >>> 0).toNumber();
            }
            if (d.derivedContentStartTimeInMS != null) {
                if ($util.Long)
                    (m.derivedContentStartTimeInMS = $util.Long.fromValue(d.derivedContentStartTimeInMS)).unsigned = false;
                else if (typeof d.derivedContentStartTimeInMS === "string")
                    m.derivedContentStartTimeInMS = parseInt(d.derivedContentStartTimeInMS, 10);
                else if (typeof d.derivedContentStartTimeInMS === "number")
                    m.derivedContentStartTimeInMS = d.derivedContentStartTimeInMS;
                else if (typeof d.derivedContentStartTimeInMS === "object")
                    m.derivedContentStartTimeInMS = new $util.LongBits(d.derivedContentStartTimeInMS.low >>> 0, d.derivedContentStartTimeInMS.high >>> 0).toNumber();
            }
            if (d.overlapDurationInMS != null) {
                if ($util.Long)
                    (m.overlapDurationInMS = $util.Long.fromValue(d.overlapDurationInMS)).unsigned = false;
                else if (typeof d.overlapDurationInMS === "string")
                    m.overlapDurationInMS = parseInt(d.overlapDurationInMS, 10);
                else if (typeof d.overlapDurationInMS === "number")
                    m.overlapDurationInMS = d.overlapDurationInMS;
                else if (typeof d.overlapDurationInMS === "object")
                    m.overlapDurationInMS = new $util.LongBits(d.overlapDurationInMS.low >>> 0, d.overlapDurationInMS.high >>> 0).toNumber();
            }
            return m;
        };

        EmbeddedMusic.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.musicContentMediaID != null && m.hasOwnProperty("musicContentMediaID")) {
                d.musicContentMediaID = m.musicContentMediaID;
                if (o.oneofs)
                    d._musicContentMediaID = "musicContentMediaID";
            }
            if (m.songID != null && m.hasOwnProperty("songID")) {
                d.songID = m.songID;
                if (o.oneofs)
                    d._songID = "songID";
            }
            if (m.author != null && m.hasOwnProperty("author")) {
                d.author = m.author;
                if (o.oneofs)
                    d._author = "author";
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.artworkDirectPath != null && m.hasOwnProperty("artworkDirectPath")) {
                d.artworkDirectPath = m.artworkDirectPath;
                if (o.oneofs)
                    d._artworkDirectPath = "artworkDirectPath";
            }
            if (m.artworkSHA256 != null && m.hasOwnProperty("artworkSHA256")) {
                d.artworkSHA256 = o.bytes === String ? $util.base64.encode(m.artworkSHA256, 0, m.artworkSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.artworkSHA256) : m.artworkSHA256;
                if (o.oneofs)
                    d._artworkSHA256 = "artworkSHA256";
            }
            if (m.artworkEncSHA256 != null && m.hasOwnProperty("artworkEncSHA256")) {
                d.artworkEncSHA256 = o.bytes === String ? $util.base64.encode(m.artworkEncSHA256, 0, m.artworkEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.artworkEncSHA256) : m.artworkEncSHA256;
                if (o.oneofs)
                    d._artworkEncSHA256 = "artworkEncSHA256";
            }
            if (m.artistAttribution != null && m.hasOwnProperty("artistAttribution")) {
                d.artistAttribution = m.artistAttribution;
                if (o.oneofs)
                    d._artistAttribution = "artistAttribution";
            }
            if (m.countryBlocklist != null && m.hasOwnProperty("countryBlocklist")) {
                d.countryBlocklist = o.bytes === String ? $util.base64.encode(m.countryBlocklist, 0, m.countryBlocklist.length) : o.bytes === Array ? Array.prototype.slice.call(m.countryBlocklist) : m.countryBlocklist;
                if (o.oneofs)
                    d._countryBlocklist = "countryBlocklist";
            }
            if (m.isExplicit != null && m.hasOwnProperty("isExplicit")) {
                d.isExplicit = m.isExplicit;
                if (o.oneofs)
                    d._isExplicit = "isExplicit";
            }
            if (m.artworkMediaKey != null && m.hasOwnProperty("artworkMediaKey")) {
                d.artworkMediaKey = o.bytes === String ? $util.base64.encode(m.artworkMediaKey, 0, m.artworkMediaKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.artworkMediaKey) : m.artworkMediaKey;
                if (o.oneofs)
                    d._artworkMediaKey = "artworkMediaKey";
            }
            if (m.musicSongStartTimeInMS != null && m.hasOwnProperty("musicSongStartTimeInMS")) {
                if (typeof m.musicSongStartTimeInMS === "number")
                    d.musicSongStartTimeInMS = o.longs === String ? String(m.musicSongStartTimeInMS) : m.musicSongStartTimeInMS;
                else
                    d.musicSongStartTimeInMS = o.longs === String ? $util.Long.prototype.toString.call(m.musicSongStartTimeInMS) : o.longs === Number ? new $util.LongBits(m.musicSongStartTimeInMS.low >>> 0, m.musicSongStartTimeInMS.high >>> 0).toNumber() : m.musicSongStartTimeInMS;
                if (o.oneofs)
                    d._musicSongStartTimeInMS = "musicSongStartTimeInMS";
            }
            if (m.derivedContentStartTimeInMS != null && m.hasOwnProperty("derivedContentStartTimeInMS")) {
                if (typeof m.derivedContentStartTimeInMS === "number")
                    d.derivedContentStartTimeInMS = o.longs === String ? String(m.derivedContentStartTimeInMS) : m.derivedContentStartTimeInMS;
                else
                    d.derivedContentStartTimeInMS = o.longs === String ? $util.Long.prototype.toString.call(m.derivedContentStartTimeInMS) : o.longs === Number ? new $util.LongBits(m.derivedContentStartTimeInMS.low >>> 0, m.derivedContentStartTimeInMS.high >>> 0).toNumber() : m.derivedContentStartTimeInMS;
                if (o.oneofs)
                    d._derivedContentStartTimeInMS = "derivedContentStartTimeInMS";
            }
            if (m.overlapDurationInMS != null && m.hasOwnProperty("overlapDurationInMS")) {
                if (typeof m.overlapDurationInMS === "number")
                    d.overlapDurationInMS = o.longs === String ? String(m.overlapDurationInMS) : m.overlapDurationInMS;
                else
                    d.overlapDurationInMS = o.longs === String ? $util.Long.prototype.toString.call(m.overlapDurationInMS) : o.longs === Number ? new $util.LongBits(m.overlapDurationInMS.low >>> 0, m.overlapDurationInMS.high >>> 0).toNumber() : m.overlapDurationInMS;
                if (o.oneofs)
                    d._overlapDurationInMS = "overlapDurationInMS";
            }
            return d;
        };

        EmbeddedMusic.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedMusic.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EmbeddedMusic";
        };

        return EmbeddedMusic;
    })();

    WAE2E.EmbeddedContent = (function() {

        function EmbeddedContent(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EmbeddedContent.prototype.embeddedMessage = null;
        EmbeddedContent.prototype.embeddedMusic = null;

        let $oneOfFields;

        Object.defineProperty(EmbeddedContent.prototype, "content", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedMessage", "embeddedMusic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedContent.create = function create(properties) {
            return new EmbeddedContent(properties);
        };

        EmbeddedContent.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.embeddedMessage != null && Object.hasOwnProperty.call(m, "embeddedMessage"))
                WAE2E.EmbeddedMessage.encode(m.embeddedMessage, w.uint32(10).fork()).ldelim();
            if (m.embeddedMusic != null && Object.hasOwnProperty.call(m, "embeddedMusic"))
                WAE2E.EmbeddedMusic.encode(m.embeddedMusic, w.uint32(18).fork()).ldelim();
            return w;
        };

        EmbeddedContent.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.EmbeddedContent();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.embeddedMessage = WAE2E.EmbeddedMessage.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.embeddedMusic = WAE2E.EmbeddedMusic.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EmbeddedContent.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.EmbeddedContent)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.EmbeddedContent();
            if (d.embeddedMessage != null) {
                if (typeof d.embeddedMessage !== "object")
                    throw TypeError(".WAE2E.EmbeddedContent.embeddedMessage: object expected");
                m.embeddedMessage = WAE2E.EmbeddedMessage.fromObject(d.embeddedMessage, n + 1);
            }
            if (d.embeddedMusic != null) {
                if (typeof d.embeddedMusic !== "object")
                    throw TypeError(".WAE2E.EmbeddedContent.embeddedMusic: object expected");
                m.embeddedMusic = WAE2E.EmbeddedMusic.fromObject(d.embeddedMusic, n + 1);
            }
            return m;
        };

        EmbeddedContent.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.embeddedMessage != null && m.hasOwnProperty("embeddedMessage")) {
                d.embeddedMessage = WAE2E.EmbeddedMessage.toObject(m.embeddedMessage, o);
                if (o.oneofs)
                    d.content = "embeddedMessage";
            }
            if (m.embeddedMusic != null && m.hasOwnProperty("embeddedMusic")) {
                d.embeddedMusic = WAE2E.EmbeddedMusic.toObject(m.embeddedMusic, o);
                if (o.oneofs)
                    d.content = "embeddedMusic";
            }
            return d;
        };

        EmbeddedContent.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedContent.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.EmbeddedContent";
        };

        return EmbeddedContent;
    })();

    WAE2E.TapLinkAction = (function() {

        function TapLinkAction(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        TapLinkAction.prototype.title = null;
        TapLinkAction.prototype.tapURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TapLinkAction.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TapLinkAction.prototype, "_tapURL", {
            get: $util.oneOfGetter($oneOfFields = ["tapURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TapLinkAction.create = function create(properties) {
            return new TapLinkAction(properties);
        };

        TapLinkAction.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(10).string(m.title);
            if (m.tapURL != null && Object.hasOwnProperty.call(m, "tapURL"))
                w.uint32(18).string(m.tapURL);
            return w;
        };

        TapLinkAction.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TapLinkAction();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.title = r.string();
                        break;
                    }
                case 2: {
                        m.tapURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        TapLinkAction.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.TapLinkAction)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.TapLinkAction();
            if (d.title != null) {
                m.title = String(d.title);
            }
            if (d.tapURL != null) {
                m.tapURL = String(d.tapURL);
            }
            return m;
        };

        TapLinkAction.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            if (m.tapURL != null && m.hasOwnProperty("tapURL")) {
                d.tapURL = m.tapURL;
                if (o.oneofs)
                    d._tapURL = "tapURL";
            }
            return d;
        };

        TapLinkAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TapLinkAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.TapLinkAction";
        };

        return TapLinkAction;
    })();

    WAE2E.Point = (function() {

        function Point(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Point.prototype.xDeprecated = null;
        Point.prototype.yDeprecated = null;
        Point.prototype.x = null;
        Point.prototype.y = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_xDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["xDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_yDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["yDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_x", {
            get: $util.oneOfGetter($oneOfFields = ["x"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Point.prototype, "_y", {
            get: $util.oneOfGetter($oneOfFields = ["y"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Point.create = function create(properties) {
            return new Point(properties);
        };

        Point.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.xDeprecated != null && Object.hasOwnProperty.call(m, "xDeprecated"))
                w.uint32(8).int32(m.xDeprecated);
            if (m.yDeprecated != null && Object.hasOwnProperty.call(m, "yDeprecated"))
                w.uint32(16).int32(m.yDeprecated);
            if (m.x != null && Object.hasOwnProperty.call(m, "x"))
                w.uint32(25).double(m.x);
            if (m.y != null && Object.hasOwnProperty.call(m, "y"))
                w.uint32(33).double(m.y);
            return w;
        };

        Point.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Point();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.xDeprecated = r.int32();
                        break;
                    }
                case 2: {
                        m.yDeprecated = r.int32();
                        break;
                    }
                case 3: {
                        m.x = r.double();
                        break;
                    }
                case 4: {
                        m.y = r.double();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Point.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Point)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Point();
            if (d.xDeprecated != null) {
                m.xDeprecated = d.xDeprecated | 0;
            }
            if (d.yDeprecated != null) {
                m.yDeprecated = d.yDeprecated | 0;
            }
            if (d.x != null) {
                m.x = Number(d.x);
            }
            if (d.y != null) {
                m.y = Number(d.y);
            }
            return m;
        };

        Point.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.xDeprecated != null && m.hasOwnProperty("xDeprecated")) {
                d.xDeprecated = m.xDeprecated;
                if (o.oneofs)
                    d._xDeprecated = "xDeprecated";
            }
            if (m.yDeprecated != null && m.hasOwnProperty("yDeprecated")) {
                d.yDeprecated = m.yDeprecated;
                if (o.oneofs)
                    d._yDeprecated = "yDeprecated";
            }
            if (m.x != null && m.hasOwnProperty("x")) {
                d.x = o.json && !isFinite(m.x) ? String(m.x) : m.x;
                if (o.oneofs)
                    d._x = "x";
            }
            if (m.y != null && m.hasOwnProperty("y")) {
                d.y = o.json && !isFinite(m.y) ? String(m.y) : m.y;
                if (o.oneofs)
                    d._y = "y";
            }
            return d;
        };

        Point.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Point.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Point";
        };

        return Point;
    })();

    WAE2E.Location = (function() {

        function Location(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Location.prototype.degreesLatitude = null;
        Location.prototype.degreesLongitude = null;
        Location.prototype.name = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_degreesLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_degreesLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Location.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Location.create = function create(properties) {
            return new Location(properties);
        };

        Location.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.degreesLatitude != null && Object.hasOwnProperty.call(m, "degreesLatitude"))
                w.uint32(9).double(m.degreesLatitude);
            if (m.degreesLongitude != null && Object.hasOwnProperty.call(m, "degreesLongitude"))
                w.uint32(17).double(m.degreesLongitude);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            return w;
        };

        Location.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Location();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.degreesLatitude = r.double();
                        break;
                    }
                case 2: {
                        m.degreesLongitude = r.double();
                        break;
                    }
                case 3: {
                        m.name = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Location.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Location)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Location();
            if (d.degreesLatitude != null) {
                m.degreesLatitude = Number(d.degreesLatitude);
            }
            if (d.degreesLongitude != null) {
                m.degreesLongitude = Number(d.degreesLongitude);
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            return m;
        };

        Location.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.degreesLatitude != null && m.hasOwnProperty("degreesLatitude")) {
                d.degreesLatitude = o.json && !isFinite(m.degreesLatitude) ? String(m.degreesLatitude) : m.degreesLatitude;
                if (o.oneofs)
                    d._degreesLatitude = "degreesLatitude";
            }
            if (m.degreesLongitude != null && m.hasOwnProperty("degreesLongitude")) {
                d.degreesLongitude = o.json && !isFinite(m.degreesLongitude) ? String(m.degreesLongitude) : m.degreesLongitude;
                if (o.oneofs)
                    d._degreesLongitude = "degreesLongitude";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            return d;
        };

        Location.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Location.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Location";
        };

        return Location;
    })();

    WAE2E.TemplateButton = (function() {

        function TemplateButton(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        TemplateButton.prototype.quickReplyButton = null;
        TemplateButton.prototype.urlButton = null;
        TemplateButton.prototype.callButton = null;
        TemplateButton.prototype.index = null;

        let $oneOfFields;

        Object.defineProperty(TemplateButton.prototype, "button", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(TemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TemplateButton.create = function create(properties) {
            return new TemplateButton(properties);
        };

        TemplateButton.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.quickReplyButton != null && Object.hasOwnProperty.call(m, "quickReplyButton"))
                WAE2E.TemplateButton.QuickReplyButton.encode(m.quickReplyButton, w.uint32(10).fork()).ldelim();
            if (m.urlButton != null && Object.hasOwnProperty.call(m, "urlButton"))
                WAE2E.TemplateButton.URLButton.encode(m.urlButton, w.uint32(18).fork()).ldelim();
            if (m.callButton != null && Object.hasOwnProperty.call(m, "callButton"))
                WAE2E.TemplateButton.CallButton.encode(m.callButton, w.uint32(26).fork()).ldelim();
            if (m.index != null && Object.hasOwnProperty.call(m, "index"))
                w.uint32(32).uint32(m.index);
            return w;
        };

        TemplateButton.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateButton();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.quickReplyButton = WAE2E.TemplateButton.QuickReplyButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.urlButton = WAE2E.TemplateButton.URLButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.callButton = WAE2E.TemplateButton.CallButton.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.index = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        TemplateButton.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.TemplateButton)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.TemplateButton();
            if (d.quickReplyButton != null) {
                if (typeof d.quickReplyButton !== "object")
                    throw TypeError(".WAE2E.TemplateButton.quickReplyButton: object expected");
                m.quickReplyButton = WAE2E.TemplateButton.QuickReplyButton.fromObject(d.quickReplyButton, n + 1);
            }
            if (d.urlButton != null) {
                if (typeof d.urlButton !== "object")
                    throw TypeError(".WAE2E.TemplateButton.urlButton: object expected");
                m.urlButton = WAE2E.TemplateButton.URLButton.fromObject(d.urlButton, n + 1);
            }
            if (d.callButton != null) {
                if (typeof d.callButton !== "object")
                    throw TypeError(".WAE2E.TemplateButton.callButton: object expected");
                m.callButton = WAE2E.TemplateButton.CallButton.fromObject(d.callButton, n + 1);
            }
            if (d.index != null) {
                m.index = d.index >>> 0;
            }
            return m;
        };

        TemplateButton.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.quickReplyButton != null && m.hasOwnProperty("quickReplyButton")) {
                d.quickReplyButton = WAE2E.TemplateButton.QuickReplyButton.toObject(m.quickReplyButton, o);
                if (o.oneofs)
                    d.button = "quickReplyButton";
            }
            if (m.urlButton != null && m.hasOwnProperty("urlButton")) {
                d.urlButton = WAE2E.TemplateButton.URLButton.toObject(m.urlButton, o);
                if (o.oneofs)
                    d.button = "urlButton";
            }
            if (m.callButton != null && m.hasOwnProperty("callButton")) {
                d.callButton = WAE2E.TemplateButton.CallButton.toObject(m.callButton, o);
                if (o.oneofs)
                    d.button = "callButton";
            }
            if (m.index != null && m.hasOwnProperty("index")) {
                d.index = m.index;
                if (o.oneofs)
                    d._index = "index";
            }
            return d;
        };

        TemplateButton.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        TemplateButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.TemplateButton";
        };

        TemplateButton.CallButton = (function() {

            function CallButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            CallButton.prototype.displayText = null;
            CallButton.prototype.phoneNumber = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(CallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CallButton.create = function create(properties) {
                return new CallButton(properties);
            };

            CallButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    WAE2E.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork()).ldelim();
                if (m.phoneNumber != null && Object.hasOwnProperty.call(m, "phoneNumber"))
                    WAE2E.HighlyStructuredMessage.encode(m.phoneNumber, w.uint32(18).fork()).ldelim();
                return w;
            };

            CallButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateButton.CallButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.phoneNumber = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            CallButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.TemplateButton.CallButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.TemplateButton.CallButton();
                if (d.displayText != null) {
                    if (typeof d.displayText !== "object")
                        throw TypeError(".WAE2E.TemplateButton.CallButton.displayText: object expected");
                    m.displayText = WAE2E.HighlyStructuredMessage.fromObject(d.displayText, n + 1);
                }
                if (d.phoneNumber != null) {
                    if (typeof d.phoneNumber !== "object")
                        throw TypeError(".WAE2E.TemplateButton.CallButton.phoneNumber: object expected");
                    m.phoneNumber = WAE2E.HighlyStructuredMessage.fromObject(d.phoneNumber, n + 1);
                }
                return m;
            };

            CallButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = WAE2E.HighlyStructuredMessage.toObject(m.displayText, o);
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.phoneNumber != null && m.hasOwnProperty("phoneNumber")) {
                    d.phoneNumber = WAE2E.HighlyStructuredMessage.toObject(m.phoneNumber, o);
                    if (o.oneofs)
                        d._phoneNumber = "phoneNumber";
                }
                return d;
            };

            CallButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.TemplateButton.CallButton";
            };

            return CallButton;
        })();

        TemplateButton.URLButton = (function() {

            function URLButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            URLButton.prototype.displayText = null;
            URLButton.prototype.URL = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(URLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(URLButton.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            URLButton.create = function create(properties) {
                return new URLButton(properties);
            };

            URLButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    WAE2E.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork()).ldelim();
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    WAE2E.HighlyStructuredMessage.encode(m.URL, w.uint32(18).fork()).ldelim();
                return w;
            };

            URLButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateButton.URLButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.URL = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            URLButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.TemplateButton.URLButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.TemplateButton.URLButton();
                if (d.displayText != null) {
                    if (typeof d.displayText !== "object")
                        throw TypeError(".WAE2E.TemplateButton.URLButton.displayText: object expected");
                    m.displayText = WAE2E.HighlyStructuredMessage.fromObject(d.displayText, n + 1);
                }
                if (d.URL != null) {
                    if (typeof d.URL !== "object")
                        throw TypeError(".WAE2E.TemplateButton.URLButton.URL: object expected");
                    m.URL = WAE2E.HighlyStructuredMessage.fromObject(d.URL, n + 1);
                }
                return m;
            };

            URLButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = WAE2E.HighlyStructuredMessage.toObject(m.displayText, o);
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = WAE2E.HighlyStructuredMessage.toObject(m.URL, o);
                    if (o.oneofs)
                        d._URL = "URL";
                }
                return d;
            };

            URLButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            URLButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.TemplateButton.URLButton";
            };

            return URLButton;
        })();

        TemplateButton.QuickReplyButton = (function() {

            function QuickReplyButton(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            QuickReplyButton.prototype.displayText = null;
            QuickReplyButton.prototype.ID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(QuickReplyButton.prototype, "_ID", {
                get: $util.oneOfGetter($oneOfFields = ["ID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuickReplyButton.create = function create(properties) {
                return new QuickReplyButton(properties);
            };

            QuickReplyButton.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.displayText != null && Object.hasOwnProperty.call(m, "displayText"))
                    WAE2E.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork()).ldelim();
                if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                    w.uint32(18).string(m.ID);
                return w;
            };

            QuickReplyButton.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.TemplateButton.QuickReplyButton();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.displayText = WAE2E.HighlyStructuredMessage.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 2: {
                            m.ID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            QuickReplyButton.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.TemplateButton.QuickReplyButton)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.TemplateButton.QuickReplyButton();
                if (d.displayText != null) {
                    if (typeof d.displayText !== "object")
                        throw TypeError(".WAE2E.TemplateButton.QuickReplyButton.displayText: object expected");
                    m.displayText = WAE2E.HighlyStructuredMessage.fromObject(d.displayText, n + 1);
                }
                if (d.ID != null) {
                    m.ID = String(d.ID);
                }
                return m;
            };

            QuickReplyButton.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.displayText != null && m.hasOwnProperty("displayText")) {
                    d.displayText = WAE2E.HighlyStructuredMessage.toObject(m.displayText, o);
                    if (o.oneofs)
                        d._displayText = "displayText";
                }
                if (m.ID != null && m.hasOwnProperty("ID")) {
                    d.ID = m.ID;
                    if (o.oneofs)
                        d._ID = "ID";
                }
                return d;
            };

            QuickReplyButton.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuickReplyButton.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.TemplateButton.QuickReplyButton";
            };

            return QuickReplyButton;
        })();

        return TemplateButton;
    })();

    WAE2E.Money = (function() {

        function Money(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        Money.prototype.value = null;
        Money.prototype.offset = null;
        Money.prototype.currencyCode = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_offset", {
            get: $util.oneOfGetter($oneOfFields = ["offset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(Money.prototype, "_currencyCode", {
            get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Money.create = function create(properties) {
            return new Money(properties);
        };

        Money.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.value != null && Object.hasOwnProperty.call(m, "value"))
                w.uint32(8).int64(m.value);
            if (m.offset != null && Object.hasOwnProperty.call(m, "offset"))
                w.uint32(16).uint32(m.offset);
            if (m.currencyCode != null && Object.hasOwnProperty.call(m, "currencyCode"))
                w.uint32(26).string(m.currencyCode);
            return w;
        };

        Money.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.Money();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.value = r.int64();
                        break;
                    }
                case 2: {
                        m.offset = r.uint32();
                        break;
                    }
                case 3: {
                        m.currencyCode = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        Money.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.Money)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.Money();
            if (d.value != null) {
                if ($util.Long)
                    (m.value = $util.Long.fromValue(d.value)).unsigned = false;
                else if (typeof d.value === "string")
                    m.value = parseInt(d.value, 10);
                else if (typeof d.value === "number")
                    m.value = d.value;
                else if (typeof d.value === "object")
                    m.value = new $util.LongBits(d.value.low >>> 0, d.value.high >>> 0).toNumber();
            }
            if (d.offset != null) {
                m.offset = d.offset >>> 0;
            }
            if (d.currencyCode != null) {
                m.currencyCode = String(d.currencyCode);
            }
            return m;
        };

        Money.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.value != null && m.hasOwnProperty("value")) {
                if (typeof m.value === "number")
                    d.value = o.longs === String ? String(m.value) : m.value;
                else
                    d.value = o.longs === String ? $util.Long.prototype.toString.call(m.value) : o.longs === Number ? new $util.LongBits(m.value.low >>> 0, m.value.high >>> 0).toNumber() : m.value;
                if (o.oneofs)
                    d._value = "value";
            }
            if (m.offset != null && m.hasOwnProperty("offset")) {
                d.offset = m.offset;
                if (o.oneofs)
                    d._offset = "offset";
            }
            if (m.currencyCode != null && m.hasOwnProperty("currencyCode")) {
                d.currencyCode = m.currencyCode;
                if (o.oneofs)
                    d._currencyCode = "currencyCode";
            }
            return d;
        };

        Money.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        Money.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.Money";
        };

        return Money;
    })();

    WAE2E.ActionLink = (function() {

        function ActionLink(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ActionLink.prototype.URL = null;
        ActionLink.prototype.buttonTitle = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ActionLink.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ActionLink.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ActionLink.create = function create(properties) {
            return new ActionLink(properties);
        };

        ActionLink.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(10).string(m.URL);
            if (m.buttonTitle != null && Object.hasOwnProperty.call(m, "buttonTitle"))
                w.uint32(18).string(m.buttonTitle);
            return w;
        };

        ActionLink.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.ActionLink();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.URL = r.string();
                        break;
                    }
                case 2: {
                        m.buttonTitle = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ActionLink.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.ActionLink)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.ActionLink();
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.buttonTitle != null) {
                m.buttonTitle = String(d.buttonTitle);
            }
            return m;
        };

        ActionLink.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.buttonTitle != null && m.hasOwnProperty("buttonTitle")) {
                d.buttonTitle = m.buttonTitle;
                if (o.oneofs)
                    d._buttonTitle = "buttonTitle";
            }
            return d;
        };

        ActionLink.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionLink.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.ActionLink";
        };

        return ActionLink;
    })();

    WAE2E.GroupMention = (function() {

        function GroupMention(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupMention.prototype.groupJID = null;
        GroupMention.prototype.groupSubject = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupMention.prototype, "_groupJID", {
            get: $util.oneOfGetter($oneOfFields = ["groupJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupMention.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupMention.create = function create(properties) {
            return new GroupMention(properties);
        };

        GroupMention.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.groupJID != null && Object.hasOwnProperty.call(m, "groupJID"))
                w.uint32(10).string(m.groupJID);
            if (m.groupSubject != null && Object.hasOwnProperty.call(m, "groupSubject"))
                w.uint32(18).string(m.groupSubject);
            return w;
        };

        GroupMention.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.GroupMention();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.groupJID = r.string();
                        break;
                    }
                case 2: {
                        m.groupSubject = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupMention.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.GroupMention)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.GroupMention();
            if (d.groupJID != null) {
                m.groupJID = String(d.groupJID);
            }
            if (d.groupSubject != null) {
                m.groupSubject = String(d.groupSubject);
            }
            return m;
        };

        GroupMention.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.groupJID != null && m.hasOwnProperty("groupJID")) {
                d.groupJID = m.groupJID;
                if (o.oneofs)
                    d._groupJID = "groupJID";
            }
            if (m.groupSubject != null && m.hasOwnProperty("groupSubject")) {
                d.groupSubject = m.groupSubject;
                if (o.oneofs)
                    d._groupSubject = "groupSubject";
            }
            return d;
        };

        GroupMention.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupMention.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.GroupMention";
        };

        return GroupMention;
    })();

    WAE2E.MessageSecretMessage = (function() {

        function MessageSecretMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageSecretMessage.prototype.version = null;
        MessageSecretMessage.prototype.encIV = null;
        MessageSecretMessage.prototype.encPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_encIV", {
            get: $util.oneOfGetter($oneOfFields = ["encIV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageSecretMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageSecretMessage.create = function create(properties) {
            return new MessageSecretMessage(properties);
        };

        MessageSecretMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.version != null && Object.hasOwnProperty.call(m, "version"))
                w.uint32(13).sfixed32(m.version);
            if (m.encIV != null && Object.hasOwnProperty.call(m, "encIV"))
                w.uint32(18).bytes(m.encIV);
            if (m.encPayload != null && Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(26).bytes(m.encPayload);
            return w;
        };

        MessageSecretMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MessageSecretMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.version = r.sfixed32();
                        break;
                    }
                case 2: {
                        m.encIV = r.bytes();
                        break;
                    }
                case 3: {
                        m.encPayload = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageSecretMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MessageSecretMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MessageSecretMessage();
            if (d.version != null) {
                m.version = d.version | 0;
            }
            if (d.encIV != null) {
                if (typeof d.encIV === "string")
                    $util.base64.decode(d.encIV, m.encIV = $util.newBuffer($util.base64.length(d.encIV)), 0);
                else if (d.encIV.length >= 0)
                    m.encIV = d.encIV;
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            return m;
        };

        MessageSecretMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.version != null && m.hasOwnProperty("version")) {
                d.version = m.version;
                if (o.oneofs)
                    d._version = "version";
            }
            if (m.encIV != null && m.hasOwnProperty("encIV")) {
                d.encIV = o.bytes === String ? $util.base64.encode(m.encIV, 0, m.encIV.length) : o.bytes === Array ? Array.prototype.slice.call(m.encIV) : m.encIV;
                if (o.oneofs)
                    d._encIV = "encIV";
            }
            if (m.encPayload != null && m.hasOwnProperty("encPayload")) {
                d.encPayload = o.bytes === String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encPayload) : m.encPayload;
                if (o.oneofs)
                    d._encPayload = "encPayload";
            }
            return d;
        };

        MessageSecretMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageSecretMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MessageSecretMessage";
        };

        return MessageSecretMessage;
    })();

    WAE2E.MediaNotifyMessage = (function() {

        function MediaNotifyMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MediaNotifyMessage.prototype.expressPathURL = null;
        MediaNotifyMessage.prototype.fileEncSHA256 = null;
        MediaNotifyMessage.prototype.fileLength = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_expressPathURL", {
            get: $util.oneOfGetter($oneOfFields = ["expressPathURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaNotifyMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaNotifyMessage.create = function create(properties) {
            return new MediaNotifyMessage(properties);
        };

        MediaNotifyMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.expressPathURL != null && Object.hasOwnProperty.call(m, "expressPathURL"))
                w.uint32(10).string(m.expressPathURL);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(18).bytes(m.fileEncSHA256);
            if (m.fileLength != null && Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(24).uint64(m.fileLength);
            return w;
        };

        MediaNotifyMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MediaNotifyMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.expressPathURL = r.string();
                        break;
                    }
                case 2: {
                        m.fileEncSHA256 = r.bytes();
                        break;
                    }
                case 3: {
                        m.fileLength = r.uint64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MediaNotifyMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MediaNotifyMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MediaNotifyMessage();
            if (d.expressPathURL != null) {
                m.expressPathURL = String(d.expressPathURL);
            }
            if (d.fileEncSHA256 != null) {
                if (typeof d.fileEncSHA256 === "string")
                    $util.base64.decode(d.fileEncSHA256, m.fileEncSHA256 = $util.newBuffer($util.base64.length(d.fileEncSHA256)), 0);
                else if (d.fileEncSHA256.length >= 0)
                    m.fileEncSHA256 = d.fileEncSHA256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    (m.fileLength = $util.Long.fromValue(d.fileLength)).unsigned = true;
                else if (typeof d.fileLength === "string")
                    m.fileLength = parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            return m;
        };

        MediaNotifyMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.expressPathURL != null && m.hasOwnProperty("expressPathURL")) {
                d.expressPathURL = m.expressPathURL;
                if (o.oneofs)
                    d._expressPathURL = "expressPathURL";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = o.bytes === String ? $util.base64.encode(m.fileEncSHA256, 0, m.fileEncSHA256.length) : o.bytes === Array ? Array.prototype.slice.call(m.fileEncSHA256) : m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.fileLength != null && m.hasOwnProperty("fileLength")) {
                if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === String ? String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                if (o.oneofs)
                    d._fileLength = "fileLength";
            }
            return d;
        };

        MediaNotifyMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaNotifyMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MediaNotifyMessage";
        };

        return MediaNotifyMessage;
    })();

    WAE2E.LIDMigrationMappingSyncMessage = (function() {

        function LIDMigrationMappingSyncMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LIDMigrationMappingSyncMessage.prototype.encodedMappingPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LIDMigrationMappingSyncMessage.prototype, "_encodedMappingPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encodedMappingPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LIDMigrationMappingSyncMessage.create = function create(properties) {
            return new LIDMigrationMappingSyncMessage(properties);
        };

        LIDMigrationMappingSyncMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.encodedMappingPayload != null && Object.hasOwnProperty.call(m, "encodedMappingPayload"))
                w.uint32(10).bytes(m.encodedMappingPayload);
            return w;
        };

        LIDMigrationMappingSyncMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.LIDMigrationMappingSyncMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.encodedMappingPayload = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LIDMigrationMappingSyncMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.LIDMigrationMappingSyncMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.LIDMigrationMappingSyncMessage();
            if (d.encodedMappingPayload != null) {
                if (typeof d.encodedMappingPayload === "string")
                    $util.base64.decode(d.encodedMappingPayload, m.encodedMappingPayload = $util.newBuffer($util.base64.length(d.encodedMappingPayload)), 0);
                else if (d.encodedMappingPayload.length >= 0)
                    m.encodedMappingPayload = d.encodedMappingPayload;
            }
            return m;
        };

        LIDMigrationMappingSyncMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.encodedMappingPayload != null && m.hasOwnProperty("encodedMappingPayload")) {
                d.encodedMappingPayload = o.bytes === String ? $util.base64.encode(m.encodedMappingPayload, 0, m.encodedMappingPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encodedMappingPayload) : m.encodedMappingPayload;
                if (o.oneofs)
                    d._encodedMappingPayload = "encodedMappingPayload";
            }
            return d;
        };

        LIDMigrationMappingSyncMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LIDMigrationMappingSyncMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.LIDMigrationMappingSyncMessage";
        };

        return LIDMigrationMappingSyncMessage;
    })();

    WAE2E.UrlTrackingMap = (function() {

        function UrlTrackingMap(p) {
            this.urlTrackingMapElements = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        UrlTrackingMap.prototype.urlTrackingMapElements = $util.emptyArray;

        UrlTrackingMap.create = function create(properties) {
            return new UrlTrackingMap(properties);
        };

        UrlTrackingMap.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.urlTrackingMapElements != null && m.urlTrackingMapElements.length) {
                for (var i = 0; i < m.urlTrackingMapElements.length; ++i)
                    WAE2E.UrlTrackingMap.UrlTrackingMapElement.encode(m.urlTrackingMapElements[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        UrlTrackingMap.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.UrlTrackingMap();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.urlTrackingMapElements && m.urlTrackingMapElements.length))
                            m.urlTrackingMapElements = [];
                        m.urlTrackingMapElements.push(WAE2E.UrlTrackingMap.UrlTrackingMapElement.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        UrlTrackingMap.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.UrlTrackingMap)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.UrlTrackingMap();
            if (d.urlTrackingMapElements) {
                if (!Array.isArray(d.urlTrackingMapElements))
                    throw TypeError(".WAE2E.UrlTrackingMap.urlTrackingMapElements: array expected");
                m.urlTrackingMapElements = [];
                for (var i = 0; i < d.urlTrackingMapElements.length; ++i) {
                    if (typeof d.urlTrackingMapElements[i] !== "object")
                        throw TypeError(".WAE2E.UrlTrackingMap.urlTrackingMapElements: object expected");
                    m.urlTrackingMapElements[i] = WAE2E.UrlTrackingMap.UrlTrackingMapElement.fromObject(d.urlTrackingMapElements[i], n + 1);
                }
            }
            return m;
        };

        UrlTrackingMap.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.urlTrackingMapElements = [];
            }
            if (m.urlTrackingMapElements && m.urlTrackingMapElements.length) {
                d.urlTrackingMapElements = [];
                for (var j = 0; j < m.urlTrackingMapElements.length; ++j) {
                    d.urlTrackingMapElements[j] = WAE2E.UrlTrackingMap.UrlTrackingMapElement.toObject(m.urlTrackingMapElements[j], o);
                }
            }
            return d;
        };

        UrlTrackingMap.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        UrlTrackingMap.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.UrlTrackingMap";
        };

        UrlTrackingMap.UrlTrackingMapElement = (function() {

            function UrlTrackingMapElement(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            UrlTrackingMapElement.prototype.originalURL = null;
            UrlTrackingMapElement.prototype.unconsentedUsersURL = null;
            UrlTrackingMapElement.prototype.consentedUsersURL = null;
            UrlTrackingMapElement.prototype.cardIndex = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_originalURL", {
                get: $util.oneOfGetter($oneOfFields = ["originalURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_unconsentedUsersURL", {
                get: $util.oneOfGetter($oneOfFields = ["unconsentedUsersURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_consentedUsersURL", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(UrlTrackingMapElement.prototype, "_cardIndex", {
                get: $util.oneOfGetter($oneOfFields = ["cardIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UrlTrackingMapElement.create = function create(properties) {
                return new UrlTrackingMapElement(properties);
            };

            UrlTrackingMapElement.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.originalURL != null && Object.hasOwnProperty.call(m, "originalURL"))
                    w.uint32(10).string(m.originalURL);
                if (m.unconsentedUsersURL != null && Object.hasOwnProperty.call(m, "unconsentedUsersURL"))
                    w.uint32(18).string(m.unconsentedUsersURL);
                if (m.consentedUsersURL != null && Object.hasOwnProperty.call(m, "consentedUsersURL"))
                    w.uint32(26).string(m.consentedUsersURL);
                if (m.cardIndex != null && Object.hasOwnProperty.call(m, "cardIndex"))
                    w.uint32(32).uint32(m.cardIndex);
                return w;
            };

            UrlTrackingMapElement.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.UrlTrackingMap.UrlTrackingMapElement();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.originalURL = r.string();
                            break;
                        }
                    case 2: {
                            m.unconsentedUsersURL = r.string();
                            break;
                        }
                    case 3: {
                            m.consentedUsersURL = r.string();
                            break;
                        }
                    case 4: {
                            m.cardIndex = r.uint32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            UrlTrackingMapElement.fromObject = function fromObject(d, n) {
                if (d instanceof WAE2E.UrlTrackingMap.UrlTrackingMapElement)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new WAE2E.UrlTrackingMap.UrlTrackingMapElement();
                if (d.originalURL != null) {
                    m.originalURL = String(d.originalURL);
                }
                if (d.unconsentedUsersURL != null) {
                    m.unconsentedUsersURL = String(d.unconsentedUsersURL);
                }
                if (d.consentedUsersURL != null) {
                    m.consentedUsersURL = String(d.consentedUsersURL);
                }
                if (d.cardIndex != null) {
                    m.cardIndex = d.cardIndex >>> 0;
                }
                return m;
            };

            UrlTrackingMapElement.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.originalURL != null && m.hasOwnProperty("originalURL")) {
                    d.originalURL = m.originalURL;
                    if (o.oneofs)
                        d._originalURL = "originalURL";
                }
                if (m.unconsentedUsersURL != null && m.hasOwnProperty("unconsentedUsersURL")) {
                    d.unconsentedUsersURL = m.unconsentedUsersURL;
                    if (o.oneofs)
                        d._unconsentedUsersURL = "unconsentedUsersURL";
                }
                if (m.consentedUsersURL != null && m.hasOwnProperty("consentedUsersURL")) {
                    d.consentedUsersURL = m.consentedUsersURL;
                    if (o.oneofs)
                        d._consentedUsersURL = "consentedUsersURL";
                }
                if (m.cardIndex != null && m.hasOwnProperty("cardIndex")) {
                    d.cardIndex = m.cardIndex;
                    if (o.oneofs)
                        d._cardIndex = "cardIndex";
                }
                return d;
            };

            UrlTrackingMapElement.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            UrlTrackingMapElement.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAE2E.UrlTrackingMap.UrlTrackingMapElement";
            };

            return UrlTrackingMapElement;
        })();

        return UrlTrackingMap;
    })();

    WAE2E.MemberLabel = (function() {

        function MemberLabel(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MemberLabel.prototype.label = null;
        MemberLabel.prototype.labelTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MemberLabel.prototype, "_label", {
            get: $util.oneOfGetter($oneOfFields = ["label"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MemberLabel.prototype, "_labelTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["labelTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MemberLabel.create = function create(properties) {
            return new MemberLabel(properties);
        };

        MemberLabel.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.label != null && Object.hasOwnProperty.call(m, "label"))
                w.uint32(10).string(m.label);
            if (m.labelTimestamp != null && Object.hasOwnProperty.call(m, "labelTimestamp"))
                w.uint32(16).int64(m.labelTimestamp);
            return w;
        };

        MemberLabel.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.MemberLabel();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.label = r.string();
                        break;
                    }
                case 2: {
                        m.labelTimestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MemberLabel.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.MemberLabel)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.MemberLabel();
            if (d.label != null) {
                m.label = String(d.label);
            }
            if (d.labelTimestamp != null) {
                if ($util.Long)
                    (m.labelTimestamp = $util.Long.fromValue(d.labelTimestamp)).unsigned = false;
                else if (typeof d.labelTimestamp === "string")
                    m.labelTimestamp = parseInt(d.labelTimestamp, 10);
                else if (typeof d.labelTimestamp === "number")
                    m.labelTimestamp = d.labelTimestamp;
                else if (typeof d.labelTimestamp === "object")
                    m.labelTimestamp = new $util.LongBits(d.labelTimestamp.low >>> 0, d.labelTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        MemberLabel.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.label != null && m.hasOwnProperty("label")) {
                d.label = m.label;
                if (o.oneofs)
                    d._label = "label";
            }
            if (m.labelTimestamp != null && m.hasOwnProperty("labelTimestamp")) {
                if (typeof m.labelTimestamp === "number")
                    d.labelTimestamp = o.longs === String ? String(m.labelTimestamp) : m.labelTimestamp;
                else
                    d.labelTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.labelTimestamp) : o.longs === Number ? new $util.LongBits(m.labelTimestamp.low >>> 0, m.labelTimestamp.high >>> 0).toNumber() : m.labelTimestamp;
                if (o.oneofs)
                    d._labelTimestamp = "labelTimestamp";
            }
            return d;
        };

        MemberLabel.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MemberLabel.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.MemberLabel";
        };

        return MemberLabel;
    })();

    WAE2E.AIRichResponseMessage = (function() {

        function AIRichResponseMessage(p) {
            this.submessages = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseMessage.prototype.messageType = null;
        AIRichResponseMessage.prototype.submessages = $util.emptyArray;
        AIRichResponseMessage.prototype.unifiedResponse = null;
        AIRichResponseMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_unifiedResponse", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponse"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseMessage.create = function create(properties) {
            return new AIRichResponseMessage(properties);
        };

        AIRichResponseMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageType != null && Object.hasOwnProperty.call(m, "messageType"))
                w.uint32(8).int32(m.messageType);
            if (m.submessages != null && m.submessages.length) {
                for (var i = 0; i < m.submessages.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseSubMessage.encode(m.submessages[i], w.uint32(18).fork()).ldelim();
            }
            if (m.unifiedResponse != null && Object.hasOwnProperty.call(m, "unifiedResponse"))
                $root.WAAICommon.AIRichResponseUnifiedResponse.encode(m.unifiedResponse, w.uint32(26).fork()).ldelim();
            if (m.contextInfo != null && Object.hasOwnProperty.call(m, "contextInfo"))
                WAE2E.ContextInfo.encode(m.contextInfo, w.uint32(34).fork()).ldelim();
            return w;
        };

        AIRichResponseMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AIRichResponseMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageType = r.int32();
                        break;
                    }
                case 2: {
                        if (!(m.submessages && m.submessages.length))
                            m.submessages = [];
                        m.submessages.push($root.WAAICommonDeprecated.AIRichResponseSubMessage.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        m.unifiedResponse = $root.WAAICommon.AIRichResponseUnifiedResponse.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.contextInfo = WAE2E.ContextInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseMessage.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AIRichResponseMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AIRichResponseMessage();
            switch (d.messageType) {
            default:
                if (typeof d.messageType === "number") {
                    m.messageType = d.messageType;
                    break;
                }
                break;
            case "AI_RICH_RESPONSE_TYPE_UNKNOWN":
            case 0:
                m.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_TYPE_STANDARD":
            case 1:
                m.messageType = 1;
                break;
            }
            if (d.submessages) {
                if (!Array.isArray(d.submessages))
                    throw TypeError(".WAE2E.AIRichResponseMessage.submessages: array expected");
                m.submessages = [];
                for (var i = 0; i < d.submessages.length; ++i) {
                    if (typeof d.submessages[i] !== "object")
                        throw TypeError(".WAE2E.AIRichResponseMessage.submessages: object expected");
                    m.submessages[i] = $root.WAAICommonDeprecated.AIRichResponseSubMessage.fromObject(d.submessages[i], n + 1);
                }
            }
            if (d.unifiedResponse != null) {
                if (typeof d.unifiedResponse !== "object")
                    throw TypeError(".WAE2E.AIRichResponseMessage.unifiedResponse: object expected");
                m.unifiedResponse = $root.WAAICommon.AIRichResponseUnifiedResponse.fromObject(d.unifiedResponse, n + 1);
            }
            if (d.contextInfo != null) {
                if (typeof d.contextInfo !== "object")
                    throw TypeError(".WAE2E.AIRichResponseMessage.contextInfo: object expected");
                m.contextInfo = WAE2E.ContextInfo.fromObject(d.contextInfo, n + 1);
            }
            return m;
        };

        AIRichResponseMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.submessages = [];
            }
            if (m.messageType != null && m.hasOwnProperty("messageType")) {
                d.messageType = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseMessageType[m.messageType] === undefined ? m.messageType : $root.WAAICommonDeprecated.AIRichResponseMessageType[m.messageType] : m.messageType;
                if (o.oneofs)
                    d._messageType = "messageType";
            }
            if (m.submessages && m.submessages.length) {
                d.submessages = [];
                for (var j = 0; j < m.submessages.length; ++j) {
                    d.submessages[j] = $root.WAAICommonDeprecated.AIRichResponseSubMessage.toObject(m.submessages[j], o);
                }
            }
            if (m.unifiedResponse != null && m.hasOwnProperty("unifiedResponse")) {
                d.unifiedResponse = $root.WAAICommon.AIRichResponseUnifiedResponse.toObject(m.unifiedResponse, o);
                if (o.oneofs)
                    d._unifiedResponse = "unifiedResponse";
            }
            if (m.contextInfo != null && m.hasOwnProperty("contextInfo")) {
                d.contextInfo = WAE2E.ContextInfo.toObject(m.contextInfo, o);
                if (o.oneofs)
                    d._contextInfo = "contextInfo";
            }
            return d;
        };

        AIRichResponseMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AIRichResponseMessage";
        };

        return AIRichResponseMessage;
    })();

    WAE2E.AIQueryFanout = (function() {

        function AIQueryFanout(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIQueryFanout.prototype.messageKey = null;
        AIQueryFanout.prototype.message = null;
        AIQueryFanout.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIQueryFanout.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIQueryFanout.create = function create(properties) {
            return new AIQueryFanout(properties);
        };

        AIQueryFanout.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageKey != null && Object.hasOwnProperty.call(m, "messageKey"))
                WACommon.MessageKey.encode(m.messageKey, w.uint32(10).fork()).ldelim();
            if (m.message != null && Object.hasOwnProperty.call(m, "message"))
                WAE2E.Message.encode(m.message, w.uint32(18).fork()).ldelim();
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(24).int64(m.timestamp);
            return w;
        };

        AIQueryFanout.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.AIQueryFanout();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.message = WAE2E.Message.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.timestamp = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIQueryFanout.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.AIQueryFanout)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.AIQueryFanout();
            if (d.messageKey != null) {
                if (typeof d.messageKey !== "object")
                    throw TypeError(".WAE2E.AIQueryFanout.messageKey: object expected");
                m.messageKey = WACommon.MessageKey.fromObject(d.messageKey, n + 1);
            }
            if (d.message != null) {
                if (typeof d.message !== "object")
                    throw TypeError(".WAE2E.AIQueryFanout.message: object expected");
                m.message = WAE2E.Message.fromObject(d.message, n + 1);
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = false;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            return m;
        };

        AIQueryFanout.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageKey != null && m.hasOwnProperty("messageKey")) {
                d.messageKey = WACommon.MessageKey.toObject(m.messageKey, o);
                if (o.oneofs)
                    d._messageKey = "messageKey";
            }
            if (m.message != null && m.hasOwnProperty("message")) {
                d.message = WAE2E.Message.toObject(m.message, o);
                if (o.oneofs)
                    d._message = "message";
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            return d;
        };

        AIQueryFanout.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIQueryFanout.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.AIQueryFanout";
        };

        return AIQueryFanout;
    })();

    WAE2E.GroupRootKeyShare = (function() {

        function GroupRootKeyShare(p) {
            this.keys = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupRootKeyShare.prototype.keys = $util.emptyArray;

        GroupRootKeyShare.create = function create(properties) {
            return new GroupRootKeyShare(properties);
        };

        GroupRootKeyShare.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keys != null && m.keys.length) {
                for (var i = 0; i < m.keys.length; ++i)
                    WAE2E.GroupRootKeyShareEntry.encode(m.keys[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        GroupRootKeyShare.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.GroupRootKeyShare();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.keys && m.keys.length))
                            m.keys = [];
                        m.keys.push(WAE2E.GroupRootKeyShareEntry.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupRootKeyShare.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.GroupRootKeyShare)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.GroupRootKeyShare();
            if (d.keys) {
                if (!Array.isArray(d.keys))
                    throw TypeError(".WAE2E.GroupRootKeyShare.keys: array expected");
                m.keys = [];
                for (var i = 0; i < d.keys.length; ++i) {
                    if (typeof d.keys[i] !== "object")
                        throw TypeError(".WAE2E.GroupRootKeyShare.keys: object expected");
                    m.keys[i] = WAE2E.GroupRootKeyShareEntry.fromObject(d.keys[i], n + 1);
                }
            }
            return m;
        };

        GroupRootKeyShare.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.keys = [];
            }
            if (m.keys && m.keys.length) {
                d.keys = [];
                for (var j = 0; j < m.keys.length; ++j) {
                    d.keys[j] = WAE2E.GroupRootKeyShareEntry.toObject(m.keys[j], o);
                }
            }
            return d;
        };

        GroupRootKeyShare.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupRootKeyShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.GroupRootKeyShare";
        };

        return GroupRootKeyShare;
    })();

    WAE2E.GroupRootKeyShareEntry = (function() {

        function GroupRootKeyShareEntry(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        GroupRootKeyShareEntry.prototype.groupRootKey = null;
        GroupRootKeyShareEntry.prototype.keyID = null;
        GroupRootKeyShareEntry.prototype.expiryTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_groupRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_keyID", {
            get: $util.oneOfGetter($oneOfFields = ["keyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(GroupRootKeyShareEntry.prototype, "_expiryTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupRootKeyShareEntry.create = function create(properties) {
            return new GroupRootKeyShareEntry(properties);
        };

        GroupRootKeyShareEntry.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.groupRootKey != null && Object.hasOwnProperty.call(m, "groupRootKey"))
                w.uint32(10).bytes(m.groupRootKey);
            if (m.keyID != null && Object.hasOwnProperty.call(m, "keyID"))
                w.uint32(18).string(m.keyID);
            if (m.expiryTimestampMS != null && Object.hasOwnProperty.call(m, "expiryTimestampMS"))
                w.uint32(24).int64(m.expiryTimestampMS);
            return w;
        };

        GroupRootKeyShareEntry.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WAE2E.GroupRootKeyShareEntry();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.groupRootKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.keyID = r.string();
                        break;
                    }
                case 3: {
                        m.expiryTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        GroupRootKeyShareEntry.fromObject = function fromObject(d, n) {
            if (d instanceof WAE2E.GroupRootKeyShareEntry)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WAE2E.GroupRootKeyShareEntry();
            if (d.groupRootKey != null) {
                if (typeof d.groupRootKey === "string")
                    $util.base64.decode(d.groupRootKey, m.groupRootKey = $util.newBuffer($util.base64.length(d.groupRootKey)), 0);
                else if (d.groupRootKey.length >= 0)
                    m.groupRootKey = d.groupRootKey;
            }
            if (d.keyID != null) {
                m.keyID = String(d.keyID);
            }
            if (d.expiryTimestampMS != null) {
                if ($util.Long)
                    (m.expiryTimestampMS = $util.Long.fromValue(d.expiryTimestampMS)).unsigned = false;
                else if (typeof d.expiryTimestampMS === "string")
                    m.expiryTimestampMS = parseInt(d.expiryTimestampMS, 10);
                else if (typeof d.expiryTimestampMS === "number")
                    m.expiryTimestampMS = d.expiryTimestampMS;
                else if (typeof d.expiryTimestampMS === "object")
                    m.expiryTimestampMS = new $util.LongBits(d.expiryTimestampMS.low >>> 0, d.expiryTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        GroupRootKeyShareEntry.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.groupRootKey != null && m.hasOwnProperty("groupRootKey")) {
                d.groupRootKey = o.bytes === String ? $util.base64.encode(m.groupRootKey, 0, m.groupRootKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.groupRootKey) : m.groupRootKey;
                if (o.oneofs)
                    d._groupRootKey = "groupRootKey";
            }
            if (m.keyID != null && m.hasOwnProperty("keyID")) {
                d.keyID = m.keyID;
                if (o.oneofs)
                    d._keyID = "keyID";
            }
            if (m.expiryTimestampMS != null && m.hasOwnProperty("expiryTimestampMS")) {
                if (typeof m.expiryTimestampMS === "number")
                    d.expiryTimestampMS = o.longs === String ? String(m.expiryTimestampMS) : m.expiryTimestampMS;
                else
                    d.expiryTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.expiryTimestampMS) : o.longs === Number ? new $util.LongBits(m.expiryTimestampMS.low >>> 0, m.expiryTimestampMS.high >>> 0).toNumber() : m.expiryTimestampMS;
                if (o.oneofs)
                    d._expiryTimestampMS = "expiryTimestampMS";
            }
            return d;
        };

        GroupRootKeyShareEntry.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupRootKeyShareEntry.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAE2E.GroupRootKeyShareEntry";
        };

        return GroupRootKeyShareEntry;
    })();

    return WAE2E;
})();

export const WAAICommon = $root.WAAICommon = (() => {

    const WAAICommon = {};

    WAAICommon.BotMetricsEntryPoint = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNDEFINED_ENTRY_POINT"] = 0;
        values[valuesById[1] = "FAVICON"] = 1;
        values[valuesById[2] = "CHATLIST"] = 2;
        values[valuesById[3] = "AISEARCH_NULL_STATE_PAPER_PLANE"] = 3;
        values[valuesById[4] = "AISEARCH_NULL_STATE_SUGGESTION"] = 4;
        values[valuesById[5] = "AISEARCH_TYPE_AHEAD_SUGGESTION"] = 5;
        values[valuesById[6] = "AISEARCH_TYPE_AHEAD_PAPER_PLANE"] = 6;
        values[valuesById[7] = "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST"] = 7;
        values[valuesById[8] = "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES"] = 8;
        values[valuesById[9] = "AIVOICE_SEARCH_BAR"] = 9;
        values[valuesById[10] = "AIVOICE_FAVICON"] = 10;
        values[valuesById[11] = "AISTUDIO"] = 11;
        values[valuesById[12] = "DEEPLINK"] = 12;
        values[valuesById[13] = "NOTIFICATION"] = 13;
        values[valuesById[14] = "PROFILE_MESSAGE_BUTTON"] = 14;
        values[valuesById[15] = "FORWARD"] = 15;
        values[valuesById[16] = "APP_SHORTCUT"] = 16;
        values[valuesById[17] = "FF_FAMILY"] = 17;
        values[valuesById[18] = "AI_TAB"] = 18;
        values[valuesById[19] = "AI_HOME"] = 19;
        values[valuesById[20] = "AI_DEEPLINK_IMMERSIVE"] = 20;
        values[valuesById[21] = "AI_DEEPLINK"] = 21;
        values[valuesById[22] = "META_AI_CHAT_SHORTCUT_AI_STUDIO"] = 22;
        values[valuesById[23] = "UGC_CHAT_SHORTCUT_AI_STUDIO"] = 23;
        values[valuesById[24] = "NEW_CHAT_AI_STUDIO"] = 24;
        values[valuesById[25] = "AIVOICE_FAVICON_CALL_HISTORY"] = 25;
        values[valuesById[26] = "ASK_META_AI_CONTEXT_MENU"] = 26;
        values[valuesById[27] = "ASK_META_AI_CONTEXT_MENU_1ON1"] = 27;
        values[valuesById[28] = "ASK_META_AI_CONTEXT_MENU_GROUP"] = 28;
        values[valuesById[29] = "INVOKE_META_AI_1ON1"] = 29;
        values[valuesById[30] = "INVOKE_META_AI_GROUP"] = 30;
        values[valuesById[31] = "META_AI_FORWARD"] = 31;
        values[valuesById[32] = "NEW_CHAT_AI_CONTACT"] = 32;
        values[valuesById[33] = "MESSAGE_QUICK_ACTION_1_ON_1_CHAT"] = 33;
        values[valuesById[34] = "MESSAGE_QUICK_ACTION_GROUP_CHAT"] = 34;
        values[valuesById[35] = "ATTACHMENT_TRAY_1_ON_1_CHAT"] = 35;
        values[valuesById[36] = "ATTACHMENT_TRAY_GROUP_CHAT"] = 36;
        values[valuesById[37] = "ASK_META_AI_MEDIA_VIEWER_1ON1"] = 37;
        values[valuesById[38] = "ASK_META_AI_MEDIA_VIEWER_GROUP"] = 38;
        values[valuesById[39] = "MEDIA_PICKER_1_ON_1_CHAT"] = 39;
        values[valuesById[40] = "MEDIA_PICKER_GROUP_CHAT"] = 40;
        values[valuesById[41] = "ASK_META_AI_NO_SEARCH_RESULTS"] = 41;
        values[valuesById[45] = "META_AI_SETTINGS"] = 45;
        values[valuesById[46] = "WEB_INTRO_PANEL"] = 46;
        values[valuesById[47] = "WEB_NAVIGATION_BAR"] = 47;
        values[valuesById[54] = "GROUP_MEMBER"] = 54;
        values[valuesById[55] = "CHATLIST_SEARCH"] = 55;
        values[valuesById[56] = "NEW_CHAT_LIST"] = 56;
        return values;
    })();

    WAAICommon.BotMetricsThreadEntryPoint = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[1] = "AI_TAB_THREAD"] = 1;
        values[valuesById[2] = "AI_HOME_THREAD"] = 2;
        values[valuesById[3] = "AI_DEEPLINK_IMMERSIVE_THREAD"] = 3;
        values[valuesById[4] = "AI_DEEPLINK_THREAD"] = 4;
        values[valuesById[5] = "ASK_META_AI_CONTEXT_MENU_THREAD"] = 5;
        return values;
    })();

    WAAICommon.BotSessionSource = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "NONE"] = 0;
        values[valuesById[1] = "NULL_STATE"] = 1;
        values[valuesById[2] = "TYPEAHEAD"] = 2;
        values[valuesById[3] = "USER_INPUT"] = 3;
        values[valuesById[4] = "EMU_FLASH"] = 4;
        values[valuesById[5] = "EMU_FLASH_FOLLOWUP"] = 5;
        values[valuesById[6] = "VOICE"] = 6;
        values[valuesById[7] = "AI_HOME_SESSION"] = 7;
        return values;
    })();

    WAAICommon.AISubscriptionRequestType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNSPECIFIED"] = 0;
        values[valuesById[1] = "THINK_HARD"] = 1;
        values[valuesById[2] = "IMAGE_GEN"] = 2;
        values[valuesById[3] = "VIDEO_GEN"] = 3;
        return values;
    })();

    WAAICommon.SessionTransparencyType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
        values[valuesById[1] = "NY_AI_SAFETY_DISCLAIMER"] = 1;
        return values;
    })();

    WAAICommon.BotPluginMetadata = (function() {

        function BotPluginMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotPluginMetadata.prototype.provider = null;
        BotPluginMetadata.prototype.pluginType = null;
        BotPluginMetadata.prototype.thumbnailCDNURL = null;
        BotPluginMetadata.prototype.profilePhotoCDNURL = null;
        BotPluginMetadata.prototype.searchProviderURL = null;
        BotPluginMetadata.prototype.referenceIndex = null;
        BotPluginMetadata.prototype.expectedLinksCount = null;
        BotPluginMetadata.prototype.searchQuery = null;
        BotPluginMetadata.prototype.parentPluginMessageKey = null;
        BotPluginMetadata.prototype.deprecatedField = null;
        BotPluginMetadata.prototype.parentPluginType = null;
        BotPluginMetadata.prototype.faviconCDNURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_provider", {
            get: $util.oneOfGetter($oneOfFields = ["provider"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_thumbnailCDNURL", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailCDNURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_profilePhotoCDNURL", {
            get: $util.oneOfGetter($oneOfFields = ["profilePhotoCDNURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_searchProviderURL", {
            get: $util.oneOfGetter($oneOfFields = ["searchProviderURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_referenceIndex", {
            get: $util.oneOfGetter($oneOfFields = ["referenceIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_expectedLinksCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedLinksCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_searchQuery", {
            get: $util.oneOfGetter($oneOfFields = ["searchQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_deprecatedField", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedField"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginType", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPluginMetadata.prototype, "_faviconCDNURL", {
            get: $util.oneOfGetter($oneOfFields = ["faviconCDNURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPluginMetadata.create = function create(properties) {
            return new BotPluginMetadata(properties);
        };

        BotPluginMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.provider != null && Object.hasOwnProperty.call(m, "provider"))
                w.uint32(8).int32(m.provider);
            if (m.pluginType != null && Object.hasOwnProperty.call(m, "pluginType"))
                w.uint32(16).int32(m.pluginType);
            if (m.thumbnailCDNURL != null && Object.hasOwnProperty.call(m, "thumbnailCDNURL"))
                w.uint32(26).string(m.thumbnailCDNURL);
            if (m.profilePhotoCDNURL != null && Object.hasOwnProperty.call(m, "profilePhotoCDNURL"))
                w.uint32(34).string(m.profilePhotoCDNURL);
            if (m.searchProviderURL != null && Object.hasOwnProperty.call(m, "searchProviderURL"))
                w.uint32(42).string(m.searchProviderURL);
            if (m.referenceIndex != null && Object.hasOwnProperty.call(m, "referenceIndex"))
                w.uint32(48).uint32(m.referenceIndex);
            if (m.expectedLinksCount != null && Object.hasOwnProperty.call(m, "expectedLinksCount"))
                w.uint32(56).uint32(m.expectedLinksCount);
            if (m.searchQuery != null && Object.hasOwnProperty.call(m, "searchQuery"))
                w.uint32(74).string(m.searchQuery);
            if (m.parentPluginMessageKey != null && Object.hasOwnProperty.call(m, "parentPluginMessageKey"))
                WACommon.MessageKey.encode(m.parentPluginMessageKey, w.uint32(82).fork()).ldelim();
            if (m.deprecatedField != null && Object.hasOwnProperty.call(m, "deprecatedField"))
                w.uint32(88).int32(m.deprecatedField);
            if (m.parentPluginType != null && Object.hasOwnProperty.call(m, "parentPluginType"))
                w.uint32(96).int32(m.parentPluginType);
            if (m.faviconCDNURL != null && Object.hasOwnProperty.call(m, "faviconCDNURL"))
                w.uint32(106).string(m.faviconCDNURL);
            return w;
        };

        BotPluginMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotPluginMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.provider = r.int32();
                        break;
                    }
                case 2: {
                        m.pluginType = r.int32();
                        break;
                    }
                case 3: {
                        m.thumbnailCDNURL = r.string();
                        break;
                    }
                case 4: {
                        m.profilePhotoCDNURL = r.string();
                        break;
                    }
                case 5: {
                        m.searchProviderURL = r.string();
                        break;
                    }
                case 6: {
                        m.referenceIndex = r.uint32();
                        break;
                    }
                case 7: {
                        m.expectedLinksCount = r.uint32();
                        break;
                    }
                case 9: {
                        m.searchQuery = r.string();
                        break;
                    }
                case 10: {
                        m.parentPluginMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.deprecatedField = r.int32();
                        break;
                    }
                case 12: {
                        m.parentPluginType = r.int32();
                        break;
                    }
                case 13: {
                        m.faviconCDNURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotPluginMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotPluginMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotPluginMetadata();
            switch (d.provider) {
            default:
                if (typeof d.provider === "number") {
                    m.provider = d.provider;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.provider = 0;
                break;
            case "BING":
            case 1:
                m.provider = 1;
                break;
            case "GOOGLE":
            case 2:
                m.provider = 2;
                break;
            case "SUPPORT":
            case 3:
                m.provider = 3;
                break;
            }
            switch (d.pluginType) {
            default:
                if (typeof d.pluginType === "number") {
                    m.pluginType = d.pluginType;
                    break;
                }
                break;
            case "UNKNOWN_PLUGIN":
            case 0:
                m.pluginType = 0;
                break;
            case "REELS":
            case 1:
                m.pluginType = 1;
                break;
            case "SEARCH":
            case 2:
                m.pluginType = 2;
                break;
            }
            if (d.thumbnailCDNURL != null) {
                m.thumbnailCDNURL = String(d.thumbnailCDNURL);
            }
            if (d.profilePhotoCDNURL != null) {
                m.profilePhotoCDNURL = String(d.profilePhotoCDNURL);
            }
            if (d.searchProviderURL != null) {
                m.searchProviderURL = String(d.searchProviderURL);
            }
            if (d.referenceIndex != null) {
                m.referenceIndex = d.referenceIndex >>> 0;
            }
            if (d.expectedLinksCount != null) {
                m.expectedLinksCount = d.expectedLinksCount >>> 0;
            }
            if (d.searchQuery != null) {
                m.searchQuery = String(d.searchQuery);
            }
            if (d.parentPluginMessageKey != null) {
                if (typeof d.parentPluginMessageKey !== "object")
                    throw TypeError(".WAAICommon.BotPluginMetadata.parentPluginMessageKey: object expected");
                m.parentPluginMessageKey = WACommon.MessageKey.fromObject(d.parentPluginMessageKey, n + 1);
            }
            switch (d.deprecatedField) {
            default:
                if (typeof d.deprecatedField === "number") {
                    m.deprecatedField = d.deprecatedField;
                    break;
                }
                break;
            case "UNKNOWN_PLUGIN":
            case 0:
                m.deprecatedField = 0;
                break;
            case "REELS":
            case 1:
                m.deprecatedField = 1;
                break;
            case "SEARCH":
            case 2:
                m.deprecatedField = 2;
                break;
            }
            switch (d.parentPluginType) {
            default:
                if (typeof d.parentPluginType === "number") {
                    m.parentPluginType = d.parentPluginType;
                    break;
                }
                break;
            case "UNKNOWN_PLUGIN":
            case 0:
                m.parentPluginType = 0;
                break;
            case "REELS":
            case 1:
                m.parentPluginType = 1;
                break;
            case "SEARCH":
            case 2:
                m.parentPluginType = 2;
                break;
            }
            if (d.faviconCDNURL != null) {
                m.faviconCDNURL = String(d.faviconCDNURL);
            }
            return m;
        };

        BotPluginMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.provider != null && m.hasOwnProperty("provider")) {
                d.provider = o.enums === String ? $root.WAAICommon.BotPluginMetadata.SearchProvider[m.provider] === undefined ? m.provider : $root.WAAICommon.BotPluginMetadata.SearchProvider[m.provider] : m.provider;
                if (o.oneofs)
                    d._provider = "provider";
            }
            if (m.pluginType != null && m.hasOwnProperty("pluginType")) {
                d.pluginType = o.enums === String ? $root.WAAICommon.BotPluginMetadata.PluginType[m.pluginType] === undefined ? m.pluginType : $root.WAAICommon.BotPluginMetadata.PluginType[m.pluginType] : m.pluginType;
                if (o.oneofs)
                    d._pluginType = "pluginType";
            }
            if (m.thumbnailCDNURL != null && m.hasOwnProperty("thumbnailCDNURL")) {
                d.thumbnailCDNURL = m.thumbnailCDNURL;
                if (o.oneofs)
                    d._thumbnailCDNURL = "thumbnailCDNURL";
            }
            if (m.profilePhotoCDNURL != null && m.hasOwnProperty("profilePhotoCDNURL")) {
                d.profilePhotoCDNURL = m.profilePhotoCDNURL;
                if (o.oneofs)
                    d._profilePhotoCDNURL = "profilePhotoCDNURL";
            }
            if (m.searchProviderURL != null && m.hasOwnProperty("searchProviderURL")) {
                d.searchProviderURL = m.searchProviderURL;
                if (o.oneofs)
                    d._searchProviderURL = "searchProviderURL";
            }
            if (m.referenceIndex != null && m.hasOwnProperty("referenceIndex")) {
                d.referenceIndex = m.referenceIndex;
                if (o.oneofs)
                    d._referenceIndex = "referenceIndex";
            }
            if (m.expectedLinksCount != null && m.hasOwnProperty("expectedLinksCount")) {
                d.expectedLinksCount = m.expectedLinksCount;
                if (o.oneofs)
                    d._expectedLinksCount = "expectedLinksCount";
            }
            if (m.searchQuery != null && m.hasOwnProperty("searchQuery")) {
                d.searchQuery = m.searchQuery;
                if (o.oneofs)
                    d._searchQuery = "searchQuery";
            }
            if (m.parentPluginMessageKey != null && m.hasOwnProperty("parentPluginMessageKey")) {
                d.parentPluginMessageKey = WACommon.MessageKey.toObject(m.parentPluginMessageKey, o);
                if (o.oneofs)
                    d._parentPluginMessageKey = "parentPluginMessageKey";
            }
            if (m.deprecatedField != null && m.hasOwnProperty("deprecatedField")) {
                d.deprecatedField = o.enums === String ? $root.WAAICommon.BotPluginMetadata.PluginType[m.deprecatedField] === undefined ? m.deprecatedField : $root.WAAICommon.BotPluginMetadata.PluginType[m.deprecatedField] : m.deprecatedField;
                if (o.oneofs)
                    d._deprecatedField = "deprecatedField";
            }
            if (m.parentPluginType != null && m.hasOwnProperty("parentPluginType")) {
                d.parentPluginType = o.enums === String ? $root.WAAICommon.BotPluginMetadata.PluginType[m.parentPluginType] === undefined ? m.parentPluginType : $root.WAAICommon.BotPluginMetadata.PluginType[m.parentPluginType] : m.parentPluginType;
                if (o.oneofs)
                    d._parentPluginType = "parentPluginType";
            }
            if (m.faviconCDNURL != null && m.hasOwnProperty("faviconCDNURL")) {
                d.faviconCDNURL = m.faviconCDNURL;
                if (o.oneofs)
                    d._faviconCDNURL = "faviconCDNURL";
            }
            return d;
        };

        BotPluginMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPluginMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotPluginMetadata";
        };

        BotPluginMetadata.PluginType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_PLUGIN"] = 0;
            values[valuesById[1] = "REELS"] = 1;
            values[valuesById[2] = "SEARCH"] = 2;
            return values;
        })();

        BotPluginMetadata.SearchProvider = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "BING"] = 1;
            values[valuesById[2] = "GOOGLE"] = 2;
            values[valuesById[3] = "SUPPORT"] = 3;
            return values;
        })();

        return BotPluginMetadata;
    })();

    WAAICommon.BotLinkedAccount = (function() {

        function BotLinkedAccount(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotLinkedAccount.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccount.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotLinkedAccount.create = function create(properties) {
            return new BotLinkedAccount(properties);
        };

        BotLinkedAccount.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            return w;
        };

        BotLinkedAccount.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotLinkedAccount();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotLinkedAccount.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotLinkedAccount)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotLinkedAccount();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "BOT_LINKED_ACCOUNT_TYPE_1P":
            case 0:
                m.type = 0;
                break;
            }
            return m;
        };

        BotLinkedAccount.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? $root.WAAICommon.BotLinkedAccount.BotLinkedAccountType[m.type] === undefined ? m.type : $root.WAAICommon.BotLinkedAccount.BotLinkedAccountType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        BotLinkedAccount.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotLinkedAccount.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotLinkedAccount";
        };

        BotLinkedAccount.BotLinkedAccountType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_LINKED_ACCOUNT_TYPE_1P"] = 0;
            return values;
        })();

        return BotLinkedAccount;
    })();

    WAAICommon.BotSignatureVerificationUseCaseProof = (function() {

        function BotSignatureVerificationUseCaseProof(p) {
            this.certificateChain = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotSignatureVerificationUseCaseProof.prototype.version = null;
        BotSignatureVerificationUseCaseProof.prototype.useCase = null;
        BotSignatureVerificationUseCaseProof.prototype.signature = null;
        BotSignatureVerificationUseCaseProof.prototype.certificateChain = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_useCase", {
            get: $util.oneOfGetter($oneOfFields = ["useCase"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSignatureVerificationUseCaseProof.create = function create(properties) {
            return new BotSignatureVerificationUseCaseProof(properties);
        };

        BotSignatureVerificationUseCaseProof.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.version != null && Object.hasOwnProperty.call(m, "version"))
                w.uint32(8).int32(m.version);
            if (m.useCase != null && Object.hasOwnProperty.call(m, "useCase"))
                w.uint32(16).int32(m.useCase);
            if (m.signature != null && Object.hasOwnProperty.call(m, "signature"))
                w.uint32(26).bytes(m.signature);
            if (m.certificateChain != null && m.certificateChain.length) {
                for (var i = 0; i < m.certificateChain.length; ++i)
                    w.uint32(34).bytes(m.certificateChain[i]);
            }
            return w;
        };

        BotSignatureVerificationUseCaseProof.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSignatureVerificationUseCaseProof();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.version = r.int32();
                        break;
                    }
                case 2: {
                        m.useCase = r.int32();
                        break;
                    }
                case 3: {
                        m.signature = r.bytes();
                        break;
                    }
                case 4: {
                        if (!(m.certificateChain && m.certificateChain.length))
                            m.certificateChain = [];
                        m.certificateChain.push(r.bytes());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotSignatureVerificationUseCaseProof.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotSignatureVerificationUseCaseProof)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotSignatureVerificationUseCaseProof();
            if (d.version != null) {
                m.version = d.version | 0;
            }
            switch (d.useCase) {
            default:
                if (typeof d.useCase === "number") {
                    m.useCase = d.useCase;
                    break;
                }
                break;
            case "UNSPECIFIED":
            case 0:
                m.useCase = 0;
                break;
            case "WA_BOT_MSG":
            case 1:
                m.useCase = 1;
                break;
            case "WA_TEE_BOT_MSG":
            case 2:
                m.useCase = 2;
                break;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            if (d.certificateChain) {
                if (!Array.isArray(d.certificateChain))
                    throw TypeError(".WAAICommon.BotSignatureVerificationUseCaseProof.certificateChain: array expected");
                m.certificateChain = [];
                for (var i = 0; i < d.certificateChain.length; ++i) {
                    if (typeof d.certificateChain[i] === "string")
                        $util.base64.decode(d.certificateChain[i], m.certificateChain[i] = $util.newBuffer($util.base64.length(d.certificateChain[i])), 0);
                    else if (d.certificateChain[i].length >= 0)
                        m.certificateChain[i] = d.certificateChain[i];
                }
            }
            return m;
        };

        BotSignatureVerificationUseCaseProof.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.certificateChain = [];
            }
            if (m.version != null && m.hasOwnProperty("version")) {
                d.version = m.version;
                if (o.oneofs)
                    d._version = "version";
            }
            if (m.useCase != null && m.hasOwnProperty("useCase")) {
                d.useCase = o.enums === String ? $root.WAAICommon.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] === undefined ? m.useCase : $root.WAAICommon.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] : m.useCase;
                if (o.oneofs)
                    d._useCase = "useCase";
            }
            if (m.signature != null && m.hasOwnProperty("signature")) {
                d.signature = o.bytes === String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === Array ? Array.prototype.slice.call(m.signature) : m.signature;
                if (o.oneofs)
                    d._signature = "signature";
            }
            if (m.certificateChain && m.certificateChain.length) {
                d.certificateChain = [];
                for (var j = 0; j < m.certificateChain.length; ++j) {
                    d.certificateChain[j] = o.bytes === String ? $util.base64.encode(m.certificateChain[j], 0, m.certificateChain[j].length) : o.bytes === Array ? Array.prototype.slice.call(m.certificateChain[j]) : m.certificateChain[j];
                }
            }
            return d;
        };

        BotSignatureVerificationUseCaseProof.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSignatureVerificationUseCaseProof.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotSignatureVerificationUseCaseProof";
        };

        BotSignatureVerificationUseCaseProof.BotSignatureUseCase = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNSPECIFIED"] = 0;
            values[valuesById[1] = "WA_BOT_MSG"] = 1;
            values[valuesById[2] = "WA_TEE_BOT_MSG"] = 2;
            return values;
        })();

        return BotSignatureVerificationUseCaseProof;
    })();

    WAAICommon.BotPromotionMessageMetadata = (function() {

        function BotPromotionMessageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotPromotionMessageMetadata.prototype.promotionType = null;
        BotPromotionMessageMetadata.prototype.buttonTitle = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromotionMessageMetadata.prototype, "_promotionType", {
            get: $util.oneOfGetter($oneOfFields = ["promotionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromotionMessageMetadata.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPromotionMessageMetadata.create = function create(properties) {
            return new BotPromotionMessageMetadata(properties);
        };

        BotPromotionMessageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.promotionType != null && Object.hasOwnProperty.call(m, "promotionType"))
                w.uint32(8).int32(m.promotionType);
            if (m.buttonTitle != null && Object.hasOwnProperty.call(m, "buttonTitle"))
                w.uint32(18).string(m.buttonTitle);
            return w;
        };

        BotPromotionMessageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotPromotionMessageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.promotionType = r.int32();
                        break;
                    }
                case 2: {
                        m.buttonTitle = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotPromotionMessageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotPromotionMessageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotPromotionMessageMetadata();
            switch (d.promotionType) {
            default:
                if (typeof d.promotionType === "number") {
                    m.promotionType = d.promotionType;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                m.promotionType = 0;
                break;
            case "C50":
            case 1:
                m.promotionType = 1;
                break;
            case "SURVEY_PLATFORM":
            case 2:
                m.promotionType = 2;
                break;
            }
            if (d.buttonTitle != null) {
                m.buttonTitle = String(d.buttonTitle);
            }
            return m;
        };

        BotPromotionMessageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.promotionType != null && m.hasOwnProperty("promotionType")) {
                d.promotionType = o.enums === String ? $root.WAAICommon.BotPromotionMessageMetadata.BotPromotionType[m.promotionType] === undefined ? m.promotionType : $root.WAAICommon.BotPromotionMessageMetadata.BotPromotionType[m.promotionType] : m.promotionType;
                if (o.oneofs)
                    d._promotionType = "promotionType";
            }
            if (m.buttonTitle != null && m.hasOwnProperty("buttonTitle")) {
                d.buttonTitle = m.buttonTitle;
                if (o.oneofs)
                    d._buttonTitle = "buttonTitle";
            }
            return d;
        };

        BotPromotionMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromotionMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotPromotionMessageMetadata";
        };

        BotPromotionMessageMetadata.BotPromotionType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "C50"] = 1;
            values[valuesById[2] = "SURVEY_PLATFORM"] = 2;
            return values;
        })();

        return BotPromotionMessageMetadata;
    })();

    WAAICommon.BotMediaMetadata = (function() {

        function BotMediaMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMediaMetadata.prototype.fileSHA256 = null;
        BotMediaMetadata.prototype.mediaKey = null;
        BotMediaMetadata.prototype.fileEncSHA256 = null;
        BotMediaMetadata.prototype.directPath = null;
        BotMediaMetadata.prototype.mediaKeyTimestamp = null;
        BotMediaMetadata.prototype.mimetype = null;
        BotMediaMetadata.prototype.orientationType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_fileSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_fileEncSHA256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSHA256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMediaMetadata.prototype, "_orientationType", {
            get: $util.oneOfGetter($oneOfFields = ["orientationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMediaMetadata.create = function create(properties) {
            return new BotMediaMetadata(properties);
        };

        BotMediaMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.fileSHA256 != null && Object.hasOwnProperty.call(m, "fileSHA256"))
                w.uint32(10).string(m.fileSHA256);
            if (m.mediaKey != null && Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(18).string(m.mediaKey);
            if (m.fileEncSHA256 != null && Object.hasOwnProperty.call(m, "fileEncSHA256"))
                w.uint32(26).string(m.fileEncSHA256);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(34).string(m.directPath);
            if (m.mediaKeyTimestamp != null && Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(40).int64(m.mediaKeyTimestamp);
            if (m.mimetype != null && Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(50).string(m.mimetype);
            if (m.orientationType != null && Object.hasOwnProperty.call(m, "orientationType"))
                w.uint32(56).int32(m.orientationType);
            return w;
        };

        BotMediaMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMediaMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.fileSHA256 = r.string();
                        break;
                    }
                case 2: {
                        m.mediaKey = r.string();
                        break;
                    }
                case 3: {
                        m.fileEncSHA256 = r.string();
                        break;
                    }
                case 4: {
                        m.directPath = r.string();
                        break;
                    }
                case 5: {
                        m.mediaKeyTimestamp = r.int64();
                        break;
                    }
                case 6: {
                        m.mimetype = r.string();
                        break;
                    }
                case 7: {
                        m.orientationType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMediaMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMediaMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMediaMetadata();
            if (d.fileSHA256 != null) {
                m.fileSHA256 = String(d.fileSHA256);
            }
            if (d.mediaKey != null) {
                m.mediaKey = String(d.mediaKey);
            }
            if (d.fileEncSHA256 != null) {
                m.fileEncSHA256 = String(d.fileEncSHA256);
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    (m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp)).unsigned = false;
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.mimetype != null) {
                m.mimetype = String(d.mimetype);
            }
            switch (d.orientationType) {
            default:
                if (typeof d.orientationType === "number") {
                    m.orientationType = d.orientationType;
                    break;
                }
                break;
            case "CENTER":
            case 1:
                m.orientationType = 1;
                break;
            case "LEFT":
            case 2:
                m.orientationType = 2;
                break;
            case "RIGHT":
            case 3:
                m.orientationType = 3;
                break;
            }
            return m;
        };

        BotMediaMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.fileSHA256 != null && m.hasOwnProperty("fileSHA256")) {
                d.fileSHA256 = m.fileSHA256;
                if (o.oneofs)
                    d._fileSHA256 = "fileSHA256";
            }
            if (m.mediaKey != null && m.hasOwnProperty("mediaKey")) {
                d.mediaKey = m.mediaKey;
                if (o.oneofs)
                    d._mediaKey = "mediaKey";
            }
            if (m.fileEncSHA256 != null && m.hasOwnProperty("fileEncSHA256")) {
                d.fileEncSHA256 = m.fileEncSHA256;
                if (o.oneofs)
                    d._fileEncSHA256 = "fileEncSHA256";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.mediaKeyTimestamp != null && m.hasOwnProperty("mediaKeyTimestamp")) {
                if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === String ? String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                if (o.oneofs)
                    d._mediaKeyTimestamp = "mediaKeyTimestamp";
            }
            if (m.mimetype != null && m.hasOwnProperty("mimetype")) {
                d.mimetype = m.mimetype;
                if (o.oneofs)
                    d._mimetype = "mimetype";
            }
            if (m.orientationType != null && m.hasOwnProperty("orientationType")) {
                d.orientationType = o.enums === String ? $root.WAAICommon.BotMediaMetadata.OrientationType[m.orientationType] === undefined ? m.orientationType : $root.WAAICommon.BotMediaMetadata.OrientationType[m.orientationType] : m.orientationType;
                if (o.oneofs)
                    d._orientationType = "orientationType";
            }
            return d;
        };

        BotMediaMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMediaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMediaMetadata";
        };

        BotMediaMetadata.OrientationType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "CENTER"] = 1;
            values[valuesById[2] = "LEFT"] = 2;
            values[valuesById[3] = "RIGHT"] = 3;
            return values;
        })();

        return BotMediaMetadata;
    })();

    WAAICommon.BotReminderMetadata = (function() {

        function BotReminderMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotReminderMetadata.prototype.requestMessageKey = null;
        BotReminderMetadata.prototype.action = null;
        BotReminderMetadata.prototype.name = null;
        BotReminderMetadata.prototype.nextTriggerTimestamp = null;
        BotReminderMetadata.prototype.frequency = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_action", {
            get: $util.oneOfGetter($oneOfFields = ["action"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_nextTriggerTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["nextTriggerTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotReminderMetadata.prototype, "_frequency", {
            get: $util.oneOfGetter($oneOfFields = ["frequency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotReminderMetadata.create = function create(properties) {
            return new BotReminderMetadata(properties);
        };

        BotReminderMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.requestMessageKey != null && Object.hasOwnProperty.call(m, "requestMessageKey"))
                WACommon.MessageKey.encode(m.requestMessageKey, w.uint32(10).fork()).ldelim();
            if (m.action != null && Object.hasOwnProperty.call(m, "action"))
                w.uint32(16).int32(m.action);
            if (m.name != null && Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            if (m.nextTriggerTimestamp != null && Object.hasOwnProperty.call(m, "nextTriggerTimestamp"))
                w.uint32(32).uint64(m.nextTriggerTimestamp);
            if (m.frequency != null && Object.hasOwnProperty.call(m, "frequency"))
                w.uint32(40).int32(m.frequency);
            return w;
        };

        BotReminderMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotReminderMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.requestMessageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.action = r.int32();
                        break;
                    }
                case 3: {
                        m.name = r.string();
                        break;
                    }
                case 4: {
                        m.nextTriggerTimestamp = r.uint64();
                        break;
                    }
                case 5: {
                        m.frequency = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotReminderMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotReminderMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotReminderMetadata();
            if (d.requestMessageKey != null) {
                if (typeof d.requestMessageKey !== "object")
                    throw TypeError(".WAAICommon.BotReminderMetadata.requestMessageKey: object expected");
                m.requestMessageKey = WACommon.MessageKey.fromObject(d.requestMessageKey, n + 1);
            }
            switch (d.action) {
            default:
                if (typeof d.action === "number") {
                    m.action = d.action;
                    break;
                }
                break;
            case "NOTIFY":
            case 1:
                m.action = 1;
                break;
            case "CREATE":
            case 2:
                m.action = 2;
                break;
            case "DELETE":
            case 3:
                m.action = 3;
                break;
            case "UPDATE":
            case 4:
                m.action = 4;
                break;
            }
            if (d.name != null) {
                m.name = String(d.name);
            }
            if (d.nextTriggerTimestamp != null) {
                if ($util.Long)
                    (m.nextTriggerTimestamp = $util.Long.fromValue(d.nextTriggerTimestamp)).unsigned = true;
                else if (typeof d.nextTriggerTimestamp === "string")
                    m.nextTriggerTimestamp = parseInt(d.nextTriggerTimestamp, 10);
                else if (typeof d.nextTriggerTimestamp === "number")
                    m.nextTriggerTimestamp = d.nextTriggerTimestamp;
                else if (typeof d.nextTriggerTimestamp === "object")
                    m.nextTriggerTimestamp = new $util.LongBits(d.nextTriggerTimestamp.low >>> 0, d.nextTriggerTimestamp.high >>> 0).toNumber(true);
            }
            switch (d.frequency) {
            default:
                if (typeof d.frequency === "number") {
                    m.frequency = d.frequency;
                    break;
                }
                break;
            case "ONCE":
            case 1:
                m.frequency = 1;
                break;
            case "DAILY":
            case 2:
                m.frequency = 2;
                break;
            case "WEEKLY":
            case 3:
                m.frequency = 3;
                break;
            case "BIWEEKLY":
            case 4:
                m.frequency = 4;
                break;
            case "MONTHLY":
            case 5:
                m.frequency = 5;
                break;
            }
            return m;
        };

        BotReminderMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.requestMessageKey != null && m.hasOwnProperty("requestMessageKey")) {
                d.requestMessageKey = WACommon.MessageKey.toObject(m.requestMessageKey, o);
                if (o.oneofs)
                    d._requestMessageKey = "requestMessageKey";
            }
            if (m.action != null && m.hasOwnProperty("action")) {
                d.action = o.enums === String ? $root.WAAICommon.BotReminderMetadata.ReminderAction[m.action] === undefined ? m.action : $root.WAAICommon.BotReminderMetadata.ReminderAction[m.action] : m.action;
                if (o.oneofs)
                    d._action = "action";
            }
            if (m.name != null && m.hasOwnProperty("name")) {
                d.name = m.name;
                if (o.oneofs)
                    d._name = "name";
            }
            if (m.nextTriggerTimestamp != null && m.hasOwnProperty("nextTriggerTimestamp")) {
                if (typeof m.nextTriggerTimestamp === "number")
                    d.nextTriggerTimestamp = o.longs === String ? String(m.nextTriggerTimestamp) : m.nextTriggerTimestamp;
                else
                    d.nextTriggerTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.nextTriggerTimestamp) : o.longs === Number ? new $util.LongBits(m.nextTriggerTimestamp.low >>> 0, m.nextTriggerTimestamp.high >>> 0).toNumber(true) : m.nextTriggerTimestamp;
                if (o.oneofs)
                    d._nextTriggerTimestamp = "nextTriggerTimestamp";
            }
            if (m.frequency != null && m.hasOwnProperty("frequency")) {
                d.frequency = o.enums === String ? $root.WAAICommon.BotReminderMetadata.ReminderFrequency[m.frequency] === undefined ? m.frequency : $root.WAAICommon.BotReminderMetadata.ReminderFrequency[m.frequency] : m.frequency;
                if (o.oneofs)
                    d._frequency = "frequency";
            }
            return d;
        };

        BotReminderMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotReminderMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotReminderMetadata";
        };

        BotReminderMetadata.ReminderFrequency = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "ONCE"] = 1;
            values[valuesById[2] = "DAILY"] = 2;
            values[valuesById[3] = "WEEKLY"] = 3;
            values[valuesById[4] = "BIWEEKLY"] = 4;
            values[valuesById[5] = "MONTHLY"] = 5;
            return values;
        })();

        BotReminderMetadata.ReminderAction = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "NOTIFY"] = 1;
            values[valuesById[2] = "CREATE"] = 2;
            values[valuesById[3] = "DELETE"] = 3;
            values[valuesById[4] = "UPDATE"] = 4;
            return values;
        })();

        return BotReminderMetadata;
    })();

    WAAICommon.BotModelMetadata = (function() {

        function BotModelMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotModelMetadata.prototype.modelType = null;
        BotModelMetadata.prototype.premiumModelStatus = null;
        BotModelMetadata.prototype.modelNameOverride = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_modelType", {
            get: $util.oneOfGetter($oneOfFields = ["modelType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_premiumModelStatus", {
            get: $util.oneOfGetter($oneOfFields = ["premiumModelStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotModelMetadata.prototype, "_modelNameOverride", {
            get: $util.oneOfGetter($oneOfFields = ["modelNameOverride"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotModelMetadata.create = function create(properties) {
            return new BotModelMetadata(properties);
        };

        BotModelMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.modelType != null && Object.hasOwnProperty.call(m, "modelType"))
                w.uint32(8).int32(m.modelType);
            if (m.premiumModelStatus != null && Object.hasOwnProperty.call(m, "premiumModelStatus"))
                w.uint32(16).int32(m.premiumModelStatus);
            if (m.modelNameOverride != null && Object.hasOwnProperty.call(m, "modelNameOverride"))
                w.uint32(26).string(m.modelNameOverride);
            return w;
        };

        BotModelMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotModelMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.modelType = r.int32();
                        break;
                    }
                case 2: {
                        m.premiumModelStatus = r.int32();
                        break;
                    }
                case 3: {
                        m.modelNameOverride = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotModelMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotModelMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotModelMetadata();
            switch (d.modelType) {
            default:
                if (typeof d.modelType === "number") {
                    m.modelType = d.modelType;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                m.modelType = 0;
                break;
            case "LLAMA_PROD":
            case 1:
                m.modelType = 1;
                break;
            case "LLAMA_PROD_PREMIUM":
            case 2:
                m.modelType = 2;
                break;
            }
            switch (d.premiumModelStatus) {
            default:
                if (typeof d.premiumModelStatus === "number") {
                    m.premiumModelStatus = d.premiumModelStatus;
                    break;
                }
                break;
            case "UNKNOWN_STATUS":
            case 0:
                m.premiumModelStatus = 0;
                break;
            case "AVAILABLE":
            case 1:
                m.premiumModelStatus = 1;
                break;
            case "QUOTA_EXCEED_LIMIT":
            case 2:
                m.premiumModelStatus = 2;
                break;
            }
            if (d.modelNameOverride != null) {
                m.modelNameOverride = String(d.modelNameOverride);
            }
            return m;
        };

        BotModelMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.modelType != null && m.hasOwnProperty("modelType")) {
                d.modelType = o.enums === String ? $root.WAAICommon.BotModelMetadata.ModelType[m.modelType] === undefined ? m.modelType : $root.WAAICommon.BotModelMetadata.ModelType[m.modelType] : m.modelType;
                if (o.oneofs)
                    d._modelType = "modelType";
            }
            if (m.premiumModelStatus != null && m.hasOwnProperty("premiumModelStatus")) {
                d.premiumModelStatus = o.enums === String ? $root.WAAICommon.BotModelMetadata.PremiumModelStatus[m.premiumModelStatus] === undefined ? m.premiumModelStatus : $root.WAAICommon.BotModelMetadata.PremiumModelStatus[m.premiumModelStatus] : m.premiumModelStatus;
                if (o.oneofs)
                    d._premiumModelStatus = "premiumModelStatus";
            }
            if (m.modelNameOverride != null && m.hasOwnProperty("modelNameOverride")) {
                d.modelNameOverride = m.modelNameOverride;
                if (o.oneofs)
                    d._modelNameOverride = "modelNameOverride";
            }
            return d;
        };

        BotModelMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotModelMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotModelMetadata";
        };

        BotModelMetadata.PremiumModelStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "AVAILABLE"] = 1;
            values[valuesById[2] = "QUOTA_EXCEED_LIMIT"] = 2;
            return values;
        })();

        BotModelMetadata.ModelType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "LLAMA_PROD"] = 1;
            values[valuesById[2] = "LLAMA_PROD_PREMIUM"] = 2;
            return values;
        })();

        return BotModelMetadata;
    })();

    WAAICommon.BotProgressIndicatorMetadata = (function() {

        function BotProgressIndicatorMetadata(p) {
            this.stepsMetadata = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotProgressIndicatorMetadata.prototype.progressDescription = null;
        BotProgressIndicatorMetadata.prototype.stepsMetadata = $util.emptyArray;
        BotProgressIndicatorMetadata.prototype.estimatedCompletionTime = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_progressDescription", {
            get: $util.oneOfGetter($oneOfFields = ["progressDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_estimatedCompletionTime", {
            get: $util.oneOfGetter($oneOfFields = ["estimatedCompletionTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotProgressIndicatorMetadata.create = function create(properties) {
            return new BotProgressIndicatorMetadata(properties);
        };

        BotProgressIndicatorMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.progressDescription != null && Object.hasOwnProperty.call(m, "progressDescription"))
                w.uint32(10).string(m.progressDescription);
            if (m.stepsMetadata != null && m.stepsMetadata.length) {
                for (var i = 0; i < m.stepsMetadata.length; ++i)
                    $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.encode(m.stepsMetadata[i], w.uint32(18).fork()).ldelim();
            }
            if (m.estimatedCompletionTime != null && Object.hasOwnProperty.call(m, "estimatedCompletionTime"))
                w.uint32(24).int64(m.estimatedCompletionTime);
            return w;
        };

        BotProgressIndicatorMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotProgressIndicatorMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.progressDescription = r.string();
                        break;
                    }
                case 2: {
                        if (!(m.stepsMetadata && m.stepsMetadata.length))
                            m.stepsMetadata = [];
                        m.stepsMetadata.push($root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        m.estimatedCompletionTime = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotProgressIndicatorMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotProgressIndicatorMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotProgressIndicatorMetadata();
            if (d.progressDescription != null) {
                m.progressDescription = String(d.progressDescription);
            }
            if (d.stepsMetadata) {
                if (!Array.isArray(d.stepsMetadata))
                    throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.stepsMetadata: array expected");
                m.stepsMetadata = [];
                for (var i = 0; i < d.stepsMetadata.length; ++i) {
                    if (typeof d.stepsMetadata[i] !== "object")
                        throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.stepsMetadata: object expected");
                    m.stepsMetadata[i] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.fromObject(d.stepsMetadata[i], n + 1);
                }
            }
            if (d.estimatedCompletionTime != null) {
                if ($util.Long)
                    (m.estimatedCompletionTime = $util.Long.fromValue(d.estimatedCompletionTime)).unsigned = false;
                else if (typeof d.estimatedCompletionTime === "string")
                    m.estimatedCompletionTime = parseInt(d.estimatedCompletionTime, 10);
                else if (typeof d.estimatedCompletionTime === "number")
                    m.estimatedCompletionTime = d.estimatedCompletionTime;
                else if (typeof d.estimatedCompletionTime === "object")
                    m.estimatedCompletionTime = new $util.LongBits(d.estimatedCompletionTime.low >>> 0, d.estimatedCompletionTime.high >>> 0).toNumber();
            }
            return m;
        };

        BotProgressIndicatorMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.stepsMetadata = [];
            }
            if (m.progressDescription != null && m.hasOwnProperty("progressDescription")) {
                d.progressDescription = m.progressDescription;
                if (o.oneofs)
                    d._progressDescription = "progressDescription";
            }
            if (m.stepsMetadata && m.stepsMetadata.length) {
                d.stepsMetadata = [];
                for (var j = 0; j < m.stepsMetadata.length; ++j) {
                    d.stepsMetadata[j] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.toObject(m.stepsMetadata[j], o);
                }
            }
            if (m.estimatedCompletionTime != null && m.hasOwnProperty("estimatedCompletionTime")) {
                if (typeof m.estimatedCompletionTime === "number")
                    d.estimatedCompletionTime = o.longs === String ? String(m.estimatedCompletionTime) : m.estimatedCompletionTime;
                else
                    d.estimatedCompletionTime = o.longs === String ? $util.Long.prototype.toString.call(m.estimatedCompletionTime) : o.longs === Number ? new $util.LongBits(m.estimatedCompletionTime.low >>> 0, m.estimatedCompletionTime.high >>> 0).toNumber() : m.estimatedCompletionTime;
                if (o.oneofs)
                    d._estimatedCompletionTime = "estimatedCompletionTime";
            }
            return d;
        };

        BotProgressIndicatorMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotProgressIndicatorMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotProgressIndicatorMetadata";
        };

        BotProgressIndicatorMetadata.BotPlanningStepMetadata = (function() {

            function BotPlanningStepMetadata(p) {
                this.sourcesMetadata = [];
                this.sections = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BotPlanningStepMetadata.prototype.statusTitle = null;
            BotPlanningStepMetadata.prototype.statusBody = null;
            BotPlanningStepMetadata.prototype.sourcesMetadata = $util.emptyArray;
            BotPlanningStepMetadata.prototype.status = null;
            BotPlanningStepMetadata.prototype.isReasoning = null;
            BotPlanningStepMetadata.prototype.isEnhancedSearch = null;
            BotPlanningStepMetadata.prototype.sections = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusTitle", {
                get: $util.oneOfGetter($oneOfFields = ["statusTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusBody", {
                get: $util.oneOfGetter($oneOfFields = ["statusBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_isReasoning", {
                get: $util.oneOfGetter($oneOfFields = ["isReasoning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotPlanningStepMetadata.prototype, "_isEnhancedSearch", {
                get: $util.oneOfGetter($oneOfFields = ["isEnhancedSearch"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotPlanningStepMetadata.create = function create(properties) {
                return new BotPlanningStepMetadata(properties);
            };

            BotPlanningStepMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.statusTitle != null && Object.hasOwnProperty.call(m, "statusTitle"))
                    w.uint32(10).string(m.statusTitle);
                if (m.statusBody != null && Object.hasOwnProperty.call(m, "statusBody"))
                    w.uint32(18).string(m.statusBody);
                if (m.sourcesMetadata != null && m.sourcesMetadata.length) {
                    for (var i = 0; i < m.sourcesMetadata.length; ++i)
                        $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.encode(m.sourcesMetadata[i], w.uint32(26).fork()).ldelim();
                }
                if (m.status != null && Object.hasOwnProperty.call(m, "status"))
                    w.uint32(32).int32(m.status);
                if (m.isReasoning != null && Object.hasOwnProperty.call(m, "isReasoning"))
                    w.uint32(40).bool(m.isReasoning);
                if (m.isEnhancedSearch != null && Object.hasOwnProperty.call(m, "isEnhancedSearch"))
                    w.uint32(48).bool(m.isEnhancedSearch);
                if (m.sections != null && m.sections.length) {
                    for (var i = 0; i < m.sections.length; ++i)
                        $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.encode(m.sections[i], w.uint32(58).fork()).ldelim();
                }
                return w;
            };

            BotPlanningStepMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.statusTitle = r.string();
                            break;
                        }
                    case 2: {
                            m.statusBody = r.string();
                            break;
                        }
                    case 3: {
                            if (!(m.sourcesMetadata && m.sourcesMetadata.length))
                                m.sourcesMetadata = [];
                            m.sourcesMetadata.push($root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    case 4: {
                            m.status = r.int32();
                            break;
                        }
                    case 5: {
                            m.isReasoning = r.bool();
                            break;
                        }
                    case 6: {
                            m.isEnhancedSearch = r.bool();
                            break;
                        }
                    case 7: {
                            if (!(m.sections && m.sections.length))
                                m.sections = [];
                            m.sections.push($root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BotPlanningStepMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata();
                if (d.statusTitle != null) {
                    m.statusTitle = String(d.statusTitle);
                }
                if (d.statusBody != null) {
                    m.statusBody = String(d.statusBody);
                }
                if (d.sourcesMetadata) {
                    if (!Array.isArray(d.sourcesMetadata))
                        throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: array expected");
                    m.sourcesMetadata = [];
                    for (var i = 0; i < d.sourcesMetadata.length; ++i) {
                        if (typeof d.sourcesMetadata[i] !== "object")
                            throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: object expected");
                        m.sourcesMetadata[i] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.fromObject(d.sourcesMetadata[i], n + 1);
                    }
                }
                switch (d.status) {
                default:
                    if (typeof d.status === "number") {
                        m.status = d.status;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.status = 0;
                    break;
                case "PLANNED":
                case 1:
                    m.status = 1;
                    break;
                case "EXECUTING":
                case 2:
                    m.status = 2;
                    break;
                case "FINISHED":
                case 3:
                    m.status = 3;
                    break;
                }
                if (d.isReasoning != null) {
                    m.isReasoning = Boolean(d.isReasoning);
                }
                if (d.isEnhancedSearch != null) {
                    m.isEnhancedSearch = Boolean(d.isEnhancedSearch);
                }
                if (d.sections) {
                    if (!Array.isArray(d.sections))
                        throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: array expected");
                    m.sections = [];
                    for (var i = 0; i < d.sections.length; ++i) {
                        if (typeof d.sections[i] !== "object")
                            throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: object expected");
                        m.sections[i] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.fromObject(d.sections[i], n + 1);
                    }
                }
                return m;
            };

            BotPlanningStepMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.sourcesMetadata = [];
                    d.sections = [];
                }
                if (m.statusTitle != null && m.hasOwnProperty("statusTitle")) {
                    d.statusTitle = m.statusTitle;
                    if (o.oneofs)
                        d._statusTitle = "statusTitle";
                }
                if (m.statusBody != null && m.hasOwnProperty("statusBody")) {
                    d.statusBody = m.statusBody;
                    if (o.oneofs)
                        d._statusBody = "statusBody";
                }
                if (m.sourcesMetadata && m.sourcesMetadata.length) {
                    d.sourcesMetadata = [];
                    for (var j = 0; j < m.sourcesMetadata.length; ++j) {
                        d.sourcesMetadata[j] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.toObject(m.sourcesMetadata[j], o);
                    }
                }
                if (m.status != null && m.hasOwnProperty("status")) {
                    d.status = o.enums === String ? $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[m.status] === undefined ? m.status : $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[m.status] : m.status;
                    if (o.oneofs)
                        d._status = "status";
                }
                if (m.isReasoning != null && m.hasOwnProperty("isReasoning")) {
                    d.isReasoning = m.isReasoning;
                    if (o.oneofs)
                        d._isReasoning = "isReasoning";
                }
                if (m.isEnhancedSearch != null && m.hasOwnProperty("isEnhancedSearch")) {
                    d.isEnhancedSearch = m.isEnhancedSearch;
                    if (o.oneofs)
                        d._isEnhancedSearch = "isEnhancedSearch";
                }
                if (m.sections && m.sections.length) {
                    d.sections = [];
                    for (var j = 0; j < m.sections.length; ++j) {
                        d.sections[j] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.toObject(m.sections[j], o);
                    }
                }
                return d;
            };

            BotPlanningStepMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotPlanningStepMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata";
            };

            BotPlanningStepMetadata.BotSearchSourceProvider = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_PROVIDER"] = 0;
                values[valuesById[1] = "OTHER"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "BING"] = 3;
                return values;
            })();

            BotPlanningStepMetadata.PlanningStepStatus = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "PLANNED"] = 1;
                values[valuesById[2] = "EXECUTING"] = 2;
                values[valuesById[3] = "FINISHED"] = 3;
                return values;
            })();

            BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata = (function() {

                function BotPlanningSearchSourcesMetadata(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                BotPlanningSearchSourcesMetadata.prototype.sourceTitle = null;
                BotPlanningSearchSourcesMetadata.prototype.provider = null;
                BotPlanningSearchSourcesMetadata.prototype.sourceURL = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceURL", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceURL"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningSearchSourcesMetadata.create = function create(properties) {
                    return new BotPlanningSearchSourcesMetadata(properties);
                };

                BotPlanningSearchSourcesMetadata.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.sourceTitle != null && Object.hasOwnProperty.call(m, "sourceTitle"))
                        w.uint32(10).string(m.sourceTitle);
                    if (m.provider != null && Object.hasOwnProperty.call(m, "provider"))
                        w.uint32(16).int32(m.provider);
                    if (m.sourceURL != null && Object.hasOwnProperty.call(m, "sourceURL"))
                        w.uint32(26).string(m.sourceURL);
                    return w;
                };

                BotPlanningSearchSourcesMetadata.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.sourceTitle = r.string();
                                break;
                            }
                        case 2: {
                                m.provider = r.int32();
                                break;
                            }
                        case 3: {
                                m.sourceURL = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                BotPlanningSearchSourcesMetadata.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata();
                    if (d.sourceTitle != null) {
                        m.sourceTitle = String(d.sourceTitle);
                    }
                    switch (d.provider) {
                    default:
                        if (typeof d.provider === "number") {
                            m.provider = d.provider;
                            break;
                        }
                        break;
                    case "UNKNOWN":
                    case 0:
                        m.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        m.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        m.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        m.provider = 3;
                        break;
                    }
                    if (d.sourceURL != null) {
                        m.sourceURL = String(d.sourceURL);
                    }
                    return m;
                };

                BotPlanningSearchSourcesMetadata.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.sourceTitle != null && m.hasOwnProperty("sourceTitle")) {
                        d.sourceTitle = m.sourceTitle;
                        if (o.oneofs)
                            d._sourceTitle = "sourceTitle";
                    }
                    if (m.provider != null && m.hasOwnProperty("provider")) {
                        d.provider = o.enums === String ? $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[m.provider] === undefined ? m.provider : $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[m.provider] : m.provider;
                        if (o.oneofs)
                            d._provider = "provider";
                    }
                    if (m.sourceURL != null && m.hasOwnProperty("sourceURL")) {
                        d.sourceURL = m.sourceURL;
                        if (o.oneofs)
                            d._sourceURL = "sourceURL";
                    }
                    return d;
                };

                BotPlanningSearchSourcesMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningSearchSourcesMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata";
                };

                BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider = (function() {
                    const valuesById = {}, values = Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "OTHER"] = 1;
                    values[valuesById[2] = "GOOGLE"] = 2;
                    values[valuesById[3] = "BING"] = 3;
                    return values;
                })();

                return BotPlanningSearchSourcesMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningStepSectionMetadata = (function() {

                function BotPlanningStepSectionMetadata(p) {
                    this.sourcesMetadata = [];
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                BotPlanningStepSectionMetadata.prototype.sectionTitle = null;
                BotPlanningStepSectionMetadata.prototype.sectionBody = null;
                BotPlanningStepSectionMetadata.prototype.sourcesMetadata = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionBody", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionBody"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningStepSectionMetadata.create = function create(properties) {
                    return new BotPlanningStepSectionMetadata(properties);
                };

                BotPlanningStepSectionMetadata.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.sectionTitle != null && Object.hasOwnProperty.call(m, "sectionTitle"))
                        w.uint32(10).string(m.sectionTitle);
                    if (m.sectionBody != null && Object.hasOwnProperty.call(m, "sectionBody"))
                        w.uint32(18).string(m.sectionBody);
                    if (m.sourcesMetadata != null && m.sourcesMetadata.length) {
                        for (var i = 0; i < m.sourcesMetadata.length; ++i)
                            $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.encode(m.sourcesMetadata[i], w.uint32(26).fork()).ldelim();
                    }
                    return w;
                };

                BotPlanningStepSectionMetadata.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.sectionTitle = r.string();
                                break;
                            }
                        case 2: {
                                m.sectionBody = r.string();
                                break;
                            }
                        case 3: {
                                if (!(m.sourcesMetadata && m.sourcesMetadata.length))
                                    m.sourcesMetadata = [];
                                m.sourcesMetadata.push($root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.decode(r, r.uint32(), undefined, n + 1));
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                BotPlanningStepSectionMetadata.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata();
                    if (d.sectionTitle != null) {
                        m.sectionTitle = String(d.sectionTitle);
                    }
                    if (d.sectionBody != null) {
                        m.sectionBody = String(d.sectionBody);
                    }
                    if (d.sourcesMetadata) {
                        if (!Array.isArray(d.sourcesMetadata))
                            throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: array expected");
                        m.sourcesMetadata = [];
                        for (var i = 0; i < d.sourcesMetadata.length; ++i) {
                            if (typeof d.sourcesMetadata[i] !== "object")
                                throw TypeError(".WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: object expected");
                            m.sourcesMetadata[i] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.fromObject(d.sourcesMetadata[i], n + 1);
                        }
                    }
                    return m;
                };

                BotPlanningStepSectionMetadata.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.sourcesMetadata = [];
                    }
                    if (m.sectionTitle != null && m.hasOwnProperty("sectionTitle")) {
                        d.sectionTitle = m.sectionTitle;
                        if (o.oneofs)
                            d._sectionTitle = "sectionTitle";
                    }
                    if (m.sectionBody != null && m.hasOwnProperty("sectionBody")) {
                        d.sectionBody = m.sectionBody;
                        if (o.oneofs)
                            d._sectionBody = "sectionBody";
                    }
                    if (m.sourcesMetadata && m.sourcesMetadata.length) {
                        d.sourcesMetadata = [];
                        for (var j = 0; j < m.sourcesMetadata.length; ++j) {
                            d.sourcesMetadata[j] = $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.toObject(m.sourcesMetadata[j], o);
                        }
                    }
                    return d;
                };

                BotPlanningStepSectionMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningStepSectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata";
                };

                return BotPlanningStepSectionMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningSearchSourceMetadata = (function() {

                function BotPlanningSearchSourceMetadata(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                BotPlanningSearchSourceMetadata.prototype.title = null;
                BotPlanningSearchSourceMetadata.prototype.provider = null;
                BotPlanningSearchSourceMetadata.prototype.sourceURL = null;
                BotPlanningSearchSourceMetadata.prototype.favIconURL = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_sourceURL", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceURL"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_favIconURL", {
                    get: $util.oneOfGetter($oneOfFields = ["favIconURL"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningSearchSourceMetadata.create = function create(properties) {
                    return new BotPlanningSearchSourceMetadata(properties);
                };

                BotPlanningSearchSourceMetadata.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.provider != null && Object.hasOwnProperty.call(m, "provider"))
                        w.uint32(16).int32(m.provider);
                    if (m.sourceURL != null && Object.hasOwnProperty.call(m, "sourceURL"))
                        w.uint32(26).string(m.sourceURL);
                    if (m.favIconURL != null && Object.hasOwnProperty.call(m, "favIconURL"))
                        w.uint32(34).string(m.favIconURL);
                    return w;
                };

                BotPlanningSearchSourceMetadata.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.title = r.string();
                                break;
                            }
                        case 2: {
                                m.provider = r.int32();
                                break;
                            }
                        case 3: {
                                m.sourceURL = r.string();
                                break;
                            }
                        case 4: {
                                m.favIconURL = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                BotPlanningSearchSourceMetadata.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata();
                    if (d.title != null) {
                        m.title = String(d.title);
                    }
                    switch (d.provider) {
                    default:
                        if (typeof d.provider === "number") {
                            m.provider = d.provider;
                            break;
                        }
                        break;
                    case "UNKNOWN_PROVIDER":
                    case 0:
                        m.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        m.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        m.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        m.provider = 3;
                        break;
                    }
                    if (d.sourceURL != null) {
                        m.sourceURL = String(d.sourceURL);
                    }
                    if (d.favIconURL != null) {
                        m.favIconURL = String(d.favIconURL);
                    }
                    return m;
                };

                BotPlanningSearchSourceMetadata.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.title != null && m.hasOwnProperty("title")) {
                        d.title = m.title;
                        if (o.oneofs)
                            d._title = "title";
                    }
                    if (m.provider != null && m.hasOwnProperty("provider")) {
                        d.provider = o.enums === String ? $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[m.provider] === undefined ? m.provider : $root.WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[m.provider] : m.provider;
                        if (o.oneofs)
                            d._provider = "provider";
                    }
                    if (m.sourceURL != null && m.hasOwnProperty("sourceURL")) {
                        d.sourceURL = m.sourceURL;
                        if (o.oneofs)
                            d._sourceURL = "sourceURL";
                    }
                    if (m.favIconURL != null && m.hasOwnProperty("favIconURL")) {
                        d.favIconURL = m.favIconURL;
                        if (o.oneofs)
                            d._favIconURL = "favIconURL";
                    }
                    return d;
                };

                BotPlanningSearchSourceMetadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningSearchSourceMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAAICommon.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata";
                };

                return BotPlanningSearchSourceMetadata;
            })();

            return BotPlanningStepMetadata;
        })();

        return BotProgressIndicatorMetadata;
    })();

    WAAICommon.BotCapabilityMetadata = (function() {

        function BotCapabilityMetadata(p) {
            this.capabilities = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotCapabilityMetadata.prototype.capabilities = $util.emptyArray;

        BotCapabilityMetadata.create = function create(properties) {
            return new BotCapabilityMetadata(properties);
        };

        BotCapabilityMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.capabilities != null && m.capabilities.length) {
                w.uint32(10).fork();
                for (var i = 0; i < m.capabilities.length; ++i)
                    w.int32(m.capabilities[i]);
                w.ldelim();
            }
            return w;
        };

        BotCapabilityMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotCapabilityMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.capabilities && m.capabilities.length))
                            m.capabilities = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.capabilities.push(r.int32());
                        } else
                            m.capabilities.push(r.int32());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotCapabilityMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotCapabilityMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotCapabilityMetadata();
            if (d.capabilities) {
                if (!Array.isArray(d.capabilities))
                    throw TypeError(".WAAICommon.BotCapabilityMetadata.capabilities: array expected");
                m.capabilities = [];
                for (var i = 0; i < d.capabilities.length; ++i) {
                    switch (d.capabilities[i]) {
                    default:
                        if (typeof d.capabilities[i] === "number") {
                            m.capabilities[i] = d.capabilities[i];
                            break;
                        }
                    case "UNKNOWN":
                    case 0:
                        m.capabilities[i] = 0;
                        break;
                    case "PROGRESS_INDICATOR":
                    case 1:
                        m.capabilities[i] = 1;
                        break;
                    case "RICH_RESPONSE_HEADING":
                    case 2:
                        m.capabilities[i] = 2;
                        break;
                    case "RICH_RESPONSE_NESTED_LIST":
                    case 3:
                        m.capabilities[i] = 3;
                        break;
                    case "AI_MEMORY":
                    case 4:
                        m.capabilities[i] = 4;
                        break;
                    case "RICH_RESPONSE_THREAD_SURFING":
                    case 5:
                        m.capabilities[i] = 5;
                        break;
                    case "RICH_RESPONSE_TABLE":
                    case 6:
                        m.capabilities[i] = 6;
                        break;
                    case "RICH_RESPONSE_CODE":
                    case 7:
                        m.capabilities[i] = 7;
                        break;
                    case "RICH_RESPONSE_STRUCTURED_RESPONSE":
                    case 8:
                        m.capabilities[i] = 8;
                        break;
                    case "RICH_RESPONSE_INLINE_IMAGE":
                    case 9:
                        m.capabilities[i] = 9;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_CONTROL":
                    case 10:
                        m.capabilities[i] = 10;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_1":
                    case 11:
                        m.capabilities[i] = 11;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_2":
                    case 12:
                        m.capabilities[i] = 12;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_3":
                    case 13:
                        m.capabilities[i] = 13;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_4":
                    case 14:
                        m.capabilities[i] = 14;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_5":
                    case 15:
                        m.capabilities[i] = 15;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_6":
                    case 16:
                        m.capabilities[i] = 16;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_7":
                    case 17:
                        m.capabilities[i] = 17;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_8":
                    case 18:
                        m.capabilities[i] = 18;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_9":
                    case 19:
                        m.capabilities[i] = 19;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_10":
                    case 20:
                        m.capabilities[i] = 20;
                        break;
                    case "RICH_RESPONSE_SUB_HEADING":
                    case 21:
                        m.capabilities[i] = 21;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE":
                    case 22:
                        m.capabilities[i] = 22;
                        break;
                    case "AI_STUDIO_UGC_MEMORY":
                    case 23:
                        m.capabilities[i] = 23;
                        break;
                    case "RICH_RESPONSE_LATEX":
                    case 24:
                        m.capabilities[i] = 24;
                        break;
                    case "RICH_RESPONSE_MAPS":
                    case 25:
                        m.capabilities[i] = 25;
                        break;
                    case "RICH_RESPONSE_INLINE_REELS":
                    case 26:
                        m.capabilities[i] = 26;
                        break;
                    case "AGENTIC_PLANNING":
                    case 27:
                        m.capabilities[i] = 27;
                        break;
                    case "ACCOUNT_LINKING":
                    case 28:
                        m.capabilities[i] = 28;
                        break;
                    case "STREAMING_DISAGGREGATION":
                    case 29:
                        m.capabilities[i] = 29;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE_3P":
                    case 30:
                        m.capabilities[i] = 30;
                        break;
                    case "RICH_RESPONSE_LATEX_INLINE":
                    case 31:
                        m.capabilities[i] = 31;
                        break;
                    case "QUERY_PLAN":
                    case 32:
                        m.capabilities[i] = 32;
                        break;
                    case "PROACTIVE_MESSAGE":
                    case 33:
                        m.capabilities[i] = 33;
                        break;
                    case "RICH_RESPONSE_UNIFIED_RESPONSE":
                    case 34:
                        m.capabilities[i] = 34;
                        break;
                    case "PROMOTION_MESSAGE":
                    case 35:
                        m.capabilities[i] = 35;
                        break;
                    case "SIMPLIFIED_PROFILE_PAGE":
                    case 36:
                        m.capabilities[i] = 36;
                        break;
                    case "RICH_RESPONSE_SOURCES_IN_MESSAGE":
                    case 37:
                        m.capabilities[i] = 37;
                        break;
                    case "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY":
                    case 38:
                        m.capabilities[i] = 38;
                        break;
                    case "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT":
                    case 39:
                        m.capabilities[i] = 39;
                        break;
                    case "AI_SHARED_MEMORY":
                    case 40:
                        m.capabilities[i] = 40;
                        break;
                    case "RICH_RESPONSE_UNIFIED_SOURCES":
                    case 41:
                        m.capabilities[i] = 41;
                        break;
                    case "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS":
                    case 42:
                        m.capabilities[i] = 42;
                        break;
                    case "RICH_RESPONSE_UR_INLINE_REELS_ENABLED":
                    case 43:
                        m.capabilities[i] = 43;
                        break;
                    case "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED":
                    case 44:
                        m.capabilities[i] = 44;
                        break;
                    case "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER":
                    case 45:
                        m.capabilities[i] = 45;
                        break;
                    case "RICH_RESPONSE_IN_APP_SURVEY":
                    case 46:
                        m.capabilities[i] = 46;
                        break;
                    case "AI_RESPONSE_MODEL_BRANDING":
                    case 47:
                        m.capabilities[i] = 47;
                        break;
                    case "SESSION_TRANSPARENCY_SYSTEM_MESSAGE":
                    case 48:
                        m.capabilities[i] = 48;
                        break;
                    case "RICH_RESPONSE_UR_REASONING":
                    case 49:
                        m.capabilities[i] = 49;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS":
                    case 50:
                        m.capabilities[i] = 50;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL":
                    case 51:
                        m.capabilities[i] = 51;
                        break;
                    case "AI_IMAGINE_LOADING_INDICATOR":
                    case 52:
                        m.capabilities[i] = 52;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE":
                    case 53:
                        m.capabilities[i] = 53;
                        break;
                    case "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR":
                    case 54:
                        m.capabilities[i] = 54;
                        break;
                    case "RICH_RESPONSE_UR_BLOKS_ENABLED":
                    case 55:
                        m.capabilities[i] = 55;
                        break;
                    case "RICH_RESPONSE_INLINE_LINKS_ENABLED":
                    case 56:
                        m.capabilities[i] = 56;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE_VIDEO":
                    case 57:
                        m.capabilities[i] = 57;
                        break;
                    case "JSON_PATCH_STREAMING":
                    case 58:
                        m.capabilities[i] = 58;
                        break;
                    case "AI_TAB_FORCE_CLIPPY":
                    case 59:
                        m.capabilities[i] = 59;
                        break;
                    case "UNIFIED_RESPONSE_EMBEDDED_SCREENS":
                    case 60:
                        m.capabilities[i] = 60;
                        break;
                    case "AI_SUBSCRIPTION_ENABLED":
                    case 61:
                        m.capabilities[i] = 61;
                        break;
                    }
                }
            }
            return m;
        };

        BotCapabilityMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilities = [];
            }
            if (m.capabilities && m.capabilities.length) {
                d.capabilities = [];
                for (var j = 0; j < m.capabilities.length; ++j) {
                    d.capabilities[j] = o.enums === String ? $root.WAAICommon.BotCapabilityMetadata.BotCapabilityType[m.capabilities[j]] === undefined ? m.capabilities[j] : $root.WAAICommon.BotCapabilityMetadata.BotCapabilityType[m.capabilities[j]] : m.capabilities[j];
                }
            }
            return d;
        };

        BotCapabilityMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotCapabilityMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotCapabilityMetadata";
        };

        BotCapabilityMetadata.BotCapabilityType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PROGRESS_INDICATOR"] = 1;
            values[valuesById[2] = "RICH_RESPONSE_HEADING"] = 2;
            values[valuesById[3] = "RICH_RESPONSE_NESTED_LIST"] = 3;
            values[valuesById[4] = "AI_MEMORY"] = 4;
            values[valuesById[5] = "RICH_RESPONSE_THREAD_SURFING"] = 5;
            values[valuesById[6] = "RICH_RESPONSE_TABLE"] = 6;
            values[valuesById[7] = "RICH_RESPONSE_CODE"] = 7;
            values[valuesById[8] = "RICH_RESPONSE_STRUCTURED_RESPONSE"] = 8;
            values[valuesById[9] = "RICH_RESPONSE_INLINE_IMAGE"] = 9;
            values[valuesById[10] = "WA_IG_1P_PLUGIN_RANKING_CONTROL"] = 10;
            values[valuesById[11] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_1"] = 11;
            values[valuesById[12] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_2"] = 12;
            values[valuesById[13] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_3"] = 13;
            values[valuesById[14] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_4"] = 14;
            values[valuesById[15] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_5"] = 15;
            values[valuesById[16] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_6"] = 16;
            values[valuesById[17] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_7"] = 17;
            values[valuesById[18] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_8"] = 18;
            values[valuesById[19] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_9"] = 19;
            values[valuesById[20] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_10"] = 20;
            values[valuesById[21] = "RICH_RESPONSE_SUB_HEADING"] = 21;
            values[valuesById[22] = "RICH_RESPONSE_GRID_IMAGE"] = 22;
            values[valuesById[23] = "AI_STUDIO_UGC_MEMORY"] = 23;
            values[valuesById[24] = "RICH_RESPONSE_LATEX"] = 24;
            values[valuesById[25] = "RICH_RESPONSE_MAPS"] = 25;
            values[valuesById[26] = "RICH_RESPONSE_INLINE_REELS"] = 26;
            values[valuesById[27] = "AGENTIC_PLANNING"] = 27;
            values[valuesById[28] = "ACCOUNT_LINKING"] = 28;
            values[valuesById[29] = "STREAMING_DISAGGREGATION"] = 29;
            values[valuesById[30] = "RICH_RESPONSE_GRID_IMAGE_3P"] = 30;
            values[valuesById[31] = "RICH_RESPONSE_LATEX_INLINE"] = 31;
            values[valuesById[32] = "QUERY_PLAN"] = 32;
            values[valuesById[33] = "PROACTIVE_MESSAGE"] = 33;
            values[valuesById[34] = "RICH_RESPONSE_UNIFIED_RESPONSE"] = 34;
            values[valuesById[35] = "PROMOTION_MESSAGE"] = 35;
            values[valuesById[36] = "SIMPLIFIED_PROFILE_PAGE"] = 36;
            values[valuesById[37] = "RICH_RESPONSE_SOURCES_IN_MESSAGE"] = 37;
            values[valuesById[38] = "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY"] = 38;
            values[valuesById[39] = "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT"] = 39;
            values[valuesById[40] = "AI_SHARED_MEMORY"] = 40;
            values[valuesById[41] = "RICH_RESPONSE_UNIFIED_SOURCES"] = 41;
            values[valuesById[42] = "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS"] = 42;
            values[valuesById[43] = "RICH_RESPONSE_UR_INLINE_REELS_ENABLED"] = 43;
            values[valuesById[44] = "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED"] = 44;
            values[valuesById[45] = "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER"] = 45;
            values[valuesById[46] = "RICH_RESPONSE_IN_APP_SURVEY"] = 46;
            values[valuesById[47] = "AI_RESPONSE_MODEL_BRANDING"] = 47;
            values[valuesById[48] = "SESSION_TRANSPARENCY_SYSTEM_MESSAGE"] = 48;
            values[valuesById[49] = "RICH_RESPONSE_UR_REASONING"] = 49;
            values[valuesById[50] = "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS"] = 50;
            values[valuesById[51] = "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL"] = 51;
            values[valuesById[52] = "AI_IMAGINE_LOADING_INDICATOR"] = 52;
            values[valuesById[53] = "RICH_RESPONSE_UR_IMAGINE"] = 53;
            values[valuesById[54] = "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR"] = 54;
            values[valuesById[55] = "RICH_RESPONSE_UR_BLOKS_ENABLED"] = 55;
            values[valuesById[56] = "RICH_RESPONSE_INLINE_LINKS_ENABLED"] = 56;
            values[valuesById[57] = "RICH_RESPONSE_UR_IMAGINE_VIDEO"] = 57;
            values[valuesById[58] = "JSON_PATCH_STREAMING"] = 58;
            values[valuesById[59] = "AI_TAB_FORCE_CLIPPY"] = 59;
            values[valuesById[60] = "UNIFIED_RESPONSE_EMBEDDED_SCREENS"] = 60;
            values[valuesById[61] = "AI_SUBSCRIPTION_ENABLED"] = 61;
            return values;
        })();

        return BotCapabilityMetadata;
    })();

    WAAICommon.BotModeSelectionMetadata = (function() {

        function BotModeSelectionMetadata(p) {
            this.mode = [];
            this.overrideMode = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotModeSelectionMetadata.prototype.mode = $util.emptyArray;
        BotModeSelectionMetadata.prototype.overrideMode = $util.emptyArray;

        BotModeSelectionMetadata.create = function create(properties) {
            return new BotModeSelectionMetadata(properties);
        };

        BotModeSelectionMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.mode != null && m.mode.length) {
                w.uint32(10).fork();
                for (var i = 0; i < m.mode.length; ++i)
                    w.int32(m.mode[i]);
                w.ldelim();
            }
            if (m.overrideMode != null && m.overrideMode.length) {
                w.uint32(18).fork();
                for (var i = 0; i < m.overrideMode.length; ++i)
                    w.uint32(m.overrideMode[i]);
                w.ldelim();
            }
            return w;
        };

        BotModeSelectionMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotModeSelectionMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.mode && m.mode.length))
                            m.mode = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.mode.push(r.int32());
                        } else
                            m.mode.push(r.int32());
                        break;
                    }
                case 2: {
                        if (!(m.overrideMode && m.overrideMode.length))
                            m.overrideMode = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.overrideMode.push(r.uint32());
                        } else
                            m.overrideMode.push(r.uint32());
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotModeSelectionMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotModeSelectionMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotModeSelectionMetadata();
            if (d.mode) {
                if (!Array.isArray(d.mode))
                    throw TypeError(".WAAICommon.BotModeSelectionMetadata.mode: array expected");
                m.mode = [];
                for (var i = 0; i < d.mode.length; ++i) {
                    switch (d.mode[i]) {
                    default:
                        if (typeof d.mode[i] === "number") {
                            m.mode[i] = d.mode[i];
                            break;
                        }
                    case "DEFAULT_MODE":
                    case 0:
                        m.mode[i] = 0;
                        break;
                    case "THINK_HARD_MODE":
                    case 1:
                        m.mode[i] = 1;
                        break;
                    }
                }
            }
            if (d.overrideMode) {
                if (!Array.isArray(d.overrideMode))
                    throw TypeError(".WAAICommon.BotModeSelectionMetadata.overrideMode: array expected");
                m.overrideMode = [];
                for (var i = 0; i < d.overrideMode.length; ++i) {
                    m.overrideMode[i] = d.overrideMode[i] >>> 0;
                }
            }
            return m;
        };

        BotModeSelectionMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.mode = [];
                d.overrideMode = [];
            }
            if (m.mode && m.mode.length) {
                d.mode = [];
                for (var j = 0; j < m.mode.length; ++j) {
                    d.mode[j] = o.enums === String ? $root.WAAICommon.BotModeSelectionMetadata.BotUserSelectionMode[m.mode[j]] === undefined ? m.mode[j] : $root.WAAICommon.BotModeSelectionMetadata.BotUserSelectionMode[m.mode[j]] : m.mode[j];
                }
            }
            if (m.overrideMode && m.overrideMode.length) {
                d.overrideMode = [];
                for (var j = 0; j < m.overrideMode.length; ++j) {
                    d.overrideMode[j] = m.overrideMode[j];
                }
            }
            return d;
        };

        BotModeSelectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotModeSelectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotModeSelectionMetadata";
        };

        BotModeSelectionMetadata.BotUserSelectionMode = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT_MODE"] = 0;
            values[valuesById[1] = "THINK_HARD_MODE"] = 1;
            return values;
        })();

        return BotModeSelectionMetadata;
    })();

    WAAICommon.BotQuotaMetadata = (function() {

        function BotQuotaMetadata(p) {
            this.botFeatureQuotaMetadata = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotQuotaMetadata.prototype.botFeatureQuotaMetadata = $util.emptyArray;

        BotQuotaMetadata.create = function create(properties) {
            return new BotQuotaMetadata(properties);
        };

        BotQuotaMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.botFeatureQuotaMetadata != null && m.botFeatureQuotaMetadata.length) {
                for (var i = 0; i < m.botFeatureQuotaMetadata.length; ++i)
                    $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.encode(m.botFeatureQuotaMetadata[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotQuotaMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotQuotaMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.botFeatureQuotaMetadata && m.botFeatureQuotaMetadata.length))
                            m.botFeatureQuotaMetadata = [];
                        m.botFeatureQuotaMetadata.push($root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotQuotaMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotQuotaMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotQuotaMetadata();
            if (d.botFeatureQuotaMetadata) {
                if (!Array.isArray(d.botFeatureQuotaMetadata))
                    throw TypeError(".WAAICommon.BotQuotaMetadata.botFeatureQuotaMetadata: array expected");
                m.botFeatureQuotaMetadata = [];
                for (var i = 0; i < d.botFeatureQuotaMetadata.length; ++i) {
                    if (typeof d.botFeatureQuotaMetadata[i] !== "object")
                        throw TypeError(".WAAICommon.BotQuotaMetadata.botFeatureQuotaMetadata: object expected");
                    m.botFeatureQuotaMetadata[i] = $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.fromObject(d.botFeatureQuotaMetadata[i], n + 1);
                }
            }
            return m;
        };

        BotQuotaMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.botFeatureQuotaMetadata = [];
            }
            if (m.botFeatureQuotaMetadata && m.botFeatureQuotaMetadata.length) {
                d.botFeatureQuotaMetadata = [];
                for (var j = 0; j < m.botFeatureQuotaMetadata.length; ++j) {
                    d.botFeatureQuotaMetadata[j] = $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.toObject(m.botFeatureQuotaMetadata[j], o);
                }
            }
            return d;
        };

        BotQuotaMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotQuotaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotQuotaMetadata";
        };

        BotQuotaMetadata.BotFeatureQuotaMetadata = (function() {

            function BotFeatureQuotaMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BotFeatureQuotaMetadata.prototype.featureType = null;
            BotFeatureQuotaMetadata.prototype.remainingQuota = null;
            BotFeatureQuotaMetadata.prototype.expirationTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_featureType", {
                get: $util.oneOfGetter($oneOfFields = ["featureType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_remainingQuota", {
                get: $util.oneOfGetter($oneOfFields = ["remainingQuota"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_expirationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expirationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotFeatureQuotaMetadata.create = function create(properties) {
                return new BotFeatureQuotaMetadata(properties);
            };

            BotFeatureQuotaMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.featureType != null && Object.hasOwnProperty.call(m, "featureType"))
                    w.uint32(8).int32(m.featureType);
                if (m.remainingQuota != null && Object.hasOwnProperty.call(m, "remainingQuota"))
                    w.uint32(16).uint32(m.remainingQuota);
                if (m.expirationTimestamp != null && Object.hasOwnProperty.call(m, "expirationTimestamp"))
                    w.uint32(24).uint64(m.expirationTimestamp);
                return w;
            };

            BotFeatureQuotaMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.featureType = r.int32();
                            break;
                        }
                    case 2: {
                            m.remainingQuota = r.uint32();
                            break;
                        }
                    case 3: {
                            m.expirationTimestamp = r.uint64();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BotFeatureQuotaMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata();
                switch (d.featureType) {
                default:
                    if (typeof d.featureType === "number") {
                        m.featureType = d.featureType;
                        break;
                    }
                    break;
                case "UNKNOWN_FEATURE":
                case 0:
                    m.featureType = 0;
                    break;
                case "REASONING_FEATURE":
                case 1:
                    m.featureType = 1;
                    break;
                }
                if (d.remainingQuota != null) {
                    m.remainingQuota = d.remainingQuota >>> 0;
                }
                if (d.expirationTimestamp != null) {
                    if ($util.Long)
                        (m.expirationTimestamp = $util.Long.fromValue(d.expirationTimestamp)).unsigned = true;
                    else if (typeof d.expirationTimestamp === "string")
                        m.expirationTimestamp = parseInt(d.expirationTimestamp, 10);
                    else if (typeof d.expirationTimestamp === "number")
                        m.expirationTimestamp = d.expirationTimestamp;
                    else if (typeof d.expirationTimestamp === "object")
                        m.expirationTimestamp = new $util.LongBits(d.expirationTimestamp.low >>> 0, d.expirationTimestamp.high >>> 0).toNumber(true);
                }
                return m;
            };

            BotFeatureQuotaMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.featureType != null && m.hasOwnProperty("featureType")) {
                    d.featureType = o.enums === String ? $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[m.featureType] === undefined ? m.featureType : $root.WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[m.featureType] : m.featureType;
                    if (o.oneofs)
                        d._featureType = "featureType";
                }
                if (m.remainingQuota != null && m.hasOwnProperty("remainingQuota")) {
                    d.remainingQuota = m.remainingQuota;
                    if (o.oneofs)
                        d._remainingQuota = "remainingQuota";
                }
                if (m.expirationTimestamp != null && m.hasOwnProperty("expirationTimestamp")) {
                    if (typeof m.expirationTimestamp === "number")
                        d.expirationTimestamp = o.longs === String ? String(m.expirationTimestamp) : m.expirationTimestamp;
                    else
                        d.expirationTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.expirationTimestamp) : o.longs === Number ? new $util.LongBits(m.expirationTimestamp.low >>> 0, m.expirationTimestamp.high >>> 0).toNumber(true) : m.expirationTimestamp;
                    if (o.oneofs)
                        d._expirationTimestamp = "expirationTimestamp";
                }
                return d;
            };

            BotFeatureQuotaMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotFeatureQuotaMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotQuotaMetadata.BotFeatureQuotaMetadata";
            };

            BotFeatureQuotaMetadata.BotFeatureType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_FEATURE"] = 0;
                values[valuesById[1] = "REASONING_FEATURE"] = 1;
                return values;
            })();

            return BotFeatureQuotaMetadata;
        })();

        return BotQuotaMetadata;
    })();

    WAAICommon.BotImagineMetadata = (function() {

        function BotImagineMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotImagineMetadata.prototype.imagineType = null;
        BotImagineMetadata.prototype.shortPrompt = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotImagineMetadata.prototype, "_imagineType", {
            get: $util.oneOfGetter($oneOfFields = ["imagineType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotImagineMetadata.prototype, "_shortPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["shortPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotImagineMetadata.create = function create(properties) {
            return new BotImagineMetadata(properties);
        };

        BotImagineMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.imagineType != null && Object.hasOwnProperty.call(m, "imagineType"))
                w.uint32(8).int32(m.imagineType);
            if (m.shortPrompt != null && Object.hasOwnProperty.call(m, "shortPrompt"))
                w.uint32(18).string(m.shortPrompt);
            return w;
        };

        BotImagineMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotImagineMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.imagineType = r.int32();
                        break;
                    }
                case 2: {
                        m.shortPrompt = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotImagineMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotImagineMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotImagineMetadata();
            switch (d.imagineType) {
            default:
                if (typeof d.imagineType === "number") {
                    m.imagineType = d.imagineType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.imagineType = 0;
                break;
            case "IMAGINE":
            case 1:
                m.imagineType = 1;
                break;
            case "MEMU":
            case 2:
                m.imagineType = 2;
                break;
            case "FLASH":
            case 3:
                m.imagineType = 3;
                break;
            case "EDIT":
            case 4:
                m.imagineType = 4;
                break;
            }
            if (d.shortPrompt != null) {
                m.shortPrompt = String(d.shortPrompt);
            }
            return m;
        };

        BotImagineMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.imagineType != null && m.hasOwnProperty("imagineType")) {
                d.imagineType = o.enums === String ? $root.WAAICommon.BotImagineMetadata.ImagineType[m.imagineType] === undefined ? m.imagineType : $root.WAAICommon.BotImagineMetadata.ImagineType[m.imagineType] : m.imagineType;
                if (o.oneofs)
                    d._imagineType = "imagineType";
            }
            if (m.shortPrompt != null && m.hasOwnProperty("shortPrompt")) {
                d.shortPrompt = m.shortPrompt;
                if (o.oneofs)
                    d._shortPrompt = "shortPrompt";
            }
            return d;
        };

        BotImagineMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotImagineMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotImagineMetadata";
        };

        BotImagineMetadata.ImagineType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "IMAGINE"] = 1;
            values[valuesById[2] = "MEMU"] = 2;
            values[valuesById[3] = "FLASH"] = 3;
            values[valuesById[4] = "EDIT"] = 4;
            return values;
        })();

        return BotImagineMetadata;
    })();

    WAAICommon.BotAgeCollectionMetadata = (function() {

        function BotAgeCollectionMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotAgeCollectionMetadata.prototype.ageCollectionEligible = null;
        BotAgeCollectionMetadata.prototype.shouldTriggerAgeCollectionOnClient = null;
        BotAgeCollectionMetadata.prototype.ageCollectionType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionEligible", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionEligible"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_shouldTriggerAgeCollectionOnClient", {
            get: $util.oneOfGetter($oneOfFields = ["shouldTriggerAgeCollectionOnClient"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionType", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgeCollectionMetadata.create = function create(properties) {
            return new BotAgeCollectionMetadata(properties);
        };

        BotAgeCollectionMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.ageCollectionEligible != null && Object.hasOwnProperty.call(m, "ageCollectionEligible"))
                w.uint32(8).bool(m.ageCollectionEligible);
            if (m.shouldTriggerAgeCollectionOnClient != null && Object.hasOwnProperty.call(m, "shouldTriggerAgeCollectionOnClient"))
                w.uint32(16).bool(m.shouldTriggerAgeCollectionOnClient);
            if (m.ageCollectionType != null && Object.hasOwnProperty.call(m, "ageCollectionType"))
                w.uint32(24).int32(m.ageCollectionType);
            return w;
        };

        BotAgeCollectionMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotAgeCollectionMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.ageCollectionEligible = r.bool();
                        break;
                    }
                case 2: {
                        m.shouldTriggerAgeCollectionOnClient = r.bool();
                        break;
                    }
                case 3: {
                        m.ageCollectionType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotAgeCollectionMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotAgeCollectionMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotAgeCollectionMetadata();
            if (d.ageCollectionEligible != null) {
                m.ageCollectionEligible = Boolean(d.ageCollectionEligible);
            }
            if (d.shouldTriggerAgeCollectionOnClient != null) {
                m.shouldTriggerAgeCollectionOnClient = Boolean(d.shouldTriggerAgeCollectionOnClient);
            }
            switch (d.ageCollectionType) {
            default:
                if (typeof d.ageCollectionType === "number") {
                    m.ageCollectionType = d.ageCollectionType;
                    break;
                }
                break;
            case "O18_BINARY":
            case 0:
                m.ageCollectionType = 0;
                break;
            case "WAFFLE":
            case 1:
                m.ageCollectionType = 1;
                break;
            }
            return m;
        };

        BotAgeCollectionMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.ageCollectionEligible != null && m.hasOwnProperty("ageCollectionEligible")) {
                d.ageCollectionEligible = m.ageCollectionEligible;
                if (o.oneofs)
                    d._ageCollectionEligible = "ageCollectionEligible";
            }
            if (m.shouldTriggerAgeCollectionOnClient != null && m.hasOwnProperty("shouldTriggerAgeCollectionOnClient")) {
                d.shouldTriggerAgeCollectionOnClient = m.shouldTriggerAgeCollectionOnClient;
                if (o.oneofs)
                    d._shouldTriggerAgeCollectionOnClient = "shouldTriggerAgeCollectionOnClient";
            }
            if (m.ageCollectionType != null && m.hasOwnProperty("ageCollectionType")) {
                d.ageCollectionType = o.enums === String ? $root.WAAICommon.BotAgeCollectionMetadata.AgeCollectionType[m.ageCollectionType] === undefined ? m.ageCollectionType : $root.WAAICommon.BotAgeCollectionMetadata.AgeCollectionType[m.ageCollectionType] : m.ageCollectionType;
                if (o.oneofs)
                    d._ageCollectionType = "ageCollectionType";
            }
            return d;
        };

        BotAgeCollectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgeCollectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotAgeCollectionMetadata";
        };

        BotAgeCollectionMetadata.AgeCollectionType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "O18_BINARY"] = 0;
            values[valuesById[1] = "WAFFLE"] = 1;
            return values;
        })();

        return BotAgeCollectionMetadata;
    })();

    WAAICommon.BotSourcesMetadata = (function() {

        function BotSourcesMetadata(p) {
            this.sources = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotSourcesMetadata.prototype.sources = $util.emptyArray;

        BotSourcesMetadata.create = function create(properties) {
            return new BotSourcesMetadata(properties);
        };

        BotSourcesMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.sources != null && m.sources.length) {
                for (var i = 0; i < m.sources.length; ++i)
                    $root.WAAICommon.BotSourcesMetadata.BotSourceItem.encode(m.sources[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotSourcesMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSourcesMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.sources && m.sources.length))
                            m.sources = [];
                        m.sources.push($root.WAAICommon.BotSourcesMetadata.BotSourceItem.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotSourcesMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotSourcesMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotSourcesMetadata();
            if (d.sources) {
                if (!Array.isArray(d.sources))
                    throw TypeError(".WAAICommon.BotSourcesMetadata.sources: array expected");
                m.sources = [];
                for (var i = 0; i < d.sources.length; ++i) {
                    if (typeof d.sources[i] !== "object")
                        throw TypeError(".WAAICommon.BotSourcesMetadata.sources: object expected");
                    m.sources[i] = $root.WAAICommon.BotSourcesMetadata.BotSourceItem.fromObject(d.sources[i], n + 1);
                }
            }
            return m;
        };

        BotSourcesMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.sources = [];
            }
            if (m.sources && m.sources.length) {
                d.sources = [];
                for (var j = 0; j < m.sources.length; ++j) {
                    d.sources[j] = $root.WAAICommon.BotSourcesMetadata.BotSourceItem.toObject(m.sources[j], o);
                }
            }
            return d;
        };

        BotSourcesMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSourcesMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotSourcesMetadata";
        };

        BotSourcesMetadata.BotSourceItem = (function() {

            function BotSourceItem(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            BotSourceItem.prototype.provider = null;
            BotSourceItem.prototype.thumbnailCDNURL = null;
            BotSourceItem.prototype.sourceProviderURL = null;
            BotSourceItem.prototype.sourceQuery = null;
            BotSourceItem.prototype.faviconCDNURL = null;
            BotSourceItem.prototype.citationNumber = null;
            BotSourceItem.prototype.sourceTitle = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_provider", {
                get: $util.oneOfGetter($oneOfFields = ["provider"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_thumbnailCDNURL", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailCDNURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceProviderURL", {
                get: $util.oneOfGetter($oneOfFields = ["sourceProviderURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceQuery", {
                get: $util.oneOfGetter($oneOfFields = ["sourceQuery"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_faviconCDNURL", {
                get: $util.oneOfGetter($oneOfFields = ["faviconCDNURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_citationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["citationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(BotSourceItem.prototype, "_sourceTitle", {
                get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotSourceItem.create = function create(properties) {
                return new BotSourceItem(properties);
            };

            BotSourceItem.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.provider != null && Object.hasOwnProperty.call(m, "provider"))
                    w.uint32(8).int32(m.provider);
                if (m.thumbnailCDNURL != null && Object.hasOwnProperty.call(m, "thumbnailCDNURL"))
                    w.uint32(18).string(m.thumbnailCDNURL);
                if (m.sourceProviderURL != null && Object.hasOwnProperty.call(m, "sourceProviderURL"))
                    w.uint32(26).string(m.sourceProviderURL);
                if (m.sourceQuery != null && Object.hasOwnProperty.call(m, "sourceQuery"))
                    w.uint32(34).string(m.sourceQuery);
                if (m.faviconCDNURL != null && Object.hasOwnProperty.call(m, "faviconCDNURL"))
                    w.uint32(42).string(m.faviconCDNURL);
                if (m.citationNumber != null && Object.hasOwnProperty.call(m, "citationNumber"))
                    w.uint32(48).uint32(m.citationNumber);
                if (m.sourceTitle != null && Object.hasOwnProperty.call(m, "sourceTitle"))
                    w.uint32(58).string(m.sourceTitle);
                return w;
            };

            BotSourceItem.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSourcesMetadata.BotSourceItem();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.provider = r.int32();
                            break;
                        }
                    case 2: {
                            m.thumbnailCDNURL = r.string();
                            break;
                        }
                    case 3: {
                            m.sourceProviderURL = r.string();
                            break;
                        }
                    case 4: {
                            m.sourceQuery = r.string();
                            break;
                        }
                    case 5: {
                            m.faviconCDNURL = r.string();
                            break;
                        }
                    case 6: {
                            m.citationNumber = r.uint32();
                            break;
                        }
                    case 7: {
                            m.sourceTitle = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            BotSourceItem.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotSourcesMetadata.BotSourceItem)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotSourcesMetadata.BotSourceItem();
                switch (d.provider) {
                default:
                    if (typeof d.provider === "number") {
                        m.provider = d.provider;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.provider = 0;
                    break;
                case "BING":
                case 1:
                    m.provider = 1;
                    break;
                case "GOOGLE":
                case 2:
                    m.provider = 2;
                    break;
                case "SUPPORT":
                case 3:
                    m.provider = 3;
                    break;
                case "OTHER":
                case 4:
                    m.provider = 4;
                    break;
                }
                if (d.thumbnailCDNURL != null) {
                    m.thumbnailCDNURL = String(d.thumbnailCDNURL);
                }
                if (d.sourceProviderURL != null) {
                    m.sourceProviderURL = String(d.sourceProviderURL);
                }
                if (d.sourceQuery != null) {
                    m.sourceQuery = String(d.sourceQuery);
                }
                if (d.faviconCDNURL != null) {
                    m.faviconCDNURL = String(d.faviconCDNURL);
                }
                if (d.citationNumber != null) {
                    m.citationNumber = d.citationNumber >>> 0;
                }
                if (d.sourceTitle != null) {
                    m.sourceTitle = String(d.sourceTitle);
                }
                return m;
            };

            BotSourceItem.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.provider != null && m.hasOwnProperty("provider")) {
                    d.provider = o.enums === String ? $root.WAAICommon.BotSourcesMetadata.BotSourceItem.SourceProvider[m.provider] === undefined ? m.provider : $root.WAAICommon.BotSourcesMetadata.BotSourceItem.SourceProvider[m.provider] : m.provider;
                    if (o.oneofs)
                        d._provider = "provider";
                }
                if (m.thumbnailCDNURL != null && m.hasOwnProperty("thumbnailCDNURL")) {
                    d.thumbnailCDNURL = m.thumbnailCDNURL;
                    if (o.oneofs)
                        d._thumbnailCDNURL = "thumbnailCDNURL";
                }
                if (m.sourceProviderURL != null && m.hasOwnProperty("sourceProviderURL")) {
                    d.sourceProviderURL = m.sourceProviderURL;
                    if (o.oneofs)
                        d._sourceProviderURL = "sourceProviderURL";
                }
                if (m.sourceQuery != null && m.hasOwnProperty("sourceQuery")) {
                    d.sourceQuery = m.sourceQuery;
                    if (o.oneofs)
                        d._sourceQuery = "sourceQuery";
                }
                if (m.faviconCDNURL != null && m.hasOwnProperty("faviconCDNURL")) {
                    d.faviconCDNURL = m.faviconCDNURL;
                    if (o.oneofs)
                        d._faviconCDNURL = "faviconCDNURL";
                }
                if (m.citationNumber != null && m.hasOwnProperty("citationNumber")) {
                    d.citationNumber = m.citationNumber;
                    if (o.oneofs)
                        d._citationNumber = "citationNumber";
                }
                if (m.sourceTitle != null && m.hasOwnProperty("sourceTitle")) {
                    d.sourceTitle = m.sourceTitle;
                    if (o.oneofs)
                        d._sourceTitle = "sourceTitle";
                }
                return d;
            };

            BotSourceItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotSourceItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotSourcesMetadata.BotSourceItem";
            };

            BotSourceItem.SourceProvider = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "BING"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "SUPPORT"] = 3;
                values[valuesById[4] = "OTHER"] = 4;
                return values;
            })();

            return BotSourceItem;
        })();

        return BotSourcesMetadata;
    })();

    WAAICommon.BotMessageOrigin = (function() {

        function BotMessageOrigin(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMessageOrigin.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageOrigin.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMessageOrigin.create = function create(properties) {
            return new BotMessageOrigin(properties);
        };

        BotMessageOrigin.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            return w;
        };

        BotMessageOrigin.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMessageOrigin();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMessageOrigin.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMessageOrigin)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMessageOrigin();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED":
            case 0:
                m.type = 0;
                break;
            }
            return m;
        };

        BotMessageOrigin.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? $root.WAAICommon.BotMessageOrigin.BotMessageOriginType[m.type] === undefined ? m.type : $root.WAAICommon.BotMessageOrigin.BotMessageOriginType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            return d;
        };

        BotMessageOrigin.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageOrigin.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMessageOrigin";
        };

        BotMessageOrigin.BotMessageOriginType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED"] = 0;
            return values;
        })();

        return BotMessageOrigin;
    })();

    WAAICommon.AIThreadInfo = (function() {

        function AIThreadInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIThreadInfo.prototype.serverInfo = null;
        AIThreadInfo.prototype.clientInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIThreadInfo.prototype, "_serverInfo", {
            get: $util.oneOfGetter($oneOfFields = ["serverInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIThreadInfo.prototype, "_clientInfo", {
            get: $util.oneOfGetter($oneOfFields = ["clientInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIThreadInfo.create = function create(properties) {
            return new AIThreadInfo(properties);
        };

        AIThreadInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.serverInfo != null && Object.hasOwnProperty.call(m, "serverInfo"))
                $root.WAAICommon.AIThreadInfo.AIThreadServerInfo.encode(m.serverInfo, w.uint32(10).fork()).ldelim();
            if (m.clientInfo != null && Object.hasOwnProperty.call(m, "clientInfo"))
                $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.encode(m.clientInfo, w.uint32(18).fork()).ldelim();
            return w;
        };

        AIThreadInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIThreadInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.serverInfo = $root.WAAICommon.AIThreadInfo.AIThreadServerInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.clientInfo = $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIThreadInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIThreadInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIThreadInfo();
            if (d.serverInfo != null) {
                if (typeof d.serverInfo !== "object")
                    throw TypeError(".WAAICommon.AIThreadInfo.serverInfo: object expected");
                m.serverInfo = $root.WAAICommon.AIThreadInfo.AIThreadServerInfo.fromObject(d.serverInfo, n + 1);
            }
            if (d.clientInfo != null) {
                if (typeof d.clientInfo !== "object")
                    throw TypeError(".WAAICommon.AIThreadInfo.clientInfo: object expected");
                m.clientInfo = $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.fromObject(d.clientInfo, n + 1);
            }
            return m;
        };

        AIThreadInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.serverInfo != null && m.hasOwnProperty("serverInfo")) {
                d.serverInfo = $root.WAAICommon.AIThreadInfo.AIThreadServerInfo.toObject(m.serverInfo, o);
                if (o.oneofs)
                    d._serverInfo = "serverInfo";
            }
            if (m.clientInfo != null && m.hasOwnProperty("clientInfo")) {
                d.clientInfo = $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.toObject(m.clientInfo, o);
                if (o.oneofs)
                    d._clientInfo = "clientInfo";
            }
            return d;
        };

        AIThreadInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIThreadInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIThreadInfo";
        };

        AIThreadInfo.AIThreadClientInfo = (function() {

            function AIThreadClientInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIThreadClientInfo.prototype.type = null;
            AIThreadClientInfo.prototype.sourceChatJID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadClientInfo.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadClientInfo.prototype, "_sourceChatJID", {
                get: $util.oneOfGetter($oneOfFields = ["sourceChatJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIThreadClientInfo.create = function create(properties) {
                return new AIThreadClientInfo(properties);
            };

            AIThreadClientInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.sourceChatJID != null && Object.hasOwnProperty.call(m, "sourceChatJID"))
                    w.uint32(18).string(m.sourceChatJID);
                return w;
            };

            AIThreadClientInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIThreadInfo.AIThreadClientInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.type = r.int32();
                            break;
                        }
                    case 2: {
                            m.sourceChatJID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIThreadClientInfo.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.AIThreadInfo.AIThreadClientInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.AIThreadInfo.AIThreadClientInfo();
                switch (d.type) {
                default:
                    if (typeof d.type === "number") {
                        m.type = d.type;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "DEFAULT":
                case 1:
                    m.type = 1;
                    break;
                case "INCOGNITO":
                case 2:
                    m.type = 2;
                    break;
                case "SIDE_CHAT":
                case 3:
                    m.type = 3;
                    break;
                }
                if (d.sourceChatJID != null) {
                    m.sourceChatJID = String(d.sourceChatJID);
                }
                return m;
            };

            AIThreadClientInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.type != null && m.hasOwnProperty("type")) {
                    d.type = o.enums === String ? $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.AIThreadType[m.type] === undefined ? m.type : $root.WAAICommon.AIThreadInfo.AIThreadClientInfo.AIThreadType[m.type] : m.type;
                    if (o.oneofs)
                        d._type = "type";
                }
                if (m.sourceChatJID != null && m.hasOwnProperty("sourceChatJID")) {
                    d.sourceChatJID = m.sourceChatJID;
                    if (o.oneofs)
                        d._sourceChatJID = "sourceChatJID";
                }
                return d;
            };

            AIThreadClientInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIThreadClientInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.AIThreadInfo.AIThreadClientInfo";
            };

            AIThreadClientInfo.AIThreadType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "DEFAULT"] = 1;
                values[valuesById[2] = "INCOGNITO"] = 2;
                values[valuesById[3] = "SIDE_CHAT"] = 3;
                return values;
            })();

            return AIThreadClientInfo;
        })();

        AIThreadInfo.AIThreadServerInfo = (function() {

            function AIThreadServerInfo(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIThreadServerInfo.prototype.title = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIThreadServerInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIThreadServerInfo.create = function create(properties) {
                return new AIThreadServerInfo(properties);
            };

            AIThreadServerInfo.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                return w;
            };

            AIThreadServerInfo.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIThreadInfo.AIThreadServerInfo();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIThreadServerInfo.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.AIThreadInfo.AIThreadServerInfo)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.AIThreadInfo.AIThreadServerInfo();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                return m;
            };

            AIThreadServerInfo.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                return d;
            };

            AIThreadServerInfo.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIThreadServerInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.AIThreadInfo.AIThreadServerInfo";
            };

            return AIThreadServerInfo;
        })();

        return AIThreadInfo;
    })();

    WAAICommon.BotFeedbackMessage = (function() {

        function BotFeedbackMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotFeedbackMessage.prototype.messageKey = null;
        BotFeedbackMessage.prototype.kind = null;
        BotFeedbackMessage.prototype.text = null;
        BotFeedbackMessage.prototype.kindNegative = null;
        BotFeedbackMessage.prototype.kindPositive = null;
        BotFeedbackMessage.prototype.kindReport = null;
        BotFeedbackMessage.prototype.sideBySideSurveyMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kind", {
            get: $util.oneOfGetter($oneOfFields = ["kind"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindNegative", {
            get: $util.oneOfGetter($oneOfFields = ["kindNegative"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindPositive", {
            get: $util.oneOfGetter($oneOfFields = ["kindPositive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_kindReport", {
            get: $util.oneOfGetter($oneOfFields = ["kindReport"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotFeedbackMessage.prototype, "_sideBySideSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sideBySideSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotFeedbackMessage.create = function create(properties) {
            return new BotFeedbackMessage(properties);
        };

        BotFeedbackMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageKey != null && Object.hasOwnProperty.call(m, "messageKey"))
                WACommon.MessageKey.encode(m.messageKey, w.uint32(10).fork()).ldelim();
            if (m.kind != null && Object.hasOwnProperty.call(m, "kind"))
                w.uint32(16).int32(m.kind);
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(26).string(m.text);
            if (m.kindNegative != null && Object.hasOwnProperty.call(m, "kindNegative"))
                w.uint32(32).uint64(m.kindNegative);
            if (m.kindPositive != null && Object.hasOwnProperty.call(m, "kindPositive"))
                w.uint32(40).uint64(m.kindPositive);
            if (m.kindReport != null && Object.hasOwnProperty.call(m, "kindReport"))
                w.uint32(48).int32(m.kindReport);
            if (m.sideBySideSurveyMetadata != null && Object.hasOwnProperty.call(m, "sideBySideSurveyMetadata"))
                $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.encode(m.sideBySideSurveyMetadata, w.uint32(58).fork()).ldelim();
            return w;
        };

        BotFeedbackMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.kind = r.int32();
                        break;
                    }
                case 3: {
                        m.text = r.string();
                        break;
                    }
                case 4: {
                        m.kindNegative = r.uint64();
                        break;
                    }
                case 5: {
                        m.kindPositive = r.uint64();
                        break;
                    }
                case 6: {
                        m.kindReport = r.int32();
                        break;
                    }
                case 7: {
                        m.sideBySideSurveyMetadata = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotFeedbackMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotFeedbackMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotFeedbackMessage();
            if (d.messageKey != null) {
                if (typeof d.messageKey !== "object")
                    throw TypeError(".WAAICommon.BotFeedbackMessage.messageKey: object expected");
                m.messageKey = WACommon.MessageKey.fromObject(d.messageKey, n + 1);
            }
            switch (d.kind) {
            default:
                if (typeof d.kind === "number") {
                    m.kind = d.kind;
                    break;
                }
                break;
            case "BOT_FEEDBACK_POSITIVE":
            case 0:
                m.kind = 0;
                break;
            case "BOT_FEEDBACK_NEGATIVE_GENERIC":
            case 1:
                m.kind = 1;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HELPFUL":
            case 2:
                m.kind = 2;
                break;
            case "BOT_FEEDBACK_NEGATIVE_INTERESTING":
            case 3:
                m.kind = 3;
                break;
            case "BOT_FEEDBACK_NEGATIVE_ACCURATE":
            case 4:
                m.kind = 4;
                break;
            case "BOT_FEEDBACK_NEGATIVE_SAFE":
            case 5:
                m.kind = 5;
                break;
            case "BOT_FEEDBACK_NEGATIVE_OTHER":
            case 6:
                m.kind = 6;
                break;
            case "BOT_FEEDBACK_NEGATIVE_REFUSED":
            case 7:
                m.kind = 7;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING":
            case 8:
                m.kind = 8;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT":
            case 9:
                m.kind = 9;
                break;
            case "BOT_FEEDBACK_NEGATIVE_PERSONALIZED":
            case 10:
                m.kind = 10;
                break;
            case "BOT_FEEDBACK_NEGATIVE_CLARITY":
            case 11:
                m.kind = 11;
                break;
            case "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON":
            case 12:
                m.kind = 12;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY":
            case 13:
                m.kind = 13;
                break;
            case "BOT_FEEDBACK_NEGATIVE":
            case 14:
                m.kind = 14;
                break;
            }
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.kindNegative != null) {
                if ($util.Long)
                    (m.kindNegative = $util.Long.fromValue(d.kindNegative)).unsigned = true;
                else if (typeof d.kindNegative === "string")
                    m.kindNegative = parseInt(d.kindNegative, 10);
                else if (typeof d.kindNegative === "number")
                    m.kindNegative = d.kindNegative;
                else if (typeof d.kindNegative === "object")
                    m.kindNegative = new $util.LongBits(d.kindNegative.low >>> 0, d.kindNegative.high >>> 0).toNumber(true);
            }
            if (d.kindPositive != null) {
                if ($util.Long)
                    (m.kindPositive = $util.Long.fromValue(d.kindPositive)).unsigned = true;
                else if (typeof d.kindPositive === "string")
                    m.kindPositive = parseInt(d.kindPositive, 10);
                else if (typeof d.kindPositive === "number")
                    m.kindPositive = d.kindPositive;
                else if (typeof d.kindPositive === "object")
                    m.kindPositive = new $util.LongBits(d.kindPositive.low >>> 0, d.kindPositive.high >>> 0).toNumber(true);
            }
            switch (d.kindReport) {
            default:
                if (typeof d.kindReport === "number") {
                    m.kindReport = d.kindReport;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.kindReport = 0;
                break;
            case "GENERIC":
            case 1:
                m.kindReport = 1;
                break;
            }
            if (d.sideBySideSurveyMetadata != null) {
                if (typeof d.sideBySideSurveyMetadata !== "object")
                    throw TypeError(".WAAICommon.BotFeedbackMessage.sideBySideSurveyMetadata: object expected");
                m.sideBySideSurveyMetadata = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.fromObject(d.sideBySideSurveyMetadata, n + 1);
            }
            return m;
        };

        BotFeedbackMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageKey != null && m.hasOwnProperty("messageKey")) {
                d.messageKey = WACommon.MessageKey.toObject(m.messageKey, o);
                if (o.oneofs)
                    d._messageKey = "messageKey";
            }
            if (m.kind != null && m.hasOwnProperty("kind")) {
                d.kind = o.enums === String ? $root.WAAICommon.BotFeedbackMessage.BotFeedbackKind[m.kind] === undefined ? m.kind : $root.WAAICommon.BotFeedbackMessage.BotFeedbackKind[m.kind] : m.kind;
                if (o.oneofs)
                    d._kind = "kind";
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.kindNegative != null && m.hasOwnProperty("kindNegative")) {
                if (typeof m.kindNegative === "number")
                    d.kindNegative = o.longs === String ? String(m.kindNegative) : m.kindNegative;
                else
                    d.kindNegative = o.longs === String ? $util.Long.prototype.toString.call(m.kindNegative) : o.longs === Number ? new $util.LongBits(m.kindNegative.low >>> 0, m.kindNegative.high >>> 0).toNumber(true) : m.kindNegative;
                if (o.oneofs)
                    d._kindNegative = "kindNegative";
            }
            if (m.kindPositive != null && m.hasOwnProperty("kindPositive")) {
                if (typeof m.kindPositive === "number")
                    d.kindPositive = o.longs === String ? String(m.kindPositive) : m.kindPositive;
                else
                    d.kindPositive = o.longs === String ? $util.Long.prototype.toString.call(m.kindPositive) : o.longs === Number ? new $util.LongBits(m.kindPositive.low >>> 0, m.kindPositive.high >>> 0).toNumber(true) : m.kindPositive;
                if (o.oneofs)
                    d._kindPositive = "kindPositive";
            }
            if (m.kindReport != null && m.hasOwnProperty("kindReport")) {
                d.kindReport = o.enums === String ? $root.WAAICommon.BotFeedbackMessage.ReportKind[m.kindReport] === undefined ? m.kindReport : $root.WAAICommon.BotFeedbackMessage.ReportKind[m.kindReport] : m.kindReport;
                if (o.oneofs)
                    d._kindReport = "kindReport";
            }
            if (m.sideBySideSurveyMetadata != null && m.hasOwnProperty("sideBySideSurveyMetadata")) {
                d.sideBySideSurveyMetadata = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.toObject(m.sideBySideSurveyMetadata, o);
                if (o.oneofs)
                    d._sideBySideSurveyMetadata = "sideBySideSurveyMetadata";
            }
            return d;
        };

        BotFeedbackMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotFeedbackMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage";
        };

        BotFeedbackMessage.ReportKind = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "GENERIC"] = 1;
            return values;
        })();

        BotFeedbackMessage.BotFeedbackKindMultiplePositive = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_POSITIVE_GENERIC"] = 1;
            return values;
        })();

        BotFeedbackMessage.BotFeedbackKindMultipleNegative = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[4] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_INTERESTING"] = 4;
            values[valuesById[8] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_ACCURATE"] = 8;
            values[valuesById[16] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_SAFE"] = 16;
            values[valuesById[32] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_OTHER"] = 32;
            values[valuesById[64] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_REFUSED"] = 64;
            values[valuesById[128] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_VISUALLY_APPEALING"] = 128;
            values[valuesById[256] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 256;
            return values;
        })();

        BotFeedbackMessage.BotFeedbackKind = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "BOT_FEEDBACK_POSITIVE"] = 0;
            values[valuesById[1] = "BOT_FEEDBACK_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[3] = "BOT_FEEDBACK_NEGATIVE_INTERESTING"] = 3;
            values[valuesById[4] = "BOT_FEEDBACK_NEGATIVE_ACCURATE"] = 4;
            values[valuesById[5] = "BOT_FEEDBACK_NEGATIVE_SAFE"] = 5;
            values[valuesById[6] = "BOT_FEEDBACK_NEGATIVE_OTHER"] = 6;
            values[valuesById[7] = "BOT_FEEDBACK_NEGATIVE_REFUSED"] = 7;
            values[valuesById[8] = "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING"] = 8;
            values[valuesById[9] = "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 9;
            values[valuesById[10] = "BOT_FEEDBACK_NEGATIVE_PERSONALIZED"] = 10;
            values[valuesById[11] = "BOT_FEEDBACK_NEGATIVE_CLARITY"] = 11;
            values[valuesById[12] = "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON"] = 12;
            values[valuesById[13] = "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY"] = 13;
            values[valuesById[14] = "BOT_FEEDBACK_NEGATIVE"] = 14;
            return values;
        })();

        BotFeedbackMessage.SideBySideSurveyMetadata = (function() {

            function SideBySideSurveyMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            SideBySideSurveyMetadata.prototype.selectedRequestID = null;
            SideBySideSurveyMetadata.prototype.surveyID = null;
            SideBySideSurveyMetadata.prototype.simonSessionFbid = null;
            SideBySideSurveyMetadata.prototype.responseOtid = null;
            SideBySideSurveyMetadata.prototype.responseTimestampMSString = null;
            SideBySideSurveyMetadata.prototype.isSelectedResponsePrimary = null;
            SideBySideSurveyMetadata.prototype.messageIDToEdit = null;
            SideBySideSurveyMetadata.prototype.analyticsData = null;
            SideBySideSurveyMetadata.prototype.metaAiAnalyticsData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_selectedRequestID", {
                get: $util.oneOfGetter($oneOfFields = ["selectedRequestID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_surveyID", {
                get: $util.oneOfGetter($oneOfFields = ["surveyID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_simonSessionFbid", {
                get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseOtid", {
                get: $util.oneOfGetter($oneOfFields = ["responseOtid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseTimestampMSString", {
                get: $util.oneOfGetter($oneOfFields = ["responseTimestampMSString"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_isSelectedResponsePrimary", {
                get: $util.oneOfGetter($oneOfFields = ["isSelectedResponsePrimary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_messageIDToEdit", {
                get: $util.oneOfGetter($oneOfFields = ["messageIDToEdit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_analyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["analyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideSurveyMetadata.prototype, "_metaAiAnalyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["metaAiAnalyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SideBySideSurveyMetadata.create = function create(properties) {
                return new SideBySideSurveyMetadata(properties);
            };

            SideBySideSurveyMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.selectedRequestID != null && Object.hasOwnProperty.call(m, "selectedRequestID"))
                    w.uint32(10).string(m.selectedRequestID);
                if (m.surveyID != null && Object.hasOwnProperty.call(m, "surveyID"))
                    w.uint32(16).uint32(m.surveyID);
                if (m.simonSessionFbid != null && Object.hasOwnProperty.call(m, "simonSessionFbid"))
                    w.uint32(26).string(m.simonSessionFbid);
                if (m.responseOtid != null && Object.hasOwnProperty.call(m, "responseOtid"))
                    w.uint32(34).string(m.responseOtid);
                if (m.responseTimestampMSString != null && Object.hasOwnProperty.call(m, "responseTimestampMSString"))
                    w.uint32(42).string(m.responseTimestampMSString);
                if (m.isSelectedResponsePrimary != null && Object.hasOwnProperty.call(m, "isSelectedResponsePrimary"))
                    w.uint32(48).bool(m.isSelectedResponsePrimary);
                if (m.messageIDToEdit != null && Object.hasOwnProperty.call(m, "messageIDToEdit"))
                    w.uint32(58).string(m.messageIDToEdit);
                if (m.analyticsData != null && Object.hasOwnProperty.call(m, "analyticsData"))
                    $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.encode(m.analyticsData, w.uint32(66).fork()).ldelim();
                if (m.metaAiAnalyticsData != null && Object.hasOwnProperty.call(m, "metaAiAnalyticsData"))
                    $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.encode(m.metaAiAnalyticsData, w.uint32(74).fork()).ldelim();
                return w;
            };

            SideBySideSurveyMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.selectedRequestID = r.string();
                            break;
                        }
                    case 2: {
                            m.surveyID = r.uint32();
                            break;
                        }
                    case 3: {
                            m.simonSessionFbid = r.string();
                            break;
                        }
                    case 4: {
                            m.responseOtid = r.string();
                            break;
                        }
                    case 5: {
                            m.responseTimestampMSString = r.string();
                            break;
                        }
                    case 6: {
                            m.isSelectedResponsePrimary = r.bool();
                            break;
                        }
                    case 7: {
                            m.messageIDToEdit = r.string();
                            break;
                        }
                    case 8: {
                            m.analyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 9: {
                            m.metaAiAnalyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            SideBySideSurveyMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata();
                if (d.selectedRequestID != null) {
                    m.selectedRequestID = String(d.selectedRequestID);
                }
                if (d.surveyID != null) {
                    m.surveyID = d.surveyID >>> 0;
                }
                if (d.simonSessionFbid != null) {
                    m.simonSessionFbid = String(d.simonSessionFbid);
                }
                if (d.responseOtid != null) {
                    m.responseOtid = String(d.responseOtid);
                }
                if (d.responseTimestampMSString != null) {
                    m.responseTimestampMSString = String(d.responseTimestampMSString);
                }
                if (d.isSelectedResponsePrimary != null) {
                    m.isSelectedResponsePrimary = Boolean(d.isSelectedResponsePrimary);
                }
                if (d.messageIDToEdit != null) {
                    m.messageIDToEdit = String(d.messageIDToEdit);
                }
                if (d.analyticsData != null) {
                    if (typeof d.analyticsData !== "object")
                        throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.analyticsData: object expected");
                    m.analyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.fromObject(d.analyticsData, n + 1);
                }
                if (d.metaAiAnalyticsData != null) {
                    if (typeof d.metaAiAnalyticsData !== "object")
                        throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.metaAiAnalyticsData: object expected");
                    m.metaAiAnalyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.fromObject(d.metaAiAnalyticsData, n + 1);
                }
                return m;
            };

            SideBySideSurveyMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.selectedRequestID != null && m.hasOwnProperty("selectedRequestID")) {
                    d.selectedRequestID = m.selectedRequestID;
                    if (o.oneofs)
                        d._selectedRequestID = "selectedRequestID";
                }
                if (m.surveyID != null && m.hasOwnProperty("surveyID")) {
                    d.surveyID = m.surveyID;
                    if (o.oneofs)
                        d._surveyID = "surveyID";
                }
                if (m.simonSessionFbid != null && m.hasOwnProperty("simonSessionFbid")) {
                    d.simonSessionFbid = m.simonSessionFbid;
                    if (o.oneofs)
                        d._simonSessionFbid = "simonSessionFbid";
                }
                if (m.responseOtid != null && m.hasOwnProperty("responseOtid")) {
                    d.responseOtid = m.responseOtid;
                    if (o.oneofs)
                        d._responseOtid = "responseOtid";
                }
                if (m.responseTimestampMSString != null && m.hasOwnProperty("responseTimestampMSString")) {
                    d.responseTimestampMSString = m.responseTimestampMSString;
                    if (o.oneofs)
                        d._responseTimestampMSString = "responseTimestampMSString";
                }
                if (m.isSelectedResponsePrimary != null && m.hasOwnProperty("isSelectedResponsePrimary")) {
                    d.isSelectedResponsePrimary = m.isSelectedResponsePrimary;
                    if (o.oneofs)
                        d._isSelectedResponsePrimary = "isSelectedResponsePrimary";
                }
                if (m.messageIDToEdit != null && m.hasOwnProperty("messageIDToEdit")) {
                    d.messageIDToEdit = m.messageIDToEdit;
                    if (o.oneofs)
                        d._messageIDToEdit = "messageIDToEdit";
                }
                if (m.analyticsData != null && m.hasOwnProperty("analyticsData")) {
                    d.analyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.toObject(m.analyticsData, o);
                    if (o.oneofs)
                        d._analyticsData = "analyticsData";
                }
                if (m.metaAiAnalyticsData != null && m.hasOwnProperty("metaAiAnalyticsData")) {
                    d.metaAiAnalyticsData = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.toObject(m.metaAiAnalyticsData, o);
                    if (o.oneofs)
                        d._metaAiAnalyticsData = "metaAiAnalyticsData";
                }
                return d;
            };

            SideBySideSurveyMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SideBySideSurveyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata";
            };

            SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData = (function() {

                function SidebySideSurveyMetaAiAnalyticsData(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                SidebySideSurveyMetaAiAnalyticsData.prototype.surveyID = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.primaryResponseID = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.testArmName = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.timestampMSString = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaImpressionEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaClickEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.cardImpressionEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.responseEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.abandonEvent = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_surveyID", {
                    get: $util.oneOfGetter($oneOfFields = ["surveyID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_primaryResponseID", {
                    get: $util.oneOfGetter($oneOfFields = ["primaryResponseID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_testArmName", {
                    get: $util.oneOfGetter($oneOfFields = ["testArmName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_timestampMSString", {
                    get: $util.oneOfGetter($oneOfFields = ["timestampMSString"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaClickEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaClickEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_cardImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["cardImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_responseEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["responseEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_abandonEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["abandonEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SidebySideSurveyMetaAiAnalyticsData.create = function create(properties) {
                    return new SidebySideSurveyMetaAiAnalyticsData(properties);
                };

                SidebySideSurveyMetaAiAnalyticsData.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.surveyID != null && Object.hasOwnProperty.call(m, "surveyID"))
                        w.uint32(8).uint32(m.surveyID);
                    if (m.primaryResponseID != null && Object.hasOwnProperty.call(m, "primaryResponseID"))
                        w.uint32(18).string(m.primaryResponseID);
                    if (m.testArmName != null && Object.hasOwnProperty.call(m, "testArmName"))
                        w.uint32(26).string(m.testArmName);
                    if (m.timestampMSString != null && Object.hasOwnProperty.call(m, "timestampMSString"))
                        w.uint32(34).string(m.timestampMSString);
                    if (m.ctaImpressionEvent != null && Object.hasOwnProperty.call(m, "ctaImpressionEvent"))
                        $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.encode(m.ctaImpressionEvent, w.uint32(42).fork()).ldelim();
                    if (m.ctaClickEvent != null && Object.hasOwnProperty.call(m, "ctaClickEvent"))
                        $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.encode(m.ctaClickEvent, w.uint32(50).fork()).ldelim();
                    if (m.cardImpressionEvent != null && Object.hasOwnProperty.call(m, "cardImpressionEvent"))
                        $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.encode(m.cardImpressionEvent, w.uint32(58).fork()).ldelim();
                    if (m.responseEvent != null && Object.hasOwnProperty.call(m, "responseEvent"))
                        $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.encode(m.responseEvent, w.uint32(66).fork()).ldelim();
                    if (m.abandonEvent != null && Object.hasOwnProperty.call(m, "abandonEvent"))
                        $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.encode(m.abandonEvent, w.uint32(74).fork()).ldelim();
                    return w;
                };

                SidebySideSurveyMetaAiAnalyticsData.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.surveyID = r.uint32();
                                break;
                            }
                        case 2: {
                                m.primaryResponseID = r.string();
                                break;
                            }
                        case 3: {
                                m.testArmName = r.string();
                                break;
                            }
                        case 4: {
                                m.timestampMSString = r.string();
                                break;
                            }
                        case 5: {
                                m.ctaImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 6: {
                                m.ctaClickEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 7: {
                                m.cardImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 8: {
                                m.responseEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        case 9: {
                                m.abandonEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.decode(r, r.uint32(), undefined, n + 1);
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                SidebySideSurveyMetaAiAnalyticsData.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData();
                    if (d.surveyID != null) {
                        m.surveyID = d.surveyID >>> 0;
                    }
                    if (d.primaryResponseID != null) {
                        m.primaryResponseID = String(d.primaryResponseID);
                    }
                    if (d.testArmName != null) {
                        m.testArmName = String(d.testArmName);
                    }
                    if (d.timestampMSString != null) {
                        m.timestampMSString = String(d.timestampMSString);
                    }
                    if (d.ctaImpressionEvent != null) {
                        if (typeof d.ctaImpressionEvent !== "object")
                            throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaImpressionEvent: object expected");
                        m.ctaImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.fromObject(d.ctaImpressionEvent, n + 1);
                    }
                    if (d.ctaClickEvent != null) {
                        if (typeof d.ctaClickEvent !== "object")
                            throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaClickEvent: object expected");
                        m.ctaClickEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.fromObject(d.ctaClickEvent, n + 1);
                    }
                    if (d.cardImpressionEvent != null) {
                        if (typeof d.cardImpressionEvent !== "object")
                            throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.cardImpressionEvent: object expected");
                        m.cardImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.fromObject(d.cardImpressionEvent, n + 1);
                    }
                    if (d.responseEvent != null) {
                        if (typeof d.responseEvent !== "object")
                            throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.responseEvent: object expected");
                        m.responseEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.fromObject(d.responseEvent, n + 1);
                    }
                    if (d.abandonEvent != null) {
                        if (typeof d.abandonEvent !== "object")
                            throw TypeError(".WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.abandonEvent: object expected");
                        m.abandonEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.fromObject(d.abandonEvent, n + 1);
                    }
                    return m;
                };

                SidebySideSurveyMetaAiAnalyticsData.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.surveyID != null && m.hasOwnProperty("surveyID")) {
                        d.surveyID = m.surveyID;
                        if (o.oneofs)
                            d._surveyID = "surveyID";
                    }
                    if (m.primaryResponseID != null && m.hasOwnProperty("primaryResponseID")) {
                        d.primaryResponseID = m.primaryResponseID;
                        if (o.oneofs)
                            d._primaryResponseID = "primaryResponseID";
                    }
                    if (m.testArmName != null && m.hasOwnProperty("testArmName")) {
                        d.testArmName = m.testArmName;
                        if (o.oneofs)
                            d._testArmName = "testArmName";
                    }
                    if (m.timestampMSString != null && m.hasOwnProperty("timestampMSString")) {
                        d.timestampMSString = m.timestampMSString;
                        if (o.oneofs)
                            d._timestampMSString = "timestampMSString";
                    }
                    if (m.ctaImpressionEvent != null && m.hasOwnProperty("ctaImpressionEvent")) {
                        d.ctaImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.toObject(m.ctaImpressionEvent, o);
                        if (o.oneofs)
                            d._ctaImpressionEvent = "ctaImpressionEvent";
                    }
                    if (m.ctaClickEvent != null && m.hasOwnProperty("ctaClickEvent")) {
                        d.ctaClickEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.toObject(m.ctaClickEvent, o);
                        if (o.oneofs)
                            d._ctaClickEvent = "ctaClickEvent";
                    }
                    if (m.cardImpressionEvent != null && m.hasOwnProperty("cardImpressionEvent")) {
                        d.cardImpressionEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.toObject(m.cardImpressionEvent, o);
                        if (o.oneofs)
                            d._cardImpressionEvent = "cardImpressionEvent";
                    }
                    if (m.responseEvent != null && m.hasOwnProperty("responseEvent")) {
                        d.responseEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.toObject(m.responseEvent, o);
                        if (o.oneofs)
                            d._responseEvent = "responseEvent";
                    }
                    if (m.abandonEvent != null && m.hasOwnProperty("abandonEvent")) {
                        d.abandonEvent = $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.toObject(m.abandonEvent, o);
                        if (o.oneofs)
                            d._abandonEvent = "abandonEvent";
                    }
                    return d;
                };

                SidebySideSurveyMetaAiAnalyticsData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                SidebySideSurveyMetaAiAnalyticsData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData";
                };

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData = (function() {

                    function SideBySideSurveyAbandonEventData(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    SideBySideSurveyAbandonEventData.prototype.abandonDwellTimeMSString = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyAbandonEventData.prototype, "_abandonDwellTimeMSString", {
                        get: $util.oneOfGetter($oneOfFields = ["abandonDwellTimeMSString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyAbandonEventData.create = function create(properties) {
                        return new SideBySideSurveyAbandonEventData(properties);
                    };

                    SideBySideSurveyAbandonEventData.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.abandonDwellTimeMSString != null && Object.hasOwnProperty.call(m, "abandonDwellTimeMSString"))
                            w.uint32(10).string(m.abandonDwellTimeMSString);
                        return w;
                    };

                    SideBySideSurveyAbandonEventData.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.abandonDwellTimeMSString = r.string();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    SideBySideSurveyAbandonEventData.fromObject = function fromObject(d, n) {
                        if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData();
                        if (d.abandonDwellTimeMSString != null) {
                            m.abandonDwellTimeMSString = String(d.abandonDwellTimeMSString);
                        }
                        return m;
                    };

                    SideBySideSurveyAbandonEventData.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.abandonDwellTimeMSString != null && m.hasOwnProperty("abandonDwellTimeMSString")) {
                            d.abandonDwellTimeMSString = m.abandonDwellTimeMSString;
                            if (o.oneofs)
                                d._abandonDwellTimeMSString = "abandonDwellTimeMSString";
                        }
                        return d;
                    };

                    SideBySideSurveyAbandonEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyAbandonEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData";
                    };

                    return SideBySideSurveyAbandonEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData = (function() {

                    function SideBySideSurveyResponseEventData(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    SideBySideSurveyResponseEventData.prototype.responseDwellTimeMSString = null;
                    SideBySideSurveyResponseEventData.prototype.selectedResponseID = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_responseDwellTimeMSString", {
                        get: $util.oneOfGetter($oneOfFields = ["responseDwellTimeMSString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_selectedResponseID", {
                        get: $util.oneOfGetter($oneOfFields = ["selectedResponseID"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyResponseEventData.create = function create(properties) {
                        return new SideBySideSurveyResponseEventData(properties);
                    };

                    SideBySideSurveyResponseEventData.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.responseDwellTimeMSString != null && Object.hasOwnProperty.call(m, "responseDwellTimeMSString"))
                            w.uint32(10).string(m.responseDwellTimeMSString);
                        if (m.selectedResponseID != null && Object.hasOwnProperty.call(m, "selectedResponseID"))
                            w.uint32(18).string(m.selectedResponseID);
                        return w;
                    };

                    SideBySideSurveyResponseEventData.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.responseDwellTimeMSString = r.string();
                                    break;
                                }
                            case 2: {
                                    m.selectedResponseID = r.string();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    SideBySideSurveyResponseEventData.fromObject = function fromObject(d, n) {
                        if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData();
                        if (d.responseDwellTimeMSString != null) {
                            m.responseDwellTimeMSString = String(d.responseDwellTimeMSString);
                        }
                        if (d.selectedResponseID != null) {
                            m.selectedResponseID = String(d.selectedResponseID);
                        }
                        return m;
                    };

                    SideBySideSurveyResponseEventData.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.responseDwellTimeMSString != null && m.hasOwnProperty("responseDwellTimeMSString")) {
                            d.responseDwellTimeMSString = m.responseDwellTimeMSString;
                            if (o.oneofs)
                                d._responseDwellTimeMSString = "responseDwellTimeMSString";
                        }
                        if (m.selectedResponseID != null && m.hasOwnProperty("selectedResponseID")) {
                            d.selectedResponseID = m.selectedResponseID;
                            if (o.oneofs)
                                d._selectedResponseID = "selectedResponseID";
                        }
                        return d;
                    };

                    SideBySideSurveyResponseEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyResponseEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData";
                    };

                    return SideBySideSurveyResponseEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData = (function() {

                    function SideBySideSurveyCardImpressionEventData(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    SideBySideSurveyCardImpressionEventData.create = function create(properties) {
                        return new SideBySideSurveyCardImpressionEventData(properties);
                    };

                    SideBySideSurveyCardImpressionEventData.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        return w;
                    };

                    SideBySideSurveyCardImpressionEventData.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    SideBySideSurveyCardImpressionEventData.fromObject = function fromObject(d, n) {
                        if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        return new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData();
                    };

                    SideBySideSurveyCardImpressionEventData.toObject = function toObject() {
                        return {};
                    };

                    SideBySideSurveyCardImpressionEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCardImpressionEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData";
                    };

                    return SideBySideSurveyCardImpressionEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData = (function() {

                    function SideBySideSurveyCTAClickEventData(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    SideBySideSurveyCTAClickEventData.prototype.isSurveyExpired = null;
                    SideBySideSurveyCTAClickEventData.prototype.clickDwellTimeMSString = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_clickDwellTimeMSString", {
                        get: $util.oneOfGetter($oneOfFields = ["clickDwellTimeMSString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyCTAClickEventData.create = function create(properties) {
                        return new SideBySideSurveyCTAClickEventData(properties);
                    };

                    SideBySideSurveyCTAClickEventData.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.isSurveyExpired != null && Object.hasOwnProperty.call(m, "isSurveyExpired"))
                            w.uint32(8).bool(m.isSurveyExpired);
                        if (m.clickDwellTimeMSString != null && Object.hasOwnProperty.call(m, "clickDwellTimeMSString"))
                            w.uint32(18).string(m.clickDwellTimeMSString);
                        return w;
                    };

                    SideBySideSurveyCTAClickEventData.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.isSurveyExpired = r.bool();
                                    break;
                                }
                            case 2: {
                                    m.clickDwellTimeMSString = r.string();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    SideBySideSurveyCTAClickEventData.fromObject = function fromObject(d, n) {
                        if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData();
                        if (d.isSurveyExpired != null) {
                            m.isSurveyExpired = Boolean(d.isSurveyExpired);
                        }
                        if (d.clickDwellTimeMSString != null) {
                            m.clickDwellTimeMSString = String(d.clickDwellTimeMSString);
                        }
                        return m;
                    };

                    SideBySideSurveyCTAClickEventData.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.isSurveyExpired != null && m.hasOwnProperty("isSurveyExpired")) {
                            d.isSurveyExpired = m.isSurveyExpired;
                            if (o.oneofs)
                                d._isSurveyExpired = "isSurveyExpired";
                        }
                        if (m.clickDwellTimeMSString != null && m.hasOwnProperty("clickDwellTimeMSString")) {
                            d.clickDwellTimeMSString = m.clickDwellTimeMSString;
                            if (o.oneofs)
                                d._clickDwellTimeMSString = "clickDwellTimeMSString";
                        }
                        return d;
                    };

                    SideBySideSurveyCTAClickEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCTAClickEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData";
                    };

                    return SideBySideSurveyCTAClickEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData = (function() {

                    function SideBySideSurveyCTAImpressionEventData(p) {
                        if (p)
                            for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    }

                    SideBySideSurveyCTAImpressionEventData.prototype.isSurveyExpired = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    Object.defineProperty(SideBySideSurveyCTAImpressionEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyCTAImpressionEventData.create = function create(properties) {
                        return new SideBySideSurveyCTAImpressionEventData(properties);
                    };

                    SideBySideSurveyCTAImpressionEventData.encode = function encode(m, w) {
                        if (!w)
                            w = $Writer.create();
                        if (m.isSurveyExpired != null && Object.hasOwnProperty.call(m, "isSurveyExpired"))
                            w.uint32(8).bool(m.isSurveyExpired);
                        return w;
                    };

                    SideBySideSurveyCTAImpressionEventData.decode = function decode(r, l, e, n) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (n === undefined)
                            n = 0;
                        if (n > $Reader.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData();
                        while (r.pos < c) {
                            var t = r.uint32();
                            if (t === e)
                                break;
                            switch (t >>> 3) {
                            case 1: {
                                    m.isSurveyExpired = r.bool();
                                    break;
                                }
                            default:
                                r.skipType(t & 7, n);
                                break;
                            }
                        }
                        return m;
                    };

                    SideBySideSurveyCTAImpressionEventData.fromObject = function fromObject(d, n) {
                        if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData)
                            return d;
                        if (n === undefined)
                            n = 0;
                        if (n > $util.recursionLimit)
                            throw Error("maximum nesting depth exceeded");
                        var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData();
                        if (d.isSurveyExpired != null) {
                            m.isSurveyExpired = Boolean(d.isSurveyExpired);
                        }
                        return m;
                    };

                    SideBySideSurveyCTAImpressionEventData.toObject = function toObject(m, o) {
                        if (!o)
                            o = {};
                        var d = {};
                        if (m.isSurveyExpired != null && m.hasOwnProperty("isSurveyExpired")) {
                            d.isSurveyExpired = m.isSurveyExpired;
                            if (o.oneofs)
                                d._isSurveyExpired = "isSurveyExpired";
                        }
                        return d;
                    };

                    SideBySideSurveyCTAImpressionEventData.prototype.toJSON = function toJSON() {
                        return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCTAImpressionEventData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                        if (typeUrlPrefix === undefined) {
                            typeUrlPrefix = "type.googleapis.com";
                        }
                        return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData";
                    };

                    return SideBySideSurveyCTAImpressionEventData;
                })();

                return SidebySideSurveyMetaAiAnalyticsData;
            })();

            SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData = (function() {

                function SideBySideSurveyAnalyticsData(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                SideBySideSurveyAnalyticsData.prototype.tessaEvent = null;
                SideBySideSurveyAnalyticsData.prototype.tessaSessionFbid = null;
                SideBySideSurveyAnalyticsData.prototype.simonSessionFbid = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_simonSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SideBySideSurveyAnalyticsData.create = function create(properties) {
                    return new SideBySideSurveyAnalyticsData(properties);
                };

                SideBySideSurveyAnalyticsData.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.tessaEvent != null && Object.hasOwnProperty.call(m, "tessaEvent"))
                        w.uint32(10).string(m.tessaEvent);
                    if (m.tessaSessionFbid != null && Object.hasOwnProperty.call(m, "tessaSessionFbid"))
                        w.uint32(18).string(m.tessaSessionFbid);
                    if (m.simonSessionFbid != null && Object.hasOwnProperty.call(m, "simonSessionFbid"))
                        w.uint32(26).string(m.simonSessionFbid);
                    return w;
                };

                SideBySideSurveyAnalyticsData.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.tessaEvent = r.string();
                                break;
                            }
                        case 2: {
                                m.tessaSessionFbid = r.string();
                                break;
                            }
                        case 3: {
                                m.simonSessionFbid = r.string();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                SideBySideSurveyAnalyticsData.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData();
                    if (d.tessaEvent != null) {
                        m.tessaEvent = String(d.tessaEvent);
                    }
                    if (d.tessaSessionFbid != null) {
                        m.tessaSessionFbid = String(d.tessaSessionFbid);
                    }
                    if (d.simonSessionFbid != null) {
                        m.simonSessionFbid = String(d.simonSessionFbid);
                    }
                    return m;
                };

                SideBySideSurveyAnalyticsData.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.tessaEvent != null && m.hasOwnProperty("tessaEvent")) {
                        d.tessaEvent = m.tessaEvent;
                        if (o.oneofs)
                            d._tessaEvent = "tessaEvent";
                    }
                    if (m.tessaSessionFbid != null && m.hasOwnProperty("tessaSessionFbid")) {
                        d.tessaSessionFbid = m.tessaSessionFbid;
                        if (o.oneofs)
                            d._tessaSessionFbid = "tessaSessionFbid";
                    }
                    if (m.simonSessionFbid != null && m.hasOwnProperty("simonSessionFbid")) {
                        d.simonSessionFbid = m.simonSessionFbid;
                        if (o.oneofs)
                            d._simonSessionFbid = "simonSessionFbid";
                    }
                    return d;
                };

                SideBySideSurveyAnalyticsData.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                SideBySideSurveyAnalyticsData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAAICommon.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData";
                };

                return SideBySideSurveyAnalyticsData;
            })();

            return SideBySideSurveyMetadata;
        })();

        return BotFeedbackMessage;
    })();

    WAAICommon.BotDocumentMessageMetadata = (function() {

        function BotDocumentMessageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotDocumentMessageMetadata.prototype.pluginType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotDocumentMessageMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotDocumentMessageMetadata.create = function create(properties) {
            return new BotDocumentMessageMetadata(properties);
        };

        BotDocumentMessageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.pluginType != null && Object.hasOwnProperty.call(m, "pluginType"))
                w.uint32(8).int32(m.pluginType);
            return w;
        };

        BotDocumentMessageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotDocumentMessageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.pluginType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotDocumentMessageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotDocumentMessageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotDocumentMessageMetadata();
            switch (d.pluginType) {
            default:
                if (typeof d.pluginType === "number") {
                    m.pluginType = d.pluginType;
                    break;
                }
                break;
            case "TEXT_EXTRACTION":
            case 0:
                m.pluginType = 0;
                break;
            case "OCR_AND_IMAGES":
            case 1:
                m.pluginType = 1;
                break;
            }
            return m;
        };

        BotDocumentMessageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.pluginType != null && m.hasOwnProperty("pluginType")) {
                d.pluginType = o.enums === String ? $root.WAAICommon.BotDocumentMessageMetadata.DocumentPluginType[m.pluginType] === undefined ? m.pluginType : $root.WAAICommon.BotDocumentMessageMetadata.DocumentPluginType[m.pluginType] : m.pluginType;
                if (o.oneofs)
                    d._pluginType = "pluginType";
            }
            return d;
        };

        BotDocumentMessageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotDocumentMessageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotDocumentMessageMetadata";
        };

        BotDocumentMessageMetadata.DocumentPluginType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "TEXT_EXTRACTION"] = 0;
            values[valuesById[1] = "OCR_AND_IMAGES"] = 1;
            return values;
        })();

        return BotDocumentMessageMetadata;
    })();

    WAAICommon.AIHomeState = (function() {

        function AIHomeState(p) {
            this.capabilityOptions = [];
            this.conversationOptions = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIHomeState.prototype.lastFetchTime = null;
        AIHomeState.prototype.capabilityOptions = $util.emptyArray;
        AIHomeState.prototype.conversationOptions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIHomeState.prototype, "_lastFetchTime", {
            get: $util.oneOfGetter($oneOfFields = ["lastFetchTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIHomeState.create = function create(properties) {
            return new AIHomeState(properties);
        };

        AIHomeState.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.lastFetchTime != null && Object.hasOwnProperty.call(m, "lastFetchTime"))
                w.uint32(8).int64(m.lastFetchTime);
            if (m.capabilityOptions != null && m.capabilityOptions.length) {
                for (var i = 0; i < m.capabilityOptions.length; ++i)
                    $root.WAAICommon.AIHomeState.AIHomeOption.encode(m.capabilityOptions[i], w.uint32(18).fork()).ldelim();
            }
            if (m.conversationOptions != null && m.conversationOptions.length) {
                for (var i = 0; i < m.conversationOptions.length; ++i)
                    $root.WAAICommon.AIHomeState.AIHomeOption.encode(m.conversationOptions[i], w.uint32(26).fork()).ldelim();
            }
            return w;
        };

        AIHomeState.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIHomeState();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.lastFetchTime = r.int64();
                        break;
                    }
                case 2: {
                        if (!(m.capabilityOptions && m.capabilityOptions.length))
                            m.capabilityOptions = [];
                        m.capabilityOptions.push($root.WAAICommon.AIHomeState.AIHomeOption.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        if (!(m.conversationOptions && m.conversationOptions.length))
                            m.conversationOptions = [];
                        m.conversationOptions.push($root.WAAICommon.AIHomeState.AIHomeOption.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIHomeState.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIHomeState)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIHomeState();
            if (d.lastFetchTime != null) {
                if ($util.Long)
                    (m.lastFetchTime = $util.Long.fromValue(d.lastFetchTime)).unsigned = false;
                else if (typeof d.lastFetchTime === "string")
                    m.lastFetchTime = parseInt(d.lastFetchTime, 10);
                else if (typeof d.lastFetchTime === "number")
                    m.lastFetchTime = d.lastFetchTime;
                else if (typeof d.lastFetchTime === "object")
                    m.lastFetchTime = new $util.LongBits(d.lastFetchTime.low >>> 0, d.lastFetchTime.high >>> 0).toNumber();
            }
            if (d.capabilityOptions) {
                if (!Array.isArray(d.capabilityOptions))
                    throw TypeError(".WAAICommon.AIHomeState.capabilityOptions: array expected");
                m.capabilityOptions = [];
                for (var i = 0; i < d.capabilityOptions.length; ++i) {
                    if (typeof d.capabilityOptions[i] !== "object")
                        throw TypeError(".WAAICommon.AIHomeState.capabilityOptions: object expected");
                    m.capabilityOptions[i] = $root.WAAICommon.AIHomeState.AIHomeOption.fromObject(d.capabilityOptions[i], n + 1);
                }
            }
            if (d.conversationOptions) {
                if (!Array.isArray(d.conversationOptions))
                    throw TypeError(".WAAICommon.AIHomeState.conversationOptions: array expected");
                m.conversationOptions = [];
                for (var i = 0; i < d.conversationOptions.length; ++i) {
                    if (typeof d.conversationOptions[i] !== "object")
                        throw TypeError(".WAAICommon.AIHomeState.conversationOptions: object expected");
                    m.conversationOptions[i] = $root.WAAICommon.AIHomeState.AIHomeOption.fromObject(d.conversationOptions[i], n + 1);
                }
            }
            return m;
        };

        AIHomeState.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilityOptions = [];
                d.conversationOptions = [];
            }
            if (m.lastFetchTime != null && m.hasOwnProperty("lastFetchTime")) {
                if (typeof m.lastFetchTime === "number")
                    d.lastFetchTime = o.longs === String ? String(m.lastFetchTime) : m.lastFetchTime;
                else
                    d.lastFetchTime = o.longs === String ? $util.Long.prototype.toString.call(m.lastFetchTime) : o.longs === Number ? new $util.LongBits(m.lastFetchTime.low >>> 0, m.lastFetchTime.high >>> 0).toNumber() : m.lastFetchTime;
                if (o.oneofs)
                    d._lastFetchTime = "lastFetchTime";
            }
            if (m.capabilityOptions && m.capabilityOptions.length) {
                d.capabilityOptions = [];
                for (var j = 0; j < m.capabilityOptions.length; ++j) {
                    d.capabilityOptions[j] = $root.WAAICommon.AIHomeState.AIHomeOption.toObject(m.capabilityOptions[j], o);
                }
            }
            if (m.conversationOptions && m.conversationOptions.length) {
                d.conversationOptions = [];
                for (var j = 0; j < m.conversationOptions.length; ++j) {
                    d.conversationOptions[j] = $root.WAAICommon.AIHomeState.AIHomeOption.toObject(m.conversationOptions[j], o);
                }
            }
            return d;
        };

        AIHomeState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIHomeState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIHomeState";
        };

        AIHomeState.AIHomeOption = (function() {

            function AIHomeOption(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIHomeOption.prototype.type = null;
            AIHomeOption.prototype.title = null;
            AIHomeOption.prototype.promptText = null;
            AIHomeOption.prototype.sessionID = null;
            AIHomeOption.prototype.imageWdsIdentifier = null;
            AIHomeOption.prototype.imageTintColor = null;
            AIHomeOption.prototype.imageBackgroundColor = null;
            AIHomeOption.prototype.cardTypeID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_promptText", {
                get: $util.oneOfGetter($oneOfFields = ["promptText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_sessionID", {
                get: $util.oneOfGetter($oneOfFields = ["sessionID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageWdsIdentifier", {
                get: $util.oneOfGetter($oneOfFields = ["imageWdsIdentifier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageTintColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageTintColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_imageBackgroundColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageBackgroundColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIHomeOption.prototype, "_cardTypeID", {
                get: $util.oneOfGetter($oneOfFields = ["cardTypeID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIHomeOption.create = function create(properties) {
                return new AIHomeOption(properties);
            };

            AIHomeOption.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(18).string(m.title);
                if (m.promptText != null && Object.hasOwnProperty.call(m, "promptText"))
                    w.uint32(26).string(m.promptText);
                if (m.sessionID != null && Object.hasOwnProperty.call(m, "sessionID"))
                    w.uint32(34).string(m.sessionID);
                if (m.imageWdsIdentifier != null && Object.hasOwnProperty.call(m, "imageWdsIdentifier"))
                    w.uint32(42).string(m.imageWdsIdentifier);
                if (m.imageTintColor != null && Object.hasOwnProperty.call(m, "imageTintColor"))
                    w.uint32(50).string(m.imageTintColor);
                if (m.imageBackgroundColor != null && Object.hasOwnProperty.call(m, "imageBackgroundColor"))
                    w.uint32(58).string(m.imageBackgroundColor);
                if (m.cardTypeID != null && Object.hasOwnProperty.call(m, "cardTypeID"))
                    w.uint32(66).string(m.cardTypeID);
                return w;
            };

            AIHomeOption.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIHomeState.AIHomeOption();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.type = r.int32();
                            break;
                        }
                    case 2: {
                            m.title = r.string();
                            break;
                        }
                    case 3: {
                            m.promptText = r.string();
                            break;
                        }
                    case 4: {
                            m.sessionID = r.string();
                            break;
                        }
                    case 5: {
                            m.imageWdsIdentifier = r.string();
                            break;
                        }
                    case 6: {
                            m.imageTintColor = r.string();
                            break;
                        }
                    case 7: {
                            m.imageBackgroundColor = r.string();
                            break;
                        }
                    case 8: {
                            m.cardTypeID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIHomeOption.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.AIHomeState.AIHomeOption)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.AIHomeState.AIHomeOption();
                switch (d.type) {
                default:
                    if (typeof d.type === "number") {
                        m.type = d.type;
                        break;
                    }
                    break;
                case "PROMPT":
                case 0:
                    m.type = 0;
                    break;
                case "CREATE_IMAGE":
                case 1:
                    m.type = 1;
                    break;
                case "ANIMATE_PHOTO":
                case 2:
                    m.type = 2;
                    break;
                case "ANALYZE_FILE":
                case 3:
                    m.type = 3;
                    break;
                case "COLLABORATE":
                case 4:
                    m.type = 4;
                    break;
                case "OPEN_GREETING_CARD":
                case 5:
                    m.type = 5;
                    break;
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.promptText != null) {
                    m.promptText = String(d.promptText);
                }
                if (d.sessionID != null) {
                    m.sessionID = String(d.sessionID);
                }
                if (d.imageWdsIdentifier != null) {
                    m.imageWdsIdentifier = String(d.imageWdsIdentifier);
                }
                if (d.imageTintColor != null) {
                    m.imageTintColor = String(d.imageTintColor);
                }
                if (d.imageBackgroundColor != null) {
                    m.imageBackgroundColor = String(d.imageBackgroundColor);
                }
                if (d.cardTypeID != null) {
                    m.cardTypeID = String(d.cardTypeID);
                }
                return m;
            };

            AIHomeOption.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.type != null && m.hasOwnProperty("type")) {
                    d.type = o.enums === String ? $root.WAAICommon.AIHomeState.AIHomeOption.AIHomeActionType[m.type] === undefined ? m.type : $root.WAAICommon.AIHomeState.AIHomeOption.AIHomeActionType[m.type] : m.type;
                    if (o.oneofs)
                        d._type = "type";
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.promptText != null && m.hasOwnProperty("promptText")) {
                    d.promptText = m.promptText;
                    if (o.oneofs)
                        d._promptText = "promptText";
                }
                if (m.sessionID != null && m.hasOwnProperty("sessionID")) {
                    d.sessionID = m.sessionID;
                    if (o.oneofs)
                        d._sessionID = "sessionID";
                }
                if (m.imageWdsIdentifier != null && m.hasOwnProperty("imageWdsIdentifier")) {
                    d.imageWdsIdentifier = m.imageWdsIdentifier;
                    if (o.oneofs)
                        d._imageWdsIdentifier = "imageWdsIdentifier";
                }
                if (m.imageTintColor != null && m.hasOwnProperty("imageTintColor")) {
                    d.imageTintColor = m.imageTintColor;
                    if (o.oneofs)
                        d._imageTintColor = "imageTintColor";
                }
                if (m.imageBackgroundColor != null && m.hasOwnProperty("imageBackgroundColor")) {
                    d.imageBackgroundColor = m.imageBackgroundColor;
                    if (o.oneofs)
                        d._imageBackgroundColor = "imageBackgroundColor";
                }
                if (m.cardTypeID != null && m.hasOwnProperty("cardTypeID")) {
                    d.cardTypeID = m.cardTypeID;
                    if (o.oneofs)
                        d._cardTypeID = "cardTypeID";
                }
                return d;
            };

            AIHomeOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIHomeOption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.AIHomeState.AIHomeOption";
            };

            AIHomeOption.AIHomeActionType = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "PROMPT"] = 0;
                values[valuesById[1] = "CREATE_IMAGE"] = 1;
                values[valuesById[2] = "ANIMATE_PHOTO"] = 2;
                values[valuesById[3] = "ANALYZE_FILE"] = 3;
                values[valuesById[4] = "COLLABORATE"] = 4;
                values[valuesById[5] = "OPEN_GREETING_CARD"] = 5;
                return values;
            })();

            return AIHomeOption;
        })();

        return AIHomeState;
    })();

    WAAICommon.BotInfrastructureDiagnostics = (function() {

        function BotInfrastructureDiagnostics(p) {
            this.toolsUsed = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotInfrastructureDiagnostics.prototype.botBackend = null;
        BotInfrastructureDiagnostics.prototype.toolsUsed = $util.emptyArray;
        BotInfrastructureDiagnostics.prototype.isThinking = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_botBackend", {
            get: $util.oneOfGetter($oneOfFields = ["botBackend"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_isThinking", {
            get: $util.oneOfGetter($oneOfFields = ["isThinking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotInfrastructureDiagnostics.create = function create(properties) {
            return new BotInfrastructureDiagnostics(properties);
        };

        BotInfrastructureDiagnostics.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.botBackend != null && Object.hasOwnProperty.call(m, "botBackend"))
                w.uint32(8).int32(m.botBackend);
            if (m.toolsUsed != null && m.toolsUsed.length) {
                for (var i = 0; i < m.toolsUsed.length; ++i)
                    w.uint32(18).string(m.toolsUsed[i]);
            }
            if (m.isThinking != null && Object.hasOwnProperty.call(m, "isThinking"))
                w.uint32(24).bool(m.isThinking);
            return w;
        };

        BotInfrastructureDiagnostics.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotInfrastructureDiagnostics();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.botBackend = r.int32();
                        break;
                    }
                case 2: {
                        if (!(m.toolsUsed && m.toolsUsed.length))
                            m.toolsUsed = [];
                        m.toolsUsed.push(r.string());
                        break;
                    }
                case 3: {
                        m.isThinking = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotInfrastructureDiagnostics.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotInfrastructureDiagnostics)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotInfrastructureDiagnostics();
            switch (d.botBackend) {
            default:
                if (typeof d.botBackend === "number") {
                    m.botBackend = d.botBackend;
                    break;
                }
                break;
            case "AAPI":
            case 0:
                m.botBackend = 0;
                break;
            case "CLIPPY":
            case 1:
                m.botBackend = 1;
                break;
            }
            if (d.toolsUsed) {
                if (!Array.isArray(d.toolsUsed))
                    throw TypeError(".WAAICommon.BotInfrastructureDiagnostics.toolsUsed: array expected");
                m.toolsUsed = [];
                for (var i = 0; i < d.toolsUsed.length; ++i) {
                    m.toolsUsed[i] = String(d.toolsUsed[i]);
                }
            }
            if (d.isThinking != null) {
                m.isThinking = Boolean(d.isThinking);
            }
            return m;
        };

        BotInfrastructureDiagnostics.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.toolsUsed = [];
            }
            if (m.botBackend != null && m.hasOwnProperty("botBackend")) {
                d.botBackend = o.enums === String ? $root.WAAICommon.BotInfrastructureDiagnostics.BotBackend[m.botBackend] === undefined ? m.botBackend : $root.WAAICommon.BotInfrastructureDiagnostics.BotBackend[m.botBackend] : m.botBackend;
                if (o.oneofs)
                    d._botBackend = "botBackend";
            }
            if (m.toolsUsed && m.toolsUsed.length) {
                d.toolsUsed = [];
                for (var j = 0; j < m.toolsUsed.length; ++j) {
                    d.toolsUsed[j] = m.toolsUsed[j];
                }
            }
            if (m.isThinking != null && m.hasOwnProperty("isThinking")) {
                d.isThinking = m.isThinking;
                if (o.oneofs)
                    d._isThinking = "isThinking";
            }
            return d;
        };

        BotInfrastructureDiagnostics.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotInfrastructureDiagnostics.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotInfrastructureDiagnostics";
        };

        BotInfrastructureDiagnostics.BotBackend = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AAPI"] = 0;
            values[valuesById[1] = "CLIPPY"] = 1;
            return values;
        })();

        return BotInfrastructureDiagnostics;
    })();

    WAAICommon.BotSuggestedPromptMetadata = (function() {

        function BotSuggestedPromptMetadata(p) {
            this.suggestedPrompts = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotSuggestedPromptMetadata.prototype.suggestedPrompts = $util.emptyArray;
        BotSuggestedPromptMetadata.prototype.selectedPromptIndex = null;
        BotSuggestedPromptMetadata.prototype.promptSuggestions = null;
        BotSuggestedPromptMetadata.prototype.selectedPromptID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptIndex", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_promptSuggestions", {
            get: $util.oneOfGetter($oneOfFields = ["promptSuggestions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptID", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSuggestedPromptMetadata.create = function create(properties) {
            return new BotSuggestedPromptMetadata(properties);
        };

        BotSuggestedPromptMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.suggestedPrompts != null && m.suggestedPrompts.length) {
                for (var i = 0; i < m.suggestedPrompts.length; ++i)
                    w.uint32(10).string(m.suggestedPrompts[i]);
            }
            if (m.selectedPromptIndex != null && Object.hasOwnProperty.call(m, "selectedPromptIndex"))
                w.uint32(16).uint32(m.selectedPromptIndex);
            if (m.promptSuggestions != null && Object.hasOwnProperty.call(m, "promptSuggestions"))
                $root.WAAICommon.BotPromptSuggestions.encode(m.promptSuggestions, w.uint32(26).fork()).ldelim();
            if (m.selectedPromptID != null && Object.hasOwnProperty.call(m, "selectedPromptID"))
                w.uint32(34).string(m.selectedPromptID);
            return w;
        };

        BotSuggestedPromptMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSuggestedPromptMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.suggestedPrompts && m.suggestedPrompts.length))
                            m.suggestedPrompts = [];
                        m.suggestedPrompts.push(r.string());
                        break;
                    }
                case 2: {
                        m.selectedPromptIndex = r.uint32();
                        break;
                    }
                case 3: {
                        m.promptSuggestions = $root.WAAICommon.BotPromptSuggestions.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.selectedPromptID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotSuggestedPromptMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotSuggestedPromptMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotSuggestedPromptMetadata();
            if (d.suggestedPrompts) {
                if (!Array.isArray(d.suggestedPrompts))
                    throw TypeError(".WAAICommon.BotSuggestedPromptMetadata.suggestedPrompts: array expected");
                m.suggestedPrompts = [];
                for (var i = 0; i < d.suggestedPrompts.length; ++i) {
                    m.suggestedPrompts[i] = String(d.suggestedPrompts[i]);
                }
            }
            if (d.selectedPromptIndex != null) {
                m.selectedPromptIndex = d.selectedPromptIndex >>> 0;
            }
            if (d.promptSuggestions != null) {
                if (typeof d.promptSuggestions !== "object")
                    throw TypeError(".WAAICommon.BotSuggestedPromptMetadata.promptSuggestions: object expected");
                m.promptSuggestions = $root.WAAICommon.BotPromptSuggestions.fromObject(d.promptSuggestions, n + 1);
            }
            if (d.selectedPromptID != null) {
                m.selectedPromptID = String(d.selectedPromptID);
            }
            return m;
        };

        BotSuggestedPromptMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.suggestedPrompts = [];
            }
            if (m.suggestedPrompts && m.suggestedPrompts.length) {
                d.suggestedPrompts = [];
                for (var j = 0; j < m.suggestedPrompts.length; ++j) {
                    d.suggestedPrompts[j] = m.suggestedPrompts[j];
                }
            }
            if (m.selectedPromptIndex != null && m.hasOwnProperty("selectedPromptIndex")) {
                d.selectedPromptIndex = m.selectedPromptIndex;
                if (o.oneofs)
                    d._selectedPromptIndex = "selectedPromptIndex";
            }
            if (m.promptSuggestions != null && m.hasOwnProperty("promptSuggestions")) {
                d.promptSuggestions = $root.WAAICommon.BotPromptSuggestions.toObject(m.promptSuggestions, o);
                if (o.oneofs)
                    d._promptSuggestions = "promptSuggestions";
            }
            if (m.selectedPromptID != null && m.hasOwnProperty("selectedPromptID")) {
                d.selectedPromptID = m.selectedPromptID;
                if (o.oneofs)
                    d._selectedPromptID = "selectedPromptID";
            }
            return d;
        };

        BotSuggestedPromptMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSuggestedPromptMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotSuggestedPromptMetadata";
        };

        return BotSuggestedPromptMetadata;
    })();

    WAAICommon.BotPromptSuggestions = (function() {

        function BotPromptSuggestions(p) {
            this.suggestions = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotPromptSuggestions.prototype.suggestions = $util.emptyArray;

        BotPromptSuggestions.create = function create(properties) {
            return new BotPromptSuggestions(properties);
        };

        BotPromptSuggestions.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.suggestions != null && m.suggestions.length) {
                for (var i = 0; i < m.suggestions.length; ++i)
                    $root.WAAICommon.BotPromptSuggestion.encode(m.suggestions[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotPromptSuggestions.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotPromptSuggestions();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.suggestions && m.suggestions.length))
                            m.suggestions = [];
                        m.suggestions.push($root.WAAICommon.BotPromptSuggestion.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotPromptSuggestions.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotPromptSuggestions)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotPromptSuggestions();
            if (d.suggestions) {
                if (!Array.isArray(d.suggestions))
                    throw TypeError(".WAAICommon.BotPromptSuggestions.suggestions: array expected");
                m.suggestions = [];
                for (var i = 0; i < d.suggestions.length; ++i) {
                    if (typeof d.suggestions[i] !== "object")
                        throw TypeError(".WAAICommon.BotPromptSuggestions.suggestions: object expected");
                    m.suggestions[i] = $root.WAAICommon.BotPromptSuggestion.fromObject(d.suggestions[i], n + 1);
                }
            }
            return m;
        };

        BotPromptSuggestions.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.suggestions = [];
            }
            if (m.suggestions && m.suggestions.length) {
                d.suggestions = [];
                for (var j = 0; j < m.suggestions.length; ++j) {
                    d.suggestions[j] = $root.WAAICommon.BotPromptSuggestion.toObject(m.suggestions[j], o);
                }
            }
            return d;
        };

        BotPromptSuggestions.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromptSuggestions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotPromptSuggestions";
        };

        return BotPromptSuggestions;
    })();

    WAAICommon.BotPromptSuggestion = (function() {

        function BotPromptSuggestion(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotPromptSuggestion.prototype.prompt = null;
        BotPromptSuggestion.prototype.promptID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromptSuggestion.prototype, "_prompt", {
            get: $util.oneOfGetter($oneOfFields = ["prompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotPromptSuggestion.prototype, "_promptID", {
            get: $util.oneOfGetter($oneOfFields = ["promptID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPromptSuggestion.create = function create(properties) {
            return new BotPromptSuggestion(properties);
        };

        BotPromptSuggestion.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.prompt != null && Object.hasOwnProperty.call(m, "prompt"))
                w.uint32(10).string(m.prompt);
            if (m.promptID != null && Object.hasOwnProperty.call(m, "promptID"))
                w.uint32(18).string(m.promptID);
            return w;
        };

        BotPromptSuggestion.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotPromptSuggestion();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.prompt = r.string();
                        break;
                    }
                case 2: {
                        m.promptID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotPromptSuggestion.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotPromptSuggestion)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotPromptSuggestion();
            if (d.prompt != null) {
                m.prompt = String(d.prompt);
            }
            if (d.promptID != null) {
                m.promptID = String(d.promptID);
            }
            return m;
        };

        BotPromptSuggestion.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.prompt != null && m.hasOwnProperty("prompt")) {
                d.prompt = m.prompt;
                if (o.oneofs)
                    d._prompt = "prompt";
            }
            if (m.promptID != null && m.hasOwnProperty("promptID")) {
                d.promptID = m.promptID;
                if (o.oneofs)
                    d._promptID = "promptID";
            }
            return d;
        };

        BotPromptSuggestion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromptSuggestion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotPromptSuggestion";
        };

        return BotPromptSuggestion;
    })();

    WAAICommon.BotLinkedAccountsMetadata = (function() {

        function BotLinkedAccountsMetadata(p) {
            this.accounts = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotLinkedAccountsMetadata.prototype.accounts = $util.emptyArray;
        BotLinkedAccountsMetadata.prototype.acAuthTokens = null;
        BotLinkedAccountsMetadata.prototype.acErrorCode = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acAuthTokens", {
            get: $util.oneOfGetter($oneOfFields = ["acAuthTokens"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acErrorCode", {
            get: $util.oneOfGetter($oneOfFields = ["acErrorCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotLinkedAccountsMetadata.create = function create(properties) {
            return new BotLinkedAccountsMetadata(properties);
        };

        BotLinkedAccountsMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.accounts != null && m.accounts.length) {
                for (var i = 0; i < m.accounts.length; ++i)
                    $root.WAAICommon.BotLinkedAccount.encode(m.accounts[i], w.uint32(10).fork()).ldelim();
            }
            if (m.acAuthTokens != null && Object.hasOwnProperty.call(m, "acAuthTokens"))
                w.uint32(18).bytes(m.acAuthTokens);
            if (m.acErrorCode != null && Object.hasOwnProperty.call(m, "acErrorCode"))
                w.uint32(24).int32(m.acErrorCode);
            return w;
        };

        BotLinkedAccountsMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotLinkedAccountsMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.accounts && m.accounts.length))
                            m.accounts = [];
                        m.accounts.push($root.WAAICommon.BotLinkedAccount.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 2: {
                        m.acAuthTokens = r.bytes();
                        break;
                    }
                case 3: {
                        m.acErrorCode = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotLinkedAccountsMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotLinkedAccountsMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotLinkedAccountsMetadata();
            if (d.accounts) {
                if (!Array.isArray(d.accounts))
                    throw TypeError(".WAAICommon.BotLinkedAccountsMetadata.accounts: array expected");
                m.accounts = [];
                for (var i = 0; i < d.accounts.length; ++i) {
                    if (typeof d.accounts[i] !== "object")
                        throw TypeError(".WAAICommon.BotLinkedAccountsMetadata.accounts: object expected");
                    m.accounts[i] = $root.WAAICommon.BotLinkedAccount.fromObject(d.accounts[i], n + 1);
                }
            }
            if (d.acAuthTokens != null) {
                if (typeof d.acAuthTokens === "string")
                    $util.base64.decode(d.acAuthTokens, m.acAuthTokens = $util.newBuffer($util.base64.length(d.acAuthTokens)), 0);
                else if (d.acAuthTokens.length >= 0)
                    m.acAuthTokens = d.acAuthTokens;
            }
            if (d.acErrorCode != null) {
                m.acErrorCode = d.acErrorCode | 0;
            }
            return m;
        };

        BotLinkedAccountsMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.accounts = [];
            }
            if (m.accounts && m.accounts.length) {
                d.accounts = [];
                for (var j = 0; j < m.accounts.length; ++j) {
                    d.accounts[j] = $root.WAAICommon.BotLinkedAccount.toObject(m.accounts[j], o);
                }
            }
            if (m.acAuthTokens != null && m.hasOwnProperty("acAuthTokens")) {
                d.acAuthTokens = o.bytes === String ? $util.base64.encode(m.acAuthTokens, 0, m.acAuthTokens.length) : o.bytes === Array ? Array.prototype.slice.call(m.acAuthTokens) : m.acAuthTokens;
                if (o.oneofs)
                    d._acAuthTokens = "acAuthTokens";
            }
            if (m.acErrorCode != null && m.hasOwnProperty("acErrorCode")) {
                d.acErrorCode = m.acErrorCode;
                if (o.oneofs)
                    d._acErrorCode = "acErrorCode";
            }
            return d;
        };

        BotLinkedAccountsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotLinkedAccountsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotLinkedAccountsMetadata";
        };

        return BotLinkedAccountsMetadata;
    })();

    WAAICommon.BotMemoryMetadata = (function() {

        function BotMemoryMetadata(p) {
            this.addedFacts = [];
            this.removedFacts = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMemoryMetadata.prototype.addedFacts = $util.emptyArray;
        BotMemoryMetadata.prototype.removedFacts = $util.emptyArray;
        BotMemoryMetadata.prototype.disclaimer = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryMetadata.prototype, "_disclaimer", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMemoryMetadata.create = function create(properties) {
            return new BotMemoryMetadata(properties);
        };

        BotMemoryMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.addedFacts != null && m.addedFacts.length) {
                for (var i = 0; i < m.addedFacts.length; ++i)
                    $root.WAAICommon.BotMemoryFact.encode(m.addedFacts[i], w.uint32(10).fork()).ldelim();
            }
            if (m.removedFacts != null && m.removedFacts.length) {
                for (var i = 0; i < m.removedFacts.length; ++i)
                    $root.WAAICommon.BotMemoryFact.encode(m.removedFacts[i], w.uint32(18).fork()).ldelim();
            }
            if (m.disclaimer != null && Object.hasOwnProperty.call(m, "disclaimer"))
                w.uint32(26).string(m.disclaimer);
            return w;
        };

        BotMemoryMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMemoryMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.addedFacts && m.addedFacts.length))
                            m.addedFacts = [];
                        m.addedFacts.push($root.WAAICommon.BotMemoryFact.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 2: {
                        if (!(m.removedFacts && m.removedFacts.length))
                            m.removedFacts = [];
                        m.removedFacts.push($root.WAAICommon.BotMemoryFact.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 3: {
                        m.disclaimer = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMemoryMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMemoryMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMemoryMetadata();
            if (d.addedFacts) {
                if (!Array.isArray(d.addedFacts))
                    throw TypeError(".WAAICommon.BotMemoryMetadata.addedFacts: array expected");
                m.addedFacts = [];
                for (var i = 0; i < d.addedFacts.length; ++i) {
                    if (typeof d.addedFacts[i] !== "object")
                        throw TypeError(".WAAICommon.BotMemoryMetadata.addedFacts: object expected");
                    m.addedFacts[i] = $root.WAAICommon.BotMemoryFact.fromObject(d.addedFacts[i], n + 1);
                }
            }
            if (d.removedFacts) {
                if (!Array.isArray(d.removedFacts))
                    throw TypeError(".WAAICommon.BotMemoryMetadata.removedFacts: array expected");
                m.removedFacts = [];
                for (var i = 0; i < d.removedFacts.length; ++i) {
                    if (typeof d.removedFacts[i] !== "object")
                        throw TypeError(".WAAICommon.BotMemoryMetadata.removedFacts: object expected");
                    m.removedFacts[i] = $root.WAAICommon.BotMemoryFact.fromObject(d.removedFacts[i], n + 1);
                }
            }
            if (d.disclaimer != null) {
                m.disclaimer = String(d.disclaimer);
            }
            return m;
        };

        BotMemoryMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.addedFacts = [];
                d.removedFacts = [];
            }
            if (m.addedFacts && m.addedFacts.length) {
                d.addedFacts = [];
                for (var j = 0; j < m.addedFacts.length; ++j) {
                    d.addedFacts[j] = $root.WAAICommon.BotMemoryFact.toObject(m.addedFacts[j], o);
                }
            }
            if (m.removedFacts && m.removedFacts.length) {
                d.removedFacts = [];
                for (var j = 0; j < m.removedFacts.length; ++j) {
                    d.removedFacts[j] = $root.WAAICommon.BotMemoryFact.toObject(m.removedFacts[j], o);
                }
            }
            if (m.disclaimer != null && m.hasOwnProperty("disclaimer")) {
                d.disclaimer = m.disclaimer;
                if (o.oneofs)
                    d._disclaimer = "disclaimer";
            }
            return d;
        };

        BotMemoryMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemoryMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMemoryMetadata";
        };

        return BotMemoryMetadata;
    })();

    WAAICommon.BotMemoryFact = (function() {

        function BotMemoryFact(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMemoryFact.prototype.fact = null;
        BotMemoryFact.prototype.factID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryFact.prototype, "_fact", {
            get: $util.oneOfGetter($oneOfFields = ["fact"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMemoryFact.prototype, "_factID", {
            get: $util.oneOfGetter($oneOfFields = ["factID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMemoryFact.create = function create(properties) {
            return new BotMemoryFact(properties);
        };

        BotMemoryFact.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.fact != null && Object.hasOwnProperty.call(m, "fact"))
                w.uint32(10).string(m.fact);
            if (m.factID != null && Object.hasOwnProperty.call(m, "factID"))
                w.uint32(18).string(m.factID);
            return w;
        };

        BotMemoryFact.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMemoryFact();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.fact = r.string();
                        break;
                    }
                case 2: {
                        m.factID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMemoryFact.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMemoryFact)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMemoryFact();
            if (d.fact != null) {
                m.fact = String(d.fact);
            }
            if (d.factID != null) {
                m.factID = String(d.factID);
            }
            return m;
        };

        BotMemoryFact.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.fact != null && m.hasOwnProperty("fact")) {
                d.fact = m.fact;
                if (o.oneofs)
                    d._fact = "fact";
            }
            if (m.factID != null && m.hasOwnProperty("factID")) {
                d.factID = m.factID;
                if (o.oneofs)
                    d._factID = "factID";
            }
            return d;
        };

        BotMemoryFact.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemoryFact.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMemoryFact";
        };

        return BotMemoryFact;
    })();

    WAAICommon.BotSignatureVerificationMetadata = (function() {

        function BotSignatureVerificationMetadata(p) {
            this.proofs = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotSignatureVerificationMetadata.prototype.proofs = $util.emptyArray;

        BotSignatureVerificationMetadata.create = function create(properties) {
            return new BotSignatureVerificationMetadata(properties);
        };

        BotSignatureVerificationMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.proofs != null && m.proofs.length) {
                for (var i = 0; i < m.proofs.length; ++i)
                    $root.WAAICommon.BotSignatureVerificationUseCaseProof.encode(m.proofs[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotSignatureVerificationMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSignatureVerificationMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.proofs && m.proofs.length))
                            m.proofs = [];
                        m.proofs.push($root.WAAICommon.BotSignatureVerificationUseCaseProof.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotSignatureVerificationMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotSignatureVerificationMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotSignatureVerificationMetadata();
            if (d.proofs) {
                if (!Array.isArray(d.proofs))
                    throw TypeError(".WAAICommon.BotSignatureVerificationMetadata.proofs: array expected");
                m.proofs = [];
                for (var i = 0; i < d.proofs.length; ++i) {
                    if (typeof d.proofs[i] !== "object")
                        throw TypeError(".WAAICommon.BotSignatureVerificationMetadata.proofs: object expected");
                    m.proofs[i] = $root.WAAICommon.BotSignatureVerificationUseCaseProof.fromObject(d.proofs[i], n + 1);
                }
            }
            return m;
        };

        BotSignatureVerificationMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.proofs = [];
            }
            if (m.proofs && m.proofs.length) {
                d.proofs = [];
                for (var j = 0; j < m.proofs.length; ++j) {
                    d.proofs[j] = $root.WAAICommon.BotSignatureVerificationUseCaseProof.toObject(m.proofs[j], o);
                }
            }
            return d;
        };

        BotSignatureVerificationMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSignatureVerificationMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotSignatureVerificationMetadata";
        };

        return BotSignatureVerificationMetadata;
    })();

    WAAICommon.BotRenderingMetadata = (function() {

        function BotRenderingMetadata(p) {
            this.keywords = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotRenderingMetadata.prototype.keywords = $util.emptyArray;

        BotRenderingMetadata.create = function create(properties) {
            return new BotRenderingMetadata(properties);
        };

        BotRenderingMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.keywords != null && m.keywords.length) {
                for (var i = 0; i < m.keywords.length; ++i)
                    $root.WAAICommon.BotRenderingMetadata.Keyword.encode(m.keywords[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotRenderingMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotRenderingMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.keywords && m.keywords.length))
                            m.keywords = [];
                        m.keywords.push($root.WAAICommon.BotRenderingMetadata.Keyword.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotRenderingMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotRenderingMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotRenderingMetadata();
            if (d.keywords) {
                if (!Array.isArray(d.keywords))
                    throw TypeError(".WAAICommon.BotRenderingMetadata.keywords: array expected");
                m.keywords = [];
                for (var i = 0; i < d.keywords.length; ++i) {
                    if (typeof d.keywords[i] !== "object")
                        throw TypeError(".WAAICommon.BotRenderingMetadata.keywords: object expected");
                    m.keywords[i] = $root.WAAICommon.BotRenderingMetadata.Keyword.fromObject(d.keywords[i], n + 1);
                }
            }
            return m;
        };

        BotRenderingMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.keywords = [];
            }
            if (m.keywords && m.keywords.length) {
                d.keywords = [];
                for (var j = 0; j < m.keywords.length; ++j) {
                    d.keywords[j] = $root.WAAICommon.BotRenderingMetadata.Keyword.toObject(m.keywords[j], o);
                }
            }
            return d;
        };

        BotRenderingMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotRenderingMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotRenderingMetadata";
        };

        BotRenderingMetadata.Keyword = (function() {

            function Keyword(p) {
                this.associatedPrompts = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Keyword.prototype.value = null;
            Keyword.prototype.associatedPrompts = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Keyword.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Keyword.create = function create(properties) {
                return new Keyword(properties);
            };

            Keyword.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.value != null && Object.hasOwnProperty.call(m, "value"))
                    w.uint32(10).string(m.value);
                if (m.associatedPrompts != null && m.associatedPrompts.length) {
                    for (var i = 0; i < m.associatedPrompts.length; ++i)
                        w.uint32(18).string(m.associatedPrompts[i]);
                }
                return w;
            };

            Keyword.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotRenderingMetadata.Keyword();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.value = r.string();
                            break;
                        }
                    case 2: {
                            if (!(m.associatedPrompts && m.associatedPrompts.length))
                                m.associatedPrompts = [];
                            m.associatedPrompts.push(r.string());
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Keyword.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotRenderingMetadata.Keyword)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotRenderingMetadata.Keyword();
                if (d.value != null) {
                    m.value = String(d.value);
                }
                if (d.associatedPrompts) {
                    if (!Array.isArray(d.associatedPrompts))
                        throw TypeError(".WAAICommon.BotRenderingMetadata.Keyword.associatedPrompts: array expected");
                    m.associatedPrompts = [];
                    for (var i = 0; i < d.associatedPrompts.length; ++i) {
                        m.associatedPrompts[i] = String(d.associatedPrompts[i]);
                    }
                }
                return m;
            };

            Keyword.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.associatedPrompts = [];
                }
                if (m.value != null && m.hasOwnProperty("value")) {
                    d.value = m.value;
                    if (o.oneofs)
                        d._value = "value";
                }
                if (m.associatedPrompts && m.associatedPrompts.length) {
                    d.associatedPrompts = [];
                    for (var j = 0; j < m.associatedPrompts.length; ++j) {
                        d.associatedPrompts[j] = m.associatedPrompts[j];
                    }
                }
                return d;
            };

            Keyword.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Keyword.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotRenderingMetadata.Keyword";
            };

            return Keyword;
        })();

        return BotRenderingMetadata;
    })();

    WAAICommon.BotMetricsMetadata = (function() {

        function BotMetricsMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMetricsMetadata.prototype.destinationID = null;
        BotMetricsMetadata.prototype.destinationEntryPoint = null;
        BotMetricsMetadata.prototype.threadOrigin = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_destinationID", {
            get: $util.oneOfGetter($oneOfFields = ["destinationID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_destinationEntryPoint", {
            get: $util.oneOfGetter($oneOfFields = ["destinationEntryPoint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetricsMetadata.prototype, "_threadOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["threadOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMetricsMetadata.create = function create(properties) {
            return new BotMetricsMetadata(properties);
        };

        BotMetricsMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.destinationID != null && Object.hasOwnProperty.call(m, "destinationID"))
                w.uint32(10).string(m.destinationID);
            if (m.destinationEntryPoint != null && Object.hasOwnProperty.call(m, "destinationEntryPoint"))
                w.uint32(16).int32(m.destinationEntryPoint);
            if (m.threadOrigin != null && Object.hasOwnProperty.call(m, "threadOrigin"))
                w.uint32(24).int32(m.threadOrigin);
            return w;
        };

        BotMetricsMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMetricsMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.destinationID = r.string();
                        break;
                    }
                case 2: {
                        m.destinationEntryPoint = r.int32();
                        break;
                    }
                case 3: {
                        m.threadOrigin = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMetricsMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMetricsMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMetricsMetadata();
            if (d.destinationID != null) {
                m.destinationID = String(d.destinationID);
            }
            switch (d.destinationEntryPoint) {
            default:
                if (typeof d.destinationEntryPoint === "number") {
                    m.destinationEntryPoint = d.destinationEntryPoint;
                    break;
                }
                break;
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                m.destinationEntryPoint = 0;
                break;
            case "FAVICON":
            case 1:
                m.destinationEntryPoint = 1;
                break;
            case "CHATLIST":
            case 2:
                m.destinationEntryPoint = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                m.destinationEntryPoint = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                m.destinationEntryPoint = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                m.destinationEntryPoint = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                m.destinationEntryPoint = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                m.destinationEntryPoint = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                m.destinationEntryPoint = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                m.destinationEntryPoint = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                m.destinationEntryPoint = 10;
                break;
            case "AISTUDIO":
            case 11:
                m.destinationEntryPoint = 11;
                break;
            case "DEEPLINK":
            case 12:
                m.destinationEntryPoint = 12;
                break;
            case "NOTIFICATION":
            case 13:
                m.destinationEntryPoint = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                m.destinationEntryPoint = 14;
                break;
            case "FORWARD":
            case 15:
                m.destinationEntryPoint = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                m.destinationEntryPoint = 16;
                break;
            case "FF_FAMILY":
            case 17:
                m.destinationEntryPoint = 17;
                break;
            case "AI_TAB":
            case 18:
                m.destinationEntryPoint = 18;
                break;
            case "AI_HOME":
            case 19:
                m.destinationEntryPoint = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                m.destinationEntryPoint = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                m.destinationEntryPoint = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                m.destinationEntryPoint = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                m.destinationEntryPoint = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                m.destinationEntryPoint = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                m.destinationEntryPoint = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                m.destinationEntryPoint = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                m.destinationEntryPoint = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                m.destinationEntryPoint = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                m.destinationEntryPoint = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                m.destinationEntryPoint = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                m.destinationEntryPoint = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                m.destinationEntryPoint = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                m.destinationEntryPoint = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                m.destinationEntryPoint = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                m.destinationEntryPoint = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                m.destinationEntryPoint = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                m.destinationEntryPoint = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                m.destinationEntryPoint = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                m.destinationEntryPoint = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                m.destinationEntryPoint = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                m.destinationEntryPoint = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                m.destinationEntryPoint = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                m.destinationEntryPoint = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                m.destinationEntryPoint = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                m.destinationEntryPoint = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                m.destinationEntryPoint = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                m.destinationEntryPoint = 56;
                break;
            }
            switch (d.threadOrigin) {
            default:
                if (typeof d.threadOrigin === "number") {
                    m.threadOrigin = d.threadOrigin;
                    break;
                }
                break;
            case "AI_TAB_THREAD":
            case 1:
                m.threadOrigin = 1;
                break;
            case "AI_HOME_THREAD":
            case 2:
                m.threadOrigin = 2;
                break;
            case "AI_DEEPLINK_IMMERSIVE_THREAD":
            case 3:
                m.threadOrigin = 3;
                break;
            case "AI_DEEPLINK_THREAD":
            case 4:
                m.threadOrigin = 4;
                break;
            case "ASK_META_AI_CONTEXT_MENU_THREAD":
            case 5:
                m.threadOrigin = 5;
                break;
            }
            return m;
        };

        BotMetricsMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.destinationID != null && m.hasOwnProperty("destinationID")) {
                d.destinationID = m.destinationID;
                if (o.oneofs)
                    d._destinationID = "destinationID";
            }
            if (m.destinationEntryPoint != null && m.hasOwnProperty("destinationEntryPoint")) {
                d.destinationEntryPoint = o.enums === String ? $root.WAAICommon.BotMetricsEntryPoint[m.destinationEntryPoint] === undefined ? m.destinationEntryPoint : $root.WAAICommon.BotMetricsEntryPoint[m.destinationEntryPoint] : m.destinationEntryPoint;
                if (o.oneofs)
                    d._destinationEntryPoint = "destinationEntryPoint";
            }
            if (m.threadOrigin != null && m.hasOwnProperty("threadOrigin")) {
                d.threadOrigin = o.enums === String ? $root.WAAICommon.BotMetricsThreadEntryPoint[m.threadOrigin] === undefined ? m.threadOrigin : $root.WAAICommon.BotMetricsThreadEntryPoint[m.threadOrigin] : m.threadOrigin;
                if (o.oneofs)
                    d._threadOrigin = "threadOrigin";
            }
            return d;
        };

        BotMetricsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMetricsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMetricsMetadata";
        };

        return BotMetricsMetadata;
    })();

    WAAICommon.BotSessionMetadata = (function() {

        function BotSessionMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotSessionMetadata.prototype.sessionID = null;
        BotSessionMetadata.prototype.sessionSource = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSessionMetadata.prototype, "_sessionID", {
            get: $util.oneOfGetter($oneOfFields = ["sessionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotSessionMetadata.prototype, "_sessionSource", {
            get: $util.oneOfGetter($oneOfFields = ["sessionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSessionMetadata.create = function create(properties) {
            return new BotSessionMetadata(properties);
        };

        BotSessionMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.sessionID != null && Object.hasOwnProperty.call(m, "sessionID"))
                w.uint32(10).string(m.sessionID);
            if (m.sessionSource != null && Object.hasOwnProperty.call(m, "sessionSource"))
                w.uint32(16).int32(m.sessionSource);
            return w;
        };

        BotSessionMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotSessionMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.sessionID = r.string();
                        break;
                    }
                case 2: {
                        m.sessionSource = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotSessionMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotSessionMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotSessionMetadata();
            if (d.sessionID != null) {
                m.sessionID = String(d.sessionID);
            }
            switch (d.sessionSource) {
            default:
                if (typeof d.sessionSource === "number") {
                    m.sessionSource = d.sessionSource;
                    break;
                }
                break;
            case "NONE":
            case 0:
                m.sessionSource = 0;
                break;
            case "NULL_STATE":
            case 1:
                m.sessionSource = 1;
                break;
            case "TYPEAHEAD":
            case 2:
                m.sessionSource = 2;
                break;
            case "USER_INPUT":
            case 3:
                m.sessionSource = 3;
                break;
            case "EMU_FLASH":
            case 4:
                m.sessionSource = 4;
                break;
            case "EMU_FLASH_FOLLOWUP":
            case 5:
                m.sessionSource = 5;
                break;
            case "VOICE":
            case 6:
                m.sessionSource = 6;
                break;
            case "AI_HOME_SESSION":
            case 7:
                m.sessionSource = 7;
                break;
            }
            return m;
        };

        BotSessionMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.sessionID != null && m.hasOwnProperty("sessionID")) {
                d.sessionID = m.sessionID;
                if (o.oneofs)
                    d._sessionID = "sessionID";
            }
            if (m.sessionSource != null && m.hasOwnProperty("sessionSource")) {
                d.sessionSource = o.enums === String ? $root.WAAICommon.BotSessionSource[m.sessionSource] === undefined ? m.sessionSource : $root.WAAICommon.BotSessionSource[m.sessionSource] : m.sessionSource;
                if (o.oneofs)
                    d._sessionSource = "sessionSource";
            }
            return d;
        };

        BotSessionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSessionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotSessionMetadata";
        };

        return BotSessionMetadata;
    })();

    WAAICommon.BotMemuMetadata = (function() {

        function BotMemuMetadata(p) {
            this.faceImages = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMemuMetadata.prototype.faceImages = $util.emptyArray;

        BotMemuMetadata.create = function create(properties) {
            return new BotMemuMetadata(properties);
        };

        BotMemuMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.faceImages != null && m.faceImages.length) {
                for (var i = 0; i < m.faceImages.length; ++i)
                    $root.WAAICommon.BotMediaMetadata.encode(m.faceImages[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotMemuMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMemuMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.faceImages && m.faceImages.length))
                            m.faceImages = [];
                        m.faceImages.push($root.WAAICommon.BotMediaMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMemuMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMemuMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMemuMetadata();
            if (d.faceImages) {
                if (!Array.isArray(d.faceImages))
                    throw TypeError(".WAAICommon.BotMemuMetadata.faceImages: array expected");
                m.faceImages = [];
                for (var i = 0; i < d.faceImages.length; ++i) {
                    if (typeof d.faceImages[i] !== "object")
                        throw TypeError(".WAAICommon.BotMemuMetadata.faceImages: object expected");
                    m.faceImages[i] = $root.WAAICommon.BotMediaMetadata.fromObject(d.faceImages[i], n + 1);
                }
            }
            return m;
        };

        BotMemuMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.faceImages = [];
            }
            if (m.faceImages && m.faceImages.length) {
                d.faceImages = [];
                for (var j = 0; j < m.faceImages.length; ++j) {
                    d.faceImages[j] = $root.WAAICommon.BotMediaMetadata.toObject(m.faceImages[j], o);
                }
            }
            return d;
        };

        BotMemuMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemuMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMemuMetadata";
        };

        return BotMemuMetadata;
    })();

    WAAICommon.InThreadSurveyMetadata = (function() {

        function InThreadSurveyMetadata(p) {
            this.questions = [];
            this.privacyStatementParts = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        InThreadSurveyMetadata.prototype.tessaSessionID = null;
        InThreadSurveyMetadata.prototype.simonSessionID = null;
        InThreadSurveyMetadata.prototype.simonSurveyID = null;
        InThreadSurveyMetadata.prototype.tessaRootID = null;
        InThreadSurveyMetadata.prototype.requestID = null;
        InThreadSurveyMetadata.prototype.tessaEvent = null;
        InThreadSurveyMetadata.prototype.invitationHeaderText = null;
        InThreadSurveyMetadata.prototype.invitationBodyText = null;
        InThreadSurveyMetadata.prototype.invitationCtaText = null;
        InThreadSurveyMetadata.prototype.invitationCtaURL = null;
        InThreadSurveyMetadata.prototype.surveyTitle = null;
        InThreadSurveyMetadata.prototype.questions = $util.emptyArray;
        InThreadSurveyMetadata.prototype.surveyContinueButtonText = null;
        InThreadSurveyMetadata.prototype.surveySubmitButtonText = null;
        InThreadSurveyMetadata.prototype.privacyStatementFull = null;
        InThreadSurveyMetadata.prototype.privacyStatementParts = $util.emptyArray;
        InThreadSurveyMetadata.prototype.feedbackToastText = null;
        InThreadSurveyMetadata.prototype.startQuestionIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaSessionID", {
            get: $util.oneOfGetter($oneOfFields = ["tessaSessionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSessionID", {
            get: $util.oneOfGetter($oneOfFields = ["simonSessionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSurveyID", {
            get: $util.oneOfGetter($oneOfFields = ["simonSurveyID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaRootID", {
            get: $util.oneOfGetter($oneOfFields = ["tessaRootID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_requestID", {
            get: $util.oneOfGetter($oneOfFields = ["requestID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaEvent", {
            get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationHeaderText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationHeaderText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationBodyText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationBodyText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaURL", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyTitle", {
            get: $util.oneOfGetter($oneOfFields = ["surveyTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyContinueButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveyContinueButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveySubmitButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveySubmitButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_privacyStatementFull", {
            get: $util.oneOfGetter($oneOfFields = ["privacyStatementFull"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_feedbackToastText", {
            get: $util.oneOfGetter($oneOfFields = ["feedbackToastText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(InThreadSurveyMetadata.prototype, "_startQuestionIndex", {
            get: $util.oneOfGetter($oneOfFields = ["startQuestionIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InThreadSurveyMetadata.create = function create(properties) {
            return new InThreadSurveyMetadata(properties);
        };

        InThreadSurveyMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.tessaSessionID != null && Object.hasOwnProperty.call(m, "tessaSessionID"))
                w.uint32(10).string(m.tessaSessionID);
            if (m.simonSessionID != null && Object.hasOwnProperty.call(m, "simonSessionID"))
                w.uint32(18).string(m.simonSessionID);
            if (m.simonSurveyID != null && Object.hasOwnProperty.call(m, "simonSurveyID"))
                w.uint32(26).string(m.simonSurveyID);
            if (m.tessaRootID != null && Object.hasOwnProperty.call(m, "tessaRootID"))
                w.uint32(34).string(m.tessaRootID);
            if (m.requestID != null && Object.hasOwnProperty.call(m, "requestID"))
                w.uint32(42).string(m.requestID);
            if (m.tessaEvent != null && Object.hasOwnProperty.call(m, "tessaEvent"))
                w.uint32(50).string(m.tessaEvent);
            if (m.invitationHeaderText != null && Object.hasOwnProperty.call(m, "invitationHeaderText"))
                w.uint32(58).string(m.invitationHeaderText);
            if (m.invitationBodyText != null && Object.hasOwnProperty.call(m, "invitationBodyText"))
                w.uint32(66).string(m.invitationBodyText);
            if (m.invitationCtaText != null && Object.hasOwnProperty.call(m, "invitationCtaText"))
                w.uint32(74).string(m.invitationCtaText);
            if (m.invitationCtaURL != null && Object.hasOwnProperty.call(m, "invitationCtaURL"))
                w.uint32(82).string(m.invitationCtaURL);
            if (m.surveyTitle != null && Object.hasOwnProperty.call(m, "surveyTitle"))
                w.uint32(90).string(m.surveyTitle);
            if (m.questions != null && m.questions.length) {
                for (var i = 0; i < m.questions.length; ++i)
                    $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.encode(m.questions[i], w.uint32(98).fork()).ldelim();
            }
            if (m.surveyContinueButtonText != null && Object.hasOwnProperty.call(m, "surveyContinueButtonText"))
                w.uint32(106).string(m.surveyContinueButtonText);
            if (m.surveySubmitButtonText != null && Object.hasOwnProperty.call(m, "surveySubmitButtonText"))
                w.uint32(114).string(m.surveySubmitButtonText);
            if (m.privacyStatementFull != null && Object.hasOwnProperty.call(m, "privacyStatementFull"))
                w.uint32(122).string(m.privacyStatementFull);
            if (m.privacyStatementParts != null && m.privacyStatementParts.length) {
                for (var i = 0; i < m.privacyStatementParts.length; ++i)
                    $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.encode(m.privacyStatementParts[i], w.uint32(130).fork()).ldelim();
            }
            if (m.feedbackToastText != null && Object.hasOwnProperty.call(m, "feedbackToastText"))
                w.uint32(138).string(m.feedbackToastText);
            if (m.startQuestionIndex != null && Object.hasOwnProperty.call(m, "startQuestionIndex"))
                w.uint32(144).int32(m.startQuestionIndex);
            return w;
        };

        InThreadSurveyMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.InThreadSurveyMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.tessaSessionID = r.string();
                        break;
                    }
                case 2: {
                        m.simonSessionID = r.string();
                        break;
                    }
                case 3: {
                        m.simonSurveyID = r.string();
                        break;
                    }
                case 4: {
                        m.tessaRootID = r.string();
                        break;
                    }
                case 5: {
                        m.requestID = r.string();
                        break;
                    }
                case 6: {
                        m.tessaEvent = r.string();
                        break;
                    }
                case 7: {
                        m.invitationHeaderText = r.string();
                        break;
                    }
                case 8: {
                        m.invitationBodyText = r.string();
                        break;
                    }
                case 9: {
                        m.invitationCtaText = r.string();
                        break;
                    }
                case 10: {
                        m.invitationCtaURL = r.string();
                        break;
                    }
                case 11: {
                        m.surveyTitle = r.string();
                        break;
                    }
                case 12: {
                        if (!(m.questions && m.questions.length))
                            m.questions = [];
                        m.questions.push($root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 13: {
                        m.surveyContinueButtonText = r.string();
                        break;
                    }
                case 14: {
                        m.surveySubmitButtonText = r.string();
                        break;
                    }
                case 15: {
                        m.privacyStatementFull = r.string();
                        break;
                    }
                case 16: {
                        if (!(m.privacyStatementParts && m.privacyStatementParts.length))
                            m.privacyStatementParts = [];
                        m.privacyStatementParts.push($root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 17: {
                        m.feedbackToastText = r.string();
                        break;
                    }
                case 18: {
                        m.startQuestionIndex = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        InThreadSurveyMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.InThreadSurveyMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.InThreadSurveyMetadata();
            if (d.tessaSessionID != null) {
                m.tessaSessionID = String(d.tessaSessionID);
            }
            if (d.simonSessionID != null) {
                m.simonSessionID = String(d.simonSessionID);
            }
            if (d.simonSurveyID != null) {
                m.simonSurveyID = String(d.simonSurveyID);
            }
            if (d.tessaRootID != null) {
                m.tessaRootID = String(d.tessaRootID);
            }
            if (d.requestID != null) {
                m.requestID = String(d.requestID);
            }
            if (d.tessaEvent != null) {
                m.tessaEvent = String(d.tessaEvent);
            }
            if (d.invitationHeaderText != null) {
                m.invitationHeaderText = String(d.invitationHeaderText);
            }
            if (d.invitationBodyText != null) {
                m.invitationBodyText = String(d.invitationBodyText);
            }
            if (d.invitationCtaText != null) {
                m.invitationCtaText = String(d.invitationCtaText);
            }
            if (d.invitationCtaURL != null) {
                m.invitationCtaURL = String(d.invitationCtaURL);
            }
            if (d.surveyTitle != null) {
                m.surveyTitle = String(d.surveyTitle);
            }
            if (d.questions) {
                if (!Array.isArray(d.questions))
                    throw TypeError(".WAAICommon.InThreadSurveyMetadata.questions: array expected");
                m.questions = [];
                for (var i = 0; i < d.questions.length; ++i) {
                    if (typeof d.questions[i] !== "object")
                        throw TypeError(".WAAICommon.InThreadSurveyMetadata.questions: object expected");
                    m.questions[i] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.fromObject(d.questions[i], n + 1);
                }
            }
            if (d.surveyContinueButtonText != null) {
                m.surveyContinueButtonText = String(d.surveyContinueButtonText);
            }
            if (d.surveySubmitButtonText != null) {
                m.surveySubmitButtonText = String(d.surveySubmitButtonText);
            }
            if (d.privacyStatementFull != null) {
                m.privacyStatementFull = String(d.privacyStatementFull);
            }
            if (d.privacyStatementParts) {
                if (!Array.isArray(d.privacyStatementParts))
                    throw TypeError(".WAAICommon.InThreadSurveyMetadata.privacyStatementParts: array expected");
                m.privacyStatementParts = [];
                for (var i = 0; i < d.privacyStatementParts.length; ++i) {
                    if (typeof d.privacyStatementParts[i] !== "object")
                        throw TypeError(".WAAICommon.InThreadSurveyMetadata.privacyStatementParts: object expected");
                    m.privacyStatementParts[i] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.fromObject(d.privacyStatementParts[i], n + 1);
                }
            }
            if (d.feedbackToastText != null) {
                m.feedbackToastText = String(d.feedbackToastText);
            }
            if (d.startQuestionIndex != null) {
                m.startQuestionIndex = d.startQuestionIndex | 0;
            }
            return m;
        };

        InThreadSurveyMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.questions = [];
                d.privacyStatementParts = [];
            }
            if (m.tessaSessionID != null && m.hasOwnProperty("tessaSessionID")) {
                d.tessaSessionID = m.tessaSessionID;
                if (o.oneofs)
                    d._tessaSessionID = "tessaSessionID";
            }
            if (m.simonSessionID != null && m.hasOwnProperty("simonSessionID")) {
                d.simonSessionID = m.simonSessionID;
                if (o.oneofs)
                    d._simonSessionID = "simonSessionID";
            }
            if (m.simonSurveyID != null && m.hasOwnProperty("simonSurveyID")) {
                d.simonSurveyID = m.simonSurveyID;
                if (o.oneofs)
                    d._simonSurveyID = "simonSurveyID";
            }
            if (m.tessaRootID != null && m.hasOwnProperty("tessaRootID")) {
                d.tessaRootID = m.tessaRootID;
                if (o.oneofs)
                    d._tessaRootID = "tessaRootID";
            }
            if (m.requestID != null && m.hasOwnProperty("requestID")) {
                d.requestID = m.requestID;
                if (o.oneofs)
                    d._requestID = "requestID";
            }
            if (m.tessaEvent != null && m.hasOwnProperty("tessaEvent")) {
                d.tessaEvent = m.tessaEvent;
                if (o.oneofs)
                    d._tessaEvent = "tessaEvent";
            }
            if (m.invitationHeaderText != null && m.hasOwnProperty("invitationHeaderText")) {
                d.invitationHeaderText = m.invitationHeaderText;
                if (o.oneofs)
                    d._invitationHeaderText = "invitationHeaderText";
            }
            if (m.invitationBodyText != null && m.hasOwnProperty("invitationBodyText")) {
                d.invitationBodyText = m.invitationBodyText;
                if (o.oneofs)
                    d._invitationBodyText = "invitationBodyText";
            }
            if (m.invitationCtaText != null && m.hasOwnProperty("invitationCtaText")) {
                d.invitationCtaText = m.invitationCtaText;
                if (o.oneofs)
                    d._invitationCtaText = "invitationCtaText";
            }
            if (m.invitationCtaURL != null && m.hasOwnProperty("invitationCtaURL")) {
                d.invitationCtaURL = m.invitationCtaURL;
                if (o.oneofs)
                    d._invitationCtaURL = "invitationCtaURL";
            }
            if (m.surveyTitle != null && m.hasOwnProperty("surveyTitle")) {
                d.surveyTitle = m.surveyTitle;
                if (o.oneofs)
                    d._surveyTitle = "surveyTitle";
            }
            if (m.questions && m.questions.length) {
                d.questions = [];
                for (var j = 0; j < m.questions.length; ++j) {
                    d.questions[j] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.toObject(m.questions[j], o);
                }
            }
            if (m.surveyContinueButtonText != null && m.hasOwnProperty("surveyContinueButtonText")) {
                d.surveyContinueButtonText = m.surveyContinueButtonText;
                if (o.oneofs)
                    d._surveyContinueButtonText = "surveyContinueButtonText";
            }
            if (m.surveySubmitButtonText != null && m.hasOwnProperty("surveySubmitButtonText")) {
                d.surveySubmitButtonText = m.surveySubmitButtonText;
                if (o.oneofs)
                    d._surveySubmitButtonText = "surveySubmitButtonText";
            }
            if (m.privacyStatementFull != null && m.hasOwnProperty("privacyStatementFull")) {
                d.privacyStatementFull = m.privacyStatementFull;
                if (o.oneofs)
                    d._privacyStatementFull = "privacyStatementFull";
            }
            if (m.privacyStatementParts && m.privacyStatementParts.length) {
                d.privacyStatementParts = [];
                for (var j = 0; j < m.privacyStatementParts.length; ++j) {
                    d.privacyStatementParts[j] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.toObject(m.privacyStatementParts[j], o);
                }
            }
            if (m.feedbackToastText != null && m.hasOwnProperty("feedbackToastText")) {
                d.feedbackToastText = m.feedbackToastText;
                if (o.oneofs)
                    d._feedbackToastText = "feedbackToastText";
            }
            if (m.startQuestionIndex != null && m.hasOwnProperty("startQuestionIndex")) {
                d.startQuestionIndex = m.startQuestionIndex;
                if (o.oneofs)
                    d._startQuestionIndex = "startQuestionIndex";
            }
            return d;
        };

        InThreadSurveyMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        InThreadSurveyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.InThreadSurveyMetadata";
        };

        InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart = (function() {

            function InThreadSurveyPrivacyStatementPart(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            InThreadSurveyPrivacyStatementPart.prototype.text = null;
            InThreadSurveyPrivacyStatementPart.prototype.URL = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyPrivacyStatementPart.create = function create(properties) {
                return new InThreadSurveyPrivacyStatementPart(properties);
            };

            InThreadSurveyPrivacyStatementPart.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    w.uint32(18).string(m.URL);
                return w;
            };

            InThreadSurveyPrivacyStatementPart.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.text = r.string();
                            break;
                        }
                    case 2: {
                            m.URL = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            InThreadSurveyPrivacyStatementPart.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart();
                if (d.text != null) {
                    m.text = String(d.text);
                }
                if (d.URL != null) {
                    m.URL = String(d.URL);
                }
                return m;
            };

            InThreadSurveyPrivacyStatementPart.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.text != null && m.hasOwnProperty("text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d._text = "text";
                }
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = m.URL;
                    if (o.oneofs)
                        d._URL = "URL";
                }
                return d;
            };

            InThreadSurveyPrivacyStatementPart.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyPrivacyStatementPart.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart";
            };

            return InThreadSurveyPrivacyStatementPart;
        })();

        InThreadSurveyMetadata.InThreadSurveyOption = (function() {

            function InThreadSurveyOption(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            InThreadSurveyOption.prototype.stringValue = null;
            InThreadSurveyOption.prototype.numericValue = null;
            InThreadSurveyOption.prototype.textTranslated = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_stringValue", {
                get: $util.oneOfGetter($oneOfFields = ["stringValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_numericValue", {
                get: $util.oneOfGetter($oneOfFields = ["numericValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyOption.prototype, "_textTranslated", {
                get: $util.oneOfGetter($oneOfFields = ["textTranslated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyOption.create = function create(properties) {
                return new InThreadSurveyOption(properties);
            };

            InThreadSurveyOption.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.stringValue != null && Object.hasOwnProperty.call(m, "stringValue"))
                    w.uint32(10).string(m.stringValue);
                if (m.numericValue != null && Object.hasOwnProperty.call(m, "numericValue"))
                    w.uint32(16).uint32(m.numericValue);
                if (m.textTranslated != null && Object.hasOwnProperty.call(m, "textTranslated"))
                    w.uint32(26).string(m.textTranslated);
                return w;
            };

            InThreadSurveyOption.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.stringValue = r.string();
                            break;
                        }
                    case 2: {
                            m.numericValue = r.uint32();
                            break;
                        }
                    case 3: {
                            m.textTranslated = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            InThreadSurveyOption.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption();
                if (d.stringValue != null) {
                    m.stringValue = String(d.stringValue);
                }
                if (d.numericValue != null) {
                    m.numericValue = d.numericValue >>> 0;
                }
                if (d.textTranslated != null) {
                    m.textTranslated = String(d.textTranslated);
                }
                return m;
            };

            InThreadSurveyOption.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.stringValue != null && m.hasOwnProperty("stringValue")) {
                    d.stringValue = m.stringValue;
                    if (o.oneofs)
                        d._stringValue = "stringValue";
                }
                if (m.numericValue != null && m.hasOwnProperty("numericValue")) {
                    d.numericValue = m.numericValue;
                    if (o.oneofs)
                        d._numericValue = "numericValue";
                }
                if (m.textTranslated != null && m.hasOwnProperty("textTranslated")) {
                    d.textTranslated = m.textTranslated;
                    if (o.oneofs)
                        d._textTranslated = "textTranslated";
                }
                return d;
            };

            InThreadSurveyOption.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyOption.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption";
            };

            return InThreadSurveyOption;
        })();

        InThreadSurveyMetadata.InThreadSurveyQuestion = (function() {

            function InThreadSurveyQuestion(p) {
                this.questionOptions = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            InThreadSurveyQuestion.prototype.questionText = null;
            InThreadSurveyQuestion.prototype.questionID = null;
            InThreadSurveyQuestion.prototype.questionOptions = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionText", {
                get: $util.oneOfGetter($oneOfFields = ["questionText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionID", {
                get: $util.oneOfGetter($oneOfFields = ["questionID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyQuestion.create = function create(properties) {
                return new InThreadSurveyQuestion(properties);
            };

            InThreadSurveyQuestion.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.questionText != null && Object.hasOwnProperty.call(m, "questionText"))
                    w.uint32(10).string(m.questionText);
                if (m.questionID != null && Object.hasOwnProperty.call(m, "questionID"))
                    w.uint32(18).string(m.questionID);
                if (m.questionOptions != null && m.questionOptions.length) {
                    for (var i = 0; i < m.questionOptions.length; ++i)
                        $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption.encode(m.questionOptions[i], w.uint32(26).fork()).ldelim();
                }
                return w;
            };

            InThreadSurveyQuestion.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.questionText = r.string();
                            break;
                        }
                    case 2: {
                            m.questionID = r.string();
                            break;
                        }
                    case 3: {
                            if (!(m.questionOptions && m.questionOptions.length))
                                m.questionOptions = [];
                            m.questionOptions.push($root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption.decode(r, r.uint32(), undefined, n + 1));
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            InThreadSurveyQuestion.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion();
                if (d.questionText != null) {
                    m.questionText = String(d.questionText);
                }
                if (d.questionID != null) {
                    m.questionID = String(d.questionID);
                }
                if (d.questionOptions) {
                    if (!Array.isArray(d.questionOptions))
                        throw TypeError(".WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: array expected");
                    m.questionOptions = [];
                    for (var i = 0; i < d.questionOptions.length; ++i) {
                        if (typeof d.questionOptions[i] !== "object")
                            throw TypeError(".WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: object expected");
                        m.questionOptions[i] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption.fromObject(d.questionOptions[i], n + 1);
                    }
                }
                return m;
            };

            InThreadSurveyQuestion.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.questionOptions = [];
                }
                if (m.questionText != null && m.hasOwnProperty("questionText")) {
                    d.questionText = m.questionText;
                    if (o.oneofs)
                        d._questionText = "questionText";
                }
                if (m.questionID != null && m.hasOwnProperty("questionID")) {
                    d.questionID = m.questionID;
                    if (o.oneofs)
                        d._questionID = "questionID";
                }
                if (m.questionOptions && m.questionOptions.length) {
                    d.questionOptions = [];
                    for (var j = 0; j < m.questionOptions.length; ++j) {
                        d.questionOptions[j] = $root.WAAICommon.InThreadSurveyMetadata.InThreadSurveyOption.toObject(m.questionOptions[j], o);
                    }
                }
                return d;
            };

            InThreadSurveyQuestion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyQuestion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.InThreadSurveyMetadata.InThreadSurveyQuestion";
            };

            return InThreadSurveyQuestion;
        })();

        return InThreadSurveyMetadata;
    })();

    WAAICommon.BotMessageOriginMetadata = (function() {

        function BotMessageOriginMetadata(p) {
            this.origins = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMessageOriginMetadata.prototype.origins = $util.emptyArray;

        BotMessageOriginMetadata.create = function create(properties) {
            return new BotMessageOriginMetadata(properties);
        };

        BotMessageOriginMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.origins != null && m.origins.length) {
                for (var i = 0; i < m.origins.length; ++i)
                    $root.WAAICommon.BotMessageOrigin.encode(m.origins[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotMessageOriginMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMessageOriginMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.origins && m.origins.length))
                            m.origins = [];
                        m.origins.push($root.WAAICommon.BotMessageOrigin.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMessageOriginMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMessageOriginMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMessageOriginMetadata();
            if (d.origins) {
                if (!Array.isArray(d.origins))
                    throw TypeError(".WAAICommon.BotMessageOriginMetadata.origins: array expected");
                m.origins = [];
                for (var i = 0; i < d.origins.length; ++i) {
                    if (typeof d.origins[i] !== "object")
                        throw TypeError(".WAAICommon.BotMessageOriginMetadata.origins: object expected");
                    m.origins[i] = $root.WAAICommon.BotMessageOrigin.fromObject(d.origins[i], n + 1);
                }
            }
            return m;
        };

        BotMessageOriginMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.origins = [];
            }
            if (m.origins && m.origins.length) {
                d.origins = [];
                for (var j = 0; j < m.origins.length; ++j) {
                    d.origins[j] = $root.WAAICommon.BotMessageOrigin.toObject(m.origins[j], o);
                }
            }
            return d;
        };

        BotMessageOriginMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageOriginMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMessageOriginMetadata";
        };

        return BotMessageOriginMetadata;
    })();

    WAAICommon.BotUnifiedResponseMutation = (function() {

        function BotUnifiedResponseMutation(p) {
            this.mediaDetailsMetadataList = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotUnifiedResponseMutation.prototype.sbsMetadata = null;
        BotUnifiedResponseMutation.prototype.mediaDetailsMetadataList = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotUnifiedResponseMutation.prototype, "_sbsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sbsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotUnifiedResponseMutation.create = function create(properties) {
            return new BotUnifiedResponseMutation(properties);
        };

        BotUnifiedResponseMutation.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.sbsMetadata != null && Object.hasOwnProperty.call(m, "sbsMetadata"))
                $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata.encode(m.sbsMetadata, w.uint32(10).fork()).ldelim();
            if (m.mediaDetailsMetadataList != null && m.mediaDetailsMetadataList.length) {
                for (var i = 0; i < m.mediaDetailsMetadataList.length; ++i)
                    $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.encode(m.mediaDetailsMetadataList[i], w.uint32(18).fork()).ldelim();
            }
            return w;
        };

        BotUnifiedResponseMutation.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotUnifiedResponseMutation();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.sbsMetadata = $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        if (!(m.mediaDetailsMetadataList && m.mediaDetailsMetadataList.length))
                            m.mediaDetailsMetadataList = [];
                        m.mediaDetailsMetadataList.push($root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotUnifiedResponseMutation.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotUnifiedResponseMutation)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotUnifiedResponseMutation();
            if (d.sbsMetadata != null) {
                if (typeof d.sbsMetadata !== "object")
                    throw TypeError(".WAAICommon.BotUnifiedResponseMutation.sbsMetadata: object expected");
                m.sbsMetadata = $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata.fromObject(d.sbsMetadata, n + 1);
            }
            if (d.mediaDetailsMetadataList) {
                if (!Array.isArray(d.mediaDetailsMetadataList))
                    throw TypeError(".WAAICommon.BotUnifiedResponseMutation.mediaDetailsMetadataList: array expected");
                m.mediaDetailsMetadataList = [];
                for (var i = 0; i < d.mediaDetailsMetadataList.length; ++i) {
                    if (typeof d.mediaDetailsMetadataList[i] !== "object")
                        throw TypeError(".WAAICommon.BotUnifiedResponseMutation.mediaDetailsMetadataList: object expected");
                    m.mediaDetailsMetadataList[i] = $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.fromObject(d.mediaDetailsMetadataList[i], n + 1);
                }
            }
            return m;
        };

        BotUnifiedResponseMutation.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.mediaDetailsMetadataList = [];
            }
            if (m.sbsMetadata != null && m.hasOwnProperty("sbsMetadata")) {
                d.sbsMetadata = $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata.toObject(m.sbsMetadata, o);
                if (o.oneofs)
                    d._sbsMetadata = "sbsMetadata";
            }
            if (m.mediaDetailsMetadataList && m.mediaDetailsMetadataList.length) {
                d.mediaDetailsMetadataList = [];
                for (var j = 0; j < m.mediaDetailsMetadataList.length; ++j) {
                    d.mediaDetailsMetadataList[j] = $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.toObject(m.mediaDetailsMetadataList[j], o);
                }
            }
            return d;
        };

        BotUnifiedResponseMutation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotUnifiedResponseMutation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotUnifiedResponseMutation";
        };

        BotUnifiedResponseMutation.MediaDetailsMetadata = (function() {

            function MediaDetailsMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            MediaDetailsMetadata.prototype.ID = null;
            MediaDetailsMetadata.prototype.highResMedia = null;
            MediaDetailsMetadata.prototype.previewMedia = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_ID", {
                get: $util.oneOfGetter($oneOfFields = ["ID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_highResMedia", {
                get: $util.oneOfGetter($oneOfFields = ["highResMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(MediaDetailsMetadata.prototype, "_previewMedia", {
                get: $util.oneOfGetter($oneOfFields = ["previewMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MediaDetailsMetadata.create = function create(properties) {
                return new MediaDetailsMetadata(properties);
            };

            MediaDetailsMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                    w.uint32(10).string(m.ID);
                if (m.highResMedia != null && Object.hasOwnProperty.call(m, "highResMedia"))
                    $root.WAAICommon.BotMediaMetadata.encode(m.highResMedia, w.uint32(18).fork()).ldelim();
                if (m.previewMedia != null && Object.hasOwnProperty.call(m, "previewMedia"))
                    $root.WAAICommon.BotMediaMetadata.encode(m.previewMedia, w.uint32(26).fork()).ldelim();
                return w;
            };

            MediaDetailsMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.ID = r.string();
                            break;
                        }
                    case 2: {
                            m.highResMedia = $root.WAAICommon.BotMediaMetadata.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    case 3: {
                            m.previewMedia = $root.WAAICommon.BotMediaMetadata.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            MediaDetailsMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata();
                if (d.ID != null) {
                    m.ID = String(d.ID);
                }
                if (d.highResMedia != null) {
                    if (typeof d.highResMedia !== "object")
                        throw TypeError(".WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.highResMedia: object expected");
                    m.highResMedia = $root.WAAICommon.BotMediaMetadata.fromObject(d.highResMedia, n + 1);
                }
                if (d.previewMedia != null) {
                    if (typeof d.previewMedia !== "object")
                        throw TypeError(".WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata.previewMedia: object expected");
                    m.previewMedia = $root.WAAICommon.BotMediaMetadata.fromObject(d.previewMedia, n + 1);
                }
                return m;
            };

            MediaDetailsMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.ID != null && m.hasOwnProperty("ID")) {
                    d.ID = m.ID;
                    if (o.oneofs)
                        d._ID = "ID";
                }
                if (m.highResMedia != null && m.hasOwnProperty("highResMedia")) {
                    d.highResMedia = $root.WAAICommon.BotMediaMetadata.toObject(m.highResMedia, o);
                    if (o.oneofs)
                        d._highResMedia = "highResMedia";
                }
                if (m.previewMedia != null && m.hasOwnProperty("previewMedia")) {
                    d.previewMedia = $root.WAAICommon.BotMediaMetadata.toObject(m.previewMedia, o);
                    if (o.oneofs)
                        d._previewMedia = "previewMedia";
                }
                return d;
            };

            MediaDetailsMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            MediaDetailsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotUnifiedResponseMutation.MediaDetailsMetadata";
            };

            return MediaDetailsMetadata;
        })();

        BotUnifiedResponseMutation.SideBySideMetadata = (function() {

            function SideBySideMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            SideBySideMetadata.prototype.primaryResponseID = null;
            SideBySideMetadata.prototype.surveyCtaHasRendered = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideMetadata.prototype, "_primaryResponseID", {
                get: $util.oneOfGetter($oneOfFields = ["primaryResponseID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(SideBySideMetadata.prototype, "_surveyCtaHasRendered", {
                get: $util.oneOfGetter($oneOfFields = ["surveyCtaHasRendered"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SideBySideMetadata.create = function create(properties) {
                return new SideBySideMetadata(properties);
            };

            SideBySideMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.primaryResponseID != null && Object.hasOwnProperty.call(m, "primaryResponseID"))
                    w.uint32(10).string(m.primaryResponseID);
                if (m.surveyCtaHasRendered != null && Object.hasOwnProperty.call(m, "surveyCtaHasRendered"))
                    w.uint32(16).bool(m.surveyCtaHasRendered);
                return w;
            };

            SideBySideMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.primaryResponseID = r.string();
                            break;
                        }
                    case 2: {
                            m.surveyCtaHasRendered = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            SideBySideMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata();
                if (d.primaryResponseID != null) {
                    m.primaryResponseID = String(d.primaryResponseID);
                }
                if (d.surveyCtaHasRendered != null) {
                    m.surveyCtaHasRendered = Boolean(d.surveyCtaHasRendered);
                }
                return m;
            };

            SideBySideMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.primaryResponseID != null && m.hasOwnProperty("primaryResponseID")) {
                    d.primaryResponseID = m.primaryResponseID;
                    if (o.oneofs)
                        d._primaryResponseID = "primaryResponseID";
                }
                if (m.surveyCtaHasRendered != null && m.hasOwnProperty("surveyCtaHasRendered")) {
                    d.surveyCtaHasRendered = m.surveyCtaHasRendered;
                    if (o.oneofs)
                        d._surveyCtaHasRendered = "surveyCtaHasRendered";
                }
                return d;
            };

            SideBySideMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            SideBySideMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommon.BotUnifiedResponseMutation.SideBySideMetadata";
            };

            return SideBySideMetadata;
        })();

        return BotUnifiedResponseMutation;
    })();

    WAAICommon.AIMediaCollectionMetadata = (function() {

        function AIMediaCollectionMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIMediaCollectionMetadata.prototype.collectionID = null;
        AIMediaCollectionMetadata.prototype.uploadOrderIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMetadata.prototype, "_collectionID", {
            get: $util.oneOfGetter($oneOfFields = ["collectionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMetadata.prototype, "_uploadOrderIndex", {
            get: $util.oneOfGetter($oneOfFields = ["uploadOrderIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIMediaCollectionMetadata.create = function create(properties) {
            return new AIMediaCollectionMetadata(properties);
        };

        AIMediaCollectionMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.collectionID != null && Object.hasOwnProperty.call(m, "collectionID"))
                w.uint32(10).string(m.collectionID);
            if (m.uploadOrderIndex != null && Object.hasOwnProperty.call(m, "uploadOrderIndex"))
                w.uint32(16).uint32(m.uploadOrderIndex);
            return w;
        };

        AIMediaCollectionMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIMediaCollectionMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.collectionID = r.string();
                        break;
                    }
                case 2: {
                        m.uploadOrderIndex = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIMediaCollectionMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIMediaCollectionMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIMediaCollectionMetadata();
            if (d.collectionID != null) {
                m.collectionID = String(d.collectionID);
            }
            if (d.uploadOrderIndex != null) {
                m.uploadOrderIndex = d.uploadOrderIndex >>> 0;
            }
            return m;
        };

        AIMediaCollectionMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.collectionID != null && m.hasOwnProperty("collectionID")) {
                d.collectionID = m.collectionID;
                if (o.oneofs)
                    d._collectionID = "collectionID";
            }
            if (m.uploadOrderIndex != null && m.hasOwnProperty("uploadOrderIndex")) {
                d.uploadOrderIndex = m.uploadOrderIndex;
                if (o.oneofs)
                    d._uploadOrderIndex = "uploadOrderIndex";
            }
            return d;
        };

        AIMediaCollectionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIMediaCollectionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIMediaCollectionMetadata";
        };

        return AIMediaCollectionMetadata;
    })();

    WAAICommon.AIMediaCollectionMessage = (function() {

        function AIMediaCollectionMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIMediaCollectionMessage.prototype.collectionID = null;
        AIMediaCollectionMessage.prototype.expectedMediaCount = null;
        AIMediaCollectionMessage.prototype.hasGlobalCaption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_collectionID", {
            get: $util.oneOfGetter($oneOfFields = ["collectionID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_expectedMediaCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedMediaCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIMediaCollectionMessage.prototype, "_hasGlobalCaption", {
            get: $util.oneOfGetter($oneOfFields = ["hasGlobalCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIMediaCollectionMessage.create = function create(properties) {
            return new AIMediaCollectionMessage(properties);
        };

        AIMediaCollectionMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.collectionID != null && Object.hasOwnProperty.call(m, "collectionID"))
                w.uint32(10).string(m.collectionID);
            if (m.expectedMediaCount != null && Object.hasOwnProperty.call(m, "expectedMediaCount"))
                w.uint32(16).uint32(m.expectedMediaCount);
            if (m.hasGlobalCaption != null && Object.hasOwnProperty.call(m, "hasGlobalCaption"))
                w.uint32(24).bool(m.hasGlobalCaption);
            return w;
        };

        AIMediaCollectionMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIMediaCollectionMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.collectionID = r.string();
                        break;
                    }
                case 2: {
                        m.expectedMediaCount = r.uint32();
                        break;
                    }
                case 3: {
                        m.hasGlobalCaption = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIMediaCollectionMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIMediaCollectionMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIMediaCollectionMessage();
            if (d.collectionID != null) {
                m.collectionID = String(d.collectionID);
            }
            if (d.expectedMediaCount != null) {
                m.expectedMediaCount = d.expectedMediaCount >>> 0;
            }
            if (d.hasGlobalCaption != null) {
                m.hasGlobalCaption = Boolean(d.hasGlobalCaption);
            }
            return m;
        };

        AIMediaCollectionMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.collectionID != null && m.hasOwnProperty("collectionID")) {
                d.collectionID = m.collectionID;
                if (o.oneofs)
                    d._collectionID = "collectionID";
            }
            if (m.expectedMediaCount != null && m.hasOwnProperty("expectedMediaCount")) {
                d.expectedMediaCount = m.expectedMediaCount;
                if (o.oneofs)
                    d._expectedMediaCount = "expectedMediaCount";
            }
            if (m.hasGlobalCaption != null && m.hasOwnProperty("hasGlobalCaption")) {
                d.hasGlobalCaption = m.hasGlobalCaption;
                if (o.oneofs)
                    d._hasGlobalCaption = "hasGlobalCaption";
            }
            return d;
        };

        AIMediaCollectionMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIMediaCollectionMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIMediaCollectionMessage";
        };

        return AIMediaCollectionMessage;
    })();

    WAAICommon.BotCommandMetadata = (function() {

        function BotCommandMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotCommandMetadata.prototype.commandName = null;
        BotCommandMetadata.prototype.commandDescription = null;
        BotCommandMetadata.prototype.commandPrompt = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandName", {
            get: $util.oneOfGetter($oneOfFields = ["commandName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandDescription", {
            get: $util.oneOfGetter($oneOfFields = ["commandDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotCommandMetadata.prototype, "_commandPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["commandPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotCommandMetadata.create = function create(properties) {
            return new BotCommandMetadata(properties);
        };

        BotCommandMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.commandName != null && Object.hasOwnProperty.call(m, "commandName"))
                w.uint32(10).string(m.commandName);
            if (m.commandDescription != null && Object.hasOwnProperty.call(m, "commandDescription"))
                w.uint32(18).string(m.commandDescription);
            if (m.commandPrompt != null && Object.hasOwnProperty.call(m, "commandPrompt"))
                w.uint32(26).string(m.commandPrompt);
            return w;
        };

        BotCommandMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotCommandMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.commandName = r.string();
                        break;
                    }
                case 2: {
                        m.commandDescription = r.string();
                        break;
                    }
                case 3: {
                        m.commandPrompt = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotCommandMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotCommandMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotCommandMetadata();
            if (d.commandName != null) {
                m.commandName = String(d.commandName);
            }
            if (d.commandDescription != null) {
                m.commandDescription = String(d.commandDescription);
            }
            if (d.commandPrompt != null) {
                m.commandPrompt = String(d.commandPrompt);
            }
            return m;
        };

        BotCommandMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.commandName != null && m.hasOwnProperty("commandName")) {
                d.commandName = m.commandName;
                if (o.oneofs)
                    d._commandName = "commandName";
            }
            if (m.commandDescription != null && m.hasOwnProperty("commandDescription")) {
                d.commandDescription = m.commandDescription;
                if (o.oneofs)
                    d._commandDescription = "commandDescription";
            }
            if (m.commandPrompt != null && m.hasOwnProperty("commandPrompt")) {
                d.commandPrompt = m.commandPrompt;
                if (o.oneofs)
                    d._commandPrompt = "commandPrompt";
            }
            return d;
        };

        BotCommandMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotCommandMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotCommandMetadata";
        };

        return BotCommandMetadata;
    })();

    WAAICommon.BotResolvedToolCallMetadata = (function() {

        function BotResolvedToolCallMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotResolvedToolCallMetadata.prototype.toolCallID = null;
        BotResolvedToolCallMetadata.prototype.resolutionDataSerialized = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_toolCallID", {
            get: $util.oneOfGetter($oneOfFields = ["toolCallID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_resolutionDataSerialized", {
            get: $util.oneOfGetter($oneOfFields = ["resolutionDataSerialized"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotResolvedToolCallMetadata.create = function create(properties) {
            return new BotResolvedToolCallMetadata(properties);
        };

        BotResolvedToolCallMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.toolCallID != null && Object.hasOwnProperty.call(m, "toolCallID"))
                w.uint32(10).string(m.toolCallID);
            if (m.resolutionDataSerialized != null && Object.hasOwnProperty.call(m, "resolutionDataSerialized"))
                w.uint32(18).string(m.resolutionDataSerialized);
            return w;
        };

        BotResolvedToolCallMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotResolvedToolCallMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.toolCallID = r.string();
                        break;
                    }
                case 2: {
                        m.resolutionDataSerialized = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotResolvedToolCallMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotResolvedToolCallMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotResolvedToolCallMetadata();
            if (d.toolCallID != null) {
                m.toolCallID = String(d.toolCallID);
            }
            if (d.resolutionDataSerialized != null) {
                m.resolutionDataSerialized = String(d.resolutionDataSerialized);
            }
            return m;
        };

        BotResolvedToolCallMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.toolCallID != null && m.hasOwnProperty("toolCallID")) {
                d.toolCallID = m.toolCallID;
                if (o.oneofs)
                    d._toolCallID = "toolCallID";
            }
            if (m.resolutionDataSerialized != null && m.hasOwnProperty("resolutionDataSerialized")) {
                d.resolutionDataSerialized = m.resolutionDataSerialized;
                if (o.oneofs)
                    d._resolutionDataSerialized = "resolutionDataSerialized";
            }
            return d;
        };

        BotResolvedToolCallMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotResolvedToolCallMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotResolvedToolCallMetadata";
        };

        return BotResolvedToolCallMetadata;
    })();

    WAAICommon.BotMetadata = (function() {

        function BotMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMetadata.prototype.personaID = null;
        BotMetadata.prototype.pluginMetadata = null;
        BotMetadata.prototype.suggestedPromptMetadata = null;
        BotMetadata.prototype.invokerJID = null;
        BotMetadata.prototype.sessionMetadata = null;
        BotMetadata.prototype.memuMetadata = null;
        BotMetadata.prototype.timezone = null;
        BotMetadata.prototype.reminderMetadata = null;
        BotMetadata.prototype.modelMetadata = null;
        BotMetadata.prototype.messageDisclaimerText = null;
        BotMetadata.prototype.progressIndicatorMetadata = null;
        BotMetadata.prototype.capabilityMetadata = null;
        BotMetadata.prototype.imagineMetadata = null;
        BotMetadata.prototype.memoryMetadata = null;
        BotMetadata.prototype.renderingMetadata = null;
        BotMetadata.prototype.botMetricsMetadata = null;
        BotMetadata.prototype.botLinkedAccountsMetadata = null;
        BotMetadata.prototype.richResponseSourcesMetadata = null;
        BotMetadata.prototype.aiConversationContext = null;
        BotMetadata.prototype.botPromotionMessageMetadata = null;
        BotMetadata.prototype.botModeSelectionMetadata = null;
        BotMetadata.prototype.botQuotaMetadata = null;
        BotMetadata.prototype.botAgeCollectionMetadata = null;
        BotMetadata.prototype.conversationStarterPromptID = null;
        BotMetadata.prototype.botResponseID = null;
        BotMetadata.prototype.verificationMetadata = null;
        BotMetadata.prototype.unifiedResponseMutation = null;
        BotMetadata.prototype.botMessageOriginMetadata = null;
        BotMetadata.prototype.inThreadSurveyMetadata = null;
        BotMetadata.prototype.botThreadInfo = null;
        BotMetadata.prototype.regenerateMetadata = null;
        BotMetadata.prototype.sessionTransparencyMetadata = null;
        BotMetadata.prototype.botDocumentMessageMetadata = null;
        BotMetadata.prototype.botGroupMetadata = null;
        BotMetadata.prototype.botRenderingConfigMetadata = null;
        BotMetadata.prototype.botInfrastructureDiagnostics = null;
        BotMetadata.prototype.aiMediaCollectionMetadata = null;
        BotMetadata.prototype.commandMetadata = null;
        BotMetadata.prototype.resolvedToolCallMetadata = null;
        BotMetadata.prototype.subscriptionUpsellMetadata = null;
        BotMetadata.prototype.internalMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_personaID", {
            get: $util.oneOfGetter($oneOfFields = ["personaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_pluginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pluginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_suggestedPromptMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["suggestedPromptMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_invokerJID", {
            get: $util.oneOfGetter($oneOfFields = ["invokerJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_sessionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_memuMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memuMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_timezone", {
            get: $util.oneOfGetter($oneOfFields = ["timezone"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_reminderMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["reminderMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_modelMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["modelMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_messageDisclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["messageDisclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_progressIndicatorMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["progressIndicatorMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_capabilityMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["capabilityMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_imagineMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imagineMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_memoryMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memoryMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_renderingMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["renderingMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botMetricsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetricsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botLinkedAccountsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botLinkedAccountsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_richResponseSourcesMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseSourcesMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_aiConversationContext", {
            get: $util.oneOfGetter($oneOfFields = ["aiConversationContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botPromotionMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botPromotionMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botModeSelectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botModeSelectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botQuotaMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botQuotaMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botAgeCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botAgeCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_conversationStarterPromptID", {
            get: $util.oneOfGetter($oneOfFields = ["conversationStarterPromptID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botResponseID", {
            get: $util.oneOfGetter($oneOfFields = ["botResponseID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_verificationMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["verificationMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_unifiedResponseMutation", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponseMutation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botMessageOriginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageOriginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_inThreadSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["inThreadSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botThreadInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botThreadInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_regenerateMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["regenerateMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_sessionTransparencyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botDocumentMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botDocumentMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botGroupMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botGroupMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botRenderingConfigMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botRenderingConfigMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_botInfrastructureDiagnostics", {
            get: $util.oneOfGetter($oneOfFields = ["botInfrastructureDiagnostics"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_aiMediaCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_commandMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commandMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_resolvedToolCallMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["resolvedToolCallMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_subscriptionUpsellMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionUpsellMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMetadata.prototype, "_internalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["internalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMetadata.create = function create(properties) {
            return new BotMetadata(properties);
        };

        BotMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.personaID != null && Object.hasOwnProperty.call(m, "personaID"))
                w.uint32(18).string(m.personaID);
            if (m.pluginMetadata != null && Object.hasOwnProperty.call(m, "pluginMetadata"))
                $root.WAAICommon.BotPluginMetadata.encode(m.pluginMetadata, w.uint32(26).fork()).ldelim();
            if (m.suggestedPromptMetadata != null && Object.hasOwnProperty.call(m, "suggestedPromptMetadata"))
                $root.WAAICommon.BotSuggestedPromptMetadata.encode(m.suggestedPromptMetadata, w.uint32(34).fork()).ldelim();
            if (m.invokerJID != null && Object.hasOwnProperty.call(m, "invokerJID"))
                w.uint32(42).string(m.invokerJID);
            if (m.sessionMetadata != null && Object.hasOwnProperty.call(m, "sessionMetadata"))
                $root.WAAICommon.BotSessionMetadata.encode(m.sessionMetadata, w.uint32(50).fork()).ldelim();
            if (m.memuMetadata != null && Object.hasOwnProperty.call(m, "memuMetadata"))
                $root.WAAICommon.BotMemuMetadata.encode(m.memuMetadata, w.uint32(58).fork()).ldelim();
            if (m.timezone != null && Object.hasOwnProperty.call(m, "timezone"))
                w.uint32(66).string(m.timezone);
            if (m.reminderMetadata != null && Object.hasOwnProperty.call(m, "reminderMetadata"))
                $root.WAAICommon.BotReminderMetadata.encode(m.reminderMetadata, w.uint32(74).fork()).ldelim();
            if (m.modelMetadata != null && Object.hasOwnProperty.call(m, "modelMetadata"))
                $root.WAAICommon.BotModelMetadata.encode(m.modelMetadata, w.uint32(82).fork()).ldelim();
            if (m.messageDisclaimerText != null && Object.hasOwnProperty.call(m, "messageDisclaimerText"))
                w.uint32(90).string(m.messageDisclaimerText);
            if (m.progressIndicatorMetadata != null && Object.hasOwnProperty.call(m, "progressIndicatorMetadata"))
                $root.WAAICommon.BotProgressIndicatorMetadata.encode(m.progressIndicatorMetadata, w.uint32(98).fork()).ldelim();
            if (m.capabilityMetadata != null && Object.hasOwnProperty.call(m, "capabilityMetadata"))
                $root.WAAICommon.BotCapabilityMetadata.encode(m.capabilityMetadata, w.uint32(106).fork()).ldelim();
            if (m.imagineMetadata != null && Object.hasOwnProperty.call(m, "imagineMetadata"))
                $root.WAAICommon.BotImagineMetadata.encode(m.imagineMetadata, w.uint32(114).fork()).ldelim();
            if (m.memoryMetadata != null && Object.hasOwnProperty.call(m, "memoryMetadata"))
                $root.WAAICommon.BotMemoryMetadata.encode(m.memoryMetadata, w.uint32(122).fork()).ldelim();
            if (m.renderingMetadata != null && Object.hasOwnProperty.call(m, "renderingMetadata"))
                $root.WAAICommon.BotRenderingMetadata.encode(m.renderingMetadata, w.uint32(130).fork()).ldelim();
            if (m.botMetricsMetadata != null && Object.hasOwnProperty.call(m, "botMetricsMetadata"))
                $root.WAAICommon.BotMetricsMetadata.encode(m.botMetricsMetadata, w.uint32(138).fork()).ldelim();
            if (m.botLinkedAccountsMetadata != null && Object.hasOwnProperty.call(m, "botLinkedAccountsMetadata"))
                $root.WAAICommon.BotLinkedAccountsMetadata.encode(m.botLinkedAccountsMetadata, w.uint32(146).fork()).ldelim();
            if (m.richResponseSourcesMetadata != null && Object.hasOwnProperty.call(m, "richResponseSourcesMetadata"))
                $root.WAAICommon.BotSourcesMetadata.encode(m.richResponseSourcesMetadata, w.uint32(154).fork()).ldelim();
            if (m.aiConversationContext != null && Object.hasOwnProperty.call(m, "aiConversationContext"))
                w.uint32(162).bytes(m.aiConversationContext);
            if (m.botPromotionMessageMetadata != null && Object.hasOwnProperty.call(m, "botPromotionMessageMetadata"))
                $root.WAAICommon.BotPromotionMessageMetadata.encode(m.botPromotionMessageMetadata, w.uint32(170).fork()).ldelim();
            if (m.botModeSelectionMetadata != null && Object.hasOwnProperty.call(m, "botModeSelectionMetadata"))
                $root.WAAICommon.BotModeSelectionMetadata.encode(m.botModeSelectionMetadata, w.uint32(178).fork()).ldelim();
            if (m.botQuotaMetadata != null && Object.hasOwnProperty.call(m, "botQuotaMetadata"))
                $root.WAAICommon.BotQuotaMetadata.encode(m.botQuotaMetadata, w.uint32(186).fork()).ldelim();
            if (m.botAgeCollectionMetadata != null && Object.hasOwnProperty.call(m, "botAgeCollectionMetadata"))
                $root.WAAICommon.BotAgeCollectionMetadata.encode(m.botAgeCollectionMetadata, w.uint32(194).fork()).ldelim();
            if (m.conversationStarterPromptID != null && Object.hasOwnProperty.call(m, "conversationStarterPromptID"))
                w.uint32(202).string(m.conversationStarterPromptID);
            if (m.botResponseID != null && Object.hasOwnProperty.call(m, "botResponseID"))
                w.uint32(210).string(m.botResponseID);
            if (m.verificationMetadata != null && Object.hasOwnProperty.call(m, "verificationMetadata"))
                $root.WAAICommon.BotSignatureVerificationMetadata.encode(m.verificationMetadata, w.uint32(218).fork()).ldelim();
            if (m.unifiedResponseMutation != null && Object.hasOwnProperty.call(m, "unifiedResponseMutation"))
                $root.WAAICommon.BotUnifiedResponseMutation.encode(m.unifiedResponseMutation, w.uint32(226).fork()).ldelim();
            if (m.botMessageOriginMetadata != null && Object.hasOwnProperty.call(m, "botMessageOriginMetadata"))
                $root.WAAICommon.BotMessageOriginMetadata.encode(m.botMessageOriginMetadata, w.uint32(234).fork()).ldelim();
            if (m.inThreadSurveyMetadata != null && Object.hasOwnProperty.call(m, "inThreadSurveyMetadata"))
                $root.WAAICommon.InThreadSurveyMetadata.encode(m.inThreadSurveyMetadata, w.uint32(242).fork()).ldelim();
            if (m.botThreadInfo != null && Object.hasOwnProperty.call(m, "botThreadInfo"))
                $root.WAAICommon.AIThreadInfo.encode(m.botThreadInfo, w.uint32(250).fork()).ldelim();
            if (m.regenerateMetadata != null && Object.hasOwnProperty.call(m, "regenerateMetadata"))
                $root.WAAICommon.AIRegenerateMetadata.encode(m.regenerateMetadata, w.uint32(258).fork()).ldelim();
            if (m.sessionTransparencyMetadata != null && Object.hasOwnProperty.call(m, "sessionTransparencyMetadata"))
                $root.WAAICommon.SessionTransparencyMetadata.encode(m.sessionTransparencyMetadata, w.uint32(266).fork()).ldelim();
            if (m.botDocumentMessageMetadata != null && Object.hasOwnProperty.call(m, "botDocumentMessageMetadata"))
                $root.WAAICommon.BotDocumentMessageMetadata.encode(m.botDocumentMessageMetadata, w.uint32(274).fork()).ldelim();
            if (m.botGroupMetadata != null && Object.hasOwnProperty.call(m, "botGroupMetadata"))
                $root.WAAICommon.BotGroupMetadata.encode(m.botGroupMetadata, w.uint32(282).fork()).ldelim();
            if (m.botRenderingConfigMetadata != null && Object.hasOwnProperty.call(m, "botRenderingConfigMetadata"))
                $root.WAAICommon.BotRenderingConfigMetadata.encode(m.botRenderingConfigMetadata, w.uint32(290).fork()).ldelim();
            if (m.botInfrastructureDiagnostics != null && Object.hasOwnProperty.call(m, "botInfrastructureDiagnostics"))
                $root.WAAICommon.BotInfrastructureDiagnostics.encode(m.botInfrastructureDiagnostics, w.uint32(298).fork()).ldelim();
            if (m.aiMediaCollectionMetadata != null && Object.hasOwnProperty.call(m, "aiMediaCollectionMetadata"))
                $root.WAAICommon.AIMediaCollectionMetadata.encode(m.aiMediaCollectionMetadata, w.uint32(306).fork()).ldelim();
            if (m.commandMetadata != null && Object.hasOwnProperty.call(m, "commandMetadata"))
                $root.WAAICommon.BotCommandMetadata.encode(m.commandMetadata, w.uint32(314).fork()).ldelim();
            if (m.resolvedToolCallMetadata != null && Object.hasOwnProperty.call(m, "resolvedToolCallMetadata"))
                $root.WAAICommon.BotResolvedToolCallMetadata.encode(m.resolvedToolCallMetadata, w.uint32(322).fork()).ldelim();
            if (m.subscriptionUpsellMetadata != null && Object.hasOwnProperty.call(m, "subscriptionUpsellMetadata"))
                $root.WAAICommon.AISubscriptionUpsellMetadata.encode(m.subscriptionUpsellMetadata, w.uint32(330).fork()).ldelim();
            if (m.internalMetadata != null && Object.hasOwnProperty.call(m, "internalMetadata"))
                w.uint32(7994).bytes(m.internalMetadata);
            return w;
        };

        BotMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 2: {
                        m.personaID = r.string();
                        break;
                    }
                case 3: {
                        m.pluginMetadata = $root.WAAICommon.BotPluginMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.suggestedPromptMetadata = $root.WAAICommon.BotSuggestedPromptMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.invokerJID = r.string();
                        break;
                    }
                case 6: {
                        m.sessionMetadata = $root.WAAICommon.BotSessionMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.memuMetadata = $root.WAAICommon.BotMemuMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.timezone = r.string();
                        break;
                    }
                case 9: {
                        m.reminderMetadata = $root.WAAICommon.BotReminderMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.modelMetadata = $root.WAAICommon.BotModelMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 11: {
                        m.messageDisclaimerText = r.string();
                        break;
                    }
                case 12: {
                        m.progressIndicatorMetadata = $root.WAAICommon.BotProgressIndicatorMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 13: {
                        m.capabilityMetadata = $root.WAAICommon.BotCapabilityMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 14: {
                        m.imagineMetadata = $root.WAAICommon.BotImagineMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 15: {
                        m.memoryMetadata = $root.WAAICommon.BotMemoryMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 16: {
                        m.renderingMetadata = $root.WAAICommon.BotRenderingMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 17: {
                        m.botMetricsMetadata = $root.WAAICommon.BotMetricsMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 18: {
                        m.botLinkedAccountsMetadata = $root.WAAICommon.BotLinkedAccountsMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 19: {
                        m.richResponseSourcesMetadata = $root.WAAICommon.BotSourcesMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 20: {
                        m.aiConversationContext = r.bytes();
                        break;
                    }
                case 21: {
                        m.botPromotionMessageMetadata = $root.WAAICommon.BotPromotionMessageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 22: {
                        m.botModeSelectionMetadata = $root.WAAICommon.BotModeSelectionMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 23: {
                        m.botQuotaMetadata = $root.WAAICommon.BotQuotaMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 24: {
                        m.botAgeCollectionMetadata = $root.WAAICommon.BotAgeCollectionMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 25: {
                        m.conversationStarterPromptID = r.string();
                        break;
                    }
                case 26: {
                        m.botResponseID = r.string();
                        break;
                    }
                case 27: {
                        m.verificationMetadata = $root.WAAICommon.BotSignatureVerificationMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 28: {
                        m.unifiedResponseMutation = $root.WAAICommon.BotUnifiedResponseMutation.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 29: {
                        m.botMessageOriginMetadata = $root.WAAICommon.BotMessageOriginMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 30: {
                        m.inThreadSurveyMetadata = $root.WAAICommon.InThreadSurveyMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 31: {
                        m.botThreadInfo = $root.WAAICommon.AIThreadInfo.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 32: {
                        m.regenerateMetadata = $root.WAAICommon.AIRegenerateMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 33: {
                        m.sessionTransparencyMetadata = $root.WAAICommon.SessionTransparencyMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 34: {
                        m.botDocumentMessageMetadata = $root.WAAICommon.BotDocumentMessageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 35: {
                        m.botGroupMetadata = $root.WAAICommon.BotGroupMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 36: {
                        m.botRenderingConfigMetadata = $root.WAAICommon.BotRenderingConfigMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 37: {
                        m.botInfrastructureDiagnostics = $root.WAAICommon.BotInfrastructureDiagnostics.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 38: {
                        m.aiMediaCollectionMetadata = $root.WAAICommon.AIMediaCollectionMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 39: {
                        m.commandMetadata = $root.WAAICommon.BotCommandMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 40: {
                        m.resolvedToolCallMetadata = $root.WAAICommon.BotResolvedToolCallMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 41: {
                        m.subscriptionUpsellMetadata = $root.WAAICommon.AISubscriptionUpsellMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 999: {
                        m.internalMetadata = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMetadata();
            if (d.personaID != null) {
                m.personaID = String(d.personaID);
            }
            if (d.pluginMetadata != null) {
                if (typeof d.pluginMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.pluginMetadata: object expected");
                m.pluginMetadata = $root.WAAICommon.BotPluginMetadata.fromObject(d.pluginMetadata, n + 1);
            }
            if (d.suggestedPromptMetadata != null) {
                if (typeof d.suggestedPromptMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.suggestedPromptMetadata: object expected");
                m.suggestedPromptMetadata = $root.WAAICommon.BotSuggestedPromptMetadata.fromObject(d.suggestedPromptMetadata, n + 1);
            }
            if (d.invokerJID != null) {
                m.invokerJID = String(d.invokerJID);
            }
            if (d.sessionMetadata != null) {
                if (typeof d.sessionMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.sessionMetadata: object expected");
                m.sessionMetadata = $root.WAAICommon.BotSessionMetadata.fromObject(d.sessionMetadata, n + 1);
            }
            if (d.memuMetadata != null) {
                if (typeof d.memuMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.memuMetadata: object expected");
                m.memuMetadata = $root.WAAICommon.BotMemuMetadata.fromObject(d.memuMetadata, n + 1);
            }
            if (d.timezone != null) {
                m.timezone = String(d.timezone);
            }
            if (d.reminderMetadata != null) {
                if (typeof d.reminderMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.reminderMetadata: object expected");
                m.reminderMetadata = $root.WAAICommon.BotReminderMetadata.fromObject(d.reminderMetadata, n + 1);
            }
            if (d.modelMetadata != null) {
                if (typeof d.modelMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.modelMetadata: object expected");
                m.modelMetadata = $root.WAAICommon.BotModelMetadata.fromObject(d.modelMetadata, n + 1);
            }
            if (d.messageDisclaimerText != null) {
                m.messageDisclaimerText = String(d.messageDisclaimerText);
            }
            if (d.progressIndicatorMetadata != null) {
                if (typeof d.progressIndicatorMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.progressIndicatorMetadata: object expected");
                m.progressIndicatorMetadata = $root.WAAICommon.BotProgressIndicatorMetadata.fromObject(d.progressIndicatorMetadata, n + 1);
            }
            if (d.capabilityMetadata != null) {
                if (typeof d.capabilityMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.capabilityMetadata: object expected");
                m.capabilityMetadata = $root.WAAICommon.BotCapabilityMetadata.fromObject(d.capabilityMetadata, n + 1);
            }
            if (d.imagineMetadata != null) {
                if (typeof d.imagineMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.imagineMetadata: object expected");
                m.imagineMetadata = $root.WAAICommon.BotImagineMetadata.fromObject(d.imagineMetadata, n + 1);
            }
            if (d.memoryMetadata != null) {
                if (typeof d.memoryMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.memoryMetadata: object expected");
                m.memoryMetadata = $root.WAAICommon.BotMemoryMetadata.fromObject(d.memoryMetadata, n + 1);
            }
            if (d.renderingMetadata != null) {
                if (typeof d.renderingMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.renderingMetadata: object expected");
                m.renderingMetadata = $root.WAAICommon.BotRenderingMetadata.fromObject(d.renderingMetadata, n + 1);
            }
            if (d.botMetricsMetadata != null) {
                if (typeof d.botMetricsMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botMetricsMetadata: object expected");
                m.botMetricsMetadata = $root.WAAICommon.BotMetricsMetadata.fromObject(d.botMetricsMetadata, n + 1);
            }
            if (d.botLinkedAccountsMetadata != null) {
                if (typeof d.botLinkedAccountsMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botLinkedAccountsMetadata: object expected");
                m.botLinkedAccountsMetadata = $root.WAAICommon.BotLinkedAccountsMetadata.fromObject(d.botLinkedAccountsMetadata, n + 1);
            }
            if (d.richResponseSourcesMetadata != null) {
                if (typeof d.richResponseSourcesMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.richResponseSourcesMetadata: object expected");
                m.richResponseSourcesMetadata = $root.WAAICommon.BotSourcesMetadata.fromObject(d.richResponseSourcesMetadata, n + 1);
            }
            if (d.aiConversationContext != null) {
                if (typeof d.aiConversationContext === "string")
                    $util.base64.decode(d.aiConversationContext, m.aiConversationContext = $util.newBuffer($util.base64.length(d.aiConversationContext)), 0);
                else if (d.aiConversationContext.length >= 0)
                    m.aiConversationContext = d.aiConversationContext;
            }
            if (d.botPromotionMessageMetadata != null) {
                if (typeof d.botPromotionMessageMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botPromotionMessageMetadata: object expected");
                m.botPromotionMessageMetadata = $root.WAAICommon.BotPromotionMessageMetadata.fromObject(d.botPromotionMessageMetadata, n + 1);
            }
            if (d.botModeSelectionMetadata != null) {
                if (typeof d.botModeSelectionMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botModeSelectionMetadata: object expected");
                m.botModeSelectionMetadata = $root.WAAICommon.BotModeSelectionMetadata.fromObject(d.botModeSelectionMetadata, n + 1);
            }
            if (d.botQuotaMetadata != null) {
                if (typeof d.botQuotaMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botQuotaMetadata: object expected");
                m.botQuotaMetadata = $root.WAAICommon.BotQuotaMetadata.fromObject(d.botQuotaMetadata, n + 1);
            }
            if (d.botAgeCollectionMetadata != null) {
                if (typeof d.botAgeCollectionMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botAgeCollectionMetadata: object expected");
                m.botAgeCollectionMetadata = $root.WAAICommon.BotAgeCollectionMetadata.fromObject(d.botAgeCollectionMetadata, n + 1);
            }
            if (d.conversationStarterPromptID != null) {
                m.conversationStarterPromptID = String(d.conversationStarterPromptID);
            }
            if (d.botResponseID != null) {
                m.botResponseID = String(d.botResponseID);
            }
            if (d.verificationMetadata != null) {
                if (typeof d.verificationMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.verificationMetadata: object expected");
                m.verificationMetadata = $root.WAAICommon.BotSignatureVerificationMetadata.fromObject(d.verificationMetadata, n + 1);
            }
            if (d.unifiedResponseMutation != null) {
                if (typeof d.unifiedResponseMutation !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.unifiedResponseMutation: object expected");
                m.unifiedResponseMutation = $root.WAAICommon.BotUnifiedResponseMutation.fromObject(d.unifiedResponseMutation, n + 1);
            }
            if (d.botMessageOriginMetadata != null) {
                if (typeof d.botMessageOriginMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botMessageOriginMetadata: object expected");
                m.botMessageOriginMetadata = $root.WAAICommon.BotMessageOriginMetadata.fromObject(d.botMessageOriginMetadata, n + 1);
            }
            if (d.inThreadSurveyMetadata != null) {
                if (typeof d.inThreadSurveyMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.inThreadSurveyMetadata: object expected");
                m.inThreadSurveyMetadata = $root.WAAICommon.InThreadSurveyMetadata.fromObject(d.inThreadSurveyMetadata, n + 1);
            }
            if (d.botThreadInfo != null) {
                if (typeof d.botThreadInfo !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botThreadInfo: object expected");
                m.botThreadInfo = $root.WAAICommon.AIThreadInfo.fromObject(d.botThreadInfo, n + 1);
            }
            if (d.regenerateMetadata != null) {
                if (typeof d.regenerateMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.regenerateMetadata: object expected");
                m.regenerateMetadata = $root.WAAICommon.AIRegenerateMetadata.fromObject(d.regenerateMetadata, n + 1);
            }
            if (d.sessionTransparencyMetadata != null) {
                if (typeof d.sessionTransparencyMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.sessionTransparencyMetadata: object expected");
                m.sessionTransparencyMetadata = $root.WAAICommon.SessionTransparencyMetadata.fromObject(d.sessionTransparencyMetadata, n + 1);
            }
            if (d.botDocumentMessageMetadata != null) {
                if (typeof d.botDocumentMessageMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botDocumentMessageMetadata: object expected");
                m.botDocumentMessageMetadata = $root.WAAICommon.BotDocumentMessageMetadata.fromObject(d.botDocumentMessageMetadata, n + 1);
            }
            if (d.botGroupMetadata != null) {
                if (typeof d.botGroupMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botGroupMetadata: object expected");
                m.botGroupMetadata = $root.WAAICommon.BotGroupMetadata.fromObject(d.botGroupMetadata, n + 1);
            }
            if (d.botRenderingConfigMetadata != null) {
                if (typeof d.botRenderingConfigMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botRenderingConfigMetadata: object expected");
                m.botRenderingConfigMetadata = $root.WAAICommon.BotRenderingConfigMetadata.fromObject(d.botRenderingConfigMetadata, n + 1);
            }
            if (d.botInfrastructureDiagnostics != null) {
                if (typeof d.botInfrastructureDiagnostics !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.botInfrastructureDiagnostics: object expected");
                m.botInfrastructureDiagnostics = $root.WAAICommon.BotInfrastructureDiagnostics.fromObject(d.botInfrastructureDiagnostics, n + 1);
            }
            if (d.aiMediaCollectionMetadata != null) {
                if (typeof d.aiMediaCollectionMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.aiMediaCollectionMetadata: object expected");
                m.aiMediaCollectionMetadata = $root.WAAICommon.AIMediaCollectionMetadata.fromObject(d.aiMediaCollectionMetadata, n + 1);
            }
            if (d.commandMetadata != null) {
                if (typeof d.commandMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.commandMetadata: object expected");
                m.commandMetadata = $root.WAAICommon.BotCommandMetadata.fromObject(d.commandMetadata, n + 1);
            }
            if (d.resolvedToolCallMetadata != null) {
                if (typeof d.resolvedToolCallMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.resolvedToolCallMetadata: object expected");
                m.resolvedToolCallMetadata = $root.WAAICommon.BotResolvedToolCallMetadata.fromObject(d.resolvedToolCallMetadata, n + 1);
            }
            if (d.subscriptionUpsellMetadata != null) {
                if (typeof d.subscriptionUpsellMetadata !== "object")
                    throw TypeError(".WAAICommon.BotMetadata.subscriptionUpsellMetadata: object expected");
                m.subscriptionUpsellMetadata = $root.WAAICommon.AISubscriptionUpsellMetadata.fromObject(d.subscriptionUpsellMetadata, n + 1);
            }
            if (d.internalMetadata != null) {
                if (typeof d.internalMetadata === "string")
                    $util.base64.decode(d.internalMetadata, m.internalMetadata = $util.newBuffer($util.base64.length(d.internalMetadata)), 0);
                else if (d.internalMetadata.length >= 0)
                    m.internalMetadata = d.internalMetadata;
            }
            return m;
        };

        BotMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.personaID != null && m.hasOwnProperty("personaID")) {
                d.personaID = m.personaID;
                if (o.oneofs)
                    d._personaID = "personaID";
            }
            if (m.pluginMetadata != null && m.hasOwnProperty("pluginMetadata")) {
                d.pluginMetadata = $root.WAAICommon.BotPluginMetadata.toObject(m.pluginMetadata, o);
                if (o.oneofs)
                    d._pluginMetadata = "pluginMetadata";
            }
            if (m.suggestedPromptMetadata != null && m.hasOwnProperty("suggestedPromptMetadata")) {
                d.suggestedPromptMetadata = $root.WAAICommon.BotSuggestedPromptMetadata.toObject(m.suggestedPromptMetadata, o);
                if (o.oneofs)
                    d._suggestedPromptMetadata = "suggestedPromptMetadata";
            }
            if (m.invokerJID != null && m.hasOwnProperty("invokerJID")) {
                d.invokerJID = m.invokerJID;
                if (o.oneofs)
                    d._invokerJID = "invokerJID";
            }
            if (m.sessionMetadata != null && m.hasOwnProperty("sessionMetadata")) {
                d.sessionMetadata = $root.WAAICommon.BotSessionMetadata.toObject(m.sessionMetadata, o);
                if (o.oneofs)
                    d._sessionMetadata = "sessionMetadata";
            }
            if (m.memuMetadata != null && m.hasOwnProperty("memuMetadata")) {
                d.memuMetadata = $root.WAAICommon.BotMemuMetadata.toObject(m.memuMetadata, o);
                if (o.oneofs)
                    d._memuMetadata = "memuMetadata";
            }
            if (m.timezone != null && m.hasOwnProperty("timezone")) {
                d.timezone = m.timezone;
                if (o.oneofs)
                    d._timezone = "timezone";
            }
            if (m.reminderMetadata != null && m.hasOwnProperty("reminderMetadata")) {
                d.reminderMetadata = $root.WAAICommon.BotReminderMetadata.toObject(m.reminderMetadata, o);
                if (o.oneofs)
                    d._reminderMetadata = "reminderMetadata";
            }
            if (m.modelMetadata != null && m.hasOwnProperty("modelMetadata")) {
                d.modelMetadata = $root.WAAICommon.BotModelMetadata.toObject(m.modelMetadata, o);
                if (o.oneofs)
                    d._modelMetadata = "modelMetadata";
            }
            if (m.messageDisclaimerText != null && m.hasOwnProperty("messageDisclaimerText")) {
                d.messageDisclaimerText = m.messageDisclaimerText;
                if (o.oneofs)
                    d._messageDisclaimerText = "messageDisclaimerText";
            }
            if (m.progressIndicatorMetadata != null && m.hasOwnProperty("progressIndicatorMetadata")) {
                d.progressIndicatorMetadata = $root.WAAICommon.BotProgressIndicatorMetadata.toObject(m.progressIndicatorMetadata, o);
                if (o.oneofs)
                    d._progressIndicatorMetadata = "progressIndicatorMetadata";
            }
            if (m.capabilityMetadata != null && m.hasOwnProperty("capabilityMetadata")) {
                d.capabilityMetadata = $root.WAAICommon.BotCapabilityMetadata.toObject(m.capabilityMetadata, o);
                if (o.oneofs)
                    d._capabilityMetadata = "capabilityMetadata";
            }
            if (m.imagineMetadata != null && m.hasOwnProperty("imagineMetadata")) {
                d.imagineMetadata = $root.WAAICommon.BotImagineMetadata.toObject(m.imagineMetadata, o);
                if (o.oneofs)
                    d._imagineMetadata = "imagineMetadata";
            }
            if (m.memoryMetadata != null && m.hasOwnProperty("memoryMetadata")) {
                d.memoryMetadata = $root.WAAICommon.BotMemoryMetadata.toObject(m.memoryMetadata, o);
                if (o.oneofs)
                    d._memoryMetadata = "memoryMetadata";
            }
            if (m.renderingMetadata != null && m.hasOwnProperty("renderingMetadata")) {
                d.renderingMetadata = $root.WAAICommon.BotRenderingMetadata.toObject(m.renderingMetadata, o);
                if (o.oneofs)
                    d._renderingMetadata = "renderingMetadata";
            }
            if (m.botMetricsMetadata != null && m.hasOwnProperty("botMetricsMetadata")) {
                d.botMetricsMetadata = $root.WAAICommon.BotMetricsMetadata.toObject(m.botMetricsMetadata, o);
                if (o.oneofs)
                    d._botMetricsMetadata = "botMetricsMetadata";
            }
            if (m.botLinkedAccountsMetadata != null && m.hasOwnProperty("botLinkedAccountsMetadata")) {
                d.botLinkedAccountsMetadata = $root.WAAICommon.BotLinkedAccountsMetadata.toObject(m.botLinkedAccountsMetadata, o);
                if (o.oneofs)
                    d._botLinkedAccountsMetadata = "botLinkedAccountsMetadata";
            }
            if (m.richResponseSourcesMetadata != null && m.hasOwnProperty("richResponseSourcesMetadata")) {
                d.richResponseSourcesMetadata = $root.WAAICommon.BotSourcesMetadata.toObject(m.richResponseSourcesMetadata, o);
                if (o.oneofs)
                    d._richResponseSourcesMetadata = "richResponseSourcesMetadata";
            }
            if (m.aiConversationContext != null && m.hasOwnProperty("aiConversationContext")) {
                d.aiConversationContext = o.bytes === String ? $util.base64.encode(m.aiConversationContext, 0, m.aiConversationContext.length) : o.bytes === Array ? Array.prototype.slice.call(m.aiConversationContext) : m.aiConversationContext;
                if (o.oneofs)
                    d._aiConversationContext = "aiConversationContext";
            }
            if (m.botPromotionMessageMetadata != null && m.hasOwnProperty("botPromotionMessageMetadata")) {
                d.botPromotionMessageMetadata = $root.WAAICommon.BotPromotionMessageMetadata.toObject(m.botPromotionMessageMetadata, o);
                if (o.oneofs)
                    d._botPromotionMessageMetadata = "botPromotionMessageMetadata";
            }
            if (m.botModeSelectionMetadata != null && m.hasOwnProperty("botModeSelectionMetadata")) {
                d.botModeSelectionMetadata = $root.WAAICommon.BotModeSelectionMetadata.toObject(m.botModeSelectionMetadata, o);
                if (o.oneofs)
                    d._botModeSelectionMetadata = "botModeSelectionMetadata";
            }
            if (m.botQuotaMetadata != null && m.hasOwnProperty("botQuotaMetadata")) {
                d.botQuotaMetadata = $root.WAAICommon.BotQuotaMetadata.toObject(m.botQuotaMetadata, o);
                if (o.oneofs)
                    d._botQuotaMetadata = "botQuotaMetadata";
            }
            if (m.botAgeCollectionMetadata != null && m.hasOwnProperty("botAgeCollectionMetadata")) {
                d.botAgeCollectionMetadata = $root.WAAICommon.BotAgeCollectionMetadata.toObject(m.botAgeCollectionMetadata, o);
                if (o.oneofs)
                    d._botAgeCollectionMetadata = "botAgeCollectionMetadata";
            }
            if (m.conversationStarterPromptID != null && m.hasOwnProperty("conversationStarterPromptID")) {
                d.conversationStarterPromptID = m.conversationStarterPromptID;
                if (o.oneofs)
                    d._conversationStarterPromptID = "conversationStarterPromptID";
            }
            if (m.botResponseID != null && m.hasOwnProperty("botResponseID")) {
                d.botResponseID = m.botResponseID;
                if (o.oneofs)
                    d._botResponseID = "botResponseID";
            }
            if (m.verificationMetadata != null && m.hasOwnProperty("verificationMetadata")) {
                d.verificationMetadata = $root.WAAICommon.BotSignatureVerificationMetadata.toObject(m.verificationMetadata, o);
                if (o.oneofs)
                    d._verificationMetadata = "verificationMetadata";
            }
            if (m.unifiedResponseMutation != null && m.hasOwnProperty("unifiedResponseMutation")) {
                d.unifiedResponseMutation = $root.WAAICommon.BotUnifiedResponseMutation.toObject(m.unifiedResponseMutation, o);
                if (o.oneofs)
                    d._unifiedResponseMutation = "unifiedResponseMutation";
            }
            if (m.botMessageOriginMetadata != null && m.hasOwnProperty("botMessageOriginMetadata")) {
                d.botMessageOriginMetadata = $root.WAAICommon.BotMessageOriginMetadata.toObject(m.botMessageOriginMetadata, o);
                if (o.oneofs)
                    d._botMessageOriginMetadata = "botMessageOriginMetadata";
            }
            if (m.inThreadSurveyMetadata != null && m.hasOwnProperty("inThreadSurveyMetadata")) {
                d.inThreadSurveyMetadata = $root.WAAICommon.InThreadSurveyMetadata.toObject(m.inThreadSurveyMetadata, o);
                if (o.oneofs)
                    d._inThreadSurveyMetadata = "inThreadSurveyMetadata";
            }
            if (m.botThreadInfo != null && m.hasOwnProperty("botThreadInfo")) {
                d.botThreadInfo = $root.WAAICommon.AIThreadInfo.toObject(m.botThreadInfo, o);
                if (o.oneofs)
                    d._botThreadInfo = "botThreadInfo";
            }
            if (m.regenerateMetadata != null && m.hasOwnProperty("regenerateMetadata")) {
                d.regenerateMetadata = $root.WAAICommon.AIRegenerateMetadata.toObject(m.regenerateMetadata, o);
                if (o.oneofs)
                    d._regenerateMetadata = "regenerateMetadata";
            }
            if (m.sessionTransparencyMetadata != null && m.hasOwnProperty("sessionTransparencyMetadata")) {
                d.sessionTransparencyMetadata = $root.WAAICommon.SessionTransparencyMetadata.toObject(m.sessionTransparencyMetadata, o);
                if (o.oneofs)
                    d._sessionTransparencyMetadata = "sessionTransparencyMetadata";
            }
            if (m.botDocumentMessageMetadata != null && m.hasOwnProperty("botDocumentMessageMetadata")) {
                d.botDocumentMessageMetadata = $root.WAAICommon.BotDocumentMessageMetadata.toObject(m.botDocumentMessageMetadata, o);
                if (o.oneofs)
                    d._botDocumentMessageMetadata = "botDocumentMessageMetadata";
            }
            if (m.botGroupMetadata != null && m.hasOwnProperty("botGroupMetadata")) {
                d.botGroupMetadata = $root.WAAICommon.BotGroupMetadata.toObject(m.botGroupMetadata, o);
                if (o.oneofs)
                    d._botGroupMetadata = "botGroupMetadata";
            }
            if (m.botRenderingConfigMetadata != null && m.hasOwnProperty("botRenderingConfigMetadata")) {
                d.botRenderingConfigMetadata = $root.WAAICommon.BotRenderingConfigMetadata.toObject(m.botRenderingConfigMetadata, o);
                if (o.oneofs)
                    d._botRenderingConfigMetadata = "botRenderingConfigMetadata";
            }
            if (m.botInfrastructureDiagnostics != null && m.hasOwnProperty("botInfrastructureDiagnostics")) {
                d.botInfrastructureDiagnostics = $root.WAAICommon.BotInfrastructureDiagnostics.toObject(m.botInfrastructureDiagnostics, o);
                if (o.oneofs)
                    d._botInfrastructureDiagnostics = "botInfrastructureDiagnostics";
            }
            if (m.aiMediaCollectionMetadata != null && m.hasOwnProperty("aiMediaCollectionMetadata")) {
                d.aiMediaCollectionMetadata = $root.WAAICommon.AIMediaCollectionMetadata.toObject(m.aiMediaCollectionMetadata, o);
                if (o.oneofs)
                    d._aiMediaCollectionMetadata = "aiMediaCollectionMetadata";
            }
            if (m.commandMetadata != null && m.hasOwnProperty("commandMetadata")) {
                d.commandMetadata = $root.WAAICommon.BotCommandMetadata.toObject(m.commandMetadata, o);
                if (o.oneofs)
                    d._commandMetadata = "commandMetadata";
            }
            if (m.resolvedToolCallMetadata != null && m.hasOwnProperty("resolvedToolCallMetadata")) {
                d.resolvedToolCallMetadata = $root.WAAICommon.BotResolvedToolCallMetadata.toObject(m.resolvedToolCallMetadata, o);
                if (o.oneofs)
                    d._resolvedToolCallMetadata = "resolvedToolCallMetadata";
            }
            if (m.subscriptionUpsellMetadata != null && m.hasOwnProperty("subscriptionUpsellMetadata")) {
                d.subscriptionUpsellMetadata = $root.WAAICommon.AISubscriptionUpsellMetadata.toObject(m.subscriptionUpsellMetadata, o);
                if (o.oneofs)
                    d._subscriptionUpsellMetadata = "subscriptionUpsellMetadata";
            }
            if (m.internalMetadata != null && m.hasOwnProperty("internalMetadata")) {
                d.internalMetadata = o.bytes === String ? $util.base64.encode(m.internalMetadata, 0, m.internalMetadata.length) : o.bytes === Array ? Array.prototype.slice.call(m.internalMetadata) : m.internalMetadata;
                if (o.oneofs)
                    d._internalMetadata = "internalMetadata";
            }
            return d;
        };

        BotMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMetadata";
        };

        return BotMetadata;
    })();

    WAAICommon.AISubscriptionUpsellMetadata = (function() {

        function AISubscriptionUpsellMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AISubscriptionUpsellMetadata.prototype.requestType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AISubscriptionUpsellMetadata.prototype, "_requestType", {
            get: $util.oneOfGetter($oneOfFields = ["requestType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AISubscriptionUpsellMetadata.create = function create(properties) {
            return new AISubscriptionUpsellMetadata(properties);
        };

        AISubscriptionUpsellMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.requestType != null && Object.hasOwnProperty.call(m, "requestType"))
                w.uint32(8).int32(m.requestType);
            return w;
        };

        AISubscriptionUpsellMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AISubscriptionUpsellMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.requestType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AISubscriptionUpsellMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AISubscriptionUpsellMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AISubscriptionUpsellMetadata();
            switch (d.requestType) {
            default:
                if (typeof d.requestType === "number") {
                    m.requestType = d.requestType;
                    break;
                }
                break;
            case "UNSPECIFIED":
            case 0:
                m.requestType = 0;
                break;
            case "THINK_HARD":
            case 1:
                m.requestType = 1;
                break;
            case "IMAGE_GEN":
            case 2:
                m.requestType = 2;
                break;
            case "VIDEO_GEN":
            case 3:
                m.requestType = 3;
                break;
            }
            return m;
        };

        AISubscriptionUpsellMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.requestType != null && m.hasOwnProperty("requestType")) {
                d.requestType = o.enums === String ? $root.WAAICommon.AISubscriptionRequestType[m.requestType] === undefined ? m.requestType : $root.WAAICommon.AISubscriptionRequestType[m.requestType] : m.requestType;
                if (o.oneofs)
                    d._requestType = "requestType";
            }
            return d;
        };

        AISubscriptionUpsellMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AISubscriptionUpsellMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AISubscriptionUpsellMetadata";
        };

        return AISubscriptionUpsellMetadata;
    })();

    WAAICommon.BotGroupMetadata = (function() {

        function BotGroupMetadata(p) {
            this.participantsMetadata = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotGroupMetadata.prototype.participantsMetadata = $util.emptyArray;

        BotGroupMetadata.create = function create(properties) {
            return new BotGroupMetadata(properties);
        };

        BotGroupMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.participantsMetadata != null && m.participantsMetadata.length) {
                for (var i = 0; i < m.participantsMetadata.length; ++i)
                    $root.WAAICommon.BotGroupParticipantMetadata.encode(m.participantsMetadata[i], w.uint32(10).fork()).ldelim();
            }
            return w;
        };

        BotGroupMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotGroupMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.participantsMetadata && m.participantsMetadata.length))
                            m.participantsMetadata = [];
                        m.participantsMetadata.push($root.WAAICommon.BotGroupParticipantMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotGroupMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotGroupMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotGroupMetadata();
            if (d.participantsMetadata) {
                if (!Array.isArray(d.participantsMetadata))
                    throw TypeError(".WAAICommon.BotGroupMetadata.participantsMetadata: array expected");
                m.participantsMetadata = [];
                for (var i = 0; i < d.participantsMetadata.length; ++i) {
                    if (typeof d.participantsMetadata[i] !== "object")
                        throw TypeError(".WAAICommon.BotGroupMetadata.participantsMetadata: object expected");
                    m.participantsMetadata[i] = $root.WAAICommon.BotGroupParticipantMetadata.fromObject(d.participantsMetadata[i], n + 1);
                }
            }
            return m;
        };

        BotGroupMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.participantsMetadata = [];
            }
            if (m.participantsMetadata && m.participantsMetadata.length) {
                d.participantsMetadata = [];
                for (var j = 0; j < m.participantsMetadata.length; ++j) {
                    d.participantsMetadata[j] = $root.WAAICommon.BotGroupParticipantMetadata.toObject(m.participantsMetadata[j], o);
                }
            }
            return d;
        };

        BotGroupMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotGroupMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotGroupMetadata";
        };

        return BotGroupMetadata;
    })();

    WAAICommon.BotRenderingConfigMetadata = (function() {

        function BotRenderingConfigMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotRenderingConfigMetadata.prototype.bloksVersioningID = null;
        BotRenderingConfigMetadata.prototype.pixelDensity = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotRenderingConfigMetadata.prototype, "_bloksVersioningID", {
            get: $util.oneOfGetter($oneOfFields = ["bloksVersioningID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotRenderingConfigMetadata.prototype, "_pixelDensity", {
            get: $util.oneOfGetter($oneOfFields = ["pixelDensity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotRenderingConfigMetadata.create = function create(properties) {
            return new BotRenderingConfigMetadata(properties);
        };

        BotRenderingConfigMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.bloksVersioningID != null && Object.hasOwnProperty.call(m, "bloksVersioningID"))
                w.uint32(10).string(m.bloksVersioningID);
            if (m.pixelDensity != null && Object.hasOwnProperty.call(m, "pixelDensity"))
                w.uint32(17).double(m.pixelDensity);
            return w;
        };

        BotRenderingConfigMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotRenderingConfigMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.bloksVersioningID = r.string();
                        break;
                    }
                case 2: {
                        m.pixelDensity = r.double();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotRenderingConfigMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotRenderingConfigMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotRenderingConfigMetadata();
            if (d.bloksVersioningID != null) {
                m.bloksVersioningID = String(d.bloksVersioningID);
            }
            if (d.pixelDensity != null) {
                m.pixelDensity = Number(d.pixelDensity);
            }
            return m;
        };

        BotRenderingConfigMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.bloksVersioningID != null && m.hasOwnProperty("bloksVersioningID")) {
                d.bloksVersioningID = m.bloksVersioningID;
                if (o.oneofs)
                    d._bloksVersioningID = "bloksVersioningID";
            }
            if (m.pixelDensity != null && m.hasOwnProperty("pixelDensity")) {
                d.pixelDensity = o.json && !isFinite(m.pixelDensity) ? String(m.pixelDensity) : m.pixelDensity;
                if (o.oneofs)
                    d._pixelDensity = "pixelDensity";
            }
            return d;
        };

        BotRenderingConfigMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotRenderingConfigMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotRenderingConfigMetadata";
        };

        return BotRenderingConfigMetadata;
    })();

    WAAICommon.BotGroupParticipantMetadata = (function() {

        function BotGroupParticipantMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotGroupParticipantMetadata.prototype.botFbid = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotGroupParticipantMetadata.prototype, "_botFbid", {
            get: $util.oneOfGetter($oneOfFields = ["botFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotGroupParticipantMetadata.create = function create(properties) {
            return new BotGroupParticipantMetadata(properties);
        };

        BotGroupParticipantMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.botFbid != null && Object.hasOwnProperty.call(m, "botFbid"))
                w.uint32(10).string(m.botFbid);
            return w;
        };

        BotGroupParticipantMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotGroupParticipantMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.botFbid = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotGroupParticipantMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotGroupParticipantMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotGroupParticipantMetadata();
            if (d.botFbid != null) {
                m.botFbid = String(d.botFbid);
            }
            return m;
        };

        BotGroupParticipantMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.botFbid != null && m.hasOwnProperty("botFbid")) {
                d.botFbid = m.botFbid;
                if (o.oneofs)
                    d._botFbid = "botFbid";
            }
            return d;
        };

        BotGroupParticipantMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotGroupParticipantMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotGroupParticipantMetadata";
        };

        return BotGroupParticipantMetadata;
    })();

    WAAICommon.ForwardedAIBotMessageInfo = (function() {

        function ForwardedAIBotMessageInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ForwardedAIBotMessageInfo.prototype.botName = null;
        ForwardedAIBotMessageInfo.prototype.botJID = null;
        ForwardedAIBotMessageInfo.prototype.creatorName = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botName", {
            get: $util.oneOfGetter($oneOfFields = ["botName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botJID", {
            get: $util.oneOfGetter($oneOfFields = ["botJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_creatorName", {
            get: $util.oneOfGetter($oneOfFields = ["creatorName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ForwardedAIBotMessageInfo.create = function create(properties) {
            return new ForwardedAIBotMessageInfo(properties);
        };

        ForwardedAIBotMessageInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.botName != null && Object.hasOwnProperty.call(m, "botName"))
                w.uint32(10).string(m.botName);
            if (m.botJID != null && Object.hasOwnProperty.call(m, "botJID"))
                w.uint32(18).string(m.botJID);
            if (m.creatorName != null && Object.hasOwnProperty.call(m, "creatorName"))
                w.uint32(26).string(m.creatorName);
            return w;
        };

        ForwardedAIBotMessageInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.ForwardedAIBotMessageInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.botName = r.string();
                        break;
                    }
                case 2: {
                        m.botJID = r.string();
                        break;
                    }
                case 3: {
                        m.creatorName = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ForwardedAIBotMessageInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.ForwardedAIBotMessageInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.ForwardedAIBotMessageInfo();
            if (d.botName != null) {
                m.botName = String(d.botName);
            }
            if (d.botJID != null) {
                m.botJID = String(d.botJID);
            }
            if (d.creatorName != null) {
                m.creatorName = String(d.creatorName);
            }
            return m;
        };

        ForwardedAIBotMessageInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.botName != null && m.hasOwnProperty("botName")) {
                d.botName = m.botName;
                if (o.oneofs)
                    d._botName = "botName";
            }
            if (m.botJID != null && m.hasOwnProperty("botJID")) {
                d.botJID = m.botJID;
                if (o.oneofs)
                    d._botJID = "botJID";
            }
            if (m.creatorName != null && m.hasOwnProperty("creatorName")) {
                d.creatorName = m.creatorName;
                if (o.oneofs)
                    d._creatorName = "creatorName";
            }
            return d;
        };

        ForwardedAIBotMessageInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ForwardedAIBotMessageInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.ForwardedAIBotMessageInfo";
        };

        return ForwardedAIBotMessageInfo;
    })();

    WAAICommon.BotMessageSharingInfo = (function() {

        function BotMessageSharingInfo(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotMessageSharingInfo.prototype.botEntryPointOrigin = null;
        BotMessageSharingInfo.prototype.forwardScore = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageSharingInfo.prototype, "_botEntryPointOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["botEntryPointOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotMessageSharingInfo.prototype, "_forwardScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMessageSharingInfo.create = function create(properties) {
            return new BotMessageSharingInfo(properties);
        };

        BotMessageSharingInfo.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.botEntryPointOrigin != null && Object.hasOwnProperty.call(m, "botEntryPointOrigin"))
                w.uint32(8).int32(m.botEntryPointOrigin);
            if (m.forwardScore != null && Object.hasOwnProperty.call(m, "forwardScore"))
                w.uint32(16).uint32(m.forwardScore);
            return w;
        };

        BotMessageSharingInfo.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotMessageSharingInfo();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.botEntryPointOrigin = r.int32();
                        break;
                    }
                case 2: {
                        m.forwardScore = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotMessageSharingInfo.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotMessageSharingInfo)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotMessageSharingInfo();
            switch (d.botEntryPointOrigin) {
            default:
                if (typeof d.botEntryPointOrigin === "number") {
                    m.botEntryPointOrigin = d.botEntryPointOrigin;
                    break;
                }
                break;
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                m.botEntryPointOrigin = 0;
                break;
            case "FAVICON":
            case 1:
                m.botEntryPointOrigin = 1;
                break;
            case "CHATLIST":
            case 2:
                m.botEntryPointOrigin = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                m.botEntryPointOrigin = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                m.botEntryPointOrigin = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                m.botEntryPointOrigin = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                m.botEntryPointOrigin = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                m.botEntryPointOrigin = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                m.botEntryPointOrigin = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                m.botEntryPointOrigin = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                m.botEntryPointOrigin = 10;
                break;
            case "AISTUDIO":
            case 11:
                m.botEntryPointOrigin = 11;
                break;
            case "DEEPLINK":
            case 12:
                m.botEntryPointOrigin = 12;
                break;
            case "NOTIFICATION":
            case 13:
                m.botEntryPointOrigin = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                m.botEntryPointOrigin = 14;
                break;
            case "FORWARD":
            case 15:
                m.botEntryPointOrigin = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                m.botEntryPointOrigin = 16;
                break;
            case "FF_FAMILY":
            case 17:
                m.botEntryPointOrigin = 17;
                break;
            case "AI_TAB":
            case 18:
                m.botEntryPointOrigin = 18;
                break;
            case "AI_HOME":
            case 19:
                m.botEntryPointOrigin = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                m.botEntryPointOrigin = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                m.botEntryPointOrigin = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                m.botEntryPointOrigin = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                m.botEntryPointOrigin = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                m.botEntryPointOrigin = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                m.botEntryPointOrigin = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                m.botEntryPointOrigin = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                m.botEntryPointOrigin = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                m.botEntryPointOrigin = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                m.botEntryPointOrigin = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                m.botEntryPointOrigin = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                m.botEntryPointOrigin = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                m.botEntryPointOrigin = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                m.botEntryPointOrigin = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                m.botEntryPointOrigin = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                m.botEntryPointOrigin = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                m.botEntryPointOrigin = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                m.botEntryPointOrigin = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                m.botEntryPointOrigin = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                m.botEntryPointOrigin = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                m.botEntryPointOrigin = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                m.botEntryPointOrigin = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                m.botEntryPointOrigin = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                m.botEntryPointOrigin = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                m.botEntryPointOrigin = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                m.botEntryPointOrigin = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                m.botEntryPointOrigin = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                m.botEntryPointOrigin = 56;
                break;
            }
            if (d.forwardScore != null) {
                m.forwardScore = d.forwardScore >>> 0;
            }
            return m;
        };

        BotMessageSharingInfo.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.botEntryPointOrigin != null && m.hasOwnProperty("botEntryPointOrigin")) {
                d.botEntryPointOrigin = o.enums === String ? $root.WAAICommon.BotMetricsEntryPoint[m.botEntryPointOrigin] === undefined ? m.botEntryPointOrigin : $root.WAAICommon.BotMetricsEntryPoint[m.botEntryPointOrigin] : m.botEntryPointOrigin;
                if (o.oneofs)
                    d._botEntryPointOrigin = "botEntryPointOrigin";
            }
            if (m.forwardScore != null && m.hasOwnProperty("forwardScore")) {
                d.forwardScore = m.forwardScore;
                if (o.oneofs)
                    d._forwardScore = "forwardScore";
            }
            return d;
        };

        BotMessageSharingInfo.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageSharingInfo.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotMessageSharingInfo";
        };

        return BotMessageSharingInfo;
    })();

    WAAICommon.AIRichResponseUnifiedResponse = (function() {

        function AIRichResponseUnifiedResponse(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseUnifiedResponse.prototype.data = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseUnifiedResponse.prototype, "_data", {
            get: $util.oneOfGetter($oneOfFields = ["data"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseUnifiedResponse.create = function create(properties) {
            return new AIRichResponseUnifiedResponse(properties);
        };

        AIRichResponseUnifiedResponse.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.data != null && Object.hasOwnProperty.call(m, "data"))
                w.uint32(10).bytes(m.data);
            return w;
        };

        AIRichResponseUnifiedResponse.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIRichResponseUnifiedResponse();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.data = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseUnifiedResponse.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIRichResponseUnifiedResponse)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIRichResponseUnifiedResponse();
            if (d.data != null) {
                if (typeof d.data === "string")
                    $util.base64.decode(d.data, m.data = $util.newBuffer($util.base64.length(d.data)), 0);
                else if (d.data.length >= 0)
                    m.data = d.data;
            }
            return m;
        };

        AIRichResponseUnifiedResponse.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.data != null && m.hasOwnProperty("data")) {
                d.data = o.bytes === String ? $util.base64.encode(m.data, 0, m.data.length) : o.bytes === Array ? Array.prototype.slice.call(m.data) : m.data;
                if (o.oneofs)
                    d._data = "data";
            }
            return d;
        };

        AIRichResponseUnifiedResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseUnifiedResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIRichResponseUnifiedResponse";
        };

        return AIRichResponseUnifiedResponse;
    })();

    WAAICommon.AIRegenerateMetadata = (function() {

        function AIRegenerateMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRegenerateMetadata.prototype.messageKey = null;
        AIRegenerateMetadata.prototype.responseTimestampMS = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRegenerateMetadata.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRegenerateMetadata.prototype, "_responseTimestampMS", {
            get: $util.oneOfGetter($oneOfFields = ["responseTimestampMS"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRegenerateMetadata.create = function create(properties) {
            return new AIRegenerateMetadata(properties);
        };

        AIRegenerateMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageKey != null && Object.hasOwnProperty.call(m, "messageKey"))
                WACommon.MessageKey.encode(m.messageKey, w.uint32(10).fork()).ldelim();
            if (m.responseTimestampMS != null && Object.hasOwnProperty.call(m, "responseTimestampMS"))
                w.uint32(16).int64(m.responseTimestampMS);
            return w;
        };

        AIRegenerateMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.AIRegenerateMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageKey = WACommon.MessageKey.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.responseTimestampMS = r.int64();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRegenerateMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.AIRegenerateMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.AIRegenerateMetadata();
            if (d.messageKey != null) {
                if (typeof d.messageKey !== "object")
                    throw TypeError(".WAAICommon.AIRegenerateMetadata.messageKey: object expected");
                m.messageKey = WACommon.MessageKey.fromObject(d.messageKey, n + 1);
            }
            if (d.responseTimestampMS != null) {
                if ($util.Long)
                    (m.responseTimestampMS = $util.Long.fromValue(d.responseTimestampMS)).unsigned = false;
                else if (typeof d.responseTimestampMS === "string")
                    m.responseTimestampMS = parseInt(d.responseTimestampMS, 10);
                else if (typeof d.responseTimestampMS === "number")
                    m.responseTimestampMS = d.responseTimestampMS;
                else if (typeof d.responseTimestampMS === "object")
                    m.responseTimestampMS = new $util.LongBits(d.responseTimestampMS.low >>> 0, d.responseTimestampMS.high >>> 0).toNumber();
            }
            return m;
        };

        AIRegenerateMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageKey != null && m.hasOwnProperty("messageKey")) {
                d.messageKey = WACommon.MessageKey.toObject(m.messageKey, o);
                if (o.oneofs)
                    d._messageKey = "messageKey";
            }
            if (m.responseTimestampMS != null && m.hasOwnProperty("responseTimestampMS")) {
                if (typeof m.responseTimestampMS === "number")
                    d.responseTimestampMS = o.longs === String ? String(m.responseTimestampMS) : m.responseTimestampMS;
                else
                    d.responseTimestampMS = o.longs === String ? $util.Long.prototype.toString.call(m.responseTimestampMS) : o.longs === Number ? new $util.LongBits(m.responseTimestampMS.low >>> 0, m.responseTimestampMS.high >>> 0).toNumber() : m.responseTimestampMS;
                if (o.oneofs)
                    d._responseTimestampMS = "responseTimestampMS";
            }
            return d;
        };

        AIRegenerateMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRegenerateMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.AIRegenerateMetadata";
        };

        return AIRegenerateMetadata;
    })();

    WAAICommon.SessionTransparencyMetadata = (function() {

        function SessionTransparencyMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        SessionTransparencyMetadata.prototype.disclaimerText = null;
        SessionTransparencyMetadata.prototype.hcaID = null;
        SessionTransparencyMetadata.prototype.sessionTransparencyType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_disclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_hcaID", {
            get: $util.oneOfGetter($oneOfFields = ["hcaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(SessionTransparencyMetadata.prototype, "_sessionTransparencyType", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SessionTransparencyMetadata.create = function create(properties) {
            return new SessionTransparencyMetadata(properties);
        };

        SessionTransparencyMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.disclaimerText != null && Object.hasOwnProperty.call(m, "disclaimerText"))
                w.uint32(10).string(m.disclaimerText);
            if (m.hcaID != null && Object.hasOwnProperty.call(m, "hcaID"))
                w.uint32(18).string(m.hcaID);
            if (m.sessionTransparencyType != null && Object.hasOwnProperty.call(m, "sessionTransparencyType"))
                w.uint32(24).int32(m.sessionTransparencyType);
            return w;
        };

        SessionTransparencyMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.SessionTransparencyMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.disclaimerText = r.string();
                        break;
                    }
                case 2: {
                        m.hcaID = r.string();
                        break;
                    }
                case 3: {
                        m.sessionTransparencyType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        SessionTransparencyMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.SessionTransparencyMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.SessionTransparencyMetadata();
            if (d.disclaimerText != null) {
                m.disclaimerText = String(d.disclaimerText);
            }
            if (d.hcaID != null) {
                m.hcaID = String(d.hcaID);
            }
            switch (d.sessionTransparencyType) {
            default:
                if (typeof d.sessionTransparencyType === "number") {
                    m.sessionTransparencyType = d.sessionTransparencyType;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                m.sessionTransparencyType = 0;
                break;
            case "NY_AI_SAFETY_DISCLAIMER":
            case 1:
                m.sessionTransparencyType = 1;
                break;
            }
            return m;
        };

        SessionTransparencyMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.disclaimerText != null && m.hasOwnProperty("disclaimerText")) {
                d.disclaimerText = m.disclaimerText;
                if (o.oneofs)
                    d._disclaimerText = "disclaimerText";
            }
            if (m.hcaID != null && m.hasOwnProperty("hcaID")) {
                d.hcaID = m.hcaID;
                if (o.oneofs)
                    d._hcaID = "hcaID";
            }
            if (m.sessionTransparencyType != null && m.hasOwnProperty("sessionTransparencyType")) {
                d.sessionTransparencyType = o.enums === String ? $root.WAAICommon.SessionTransparencyType[m.sessionTransparencyType] === undefined ? m.sessionTransparencyType : $root.WAAICommon.SessionTransparencyType[m.sessionTransparencyType] : m.sessionTransparencyType;
                if (o.oneofs)
                    d._sessionTransparencyType = "sessionTransparencyType";
            }
            return d;
        };

        SessionTransparencyMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        SessionTransparencyMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.SessionTransparencyMetadata";
        };

        return SessionTransparencyMetadata;
    })();

    WAAICommon.BotAgentMetadata = (function() {

        function BotAgentMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotAgentMetadata.prototype.deepLinkMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgentMetadata.prototype, "_deepLinkMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deepLinkMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgentMetadata.create = function create(properties) {
            return new BotAgentMetadata(properties);
        };

        BotAgentMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.deepLinkMetadata != null && Object.hasOwnProperty.call(m, "deepLinkMetadata"))
                $root.WAAICommon.BotAgentDeepLinkMetadata.encode(m.deepLinkMetadata, w.uint32(10).fork()).ldelim();
            return w;
        };

        BotAgentMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotAgentMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.deepLinkMetadata = $root.WAAICommon.BotAgentDeepLinkMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotAgentMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotAgentMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotAgentMetadata();
            if (d.deepLinkMetadata != null) {
                if (typeof d.deepLinkMetadata !== "object")
                    throw TypeError(".WAAICommon.BotAgentMetadata.deepLinkMetadata: object expected");
                m.deepLinkMetadata = $root.WAAICommon.BotAgentDeepLinkMetadata.fromObject(d.deepLinkMetadata, n + 1);
            }
            return m;
        };

        BotAgentMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.deepLinkMetadata != null && m.hasOwnProperty("deepLinkMetadata")) {
                d.deepLinkMetadata = $root.WAAICommon.BotAgentDeepLinkMetadata.toObject(m.deepLinkMetadata, o);
                if (o.oneofs)
                    d._deepLinkMetadata = "deepLinkMetadata";
            }
            return d;
        };

        BotAgentMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgentMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotAgentMetadata";
        };

        return BotAgentMetadata;
    })();

    WAAICommon.BotAgentDeepLinkMetadata = (function() {

        function BotAgentDeepLinkMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        BotAgentDeepLinkMetadata.prototype.token = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(BotAgentDeepLinkMetadata.prototype, "_token", {
            get: $util.oneOfGetter($oneOfFields = ["token"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgentDeepLinkMetadata.create = function create(properties) {
            return new BotAgentDeepLinkMetadata(properties);
        };

        BotAgentDeepLinkMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.token != null && Object.hasOwnProperty.call(m, "token"))
                w.uint32(10).string(m.token);
            return w;
        };

        BotAgentDeepLinkMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommon.BotAgentDeepLinkMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.token = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        BotAgentDeepLinkMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommon.BotAgentDeepLinkMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommon.BotAgentDeepLinkMetadata();
            if (d.token != null) {
                m.token = String(d.token);
            }
            return m;
        };

        BotAgentDeepLinkMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.token != null && m.hasOwnProperty("token")) {
                d.token = m.token;
                if (o.oneofs)
                    d._token = "token";
            }
            return d;
        };

        BotAgentDeepLinkMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgentDeepLinkMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommon.BotAgentDeepLinkMetadata";
        };

        return BotAgentDeepLinkMetadata;
    })();

    return WAAICommon;
})();

export const WACommon = WACommon = (() => {

    const WACommon = {};

    WACommon.LimitSharing = (function() {

        function LimitSharing(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        LimitSharing.prototype.sharingLimited = null;
        LimitSharing.prototype.trigger = null;
        LimitSharing.prototype.limitSharingSettingTimestamp = null;
        LimitSharing.prototype.initiatedByMe = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_sharingLimited", {
            get: $util.oneOfGetter($oneOfFields = ["sharingLimited"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_limitSharingSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(LimitSharing.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LimitSharing.create = function create(properties) {
            return new LimitSharing(properties);
        };

        LimitSharing.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.sharingLimited != null && Object.hasOwnProperty.call(m, "sharingLimited"))
                w.uint32(8).bool(m.sharingLimited);
            if (m.trigger != null && Object.hasOwnProperty.call(m, "trigger"))
                w.uint32(16).int32(m.trigger);
            if (m.limitSharingSettingTimestamp != null && Object.hasOwnProperty.call(m, "limitSharingSettingTimestamp"))
                w.uint32(24).int64(m.limitSharingSettingTimestamp);
            if (m.initiatedByMe != null && Object.hasOwnProperty.call(m, "initiatedByMe"))
                w.uint32(32).bool(m.initiatedByMe);
            return w;
        };

        LimitSharing.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WACommon.LimitSharing();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.sharingLimited = r.bool();
                        break;
                    }
                case 2: {
                        m.trigger = r.int32();
                        break;
                    }
                case 3: {
                        m.limitSharingSettingTimestamp = r.int64();
                        break;
                    }
                case 4: {
                        m.initiatedByMe = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        LimitSharing.fromObject = function fromObject(d, n) {
            if (d instanceof WACommon.LimitSharing)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WACommon.LimitSharing();
            if (d.sharingLimited != null) {
                m.sharingLimited = Boolean(d.sharingLimited);
            }
            switch (d.trigger) {
            default:
                if (typeof d.trigger === "number") {
                    m.trigger = d.trigger;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                m.trigger = 1;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 2:
                m.trigger = 2;
                break;
            case "UNKNOWN_GROUP":
            case 3:
                m.trigger = 3;
                break;
            }
            if (d.limitSharingSettingTimestamp != null) {
                if ($util.Long)
                    (m.limitSharingSettingTimestamp = $util.Long.fromValue(d.limitSharingSettingTimestamp)).unsigned = false;
                else if (typeof d.limitSharingSettingTimestamp === "string")
                    m.limitSharingSettingTimestamp = parseInt(d.limitSharingSettingTimestamp, 10);
                else if (typeof d.limitSharingSettingTimestamp === "number")
                    m.limitSharingSettingTimestamp = d.limitSharingSettingTimestamp;
                else if (typeof d.limitSharingSettingTimestamp === "object")
                    m.limitSharingSettingTimestamp = new $util.LongBits(d.limitSharingSettingTimestamp.low >>> 0, d.limitSharingSettingTimestamp.high >>> 0).toNumber();
            }
            if (d.initiatedByMe != null) {
                m.initiatedByMe = Boolean(d.initiatedByMe);
            }
            return m;
        };

        LimitSharing.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.sharingLimited != null && m.hasOwnProperty("sharingLimited")) {
                d.sharingLimited = m.sharingLimited;
                if (o.oneofs)
                    d._sharingLimited = "sharingLimited";
            }
            if (m.trigger != null && m.hasOwnProperty("trigger")) {
                d.trigger = o.enums === String ? WACommon.LimitSharing.TriggerType[m.trigger] === undefined ? m.trigger : WACommon.LimitSharing.TriggerType[m.trigger] : m.trigger;
                if (o.oneofs)
                    d._trigger = "trigger";
            }
            if (m.limitSharingSettingTimestamp != null && m.hasOwnProperty("limitSharingSettingTimestamp")) {
                if (typeof m.limitSharingSettingTimestamp === "number")
                    d.limitSharingSettingTimestamp = o.longs === String ? String(m.limitSharingSettingTimestamp) : m.limitSharingSettingTimestamp;
                else
                    d.limitSharingSettingTimestamp = o.longs === String ? $util.Long.prototype.toString.call(m.limitSharingSettingTimestamp) : o.longs === Number ? new $util.LongBits(m.limitSharingSettingTimestamp.low >>> 0, m.limitSharingSettingTimestamp.high >>> 0).toNumber() : m.limitSharingSettingTimestamp;
                if (o.oneofs)
                    d._limitSharingSettingTimestamp = "limitSharingSettingTimestamp";
            }
            if (m.initiatedByMe != null && m.hasOwnProperty("initiatedByMe")) {
                d.initiatedByMe = m.initiatedByMe;
                if (o.oneofs)
                    d._initiatedByMe = "initiatedByMe";
            }
            return d;
        };

        LimitSharing.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        LimitSharing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACommon.LimitSharing";
        };

        LimitSharing.TriggerType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "BIZ_SUPPORTS_FB_HOSTING"] = 2;
            values[valuesById[3] = "UNKNOWN_GROUP"] = 3;
            return values;
        })();

        return LimitSharing;
    })();

    WACommon.MessageKey = (function() {

        function MessageKey(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MessageKey.prototype.remoteJID = null;
        MessageKey.prototype.fromMe = null;
        MessageKey.prototype.ID = null;
        MessageKey.prototype.participant = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_remoteJID", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_fromMe", {
            get: $util.oneOfGetter($oneOfFields = ["fromMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_ID", {
            get: $util.oneOfGetter($oneOfFields = ["ID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MessageKey.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageKey.create = function create(properties) {
            return new MessageKey(properties);
        };

        MessageKey.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.remoteJID != null && Object.hasOwnProperty.call(m, "remoteJID"))
                w.uint32(10).string(m.remoteJID);
            if (m.fromMe != null && Object.hasOwnProperty.call(m, "fromMe"))
                w.uint32(16).bool(m.fromMe);
            if (m.ID != null && Object.hasOwnProperty.call(m, "ID"))
                w.uint32(26).string(m.ID);
            if (m.participant != null && Object.hasOwnProperty.call(m, "participant"))
                w.uint32(34).string(m.participant);
            return w;
        };

        MessageKey.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new WACommon.MessageKey();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.remoteJID = r.string();
                        break;
                    }
                case 2: {
                        m.fromMe = r.bool();
                        break;
                    }
                case 3: {
                        m.ID = r.string();
                        break;
                    }
                case 4: {
                        m.participant = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MessageKey.fromObject = function fromObject(d, n) {
            if (d instanceof WACommon.MessageKey)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new WACommon.MessageKey();
            if (d.remoteJID != null) {
                m.remoteJID = String(d.remoteJID);
            }
            if (d.fromMe != null) {
                m.fromMe = Boolean(d.fromMe);
            }
            if (d.ID != null) {
                m.ID = String(d.ID);
            }
            if (d.participant != null) {
                m.participant = String(d.participant);
            }
            return m;
        };

        MessageKey.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.remoteJID != null && m.hasOwnProperty("remoteJID")) {
                d.remoteJID = m.remoteJID;
                if (o.oneofs)
                    d._remoteJID = "remoteJID";
            }
            if (m.fromMe != null && m.hasOwnProperty("fromMe")) {
                d.fromMe = m.fromMe;
                if (o.oneofs)
                    d._fromMe = "fromMe";
            }
            if (m.ID != null && m.hasOwnProperty("ID")) {
                d.ID = m.ID;
                if (o.oneofs)
                    d._ID = "ID";
            }
            if (m.participant != null && m.hasOwnProperty("participant")) {
                d.participant = m.participant;
                if (o.oneofs)
                    d._participant = "participant";
            }
            return d;
        };

        MessageKey.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACommon.MessageKey";
        };

        return MessageKey;
    })();

    return WACommon;
})();

export const WAAICommonDeprecated = $root.WAAICommonDeprecated = (() => {

    const WAAICommonDeprecated = {};

    WAAICommonDeprecated.AIRichResponseMessageType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_TYPE_STANDARD"] = 1;
        return values;
    })();

    WAAICommonDeprecated.AIRichResponseSubMessageType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_GRID_IMAGE"] = 1;
        values[valuesById[2] = "AI_RICH_RESPONSE_TEXT"] = 2;
        values[valuesById[3] = "AI_RICH_RESPONSE_INLINE_IMAGE"] = 3;
        values[valuesById[4] = "AI_RICH_RESPONSE_TABLE"] = 4;
        values[valuesById[5] = "AI_RICH_RESPONSE_CODE"] = 5;
        values[valuesById[6] = "AI_RICH_RESPONSE_DYNAMIC"] = 6;
        values[valuesById[7] = "AI_RICH_RESPONSE_MAP"] = 7;
        values[valuesById[8] = "AI_RICH_RESPONSE_LATEX"] = 8;
        values[valuesById[9] = "AI_RICH_RESPONSE_CONTENT_ITEMS"] = 9;
        return values;
    })();

    WAAICommonDeprecated.AIRichResponseInlineImageMetadata = (function() {

        function AIRichResponseInlineImageMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseInlineImageMetadata.prototype.imageURL = null;
        AIRichResponseInlineImageMetadata.prototype.imageText = null;
        AIRichResponseInlineImageMetadata.prototype.alignment = null;
        AIRichResponseInlineImageMetadata.prototype.tapLinkURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageURL", {
            get: $util.oneOfGetter($oneOfFields = ["imageURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageText", {
            get: $util.oneOfGetter($oneOfFields = ["imageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_alignment", {
            get: $util.oneOfGetter($oneOfFields = ["alignment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_tapLinkURL", {
            get: $util.oneOfGetter($oneOfFields = ["tapLinkURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseInlineImageMetadata.create = function create(properties) {
            return new AIRichResponseInlineImageMetadata(properties);
        };

        AIRichResponseInlineImageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.imageURL != null && Object.hasOwnProperty.call(m, "imageURL"))
                $root.WAAICommonDeprecated.AIRichResponseImageURL.encode(m.imageURL, w.uint32(10).fork()).ldelim();
            if (m.imageText != null && Object.hasOwnProperty.call(m, "imageText"))
                w.uint32(18).string(m.imageText);
            if (m.alignment != null && Object.hasOwnProperty.call(m, "alignment"))
                w.uint32(24).int32(m.alignment);
            if (m.tapLinkURL != null && Object.hasOwnProperty.call(m, "tapLinkURL"))
                w.uint32(34).string(m.tapLinkURL);
            return w;
        };

        AIRichResponseInlineImageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.imageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        m.imageText = r.string();
                        break;
                    }
                case 3: {
                        m.alignment = r.int32();
                        break;
                    }
                case 4: {
                        m.tapLinkURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseInlineImageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata();
            if (d.imageURL != null) {
                if (typeof d.imageURL !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseInlineImageMetadata.imageURL: object expected");
                m.imageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.fromObject(d.imageURL, n + 1);
            }
            if (d.imageText != null) {
                m.imageText = String(d.imageText);
            }
            switch (d.alignment) {
            default:
                if (typeof d.alignment === "number") {
                    m.alignment = d.alignment;
                    break;
                }
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED":
            case 0:
                m.alignment = 0;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED":
            case 1:
                m.alignment = 1;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED":
            case 2:
                m.alignment = 2;
                break;
            }
            if (d.tapLinkURL != null) {
                m.tapLinkURL = String(d.tapLinkURL);
            }
            return m;
        };

        AIRichResponseInlineImageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.imageURL != null && m.hasOwnProperty("imageURL")) {
                d.imageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.toObject(m.imageURL, o);
                if (o.oneofs)
                    d._imageURL = "imageURL";
            }
            if (m.imageText != null && m.hasOwnProperty("imageText")) {
                d.imageText = m.imageText;
                if (o.oneofs)
                    d._imageText = "imageText";
            }
            if (m.alignment != null && m.hasOwnProperty("alignment")) {
                d.alignment = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[m.alignment] === undefined ? m.alignment : $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[m.alignment] : m.alignment;
                if (o.oneofs)
                    d._alignment = "alignment";
            }
            if (m.tapLinkURL != null && m.hasOwnProperty("tapLinkURL")) {
                d.tapLinkURL = m.tapLinkURL;
                if (o.oneofs)
                    d._tapLinkURL = "tapLinkURL";
            }
            return d;
        };

        AIRichResponseInlineImageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseInlineImageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseInlineImageMetadata";
        };

        AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED"] = 2;
            return values;
        })();

        return AIRichResponseInlineImageMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseCodeMetadata = (function() {

        function AIRichResponseCodeMetadata(p) {
            this.codeBlocks = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseCodeMetadata.prototype.codeLanguage = null;
        AIRichResponseCodeMetadata.prototype.codeBlocks = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseCodeMetadata.prototype, "_codeLanguage", {
            get: $util.oneOfGetter($oneOfFields = ["codeLanguage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseCodeMetadata.create = function create(properties) {
            return new AIRichResponseCodeMetadata(properties);
        };

        AIRichResponseCodeMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.codeLanguage != null && Object.hasOwnProperty.call(m, "codeLanguage"))
                w.uint32(10).string(m.codeLanguage);
            if (m.codeBlocks != null && m.codeBlocks.length) {
                for (var i = 0; i < m.codeBlocks.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.encode(m.codeBlocks[i], w.uint32(18).fork()).ldelim();
            }
            return w;
        };

        AIRichResponseCodeMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseCodeMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.codeLanguage = r.string();
                        break;
                    }
                case 2: {
                        if (!(m.codeBlocks && m.codeBlocks.length))
                            m.codeBlocks = [];
                        m.codeBlocks.push($root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseCodeMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseCodeMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseCodeMetadata();
            if (d.codeLanguage != null) {
                m.codeLanguage = String(d.codeLanguage);
            }
            if (d.codeBlocks) {
                if (!Array.isArray(d.codeBlocks))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseCodeMetadata.codeBlocks: array expected");
                m.codeBlocks = [];
                for (var i = 0; i < d.codeBlocks.length; ++i) {
                    if (typeof d.codeBlocks[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseCodeMetadata.codeBlocks: object expected");
                    m.codeBlocks[i] = $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.fromObject(d.codeBlocks[i], n + 1);
                }
            }
            return m;
        };

        AIRichResponseCodeMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.codeBlocks = [];
            }
            if (m.codeLanguage != null && m.hasOwnProperty("codeLanguage")) {
                d.codeLanguage = m.codeLanguage;
                if (o.oneofs)
                    d._codeLanguage = "codeLanguage";
            }
            if (m.codeBlocks && m.codeBlocks.length) {
                d.codeBlocks = [];
                for (var j = 0; j < m.codeBlocks.length; ++j) {
                    d.codeBlocks[j] = $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.toObject(m.codeBlocks[j], o);
                }
            }
            return d;
        };

        AIRichResponseCodeMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseCodeMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseCodeMetadata";
        };

        AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD"] = 2;
            values[valuesById[3] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING"] = 3;
            values[valuesById[4] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER"] = 4;
            values[valuesById[5] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT"] = 5;
            return values;
        })();

        AIRichResponseCodeMetadata.AIRichResponseCodeBlock = (function() {

            function AIRichResponseCodeBlock(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseCodeBlock.prototype.highlightType = null;
            AIRichResponseCodeBlock.prototype.codeContent = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseCodeBlock.prototype, "_highlightType", {
                get: $util.oneOfGetter($oneOfFields = ["highlightType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseCodeBlock.prototype, "_codeContent", {
                get: $util.oneOfGetter($oneOfFields = ["codeContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseCodeBlock.create = function create(properties) {
                return new AIRichResponseCodeBlock(properties);
            };

            AIRichResponseCodeBlock.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.highlightType != null && Object.hasOwnProperty.call(m, "highlightType"))
                    w.uint32(8).int32(m.highlightType);
                if (m.codeContent != null && Object.hasOwnProperty.call(m, "codeContent"))
                    w.uint32(18).string(m.codeContent);
                return w;
            };

            AIRichResponseCodeBlock.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.highlightType = r.int32();
                            break;
                        }
                    case 2: {
                            m.codeContent = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseCodeBlock.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock();
                switch (d.highlightType) {
                default:
                    if (typeof d.highlightType === "number") {
                        m.highlightType = d.highlightType;
                        break;
                    }
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT":
                case 0:
                    m.highlightType = 0;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD":
                case 1:
                    m.highlightType = 1;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD":
                case 2:
                    m.highlightType = 2;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING":
                case 3:
                    m.highlightType = 3;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER":
                case 4:
                    m.highlightType = 4;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT":
                case 5:
                    m.highlightType = 5;
                    break;
                }
                if (d.codeContent != null) {
                    m.codeContent = String(d.codeContent);
                }
                return m;
            };

            AIRichResponseCodeBlock.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.highlightType != null && m.hasOwnProperty("highlightType")) {
                    d.highlightType = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[m.highlightType] === undefined ? m.highlightType : $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[m.highlightType] : m.highlightType;
                    if (o.oneofs)
                        d._highlightType = "highlightType";
                }
                if (m.codeContent != null && m.hasOwnProperty("codeContent")) {
                    d.codeContent = m.codeContent;
                    if (o.oneofs)
                        d._codeContent = "codeContent";
                }
                return d;
            };

            AIRichResponseCodeBlock.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseCodeBlock.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseCodeMetadata.AIRichResponseCodeBlock";
            };

            return AIRichResponseCodeBlock;
        })();

        return AIRichResponseCodeMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseDynamicMetadata = (function() {

        function AIRichResponseDynamicMetadata(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseDynamicMetadata.prototype.type = null;
        AIRichResponseDynamicMetadata.prototype.version = null;
        AIRichResponseDynamicMetadata.prototype.URL = null;
        AIRichResponseDynamicMetadata.prototype.loopCount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_URL", {
            get: $util.oneOfGetter($oneOfFields = ["URL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_loopCount", {
            get: $util.oneOfGetter($oneOfFields = ["loopCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseDynamicMetadata.create = function create(properties) {
            return new AIRichResponseDynamicMetadata(properties);
        };

        AIRichResponseDynamicMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.version != null && Object.hasOwnProperty.call(m, "version"))
                w.uint32(16).uint64(m.version);
            if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                w.uint32(26).string(m.URL);
            if (m.loopCount != null && Object.hasOwnProperty.call(m, "loopCount"))
                w.uint32(32).uint32(m.loopCount);
            return w;
        };

        AIRichResponseDynamicMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                case 2: {
                        m.version = r.uint64();
                        break;
                    }
                case 3: {
                        m.URL = r.string();
                        break;
                    }
                case 4: {
                        m.loopCount = r.uint32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseDynamicMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata();
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE":
            case 1:
                m.type = 1;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF":
            case 2:
                m.type = 2;
                break;
            }
            if (d.version != null) {
                if ($util.Long)
                    (m.version = $util.Long.fromValue(d.version)).unsigned = true;
                else if (typeof d.version === "string")
                    m.version = parseInt(d.version, 10);
                else if (typeof d.version === "number")
                    m.version = d.version;
                else if (typeof d.version === "object")
                    m.version = new $util.LongBits(d.version.low >>> 0, d.version.high >>> 0).toNumber(true);
            }
            if (d.URL != null) {
                m.URL = String(d.URL);
            }
            if (d.loopCount != null) {
                m.loopCount = d.loopCount >>> 0;
            }
            return m;
        };

        AIRichResponseDynamicMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[m.type] === undefined ? m.type : $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.version != null && m.hasOwnProperty("version")) {
                if (typeof m.version === "number")
                    d.version = o.longs === String ? String(m.version) : m.version;
                else
                    d.version = o.longs === String ? $util.Long.prototype.toString.call(m.version) : o.longs === Number ? new $util.LongBits(m.version.low >>> 0, m.version.high >>> 0).toNumber(true) : m.version;
                if (o.oneofs)
                    d._version = "version";
            }
            if (m.URL != null && m.hasOwnProperty("URL")) {
                d.URL = m.URL;
                if (o.oneofs)
                    d._URL = "URL";
            }
            if (m.loopCount != null && m.hasOwnProperty("loopCount")) {
                d.loopCount = m.loopCount;
                if (o.oneofs)
                    d._loopCount = "loopCount";
            }
            return d;
        };

        AIRichResponseDynamicMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseDynamicMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseDynamicMetadata";
        };

        AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF"] = 2;
            return values;
        })();

        return AIRichResponseDynamicMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseContentItemsMetadata = (function() {

        function AIRichResponseContentItemsMetadata(p) {
            this.itemsMetadata = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseContentItemsMetadata.prototype.itemsMetadata = $util.emptyArray;
        AIRichResponseContentItemsMetadata.prototype.contentType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseContentItemsMetadata.prototype, "_contentType", {
            get: $util.oneOfGetter($oneOfFields = ["contentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseContentItemsMetadata.create = function create(properties) {
            return new AIRichResponseContentItemsMetadata(properties);
        };

        AIRichResponseContentItemsMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.itemsMetadata != null && m.itemsMetadata.length) {
                for (var i = 0; i < m.itemsMetadata.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.encode(m.itemsMetadata[i], w.uint32(10).fork()).ldelim();
            }
            if (m.contentType != null && Object.hasOwnProperty.call(m, "contentType"))
                w.uint32(16).int32(m.contentType);
            return w;
        };

        AIRichResponseContentItemsMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.itemsMetadata && m.itemsMetadata.length))
                            m.itemsMetadata = [];
                        m.itemsMetadata.push($root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 2: {
                        m.contentType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseContentItemsMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata();
            if (d.itemsMetadata) {
                if (!Array.isArray(d.itemsMetadata))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseContentItemsMetadata.itemsMetadata: array expected");
                m.itemsMetadata = [];
                for (var i = 0; i < d.itemsMetadata.length; ++i) {
                    if (typeof d.itemsMetadata[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseContentItemsMetadata.itemsMetadata: object expected");
                    m.itemsMetadata[i] = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.fromObject(d.itemsMetadata[i], n + 1);
                }
            }
            switch (d.contentType) {
            default:
                if (typeof d.contentType === "number") {
                    m.contentType = d.contentType;
                    break;
                }
                break;
            case "DEFAULT":
            case 0:
                m.contentType = 0;
                break;
            case "CAROUSEL":
            case 1:
                m.contentType = 1;
                break;
            }
            return m;
        };

        AIRichResponseContentItemsMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.itemsMetadata = [];
            }
            if (m.itemsMetadata && m.itemsMetadata.length) {
                d.itemsMetadata = [];
                for (var j = 0; j < m.itemsMetadata.length; ++j) {
                    d.itemsMetadata[j] = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.toObject(m.itemsMetadata[j], o);
                }
            }
            if (m.contentType != null && m.hasOwnProperty("contentType")) {
                d.contentType = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.ContentType[m.contentType] === undefined ? m.contentType : $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.ContentType[m.contentType] : m.contentType;
                if (o.oneofs)
                    d._contentType = "contentType";
            }
            return d;
        };

        AIRichResponseContentItemsMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseContentItemsMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseContentItemsMetadata";
        };

        AIRichResponseContentItemsMetadata.ContentType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "CAROUSEL"] = 1;
            return values;
        })();

        AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata = (function() {

            function AIRichResponseContentItemMetadata(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseContentItemMetadata.prototype.reelItem = null;

            let $oneOfFields;

            Object.defineProperty(AIRichResponseContentItemMetadata.prototype, "aIRichResponseContentItem", {
                get: $util.oneOfGetter($oneOfFields = ["reelItem"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseContentItemMetadata.create = function create(properties) {
                return new AIRichResponseContentItemMetadata(properties);
            };

            AIRichResponseContentItemMetadata.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.reelItem != null && Object.hasOwnProperty.call(m, "reelItem"))
                    $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.encode(m.reelItem, w.uint32(10).fork()).ldelim();
                return w;
            };

            AIRichResponseContentItemMetadata.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.reelItem = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseContentItemMetadata.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata();
                if (d.reelItem != null) {
                    if (typeof d.reelItem !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.reelItem: object expected");
                    m.reelItem = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.fromObject(d.reelItem, n + 1);
                }
                return m;
            };

            AIRichResponseContentItemMetadata.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.reelItem != null && m.hasOwnProperty("reelItem")) {
                    d.reelItem = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.toObject(m.reelItem, o);
                    if (o.oneofs)
                        d.aIRichResponseContentItem = "reelItem";
                }
                return d;
            };

            AIRichResponseContentItemMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseContentItemMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata";
            };

            return AIRichResponseContentItemMetadata;
        })();

        AIRichResponseContentItemsMetadata.AIRichResponseReelItem = (function() {

            function AIRichResponseReelItem(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseReelItem.prototype.title = null;
            AIRichResponseReelItem.prototype.profileIconURL = null;
            AIRichResponseReelItem.prototype.thumbnailURL = null;
            AIRichResponseReelItem.prototype.videoURL = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_profileIconURL", {
                get: $util.oneOfGetter($oneOfFields = ["profileIconURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_thumbnailURL", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseReelItem.prototype, "_videoURL", {
                get: $util.oneOfGetter($oneOfFields = ["videoURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseReelItem.create = function create(properties) {
                return new AIRichResponseReelItem(properties);
            };

            AIRichResponseReelItem.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.profileIconURL != null && Object.hasOwnProperty.call(m, "profileIconURL"))
                    w.uint32(18).string(m.profileIconURL);
                if (m.thumbnailURL != null && Object.hasOwnProperty.call(m, "thumbnailURL"))
                    w.uint32(26).string(m.thumbnailURL);
                if (m.videoURL != null && Object.hasOwnProperty.call(m, "videoURL"))
                    w.uint32(34).string(m.videoURL);
                return w;
            };

            AIRichResponseReelItem.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.title = r.string();
                            break;
                        }
                    case 2: {
                            m.profileIconURL = r.string();
                            break;
                        }
                    case 3: {
                            m.thumbnailURL = r.string();
                            break;
                        }
                    case 4: {
                            m.videoURL = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseReelItem.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem();
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.profileIconURL != null) {
                    m.profileIconURL = String(d.profileIconURL);
                }
                if (d.thumbnailURL != null) {
                    m.thumbnailURL = String(d.thumbnailURL);
                }
                if (d.videoURL != null) {
                    m.videoURL = String(d.videoURL);
                }
                return m;
            };

            AIRichResponseReelItem.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.profileIconURL != null && m.hasOwnProperty("profileIconURL")) {
                    d.profileIconURL = m.profileIconURL;
                    if (o.oneofs)
                        d._profileIconURL = "profileIconURL";
                }
                if (m.thumbnailURL != null && m.hasOwnProperty("thumbnailURL")) {
                    d.thumbnailURL = m.thumbnailURL;
                    if (o.oneofs)
                        d._thumbnailURL = "thumbnailURL";
                }
                if (m.videoURL != null && m.hasOwnProperty("videoURL")) {
                    d.videoURL = m.videoURL;
                    if (o.oneofs)
                        d._videoURL = "videoURL";
                }
                return d;
            };

            AIRichResponseReelItem.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseReelItem.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseContentItemsMetadata.AIRichResponseReelItem";
            };

            return AIRichResponseReelItem;
        })();

        return AIRichResponseContentItemsMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseImageURL = (function() {

        function AIRichResponseImageURL(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseImageURL.prototype.imagePreviewURL = null;
        AIRichResponseImageURL.prototype.imageHighResURL = null;
        AIRichResponseImageURL.prototype.sourceURL = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_imagePreviewURL", {
            get: $util.oneOfGetter($oneOfFields = ["imagePreviewURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_imageHighResURL", {
            get: $util.oneOfGetter($oneOfFields = ["imageHighResURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseImageURL.prototype, "_sourceURL", {
            get: $util.oneOfGetter($oneOfFields = ["sourceURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseImageURL.create = function create(properties) {
            return new AIRichResponseImageURL(properties);
        };

        AIRichResponseImageURL.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.imagePreviewURL != null && Object.hasOwnProperty.call(m, "imagePreviewURL"))
                w.uint32(10).string(m.imagePreviewURL);
            if (m.imageHighResURL != null && Object.hasOwnProperty.call(m, "imageHighResURL"))
                w.uint32(18).string(m.imageHighResURL);
            if (m.sourceURL != null && Object.hasOwnProperty.call(m, "sourceURL"))
                w.uint32(26).string(m.sourceURL);
            return w;
        };

        AIRichResponseImageURL.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseImageURL();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.imagePreviewURL = r.string();
                        break;
                    }
                case 2: {
                        m.imageHighResURL = r.string();
                        break;
                    }
                case 3: {
                        m.sourceURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseImageURL.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseImageURL)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseImageURL();
            if (d.imagePreviewURL != null) {
                m.imagePreviewURL = String(d.imagePreviewURL);
            }
            if (d.imageHighResURL != null) {
                m.imageHighResURL = String(d.imageHighResURL);
            }
            if (d.sourceURL != null) {
                m.sourceURL = String(d.sourceURL);
            }
            return m;
        };

        AIRichResponseImageURL.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.imagePreviewURL != null && m.hasOwnProperty("imagePreviewURL")) {
                d.imagePreviewURL = m.imagePreviewURL;
                if (o.oneofs)
                    d._imagePreviewURL = "imagePreviewURL";
            }
            if (m.imageHighResURL != null && m.hasOwnProperty("imageHighResURL")) {
                d.imageHighResURL = m.imageHighResURL;
                if (o.oneofs)
                    d._imageHighResURL = "imageHighResURL";
            }
            if (m.sourceURL != null && m.hasOwnProperty("sourceURL")) {
                d.sourceURL = m.sourceURL;
                if (o.oneofs)
                    d._sourceURL = "sourceURL";
            }
            return d;
        };

        AIRichResponseImageURL.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseImageURL.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseImageURL";
        };

        return AIRichResponseImageURL;
    })();

    WAAICommonDeprecated.AIRichResponseGridImageMetadata = (function() {

        function AIRichResponseGridImageMetadata(p) {
            this.imageURLs = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseGridImageMetadata.prototype.gridImageURL = null;
        AIRichResponseGridImageMetadata.prototype.imageURLs = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseGridImageMetadata.prototype, "_gridImageURL", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseGridImageMetadata.create = function create(properties) {
            return new AIRichResponseGridImageMetadata(properties);
        };

        AIRichResponseGridImageMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.gridImageURL != null && Object.hasOwnProperty.call(m, "gridImageURL"))
                $root.WAAICommonDeprecated.AIRichResponseImageURL.encode(m.gridImageURL, w.uint32(10).fork()).ldelim();
            if (m.imageURLs != null && m.imageURLs.length) {
                for (var i = 0; i < m.imageURLs.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseImageURL.encode(m.imageURLs[i], w.uint32(18).fork()).ldelim();
            }
            return w;
        };

        AIRichResponseGridImageMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.gridImageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 2: {
                        if (!(m.imageURLs && m.imageURLs.length))
                            m.imageURLs = [];
                        m.imageURLs.push($root.WAAICommonDeprecated.AIRichResponseImageURL.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseGridImageMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata();
            if (d.gridImageURL != null) {
                if (typeof d.gridImageURL !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseGridImageMetadata.gridImageURL: object expected");
                m.gridImageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.fromObject(d.gridImageURL, n + 1);
            }
            if (d.imageURLs) {
                if (!Array.isArray(d.imageURLs))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseGridImageMetadata.imageURLs: array expected");
                m.imageURLs = [];
                for (var i = 0; i < d.imageURLs.length; ++i) {
                    if (typeof d.imageURLs[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseGridImageMetadata.imageURLs: object expected");
                    m.imageURLs[i] = $root.WAAICommonDeprecated.AIRichResponseImageURL.fromObject(d.imageURLs[i], n + 1);
                }
            }
            return m;
        };

        AIRichResponseGridImageMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.imageURLs = [];
            }
            if (m.gridImageURL != null && m.hasOwnProperty("gridImageURL")) {
                d.gridImageURL = $root.WAAICommonDeprecated.AIRichResponseImageURL.toObject(m.gridImageURL, o);
                if (o.oneofs)
                    d._gridImageURL = "gridImageURL";
            }
            if (m.imageURLs && m.imageURLs.length) {
                d.imageURLs = [];
                for (var j = 0; j < m.imageURLs.length; ++j) {
                    d.imageURLs[j] = $root.WAAICommonDeprecated.AIRichResponseImageURL.toObject(m.imageURLs[j], o);
                }
            }
            return d;
        };

        AIRichResponseGridImageMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseGridImageMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseGridImageMetadata";
        };

        return AIRichResponseGridImageMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseTableMetadata = (function() {

        function AIRichResponseTableMetadata(p) {
            this.rows = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseTableMetadata.prototype.rows = $util.emptyArray;
        AIRichResponseTableMetadata.prototype.title = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseTableMetadata.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseTableMetadata.create = function create(properties) {
            return new AIRichResponseTableMetadata(properties);
        };

        AIRichResponseTableMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.rows != null && m.rows.length) {
                for (var i = 0; i < m.rows.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow.encode(m.rows[i], w.uint32(10).fork()).ldelim();
            }
            if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                w.uint32(18).string(m.title);
            return w;
        };

        AIRichResponseTableMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseTableMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        if (!(m.rows && m.rows.length))
                            m.rows = [];
                        m.rows.push($root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 2: {
                        m.title = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseTableMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseTableMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseTableMetadata();
            if (d.rows) {
                if (!Array.isArray(d.rows))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseTableMetadata.rows: array expected");
                m.rows = [];
                for (var i = 0; i < d.rows.length; ++i) {
                    if (typeof d.rows[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseTableMetadata.rows: object expected");
                    m.rows[i] = $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow.fromObject(d.rows[i], n + 1);
                }
            }
            if (d.title != null) {
                m.title = String(d.title);
            }
            return m;
        };

        AIRichResponseTableMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.rows = [];
            }
            if (m.rows && m.rows.length) {
                d.rows = [];
                for (var j = 0; j < m.rows.length; ++j) {
                    d.rows[j] = $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow.toObject(m.rows[j], o);
                }
            }
            if (m.title != null && m.hasOwnProperty("title")) {
                d.title = m.title;
                if (o.oneofs)
                    d._title = "title";
            }
            return d;
        };

        AIRichResponseTableMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseTableMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseTableMetadata";
        };

        AIRichResponseTableMetadata.AIRichResponseTableRow = (function() {

            function AIRichResponseTableRow(p) {
                this.items = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseTableRow.prototype.items = $util.emptyArray;
            AIRichResponseTableRow.prototype.isHeading = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseTableRow.prototype, "_isHeading", {
                get: $util.oneOfGetter($oneOfFields = ["isHeading"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseTableRow.create = function create(properties) {
                return new AIRichResponseTableRow(properties);
            };

            AIRichResponseTableRow.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.items != null && m.items.length) {
                    for (var i = 0; i < m.items.length; ++i)
                        w.uint32(10).string(m.items[i]);
                }
                if (m.isHeading != null && Object.hasOwnProperty.call(m, "isHeading"))
                    w.uint32(16).bool(m.isHeading);
                return w;
            };

            AIRichResponseTableRow.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            if (!(m.items && m.items.length))
                                m.items = [];
                            m.items.push(r.string());
                            break;
                        }
                    case 2: {
                            m.isHeading = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseTableRow.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow();
                if (d.items) {
                    if (!Array.isArray(d.items))
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow.items: array expected");
                    m.items = [];
                    for (var i = 0; i < d.items.length; ++i) {
                        m.items[i] = String(d.items[i]);
                    }
                }
                if (d.isHeading != null) {
                    m.isHeading = Boolean(d.isHeading);
                }
                return m;
            };

            AIRichResponseTableRow.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.items = [];
                }
                if (m.items && m.items.length) {
                    d.items = [];
                    for (var j = 0; j < m.items.length; ++j) {
                        d.items[j] = m.items[j];
                    }
                }
                if (m.isHeading != null && m.hasOwnProperty("isHeading")) {
                    d.isHeading = m.isHeading;
                    if (o.oneofs)
                        d._isHeading = "isHeading";
                }
                return d;
            };

            AIRichResponseTableRow.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseTableRow.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseTableMetadata.AIRichResponseTableRow";
            };

            return AIRichResponseTableRow;
        })();

        return AIRichResponseTableMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseLatexMetadata = (function() {

        function AIRichResponseLatexMetadata(p) {
            this.expressions = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseLatexMetadata.prototype.text = null;
        AIRichResponseLatexMetadata.prototype.expressions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseLatexMetadata.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseLatexMetadata.create = function create(properties) {
            return new AIRichResponseLatexMetadata(properties);
        };

        AIRichResponseLatexMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.text != null && Object.hasOwnProperty.call(m, "text"))
                w.uint32(10).string(m.text);
            if (m.expressions != null && m.expressions.length) {
                for (var i = 0; i < m.expressions.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.encode(m.expressions[i], w.uint32(18).fork()).ldelim();
            }
            return w;
        };

        AIRichResponseLatexMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseLatexMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.text = r.string();
                        break;
                    }
                case 2: {
                        if (!(m.expressions && m.expressions.length))
                            m.expressions = [];
                        m.expressions.push($root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseLatexMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseLatexMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseLatexMetadata();
            if (d.text != null) {
                m.text = String(d.text);
            }
            if (d.expressions) {
                if (!Array.isArray(d.expressions))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseLatexMetadata.expressions: array expected");
                m.expressions = [];
                for (var i = 0; i < d.expressions.length; ++i) {
                    if (typeof d.expressions[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseLatexMetadata.expressions: object expected");
                    m.expressions[i] = $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.fromObject(d.expressions[i], n + 1);
                }
            }
            return m;
        };

        AIRichResponseLatexMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.expressions = [];
            }
            if (m.text != null && m.hasOwnProperty("text")) {
                d.text = m.text;
                if (o.oneofs)
                    d._text = "text";
            }
            if (m.expressions && m.expressions.length) {
                d.expressions = [];
                for (var j = 0; j < m.expressions.length; ++j) {
                    d.expressions[j] = $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.toObject(m.expressions[j], o);
                }
            }
            return d;
        };

        AIRichResponseLatexMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseLatexMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseLatexMetadata";
        };

        AIRichResponseLatexMetadata.AIRichResponseLatexExpression = (function() {

            function AIRichResponseLatexExpression(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseLatexExpression.prototype.latexExpression = null;
            AIRichResponseLatexExpression.prototype.URL = null;
            AIRichResponseLatexExpression.prototype.width = null;
            AIRichResponseLatexExpression.prototype.height = null;
            AIRichResponseLatexExpression.prototype.fontHeight = null;
            AIRichResponseLatexExpression.prototype.imageTopPadding = null;
            AIRichResponseLatexExpression.prototype.imageLeadingPadding = null;
            AIRichResponseLatexExpression.prototype.imageBottomPadding = null;
            AIRichResponseLatexExpression.prototype.imageTrailingPadding = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_latexExpression", {
                get: $util.oneOfGetter($oneOfFields = ["latexExpression"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_URL", {
                get: $util.oneOfGetter($oneOfFields = ["URL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_fontHeight", {
                get: $util.oneOfGetter($oneOfFields = ["fontHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTopPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTopPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageLeadingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageLeadingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageBottomPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageBottomPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTrailingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTrailingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseLatexExpression.create = function create(properties) {
                return new AIRichResponseLatexExpression(properties);
            };

            AIRichResponseLatexExpression.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.latexExpression != null && Object.hasOwnProperty.call(m, "latexExpression"))
                    w.uint32(10).string(m.latexExpression);
                if (m.URL != null && Object.hasOwnProperty.call(m, "URL"))
                    w.uint32(18).string(m.URL);
                if (m.width != null && Object.hasOwnProperty.call(m, "width"))
                    w.uint32(25).double(m.width);
                if (m.height != null && Object.hasOwnProperty.call(m, "height"))
                    w.uint32(33).double(m.height);
                if (m.fontHeight != null && Object.hasOwnProperty.call(m, "fontHeight"))
                    w.uint32(41).double(m.fontHeight);
                if (m.imageTopPadding != null && Object.hasOwnProperty.call(m, "imageTopPadding"))
                    w.uint32(49).double(m.imageTopPadding);
                if (m.imageLeadingPadding != null && Object.hasOwnProperty.call(m, "imageLeadingPadding"))
                    w.uint32(57).double(m.imageLeadingPadding);
                if (m.imageBottomPadding != null && Object.hasOwnProperty.call(m, "imageBottomPadding"))
                    w.uint32(65).double(m.imageBottomPadding);
                if (m.imageTrailingPadding != null && Object.hasOwnProperty.call(m, "imageTrailingPadding"))
                    w.uint32(73).double(m.imageTrailingPadding);
                return w;
            };

            AIRichResponseLatexExpression.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.latexExpression = r.string();
                            break;
                        }
                    case 2: {
                            m.URL = r.string();
                            break;
                        }
                    case 3: {
                            m.width = r.double();
                            break;
                        }
                    case 4: {
                            m.height = r.double();
                            break;
                        }
                    case 5: {
                            m.fontHeight = r.double();
                            break;
                        }
                    case 6: {
                            m.imageTopPadding = r.double();
                            break;
                        }
                    case 7: {
                            m.imageLeadingPadding = r.double();
                            break;
                        }
                    case 8: {
                            m.imageBottomPadding = r.double();
                            break;
                        }
                    case 9: {
                            m.imageTrailingPadding = r.double();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseLatexExpression.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression();
                if (d.latexExpression != null) {
                    m.latexExpression = String(d.latexExpression);
                }
                if (d.URL != null) {
                    m.URL = String(d.URL);
                }
                if (d.width != null) {
                    m.width = Number(d.width);
                }
                if (d.height != null) {
                    m.height = Number(d.height);
                }
                if (d.fontHeight != null) {
                    m.fontHeight = Number(d.fontHeight);
                }
                if (d.imageTopPadding != null) {
                    m.imageTopPadding = Number(d.imageTopPadding);
                }
                if (d.imageLeadingPadding != null) {
                    m.imageLeadingPadding = Number(d.imageLeadingPadding);
                }
                if (d.imageBottomPadding != null) {
                    m.imageBottomPadding = Number(d.imageBottomPadding);
                }
                if (d.imageTrailingPadding != null) {
                    m.imageTrailingPadding = Number(d.imageTrailingPadding);
                }
                return m;
            };

            AIRichResponseLatexExpression.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.latexExpression != null && m.hasOwnProperty("latexExpression")) {
                    d.latexExpression = m.latexExpression;
                    if (o.oneofs)
                        d._latexExpression = "latexExpression";
                }
                if (m.URL != null && m.hasOwnProperty("URL")) {
                    d.URL = m.URL;
                    if (o.oneofs)
                        d._URL = "URL";
                }
                if (m.width != null && m.hasOwnProperty("width")) {
                    d.width = o.json && !isFinite(m.width) ? String(m.width) : m.width;
                    if (o.oneofs)
                        d._width = "width";
                }
                if (m.height != null && m.hasOwnProperty("height")) {
                    d.height = o.json && !isFinite(m.height) ? String(m.height) : m.height;
                    if (o.oneofs)
                        d._height = "height";
                }
                if (m.fontHeight != null && m.hasOwnProperty("fontHeight")) {
                    d.fontHeight = o.json && !isFinite(m.fontHeight) ? String(m.fontHeight) : m.fontHeight;
                    if (o.oneofs)
                        d._fontHeight = "fontHeight";
                }
                if (m.imageTopPadding != null && m.hasOwnProperty("imageTopPadding")) {
                    d.imageTopPadding = o.json && !isFinite(m.imageTopPadding) ? String(m.imageTopPadding) : m.imageTopPadding;
                    if (o.oneofs)
                        d._imageTopPadding = "imageTopPadding";
                }
                if (m.imageLeadingPadding != null && m.hasOwnProperty("imageLeadingPadding")) {
                    d.imageLeadingPadding = o.json && !isFinite(m.imageLeadingPadding) ? String(m.imageLeadingPadding) : m.imageLeadingPadding;
                    if (o.oneofs)
                        d._imageLeadingPadding = "imageLeadingPadding";
                }
                if (m.imageBottomPadding != null && m.hasOwnProperty("imageBottomPadding")) {
                    d.imageBottomPadding = o.json && !isFinite(m.imageBottomPadding) ? String(m.imageBottomPadding) : m.imageBottomPadding;
                    if (o.oneofs)
                        d._imageBottomPadding = "imageBottomPadding";
                }
                if (m.imageTrailingPadding != null && m.hasOwnProperty("imageTrailingPadding")) {
                    d.imageTrailingPadding = o.json && !isFinite(m.imageTrailingPadding) ? String(m.imageTrailingPadding) : m.imageTrailingPadding;
                    if (o.oneofs)
                        d._imageTrailingPadding = "imageTrailingPadding";
                }
                return d;
            };

            AIRichResponseLatexExpression.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseLatexExpression.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseLatexMetadata.AIRichResponseLatexExpression";
            };

            return AIRichResponseLatexExpression;
        })();

        return AIRichResponseLatexMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseMapMetadata = (function() {

        function AIRichResponseMapMetadata(p) {
            this.annotations = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseMapMetadata.prototype.centerLatitude = null;
        AIRichResponseMapMetadata.prototype.centerLongitude = null;
        AIRichResponseMapMetadata.prototype.latitudeDelta = null;
        AIRichResponseMapMetadata.prototype.longitudeDelta = null;
        AIRichResponseMapMetadata.prototype.annotations = $util.emptyArray;
        AIRichResponseMapMetadata.prototype.showInfoList = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_latitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["latitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_longitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["longitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseMapMetadata.prototype, "_showInfoList", {
            get: $util.oneOfGetter($oneOfFields = ["showInfoList"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseMapMetadata.create = function create(properties) {
            return new AIRichResponseMapMetadata(properties);
        };

        AIRichResponseMapMetadata.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.centerLatitude != null && Object.hasOwnProperty.call(m, "centerLatitude"))
                w.uint32(9).double(m.centerLatitude);
            if (m.centerLongitude != null && Object.hasOwnProperty.call(m, "centerLongitude"))
                w.uint32(17).double(m.centerLongitude);
            if (m.latitudeDelta != null && Object.hasOwnProperty.call(m, "latitudeDelta"))
                w.uint32(25).double(m.latitudeDelta);
            if (m.longitudeDelta != null && Object.hasOwnProperty.call(m, "longitudeDelta"))
                w.uint32(33).double(m.longitudeDelta);
            if (m.annotations != null && m.annotations.length) {
                for (var i = 0; i < m.annotations.length; ++i)
                    $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.encode(m.annotations[i], w.uint32(42).fork()).ldelim();
            }
            if (m.showInfoList != null && Object.hasOwnProperty.call(m, "showInfoList"))
                w.uint32(48).bool(m.showInfoList);
            return w;
        };

        AIRichResponseMapMetadata.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseMapMetadata();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.centerLatitude = r.double();
                        break;
                    }
                case 2: {
                        m.centerLongitude = r.double();
                        break;
                    }
                case 3: {
                        m.latitudeDelta = r.double();
                        break;
                    }
                case 4: {
                        m.longitudeDelta = r.double();
                        break;
                    }
                case 5: {
                        if (!(m.annotations && m.annotations.length))
                            m.annotations = [];
                        m.annotations.push($root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.decode(r, r.uint32(), undefined, n + 1));
                        break;
                    }
                case 6: {
                        m.showInfoList = r.bool();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseMapMetadata.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseMapMetadata)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseMapMetadata();
            if (d.centerLatitude != null) {
                m.centerLatitude = Number(d.centerLatitude);
            }
            if (d.centerLongitude != null) {
                m.centerLongitude = Number(d.centerLongitude);
            }
            if (d.latitudeDelta != null) {
                m.latitudeDelta = Number(d.latitudeDelta);
            }
            if (d.longitudeDelta != null) {
                m.longitudeDelta = Number(d.longitudeDelta);
            }
            if (d.annotations) {
                if (!Array.isArray(d.annotations))
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseMapMetadata.annotations: array expected");
                m.annotations = [];
                for (var i = 0; i < d.annotations.length; ++i) {
                    if (typeof d.annotations[i] !== "object")
                        throw TypeError(".WAAICommonDeprecated.AIRichResponseMapMetadata.annotations: object expected");
                    m.annotations[i] = $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.fromObject(d.annotations[i], n + 1);
                }
            }
            if (d.showInfoList != null) {
                m.showInfoList = Boolean(d.showInfoList);
            }
            return m;
        };

        AIRichResponseMapMetadata.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.annotations = [];
            }
            if (m.centerLatitude != null && m.hasOwnProperty("centerLatitude")) {
                d.centerLatitude = o.json && !isFinite(m.centerLatitude) ? String(m.centerLatitude) : m.centerLatitude;
                if (o.oneofs)
                    d._centerLatitude = "centerLatitude";
            }
            if (m.centerLongitude != null && m.hasOwnProperty("centerLongitude")) {
                d.centerLongitude = o.json && !isFinite(m.centerLongitude) ? String(m.centerLongitude) : m.centerLongitude;
                if (o.oneofs)
                    d._centerLongitude = "centerLongitude";
            }
            if (m.latitudeDelta != null && m.hasOwnProperty("latitudeDelta")) {
                d.latitudeDelta = o.json && !isFinite(m.latitudeDelta) ? String(m.latitudeDelta) : m.latitudeDelta;
                if (o.oneofs)
                    d._latitudeDelta = "latitudeDelta";
            }
            if (m.longitudeDelta != null && m.hasOwnProperty("longitudeDelta")) {
                d.longitudeDelta = o.json && !isFinite(m.longitudeDelta) ? String(m.longitudeDelta) : m.longitudeDelta;
                if (o.oneofs)
                    d._longitudeDelta = "longitudeDelta";
            }
            if (m.annotations && m.annotations.length) {
                d.annotations = [];
                for (var j = 0; j < m.annotations.length; ++j) {
                    d.annotations[j] = $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.toObject(m.annotations[j], o);
                }
            }
            if (m.showInfoList != null && m.hasOwnProperty("showInfoList")) {
                d.showInfoList = m.showInfoList;
                if (o.oneofs)
                    d._showInfoList = "showInfoList";
            }
            return d;
        };

        AIRichResponseMapMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseMapMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseMapMetadata";
        };

        AIRichResponseMapMetadata.AIRichResponseMapAnnotation = (function() {

            function AIRichResponseMapAnnotation(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AIRichResponseMapAnnotation.prototype.annotationNumber = null;
            AIRichResponseMapAnnotation.prototype.latitude = null;
            AIRichResponseMapAnnotation.prototype.longitude = null;
            AIRichResponseMapAnnotation.prototype.title = null;
            AIRichResponseMapAnnotation.prototype.body = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_annotationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["annotationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_latitude", {
                get: $util.oneOfGetter($oneOfFields = ["latitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_longitude", {
                get: $util.oneOfGetter($oneOfFields = ["longitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseMapAnnotation.create = function create(properties) {
                return new AIRichResponseMapAnnotation(properties);
            };

            AIRichResponseMapAnnotation.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.annotationNumber != null && Object.hasOwnProperty.call(m, "annotationNumber"))
                    w.uint32(8).uint32(m.annotationNumber);
                if (m.latitude != null && Object.hasOwnProperty.call(m, "latitude"))
                    w.uint32(17).double(m.latitude);
                if (m.longitude != null && Object.hasOwnProperty.call(m, "longitude"))
                    w.uint32(25).double(m.longitude);
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(34).string(m.title);
                if (m.body != null && Object.hasOwnProperty.call(m, "body"))
                    w.uint32(42).string(m.body);
                return w;
            };

            AIRichResponseMapAnnotation.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.annotationNumber = r.uint32();
                            break;
                        }
                    case 2: {
                            m.latitude = r.double();
                            break;
                        }
                    case 3: {
                            m.longitude = r.double();
                            break;
                        }
                    case 4: {
                            m.title = r.string();
                            break;
                        }
                    case 5: {
                            m.body = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AIRichResponseMapAnnotation.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation();
                if (d.annotationNumber != null) {
                    m.annotationNumber = d.annotationNumber >>> 0;
                }
                if (d.latitude != null) {
                    m.latitude = Number(d.latitude);
                }
                if (d.longitude != null) {
                    m.longitude = Number(d.longitude);
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.body != null) {
                    m.body = String(d.body);
                }
                return m;
            };

            AIRichResponseMapAnnotation.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.annotationNumber != null && m.hasOwnProperty("annotationNumber")) {
                    d.annotationNumber = m.annotationNumber;
                    if (o.oneofs)
                        d._annotationNumber = "annotationNumber";
                }
                if (m.latitude != null && m.hasOwnProperty("latitude")) {
                    d.latitude = o.json && !isFinite(m.latitude) ? String(m.latitude) : m.latitude;
                    if (o.oneofs)
                        d._latitude = "latitude";
                }
                if (m.longitude != null && m.hasOwnProperty("longitude")) {
                    d.longitude = o.json && !isFinite(m.longitude) ? String(m.longitude) : m.longitude;
                    if (o.oneofs)
                        d._longitude = "longitude";
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.body != null && m.hasOwnProperty("body")) {
                    d.body = m.body;
                    if (o.oneofs)
                        d._body = "body";
                }
                return d;
            };

            AIRichResponseMapAnnotation.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseMapAnnotation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseMapMetadata.AIRichResponseMapAnnotation";
            };

            return AIRichResponseMapAnnotation;
        })();

        return AIRichResponseMapMetadata;
    })();

    WAAICommonDeprecated.AIRichResponseSubMessage = (function() {

        function AIRichResponseSubMessage(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        AIRichResponseSubMessage.prototype.messageType = null;
        AIRichResponseSubMessage.prototype.gridImageMetadata = null;
        AIRichResponseSubMessage.prototype.messageText = null;
        AIRichResponseSubMessage.prototype.imageMetadata = null;
        AIRichResponseSubMessage.prototype.codeMetadata = null;
        AIRichResponseSubMessage.prototype.tableMetadata = null;
        AIRichResponseSubMessage.prototype.dynamicMetadata = null;
        AIRichResponseSubMessage.prototype.latexMetadata = null;
        AIRichResponseSubMessage.prototype.mapMetadata = null;
        AIRichResponseSubMessage.prototype.contentItemsMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_gridImageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageText", {
            get: $util.oneOfGetter($oneOfFields = ["messageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_imageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_codeMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["codeMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_tableMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["tableMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_dynamicMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["dynamicMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_latexMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["latexMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_mapMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["mapMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(AIRichResponseSubMessage.prototype, "_contentItemsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["contentItemsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseSubMessage.create = function create(properties) {
            return new AIRichResponseSubMessage(properties);
        };

        AIRichResponseSubMessage.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.messageType != null && Object.hasOwnProperty.call(m, "messageType"))
                w.uint32(8).int32(m.messageType);
            if (m.gridImageMetadata != null && Object.hasOwnProperty.call(m, "gridImageMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata.encode(m.gridImageMetadata, w.uint32(18).fork()).ldelim();
            if (m.messageText != null && Object.hasOwnProperty.call(m, "messageText"))
                w.uint32(26).string(m.messageText);
            if (m.imageMetadata != null && Object.hasOwnProperty.call(m, "imageMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.encode(m.imageMetadata, w.uint32(34).fork()).ldelim();
            if (m.codeMetadata != null && Object.hasOwnProperty.call(m, "codeMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.encode(m.codeMetadata, w.uint32(42).fork()).ldelim();
            if (m.tableMetadata != null && Object.hasOwnProperty.call(m, "tableMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseTableMetadata.encode(m.tableMetadata, w.uint32(50).fork()).ldelim();
            if (m.dynamicMetadata != null && Object.hasOwnProperty.call(m, "dynamicMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.encode(m.dynamicMetadata, w.uint32(58).fork()).ldelim();
            if (m.latexMetadata != null && Object.hasOwnProperty.call(m, "latexMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.encode(m.latexMetadata, w.uint32(66).fork()).ldelim();
            if (m.mapMetadata != null && Object.hasOwnProperty.call(m, "mapMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseMapMetadata.encode(m.mapMetadata, w.uint32(74).fork()).ldelim();
            if (m.contentItemsMetadata != null && Object.hasOwnProperty.call(m, "contentItemsMetadata"))
                $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.encode(m.contentItemsMetadata, w.uint32(82).fork()).ldelim();
            return w;
        };

        AIRichResponseSubMessage.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAICommonDeprecated.AIRichResponseSubMessage();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.messageType = r.int32();
                        break;
                    }
                case 2: {
                        m.gridImageMetadata = $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.messageText = r.string();
                        break;
                    }
                case 4: {
                        m.imageMetadata = $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.codeMetadata = $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.tableMetadata = $root.WAAICommonDeprecated.AIRichResponseTableMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.dynamicMetadata = $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.latexMetadata = $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 9: {
                        m.mapMetadata = $root.WAAICommonDeprecated.AIRichResponseMapMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 10: {
                        m.contentItemsMetadata = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        AIRichResponseSubMessage.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAICommonDeprecated.AIRichResponseSubMessage)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAICommonDeprecated.AIRichResponseSubMessage();
            switch (d.messageType) {
            default:
                if (typeof d.messageType === "number") {
                    m.messageType = d.messageType;
                    break;
                }
                break;
            case "AI_RICH_RESPONSE_UNKNOWN":
            case 0:
                m.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_GRID_IMAGE":
            case 1:
                m.messageType = 1;
                break;
            case "AI_RICH_RESPONSE_TEXT":
            case 2:
                m.messageType = 2;
                break;
            case "AI_RICH_RESPONSE_INLINE_IMAGE":
            case 3:
                m.messageType = 3;
                break;
            case "AI_RICH_RESPONSE_TABLE":
            case 4:
                m.messageType = 4;
                break;
            case "AI_RICH_RESPONSE_CODE":
            case 5:
                m.messageType = 5;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC":
            case 6:
                m.messageType = 6;
                break;
            case "AI_RICH_RESPONSE_MAP":
            case 7:
                m.messageType = 7;
                break;
            case "AI_RICH_RESPONSE_LATEX":
            case 8:
                m.messageType = 8;
                break;
            case "AI_RICH_RESPONSE_CONTENT_ITEMS":
            case 9:
                m.messageType = 9;
                break;
            }
            if (d.gridImageMetadata != null) {
                if (typeof d.gridImageMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.gridImageMetadata: object expected");
                m.gridImageMetadata = $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata.fromObject(d.gridImageMetadata, n + 1);
            }
            if (d.messageText != null) {
                m.messageText = String(d.messageText);
            }
            if (d.imageMetadata != null) {
                if (typeof d.imageMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.imageMetadata: object expected");
                m.imageMetadata = $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.fromObject(d.imageMetadata, n + 1);
            }
            if (d.codeMetadata != null) {
                if (typeof d.codeMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.codeMetadata: object expected");
                m.codeMetadata = $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.fromObject(d.codeMetadata, n + 1);
            }
            if (d.tableMetadata != null) {
                if (typeof d.tableMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.tableMetadata: object expected");
                m.tableMetadata = $root.WAAICommonDeprecated.AIRichResponseTableMetadata.fromObject(d.tableMetadata, n + 1);
            }
            if (d.dynamicMetadata != null) {
                if (typeof d.dynamicMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.dynamicMetadata: object expected");
                m.dynamicMetadata = $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.fromObject(d.dynamicMetadata, n + 1);
            }
            if (d.latexMetadata != null) {
                if (typeof d.latexMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.latexMetadata: object expected");
                m.latexMetadata = $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.fromObject(d.latexMetadata, n + 1);
            }
            if (d.mapMetadata != null) {
                if (typeof d.mapMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.mapMetadata: object expected");
                m.mapMetadata = $root.WAAICommonDeprecated.AIRichResponseMapMetadata.fromObject(d.mapMetadata, n + 1);
            }
            if (d.contentItemsMetadata != null) {
                if (typeof d.contentItemsMetadata !== "object")
                    throw TypeError(".WAAICommonDeprecated.AIRichResponseSubMessage.contentItemsMetadata: object expected");
                m.contentItemsMetadata = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.fromObject(d.contentItemsMetadata, n + 1);
            }
            return m;
        };

        AIRichResponseSubMessage.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.messageType != null && m.hasOwnProperty("messageType")) {
                d.messageType = o.enums === String ? $root.WAAICommonDeprecated.AIRichResponseSubMessageType[m.messageType] === undefined ? m.messageType : $root.WAAICommonDeprecated.AIRichResponseSubMessageType[m.messageType] : m.messageType;
                if (o.oneofs)
                    d._messageType = "messageType";
            }
            if (m.gridImageMetadata != null && m.hasOwnProperty("gridImageMetadata")) {
                d.gridImageMetadata = $root.WAAICommonDeprecated.AIRichResponseGridImageMetadata.toObject(m.gridImageMetadata, o);
                if (o.oneofs)
                    d._gridImageMetadata = "gridImageMetadata";
            }
            if (m.messageText != null && m.hasOwnProperty("messageText")) {
                d.messageText = m.messageText;
                if (o.oneofs)
                    d._messageText = "messageText";
            }
            if (m.imageMetadata != null && m.hasOwnProperty("imageMetadata")) {
                d.imageMetadata = $root.WAAICommonDeprecated.AIRichResponseInlineImageMetadata.toObject(m.imageMetadata, o);
                if (o.oneofs)
                    d._imageMetadata = "imageMetadata";
            }
            if (m.codeMetadata != null && m.hasOwnProperty("codeMetadata")) {
                d.codeMetadata = $root.WAAICommonDeprecated.AIRichResponseCodeMetadata.toObject(m.codeMetadata, o);
                if (o.oneofs)
                    d._codeMetadata = "codeMetadata";
            }
            if (m.tableMetadata != null && m.hasOwnProperty("tableMetadata")) {
                d.tableMetadata = $root.WAAICommonDeprecated.AIRichResponseTableMetadata.toObject(m.tableMetadata, o);
                if (o.oneofs)
                    d._tableMetadata = "tableMetadata";
            }
            if (m.dynamicMetadata != null && m.hasOwnProperty("dynamicMetadata")) {
                d.dynamicMetadata = $root.WAAICommonDeprecated.AIRichResponseDynamicMetadata.toObject(m.dynamicMetadata, o);
                if (o.oneofs)
                    d._dynamicMetadata = "dynamicMetadata";
            }
            if (m.latexMetadata != null && m.hasOwnProperty("latexMetadata")) {
                d.latexMetadata = $root.WAAICommonDeprecated.AIRichResponseLatexMetadata.toObject(m.latexMetadata, o);
                if (o.oneofs)
                    d._latexMetadata = "latexMetadata";
            }
            if (m.mapMetadata != null && m.hasOwnProperty("mapMetadata")) {
                d.mapMetadata = $root.WAAICommonDeprecated.AIRichResponseMapMetadata.toObject(m.mapMetadata, o);
                if (o.oneofs)
                    d._mapMetadata = "mapMetadata";
            }
            if (m.contentItemsMetadata != null && m.hasOwnProperty("contentItemsMetadata")) {
                d.contentItemsMetadata = $root.WAAICommonDeprecated.AIRichResponseContentItemsMetadata.toObject(m.contentItemsMetadata, o);
                if (o.oneofs)
                    d._contentItemsMetadata = "contentItemsMetadata";
            }
            return d;
        };

        AIRichResponseSubMessage.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseSubMessage.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAICommonDeprecated.AIRichResponseSubMessage";
        };

        return AIRichResponseSubMessage;
    })();

    return WAAICommonDeprecated;
})();

export const WAAdv = $root.WAAdv = (() => {

    const WAAdv = {};

    WAAdv.ADVEncryptionType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "E2EE"] = 0;
        values[valuesById[1] = "HOSTED"] = 1;
        values[valuesById[2] = "NON_E2EE"] = 2;
        return values;
    })();

    WAAdv.ADVKeyIndexList = (function() {

        function ADVKeyIndexList(p) {
            this.validIndexes = [];
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ADVKeyIndexList.prototype.rawID = null;
        ADVKeyIndexList.prototype.timestamp = null;
        ADVKeyIndexList.prototype.currentIndex = null;
        ADVKeyIndexList.prototype.validIndexes = $util.emptyArray;
        ADVKeyIndexList.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_rawID", {
            get: $util.oneOfGetter($oneOfFields = ["rawID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_currentIndex", {
            get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVKeyIndexList.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVKeyIndexList.create = function create(properties) {
            return new ADVKeyIndexList(properties);
        };

        ADVKeyIndexList.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.rawID != null && Object.hasOwnProperty.call(m, "rawID"))
                w.uint32(8).uint32(m.rawID);
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.currentIndex != null && Object.hasOwnProperty.call(m, "currentIndex"))
                w.uint32(24).uint32(m.currentIndex);
            if (m.validIndexes != null && m.validIndexes.length) {
                w.uint32(34).fork();
                for (var i = 0; i < m.validIndexes.length; ++i)
                    w.uint32(m.validIndexes[i]);
                w.ldelim();
            }
            if (m.accountType != null && Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(40).int32(m.accountType);
            return w;
        };

        ADVKeyIndexList.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAdv.ADVKeyIndexList();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.rawID = r.uint32();
                        break;
                    }
                case 2: {
                        m.timestamp = r.uint64();
                        break;
                    }
                case 3: {
                        m.currentIndex = r.uint32();
                        break;
                    }
                case 4: {
                        if (!(m.validIndexes && m.validIndexes.length))
                            m.validIndexes = [];
                        if ((t & 7) === 2) {
                            var c2 = r.uint32() + r.pos;
                            while (r.pos < c2)
                                m.validIndexes.push(r.uint32());
                        } else
                            m.validIndexes.push(r.uint32());
                        break;
                    }
                case 5: {
                        m.accountType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ADVKeyIndexList.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAdv.ADVKeyIndexList)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAdv.ADVKeyIndexList();
            if (d.rawID != null) {
                m.rawID = d.rawID >>> 0;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = true;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.currentIndex != null) {
                m.currentIndex = d.currentIndex >>> 0;
            }
            if (d.validIndexes) {
                if (!Array.isArray(d.validIndexes))
                    throw TypeError(".WAAdv.ADVKeyIndexList.validIndexes: array expected");
                m.validIndexes = [];
                for (var i = 0; i < d.validIndexes.length; ++i) {
                    m.validIndexes[i] = d.validIndexes[i] >>> 0;
                }
            }
            switch (d.accountType) {
            default:
                if (typeof d.accountType === "number") {
                    m.accountType = d.accountType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            }
            return m;
        };

        ADVKeyIndexList.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (o.arrays || o.defaults) {
                d.validIndexes = [];
            }
            if (m.rawID != null && m.hasOwnProperty("rawID")) {
                d.rawID = m.rawID;
                if (o.oneofs)
                    d._rawID = "rawID";
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            if (m.currentIndex != null && m.hasOwnProperty("currentIndex")) {
                d.currentIndex = m.currentIndex;
                if (o.oneofs)
                    d._currentIndex = "currentIndex";
            }
            if (m.validIndexes && m.validIndexes.length) {
                d.validIndexes = [];
                for (var j = 0; j < m.validIndexes.length; ++j) {
                    d.validIndexes[j] = m.validIndexes[j];
                }
            }
            if (m.accountType != null && m.hasOwnProperty("accountType")) {
                d.accountType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.accountType] === undefined ? m.accountType : $root.WAAdv.ADVEncryptionType[m.accountType] : m.accountType;
                if (o.oneofs)
                    d._accountType = "accountType";
            }
            return d;
        };

        ADVKeyIndexList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVKeyIndexList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAdv.ADVKeyIndexList";
        };

        return ADVKeyIndexList;
    })();

    WAAdv.ADVSignedKeyIndexList = (function() {

        function ADVSignedKeyIndexList(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ADVSignedKeyIndexList.prototype.details = null;
        ADVSignedKeyIndexList.prototype.accountSignature = null;
        ADVSignedKeyIndexList.prototype.accountSignatureKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedKeyIndexList.create = function create(properties) {
            return new ADVSignedKeyIndexList(properties);
        };

        ADVSignedKeyIndexList.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.details != null && Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.accountSignature != null && Object.hasOwnProperty.call(m, "accountSignature"))
                w.uint32(18).bytes(m.accountSignature);
            if (m.accountSignatureKey != null && Object.hasOwnProperty.call(m, "accountSignatureKey"))
                w.uint32(26).bytes(m.accountSignatureKey);
            return w;
        };

        ADVSignedKeyIndexList.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAdv.ADVSignedKeyIndexList();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.details = r.bytes();
                        break;
                    }
                case 2: {
                        m.accountSignature = r.bytes();
                        break;
                    }
                case 3: {
                        m.accountSignatureKey = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ADVSignedKeyIndexList.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAdv.ADVSignedKeyIndexList)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAdv.ADVSignedKeyIndexList();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.accountSignature != null) {
                if (typeof d.accountSignature === "string")
                    $util.base64.decode(d.accountSignature, m.accountSignature = $util.newBuffer($util.base64.length(d.accountSignature)), 0);
                else if (d.accountSignature.length >= 0)
                    m.accountSignature = d.accountSignature;
            }
            if (d.accountSignatureKey != null) {
                if (typeof d.accountSignatureKey === "string")
                    $util.base64.decode(d.accountSignatureKey, m.accountSignatureKey = $util.newBuffer($util.base64.length(d.accountSignatureKey)), 0);
                else if (d.accountSignatureKey.length >= 0)
                    m.accountSignatureKey = d.accountSignatureKey;
            }
            return m;
        };

        ADVSignedKeyIndexList.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.details != null && m.hasOwnProperty("details")) {
                d.details = o.bytes === String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === Array ? Array.prototype.slice.call(m.details) : m.details;
                if (o.oneofs)
                    d._details = "details";
            }
            if (m.accountSignature != null && m.hasOwnProperty("accountSignature")) {
                d.accountSignature = o.bytes === String ? $util.base64.encode(m.accountSignature, 0, m.accountSignature.length) : o.bytes === Array ? Array.prototype.slice.call(m.accountSignature) : m.accountSignature;
                if (o.oneofs)
                    d._accountSignature = "accountSignature";
            }
            if (m.accountSignatureKey != null && m.hasOwnProperty("accountSignatureKey")) {
                d.accountSignatureKey = o.bytes === String ? $util.base64.encode(m.accountSignatureKey, 0, m.accountSignatureKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.accountSignatureKey) : m.accountSignatureKey;
                if (o.oneofs)
                    d._accountSignatureKey = "accountSignatureKey";
            }
            return d;
        };

        ADVSignedKeyIndexList.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedKeyIndexList.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAdv.ADVSignedKeyIndexList";
        };

        return ADVSignedKeyIndexList;
    })();

    WAAdv.ADVDeviceIdentity = (function() {

        function ADVDeviceIdentity(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ADVDeviceIdentity.prototype.rawID = null;
        ADVDeviceIdentity.prototype.timestamp = null;
        ADVDeviceIdentity.prototype.keyIndex = null;
        ADVDeviceIdentity.prototype.accountType = null;
        ADVDeviceIdentity.prototype.deviceType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_rawID", {
            get: $util.oneOfGetter($oneOfFields = ["rawID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_keyIndex", {
            get: $util.oneOfGetter($oneOfFields = ["keyIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVDeviceIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVDeviceIdentity.create = function create(properties) {
            return new ADVDeviceIdentity(properties);
        };

        ADVDeviceIdentity.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.rawID != null && Object.hasOwnProperty.call(m, "rawID"))
                w.uint32(8).uint32(m.rawID);
            if (m.timestamp != null && Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.keyIndex != null && Object.hasOwnProperty.call(m, "keyIndex"))
                w.uint32(24).uint32(m.keyIndex);
            if (m.accountType != null && Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(32).int32(m.accountType);
            if (m.deviceType != null && Object.hasOwnProperty.call(m, "deviceType"))
                w.uint32(40).int32(m.deviceType);
            return w;
        };

        ADVDeviceIdentity.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAdv.ADVDeviceIdentity();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.rawID = r.uint32();
                        break;
                    }
                case 2: {
                        m.timestamp = r.uint64();
                        break;
                    }
                case 3: {
                        m.keyIndex = r.uint32();
                        break;
                    }
                case 4: {
                        m.accountType = r.int32();
                        break;
                    }
                case 5: {
                        m.deviceType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ADVDeviceIdentity.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAdv.ADVDeviceIdentity)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAdv.ADVDeviceIdentity();
            if (d.rawID != null) {
                m.rawID = d.rawID >>> 0;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    (m.timestamp = $util.Long.fromValue(d.timestamp)).unsigned = true;
                else if (typeof d.timestamp === "string")
                    m.timestamp = parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.keyIndex != null) {
                m.keyIndex = d.keyIndex >>> 0;
            }
            switch (d.accountType) {
            default:
                if (typeof d.accountType === "number") {
                    m.accountType = d.accountType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            }
            switch (d.deviceType) {
            default:
                if (typeof d.deviceType === "number") {
                    m.deviceType = d.deviceType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.deviceType = 0;
                break;
            case "HOSTED":
            case 1:
                m.deviceType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.deviceType = 2;
                break;
            }
            return m;
        };

        ADVDeviceIdentity.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.rawID != null && m.hasOwnProperty("rawID")) {
                d.rawID = m.rawID;
                if (o.oneofs)
                    d._rawID = "rawID";
            }
            if (m.timestamp != null && m.hasOwnProperty("timestamp")) {
                if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === String ? String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
                if (o.oneofs)
                    d._timestamp = "timestamp";
            }
            if (m.keyIndex != null && m.hasOwnProperty("keyIndex")) {
                d.keyIndex = m.keyIndex;
                if (o.oneofs)
                    d._keyIndex = "keyIndex";
            }
            if (m.accountType != null && m.hasOwnProperty("accountType")) {
                d.accountType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.accountType] === undefined ? m.accountType : $root.WAAdv.ADVEncryptionType[m.accountType] : m.accountType;
                if (o.oneofs)
                    d._accountType = "accountType";
            }
            if (m.deviceType != null && m.hasOwnProperty("deviceType")) {
                d.deviceType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.deviceType] === undefined ? m.deviceType : $root.WAAdv.ADVEncryptionType[m.deviceType] : m.deviceType;
                if (o.oneofs)
                    d._deviceType = "deviceType";
            }
            return d;
        };

        ADVDeviceIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVDeviceIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAdv.ADVDeviceIdentity";
        };

        return ADVDeviceIdentity;
    })();

    WAAdv.ADVSignedDeviceIdentity = (function() {

        function ADVSignedDeviceIdentity(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ADVSignedDeviceIdentity.prototype.details = null;
        ADVSignedDeviceIdentity.prototype.accountSignatureKey = null;
        ADVSignedDeviceIdentity.prototype.accountSignature = null;
        ADVSignedDeviceIdentity.prototype.deviceSignature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_deviceSignature", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedDeviceIdentity.create = function create(properties) {
            return new ADVSignedDeviceIdentity(properties);
        };

        ADVSignedDeviceIdentity.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.details != null && Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.accountSignatureKey != null && Object.hasOwnProperty.call(m, "accountSignatureKey"))
                w.uint32(18).bytes(m.accountSignatureKey);
            if (m.accountSignature != null && Object.hasOwnProperty.call(m, "accountSignature"))
                w.uint32(26).bytes(m.accountSignature);
            if (m.deviceSignature != null && Object.hasOwnProperty.call(m, "deviceSignature"))
                w.uint32(34).bytes(m.deviceSignature);
            return w;
        };

        ADVSignedDeviceIdentity.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAdv.ADVSignedDeviceIdentity();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.details = r.bytes();
                        break;
                    }
                case 2: {
                        m.accountSignatureKey = r.bytes();
                        break;
                    }
                case 3: {
                        m.accountSignature = r.bytes();
                        break;
                    }
                case 4: {
                        m.deviceSignature = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ADVSignedDeviceIdentity.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAdv.ADVSignedDeviceIdentity)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAdv.ADVSignedDeviceIdentity();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.accountSignatureKey != null) {
                if (typeof d.accountSignatureKey === "string")
                    $util.base64.decode(d.accountSignatureKey, m.accountSignatureKey = $util.newBuffer($util.base64.length(d.accountSignatureKey)), 0);
                else if (d.accountSignatureKey.length >= 0)
                    m.accountSignatureKey = d.accountSignatureKey;
            }
            if (d.accountSignature != null) {
                if (typeof d.accountSignature === "string")
                    $util.base64.decode(d.accountSignature, m.accountSignature = $util.newBuffer($util.base64.length(d.accountSignature)), 0);
                else if (d.accountSignature.length >= 0)
                    m.accountSignature = d.accountSignature;
            }
            if (d.deviceSignature != null) {
                if (typeof d.deviceSignature === "string")
                    $util.base64.decode(d.deviceSignature, m.deviceSignature = $util.newBuffer($util.base64.length(d.deviceSignature)), 0);
                else if (d.deviceSignature.length >= 0)
                    m.deviceSignature = d.deviceSignature;
            }
            return m;
        };

        ADVSignedDeviceIdentity.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.details != null && m.hasOwnProperty("details")) {
                d.details = o.bytes === String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === Array ? Array.prototype.slice.call(m.details) : m.details;
                if (o.oneofs)
                    d._details = "details";
            }
            if (m.accountSignatureKey != null && m.hasOwnProperty("accountSignatureKey")) {
                d.accountSignatureKey = o.bytes === String ? $util.base64.encode(m.accountSignatureKey, 0, m.accountSignatureKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.accountSignatureKey) : m.accountSignatureKey;
                if (o.oneofs)
                    d._accountSignatureKey = "accountSignatureKey";
            }
            if (m.accountSignature != null && m.hasOwnProperty("accountSignature")) {
                d.accountSignature = o.bytes === String ? $util.base64.encode(m.accountSignature, 0, m.accountSignature.length) : o.bytes === Array ? Array.prototype.slice.call(m.accountSignature) : m.accountSignature;
                if (o.oneofs)
                    d._accountSignature = "accountSignature";
            }
            if (m.deviceSignature != null && m.hasOwnProperty("deviceSignature")) {
                d.deviceSignature = o.bytes === String ? $util.base64.encode(m.deviceSignature, 0, m.deviceSignature.length) : o.bytes === Array ? Array.prototype.slice.call(m.deviceSignature) : m.deviceSignature;
                if (o.oneofs)
                    d._deviceSignature = "deviceSignature";
            }
            return d;
        };

        ADVSignedDeviceIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedDeviceIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAdv.ADVSignedDeviceIdentity";
        };

        return ADVSignedDeviceIdentity;
    })();

    WAAdv.ADVSignedDeviceIdentityHMAC = (function() {

        function ADVSignedDeviceIdentityHMAC(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ADVSignedDeviceIdentityHMAC.prototype.details = null;
        ADVSignedDeviceIdentityHMAC.prototype.HMAC = null;
        ADVSignedDeviceIdentityHMAC.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_HMAC", {
            get: $util.oneOfGetter($oneOfFields = ["HMAC"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedDeviceIdentityHMAC.create = function create(properties) {
            return new ADVSignedDeviceIdentityHMAC(properties);
        };

        ADVSignedDeviceIdentityHMAC.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.details != null && Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.HMAC != null && Object.hasOwnProperty.call(m, "HMAC"))
                w.uint32(18).bytes(m.HMAC);
            if (m.accountType != null && Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(24).int32(m.accountType);
            return w;
        };

        ADVSignedDeviceIdentityHMAC.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAAdv.ADVSignedDeviceIdentityHMAC();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.details = r.bytes();
                        break;
                    }
                case 2: {
                        m.HMAC = r.bytes();
                        break;
                    }
                case 3: {
                        m.accountType = r.int32();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ADVSignedDeviceIdentityHMAC.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAAdv.ADVSignedDeviceIdentityHMAC)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAAdv.ADVSignedDeviceIdentityHMAC();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.HMAC != null) {
                if (typeof d.HMAC === "string")
                    $util.base64.decode(d.HMAC, m.HMAC = $util.newBuffer($util.base64.length(d.HMAC)), 0);
                else if (d.HMAC.length >= 0)
                    m.HMAC = d.HMAC;
            }
            switch (d.accountType) {
            default:
                if (typeof d.accountType === "number") {
                    m.accountType = d.accountType;
                    break;
                }
                break;
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            }
            return m;
        };

        ADVSignedDeviceIdentityHMAC.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.details != null && m.hasOwnProperty("details")) {
                d.details = o.bytes === String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === Array ? Array.prototype.slice.call(m.details) : m.details;
                if (o.oneofs)
                    d._details = "details";
            }
            if (m.HMAC != null && m.hasOwnProperty("HMAC")) {
                d.HMAC = o.bytes === String ? $util.base64.encode(m.HMAC, 0, m.HMAC.length) : o.bytes === Array ? Array.prototype.slice.call(m.HMAC) : m.HMAC;
                if (o.oneofs)
                    d._HMAC = "HMAC";
            }
            if (m.accountType != null && m.hasOwnProperty("accountType")) {
                d.accountType = o.enums === String ? $root.WAAdv.ADVEncryptionType[m.accountType] === undefined ? m.accountType : $root.WAAdv.ADVEncryptionType[m.accountType] : m.accountType;
                if (o.oneofs)
                    d._accountType = "accountType";
            }
            return d;
        };

        ADVSignedDeviceIdentityHMAC.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedDeviceIdentityHMAC.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAAdv.ADVSignedDeviceIdentityHMAC";
        };

        return ADVSignedDeviceIdentityHMAC;
    })();

    return WAAdv;
})();

export const WACompanionReg = $root.WACompanionReg = (() => {

    const WACompanionReg = {};

    WACompanionReg.DeviceProps = (function() {

        function DeviceProps(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        DeviceProps.prototype.os = null;
        DeviceProps.prototype.version = null;
        DeviceProps.prototype.platformType = null;
        DeviceProps.prototype.requireFullSync = null;
        DeviceProps.prototype.historySyncConfig = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_os", {
            get: $util.oneOfGetter($oneOfFields = ["os"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_platformType", {
            get: $util.oneOfGetter($oneOfFields = ["platformType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_requireFullSync", {
            get: $util.oneOfGetter($oneOfFields = ["requireFullSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(DeviceProps.prototype, "_historySyncConfig", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceProps.create = function create(properties) {
            return new DeviceProps(properties);
        };

        DeviceProps.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.os != null && Object.hasOwnProperty.call(m, "os"))
                w.uint32(10).string(m.os);
            if (m.version != null && Object.hasOwnProperty.call(m, "version"))
                $root.WACompanionReg.DeviceProps.AppVersion.encode(m.version, w.uint32(18).fork()).ldelim();
            if (m.platformType != null && Object.hasOwnProperty.call(m, "platformType"))
                w.uint32(24).int32(m.platformType);
            if (m.requireFullSync != null && Object.hasOwnProperty.call(m, "requireFullSync"))
                w.uint32(32).bool(m.requireFullSync);
            if (m.historySyncConfig != null && Object.hasOwnProperty.call(m, "historySyncConfig"))
                $root.WACompanionReg.DeviceProps.HistorySyncConfig.encode(m.historySyncConfig, w.uint32(42).fork()).ldelim();
            return w;
        };

        DeviceProps.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.DeviceProps();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.os = r.string();
                        break;
                    }
                case 2: {
                        m.version = $root.WACompanionReg.DeviceProps.AppVersion.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 3: {
                        m.platformType = r.int32();
                        break;
                    }
                case 4: {
                        m.requireFullSync = r.bool();
                        break;
                    }
                case 5: {
                        m.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        DeviceProps.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.DeviceProps)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.DeviceProps();
            if (d.os != null) {
                m.os = String(d.os);
            }
            if (d.version != null) {
                if (typeof d.version !== "object")
                    throw TypeError(".WACompanionReg.DeviceProps.version: object expected");
                m.version = $root.WACompanionReg.DeviceProps.AppVersion.fromObject(d.version, n + 1);
            }
            switch (d.platformType) {
            default:
                if (typeof d.platformType === "number") {
                    m.platformType = d.platformType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.platformType = 0;
                break;
            case "CHROME":
            case 1:
                m.platformType = 1;
                break;
            case "FIREFOX":
            case 2:
                m.platformType = 2;
                break;
            case "IE":
            case 3:
                m.platformType = 3;
                break;
            case "OPERA":
            case 4:
                m.platformType = 4;
                break;
            case "SAFARI":
            case 5:
                m.platformType = 5;
                break;
            case "EDGE":
            case 6:
                m.platformType = 6;
                break;
            case "DESKTOP":
            case 7:
                m.platformType = 7;
                break;
            case "IPAD":
            case 8:
                m.platformType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                m.platformType = 9;
                break;
            case "OHANA":
            case 10:
                m.platformType = 10;
                break;
            case "ALOHA":
            case 11:
                m.platformType = 11;
                break;
            case "CATALINA":
            case 12:
                m.platformType = 12;
                break;
            case "TCL_TV":
            case 13:
                m.platformType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                m.platformType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                m.platformType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                m.platformType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                m.platformType = 17;
                break;
            case "WEAR_OS":
            case 18:
                m.platformType = 18;
                break;
            case "AR_WRIST":
            case 19:
                m.platformType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                m.platformType = 20;
                break;
            case "UWP":
            case 21:
                m.platformType = 21;
                break;
            case "VR":
            case 22:
                m.platformType = 22;
                break;
            case "CLOUD_API":
            case 23:
                m.platformType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                m.platformType = 24;
                break;
            }
            if (d.requireFullSync != null) {
                m.requireFullSync = Boolean(d.requireFullSync);
            }
            if (d.historySyncConfig != null) {
                if (typeof d.historySyncConfig !== "object")
                    throw TypeError(".WACompanionReg.DeviceProps.historySyncConfig: object expected");
                m.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.fromObject(d.historySyncConfig, n + 1);
            }
            return m;
        };

        DeviceProps.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.os != null && m.hasOwnProperty("os")) {
                d.os = m.os;
                if (o.oneofs)
                    d._os = "os";
            }
            if (m.version != null && m.hasOwnProperty("version")) {
                d.version = $root.WACompanionReg.DeviceProps.AppVersion.toObject(m.version, o);
                if (o.oneofs)
                    d._version = "version";
            }
            if (m.platformType != null && m.hasOwnProperty("platformType")) {
                d.platformType = o.enums === String ? $root.WACompanionReg.DeviceProps.PlatformType[m.platformType] === undefined ? m.platformType : $root.WACompanionReg.DeviceProps.PlatformType[m.platformType] : m.platformType;
                if (o.oneofs)
                    d._platformType = "platformType";
            }
            if (m.requireFullSync != null && m.hasOwnProperty("requireFullSync")) {
                d.requireFullSync = m.requireFullSync;
                if (o.oneofs)
                    d._requireFullSync = "requireFullSync";
            }
            if (m.historySyncConfig != null && m.hasOwnProperty("historySyncConfig")) {
                d.historySyncConfig = $root.WACompanionReg.DeviceProps.HistorySyncConfig.toObject(m.historySyncConfig, o);
                if (o.oneofs)
                    d._historySyncConfig = "historySyncConfig";
            }
            return d;
        };

        DeviceProps.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceProps.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.DeviceProps";
        };

        DeviceProps.PlatformType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHROME"] = 1;
            values[valuesById[2] = "FIREFOX"] = 2;
            values[valuesById[3] = "IE"] = 3;
            values[valuesById[4] = "OPERA"] = 4;
            values[valuesById[5] = "SAFARI"] = 5;
            values[valuesById[6] = "EDGE"] = 6;
            values[valuesById[7] = "DESKTOP"] = 7;
            values[valuesById[8] = "IPAD"] = 8;
            values[valuesById[9] = "ANDROID_TABLET"] = 9;
            values[valuesById[10] = "OHANA"] = 10;
            values[valuesById[11] = "ALOHA"] = 11;
            values[valuesById[12] = "CATALINA"] = 12;
            values[valuesById[13] = "TCL_TV"] = 13;
            values[valuesById[14] = "IOS_PHONE"] = 14;
            values[valuesById[15] = "IOS_CATALYST"] = 15;
            values[valuesById[16] = "ANDROID_PHONE"] = 16;
            values[valuesById[17] = "ANDROID_AMBIGUOUS"] = 17;
            values[valuesById[18] = "WEAR_OS"] = 18;
            values[valuesById[19] = "AR_WRIST"] = 19;
            values[valuesById[20] = "AR_DEVICE"] = 20;
            values[valuesById[21] = "UWP"] = 21;
            values[valuesById[22] = "VR"] = 22;
            values[valuesById[23] = "CLOUD_API"] = 23;
            values[valuesById[24] = "SMARTGLASSES"] = 24;
            return values;
        })();

        DeviceProps.HistorySyncConfig = (function() {

            function HistorySyncConfig(p) {
                this.supportedBotChannelFbids = [];
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            HistorySyncConfig.prototype.fullSyncDaysLimit = null;
            HistorySyncConfig.prototype.fullSyncSizeMbLimit = null;
            HistorySyncConfig.prototype.storageQuotaMb = null;
            HistorySyncConfig.prototype.inlineInitialPayloadInE2EeMsg = null;
            HistorySyncConfig.prototype.recentSyncDaysLimit = null;
            HistorySyncConfig.prototype.supportCallLogHistory = null;
            HistorySyncConfig.prototype.supportBotUserAgentChatHistory = null;
            HistorySyncConfig.prototype.supportCagReactionsAndPolls = null;
            HistorySyncConfig.prototype.supportBizHostedMsg = null;
            HistorySyncConfig.prototype.supportRecentSyncChunkMessageCountTuning = null;
            HistorySyncConfig.prototype.supportHostedGroupMsg = null;
            HistorySyncConfig.prototype.supportFbidBotChatHistory = null;
            HistorySyncConfig.prototype.supportAddOnHistorySyncMigration = null;
            HistorySyncConfig.prototype.supportMessageAssociation = null;
            HistorySyncConfig.prototype.supportGroupHistory = null;
            HistorySyncConfig.prototype.onDemandReady = null;
            HistorySyncConfig.prototype.supportGuestChat = null;
            HistorySyncConfig.prototype.completeOnDemandReady = null;
            HistorySyncConfig.prototype.thumbnailSyncDaysLimit = null;
            HistorySyncConfig.prototype.initialSyncMaxMessagesPerChat = null;
            HistorySyncConfig.prototype.supportManusHistory = null;
            HistorySyncConfig.prototype.supportHatchHistory = null;
            HistorySyncConfig.prototype.supportedBotChannelFbids = $util.emptyArray;
            HistorySyncConfig.prototype.supportInlineContacts = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncSizeMbLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncSizeMbLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_storageQuotaMb", {
                get: $util.oneOfGetter($oneOfFields = ["storageQuotaMb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_inlineInitialPayloadInE2EeMsg", {
                get: $util.oneOfGetter($oneOfFields = ["inlineInitialPayloadInE2EeMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_recentSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["recentSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportCallLogHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportCallLogHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportBotUserAgentChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportBotUserAgentChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportCagReactionsAndPolls", {
                get: $util.oneOfGetter($oneOfFields = ["supportCagReactionsAndPolls"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportBizHostedMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportBizHostedMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportRecentSyncChunkMessageCountTuning", {
                get: $util.oneOfGetter($oneOfFields = ["supportRecentSyncChunkMessageCountTuning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportHostedGroupMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportHostedGroupMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportFbidBotChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportFbidBotChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportAddOnHistorySyncMigration", {
                get: $util.oneOfGetter($oneOfFields = ["supportAddOnHistorySyncMigration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportMessageAssociation", {
                get: $util.oneOfGetter($oneOfFields = ["supportMessageAssociation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportGroupHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportGroupHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_onDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["onDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportGuestChat", {
                get: $util.oneOfGetter($oneOfFields = ["supportGuestChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_completeOnDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["completeOnDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_thumbnailSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_initialSyncMaxMessagesPerChat", {
                get: $util.oneOfGetter($oneOfFields = ["initialSyncMaxMessagesPerChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportManusHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportManusHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportHatchHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportHatchHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(HistorySyncConfig.prototype, "_supportInlineContacts", {
                get: $util.oneOfGetter($oneOfFields = ["supportInlineContacts"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncConfig.create = function create(properties) {
                return new HistorySyncConfig(properties);
            };

            HistorySyncConfig.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.fullSyncDaysLimit != null && Object.hasOwnProperty.call(m, "fullSyncDaysLimit"))
                    w.uint32(8).uint32(m.fullSyncDaysLimit);
                if (m.fullSyncSizeMbLimit != null && Object.hasOwnProperty.call(m, "fullSyncSizeMbLimit"))
                    w.uint32(16).uint32(m.fullSyncSizeMbLimit);
                if (m.storageQuotaMb != null && Object.hasOwnProperty.call(m, "storageQuotaMb"))
                    w.uint32(24).uint32(m.storageQuotaMb);
                if (m.inlineInitialPayloadInE2EeMsg != null && Object.hasOwnProperty.call(m, "inlineInitialPayloadInE2EeMsg"))
                    w.uint32(32).bool(m.inlineInitialPayloadInE2EeMsg);
                if (m.recentSyncDaysLimit != null && Object.hasOwnProperty.call(m, "recentSyncDaysLimit"))
                    w.uint32(40).uint32(m.recentSyncDaysLimit);
                if (m.supportCallLogHistory != null && Object.hasOwnProperty.call(m, "supportCallLogHistory"))
                    w.uint32(48).bool(m.supportCallLogHistory);
                if (m.supportBotUserAgentChatHistory != null && Object.hasOwnProperty.call(m, "supportBotUserAgentChatHistory"))
                    w.uint32(56).bool(m.supportBotUserAgentChatHistory);
                if (m.supportCagReactionsAndPolls != null && Object.hasOwnProperty.call(m, "supportCagReactionsAndPolls"))
                    w.uint32(64).bool(m.supportCagReactionsAndPolls);
                if (m.supportBizHostedMsg != null && Object.hasOwnProperty.call(m, "supportBizHostedMsg"))
                    w.uint32(72).bool(m.supportBizHostedMsg);
                if (m.supportRecentSyncChunkMessageCountTuning != null && Object.hasOwnProperty.call(m, "supportRecentSyncChunkMessageCountTuning"))
                    w.uint32(80).bool(m.supportRecentSyncChunkMessageCountTuning);
                if (m.supportHostedGroupMsg != null && Object.hasOwnProperty.call(m, "supportHostedGroupMsg"))
                    w.uint32(88).bool(m.supportHostedGroupMsg);
                if (m.supportFbidBotChatHistory != null && Object.hasOwnProperty.call(m, "supportFbidBotChatHistory"))
                    w.uint32(96).bool(m.supportFbidBotChatHistory);
                if (m.supportAddOnHistorySyncMigration != null && Object.hasOwnProperty.call(m, "supportAddOnHistorySyncMigration"))
                    w.uint32(104).bool(m.supportAddOnHistorySyncMigration);
                if (m.supportMessageAssociation != null && Object.hasOwnProperty.call(m, "supportMessageAssociation"))
                    w.uint32(112).bool(m.supportMessageAssociation);
                if (m.supportGroupHistory != null && Object.hasOwnProperty.call(m, "supportGroupHistory"))
                    w.uint32(120).bool(m.supportGroupHistory);
                if (m.onDemandReady != null && Object.hasOwnProperty.call(m, "onDemandReady"))
                    w.uint32(128).bool(m.onDemandReady);
                if (m.supportGuestChat != null && Object.hasOwnProperty.call(m, "supportGuestChat"))
                    w.uint32(136).bool(m.supportGuestChat);
                if (m.completeOnDemandReady != null && Object.hasOwnProperty.call(m, "completeOnDemandReady"))
                    w.uint32(144).bool(m.completeOnDemandReady);
                if (m.thumbnailSyncDaysLimit != null && Object.hasOwnProperty.call(m, "thumbnailSyncDaysLimit"))
                    w.uint32(152).uint32(m.thumbnailSyncDaysLimit);
                if (m.initialSyncMaxMessagesPerChat != null && Object.hasOwnProperty.call(m, "initialSyncMaxMessagesPerChat"))
                    w.uint32(160).uint32(m.initialSyncMaxMessagesPerChat);
                if (m.supportManusHistory != null && Object.hasOwnProperty.call(m, "supportManusHistory"))
                    w.uint32(168).bool(m.supportManusHistory);
                if (m.supportHatchHistory != null && Object.hasOwnProperty.call(m, "supportHatchHistory"))
                    w.uint32(176).bool(m.supportHatchHistory);
                if (m.supportedBotChannelFbids != null && m.supportedBotChannelFbids.length) {
                    for (var i = 0; i < m.supportedBotChannelFbids.length; ++i)
                        w.uint32(186).string(m.supportedBotChannelFbids[i]);
                }
                if (m.supportInlineContacts != null && Object.hasOwnProperty.call(m, "supportInlineContacts"))
                    w.uint32(192).bool(m.supportInlineContacts);
                return w;
            };

            HistorySyncConfig.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.DeviceProps.HistorySyncConfig();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.fullSyncDaysLimit = r.uint32();
                            break;
                        }
                    case 2: {
                            m.fullSyncSizeMbLimit = r.uint32();
                            break;
                        }
                    case 3: {
                            m.storageQuotaMb = r.uint32();
                            break;
                        }
                    case 4: {
                            m.inlineInitialPayloadInE2EeMsg = r.bool();
                            break;
                        }
                    case 5: {
                            m.recentSyncDaysLimit = r.uint32();
                            break;
                        }
                    case 6: {
                            m.supportCallLogHistory = r.bool();
                            break;
                        }
                    case 7: {
                            m.supportBotUserAgentChatHistory = r.bool();
                            break;
                        }
                    case 8: {
                            m.supportCagReactionsAndPolls = r.bool();
                            break;
                        }
                    case 9: {
                            m.supportBizHostedMsg = r.bool();
                            break;
                        }
                    case 10: {
                            m.supportRecentSyncChunkMessageCountTuning = r.bool();
                            break;
                        }
                    case 11: {
                            m.supportHostedGroupMsg = r.bool();
                            break;
                        }
                    case 12: {
                            m.supportFbidBotChatHistory = r.bool();
                            break;
                        }
                    case 13: {
                            m.supportAddOnHistorySyncMigration = r.bool();
                            break;
                        }
                    case 14: {
                            m.supportMessageAssociation = r.bool();
                            break;
                        }
                    case 15: {
                            m.supportGroupHistory = r.bool();
                            break;
                        }
                    case 16: {
                            m.onDemandReady = r.bool();
                            break;
                        }
                    case 17: {
                            m.supportGuestChat = r.bool();
                            break;
                        }
                    case 18: {
                            m.completeOnDemandReady = r.bool();
                            break;
                        }
                    case 19: {
                            m.thumbnailSyncDaysLimit = r.uint32();
                            break;
                        }
                    case 20: {
                            m.initialSyncMaxMessagesPerChat = r.uint32();
                            break;
                        }
                    case 21: {
                            m.supportManusHistory = r.bool();
                            break;
                        }
                    case 22: {
                            m.supportHatchHistory = r.bool();
                            break;
                        }
                    case 23: {
                            if (!(m.supportedBotChannelFbids && m.supportedBotChannelFbids.length))
                                m.supportedBotChannelFbids = [];
                            m.supportedBotChannelFbids.push(r.string());
                            break;
                        }
                    case 24: {
                            m.supportInlineContacts = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            HistorySyncConfig.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WACompanionReg.DeviceProps.HistorySyncConfig)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WACompanionReg.DeviceProps.HistorySyncConfig();
                if (d.fullSyncDaysLimit != null) {
                    m.fullSyncDaysLimit = d.fullSyncDaysLimit >>> 0;
                }
                if (d.fullSyncSizeMbLimit != null) {
                    m.fullSyncSizeMbLimit = d.fullSyncSizeMbLimit >>> 0;
                }
                if (d.storageQuotaMb != null) {
                    m.storageQuotaMb = d.storageQuotaMb >>> 0;
                }
                if (d.inlineInitialPayloadInE2EeMsg != null) {
                    m.inlineInitialPayloadInE2EeMsg = Boolean(d.inlineInitialPayloadInE2EeMsg);
                }
                if (d.recentSyncDaysLimit != null) {
                    m.recentSyncDaysLimit = d.recentSyncDaysLimit >>> 0;
                }
                if (d.supportCallLogHistory != null) {
                    m.supportCallLogHistory = Boolean(d.supportCallLogHistory);
                }
                if (d.supportBotUserAgentChatHistory != null) {
                    m.supportBotUserAgentChatHistory = Boolean(d.supportBotUserAgentChatHistory);
                }
                if (d.supportCagReactionsAndPolls != null) {
                    m.supportCagReactionsAndPolls = Boolean(d.supportCagReactionsAndPolls);
                }
                if (d.supportBizHostedMsg != null) {
                    m.supportBizHostedMsg = Boolean(d.supportBizHostedMsg);
                }
                if (d.supportRecentSyncChunkMessageCountTuning != null) {
                    m.supportRecentSyncChunkMessageCountTuning = Boolean(d.supportRecentSyncChunkMessageCountTuning);
                }
                if (d.supportHostedGroupMsg != null) {
                    m.supportHostedGroupMsg = Boolean(d.supportHostedGroupMsg);
                }
                if (d.supportFbidBotChatHistory != null) {
                    m.supportFbidBotChatHistory = Boolean(d.supportFbidBotChatHistory);
                }
                if (d.supportAddOnHistorySyncMigration != null) {
                    m.supportAddOnHistorySyncMigration = Boolean(d.supportAddOnHistorySyncMigration);
                }
                if (d.supportMessageAssociation != null) {
                    m.supportMessageAssociation = Boolean(d.supportMessageAssociation);
                }
                if (d.supportGroupHistory != null) {
                    m.supportGroupHistory = Boolean(d.supportGroupHistory);
                }
                if (d.onDemandReady != null) {
                    m.onDemandReady = Boolean(d.onDemandReady);
                }
                if (d.supportGuestChat != null) {
                    m.supportGuestChat = Boolean(d.supportGuestChat);
                }
                if (d.completeOnDemandReady != null) {
                    m.completeOnDemandReady = Boolean(d.completeOnDemandReady);
                }
                if (d.thumbnailSyncDaysLimit != null) {
                    m.thumbnailSyncDaysLimit = d.thumbnailSyncDaysLimit >>> 0;
                }
                if (d.initialSyncMaxMessagesPerChat != null) {
                    m.initialSyncMaxMessagesPerChat = d.initialSyncMaxMessagesPerChat >>> 0;
                }
                if (d.supportManusHistory != null) {
                    m.supportManusHistory = Boolean(d.supportManusHistory);
                }
                if (d.supportHatchHistory != null) {
                    m.supportHatchHistory = Boolean(d.supportHatchHistory);
                }
                if (d.supportedBotChannelFbids) {
                    if (!Array.isArray(d.supportedBotChannelFbids))
                        throw TypeError(".WACompanionReg.DeviceProps.HistorySyncConfig.supportedBotChannelFbids: array expected");
                    m.supportedBotChannelFbids = [];
                    for (var i = 0; i < d.supportedBotChannelFbids.length; ++i) {
                        m.supportedBotChannelFbids[i] = String(d.supportedBotChannelFbids[i]);
                    }
                }
                if (d.supportInlineContacts != null) {
                    m.supportInlineContacts = Boolean(d.supportInlineContacts);
                }
                return m;
            };

            HistorySyncConfig.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (o.arrays || o.defaults) {
                    d.supportedBotChannelFbids = [];
                }
                if (m.fullSyncDaysLimit != null && m.hasOwnProperty("fullSyncDaysLimit")) {
                    d.fullSyncDaysLimit = m.fullSyncDaysLimit;
                    if (o.oneofs)
                        d._fullSyncDaysLimit = "fullSyncDaysLimit";
                }
                if (m.fullSyncSizeMbLimit != null && m.hasOwnProperty("fullSyncSizeMbLimit")) {
                    d.fullSyncSizeMbLimit = m.fullSyncSizeMbLimit;
                    if (o.oneofs)
                        d._fullSyncSizeMbLimit = "fullSyncSizeMbLimit";
                }
                if (m.storageQuotaMb != null && m.hasOwnProperty("storageQuotaMb")) {
                    d.storageQuotaMb = m.storageQuotaMb;
                    if (o.oneofs)
                        d._storageQuotaMb = "storageQuotaMb";
                }
                if (m.inlineInitialPayloadInE2EeMsg != null && m.hasOwnProperty("inlineInitialPayloadInE2EeMsg")) {
                    d.inlineInitialPayloadInE2EeMsg = m.inlineInitialPayloadInE2EeMsg;
                    if (o.oneofs)
                        d._inlineInitialPayloadInE2EeMsg = "inlineInitialPayloadInE2EeMsg";
                }
                if (m.recentSyncDaysLimit != null && m.hasOwnProperty("recentSyncDaysLimit")) {
                    d.recentSyncDaysLimit = m.recentSyncDaysLimit;
                    if (o.oneofs)
                        d._recentSyncDaysLimit = "recentSyncDaysLimit";
                }
                if (m.supportCallLogHistory != null && m.hasOwnProperty("supportCallLogHistory")) {
                    d.supportCallLogHistory = m.supportCallLogHistory;
                    if (o.oneofs)
                        d._supportCallLogHistory = "supportCallLogHistory";
                }
                if (m.supportBotUserAgentChatHistory != null && m.hasOwnProperty("supportBotUserAgentChatHistory")) {
                    d.supportBotUserAgentChatHistory = m.supportBotUserAgentChatHistory;
                    if (o.oneofs)
                        d._supportBotUserAgentChatHistory = "supportBotUserAgentChatHistory";
                }
                if (m.supportCagReactionsAndPolls != null && m.hasOwnProperty("supportCagReactionsAndPolls")) {
                    d.supportCagReactionsAndPolls = m.supportCagReactionsAndPolls;
                    if (o.oneofs)
                        d._supportCagReactionsAndPolls = "supportCagReactionsAndPolls";
                }
                if (m.supportBizHostedMsg != null && m.hasOwnProperty("supportBizHostedMsg")) {
                    d.supportBizHostedMsg = m.supportBizHostedMsg;
                    if (o.oneofs)
                        d._supportBizHostedMsg = "supportBizHostedMsg";
                }
                if (m.supportRecentSyncChunkMessageCountTuning != null && m.hasOwnProperty("supportRecentSyncChunkMessageCountTuning")) {
                    d.supportRecentSyncChunkMessageCountTuning = m.supportRecentSyncChunkMessageCountTuning;
                    if (o.oneofs)
                        d._supportRecentSyncChunkMessageCountTuning = "supportRecentSyncChunkMessageCountTuning";
                }
                if (m.supportHostedGroupMsg != null && m.hasOwnProperty("supportHostedGroupMsg")) {
                    d.supportHostedGroupMsg = m.supportHostedGroupMsg;
                    if (o.oneofs)
                        d._supportHostedGroupMsg = "supportHostedGroupMsg";
                }
                if (m.supportFbidBotChatHistory != null && m.hasOwnProperty("supportFbidBotChatHistory")) {
                    d.supportFbidBotChatHistory = m.supportFbidBotChatHistory;
                    if (o.oneofs)
                        d._supportFbidBotChatHistory = "supportFbidBotChatHistory";
                }
                if (m.supportAddOnHistorySyncMigration != null && m.hasOwnProperty("supportAddOnHistorySyncMigration")) {
                    d.supportAddOnHistorySyncMigration = m.supportAddOnHistorySyncMigration;
                    if (o.oneofs)
                        d._supportAddOnHistorySyncMigration = "supportAddOnHistorySyncMigration";
                }
                if (m.supportMessageAssociation != null && m.hasOwnProperty("supportMessageAssociation")) {
                    d.supportMessageAssociation = m.supportMessageAssociation;
                    if (o.oneofs)
                        d._supportMessageAssociation = "supportMessageAssociation";
                }
                if (m.supportGroupHistory != null && m.hasOwnProperty("supportGroupHistory")) {
                    d.supportGroupHistory = m.supportGroupHistory;
                    if (o.oneofs)
                        d._supportGroupHistory = "supportGroupHistory";
                }
                if (m.onDemandReady != null && m.hasOwnProperty("onDemandReady")) {
                    d.onDemandReady = m.onDemandReady;
                    if (o.oneofs)
                        d._onDemandReady = "onDemandReady";
                }
                if (m.supportGuestChat != null && m.hasOwnProperty("supportGuestChat")) {
                    d.supportGuestChat = m.supportGuestChat;
                    if (o.oneofs)
                        d._supportGuestChat = "supportGuestChat";
                }
                if (m.completeOnDemandReady != null && m.hasOwnProperty("completeOnDemandReady")) {
                    d.completeOnDemandReady = m.completeOnDemandReady;
                    if (o.oneofs)
                        d._completeOnDemandReady = "completeOnDemandReady";
                }
                if (m.thumbnailSyncDaysLimit != null && m.hasOwnProperty("thumbnailSyncDaysLimit")) {
                    d.thumbnailSyncDaysLimit = m.thumbnailSyncDaysLimit;
                    if (o.oneofs)
                        d._thumbnailSyncDaysLimit = "thumbnailSyncDaysLimit";
                }
                if (m.initialSyncMaxMessagesPerChat != null && m.hasOwnProperty("initialSyncMaxMessagesPerChat")) {
                    d.initialSyncMaxMessagesPerChat = m.initialSyncMaxMessagesPerChat;
                    if (o.oneofs)
                        d._initialSyncMaxMessagesPerChat = "initialSyncMaxMessagesPerChat";
                }
                if (m.supportManusHistory != null && m.hasOwnProperty("supportManusHistory")) {
                    d.supportManusHistory = m.supportManusHistory;
                    if (o.oneofs)
                        d._supportManusHistory = "supportManusHistory";
                }
                if (m.supportHatchHistory != null && m.hasOwnProperty("supportHatchHistory")) {
                    d.supportHatchHistory = m.supportHatchHistory;
                    if (o.oneofs)
                        d._supportHatchHistory = "supportHatchHistory";
                }
                if (m.supportedBotChannelFbids && m.supportedBotChannelFbids.length) {
                    d.supportedBotChannelFbids = [];
                    for (var j = 0; j < m.supportedBotChannelFbids.length; ++j) {
                        d.supportedBotChannelFbids[j] = m.supportedBotChannelFbids[j];
                    }
                }
                if (m.supportInlineContacts != null && m.hasOwnProperty("supportInlineContacts")) {
                    d.supportInlineContacts = m.supportInlineContacts;
                    if (o.oneofs)
                        d._supportInlineContacts = "supportInlineContacts";
                }
                return d;
            };

            HistorySyncConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WACompanionReg.DeviceProps.HistorySyncConfig";
            };

            return HistorySyncConfig;
        })();

        DeviceProps.AppVersion = (function() {

            function AppVersion(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AppVersion.prototype.primary = null;
            AppVersion.prototype.secondary = null;
            AppVersion.prototype.tertiary = null;
            AppVersion.prototype.quaternary = null;
            AppVersion.prototype.quinary = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_primary", {
                get: $util.oneOfGetter($oneOfFields = ["primary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_secondary", {
                get: $util.oneOfGetter($oneOfFields = ["secondary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_tertiary", {
                get: $util.oneOfGetter($oneOfFields = ["tertiary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_quaternary", {
                get: $util.oneOfGetter($oneOfFields = ["quaternary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AppVersion.prototype, "_quinary", {
                get: $util.oneOfGetter($oneOfFields = ["quinary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppVersion.create = function create(properties) {
                return new AppVersion(properties);
            };

            AppVersion.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.primary != null && Object.hasOwnProperty.call(m, "primary"))
                    w.uint32(8).uint32(m.primary);
                if (m.secondary != null && Object.hasOwnProperty.call(m, "secondary"))
                    w.uint32(16).uint32(m.secondary);
                if (m.tertiary != null && Object.hasOwnProperty.call(m, "tertiary"))
                    w.uint32(24).uint32(m.tertiary);
                if (m.quaternary != null && Object.hasOwnProperty.call(m, "quaternary"))
                    w.uint32(32).uint32(m.quaternary);
                if (m.quinary != null && Object.hasOwnProperty.call(m, "quinary"))
                    w.uint32(40).uint32(m.quinary);
                return w;
            };

            AppVersion.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.DeviceProps.AppVersion();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.primary = r.uint32();
                            break;
                        }
                    case 2: {
                            m.secondary = r.uint32();
                            break;
                        }
                    case 3: {
                            m.tertiary = r.uint32();
                            break;
                        }
                    case 4: {
                            m.quaternary = r.uint32();
                            break;
                        }
                    case 5: {
                            m.quinary = r.uint32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AppVersion.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WACompanionReg.DeviceProps.AppVersion)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WACompanionReg.DeviceProps.AppVersion();
                if (d.primary != null) {
                    m.primary = d.primary >>> 0;
                }
                if (d.secondary != null) {
                    m.secondary = d.secondary >>> 0;
                }
                if (d.tertiary != null) {
                    m.tertiary = d.tertiary >>> 0;
                }
                if (d.quaternary != null) {
                    m.quaternary = d.quaternary >>> 0;
                }
                if (d.quinary != null) {
                    m.quinary = d.quinary >>> 0;
                }
                return m;
            };

            AppVersion.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.primary != null && m.hasOwnProperty("primary")) {
                    d.primary = m.primary;
                    if (o.oneofs)
                        d._primary = "primary";
                }
                if (m.secondary != null && m.hasOwnProperty("secondary")) {
                    d.secondary = m.secondary;
                    if (o.oneofs)
                        d._secondary = "secondary";
                }
                if (m.tertiary != null && m.hasOwnProperty("tertiary")) {
                    d.tertiary = m.tertiary;
                    if (o.oneofs)
                        d._tertiary = "tertiary";
                }
                if (m.quaternary != null && m.hasOwnProperty("quaternary")) {
                    d.quaternary = m.quaternary;
                    if (o.oneofs)
                        d._quaternary = "quaternary";
                }
                if (m.quinary != null && m.hasOwnProperty("quinary")) {
                    d.quinary = m.quinary;
                    if (o.oneofs)
                        d._quinary = "quinary";
                }
                return d;
            };

            AppVersion.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppVersion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WACompanionReg.DeviceProps.AppVersion";
            };

            return AppVersion;
        })();

        return DeviceProps;
    })();

    WACompanionReg.CompanionEphemeralIdentity = (function() {

        function CompanionEphemeralIdentity(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CompanionEphemeralIdentity.prototype.publicKey = null;
        CompanionEphemeralIdentity.prototype.deviceType = null;
        CompanionEphemeralIdentity.prototype.ref = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionEphemeralIdentity.prototype, "_ref", {
            get: $util.oneOfGetter($oneOfFields = ["ref"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CompanionEphemeralIdentity.create = function create(properties) {
            return new CompanionEphemeralIdentity(properties);
        };

        CompanionEphemeralIdentity.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.publicKey != null && Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.deviceType != null && Object.hasOwnProperty.call(m, "deviceType"))
                w.uint32(16).int32(m.deviceType);
            if (m.ref != null && Object.hasOwnProperty.call(m, "ref"))
                w.uint32(26).string(m.ref);
            return w;
        };

        CompanionEphemeralIdentity.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.CompanionEphemeralIdentity();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.publicKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.deviceType = r.int32();
                        break;
                    }
                case 3: {
                        m.ref = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CompanionEphemeralIdentity.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.CompanionEphemeralIdentity)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.CompanionEphemeralIdentity();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            switch (d.deviceType) {
            default:
                if (typeof d.deviceType === "number") {
                    m.deviceType = d.deviceType;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.deviceType = 0;
                break;
            case "CHROME":
            case 1:
                m.deviceType = 1;
                break;
            case "FIREFOX":
            case 2:
                m.deviceType = 2;
                break;
            case "IE":
            case 3:
                m.deviceType = 3;
                break;
            case "OPERA":
            case 4:
                m.deviceType = 4;
                break;
            case "SAFARI":
            case 5:
                m.deviceType = 5;
                break;
            case "EDGE":
            case 6:
                m.deviceType = 6;
                break;
            case "DESKTOP":
            case 7:
                m.deviceType = 7;
                break;
            case "IPAD":
            case 8:
                m.deviceType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                m.deviceType = 9;
                break;
            case "OHANA":
            case 10:
                m.deviceType = 10;
                break;
            case "ALOHA":
            case 11:
                m.deviceType = 11;
                break;
            case "CATALINA":
            case 12:
                m.deviceType = 12;
                break;
            case "TCL_TV":
            case 13:
                m.deviceType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                m.deviceType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                m.deviceType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                m.deviceType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                m.deviceType = 17;
                break;
            case "WEAR_OS":
            case 18:
                m.deviceType = 18;
                break;
            case "AR_WRIST":
            case 19:
                m.deviceType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                m.deviceType = 20;
                break;
            case "UWP":
            case 21:
                m.deviceType = 21;
                break;
            case "VR":
            case 22:
                m.deviceType = 22;
                break;
            case "CLOUD_API":
            case 23:
                m.deviceType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                m.deviceType = 24;
                break;
            }
            if (d.ref != null) {
                m.ref = String(d.ref);
            }
            return m;
        };

        CompanionEphemeralIdentity.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.publicKey != null && m.hasOwnProperty("publicKey")) {
                d.publicKey = o.bytes === String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.publicKey) : m.publicKey;
                if (o.oneofs)
                    d._publicKey = "publicKey";
            }
            if (m.deviceType != null && m.hasOwnProperty("deviceType")) {
                d.deviceType = o.enums === String ? $root.WACompanionReg.DeviceProps.PlatformType[m.deviceType] === undefined ? m.deviceType : $root.WACompanionReg.DeviceProps.PlatformType[m.deviceType] : m.deviceType;
                if (o.oneofs)
                    d._deviceType = "deviceType";
            }
            if (m.ref != null && m.hasOwnProperty("ref")) {
                d.ref = m.ref;
                if (o.oneofs)
                    d._ref = "ref";
            }
            return d;
        };

        CompanionEphemeralIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CompanionEphemeralIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.CompanionEphemeralIdentity";
        };

        return CompanionEphemeralIdentity;
    })();

    WACompanionReg.CompanionCommitment = (function() {

        function CompanionCommitment(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        CompanionCommitment.prototype.hash = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(CompanionCommitment.prototype, "_hash", {
            get: $util.oneOfGetter($oneOfFields = ["hash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CompanionCommitment.create = function create(properties) {
            return new CompanionCommitment(properties);
        };

        CompanionCommitment.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.hash != null && Object.hasOwnProperty.call(m, "hash"))
                w.uint32(10).bytes(m.hash);
            return w;
        };

        CompanionCommitment.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.CompanionCommitment();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.hash = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        CompanionCommitment.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.CompanionCommitment)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.CompanionCommitment();
            if (d.hash != null) {
                if (typeof d.hash === "string")
                    $util.base64.decode(d.hash, m.hash = $util.newBuffer($util.base64.length(d.hash)), 0);
                else if (d.hash.length >= 0)
                    m.hash = d.hash;
            }
            return m;
        };

        CompanionCommitment.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.hash != null && m.hasOwnProperty("hash")) {
                d.hash = o.bytes === String ? $util.base64.encode(m.hash, 0, m.hash.length) : o.bytes === Array ? Array.prototype.slice.call(m.hash) : m.hash;
                if (o.oneofs)
                    d._hash = "hash";
            }
            return d;
        };

        CompanionCommitment.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        CompanionCommitment.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.CompanionCommitment";
        };

        return CompanionCommitment;
    })();

    WACompanionReg.ProloguePayload = (function() {

        function ProloguePayload(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ProloguePayload.prototype.companionEphemeralIdentity = null;
        ProloguePayload.prototype.commitment = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProloguePayload.prototype, "_companionEphemeralIdentity", {
            get: $util.oneOfGetter($oneOfFields = ["companionEphemeralIdentity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ProloguePayload.prototype, "_commitment", {
            get: $util.oneOfGetter($oneOfFields = ["commitment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProloguePayload.create = function create(properties) {
            return new ProloguePayload(properties);
        };

        ProloguePayload.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.companionEphemeralIdentity != null && Object.hasOwnProperty.call(m, "companionEphemeralIdentity"))
                w.uint32(10).bytes(m.companionEphemeralIdentity);
            if (m.commitment != null && Object.hasOwnProperty.call(m, "commitment"))
                $root.WACompanionReg.CompanionCommitment.encode(m.commitment, w.uint32(18).fork()).ldelim();
            return w;
        };

        ProloguePayload.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.ProloguePayload();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.companionEphemeralIdentity = r.bytes();
                        break;
                    }
                case 2: {
                        m.commitment = $root.WACompanionReg.CompanionCommitment.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ProloguePayload.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.ProloguePayload)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.ProloguePayload();
            if (d.companionEphemeralIdentity != null) {
                if (typeof d.companionEphemeralIdentity === "string")
                    $util.base64.decode(d.companionEphemeralIdentity, m.companionEphemeralIdentity = $util.newBuffer($util.base64.length(d.companionEphemeralIdentity)), 0);
                else if (d.companionEphemeralIdentity.length >= 0)
                    m.companionEphemeralIdentity = d.companionEphemeralIdentity;
            }
            if (d.commitment != null) {
                if (typeof d.commitment !== "object")
                    throw TypeError(".WACompanionReg.ProloguePayload.commitment: object expected");
                m.commitment = $root.WACompanionReg.CompanionCommitment.fromObject(d.commitment, n + 1);
            }
            return m;
        };

        ProloguePayload.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.companionEphemeralIdentity != null && m.hasOwnProperty("companionEphemeralIdentity")) {
                d.companionEphemeralIdentity = o.bytes === String ? $util.base64.encode(m.companionEphemeralIdentity, 0, m.companionEphemeralIdentity.length) : o.bytes === Array ? Array.prototype.slice.call(m.companionEphemeralIdentity) : m.companionEphemeralIdentity;
                if (o.oneofs)
                    d._companionEphemeralIdentity = "companionEphemeralIdentity";
            }
            if (m.commitment != null && m.hasOwnProperty("commitment")) {
                d.commitment = $root.WACompanionReg.CompanionCommitment.toObject(m.commitment, o);
                if (o.oneofs)
                    d._commitment = "commitment";
            }
            return d;
        };

        ProloguePayload.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProloguePayload.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.ProloguePayload";
        };

        return ProloguePayload;
    })();

    WACompanionReg.PrimaryEphemeralIdentity = (function() {

        function PrimaryEphemeralIdentity(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PrimaryEphemeralIdentity.prototype.publicKey = null;
        PrimaryEphemeralIdentity.prototype.nonce = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_nonce", {
            get: $util.oneOfGetter($oneOfFields = ["nonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PrimaryEphemeralIdentity.create = function create(properties) {
            return new PrimaryEphemeralIdentity(properties);
        };

        PrimaryEphemeralIdentity.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.publicKey != null && Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.nonce != null && Object.hasOwnProperty.call(m, "nonce"))
                w.uint32(18).bytes(m.nonce);
            return w;
        };

        PrimaryEphemeralIdentity.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.PrimaryEphemeralIdentity();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.publicKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.nonce = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PrimaryEphemeralIdentity.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.PrimaryEphemeralIdentity)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.PrimaryEphemeralIdentity();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.nonce != null) {
                if (typeof d.nonce === "string")
                    $util.base64.decode(d.nonce, m.nonce = $util.newBuffer($util.base64.length(d.nonce)), 0);
                else if (d.nonce.length >= 0)
                    m.nonce = d.nonce;
            }
            return m;
        };

        PrimaryEphemeralIdentity.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.publicKey != null && m.hasOwnProperty("publicKey")) {
                d.publicKey = o.bytes === String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.publicKey) : m.publicKey;
                if (o.oneofs)
                    d._publicKey = "publicKey";
            }
            if (m.nonce != null && m.hasOwnProperty("nonce")) {
                d.nonce = o.bytes === String ? $util.base64.encode(m.nonce, 0, m.nonce.length) : o.bytes === Array ? Array.prototype.slice.call(m.nonce) : m.nonce;
                if (o.oneofs)
                    d._nonce = "nonce";
            }
            return d;
        };

        PrimaryEphemeralIdentity.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PrimaryEphemeralIdentity.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.PrimaryEphemeralIdentity";
        };

        return PrimaryEphemeralIdentity;
    })();

    WACompanionReg.PairingRequest = (function() {

        function PairingRequest(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        PairingRequest.prototype.companionPublicKey = null;
        PairingRequest.prototype.companionIdentityKey = null;
        PairingRequest.prototype.advSecret = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_companionPublicKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionPublicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_companionIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(PairingRequest.prototype, "_advSecret", {
            get: $util.oneOfGetter($oneOfFields = ["advSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PairingRequest.create = function create(properties) {
            return new PairingRequest(properties);
        };

        PairingRequest.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.companionPublicKey != null && Object.hasOwnProperty.call(m, "companionPublicKey"))
                w.uint32(10).bytes(m.companionPublicKey);
            if (m.companionIdentityKey != null && Object.hasOwnProperty.call(m, "companionIdentityKey"))
                w.uint32(18).bytes(m.companionIdentityKey);
            if (m.advSecret != null && Object.hasOwnProperty.call(m, "advSecret"))
                w.uint32(26).bytes(m.advSecret);
            return w;
        };

        PairingRequest.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.PairingRequest();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.companionPublicKey = r.bytes();
                        break;
                    }
                case 2: {
                        m.companionIdentityKey = r.bytes();
                        break;
                    }
                case 3: {
                        m.advSecret = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        PairingRequest.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.PairingRequest)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.PairingRequest();
            if (d.companionPublicKey != null) {
                if (typeof d.companionPublicKey === "string")
                    $util.base64.decode(d.companionPublicKey, m.companionPublicKey = $util.newBuffer($util.base64.length(d.companionPublicKey)), 0);
                else if (d.companionPublicKey.length >= 0)
                    m.companionPublicKey = d.companionPublicKey;
            }
            if (d.companionIdentityKey != null) {
                if (typeof d.companionIdentityKey === "string")
                    $util.base64.decode(d.companionIdentityKey, m.companionIdentityKey = $util.newBuffer($util.base64.length(d.companionIdentityKey)), 0);
                else if (d.companionIdentityKey.length >= 0)
                    m.companionIdentityKey = d.companionIdentityKey;
            }
            if (d.advSecret != null) {
                if (typeof d.advSecret === "string")
                    $util.base64.decode(d.advSecret, m.advSecret = $util.newBuffer($util.base64.length(d.advSecret)), 0);
                else if (d.advSecret.length >= 0)
                    m.advSecret = d.advSecret;
            }
            return m;
        };

        PairingRequest.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.companionPublicKey != null && m.hasOwnProperty("companionPublicKey")) {
                d.companionPublicKey = o.bytes === String ? $util.base64.encode(m.companionPublicKey, 0, m.companionPublicKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.companionPublicKey) : m.companionPublicKey;
                if (o.oneofs)
                    d._companionPublicKey = "companionPublicKey";
            }
            if (m.companionIdentityKey != null && m.hasOwnProperty("companionIdentityKey")) {
                d.companionIdentityKey = o.bytes === String ? $util.base64.encode(m.companionIdentityKey, 0, m.companionIdentityKey.length) : o.bytes === Array ? Array.prototype.slice.call(m.companionIdentityKey) : m.companionIdentityKey;
                if (o.oneofs)
                    d._companionIdentityKey = "companionIdentityKey";
            }
            if (m.advSecret != null && m.hasOwnProperty("advSecret")) {
                d.advSecret = o.bytes === String ? $util.base64.encode(m.advSecret, 0, m.advSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.advSecret) : m.advSecret;
                if (o.oneofs)
                    d._advSecret = "advSecret";
            }
            return d;
        };

        PairingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        PairingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.PairingRequest";
        };

        return PairingRequest;
    })();

    WACompanionReg.EncryptedPairingRequest = (function() {

        function EncryptedPairingRequest(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        EncryptedPairingRequest.prototype.encryptedPayload = null;
        EncryptedPairingRequest.prototype.IV = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncryptedPairingRequest.prototype, "_encryptedPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(EncryptedPairingRequest.prototype, "_IV", {
            get: $util.oneOfGetter($oneOfFields = ["IV"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncryptedPairingRequest.create = function create(properties) {
            return new EncryptedPairingRequest(properties);
        };

        EncryptedPairingRequest.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.encryptedPayload != null && Object.hasOwnProperty.call(m, "encryptedPayload"))
                w.uint32(10).bytes(m.encryptedPayload);
            if (m.IV != null && Object.hasOwnProperty.call(m, "IV"))
                w.uint32(18).bytes(m.IV);
            return w;
        };

        EncryptedPairingRequest.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.EncryptedPairingRequest();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.encryptedPayload = r.bytes();
                        break;
                    }
                case 2: {
                        m.IV = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        EncryptedPairingRequest.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.EncryptedPairingRequest)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.EncryptedPairingRequest();
            if (d.encryptedPayload != null) {
                if (typeof d.encryptedPayload === "string")
                    $util.base64.decode(d.encryptedPayload, m.encryptedPayload = $util.newBuffer($util.base64.length(d.encryptedPayload)), 0);
                else if (d.encryptedPayload.length >= 0)
                    m.encryptedPayload = d.encryptedPayload;
            }
            if (d.IV != null) {
                if (typeof d.IV === "string")
                    $util.base64.decode(d.IV, m.IV = $util.newBuffer($util.base64.length(d.IV)), 0);
                else if (d.IV.length >= 0)
                    m.IV = d.IV;
            }
            return m;
        };

        EncryptedPairingRequest.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.encryptedPayload != null && m.hasOwnProperty("encryptedPayload")) {
                d.encryptedPayload = o.bytes === String ? $util.base64.encode(m.encryptedPayload, 0, m.encryptedPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.encryptedPayload) : m.encryptedPayload;
                if (o.oneofs)
                    d._encryptedPayload = "encryptedPayload";
            }
            if (m.IV != null && m.hasOwnProperty("IV")) {
                d.IV = o.bytes === String ? $util.base64.encode(m.IV, 0, m.IV.length) : o.bytes === Array ? Array.prototype.slice.call(m.IV) : m.IV;
                if (o.oneofs)
                    d._IV = "IV";
            }
            return d;
        };

        EncryptedPairingRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncryptedPairingRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.EncryptedPairingRequest";
        };

        return EncryptedPairingRequest;
    })();

    WACompanionReg.ClientPairingProps = (function() {

        function ClientPairingProps(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ClientPairingProps.prototype.isChatDbLidMigrated = null;
        ClientPairingProps.prototype.isSyncdPureLidSession = null;
        ClientPairingProps.prototype.isSyncdSnapshotRecoveryEnabled = null;
        ClientPairingProps.prototype.isHsThumbnailSyncEnabled = null;
        ClientPairingProps.prototype.subscriptionSyncPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isChatDbLidMigrated", {
            get: $util.oneOfGetter($oneOfFields = ["isChatDbLidMigrated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isSyncdPureLidSession", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdPureLidSession"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isSyncdSnapshotRecoveryEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdSnapshotRecoveryEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_isHsThumbnailSyncEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isHsThumbnailSyncEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ClientPairingProps.prototype, "_subscriptionSyncPayload", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionSyncPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ClientPairingProps.create = function create(properties) {
            return new ClientPairingProps(properties);
        };

        ClientPairingProps.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.isChatDbLidMigrated != null && Object.hasOwnProperty.call(m, "isChatDbLidMigrated"))
                w.uint32(8).bool(m.isChatDbLidMigrated);
            if (m.isSyncdPureLidSession != null && Object.hasOwnProperty.call(m, "isSyncdPureLidSession"))
                w.uint32(16).bool(m.isSyncdPureLidSession);
            if (m.isSyncdSnapshotRecoveryEnabled != null && Object.hasOwnProperty.call(m, "isSyncdSnapshotRecoveryEnabled"))
                w.uint32(24).bool(m.isSyncdSnapshotRecoveryEnabled);
            if (m.isHsThumbnailSyncEnabled != null && Object.hasOwnProperty.call(m, "isHsThumbnailSyncEnabled"))
                w.uint32(32).bool(m.isHsThumbnailSyncEnabled);
            if (m.subscriptionSyncPayload != null && Object.hasOwnProperty.call(m, "subscriptionSyncPayload"))
                w.uint32(42).bytes(m.subscriptionSyncPayload);
            return w;
        };

        ClientPairingProps.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WACompanionReg.ClientPairingProps();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.isChatDbLidMigrated = r.bool();
                        break;
                    }
                case 2: {
                        m.isSyncdPureLidSession = r.bool();
                        break;
                    }
                case 3: {
                        m.isSyncdSnapshotRecoveryEnabled = r.bool();
                        break;
                    }
                case 4: {
                        m.isHsThumbnailSyncEnabled = r.bool();
                        break;
                    }
                case 5: {
                        m.subscriptionSyncPayload = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ClientPairingProps.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WACompanionReg.ClientPairingProps)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WACompanionReg.ClientPairingProps();
            if (d.isChatDbLidMigrated != null) {
                m.isChatDbLidMigrated = Boolean(d.isChatDbLidMigrated);
            }
            if (d.isSyncdPureLidSession != null) {
                m.isSyncdPureLidSession = Boolean(d.isSyncdPureLidSession);
            }
            if (d.isSyncdSnapshotRecoveryEnabled != null) {
                m.isSyncdSnapshotRecoveryEnabled = Boolean(d.isSyncdSnapshotRecoveryEnabled);
            }
            if (d.isHsThumbnailSyncEnabled != null) {
                m.isHsThumbnailSyncEnabled = Boolean(d.isHsThumbnailSyncEnabled);
            }
            if (d.subscriptionSyncPayload != null) {
                if (typeof d.subscriptionSyncPayload === "string")
                    $util.base64.decode(d.subscriptionSyncPayload, m.subscriptionSyncPayload = $util.newBuffer($util.base64.length(d.subscriptionSyncPayload)), 0);
                else if (d.subscriptionSyncPayload.length >= 0)
                    m.subscriptionSyncPayload = d.subscriptionSyncPayload;
            }
            return m;
        };

        ClientPairingProps.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.isChatDbLidMigrated != null && m.hasOwnProperty("isChatDbLidMigrated")) {
                d.isChatDbLidMigrated = m.isChatDbLidMigrated;
                if (o.oneofs)
                    d._isChatDbLidMigrated = "isChatDbLidMigrated";
            }
            if (m.isSyncdPureLidSession != null && m.hasOwnProperty("isSyncdPureLidSession")) {
                d.isSyncdPureLidSession = m.isSyncdPureLidSession;
                if (o.oneofs)
                    d._isSyncdPureLidSession = "isSyncdPureLidSession";
            }
            if (m.isSyncdSnapshotRecoveryEnabled != null && m.hasOwnProperty("isSyncdSnapshotRecoveryEnabled")) {
                d.isSyncdSnapshotRecoveryEnabled = m.isSyncdSnapshotRecoveryEnabled;
                if (o.oneofs)
                    d._isSyncdSnapshotRecoveryEnabled = "isSyncdSnapshotRecoveryEnabled";
            }
            if (m.isHsThumbnailSyncEnabled != null && m.hasOwnProperty("isHsThumbnailSyncEnabled")) {
                d.isHsThumbnailSyncEnabled = m.isHsThumbnailSyncEnabled;
                if (o.oneofs)
                    d._isHsThumbnailSyncEnabled = "isHsThumbnailSyncEnabled";
            }
            if (m.subscriptionSyncPayload != null && m.hasOwnProperty("subscriptionSyncPayload")) {
                d.subscriptionSyncPayload = o.bytes === String ? $util.base64.encode(m.subscriptionSyncPayload, 0, m.subscriptionSyncPayload.length) : o.bytes === Array ? Array.prototype.slice.call(m.subscriptionSyncPayload) : m.subscriptionSyncPayload;
                if (o.oneofs)
                    d._subscriptionSyncPayload = "subscriptionSyncPayload";
            }
            return d;
        };

        ClientPairingProps.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientPairingProps.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WACompanionReg.ClientPairingProps";
        };

        return ClientPairingProps;
    })();

    return WACompanionReg;
})();

export const WAMmsRetry = $root.WAMmsRetry = (() => {

    const WAMmsRetry = {};

    WAMmsRetry.MediaRetryNotification = (function() {

        function MediaRetryNotification(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        MediaRetryNotification.prototype.stanzaID = null;
        MediaRetryNotification.prototype.directPath = null;
        MediaRetryNotification.prototype.result = null;
        MediaRetryNotification.prototype.messageSecret = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_stanzaID", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_result", {
            get: $util.oneOfGetter($oneOfFields = ["result"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(MediaRetryNotification.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaRetryNotification.create = function create(properties) {
            return new MediaRetryNotification(properties);
        };

        MediaRetryNotification.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                w.uint32(10).string(m.stanzaID);
            if (m.directPath != null && Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(18).string(m.directPath);
            if (m.result != null && Object.hasOwnProperty.call(m, "result"))
                w.uint32(24).int32(m.result);
            if (m.messageSecret != null && Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(34).bytes(m.messageSecret);
            return w;
        };

        MediaRetryNotification.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAMmsRetry.MediaRetryNotification();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stanzaID = r.string();
                        break;
                    }
                case 2: {
                        m.directPath = r.string();
                        break;
                    }
                case 3: {
                        m.result = r.int32();
                        break;
                    }
                case 4: {
                        m.messageSecret = r.bytes();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        MediaRetryNotification.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAMmsRetry.MediaRetryNotification)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAMmsRetry.MediaRetryNotification();
            if (d.stanzaID != null) {
                m.stanzaID = String(d.stanzaID);
            }
            if (d.directPath != null) {
                m.directPath = String(d.directPath);
            }
            switch (d.result) {
            default:
                if (typeof d.result === "number") {
                    m.result = d.result;
                    break;
                }
                break;
            case "GENERAL_ERROR":
            case 0:
                m.result = 0;
                break;
            case "SUCCESS":
            case 1:
                m.result = 1;
                break;
            case "NOT_FOUND":
            case 2:
                m.result = 2;
                break;
            case "DECRYPTION_ERROR":
            case 3:
                m.result = 3;
                break;
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            return m;
        };

        MediaRetryNotification.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                d.stanzaID = m.stanzaID;
                if (o.oneofs)
                    d._stanzaID = "stanzaID";
            }
            if (m.directPath != null && m.hasOwnProperty("directPath")) {
                d.directPath = m.directPath;
                if (o.oneofs)
                    d._directPath = "directPath";
            }
            if (m.result != null && m.hasOwnProperty("result")) {
                d.result = o.enums === String ? $root.WAMmsRetry.MediaRetryNotification.ResultType[m.result] === undefined ? m.result : $root.WAMmsRetry.MediaRetryNotification.ResultType[m.result] : m.result;
                if (o.oneofs)
                    d._result = "result";
            }
            if (m.messageSecret != null && m.hasOwnProperty("messageSecret")) {
                d.messageSecret = o.bytes === String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === Array ? Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
                if (o.oneofs)
                    d._messageSecret = "messageSecret";
            }
            return d;
        };

        MediaRetryNotification.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaRetryNotification.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAMmsRetry.MediaRetryNotification";
        };

        MediaRetryNotification.ResultType = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "GENERAL_ERROR"] = 0;
            values[valuesById[1] = "SUCCESS"] = 1;
            values[valuesById[2] = "NOT_FOUND"] = 2;
            values[valuesById[3] = "DECRYPTION_ERROR"] = 3;
            return values;
        })();

        return MediaRetryNotification;
    })();

    WAMmsRetry.ServerErrorReceipt = (function() {

        function ServerErrorReceipt(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        ServerErrorReceipt.prototype.stanzaID = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(ServerErrorReceipt.prototype, "_stanzaID", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaID"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ServerErrorReceipt.create = function create(properties) {
            return new ServerErrorReceipt(properties);
        };

        ServerErrorReceipt.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.stanzaID != null && Object.hasOwnProperty.call(m, "stanzaID"))
                w.uint32(10).string(m.stanzaID);
            return w;
        };

        ServerErrorReceipt.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAMmsRetry.ServerErrorReceipt();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 1: {
                        m.stanzaID = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        ServerErrorReceipt.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAMmsRetry.ServerErrorReceipt)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAMmsRetry.ServerErrorReceipt();
            if (d.stanzaID != null) {
                m.stanzaID = String(d.stanzaID);
            }
            return m;
        };

        ServerErrorReceipt.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.stanzaID != null && m.hasOwnProperty("stanzaID")) {
                d.stanzaID = m.stanzaID;
                if (o.oneofs)
                    d._stanzaID = "stanzaID";
            }
            return d;
        };

        ServerErrorReceipt.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        ServerErrorReceipt.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAMmsRetry.ServerErrorReceipt";
        };

        return ServerErrorReceipt;
    })();

    return WAMmsRetry;
})();

export const WAStatusAttributions = $root.WAStatusAttributions = (() => {

    const WAStatusAttributions = {};

    WAStatusAttributions.StatusAttribution = (function() {

        function StatusAttribution(p) {
            if (p)
                for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        }

        StatusAttribution.prototype.statusReshare = null;
        StatusAttribution.prototype.externalShare = null;
        StatusAttribution.prototype.music = null;
        StatusAttribution.prototype.groupStatus = null;
        StatusAttribution.prototype.rlAttribution = null;
        StatusAttribution.prototype.aiCreatedAttribution = null;
        StatusAttribution.prototype.type = null;
        StatusAttribution.prototype.actionURL = null;

        let $oneOfFields;

        Object.defineProperty(StatusAttribution.prototype, "attributionData", {
            get: $util.oneOfGetter($oneOfFields = ["statusReshare", "externalShare", "music", "groupStatus", "rlAttribution", "aiCreatedAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusAttribution.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        Object.defineProperty(StatusAttribution.prototype, "_actionURL", {
            get: $util.oneOfGetter($oneOfFields = ["actionURL"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusAttribution.create = function create(properties) {
            return new StatusAttribution(properties);
        };

        StatusAttribution.encode = function encode(m, w) {
            if (!w)
                w = $Writer.create();
            if (m.type != null && Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.actionURL != null && Object.hasOwnProperty.call(m, "actionURL"))
                w.uint32(18).string(m.actionURL);
            if (m.statusReshare != null && Object.hasOwnProperty.call(m, "statusReshare"))
                $root.WAStatusAttributions.StatusAttribution.StatusReshare.encode(m.statusReshare, w.uint32(26).fork()).ldelim();
            if (m.externalShare != null && Object.hasOwnProperty.call(m, "externalShare"))
                $root.WAStatusAttributions.StatusAttribution.ExternalShare.encode(m.externalShare, w.uint32(34).fork()).ldelim();
            if (m.music != null && Object.hasOwnProperty.call(m, "music"))
                $root.WAStatusAttributions.StatusAttribution.Music.encode(m.music, w.uint32(42).fork()).ldelim();
            if (m.groupStatus != null && Object.hasOwnProperty.call(m, "groupStatus"))
                $root.WAStatusAttributions.StatusAttribution.GroupStatus.encode(m.groupStatus, w.uint32(50).fork()).ldelim();
            if (m.rlAttribution != null && Object.hasOwnProperty.call(m, "rlAttribution"))
                $root.WAStatusAttributions.StatusAttribution.RLAttribution.encode(m.rlAttribution, w.uint32(58).fork()).ldelim();
            if (m.aiCreatedAttribution != null && Object.hasOwnProperty.call(m, "aiCreatedAttribution"))
                $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.encode(m.aiCreatedAttribution, w.uint32(66).fork()).ldelim();
            return w;
        };

        StatusAttribution.decode = function decode(r, l, e, n) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (n === undefined)
                n = 0;
            if (n > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution();
            while (r.pos < c) {
                var t = r.uint32();
                if (t === e)
                    break;
                switch (t >>> 3) {
                case 3: {
                        m.statusReshare = $root.WAStatusAttributions.StatusAttribution.StatusReshare.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 4: {
                        m.externalShare = $root.WAStatusAttributions.StatusAttribution.ExternalShare.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 5: {
                        m.music = $root.WAStatusAttributions.StatusAttribution.Music.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 6: {
                        m.groupStatus = $root.WAStatusAttributions.StatusAttribution.GroupStatus.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 7: {
                        m.rlAttribution = $root.WAStatusAttributions.StatusAttribution.RLAttribution.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 8: {
                        m.aiCreatedAttribution = $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.decode(r, r.uint32(), undefined, n + 1);
                        break;
                    }
                case 1: {
                        m.type = r.int32();
                        break;
                    }
                case 2: {
                        m.actionURL = r.string();
                        break;
                    }
                default:
                    r.skipType(t & 7, n);
                    break;
                }
            }
            return m;
        };

        StatusAttribution.fromObject = function fromObject(d, n) {
            if (d instanceof $root.WAStatusAttributions.StatusAttribution)
                return d;
            if (n === undefined)
                n = 0;
            if (n > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            var m = new $root.WAStatusAttributions.StatusAttribution();
            if (d.statusReshare != null) {
                if (typeof d.statusReshare !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.statusReshare: object expected");
                m.statusReshare = $root.WAStatusAttributions.StatusAttribution.StatusReshare.fromObject(d.statusReshare, n + 1);
            }
            if (d.externalShare != null) {
                if (typeof d.externalShare !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.externalShare: object expected");
                m.externalShare = $root.WAStatusAttributions.StatusAttribution.ExternalShare.fromObject(d.externalShare, n + 1);
            }
            if (d.music != null) {
                if (typeof d.music !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.music: object expected");
                m.music = $root.WAStatusAttributions.StatusAttribution.Music.fromObject(d.music, n + 1);
            }
            if (d.groupStatus != null) {
                if (typeof d.groupStatus !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.groupStatus: object expected");
                m.groupStatus = $root.WAStatusAttributions.StatusAttribution.GroupStatus.fromObject(d.groupStatus, n + 1);
            }
            if (d.rlAttribution != null) {
                if (typeof d.rlAttribution !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.rlAttribution: object expected");
                m.rlAttribution = $root.WAStatusAttributions.StatusAttribution.RLAttribution.fromObject(d.rlAttribution, n + 1);
            }
            if (d.aiCreatedAttribution != null) {
                if (typeof d.aiCreatedAttribution !== "object")
                    throw TypeError(".WAStatusAttributions.StatusAttribution.aiCreatedAttribution: object expected");
                m.aiCreatedAttribution = $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.fromObject(d.aiCreatedAttribution, n + 1);
            }
            switch (d.type) {
            default:
                if (typeof d.type === "number") {
                    m.type = d.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "RESHARE":
            case 1:
                m.type = 1;
                break;
            case "EXTERNAL_SHARE":
            case 2:
                m.type = 2;
                break;
            case "MUSIC":
            case 3:
                m.type = 3;
                break;
            case "STATUS_MENTION":
            case 4:
                m.type = 4;
                break;
            case "GROUP_STATUS":
            case 5:
                m.type = 5;
                break;
            case "RL_ATTRIBUTION":
            case 6:
                m.type = 6;
                break;
            case "AI_CREATED":
            case 7:
                m.type = 7;
                break;
            case "LAYOUTS":
            case 8:
                m.type = 8;
                break;
            case "NEWSLETTER_STATUS":
            case 9:
                m.type = 9;
                break;
            case "STATUS_CLOSE_SHARING":
            case 10:
                m.type = 10;
                break;
            case "PAID_PARTNERSHIP":
            case 11:
                m.type = 11;
                break;
            }
            if (d.actionURL != null) {
                m.actionURL = String(d.actionURL);
            }
            return m;
        };

        StatusAttribution.toObject = function toObject(m, o) {
            if (!o)
                o = {};
            var d = {};
            if (m.type != null && m.hasOwnProperty("type")) {
                d.type = o.enums === String ? $root.WAStatusAttributions.StatusAttribution.Type[m.type] === undefined ? m.type : $root.WAStatusAttributions.StatusAttribution.Type[m.type] : m.type;
                if (o.oneofs)
                    d._type = "type";
            }
            if (m.actionURL != null && m.hasOwnProperty("actionURL")) {
                d.actionURL = m.actionURL;
                if (o.oneofs)
                    d._actionURL = "actionURL";
            }
            if (m.statusReshare != null && m.hasOwnProperty("statusReshare")) {
                d.statusReshare = $root.WAStatusAttributions.StatusAttribution.StatusReshare.toObject(m.statusReshare, o);
                if (o.oneofs)
                    d.attributionData = "statusReshare";
            }
            if (m.externalShare != null && m.hasOwnProperty("externalShare")) {
                d.externalShare = $root.WAStatusAttributions.StatusAttribution.ExternalShare.toObject(m.externalShare, o);
                if (o.oneofs)
                    d.attributionData = "externalShare";
            }
            if (m.music != null && m.hasOwnProperty("music")) {
                d.music = $root.WAStatusAttributions.StatusAttribution.Music.toObject(m.music, o);
                if (o.oneofs)
                    d.attributionData = "music";
            }
            if (m.groupStatus != null && m.hasOwnProperty("groupStatus")) {
                d.groupStatus = $root.WAStatusAttributions.StatusAttribution.GroupStatus.toObject(m.groupStatus, o);
                if (o.oneofs)
                    d.attributionData = "groupStatus";
            }
            if (m.rlAttribution != null && m.hasOwnProperty("rlAttribution")) {
                d.rlAttribution = $root.WAStatusAttributions.StatusAttribution.RLAttribution.toObject(m.rlAttribution, o);
                if (o.oneofs)
                    d.attributionData = "rlAttribution";
            }
            if (m.aiCreatedAttribution != null && m.hasOwnProperty("aiCreatedAttribution")) {
                d.aiCreatedAttribution = $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.toObject(m.aiCreatedAttribution, o);
                if (o.oneofs)
                    d.attributionData = "aiCreatedAttribution";
            }
            return d;
        };

        StatusAttribution.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution";
        };

        StatusAttribution.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RESHARE"] = 1;
            values[valuesById[2] = "EXTERNAL_SHARE"] = 2;
            values[valuesById[3] = "MUSIC"] = 3;
            values[valuesById[4] = "STATUS_MENTION"] = 4;
            values[valuesById[5] = "GROUP_STATUS"] = 5;
            values[valuesById[6] = "RL_ATTRIBUTION"] = 6;
            values[valuesById[7] = "AI_CREATED"] = 7;
            values[valuesById[8] = "LAYOUTS"] = 8;
            values[valuesById[9] = "NEWSLETTER_STATUS"] = 9;
            values[valuesById[10] = "STATUS_CLOSE_SHARING"] = 10;
            values[valuesById[11] = "PAID_PARTNERSHIP"] = 11;
            return values;
        })();

        StatusAttribution.AiCreatedAttribution = (function() {

            function AiCreatedAttribution(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            AiCreatedAttribution.prototype.source = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(AiCreatedAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AiCreatedAttribution.create = function create(properties) {
                return new AiCreatedAttribution(properties);
            };

            AiCreatedAttribution.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.source != null && Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                return w;
            };

            AiCreatedAttribution.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.source = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            AiCreatedAttribution.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution();
                switch (d.source) {
                default:
                    if (typeof d.source === "number") {
                        m.source = d.source;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "STATUS_MIMICRY":
                case 1:
                    m.source = 1;
                    break;
                }
                return m;
            };

            AiCreatedAttribution.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.source != null && m.hasOwnProperty("source")) {
                    d.source = o.enums === String ? $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.Source[m.source] === undefined ? m.source : $root.WAStatusAttributions.StatusAttribution.AiCreatedAttribution.Source[m.source] : m.source;
                    if (o.oneofs)
                        d._source = "source";
                }
                return d;
            };

            AiCreatedAttribution.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            AiCreatedAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.AiCreatedAttribution";
            };

            AiCreatedAttribution.Source = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "STATUS_MIMICRY"] = 1;
                return values;
            })();

            return AiCreatedAttribution;
        })();

        StatusAttribution.RLAttribution = (function() {

            function RLAttribution(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            RLAttribution.prototype.source = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(RLAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RLAttribution.create = function create(properties) {
                return new RLAttribution(properties);
            };

            RLAttribution.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.source != null && Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                return w;
            };

            RLAttribution.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.RLAttribution();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.source = r.int32();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            RLAttribution.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.RLAttribution)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.RLAttribution();
                switch (d.source) {
                default:
                    if (typeof d.source === "number") {
                        m.source = d.source;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "RAY_BAN_META_GLASSES":
                case 1:
                    m.source = 1;
                    break;
                case "OAKLEY_META_GLASSES":
                case 2:
                    m.source = 2;
                    break;
                case "HYPERNOVA_GLASSES":
                case 3:
                    m.source = 3;
                    break;
                }
                return m;
            };

            RLAttribution.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.source != null && m.hasOwnProperty("source")) {
                    d.source = o.enums === String ? $root.WAStatusAttributions.StatusAttribution.RLAttribution.Source[m.source] === undefined ? m.source : $root.WAStatusAttributions.StatusAttribution.RLAttribution.Source[m.source] : m.source;
                    if (o.oneofs)
                        d._source = "source";
                }
                return d;
            };

            RLAttribution.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            RLAttribution.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.RLAttribution";
            };

            RLAttribution.Source = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "RAY_BAN_META_GLASSES"] = 1;
                values[valuesById[2] = "OAKLEY_META_GLASSES"] = 2;
                values[valuesById[3] = "HYPERNOVA_GLASSES"] = 3;
                return values;
            })();

            return RLAttribution;
        })();

        StatusAttribution.ExternalShare = (function() {

            function ExternalShare(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            ExternalShare.prototype.actionURL = null;
            ExternalShare.prototype.source = null;
            ExternalShare.prototype.duration = null;
            ExternalShare.prototype.actionFallbackURL = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_actionURL", {
                get: $util.oneOfGetter($oneOfFields = ["actionURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_duration", {
                get: $util.oneOfGetter($oneOfFields = ["duration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(ExternalShare.prototype, "_actionFallbackURL", {
                get: $util.oneOfGetter($oneOfFields = ["actionFallbackURL"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExternalShare.create = function create(properties) {
                return new ExternalShare(properties);
            };

            ExternalShare.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.actionURL != null && Object.hasOwnProperty.call(m, "actionURL"))
                    w.uint32(10).string(m.actionURL);
                if (m.source != null && Object.hasOwnProperty.call(m, "source"))
                    w.uint32(16).int32(m.source);
                if (m.duration != null && Object.hasOwnProperty.call(m, "duration"))
                    w.uint32(24).int32(m.duration);
                if (m.actionFallbackURL != null && Object.hasOwnProperty.call(m, "actionFallbackURL"))
                    w.uint32(34).string(m.actionFallbackURL);
                return w;
            };

            ExternalShare.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.ExternalShare();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.actionURL = r.string();
                            break;
                        }
                    case 2: {
                            m.source = r.int32();
                            break;
                        }
                    case 3: {
                            m.duration = r.int32();
                            break;
                        }
                    case 4: {
                            m.actionFallbackURL = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            ExternalShare.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.ExternalShare)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.ExternalShare();
                if (d.actionURL != null) {
                    m.actionURL = String(d.actionURL);
                }
                switch (d.source) {
                default:
                    if (typeof d.source === "number") {
                        m.source = d.source;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "INSTAGRAM":
                case 1:
                    m.source = 1;
                    break;
                case "FACEBOOK":
                case 2:
                    m.source = 2;
                    break;
                case "MESSENGER":
                case 3:
                    m.source = 3;
                    break;
                case "SPOTIFY":
                case 4:
                    m.source = 4;
                    break;
                case "YOUTUBE":
                case 5:
                    m.source = 5;
                    break;
                case "PINTEREST":
                case 6:
                    m.source = 6;
                    break;
                case "THREADS":
                case 7:
                    m.source = 7;
                    break;
                case "APPLE_MUSIC":
                case 8:
                    m.source = 8;
                    break;
                case "SHARECHAT":
                case 9:
                    m.source = 9;
                    break;
                case "GOOGLE_PHOTOS":
                case 10:
                    m.source = 10;
                    break;
                case "SOUNDCLOUD":
                case 11:
                    m.source = 11;
                    break;
                case "SHAZAM":
                case 12:
                    m.source = 12;
                    break;
                }
                if (d.duration != null) {
                    m.duration = d.duration | 0;
                }
                if (d.actionFallbackURL != null) {
                    m.actionFallbackURL = String(d.actionFallbackURL);
                }
                return m;
            };

            ExternalShare.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.actionURL != null && m.hasOwnProperty("actionURL")) {
                    d.actionURL = m.actionURL;
                    if (o.oneofs)
                        d._actionURL = "actionURL";
                }
                if (m.source != null && m.hasOwnProperty("source")) {
                    d.source = o.enums === String ? $root.WAStatusAttributions.StatusAttribution.ExternalShare.Source[m.source] === undefined ? m.source : $root.WAStatusAttributions.StatusAttribution.ExternalShare.Source[m.source] : m.source;
                    if (o.oneofs)
                        d._source = "source";
                }
                if (m.duration != null && m.hasOwnProperty("duration")) {
                    d.duration = m.duration;
                    if (o.oneofs)
                        d._duration = "duration";
                }
                if (m.actionFallbackURL != null && m.hasOwnProperty("actionFallbackURL")) {
                    d.actionFallbackURL = m.actionFallbackURL;
                    if (o.oneofs)
                        d._actionFallbackURL = "actionFallbackURL";
                }
                return d;
            };

            ExternalShare.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExternalShare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.ExternalShare";
            };

            ExternalShare.Source = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INSTAGRAM"] = 1;
                values[valuesById[2] = "FACEBOOK"] = 2;
                values[valuesById[3] = "MESSENGER"] = 3;
                values[valuesById[4] = "SPOTIFY"] = 4;
                values[valuesById[5] = "YOUTUBE"] = 5;
                values[valuesById[6] = "PINTEREST"] = 6;
                values[valuesById[7] = "THREADS"] = 7;
                values[valuesById[8] = "APPLE_MUSIC"] = 8;
                values[valuesById[9] = "SHARECHAT"] = 9;
                values[valuesById[10] = "GOOGLE_PHOTOS"] = 10;
                values[valuesById[11] = "SOUNDCLOUD"] = 11;
                values[valuesById[12] = "SHAZAM"] = 12;
                return values;
            })();

            return ExternalShare;
        })();

        StatusAttribution.StatusReshare = (function() {

            function StatusReshare(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            StatusReshare.prototype.source = null;
            StatusReshare.prototype.metadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusReshare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(StatusReshare.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusReshare.create = function create(properties) {
                return new StatusReshare(properties);
            };

            StatusReshare.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.source != null && Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                if (m.metadata != null && Object.hasOwnProperty.call(m, "metadata"))
                    $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata.encode(m.metadata, w.uint32(18).fork()).ldelim();
                return w;
            };

            StatusReshare.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.StatusReshare();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.source = r.int32();
                            break;
                        }
                    case 2: {
                            m.metadata = $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata.decode(r, r.uint32(), undefined, n + 1);
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            StatusReshare.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.StatusReshare)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.StatusReshare();
                switch (d.source) {
                default:
                    if (typeof d.source === "number") {
                        m.source = d.source;
                        break;
                    }
                    break;
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "INTERNAL_RESHARE":
                case 1:
                    m.source = 1;
                    break;
                case "MENTION_RESHARE":
                case 2:
                    m.source = 2;
                    break;
                case "CHANNEL_RESHARE":
                case 3:
                    m.source = 3;
                    break;
                case "FORWARD":
                case 4:
                    m.source = 4;
                    break;
                }
                if (d.metadata != null) {
                    if (typeof d.metadata !== "object")
                        throw TypeError(".WAStatusAttributions.StatusAttribution.StatusReshare.metadata: object expected");
                    m.metadata = $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata.fromObject(d.metadata, n + 1);
                }
                return m;
            };

            StatusReshare.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.source != null && m.hasOwnProperty("source")) {
                    d.source = o.enums === String ? $root.WAStatusAttributions.StatusAttribution.StatusReshare.Source[m.source] === undefined ? m.source : $root.WAStatusAttributions.StatusAttribution.StatusReshare.Source[m.source] : m.source;
                    if (o.oneofs)
                        d._source = "source";
                }
                if (m.metadata != null && m.hasOwnProperty("metadata")) {
                    d.metadata = $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata.toObject(m.metadata, o);
                    if (o.oneofs)
                        d._metadata = "metadata";
                }
                return d;
            };

            StatusReshare.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusReshare.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.StatusReshare";
            };

            StatusReshare.Source = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INTERNAL_RESHARE"] = 1;
                values[valuesById[2] = "MENTION_RESHARE"] = 2;
                values[valuesById[3] = "CHANNEL_RESHARE"] = 3;
                values[valuesById[4] = "FORWARD"] = 4;
                return values;
            })();

            StatusReshare.Metadata = (function() {

                function Metadata(p) {
                    if (p)
                        for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                }

                Metadata.prototype.duration = null;
                Metadata.prototype.channelJID = null;
                Metadata.prototype.channelMessageID = null;
                Metadata.prototype.hasMultipleReshares = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_duration", {
                    get: $util.oneOfGetter($oneOfFields = ["duration"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_channelJID", {
                    get: $util.oneOfGetter($oneOfFields = ["channelJID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_channelMessageID", {
                    get: $util.oneOfGetter($oneOfFields = ["channelMessageID"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                Object.defineProperty(Metadata.prototype, "_hasMultipleReshares", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMultipleReshares"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Metadata.create = function create(properties) {
                    return new Metadata(properties);
                };

                Metadata.encode = function encode(m, w) {
                    if (!w)
                        w = $Writer.create();
                    if (m.duration != null && Object.hasOwnProperty.call(m, "duration"))
                        w.uint32(8).int32(m.duration);
                    if (m.channelJID != null && Object.hasOwnProperty.call(m, "channelJID"))
                        w.uint32(18).string(m.channelJID);
                    if (m.channelMessageID != null && Object.hasOwnProperty.call(m, "channelMessageID"))
                        w.uint32(24).int32(m.channelMessageID);
                    if (m.hasMultipleReshares != null && Object.hasOwnProperty.call(m, "hasMultipleReshares"))
                        w.uint32(32).bool(m.hasMultipleReshares);
                    return w;
                };

                Metadata.decode = function decode(r, l, e, n) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (n === undefined)
                        n = 0;
                    if (n > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata();
                    while (r.pos < c) {
                        var t = r.uint32();
                        if (t === e)
                            break;
                        switch (t >>> 3) {
                        case 1: {
                                m.duration = r.int32();
                                break;
                            }
                        case 2: {
                                m.channelJID = r.string();
                                break;
                            }
                        case 3: {
                                m.channelMessageID = r.int32();
                                break;
                            }
                        case 4: {
                                m.hasMultipleReshares = r.bool();
                                break;
                            }
                        default:
                            r.skipType(t & 7, n);
                            break;
                        }
                    }
                    return m;
                };

                Metadata.fromObject = function fromObject(d, n) {
                    if (d instanceof $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata)
                        return d;
                    if (n === undefined)
                        n = 0;
                    if (n > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    var m = new $root.WAStatusAttributions.StatusAttribution.StatusReshare.Metadata();
                    if (d.duration != null) {
                        m.duration = d.duration | 0;
                    }
                    if (d.channelJID != null) {
                        m.channelJID = String(d.channelJID);
                    }
                    if (d.channelMessageID != null) {
                        m.channelMessageID = d.channelMessageID | 0;
                    }
                    if (d.hasMultipleReshares != null) {
                        m.hasMultipleReshares = Boolean(d.hasMultipleReshares);
                    }
                    return m;
                };

                Metadata.toObject = function toObject(m, o) {
                    if (!o)
                        o = {};
                    var d = {};
                    if (m.duration != null && m.hasOwnProperty("duration")) {
                        d.duration = m.duration;
                        if (o.oneofs)
                            d._duration = "duration";
                    }
                    if (m.channelJID != null && m.hasOwnProperty("channelJID")) {
                        d.channelJID = m.channelJID;
                        if (o.oneofs)
                            d._channelJID = "channelJID";
                    }
                    if (m.channelMessageID != null && m.hasOwnProperty("channelMessageID")) {
                        d.channelMessageID = m.channelMessageID;
                        if (o.oneofs)
                            d._channelMessageID = "channelMessageID";
                    }
                    if (m.hasMultipleReshares != null && m.hasOwnProperty("hasMultipleReshares")) {
                        d.hasMultipleReshares = m.hasMultipleReshares;
                        if (o.oneofs)
                            d._hasMultipleReshares = "hasMultipleReshares";
                    }
                    return d;
                };

                Metadata.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
                };

                Metadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.StatusReshare.Metadata";
                };

                return Metadata;
            })();

            return StatusReshare;
        })();

        StatusAttribution.GroupStatus = (function() {

            function GroupStatus(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            GroupStatus.prototype.authorJID = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(GroupStatus.prototype, "_authorJID", {
                get: $util.oneOfGetter($oneOfFields = ["authorJID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            GroupStatus.create = function create(properties) {
                return new GroupStatus(properties);
            };

            GroupStatus.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.authorJID != null && Object.hasOwnProperty.call(m, "authorJID"))
                    w.uint32(10).string(m.authorJID);
                return w;
            };

            GroupStatus.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.GroupStatus();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.authorJID = r.string();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            GroupStatus.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.GroupStatus)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.GroupStatus();
                if (d.authorJID != null) {
                    m.authorJID = String(d.authorJID);
                }
                return m;
            };

            GroupStatus.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.authorJID != null && m.hasOwnProperty("authorJID")) {
                    d.authorJID = m.authorJID;
                    if (o.oneofs)
                        d._authorJID = "authorJID";
                }
                return d;
            };

            GroupStatus.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            GroupStatus.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.GroupStatus";
            };

            return GroupStatus;
        })();

        StatusAttribution.Music = (function() {

            function Music(p) {
                if (p)
                    for (var ks = Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            }

            Music.prototype.authorName = null;
            Music.prototype.songID = null;
            Music.prototype.title = null;
            Music.prototype.author = null;
            Music.prototype.artistAttribution = null;
            Music.prototype.isExplicit = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_authorName", {
                get: $util.oneOfGetter($oneOfFields = ["authorName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_songID", {
                get: $util.oneOfGetter($oneOfFields = ["songID"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_author", {
                get: $util.oneOfGetter($oneOfFields = ["author"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_artistAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(Music.prototype, "_isExplicit", {
                get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Music.create = function create(properties) {
                return new Music(properties);
            };

            Music.encode = function encode(m, w) {
                if (!w)
                    w = $Writer.create();
                if (m.authorName != null && Object.hasOwnProperty.call(m, "authorName"))
                    w.uint32(10).string(m.authorName);
                if (m.songID != null && Object.hasOwnProperty.call(m, "songID"))
                    w.uint32(18).string(m.songID);
                if (m.title != null && Object.hasOwnProperty.call(m, "title"))
                    w.uint32(26).string(m.title);
                if (m.author != null && Object.hasOwnProperty.call(m, "author"))
                    w.uint32(34).string(m.author);
                if (m.artistAttribution != null && Object.hasOwnProperty.call(m, "artistAttribution"))
                    w.uint32(42).string(m.artistAttribution);
                if (m.isExplicit != null && Object.hasOwnProperty.call(m, "isExplicit"))
                    w.uint32(48).bool(m.isExplicit);
                return w;
            };

            Music.decode = function decode(r, l, e, n) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (n === undefined)
                    n = 0;
                if (n > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var c = l === undefined ? r.len : r.pos + l, m = new $root.WAStatusAttributions.StatusAttribution.Music();
                while (r.pos < c) {
                    var t = r.uint32();
                    if (t === e)
                        break;
                    switch (t >>> 3) {
                    case 1: {
                            m.authorName = r.string();
                            break;
                        }
                    case 2: {
                            m.songID = r.string();
                            break;
                        }
                    case 3: {
                            m.title = r.string();
                            break;
                        }
                    case 4: {
                            m.author = r.string();
                            break;
                        }
                    case 5: {
                            m.artistAttribution = r.string();
                            break;
                        }
                    case 6: {
                            m.isExplicit = r.bool();
                            break;
                        }
                    default:
                        r.skipType(t & 7, n);
                        break;
                    }
                }
                return m;
            };

            Music.fromObject = function fromObject(d, n) {
                if (d instanceof $root.WAStatusAttributions.StatusAttribution.Music)
                    return d;
                if (n === undefined)
                    n = 0;
                if (n > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                var m = new $root.WAStatusAttributions.StatusAttribution.Music();
                if (d.authorName != null) {
                    m.authorName = String(d.authorName);
                }
                if (d.songID != null) {
                    m.songID = String(d.songID);
                }
                if (d.title != null) {
                    m.title = String(d.title);
                }
                if (d.author != null) {
                    m.author = String(d.author);
                }
                if (d.artistAttribution != null) {
                    m.artistAttribution = String(d.artistAttribution);
                }
                if (d.isExplicit != null) {
                    m.isExplicit = Boolean(d.isExplicit);
                }
                return m;
            };

            Music.toObject = function toObject(m, o) {
                if (!o)
                    o = {};
                var d = {};
                if (m.authorName != null && m.hasOwnProperty("authorName")) {
                    d.authorName = m.authorName;
                    if (o.oneofs)
                        d._authorName = "authorName";
                }
                if (m.songID != null && m.hasOwnProperty("songID")) {
                    d.songID = m.songID;
                    if (o.oneofs)
                        d._songID = "songID";
                }
                if (m.title != null && m.hasOwnProperty("title")) {
                    d.title = m.title;
                    if (o.oneofs)
                        d._title = "title";
                }
                if (m.author != null && m.hasOwnProperty("author")) {
                    d.author = m.author;
                    if (o.oneofs)
                        d._author = "author";
                }
                if (m.artistAttribution != null && m.hasOwnProperty("artistAttribution")) {
                    d.artistAttribution = m.artistAttribution;
                    if (o.oneofs)
                        d._artistAttribution = "artistAttribution";
                }
                if (m.isExplicit != null && m.hasOwnProperty("isExplicit")) {
                    d.isExplicit = m.isExplicit;
                    if (o.oneofs)
                        d._isExplicit = "isExplicit";
                }
                return d;
            };

            Music.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf.util.toJSONOptions);
            };

            Music.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/WAStatusAttributions.StatusAttribution.Music";
            };

            return Music;
        })();

        return StatusAttribution;
    })();

    return WAStatusAttributions;
})();

export { $root as default };

